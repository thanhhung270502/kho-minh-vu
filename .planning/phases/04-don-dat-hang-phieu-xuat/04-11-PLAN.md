---
phase: 04-don-dat-hang-phieu-xuat
plan: 11
type: execute
wave: 9
depends_on: ["04-10"]
files_modified:
  - src/app/(app)/xuat-kho/[id]/page.tsx
  - src/features/stock-out/components/issue-detail.tsx
  - src/features/stock-out/components/issue-header.tsx
  - src/features/stock-out/components/issue-line-table.tsx
autonomous: false
requirements: [XUAT-01, XUAT-02, XUAT-04, XUAT-07]

must_haves:
  truths:
    - "Mở phiếu xuất sinh từ đơn thấy mọi dòng đã điền sẵn số lượng bằng số đặt"
    - "Dòng nào số xuất vượt tồn thì đổi màu ngay khi gõ, không phải đợi bấm ghi sổ"
    - "Kho của từng dòng sửa được, mặc định là kho mặc định của mã hàng"
    - "Mã hàng chưa gán kho mặc định thì không thêm được dòng, kèm câu chỉ đúng chỗ sửa"
    - "Thêm dòng thủ công hoàn toàn bằng bàn phím"
  artifacts:
    - path: "src/app/(app)/xuat-kho/[id]/page.tsx"
      provides: "Route chi tiết phiếu xuất, kiểm uuid, chặn quyền"
      contains: "requirePermission"
    - path: "src/features/stock-out/components/issue-detail.tsx"
      provides: "Khung trang: đầu phiếu, bảng dòng, khu vực nút hành động"
    - path: "src/features/stock-out/components/issue-line-table.tsx"
      provides: "Bảng dòng gõ bàn phím, kho từng dòng, tô màu dòng vượt tồn"
      min_lines: 140
  key_links:
    - from: "src/features/stock-out/components/issue-line-table.tsx"
      to: "IssueLine.currentStock"
      via: "so sánh số xuất với tồn của kho dòng để tô màu (D-12 lớp 1)"
      pattern: "currentStock"
    - from: "src/features/stock-out/components/issue-line-table.tsx"
      to: "ProductSearchInput.defaultWarehouseId"
      via: "điền sẵn kho dòng từ san_pham.kho_mac_dinh_id, chặn khi thiếu"
      pattern: "defaultWarehouseId"
---

<objective>
Trang chi tiết phiếu xuất: đầu phiếu và bảng dòng.

**D-10 — đường đạt mốc dưới 20 giây (XUAT-01).** Phiếu sinh từ đơn đã có sẵn mọi dòng với
`so_luong = so_luong_dat` (RPC `tao_phieu_xuat_tu_don` của plan 04-04 đã làm). Người nhập
chỉ **gõ lại dòng nào kho lấy thiếu** rồi ghi sổ. Bảng phải làm cho việc sửa một ô số
lượng nhanh nhất có thể: con trỏ vào được bằng phím, sửa xong Enter là lưu.

**D-12 lớp 1 — dòng đổi màu NGAY khi số xuất vượt tồn.** `dong_chung_tu` đã trả
`ton_hien_tai` theo kho của dòng (plan 04-01) và mapper trả `currentStock` (plan 04-05).
Lớp 2 — hộp tóm tắt trước khi ghi sổ — nằm ở plan 04-13.

**D-13 — kho sửa được từng dòng**, mặc định từ `san_pham.kho_mac_dinh_id`. Cơ chế cột
nullable + `coalesce(dòng, header)` đã dựng ở migration 0041 và plan 04-01 đã vá cả ba hàm
ghi sổ chiều xuất cho khớp.

Purpose: đây là màn nghiệp vụ được dùng nhiều nhất — 92 phiếu/ngày.
Output: `/xuat-kho/{id}` sửa được bằng bàn phím, cảnh báo tồn âm ngay tại dòng.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-CONTEXT.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-07-SUMMARY.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-09-SUMMARY.md

**Ba bẫy bàn phím — chép đúng cách đã chữa, giống plan 04-09:**
- **Bẫy 14a:** rc-select tự focus lại ô tìm của nó sau khi Enter chọn option → bắt Enter ở
  `onKeyDownCapture` của div bọc ngoài (pha capture chạy trước rc-select), rồi
  `preventDefault()` + `stopPropagation()`.
- **Bẫy 14b:** `focus()` gọi ngay sau khi mutation resolve bị vòng render kế tiếp dọn đi →
  `setTimeout(..., 0)`.
- **Bẫy 15:** `ProductSearchInput` (đã ở `src/shared/components/` từ plan 04-05) ưu tiên mã
  khớp tuyệt đối — dùng lại, không viết lại.

**4 mã thiếu `kho_mac_dinh_id`** (3.266/3.270 đã có, đo 20/09). Claude's Discretion trong
04-CONTEXT.md đã chốt: **chặn khi thêm dòng**, thông báo chỉ đúng chỗ sửa
(Danh mục → mã hàng → kho mặc định). **Không đoán kho**, không rơi về kho đầu phiếu.

**Bẫy 11 — antd v6:** `Descriptions items` bỏ `span` cố định trong lưới responsive;
`Alert` dùng `title`; `Drawer` dùng `size`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route chi tiết phiếu xuất và khung trang</name>
  <files>src/app/(app)/xuat-kho/[id]/page.tsx, src/features/stock-out/components/issue-detail.tsx</files>
  <read_first>
    - src/app/(app)/nhap-kho/[id]/page.tsx (khuôn route `[id]`)
    - src/features/stock-in/components/receipt-detail.tsx (toàn bộ — khung trang + `Space wrap` nút hành động)
    - src/features/sales-order/components/order-detail.tsx (plan 04-09 — giữ hai màn nhất quán)
    - src/features/stock-out/hooks/useIssues.ts và types.ts (plan 04-07)
    - src/shared/components/query-state.tsx
  </read_first>
  <action>
`src/app/(app)/xuat-kho/[id]/page.tsx` — Server Component theo khuôn `nhap-kho/[id]/page.tsx`:
`const { id } = await params`, regex UUID → `notFound()`, `const user = await
requirePermission("view-catalog")`, render `<IssueDetailView id={id} permissions={{ canEdit,
canVoid }} />` với:
- `canEdit: hasPermission(user.role, "edit-catalog")`
- `canVoid: user.role === "quan_ly"` — hủy chứng từ đã ghi sổ chỉ quản lý; chặn thật ở
  `huy_chung_tu` (plan 04-01 đã siết cho cả `XUAT`).
Không import `antd` trong route.

`issue-detail.tsx` (`"use client"`):
- `useIssueDetail(id)` bọc `<QueryState>` đủ bốn trạng thái; phiếu không tồn tại → trạng
  thái rỗng có link về `/xuat-kho`.
- Tiêu đề: `{so_ct}` + `Tag` trạng thái + **link "Từ đơn {orderNo}"** sang
  `/dat-hang/{orderId}` khi phiếu gắn đơn.
- Khi phiếu là `TRA_KHACH`/`TRA_NCC` thì hiện link "Từ chứng từ {sourceDocNo}" — dùng chung
  component này cho màn phiếu trả ở plan 04-14 nếu thuận, hoặc ghi rõ trong SUMMARY là
  không dùng chung.
- Khu vực `<Space wrap>` cho nút hành động, **để trống có comment**: plan 04-13 cắm nút
  ghi sổ và khối cảnh báo xuất âm; plan 04-14 cắm nút in và nút "Khách trả hàng". Khai
  thành prop `actions?: ReactNode` hoặc một vùng rõ ràng, ghi tên plan sẽ điền.
- Dưới đó: `<IssueHeader>` rồi `<IssueLineTable>`.
- `editable = permissions.canEdit && issue.status === "NHAP_LIEU"`. Phiếu đã ghi sổ khóa
  sửa ở giao diện; policy `chi sua chung tu dang nhap lieu` (0016) là chốt chặn thật.
- Khi phiếu đã ghi sổ và có `negativeReason`, hiện `Alert` (prop `title`) nêu nhãn lý do
  xuất âm bằng `negativeReasonLabel(...)` của `features/stock-out/lib/negative-reasons.ts`
  kèm ghi chú — để người xem lại biết vì sao phiếu này được cho xuất âm.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0.
    - `grep -c "antd" "src/app/(app)/xuat-kho/[id]/page.tsx"` trả `0`; file có `await params`,
      `notFound`, `requirePermission`.
    - `issue-detail.tsx` mở bằng `"use client";`, chứa `QueryState`.
    - `grep -c "dat-hang/" src/features/stock-out/components/issue-detail.tsx` ≥ 1 — có link
      về đơn gốc.
    - `grep -c "negativeReasonLabel" src/features/stock-out/components/issue-detail.tsx` ≥ 1.
    - `grep -c "message=" src/features/stock-out/components/issue-detail.tsx` trả `0`.
    - Có chỗ dành sẵn cho nút hành động kèm comment nêu plan sẽ điền
      (`grep -c "04-13\|04-14" src/features/stock-out/components/issue-detail.tsx` ≥ 1).
  </acceptance_criteria>
  <done>`/xuat-kho/{id}` mở được, hiện đầu phiếu, link đơn gốc, chỗ trống cho nút hành động.</done>
</task>

<task type="auto">
  <name>Task 2: Đầu phiếu xuất sửa tại chỗ</name>
  <files>src/features/stock-out/components/issue-header.tsx</files>
  <read_first>
    - src/features/stock-in/components/receipt-header.tsx (toàn bộ — khuôn `Descriptions` sửa tại chỗ)
    - src/shared/components/partner-search-input.tsx (plan 04-08)
    - src/features/stock-out/hooks/useIssues.ts (`useUpdateIssueHeader`)
    - src/shared/lib/errors.ts
  </read_first>
  <action>
Tạo `src/features/stock-out/components/issue-header.tsx` (`"use client"`) theo khuôn
`receipt-header.tsx`: `Descriptions` sửa tại chỗ, lưu khi rời ô, hiện dấu "đã lưu" 2 giây.

Mục:
- **Số phiếu**, **Ngày phiếu**, **Trạng thái**, **Người tạo**, **Ngày ghi sổ** — chỉ đọc.
- **Người nhận** — `PartnerSearchInput` của `@/shared/components/partner-search-input`.
  **Không** chép `Select` NCC nạp toàn bộ của `receipt-header.tsx`.
- **Kho đầu phiếu** — `Select` danh sách kho, kèm dòng chú thích
  "Dòng nào chọn kho riêng thì theo kho đó." (D-13).
- **Ghi chú** — `Input.TextArea`.
- **Đơn gốc** — chỉ đọc, hiện `orderNo` kèm link, hoặc "Không gắn đơn".

**Không có mục Nguồn nhập** — chỉ chiều nhập mới có. **Không có mục lý do xuất âm** ở đây:
lý do thu thập ở khối cảnh báo ngay trước khi ghi sổ (plan 04-13), đặt ở đầu phiếu sẽ mời
người dùng chọn sẵn một lý do rồi quên.

Khi `editable === false` mọi mục về chỉ đọc.

Lưu bằng `useUpdateIssueHeader(id)`; lớp `api/` đã ném lỗi khi `count` rỗng — bắt và hiện
nguyên văn. Bắt `42501` riêng. Dùng `errorCode` / `isPostgrestError` / `explainError`.

**antd v6:** `Descriptions items` không đặt `span` cố định.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0; dòng đầu là `"use client";`.
    - `grep -c "PartnerSearchInput" …` ≥ 1 và `grep -c "usePartners(" …` trả `0`.
    - `grep -c "span:" src/features/stock-out/components/issue-header.tsx` trả `0`.
    - `grep -rc "nguon_nhap\|Nguồn nhập\|ly_do_xuat_am\|NEGATIVE_REASON" src/features/stock-out/components/issue-header.tsx`
      trả `0`.
    - File xử lý riêng `42501` và có nhánh `explainError`; dưới 200 dòng.
  </acceptance_criteria>
  <done>Đầu phiếu xuất sửa được khi phiếu còn nhập liệu, khóa khi đã ghi sổ.</done>
</task>

<task type="auto">
  <name>Task 3: Bảng dòng phiếu xuất — bàn phím, kho từng dòng, tô màu vượt tồn</name>
  <files>src/features/stock-out/components/issue-line-table.tsx</files>
  <read_first>
    - src/features/stock-in/components/receipt-line-table.tsx (TOÀN BỘ — đặc biệt khối `hasMultipleWarehouses` + cột `Select` kho ở khoảng dòng 126–150, và luồng bàn phím)
    - src/shared/components/product-search-input.tsx (plan 04-05 — trả `defaultWarehouseId`)
    - src/features/sales-order/components/order-line-table.tsx (plan 04-09 — bản nhân bản đầu tiên, giữ hai bảng nhất quán)
    - src/features/stock-out/types.ts (`IssueLine.currentStock`, `exceedsStock`)
    - src/features/stock-out/hooks/useIssues.ts (`useAddIssueLine`, `useUpdateIssueLine`, `useDeleteIssueLine`)
  </read_first>
  <action>
Tạo `src/features/stock-out/components/issue-line-table.tsx` (`"use client"`) từ khuôn
`receipt-line-table.tsx`. Bốn điểm khác:

**(a) Bỏ cột Đơn giá và Thành tiền.** Phiếu xuất không mang giá bán; giá vốn do trigger
tính lúc ghi sổ và không hiện ra ở đây (cột `gia_von` bị thu quyền đọc từ migration 0029).
Giữ hàng tổng số lượng.

**(b) Cột Kho của dòng (D-13).** Dùng lại nguyên khối kho theo dòng đã có trong
`receipt-line-table.tsx` (`hasMultipleWarehouses` + cột `Select` kho), **không viết mới**.
Khi thêm dòng mới, điền sẵn kho bằng `defaultWarehouseId` mà `ProductSearchInput` trả về.

**(c) Chặn mã chưa gán kho mặc định.** Nếu sản phẩm vừa chọn có `defaultWarehouseId === null`
thì **không thêm dòng**; hiện `message.error` (hoặc lỗi dưới ô mã) với câu:
"Mã {code} chưa gán kho mặc định. Sửa ở Danh mục → mở mã hàng → Kho mặc định, rồi quay lại."
Con trỏ ở lại ô mã để người dùng gõ mã khác. **Không đoán kho, không rơi về kho đầu phiếu.**
Đúng 4/3.270 mã dính chuyện này, nhưng đoán sai kho thì lệch tồn không ai thấy.

**(d) Tô màu dòng vượt tồn — D-12 lớp 1.** Với mỗi dòng, so `quantity` với `currentStock`.
Vượt thì `rowClassName` đổi nền (dùng token của `src/providers/antd-theme.ts` hoặc class
Tailwind — **không** dùng `!` trước class Tailwind; nếu thấy cần `!` thì thứ tự layer đang
sai, xem bẫy 2 của CLAUDE.md) và ô số lượng hiện `Tooltip` "Tồn kho {kho} còn {n}, xuất {m}".
Việc so sánh tính **khi render**, không lưu state. Đổi màu phải xảy ra **ngay khi gõ**,
trước khi bấm ghi sổ.
`currentStock` là ảnh chụp lúc tải dòng, không phải thời gian thực — ghi comment nói rõ để
người sau không tưởng đó là con số bảo đảm; chốt chặn thật vẫn là `ghi_so_chung_tu`.

**Giữ nguyên luồng bàn phím** (XUAT-07): ô mã → Enter → ô số lượng → Enter → lưu →
`setTimeout(..., 0)` đưa con trỏ về ô mã. Bắt Enter ở `onKeyDownCapture` của div bọc ngoài.

Khi `editable === false`: ẩn hàng nhập liệu và nút xóa.

Nếu file vượt ~200 dòng thì tách cấu hình cột ra file thuần riêng.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0; dòng đầu là `"use client";`.
    - `grep -c "onKeyDownCapture" src/features/stock-out/components/issue-line-table.tsx` ≥ 1.
    - `grep -c "setTimeout" …` ≥ 1.
    - `grep -c "currentStock" …` ≥ 2 (dùng cho tô màu và cho Tooltip).
    - `grep -c "rowClassName" …` ≥ 1.
    - `grep -c "defaultWarehouseId" …` ≥ 1 và file chứa chuỗi `Kho mặc định` (thông báo chặn).
    - `grep -rc "Đơn giá\|Thành tiền\|unitPrice\|gia_von" …` trả `0`.
    - `grep -c 'className="!' …` trả `0` — không dùng `!` trước class Tailwind trên component antd.
    - `grep -c "shared/components/product-search-input" …` ≥ 1.
    - File dưới 200 dòng (tách nếu vượt).
  </acceptance_criteria>
  <done>Bảng dòng phiếu xuất gõ được bằng bàn phím, kho sửa từng dòng, dòng vượt tồn đổi màu ngay.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Kiểm luồng phiếu xuất trên trình duyệt</name>
  <files>—</files>
  <read_first>
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-09-SUMMARY.md (kết quả checkpoint bàn phím của màn đơn)
    - .planning/phases/03-phieu-nhap/03-SUMMARY.md mục "Dữ liệu thử để lại"
  </read_first>
  <what-built>
`/xuat-kho/{id}` với đầu phiếu sửa tại chỗ, bảng dòng gõ bàn phím, kho theo dòng, và tô
màu dòng vượt tồn. `npm run check` xanh — nhưng antd v6 chỉ cảnh báo lúc chạy (bẫy 11) và
luồng focus bàn phím không kiểm được bằng typecheck.
  </what-built>
  <how-to-verify>
`npm run dev`, đăng nhập `vanphong@khominhvu.local`, **mở DevTools Console trước**.

1. `/xuat-kho` → "Tạo phiếu xuất" → chọn người nhận và kho → trang chi tiết mở ra.
2. Bảng dòng: gõ một phần mã → **Enter** → con trỏ sang ô số lượng → gõ `1` → **Enter** →
   dòng lưu, con trỏ **quay về ô mã**. Thêm hai dòng nữa, không chạm chuột.
3. Ở một dòng, gõ số lượng **lớn hơn tồn** (ví dụ 999999). Dòng **đổi màu ngay**, không
   phải đợi bấm gì. Rê chuột vào ô số lượng thấy Tooltip nêu tồn thật và số đang xuất.
4. Đổi kho của một dòng sang kho còn lại. Nếu tồn kho mới khác, màu cảnh báo của dòng đó
   phải cập nhật theo.
5. Tìm một mã chưa gán kho mặc định (4 mã trong 3.270 — nếu không tìm ra thì tạm bỏ trống
   `kho_mac_dinh_id` của một mã test rồi trả lại sau). Chọn nó → **không** thêm được dòng,
   thông báo nêu rõ "Danh mục → mã hàng → Kho mặc định", con trỏ còn ở ô mã.
6. Bảng **không** có cột Đơn giá, Thành tiền, hay bất kỳ cột giá nào.
7. Thu cửa sổ xuống dưới 992px: bảng cuộn ngang trong khung riêng, cả trang không tràn ngang.

Nếu Enter có vẻ không ăn, chạy trước trong Console:
`document.addEventListener("keydown", e => console.log("key=", JSON.stringify(e.key)), true)`
— thấy `key= ""` là lỗi công cụ gõ phím, không phải code (bẫy 13).

Báo lại bước lệch và **nguyên văn** mọi dòng console.
  </how-to-verify>
  <action>
Dừng thực thi tại đây. Đưa nguyên văn phần "how-to-verify" ở trên cho người dùng, chờ họ
tự chạy và trả lời. KHÔNG tự đánh giá thay, KHÔNG đi tiếp khi chưa có trả lời. Khi có phản
hồi: ghi nguyên văn vào SUMMARY (kể cả từng dòng console người dùng chép lại), và nếu có
bước lệch thì sửa rồi mời kiểm lại đúng bước đó trước khi khép task.
  </action>
  <verify>
    <human-check>Người dùng trả lời theo đúng resume-signal bên dưới</human-check>
  </verify>
  <acceptance_criteria>
    - Bảy bước chạy đúng mô tả.
    - Console không có `Warning:` của antd, không lỗi đỏ.
    - Bước 3 và 4: màu cảnh báo xuất hiện và cập nhật đúng theo kho của dòng.
    - Bước 5: dòng KHÔNG được thêm, và thông báo chỉ đúng chỗ sửa.
  </acceptance_criteria>
  <resume-signal>Gõ "đạt" nếu cả bảy bước đúng, hoặc mô tả bước lệch kèm nguyên văn console.</resume-signal>
  <done>Người dùng đã trả lời "đạt", hoặc mọi bước lệch đã được sửa và kiểm lại xong.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|---|---|
| Server Component → client | `requirePermission` ở server; `editable` ở client chỉ ẩn/hiện |
| ô số lượng → `chung_tu_dong` | Người dùng gõ tự do, đi thẳng vào bảng qua PostgREST |
| `currentStock` hiển thị | Ảnh chụp lúc tải, không phải số thời gian thực |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-54 | Tampering | Sửa dòng của phiếu đã ghi sổ bằng `PATCH` thẳng | mitigate | Policy `chi sua dong cua chung tu nhap lieu` (0016) đòi cha `NHAP_LIEU`; `editable` là lớp thứ hai |
| T-04-55 | Tampering | Dòng gán kho không thuộc phạm vi thủ kho → hàng ra khỏi kho họ không thấy | accept | `chung_tu_dong.kho_id` tham chiếu `kho`, và `danh_sach_chung_tu` từ 0041 đã cho thủ kho thấy phiếu có DÒNG thuộc kho mình. Không siết thêm ở phase này; ghi lại làm đầu vào cho phase kiểm kê |
| T-04-56 | Tampering | Mã thiếu `kho_mac_dinh_id` rơi về kho đầu phiếu → trừ nhầm kho, lệch tồn âm thầm | mitigate | Chặn thêm dòng, thông báo chỉ đúng chỗ sửa; RPC `tao_phieu_xuat_tu_don` (plan 04-04) cũng chặn cùng lý do ở đường tạo từ đơn |
| T-04-57 | Information Disclosure | Bảng dòng lộ giá vốn qua cột giá | mitigate | Không có cột giá nào; `dong_chung_tu` không trả cột giá vốn; acceptance criteria chặn chuỗi `gia_von` |
| T-04-58 | Tampering | Người dùng tin màu cảnh báo là bảo đảm, ghi sổ vẫn âm vì ai đó vừa xuất trước | accept | `currentStock` là ảnh chụp — ghi comment nói rõ. Chốt chặn thật là `ghi_so_chung_tu` đọc `ton_kho` trong cùng transaction lúc ghi sổ |
| T-04-59 | Spoofing | Enter chọn nhầm mã gần giống → xuất sai hàng | mitigate | `ProductSearchInput` ưu tiên mã khớp tuyệt đối (bẫy 15); checkpoint Task 4 bước 2 |
| T-04-SC | Tampering | Cài gói npm/pip/cargo | accept | Plan này không cài gói mới nào |
</threat_model>

<verification>
- `npm run check` exit 0
- grep xác nhận `onKeyDownCapture`, `setTimeout`, `currentStock`, `rowClassName`,
  `defaultWarehouseId`; không cột giá; không `!` trước class Tailwind
- Checkpoint Task 4: bảy bước chạy đúng, console sạch
</verification>

<success_criteria>
- `/xuat-kho/{id}` hiện đầu phiếu và bảng dòng, đủ bốn trạng thái
- Phiếu sinh từ đơn hiện sẵn mọi dòng; sửa một ô số lượng chỉ tốn vài phím
- Dòng vượt tồn đổi màu ngay khi gõ, và cập nhật khi đổi kho dòng
- Mã chưa gán kho mặc định bị chặn kèm câu chỉ đúng chỗ sửa
- Không có cột giá nào trên màn phiếu xuất
</success_criteria>

<output>
Create `.planning/phases/04-don-dat-hang-phieu-xuat/04-11-SUMMARY.md` when done.
Ghi lại: route mới (`/xuat-kho/[id]`) cho plan 04-15; quyết định dùng chung hay không dùng
chung `issue-detail.tsx` cho màn phiếu trả của plan 04-14; kết quả checkpoint kèm console.
</output>
</content>
