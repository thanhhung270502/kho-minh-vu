---
phase: 04-don-dat-hang-phieu-xuat
plan: 10
type: execute
wave: 8
depends_on: ["04-07", "04-08"]
files_modified:
  - src/app/(app)/xuat-kho/page.tsx
  - src/features/stock-out/components/issue-table.tsx
  - src/features/stock-out/components/issue-table-body.tsx
  - src/features/stock-out/components/issue-filter-panel.tsx
  - src/features/stock-out/components/issue-toolbar.tsx
  - src/features/stock-out/components/create-issue-button.tsx
autonomous: true
requirements: [XUAT-01, XUAT-02]

must_haves:
  truths:
    - "Vào /xuat-kho thấy danh sách phiếu xuất, lọc được, bộ lọc nằm trên URL"
    - "Phiếu xuất sinh từ đơn hiện được số đơn gốc kèm link mở đơn đó"
    - "Tạo được phiếu xuất mới không cần đơn đặt hàng"
  artifacts:
    - path: "src/app/(app)/xuat-kho/page.tsx"
      provides: "Route danh sách phiếu xuất, chặn quyền bằng requirePermission"
      contains: "requirePermission"
    - path: "src/features/stock-out/components/issue-table.tsx"
      provides: "Điều phối ListLayout + QueryState + bộ lọc URL của màn xuất"
      min_lines: 80
    - path: "src/features/stock-out/components/create-issue-button.tsx"
      provides: "Tạo phiếu xuất không cần đơn (XUAT-02): người nhận + kho, cấp số trên server"
  key_links:
    - from: "src/features/stock-out/components/issue-table.tsx"
      to: "toIssueListRpcArgs"
      via: "lọc loai_ct XUAT ở server, không lọc ở client"
      pattern: "toIssueListRpcArgs"
    - from: "src/features/stock-out/components/create-issue-button.tsx"
      to: "createIssue"
      via: "cấp số bằng sinh_so_ct trên server rồi insert"
      pattern: "createIssue"
---

<objective>
Màn danh sách phiếu xuất và đường tạo phiếu xuất **không cần đơn** (XUAT-02).

Đường chính — tạo phiếu từ đơn đã xác nhận (XUAT-01, DDH-04) — nằm ở plan 04-12, bấm từ
trang chi tiết đơn. Plan này lo đường phụ nhưng vẫn bắt buộc: kho xuất hàng lẻ, xuất nội
bộ, xuất không có đơn nào đi trước.

Khuôn lấy gần như 1:1 từ màn phiếu nhập của Phase 3, đổi ba chỗ:
- `p_loai_ct` từ `"NHAP"` sang `"XUAT"` (đã nằm trong `toIssueListRpcArgs` của plan 04-07);
- bỏ cột "Nguồn" (chỉ NHẬP mới có `nguon_nhap`), thay bằng cột **"Đơn gốc"** nullable —
  phiếu xuất có thể không gắn đơn;
- bỏ bộ lọc theo nguồn nhập.

Purpose: cửa vào của màn phiếu xuất, và đường tạo phiếu cho trường hợp không có đơn.
Output: `/xuat-kho` chạy được; tạo được phiếu rỗng và nhảy sang trang chi tiết.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-CONTEXT.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-07-SUMMARY.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-08-SUMMARY.md

**Điều kiện dữ liệu:** `chung_tu` với `loai_ct='XUAT'` hiện có **0 dòng** (đo 20/09). Màn
này sẽ rỗng cho tới khi ai đó tạo phiếu đầu tiên — trạng thái rỗng phải tử tế, có câu giải
thích và nút tạo phiếu, không phải một bảng trống trơn.

**Bẫy áp dụng:**
- **Bẫy 1:** file import `antd` phải có `"use client"` dòng đầu; route Server Component
  không import `antd`.
- **Bẫy 11:** antd v6 chỉ cảnh báo lúc chạy. Mục "tất cả" trong `Select` bộ lọc dùng
  `value: ""`, không `null`. `Drawer` dùng `size` chứ không `width`.
- **Bẫy 12:** ghi route mới vào SUMMARY để plan 04-15 thêm vào ma trận quyền.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Màn danh sách phiếu xuất</name>
  <files>src/app/(app)/xuat-kho/page.tsx, src/features/stock-out/components/issue-table.tsx, src/features/stock-out/components/issue-table-body.tsx, src/features/stock-out/components/issue-filter-panel.tsx, src/features/stock-out/components/issue-toolbar.tsx</files>
  <read_first>
    - src/app/(app)/nhap-kho/page.tsx (khuôn route + `Suspense` + `requirePermission`)
    - src/features/stock-in/components/receipt-table.tsx (khuôn điều phối)
    - src/features/stock-in/components/receipt-table-body.tsx (khuôn cột + `Table` + `pagination`)
    - src/features/stock-in/components/receipt-filter-panel.tsx
    - src/features/stock-in/components/receipt-toolbar.tsx
    - src/shared/components/list-layout.tsx và src/shared/components/query-state.tsx
    - src/features/stock-out/schemas/issue.schema.ts + hooks/useIssues.ts + types.ts (plan 04-07)
    - src/features/sales-order/components/order-table.tsx (plan 04-08 — cùng khuôn, giữ hai màn nhất quán)
  </read_first>
  <action>
`src/app/(app)/xuat-kho/page.tsx` — Server Component: `metadata.title = "Phiếu xuất"`,
`const user = await requirePermission("view-catalog")`, `PageHeader` với mô tả tiếng Việt
("Hàng ra kho — ghi sổ xong là tồn giảm."), rồi `<Suspense fallback={null}><IssueTable
canCreate={hasPermission(user.role, "edit-catalog")} /></Suspense>`.
Không import `antd`, không gọi hàm export từ file `"use client"`.

`issue-table.tsx` (`"use client"`) — chép khuôn `receipt-table.tsx`: đọc bộ lọc bằng
`readIssueFilterFromUrl(useSearchParams())`, ghi ngược bằng `writeIssueFilterToUrl` +
`router.replace`, gọi `useIssues(filter)`, bọc `<QueryState>` đủ bốn trạng thái:
- loading → skeleton;
- error → câu đọc hiểu được **và** nút "Thử lại";
- empty → "Chưa có phiếu xuất nào" + nút tạo phiếu;
- success → bảng.
Bố cục `ListLayout`.

`issue-table-body.tsx` (`"use client"`) — cột:
Số phiếu (link `/xuat-kho/{id}`) · Ngày · Người nhận · **Đơn gốc** (`orderNo`, render
`<Link href={\`/dat-hang/${orderId}\`}>` khi có, để trống khi không) · Kho · Số dòng ·
Tổng số lượng · Trạng thái (`Tag` theo `DOC_STATUS_LABELS`/`DOC_STATUS_COLORS`) · Người tạo.
**Không có cột "Nguồn"** và **không có cột tiền** — phiếu xuất không mang giá bán, cột
`tong_tien` của nó luôn 0 nên hiện ra chỉ gây hiểu nhầm.
`pagination` lấy `total` từ `tong_so_dong`, `showSizeChanger={false}`.
Bảng cuộn ngang trong khung riêng.

`issue-filter-panel.tsx` (`"use client"`): trạng thái (mục "Tất cả" dùng `value: ""`),
người nhận (`PartnerSearchInput` của `@/shared/components/partner-search-input`), kho
(`Select` nạp danh sách kho — chỉ 2 kho, nạp hết là hợp lý), khoảng ngày. Nút "Xóa lọc"
hiện khi `countActiveIssueFilters(filter) > 0`. **Không có bộ lọc nguồn nhập.**

`issue-toolbar.tsx` (`"use client"`): ô tìm `q` có debounce + prop `addButton?: ReactNode`.

Component nào vượt ~200 dòng thì tách.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0.
    - `grep -c "antd" "src/app/(app)/xuat-kho/page.tsx"` trả `0`; file có `requirePermission`
      và `Suspense`.
    - Bốn file component mở bằng `"use client";`.
    - `grep -c "QueryState" src/features/stock-out/components/issue-table.tsx` ≥ 1.
    - `grep -rc "nguon_nhap\|RECEIPT_SOURCE\|Nguồn" src/features/stock-out/components/`
      trả `0` cho mọi file.
    - `grep -c "dat-hang/" src/features/stock-out/components/issue-table-body.tsx` ≥ 1 —
      cột Đơn gốc có link.
    - `grep -c "value: null" src/features/stock-out/components/issue-filter-panel.tsx` trả `0`.
    - `grep -rc "tong_tien\|Thành tiền\|Tổng tiền" src/features/stock-out/components/` trả `0`.
    - Không file nào trong `src/features/stock-out/components/` vượt 200 dòng.
  </acceptance_criteria>
  <done>`/xuat-kho` render danh sách, lọc được, bộ lọc trên URL, cột Đơn gốc có link.</done>
</task>

<task type="auto">
  <name>Task 2: Nút tạo phiếu xuất không cần đơn</name>
  <files>src/features/stock-out/components/create-issue-button.tsx</files>
  <read_first>
    - src/features/stock-in/components/create-receipt-button.tsx (toàn bộ — khuôn Modal + form + `router.push`)
    - src/shared/components/partner-search-input.tsx (plan 04-08)
    - src/features/stock-out/hooks/useIssues.ts (`useCreateIssue`)
    - src/features/stock-out/schemas/issue.schema.ts (`newIssueSchema`)
    - src/shared/lib/errors.ts
  </read_first>
  <action>
Tạo `src/features/stock-out/components/create-issue-button.tsx` (`"use client"`).

`Button` "Tạo phiếu xuất" mở `Modal` gồm: người nhận (`PartnerSearchInput`, `autoFocus`),
kho xuất (`Select` danh sách kho), ngày phiếu (`DatePicker`, mặc định hôm nay).
**Không có ô số phiếu** — số cấp trên server bởi `sinh_so_ct`.

Kho ở đây là **kho đầu phiếu**; `chung_tu.kho_id` là NOT NULL nên bắt buộc chọn. D-13 cho
sửa kho theo từng dòng ở màn chi tiết (plan 04-11) — nói rõ điều này bằng một dòng
`Typography.Text type="secondary"` dưới ô kho: "Kho từng dòng sửa được sau khi mở phiếu."

Validate bằng `newIssueSchema`; thiếu người nhận hoặc thiếu kho thì lỗi hiện dưới đúng ô đó.

`onOk` gọi `useCreateIssue()`, nhận `id`, `router.push(\`/xuat-kho/${id}\`)`.

Bắt lỗi theo mã, khuôn `post-receipt-button.tsx`: `42501` → câu cố định về quyền;
`23514` → hiện nguyên văn message RPC; còn lại → `explainError`. Dùng `errorCode` /
`isPostgrestError`, **không** `instanceof PostgrestError` (bẫy 8).
Nút OK `loading` khi đang gửi; đóng modal bị chặn khi đang gửi.

Cắm nút vào `issue-toolbar` qua prop `addButton` và vào trạng thái rỗng của `QueryState`
trong `issue-table.tsx`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0; dòng đầu file là `"use client";`.
    - File chứa `useCreateIssue` và `router.push`.
    - `grep -c "sinh_so_ct\|so_ct" src/features/stock-out/components/create-issue-button.tsx`
      trả `0` — cấp số là chuyện của lớp `api/`.
    - File xử lý riêng `42501` và `23514`, có nhánh `explainError`.
    - `grep -c "maskClosable" …` trả `0`.
    - `grep -c "CreateIssueButton" src/features/stock-out/components/issue-table.tsx` ≥ 2 —
      cắm cả ở toolbar lẫn trạng thái rỗng.
  </acceptance_criteria>
  <done>Tạo được phiếu xuất không cần đơn, xong thì nhảy sang trang chi tiết.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|---|---|
| Server Component → client | `requirePermission` ở server; `canCreate` ở client chỉ ẩn/hiện nút |
| tham số URL → RPC | Bộ lọc do người dùng gõ đi vào `danh_sach_chung_tu` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-49 | Elevation of Privilege | Gõ tay `/xuat-kho` khi chưa đăng nhập hoặc sai vai trò | mitigate | `await requirePermission("view-catalog")` đầu Server Component; plan 04-15 thêm dòng vào ma trận quyền route |
| T-04-50 | Elevation of Privilege | `chi_xem` bấm "Tạo phiếu xuất" | mitigate | `canCreate = hasPermission(role, "edit-catalog")` ẩn nút; chặn thật là policy `tao chung tu tru chi xem` (0016) và `sinh_so_ct` chặn `chi_xem` (0048) |
| T-04-51 | Information Disclosure | Thủ kho thấy phiếu xuất của kho không thuộc phạm vi mình | mitigate | `danh_sach_chung_tu` đã lọc phạm vi kho cho `thu_kho` từ 0041 (header, kho đến, hoặc dòng); plan này không đụng vào nhánh đó |
| T-04-52 | Tampering | Bộ lọc URL chứa giá trị bậy | mitigate | `readIssueFilterFromUrl` của plan 04-07 lọc qua `readUuid`/`readDate` và kiểm trạng thái theo danh sách hợp lệ |
| T-04-53 | Tampering | Tạo phiếu xuất thiếu kho → `chung_tu.kho_id` NOT NULL nổ lỗi thô ra người dùng | mitigate | `newIssueSchema` bắt buộc `warehouseId` và hiện lỗi dưới đúng ô, trước khi gửi |
| T-04-SC | Tampering | Cài gói npm/pip/cargo | accept | Plan này không cài gói mới nào |
</threat_model>

<verification>
- `npm run check` exit 0
- grep xác nhận: route không import antd, có `requirePermission` + `Suspense`; component có
  `"use client"`; có `QueryState`; không có cột Nguồn, không có cột tiền; không `value: null`
- Không file component nào vượt 200 dòng
</verification>

<success_criteria>
- `/xuat-kho` render được với đủ bốn trạng thái
- Bộ lọc đọc/ghi trên URL tiếng Việt không dấu; không có bộ lọc nguồn nhập
- Cột "Đơn gốc" hiện số đơn kèm link, để trống khi phiếu không gắn đơn
- Tạo được phiếu xuất không cần đơn, cấp số trên server, chuyển sang `/xuat-kho/{id}`
</success_criteria>

<output>
Create `.planning/phases/04-don-dat-hang-phieu-xuat/04-10-SUMMARY.md` when done.
Ghi lại: route mới (`/xuat-kho`) kèm quyền, cho plan 04-15; mọi prop antd v6 phải đổi so
với khuôn Phase 3.
</output>
</content>
