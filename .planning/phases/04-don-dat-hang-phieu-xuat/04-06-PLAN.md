---
phase: 04-don-dat-hang-phieu-xuat
plan: 06
type: execute
wave: 6
depends_on: ["04-05"]
files_modified:
  - src/features/sales-order/types.ts
  - src/features/sales-order/lib/order-status.ts
  - src/features/sales-order/schemas/order.schema.ts
  - src/features/sales-order/api/order.keys.ts
  - src/features/sales-order/api/order.api.ts
  - src/features/sales-order/hooks/useOrders.ts
autonomous: true
requirements: [DDH-01, DDH-02, DDH-03, DDH-04]

must_haves:
  truths:
    - "Component và hook của màn đơn không bao giờ thấy tên cột tiếng Việt"
    - "Số còn lại của một dòng đơn tính khi đọc, không lưu thành state hay cột"
    - "Bộ lọc màn đơn đọc và ghi được trên URL tiếng Việt không dấu"
    - "Mỗi thao tác đổi trạng thái đơn có đúng một hàm gọi RPC tương ứng"
  artifacts:
    - path: "src/features/sales-order/types.ts"
      provides: "OrderRow/OrderDetail/OrderLine + mapper từ ba RPC đơn"
      exports: ["OrderRow", "OrderDetail", "OrderLine", "toOrderRow", "toOrderDetail", "toOrderLine"]
      min_lines: 90
    - path: "src/features/sales-order/lib/order-status.ts"
      provides: "OrderStatus + nhãn + màu, file thuần để cả server và client import"
      exports: ["ORDER_STATUS_LABELS", "ORDER_STATUS_COLORS"]
    - path: "src/features/sales-order/api/order.api.ts"
      provides: "Toàn bộ hàm gọi Supabase của đơn đặt hàng"
      exports: ["fetchOrders", "fetchOrderDetail", "fetchOrderLines", "createOrder", "approveOrder", "createIssueFromOrder"]
    - path: "src/features/sales-order/hooks/useOrders.ts"
      provides: "Hook TanStack Query bọc lớp api"
      exports: ["useOrders", "useOrderDetail", "useOrderLines", "useApproveOrder"]
  key_links:
    - from: "src/features/sales-order/types.ts"
      to: "Database Functions danh_sach_don / chi_tiet_don / dong_don"
      via: "kiểu suy ra từ database.types.ts, không tự viết"
      pattern: "Functions"
    - from: "src/features/sales-order/hooks/useOrders.ts"
      to: "orderKeys"
      via: "mutation invalidate đúng key sau khi thành công"
      pattern: "invalidateQueries"
---

<objective>
Lớp dữ liệu của đơn đặt hàng: kiểu, schema, query key, hàm gọi Supabase, hook.

Khuôn lấy từ `src/features/stock-in/` (schema + api + hooks) nhưng **đơn không phải
chứng từ** — nó có RPC riêng (`danh_sach_don` / `chi_tiet_don` / `dong_don` của plan
04-03), enum trạng thái riêng (`trang_thai_ddh`), và **không có cột giá**. Không tái dùng
`DocStatus`, không tái dùng `toReceiptListRpcArgs`.

D-04: "đã xuất / còn lại" **tính khi đọc**. `dong_don` trả `so_luong_dat` và
`so_luong_da_xuat`; phép trừ làm trong `toOrderLine`, không lưu cột, không lưu state.

Purpose: khóa hợp đồng dữ liệu cho ba plan giao diện đơn ở Wave 7–9.
Output: sáu file trong `src/features/sales-order/`, chưa có component nào.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-CONTEXT.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-03-SUMMARY.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-05-SUMMARY.md

**Quy ước URL (CLAUDE.md):** route và tham số truy vấn là **tiếng Việt không dấu**.
Màn đơn dùng `/dat-hang` với `?q=`, `?trang_thai=`, `?doi_tac=`, `?tu_ngay=`,
`?den_ngay=`, `?trang=`. Ruột code tiếng Anh.

**Bẫy phải tránh trong plan này:**
- **Bẫy 8:** lỗi PostgREST **không** phải instance của `PostgrestError`. Dùng
  `errorCode(e)` / `isPostgrestError(e)` từ `src/shared/lib/errors.ts`, không `instanceof`.
- **Bẫy 10:** hook đọc một bản ghi phải có `enabled: id !== ""`, nếu không sẽ bắn RPC với
  uuid rỗng và nhận HTTP 400 mỗi lần mở trang.
- **Bẫy 9:** hằng số và hàm thuần để ở `lib/*.ts` không `"use client"` — Server Component
  cũng import được.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kiểu, nhãn trạng thái, schema và query key của đơn</name>
  <files>src/features/sales-order/types.ts, src/features/sales-order/lib/order-status.ts, src/features/sales-order/schemas/order.schema.ts, src/features/sales-order/api/order.keys.ts</files>
  <read_first>
    - src/features/documents/types.ts (khuôn mapper — chép cách đặt tên và cách `Number()` cột numeric)
    - src/features/stock-in/schemas/receipt.schema.ts (khuôn schema + bộ lọc URL, đặc biệt `readReceiptFilterFromUrl` / `writeReceiptFilterToUrl` / `toReceiptListRpcArgs`)
    - src/features/documents/lib/url-filter.ts (`readUuid`, `readDate` đã rút ra ở plan 04-05)
    - src/features/stock-in/api/receipt.keys.ts
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-03-SUMMARY.md (chữ ký đầy đủ của `danh_sach_don`, `chi_tiet_don`, `dong_don`)
    - src/types/database.types.ts — Grep `danh_sach_don` rồi Read đúng đoạn `Returns`
  </read_first>
  <action>
**`src/features/sales-order/lib/order-status.ts`** (file thuần, KHÔNG `"use client"`):
`export type OrderStatus = Database["public"]["Enums"]["trang_thai_ddh"];` cùng
`ORDER_STATUS_LABELS: Record<OrderStatus, string>` = `TAM` → "Đơn tạm",
`DA_XAC_NHAN` → "Đã xác nhận", `HOAN_THANH` → "Hoàn thành", `DA_HUY` → "Đã hủy"; và
`ORDER_STATUS_COLORS` theo khuôn `DOC_STATUS_COLORS` (`gold` / `blue` / `green` /
`undefined`). Thêm hằng `ORDER_STATUSES: OrderStatus[]` để bộ lọc kiểm giá trị URL.

**`src/features/sales-order/types.ts`**: kiểu suy ra từ `database.types.ts`, không tự
viết. Ba kiểu miền tiếng Anh + ba mapper:
- `OrderRow`: `id`, `orderNo`, `orderDate`, `status`, `deliveryDate`, `partnerId`,
  `partnerName`, `lineCount`, `orderedQuantity`, `shippedQuantity`, `createdByName`,
  `note`, `createdAt`, `totalRows`.
- `OrderDetail`: như trên bỏ `lineCount`/`totalRows`, thêm `partnerCode`.
- `OrderLine`: `id`, `productId`, `productCode`, `productName`, `unitName`,
  `orderedQuantity`, `shippedQuantity`, **`remainingQuantity`**, `defaultWarehouseId`,
  `defaultWarehouseName`, `createdAt`.
  `remainingQuantity = Math.max(0, orderedQuantity - shippedQuantity)` tính **trong
  `toOrderLine`** — D-04, không lưu ở database.
- Thêm `isFullyShipped(line: OrderLine): boolean` nếu cần, dạng hàm thuần.
Mọi cột `numeric` map qua `Number(...)`.
**Không** có field nào tên `unitPrice`/`amount` — chốt 19/09 câu 7, đơn không mang giá.

**`src/features/sales-order/schemas/order.schema.ts`**:
- `orderHeaderSchema`: `partnerId` là `z.string().uuid()` **bắt buộc** (D-03 giữ
  `doi_tac_id` NOT NULL, không có ô text tự do), `deliveryDate` là chuỗi `yyyy-mm-dd` hoặc
  `null`, `note` là chuỗi hoặc `null`. `orderDate` tùy chọn.
- `orderLineSchema`: `productId` uuid, `quantity` số dương (`z.coerce.number().positive()`),
  thông báo lỗi bằng tiếng Việt. **Không có trường giá.**
- `toOrderUpdate` / `toOrderLineUpdate`: map input tiếng Anh → cột tiếng Việt
  (`doi_tac_id`, `ngay_giao_du_kien`, `ghi_chu`, `so_luong_dat`, `san_pham_id`).
  Đây là ranh giới duy nhất được nhắc tên cột.
- `OrderFilter` + `DEFAULT_ORDER_FILTER` + `ORDER_PAGE_SIZE = 50`.
- `readOrderFilterFromUrl` / `writeOrderFilterToUrl` dùng tham số
  `q`, `trang_thai`, `doi_tac`, `tu_ngay`, `den_ngay`, `trang`. Dùng lại `readUuid` /
  `readDate` từ `@/features/documents/lib/url-filter`. Chặn `Number(null) === 0` giống
  `readReceiptFilterFromUrl` (đọc comment ở đó).
- `countActiveOrderFilters(filter)` — không tính ô tìm.
- `toOrderListRpcArgs(filter)` trả kiểu
  `Database["public"]["Functions"]["danh_sach_don"]["Args"]`. **Viết riêng**, không map
  qua `toReceiptListRpcArgs` — tham số hai RPC khác nhau.

**`src/features/sales-order/api/order.keys.ts`**: `orderKeys` với `all`, `list(filter)`,
`detail(id)`, `lines(id)` theo khuôn `receiptKeys`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Bốn file tồn tại đúng đường dẫn.
    - `grep -l "use client" src/features/sales-order/lib/order-status.ts src/features/sales-order/types.ts src/features/sales-order/schemas/order.schema.ts`
      không in ra file nào.
    - `grep -rc "unitPrice\|don_gia\|thanh_tien" src/features/sales-order/` trả `0` cho mọi file.
    - `src/features/sales-order/types.ts` export `toOrderRow`, `toOrderDetail`, `toOrderLine`
      và kiểu `OrderLine` có field `remainingQuantity`.
    - `grep -c "so_luong_dat - \|orderedQuantity - " src/features/sales-order/types.ts` ≥ 1
      — số còn lại tính trong mapper.
    - `grep -o 'params.get("[a-z_]*")' src/features/sales-order/schemas/order.schema.ts`
      in ra đủ `q`, `trang_thai`, `doi_tac`, `tu_ngay`, `den_ngay`, `trang`.
    - `grep -c "toReceiptListRpcArgs" src/features/sales-order/schemas/order.schema.ts` trả `0`.
    - `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>Kiểu, nhãn, schema, bộ lọc URL và query key của đơn đã có; chưa gọi Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: Hàm gọi Supabase và hook TanStack Query cho đơn</name>
  <files>src/features/sales-order/api/order.api.ts, src/features/sales-order/hooks/useOrders.ts</files>
  <read_first>
    - src/features/documents/api/document.api.ts (khuôn hàm gọi Supabase sau refactor 04-05)
    - src/features/stock-in/api/receipt.api.ts (`createReceipt` — khuôn "cấp số trên server rồi insert")
    - src/features/stock-in/hooks/useReceipts.ts (toàn bộ — khuôn hook, `enabled`, `onSuccess: refresh`)
    - src/features/sales-order/types.ts và schemas/order.schema.ts (vừa viết ở Task 1)
    - src/shared/lib/errors.ts
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-02-SUMMARY.md và 04-04-SUMMARY.md (chữ ký `sinh_so_dh`, `xac_nhan_don`, `mo_khoa_don`, `dong_don_som`, `tao_phieu_xuat_tu_don`)
  </read_first>
  <action>
**`src/features/sales-order/api/order.api.ts`** — hàm thuần, nhận tham số, trả dữ liệu đã
có kiểu; không JSX, không hook. Mọi hàm giữ khuôn
`const { data, error } = await ...; if (error) throw error;`.

Đọc:
- `fetchOrders(filter: OrderFilter): Promise<{ items: OrderRow[]; total: number }>` —
  gọi `rpc("danh_sach_don", toOrderListRpcArgs(filter))`, `total` lấy từ `tong_so_dong`
  của dòng đầu (0 dòng → 0).
- `fetchOrderDetail(id): Promise<OrderDetail | null>` — `rpc("chi_tiet_don", { p_id: id })`.
- `fetchOrderLines(id): Promise<OrderLine[]>` — `rpc("dong_don", { p_id: id })`.

Ghi:
- `createOrder(input: { partnerId: string; deliveryDate?: string | null }): Promise<string>`
  — hai bước trong một hàm, đúng khuôn `createReceipt`: `rpc("sinh_so_dh")` lấy số rồi
  `.from("don_dat_hang").insert({ so_dh, doi_tac_id, ngay_giao_du_kien }).select("id").single()`.
  **Không ghép số ở client** — hai người tạo cùng lúc sẽ trùng.
  Liệt kê cột trong `.select("id")`, không để `.select()` trống (bẫy 5).
- `updateOrderHeader(id, input)` — `.update(toOrderUpdate(input), { count: "exact" })`;
  `if (!count) throw new Error("Không lưu được — đơn đã xác nhận hoặc tài khoản không có
  quyền sửa.")`. RLS của plan 04-02 chặn im lặng, không ném lỗi, nên phải tự phát hiện
  bằng `count`.
- `addOrderLine(orderId, line)` / `updateOrderLine(lineId, line)` / `deleteOrderLine(lineId)`
  trên `don_dat_hang_dong`, dùng `toOrderLineUpdate`.
- `approveOrder(id)` → `rpc("xac_nhan_don", { p_id: id })`.
- `unlockOrder(id, reason)` → `rpc("mo_khoa_don", { p_id: id, p_ly_do: reason })`.
- `closeOrderEarly(id, reason)` → `rpc("dong_don_som", { p_id: id, p_ly_do: reason })`.
- `createIssueFromOrder(orderId): Promise<string>` → `rpc("tao_phieu_xuat_tu_don",
  { p_don_id: orderId })`, trả `id` của chứng từ vừa sinh.

**`src/features/sales-order/hooks/useOrders.ts`** — `"use client"` ở dòng đầu.
- `useOrders(filter)`, `useOrderDetail(id)`, `useOrderLines(id)`. Hai hook sau **bắt buộc**
  `enabled: id !== ""` (bẫy 10).
- `useCreateOrder`, `useUpdateOrderHeader(id)`, `useAddOrderLine(orderId)`,
  `useUpdateOrderLine(orderId)`, `useDeleteOrderLine(orderId)`, `useApproveOrder(id)`,
  `useUnlockOrder(id)`, `useCloseOrderEarly(id)`, `useCreateIssueFromOrder(id)`.
- Mỗi mutation `onSuccess` invalidate đúng key: sửa dòng → `orderKeys.lines(id)` +
  `orderKeys.detail(id)`; đổi trạng thái → thêm `orderKeys.all`.
- `useCreateIssueFromOrder` invalidate thêm `documentKeys.all` — phiếu xuất mới phải hiện
  ở màn `/xuat-kho`. **Không** invalidate `productKeys` ở bất kỳ hook nào của plan này:
  tạo/duyệt đơn không đụng tồn và giá vốn (khác `useRefreshAfterPosting` của `stock-in`).
- Không dùng `useEffect` + `useState` để lấy dữ liệu — đã có TanStack Query.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0.
    - `grep -c "if (error) throw error" src/features/sales-order/api/order.api.ts` ≥ 8 —
      mọi lượt gọi Supabase đều kiểm lỗi.
    - `grep -c "instanceof PostgrestError" src/features/sales-order/` trả `0` (bẫy 8).
    - `grep -c 'enabled: id !== ""' src/features/sales-order/hooks/useOrders.ts` ≥ 2.
    - `grep -c "productKeys" src/features/sales-order/` trả `0`.
    - `grep -c "useEffect" src/features/sales-order/hooks/useOrders.ts` trả `0`.
    - `order.api.ts` chứa cả năm tên RPC: `sinh_so_dh`, `xac_nhan_don`, `mo_khoa_don`,
      `dong_don_som`, `tao_phieu_xuat_tu_don`.
    - `grep -c '.select()' src/features/sales-order/api/order.api.ts` trả `0` — không có
      `.select()` trống sau insert/update (bẫy 5).
    - Dòng đầu của `hooks/useOrders.ts` là `"use client";`.
  </acceptance_criteria>
  <done>Lớp gọi dữ liệu và hook của đơn đã xong; component Wave 7–9 chỉ việc dùng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|---|---|
| trình duyệt → PostgREST | `order.api.ts` là nơi duy nhất dựng truy vấn đơn |
| tham số URL → truy vấn | Giá trị trên URL do người dùng gõ, đi thẳng vào tham số RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-28 | Tampering | `?doi_tac=` hoặc `?tu_ngay=` chứa chuỗi bậy đi thẳng vào RPC | mitigate | `readOrderFilterFromUrl` lọc qua `readUuid` / `readDate`; giá trị không hợp lệ thành `null`, không ném lỗi và không rơi vào truy vấn |
| T-04-29 | Tampering | `?trang_thai=` chứa giá trị ngoài enum → PostgREST trả 400, màn hình trắng | mitigate | Kiểm bằng `ORDER_STATUSES.includes(...)` trước khi nhận, giống `STATUSES` của `receipt.schema.ts` |
| T-04-30 | Elevation of Privilege | `updateOrderHeader` bị RLS lọc im lặng, người dùng tưởng đã lưu | mitigate | `{ count: "exact" }` + ném lỗi có câu tiếng Việt khi `count` rỗng |
| T-04-31 | Tampering | Ghép `so_dh` ở client → hai người tạo cùng lúc trùng số | mitigate | `createOrder` gọi `sinh_so_dh` trên server; unique index trên `don_dat_hang.so_dh` là chốt chặn cuối |
| T-04-32 | Information Disclosure | `.select()` trống sau insert kéo về mọi cột, lỗi 42501 hoặc lộ cột không định trả | mitigate | Acceptance criteria cấm `.select()` trống; luôn liệt kê cột |
| T-04-SC | Tampering | Cài gói npm/pip/cargo | accept | Plan này không cài gói mới nào |
</threat_model>

<verification>
- `npm run check` exit 0
- grep xác nhận: không có trường giá trong `sales-order`, không `instanceof PostgrestError`,
  mọi hook đọc một bản ghi có `enabled`, mọi lượt gọi Supabase có `if (error) throw error`
- `toOrderListRpcArgs` viết riêng, không mượn của màn nhập
</verification>

<success_criteria>
- `src/features/sales-order/` có đủ kiểu, nhãn trạng thái, schema, bộ lọc URL, query key,
  lớp api và hook
- Số còn lại của một dòng đơn tính trong mapper, không lưu ở đâu
- Đơn không mang giá ở bất kỳ tầng nào
- Component và hook không thấy tên cột tiếng Việt
</success_criteria>

<output>
Create `.planning/phases/04-don-dat-hang-phieu-xuat/04-06-SUMMARY.md` when done.
Ghi lại: danh sách export đầy đủ của bốn file (tên + chữ ký) để plan 04-08/04-09/04-12
khỏi đoán, và tên tham số URL đã chốt.
</output>
</content>
