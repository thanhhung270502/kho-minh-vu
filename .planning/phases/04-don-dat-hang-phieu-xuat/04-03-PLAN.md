---
phase: 04-don-dat-hang-phieu-xuat
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - supabase/migrations/0054_rpc_don_dat_hang.sql
  - supabase/migrations/0055_de_nghi_gop_ma.sql
  - supabase/tests/27_rpc_don_test.sql
  - supabase/tests/28_goi_y_ma_trung_test.sql
  - src/types/database.types.ts
autonomous: true
requirements: [DDH-01, DDH-02]

must_haves:
  truths:
    - "Màn danh sách đơn lấy được một trang đơn đã lọc kèm tổng số dòng trong một lượt gọi"
    - "Màn chi tiết đơn lấy được đầu đơn và từng dòng kèm số đã xuất"
    - "Khi một dòng làm tồn âm, hệ thống chỉ ra được mã tên gần giống đang còn tồn ở kho nào"
    - "Bấm Đề nghị gộp hai mã chỉ ghi lại đề nghị, không đụng tới tồn hay sổ cái"
  artifacts:
    - path: "supabase/migrations/0054_rpc_don_dat_hang.sql"
      provides: "danh_sach_don / chi_tiet_don / dong_don"
      contains: "tong_so_dong"
    - path: "supabase/migrations/0055_de_nghi_gop_ma.sql"
      provides: "Bảng de_nghi_gop_ma + ghi_de_nghi_gop_ma + goi_y_ma_trung"
      contains: "word_similarity"
    - path: "supabase/tests/27_rpc_don_test.sql"
      provides: "pgTAP cho ba RPC đơn"
      contains: "select plan("
    - path: "supabase/tests/28_goi_y_ma_trung_test.sql"
      provides: "pgTAP cho gợi ý mã trùng và ghi đề nghị gộp"
      contains: "de_nghi_gop_ma"
  key_links:
    - from: "public.dong_don"
      to: "public.chi_tiet_don"
      via: "kiểm quyền một lần ở chi_tiet_don rồi dong_don gọi lại, không lặp logic"
      pattern: "chi_tiet_don"
    - from: "public.goi_y_ma_trung"
      to: "public.f_unaccent + extensions.word_similarity"
      via: "cùng cách so khớp với tim_san_pham"
      pattern: "word_similarity"
---

<objective>
Hai nhóm RPC đọc/ghi mà tầng giao diện Wave 5 trở đi gọi thẳng.

**(1) RPC đơn đặt hàng.** Theo đúng khuôn ba hàm `danh_sach_chung_tu` /
`chi_tiet_chung_tu` / `dong_chung_tu` của migration 0045: lọc + phân trang **ở server**,
trả `tong_so_dong` kèm mọi dòng để giao diện khỏi gọi thêm một lượt đếm.
**D-04 — "còn lại" không lưu cột riêng**: `dong_don` trả cả `so_luong_dat` và
`so_luong_da_xuat`, phép trừ làm ở mapper `types.ts` của tầng client.

**(2) Đề nghị gộp mã (D-14).** Nguyên nhân gốc của xuất âm theo lời người dùng: cùng một
món hàng vật lý bị tách thành hai mã vì quy chuẩn đặt mã đổi giữa chừng. Phase 4 đi nửa
đường: **phát hiện + hỏi + ghi lại đề nghị**. Gộp thật là phase riêng — plan này tuyệt
đối không viết code gộp, không đụng `kho_movement`, không đụng `ton_kho`.

Purpose: khóa hợp đồng dữ liệu trước khi viết tầng client.
Output: hai migration (0054, 0055), hai file pgTAP (27, 28).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-CONTEXT.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-02-SUMMARY.md

**Quyết định phân quyền đọc đơn (04-PATTERNS.md để ngỏ cho bước lập kế hoạch — chốt ở đây):**
`danh_sach_don` / `chi_tiet_don` / `dong_don` cho **cả bốn vai trò đọc, không lọc theo
kho**. Lý do: policy `"doc don dat hang"` của 0016 đã là `using (true)`, và tờ phiếu đi
lấy hàng in ra là để thủ kho cầm — giấu đơn khỏi thủ kho không có lợi ích nghiệp vụ nào.
Chặn thật nằm ở chiều GHI (plan 04-02 đã làm). Route `/dat-hang` dùng quyền `view-catalog`.

**Trạng thái đo 20/09:** `don_dat_hang` 0 dòng, `san_pham` 3.270 dòng, `doi_tac` 24 dòng.
</context>

<tasks>

<task type="auto">
  <name>Task 1: RPC đọc đơn đặt hàng — danh sách, chi tiết, dòng</name>
  <files>supabase/migrations/0054_rpc_don_dat_hang.sql, supabase/tests/27_rpc_don_test.sql</files>
  <read_first>
    - supabase/migrations/0045_rpc_chung_tu.sql (toàn bộ — khuôn CTE `loc` + CTE `dem`, cách `dong_chung_tu` gọi lại `chi_tiet_chung_tu` để không lặp logic quyền)
    - supabase/migrations/0006_don_dat_hang.sql (cột thật của `don_dat_hang` và `don_dat_hang_dong`)
    - supabase/migrations/0029_*.sql (cấm `select *` trên `san_pham`; phải liệt kê cột)
    - supabase/tests/21_danh_sach_chung_tu_test.sql + supabase/tests/00_helper.sql.inc
  </read_first>
  <action>
Viết `supabase/migrations/0054_rpc_don_dat_hang.sql`. Ba hàm, đều
`language plpgsql stable security definer set search_path = ''`, đều kèm
`revoke all ... from public, anon;` + `grant execute ... to authenticated;`.

**`public.danh_sach_don(p_trang_thai public.trang_thai_ddh default null, p_doi_tac_id uuid
default null, p_tu_ngay date default null, p_den_ngay date default null, p_tu_khoa text
default null, p_trang integer default 1, p_kich_thuoc integer default 50)`**
returns table: `id uuid, so_dh text, ngay_dh date, trang_thai public.trang_thai_ddh,
ngay_giao_du_kien date, doi_tac_id uuid, ten_doi_tac text, so_dong bigint,
tong_so_luong_dat numeric, tong_so_luong_da_xuat numeric, ho_ten_nguoi_tao text,
ghi_chu text, created_at timestamptz, tong_so_dong bigint`.
- Mọi điều kiện lọc viết dạng `(p_x is null or <cột> = p_x)` giống 0045.
- `p_tu_khoa` khớp `so_dh` hoặc tên đối tác, qua `public.f_unaccent(...) ilike '%' ||
  public.f_unaccent(p_tu_khoa) || '%'` — cùng cách gõ-không-dấu của `tim_san_pham`.
- `so_dong`, `tong_so_luong_dat`, `tong_so_luong_da_xuat` tổng từ `don_dat_hang_dong`.
- `tong_so_dong` là tổng số ĐƠN khớp bộ lọc (trước phân trang), trả kèm mọi dòng.
- Sắp xếp `ngay_dh desc, so_dh desc`.

**`public.chi_tiet_don(p_id uuid)`** returns table: `id uuid, so_dh text, ngay_dh date,
trang_thai public.trang_thai_ddh, ngay_giao_du_kien date, doi_tac_id uuid, ma_doi_tac text,
ten_doi_tac text, ghi_chu text, tong_so_luong_dat numeric, tong_so_luong_da_xuat numeric,
ho_ten_nguoi_tao text, created_at timestamptz`.

**`public.dong_don(p_id uuid)`** returns table: `id uuid, san_pham_id uuid, ma_hang text,
ten_hang text, ten_dvt text, so_luong_dat numeric, so_luong_da_xuat numeric,
kho_mac_dinh_id uuid, ten_kho_mac_dinh text, created_at timestamptz`.
- **Không** trả cột "còn lại" và **không** trả `don_gia` — D-04 (còn lại tính khi đọc ở
  mapper client) và chốt 19/09 câu 7 (không để giá trên đơn).
- `kho_mac_dinh_id` lấy từ `san_pham.kho_mac_dinh_id`, trả `null` khi mã chưa gán kho —
  giao diện dùng để chặn đúng 4 mã còn thiếu (Claude's Discretion trong 04-CONTEXT.md).
- Mở đầu bằng `if not exists (select 1 from public.chi_tiet_don(p_id)) then return; end if;`
  để thừa hưởng logic quyền của `chi_tiet_don`, đúng cách `dong_chung_tu` làm ở 0045 dòng 161.
- Sắp xếp `created_at, id` — giữ đúng thứ tự người nhập gõ vào.
- Liệt kê cột `san_pham` tường minh, tuyệt đối không `sp.*` (migration 0029 đã thu quyền
  đọc mức bảng; `select *` trả 42501).

Viết `supabase/tests/27_rpc_don_test.sql` theo khuôn `21_danh_sach_chung_tu_test.sql`.
Assert bắt buộc:
- Tạo 3 đơn với trạng thái khác nhau; `danh_sach_don(p_trang_thai => 'TAM')` chỉ trả đơn `TAM`.
- `danh_sach_don(p_kich_thuoc => 1)` trả 1 dòng nhưng `tong_so_dong` bằng tổng thật.
- `danh_sach_don(p_tu_khoa => <một phần so_dh>)` tìm ra đúng đơn đó.
- `chi_tiet_don` trả `tong_so_luong_dat` bằng tổng `so_luong_dat` các dòng.
- `dong_don` trả đúng số dòng, đúng `so_luong_da_xuat` sau khi ghi sổ một phiếu xuất một phần.
- `dong_don` trả `kho_mac_dinh_id` là `null` cho một mã test cố ý không gán kho.
- `dong_don(uuid không tồn tại)` trả 0 dòng, không ném lỗi.
- Cả bốn vai trò (`quan_ly`, `van_phong`, `thukho1`, `chi_xem`) gọi `danh_sach_don` đều
  thấy đơn — khẳng định quyết định phân quyền đọc ở trên.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0054_rpc_don_dat_hang.sql','utf8'); for (const t of ['public.danh_sach_don','public.chi_tiet_don','public.dong_don','tong_so_dong','kho_mac_dinh_id','stable']) { if(!s.includes(t)) throw new Error('thieu: '+t); } if(/\bsp\.\*/.test(s)||/select \*\s+from public\.san_pham/i.test(s)) throw new Error('dang select * tren san_pham'); if(/don_gia/.test(s)) throw new Error('don khong duoc co gia'); const n=(s.match(/grant execute on function/g)||[]).length; if(n<3) throw new Error('thieu grant, dem '+n); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh verify exit 0: có đủ ba hàm, có `tong_so_dong`, có `kho_mac_dinh_id`, đánh dấu
      `stable`, không có `select *` trên `san_pham`, không có cột `don_gia`, và ≥ 3 grant execute.
    - Thân `dong_don` chứa `chi_tiet_don(p_id)` — kiểm quyền qua hàm kia, không chép lại.
    - `dong_don` KHÔNG trả cột tên kiểu `con_lai`/`so_luong_con_lai`
      (`grep -c "con_lai" supabase/migrations/0054_rpc_don_dat_hang.sql` trả `0`).
    - `supabase/tests/27_rpc_don_test.sql` mở `begin;` đóng `rollback;`, có `select plan(`,
      `select * from finish();`, và chứa cả bốn email tài khoản mẫu
      (`quanly@`, `vanphong@`, `thukho1@`, `chixem@`).
  </acceptance_criteria>
  <done>Migration 0054 và test 27 đã viết, chưa push.</done>
</task>

<task type="auto">
  <name>Task 2: Bảng đề nghị gộp mã và RPC gợi ý mã tên gần giống còn tồn</name>
  <files>supabase/migrations/0055_de_nghi_gop_ma.sql, supabase/tests/28_goi_y_ma_trung_test.sql</files>
  <read_first>
    - supabase/migrations/0033_ra_ghi_chu_lich_su.sql (khuôn bảng "ghi lại quyết định, không tự hành động": RLS chỉ SELECT theo vai trò, ghi chỉ qua RPC `SECURITY DEFINER`; và cách dùng `public.f_unaccent`)
    - supabase/migrations/0029_*.sql dòng 85–135 (bản `tim_san_pham` đang chạy — ngưỡng và cách so khớp phải dùng lại y hệt)
    - supabase/migrations/0022_tim_kiem_theo_chuoi_con.sql (giải thích hai nhánh ILIKE + `word_similarity`)
    - supabase/tests/22_kho_theo_dong_test.sql + supabase/tests/00_helper.sql.inc
  </read_first>
  <action>
Viết `supabase/migrations/0055_de_nghi_gop_ma.sql`:

**(a) Bảng `public.de_nghi_gop_ma`** — chỉ ghi lại đề nghị, không có tác dụng phụ nào:
`id uuid primary key default uuid_generate_v4()`, `san_pham_id_a uuid not null references
public.san_pham(id)`, `san_pham_id_b uuid not null references public.san_pham(id)`,
`nguoi_de_nghi_id uuid references public.nguoi_dung(id)`, `chung_tu_id uuid references
public.chung_tu(id)`, `trang_thai text not null default 'CHO_XU_LY' check (trang_thai in
('CHO_XU_LY','DA_XU_LY','TU_CHOI'))`, `ghi_chu text`,
`created_at timestamptz not null default now()`.
- `check (san_pham_id_a <> san_pham_id_b)`.
- Unique index chống ghi trùng đề nghị cho cùng một cặp còn chờ xử lý: index trên
  `(least(san_pham_id_a, san_pham_id_b), greatest(san_pham_id_a, san_pham_id_b))`
  `where trang_thai = 'CHO_XU_LY'`. Bấm nút hai lần không tạo hai dòng.
- `alter table ... enable row level security;`
- Policy `for select to authenticated using ((select public.vai_tro_hien_tai()) in
  ('quan_ly','van_phong'))`.
- `revoke insert, update, delete on public.de_nghi_gop_ma from anon, authenticated;`

**(b) `public.ghi_de_nghi_gop_ma(p_san_pham_id_a uuid, p_san_pham_id_b uuid,
p_chung_tu_id uuid default null, p_ghi_chu text default null)`**
returns `public.de_nghi_gop_ma`, `language plpgsql security definer set search_path = ''`.
Theo khuôn `quyet_ghi_chu` của 0033: chặn `chi_xem` → `42501`; chặn hai mã trùng nhau →
`23514`; `nguoi_de_nghi_id = auth.uid()`; `insert ... on conflict do nothing` rồi nếu
không có dòng mới thì `select` lại dòng đang chờ và trả về (bấm lại không lỗi, trả đúng
đề nghị đã có). `revoke`/`grant execute to authenticated`.

**(c) `public.goi_y_ma_trung(p_san_pham_id uuid, p_kho_id uuid, p_gioi_han int default 5)`**
returns table `(san_pham_id uuid, ma_hang text, ten_hang text, kho_id uuid, ten_kho text,
ton numeric, do_giong real)`, `language sql stable security definer set search_path = ''`.
- Lấy `ma_hang || ' ' || ten_hang` của `p_san_pham_id` làm chuỗi nguồn.
- Tìm các `san_pham` khác (`sp.id <> p_san_pham_id`, `sp.dang_kinh_doanh`) có
  `public.f_unaccent(nguồn) operator(extensions.<%) public.f_unaccent(sp.ma_hang || ' ' ||
  sp.ten_hang)` — dùng **đúng toán tử và đúng cách unaccent** của `tim_san_pham` bản 0029,
  không tự đặt ngưỡng mới.
- `join public.ton_kho tk on tk.san_pham_id = sp.id` và **chỉ trả mã đang còn tồn**
  (`tk.so_luong > 0`). Nếu `p_kho_id` khác null thì lọc đúng kho đó; null thì trả mọi kho.
- `order by extensions.word_similarity(...) desc, tk.so_luong desc limit p_gioi_han`.
- Liệt kê cột `san_pham` tường minh. Hàm là `SECURITY DEFINER` nhưng **không** trả bất kỳ
  cột giá vốn nào.
- `revoke`/`grant execute to authenticated`.

Viết `supabase/tests/28_goi_y_ma_trung_test.sql`. Assert bắt buộc:
- Dựng hai mã tên gần giống (ví dụ `GOP-TEST-A` "Nhông xích 428" và `GOP-TEST-B`
  "Nhong xich 428 loai 2"), ghi sổ một phiếu nhập cho mã B để nó có tồn ở K1.
- `goi_y_ma_trung(A, K1)` trả ít nhất 1 dòng và dòng đó là mã B với `ton > 0`.
- `goi_y_ma_trung(A, K2)` (kho B không có tồn) trả 0 dòng.
- `goi_y_ma_trung` không trả chính `p_san_pham_id`.
- `ghi_de_nghi_gop_ma(A, B)` tạo đúng 1 dòng `CHO_XU_LY`; gọi lại lần hai vẫn chỉ có 1 dòng.
- `ghi_de_nghi_gop_ma(A, A)` → `23514`.
- `chi_xem` gọi `ghi_de_nghi_gop_ma` → `42501`.
- `thu_kho` `select` trên `de_nghi_gop_ma` → 0 dòng (RLS lọc).
- **Assert không có tác dụng phụ:** sau khi gọi `ghi_de_nghi_gop_ma`, số dòng
  `public.kho_movement` và tồn của cả hai mã KHÔNG đổi.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0055_de_nghi_gop_ma.sql','utf8'); for (const t of ['public.de_nghi_gop_ma','public.ghi_de_nghi_gop_ma','public.goi_y_ma_trung','enable row level security','revoke insert, update, delete on public.de_nghi_gop_ma','word_similarity','f_unaccent']) { if(!s.includes(t)) throw new Error('thieu: '+t); } for (const bad of ['gia_von','update public.ton_kho','insert into public.kho_movement','delete from public.san_pham']) { if (s.includes(bad)) throw new Error('cam: '+bad); } console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh verify exit 0: có bảng + hai RPC, có bật RLS, có revoke ghi, dùng
      `word_similarity` + `f_unaccent`, và KHÔNG chứa `gia_von`, không ghi `ton_kho`,
      không ghi `kho_movement`, không xóa `san_pham`.
    - Bảng có unique index điều kiện `where trang_thai = 'CHO_XU_LY'`
      (`grep -c "CHO_XU_LY'" supabase/migrations/0055_de_nghi_gop_ma.sql` ≥ 2).
    - `goi_y_ma_trung` khai báo `stable` và có `tk.so_luong > 0`.
    - `supabase/tests/28_goi_y_ma_trung_test.sql` chứa `kho_movement` (assert không tác
      dụng phụ), `42501`, `23514`, mở `begin;` đóng `rollback;`.
  </acceptance_criteria>
  <done>Migration 0055 và test 28 đã viết, chưa push.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Đẩy migration, sinh lại kiểu, chạy toàn bộ pgTAP</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - supabase/README.md
    - supabase/migrations/0054_rpc_don_dat_hang.sql
    - supabase/migrations/0055_de_nghi_gop_ma.sql
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-02-SUMMARY.md
  </read_first>
  <action>
1. `npm run db:push`
2. `npm run db:types`
3. `npm run db:test:linked` — toàn bộ bộ test, đếm cả `ERROR` lẫn `not ok`.
4. Kiểm nhanh trên database thật (transaction + rollback): `select * from
   public.danh_sach_don(p_kich_thuoc => 5)` chạy được dưới cả bốn tài khoản mẫu và không
   trả cột nào tên `gia_von`.

Hai migration này chỉ THÊM đối tượng mới, không sửa đối tượng cũ — nếu có assert cũ đỏ
thì đó là dấu hiệu tên hàm đụng nhau, dừng lại điều tra thay vì đổi test.
  </action>
  <verify>
    <automated>npm run db:test:linked 2>&1 | tee /tmp/pgtap-04-03.log; grep -c "^not ok\|ERROR:" /tmp/pgtap-04-03.log</automated>
  </verify>
  <acceptance_criteria>
    - `npm run db:push`, `npm run db:types`, `npm run db:test:linked` đều exit 0.
    - Log pgTAP: 0 dòng `not ok`, 0 dòng `ERROR:`.
    - Tổng assert ≥ số cuối plan 04-02 cộng assert của file 27 và 28.
    - `grep -c "danh_sach_don\|goi_y_ma_trung\|ghi_de_nghi_gop_ma" src/types/database.types.ts` ≥ 3.
    - `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>0054 và 0055 trên database thật, kiểu đã sinh lại, toàn bộ pgTAP xanh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|---|---|
| trình duyệt → PostgREST | RPC đọc gọi thẳng được, tham số do client đặt |
| RPC `SECURITY DEFINER` → bảng | RLS bị bỏ qua bên trong, lọc phải viết tay trong hàm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-12 | Information Disclosure | `goi_y_ma_trung` là `SECURITY DEFINER` đọc `san_pham` + `ton_kho`, có thể lộ `gia_von` | mitigate | Kiểu trả về liệt kê tường minh 7 cột, không có cột giá nào; lệnh verify Task 2 chặn chuỗi `gia_von` trong file |
| T-04-13 | Information Disclosure | `danh_sach_don`/`chi_tiet_don` lộ đơn cho vai trò không nên thấy | accept | Quyết định có chủ đích: cả bốn vai trò đọc đơn được (policy `doc don dat hang using(true)` của 0016 đã vậy từ Phase 1; tờ phiếu đi lấy hàng vốn dành cho thủ kho). Đơn không chứa giá, không chứa giá vốn. Chặn thật nằm ở chiều ghi (plan 04-02) |
| T-04-14 | Tampering | `ghi_de_nghi_gop_ma` bị dùng để đổi dữ liệu mã hàng hoặc tồn | mitigate | Hàm chỉ `insert` vào `de_nghi_gop_ma`; lệnh verify chặn mọi chuỗi ghi vào `ton_kho`/`kho_movement`; pgTAP 28 assert tồn và số dòng `kho_movement` không đổi sau khi gọi |
| T-04-15 | Elevation of Privilege | `chi_xem` hoặc `anon` ghi vào `de_nghi_gop_ma` bằng `POST /de_nghi_gop_ma` | mitigate | `revoke insert, update, delete ... from anon, authenticated`; RPC chặn `chi_xem` → `42501`; pgTAP 28 assert |
| T-04-16 | Denial of Service | `p_kich_thuoc` khổng lồ kéo cả bảng đơn về | mitigate | Kẹp `p_kich_thuoc` trong hàm (`least(coalesce(p_kich_thuoc,50), 200)`) đúng cách 0045 đã làm |
| T-04-SC | Tampering | Cài gói npm/pip/cargo | accept | Plan này không cài gói nào |
</threat_model>

<verification>
- `npm run db:push` · `npm run db:types` · `npm run db:test:linked` exit 0
- pgTAP 0 `not ok`, 0 `ERROR:`
- Thử tay: `danh_sach_don(p_kich_thuoc => 5)` chạy dưới cả bốn tài khoản mẫu
- `npm run typecheck` exit 0
</verification>

<success_criteria>
- `danh_sach_don` lọc + phân trang ở server và trả `tong_so_dong` kèm mọi dòng
- `chi_tiet_don` + `dong_don` trả đủ dữ liệu cho màn chi tiết, `dong_don` không có cột giá
  và không có cột "còn lại"
- `goi_y_ma_trung` chỉ trả mã tên gần giống **đang còn tồn**, không trả chính mã đang xét
- `ghi_de_nghi_gop_ma` ghi đúng một dòng cho mỗi cặp đang chờ, không đụng tồn hay sổ cái
</success_criteria>

<output>
Create `.planning/phases/04-don-dat-hang-phieu-xuat/04-03-SUMMARY.md` when done.
Ghi lại: chữ ký đầy đủ của năm RPC mới (tên tham số + cột trả về) để tầng client khỏi đoán,
và số assert trước/sau.
</output>
</content>
