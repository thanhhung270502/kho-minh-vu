---
phase: 04-don-dat-hang-phieu-xuat
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - supabase/migrations/0052_rpc_duyet_don.sql
  - supabase/migrations/0053_sinh_so_dh.sql
  - supabase/tests/25_duyet_don_test.sql
  - supabase/tests/26_so_dh_test.sql
  - src/types/database.types.ts
autonomous: true
requirements: [DDH-01, DDH-03]

must_haves:
  truths:
    - "Chỉ quản lý xác nhận được đơn, mở khóa được đơn và đóng sớm được đơn"
    - "Văn phòng sửa được đơn khi đơn còn TAM, không sửa được khi đơn đã DA_XAC_NHAN"
    - "Mỗi lần đổi trạng thái đơn để lại một dòng trong nhat_ky_sua"
    - "Hai người tạo đơn cùng lúc nhận hai số đơn khác nhau"
  artifacts:
    - path: "supabase/migrations/0052_rpc_duyet_don.sql"
      provides: "xac_nhan_don / mo_khoa_don / dong_don_som + policy sửa đơn khi TAM + trigger nhật ký"
      contains: "xac_nhan_don"
    - path: "supabase/migrations/0053_sinh_so_dh.sql"
      provides: "Bảng đếm chuoi_so_dh + hàm sinh_so_dh cấp số không trùng"
      contains: "on conflict"
    - path: "supabase/tests/25_duyet_don_test.sql"
      provides: "pgTAP cho ba RPC duyệt và policy sửa đơn"
      contains: "42501"
    - path: "supabase/tests/26_so_dh_test.sql"
      provides: "pgTAP cho bộ đếm số đơn"
      contains: "sinh_so_dh"
  key_links:
    - from: "public.xac_nhan_don"
      to: "public.vai_tro_hien_tai"
      via: "kiểm vai trò tường minh vì SECURITY DEFINER bỏ qua RLS"
      pattern: "vai_tro_hien_tai"
    - from: "public.don_dat_hang"
      to: "public.nhat_ky_sua"
      via: "trigger ghi_nhat_ky_sua after insert or update"
      pattern: "ghi_nhat_ky_sua"
---

<objective>
Dựng bước duyệt đơn ở tầng database và bộ cấp số đơn.

D-06: văn phòng tạo và sửa đơn tạm, **chỉ quản lý** bấm xác nhận. D-07: đơn đã xác nhận
mà cần sửa thì **chỉ quản lý mở khóa** về `TAM`, mỗi lần mở khóa ghi nhật ký. D-05: quản
lý **đóng sớm** được đơn khi khách không lấy nốt phần còn lại.

`don_dat_hang.so_dh` là `text not null unique` nhưng chưa có bộ sinh. `sinh_so_ct` /
`chuoi_so_ct` khóa theo enum `loai_ct` (bảy giá trị chứng từ) nên **không dùng lại nguyên
trạng được** — đơn đặt hàng không phải một `loai_ct`, và thêm giá trị enum giả sẽ làm ô
nhiễm mọi chỗ đang phân nhánh theo bảy loại chứng từ. Chép **kỹ thuật** atomic, không
chép bảng.

Purpose: mọi màn hình đơn ở Wave 6–9 gọi ba RPC này; chặn thật nằm ở database, giao diện
chỉ ẩn/hiện nút.
Output: hai migration (0052, 0053), hai file pgTAP (25, 26).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-CONTEXT.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-01-SUMMARY.md

**Quy ước kiểm vai trò trong RPC (bài học Phase 3, lỗi số 4 của `03-SUMMARY.md`):**
chỉ chặn `chi_xem`, **để null đi qua**. Chặn cả ngữ cảnh không có JWT sẽ làm đỏ
migration, script nạp dữ liệu và pgTAP chạy dưới `postgres`. Với ba RPC duyệt đơn thì
điều kiện là *phải đúng* `quan_ly`, nên viết dạng
`if coalesce((select public.vai_tro_hien_tai())::text, 'quan_ly') <> 'quan_ly' then ...`
— ngữ cảnh không JWT được coi như quản lý, đúng tinh thần quy ước trên.

**Trạng thái đo ngày 20/09:** `don_dat_hang` 0 dòng, `doi_tac` 24 dòng (23 NCC + 1 Khách lẻ).
</context>

<tasks>

<task type="auto">
  <name>Task 1: RPC duyệt đơn, policy sửa đơn khi TAM, trigger nhật ký</name>
  <files>supabase/migrations/0052_rpc_duyet_don.sql, supabase/tests/25_duyet_don_test.sql</files>
  <read_first>
    - supabase/migrations/0046_chi_quan_ly_huy_nhap.sql (khuôn RPC `SECURITY DEFINER` tự kiểm vai trò, `for update`, `raise ... using errcode = '42501'`)
    - supabase/migrations/0016_rls_chung_tu.sql dòng 45–110 (policy `doc/tao/sua don dat hang` và `don_dat_hang_dong` đang chạy — LƯU Ý: hiện KHÔNG có policy `for delete` trên `don_dat_hang_dong`)
    - supabase/migrations/0027_nhat_ky_sua.sql dòng 56–95 (hàm `public.ghi_nhat_ky_sua()` và cách các bảng gắn trigger — `don_dat_hang` hiện CHƯA gắn)
    - supabase/migrations/0028_cau_hinh_so_ct.sql dòng 50–57 (khuôn policy `for update using(...) with check(...)` theo vai trò)
    - supabase/migrations/0026_nguoi_dung_nhieu_kho.sql dòng 71–85 (`vai_tro_hien_tai()` bản đang chạy)
    - supabase/tests/22_kho_theo_dong_test.sql + supabase/tests/00_helper.sql.inc
  </read_first>
  <action>
Viết `supabase/migrations/0052_rpc_duyet_don.sql` gồm bốn khối:

**(a) Ba RPC.** Cùng khuôn `0046`: `returns public.don_dat_hang`,
`language plpgsql security definer set search_path = ''`, kiểm vai trò TRƯỚC, rồi
`select * into v_don from public.don_dat_hang where id = p_id for update`, kiểm tồn tại
(`23514`), kiểm trạng thái nguồn (`23514`), rồi `update ... returning * into v_don`.

| Hàm | Trạng thái nguồn | Trạng thái đích | Ghi chú |
|---|---|---|---|
| `public.xac_nhan_don(p_id uuid)` | `TAM` | `DA_XAC_NHAN` | D-06 |
| `public.mo_khoa_don(p_id uuid, p_ly_do text)` | `DA_XAC_NHAN` | `TAM` | D-07, bắt buộc lý do ≥ 5 ký tự, lỗi `23514` nếu ngắn hơn |
| `public.dong_don_som(p_id uuid, p_ly_do text)` | `DA_XAC_NHAN` | `HOAN_THANH` | D-05, đóng bất kể `so_luong_da_xuat`, bắt buộc lý do ≥ 5 ký tự |

`mo_khoa_don` và `dong_don_som` ghi lý do vào `don_dat_hang.ghi_chu` bằng cách nối thêm
một dòng có tiền tố (`'[mở khóa] '` / `'[đóng sớm] '`) — cột `ghi_chu` đã có sẵn, không
thêm cột mới. Thông báo lỗi soạn sẵn bằng tiếng Việt vì giao diện hiện nguyên văn message
khi mã lỗi là `23514` (khuôn `post-receipt-button.tsx`).

Cả ba kèm `revoke all on function ... from public, anon;` +
`grant execute on function ... to authenticated;`.

**(b) Khóa sửa đơn đã duyệt ở tầng RLS.** Policy hiện tại `"sua don dat hang"` cho phép
mọi vai trò trừ `chi_xem` sửa đơn ở **bất kỳ** trạng thái — trái D-07. Lấy định nghĩa
ĐANG CHẠY từ `pg_policies` (không chép từ file 0016 — bài học Phase 3 lỗi số 2), rồi
`drop policy` và tạo lại:
- `"sua don dat hang"` → `using (trang_thai = 'TAM' and (select public.vai_tro_hien_tai()) <> 'chi_xem')`,
  `with check ((select public.vai_tro_hien_tai()) <> 'chi_xem')`.
- `"sua dong don dat hang"` → thêm điều kiện `exists (select 1 from public.don_dat_hang d
  where d.id = don_dat_hang_id and d.trang_thai = 'TAM')`.
- Tạo mới `"xoa dong don dat hang khi tam"` cho `for delete` — hiện **chưa có policy
  delete nào** trên `don_dat_hang_dong` nên xóa một dòng đơn đang bất khả thi. Cùng điều
  kiện như trên.
- Policy `"tao dong don dat hang tru chi xem"` (`for insert`) cũng phải thêm điều kiện cha
  đang `TAM`, nếu không thêm được dòng vào đơn đã duyệt.

Ba RPC ở mục (a) là `SECURITY DEFINER` nên không bị các policy này chặn — đúng ý đồ: đổi
trạng thái chỉ đi qua RPC có kiểm vai trò.

**(c) Trigger nhật ký (D-07).** `create trigger ghi_nhat_ky_don_dat_hang after insert or
update on public.don_dat_hang for each row execute function public.ghi_nhat_ky_sua();`
Dùng đúng hàm generic đã có từ 0027, không viết hàm mới. Hàm đó tự bỏ qua `updated_at`,
`created_at` nên chỉ đổi trạng thái/ghi chú mới sinh dòng nhật ký.

**(d) Chống đệ quy.** Policy mới của `don_dat_hang_dong` hỏi `don_dat_hang`, nhưng policy
`"doc dong don dat hang"` cũng đã hỏi `don_dat_hang` — kiểm rằng policy của `don_dat_hang`
KHÔNG hỏi ngược lại `don_dat_hang_dong`. Nếu có thì cắt vòng bằng hàm `SECURITY DEFINER`
theo đúng cách 0042 đã làm cho `chung_tu`/`chung_tu_dong`. Ghi kết luận kiểm tra vào
comment đầu file.

Viết `supabase/tests/25_duyet_don_test.sql`, khuôn `22_kho_theo_dong_test.sql`. Assert:
- `van_phong` gọi `xac_nhan_don` → `42501` (`throws_ok` so mã lỗi).
- `quan_ly` gọi `xac_nhan_don` trên đơn `TAM` → trạng thái thành `DA_XAC_NHAN`.
- `quan_ly` gọi `xac_nhan_don` lần hai trên cùng đơn → `23514`.
- `van_phong` `update public.don_dat_hang set ghi_chu = ...` trên đơn `DA_XAC_NHAN` →
  0 dòng bị sửa (RLS lọc, không ném lỗi) — assert bằng `is((select count(*) ...), 0)` hoặc
  so `ghi_chu` không đổi.
- `van_phong` sửa được chính đơn đó sau khi `quan_ly` gọi `mo_khoa_don`.
- `mo_khoa_don` với lý do 3 ký tự → `23514`.
- `dong_don_som` trên đơn `DA_XAC_NHAN` chưa giao đủ → `HOAN_THANH`.
- Sau `xac_nhan_don`, `public.nhat_ky_sua` có ít nhất một dòng `bang = 'don_dat_hang'`,
  `truong = 'trang_thai'`. **Không** dùng `order by cot_thoi_gian desc` để tìm dòng mới
  nhất — trong một transaction `now()` không đổi (quyết định đã ghi ở STATE.md Phase 02);
  kiểm theo nội dung cụ thể.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0052_rpc_duyet_don.sql','utf8'); for (const t of ['public.xac_nhan_don','public.mo_khoa_don','public.dong_don_som','42501','security definer','drop policy','ghi_nhat_ky_don_dat_hang','for delete']) { if (!s.includes(t)) throw new Error('thieu: '+t); } const n=(s.match(/grant execute on function/g)||[]).length; if (n<3) throw new Error('thieu grant execute, dem duoc '+n); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh verify exit 0: file có đủ ba hàm, mã lỗi `42501`, `security definer`, có
      `drop policy`, có trigger `ghi_nhat_ky_don_dat_hang`, có policy `for delete`, và
      ≥ 3 lệnh `grant execute on function`.
    - Mỗi hàm trong ba hàm có đúng một lệnh `for update` trên `public.don_dat_hang`
      (`grep -c "for update" supabase/migrations/0052_rpc_duyet_don.sql` ≥ 3).
    - File không chứa `create or replace function public.ghi_nhat_ky_sua` — phải dùng lại
      hàm generic của 0027, không viết lại.
    - `supabase/tests/25_duyet_don_test.sql` chứa `throws_ok`, `42501`, `23514` và
      `nhat_ky_sua`; mở `begin;` đóng `rollback;`.
    - Test không dùng `order by` trên cột thời gian để chọn dòng nhật ký.
  </acceptance_criteria>
  <done>Migration 0052 và test 25 đã viết, chưa push.</done>
</task>

<task type="auto">
  <name>Task 2: Bộ cấp số đơn không trùng khi hai người tạo cùng lúc</name>
  <files>supabase/migrations/0053_sinh_so_dh.sql, supabase/tests/26_so_dh_test.sql</files>
  <read_first>
    - supabase/migrations/0048_sinh_so_ct_cho_phep_script.sql (kỹ thuật `insert ... on conflict do update ... returning` và quy ước "chỉ chặn chi_xem, để null đi qua")
    - supabase/migrations/0047_sinh_so_ct_definer.sql (vì sao bộ cấp số bắt buộc `SECURITY DEFINER`)
    - supabase/migrations/0028_cau_hinh_so_ct.sql (bảng `chuoi_so_ct` — khóa theo `loai_ct`, lý do không dùng lại được)
    - supabase/tests/80_cau_hinh_so_ct_test.sql (khuôn test bộ đếm, và cách tránh neo vào bộ đếm sống)
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md mục "WU-3"
  </read_first>
  <action>
Viết `supabase/migrations/0053_sinh_so_dh.sql`:

1. Bảng `public.chuoi_so_dh (nam smallint primary key, so_hien_tai integer not null default 0)`.
   `alter table ... enable row level security;` và **không tạo policy ghi nào** — client
   chỉ chạm qua RPC `SECURITY DEFINER`. Thêm policy `for select to authenticated using
   ((select public.vai_tro_hien_tai()) is not null)` nếu muốn màn Cài đặt xem được; nếu
   không cần thì bỏ hẳn policy select và ghi comment nói rõ.
   `revoke insert, update, delete on public.chuoi_so_dh from anon, authenticated;`
2. Hàm `public.sinh_so_dh(p_nam smallint default null) returns text`,
   `language plpgsql security definer set search_path = ''`:
   - `v_nam := coalesce(p_nam, extract(year from current_date)::smallint)`
   - Chặn đúng một vai trò: `if (select public.vai_tro_hien_tai()) = 'chi_xem' then raise
     exception 'Vai trò chỉ xem không được cấp số đơn' using errcode = '42501'; end if;`
     — **để null đi qua**, đúng quy ước 0048.
   - Cấp số bằng MỘT câu lệnh, không đọc-rồi-ghi:
     `insert into public.chuoi_so_dh (nam, so_hien_tai) values (v_nam, 1) on conflict (nam)
     do update set so_hien_tai = public.chuoi_so_dh.so_hien_tai + 1 returning so_hien_tai
     into v_so;`
   - Trả `format('DH%s-%s', to_char(v_nam % 100, 'FM00'), lpad(v_so::text, 6, '0'))` →
     dạng `DH26-000001`, cùng hình dạng với số chứng từ hiện có.
   - `revoke all ... from public, anon;` + `grant execute ... to authenticated;`
   - `comment on function` nói rõ: không dùng `chuoi_so_ct` vì bảng đó khóa theo enum
     `loai_ct` và đơn đặt hàng không phải một `loai_ct`.

Viết `supabase/tests/26_so_dh_test.sql`. **Bẫy 16 — cấm neo vào bộ đếm sống.** Dùng năm
2091–2093 (`sinh_so_dh(2091::smallint)`) hoặc so tương đối. Assert bắt buộc:
- `sinh_so_dh(2091)` gọi hai lần liên tiếp trả hai chuỗi **khác nhau**, và số phần đuôi
  lần sau **lớn hơn** lần trước (tách bằng `split_part(..., '-', 2)::int`).
- Chuỗi trả về khớp biểu thức `^DH91-\d{6}$` (`matches`).
- Với năm hiện tại: gọi `sinh_so_dh()` hai lần, assert hai kết quả khác nhau — **không**
  assert giá trị cụ thể.
- `chi_xem` gọi `sinh_so_dh` → `42501` (`throws_ok`).
- Ngữ cảnh không có JWT (`pg_temp.dang_xuat()`) gọi `sinh_so_dh` → **không** ném lỗi.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0053_sinh_so_dh.sql','utf8'); if(!/on conflict \(nam\)\s*\n?\s*do update set/i.test(s.replace(/\s+/g,' ').replace(/on conflict \(nam\) do update set/i,'on conflict (nam) do update set'))) throw new Error('thieu ky thuat atomic'); for (const t of ['public.chuoi_so_dh','public.sinh_so_dh','security definer','returning so_hien_tai','grant execute on function public.sinh_so_dh']) { if(!s.includes(t)) throw new Error('thieu: '+t); } const t=require('fs').readFileSync('supabase/tests/26_so_dh_test.sql','utf8'); if(/DH2[0-9]-0{5}1/.test(t)) throw new Error('test dang neo vao bo dem song'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh verify exit 0: migration có bảng `chuoi_so_dh`, hàm `sinh_so_dh`,
      `security definer`, một câu `on conflict (nam) do update set ... returning so_hien_tai`,
      và grant execute; file test KHÔNG chứa số đơn cố định dạng `DH26-000001`.
    - `grep -c "select .* into .* from public.chuoi_so_dh" supabase/migrations/0053_sinh_so_dh.sql`
      trả `0` — không có bước đọc riêng trước khi ghi (đó chính là khoảng hở gây trùng số).
    - Migration có `alter table public.chuoi_so_dh enable row level security;` và
      `revoke insert, update, delete on public.chuoi_so_dh from anon, authenticated;`.
    - File test chứa `2091` (hoặc 2092/2093) và `throws_ok` với `42501`.
  </acceptance_criteria>
  <done>Migration 0053 và test 26 đã viết, chưa push.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Đẩy migration, sinh lại kiểu, chạy toàn bộ pgTAP</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - supabase/README.md
    - supabase/migrations/0052_rpc_duyet_don.sql
    - supabase/migrations/0053_sinh_so_dh.sql
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-01-SUMMARY.md (số assert sau plan 01)
  </read_first>
  <action>
1. `npm run db:push`
2. `npm run db:types`
3. `npm run db:test:linked` — **toàn bộ** bộ test. 0052 thay bốn policy trên
   `don_dat_hang`/`don_dat_hang_dong`; test 20 và 30 có thể đang khẳng định hành vi cũ
   (sửa đơn ở mọi trạng thái).
4. Đếm cả dòng `ERROR` lẫn `not ok`.

Nếu một assert cũ đỏ vì policy mới siết chặt: sửa assert cho khớp D-07 và ghi lại lý do
vào SUMMARY. Không nới policy để test cũ xanh.

Kiểm nhanh trên database thật sau khi push (chạy trong một transaction rồi rollback):
đăng nhập như `vanphong`, thử `update public.don_dat_hang set ghi_chu='x' where trang_thai='DA_XAC_NHAN'`
→ phải 0 dòng.
  </action>
  <verify>
    <automated>npm run db:test:linked 2>&1 | tee /tmp/pgtap-04-02.log; grep -c "^not ok\|ERROR:" /tmp/pgtap-04-02.log</automated>
  </verify>
  <acceptance_criteria>
    - `npm run db:push` exit 0, `npm run db:types` exit 0, `npm run db:test:linked` exit 0.
    - Log pgTAP không có dòng `not ok` và không có dòng chứa `ERROR:`.
    - Tổng assert ≥ số cuối của plan 04-01, cộng thêm assert của file 25 và 26.
    - `src/types/database.types.ts` có `sinh_so_dh`, `xac_nhan_don`, `mo_khoa_don`,
      `dong_don_som` trong khối `Functions` (`grep -c "xac_nhan_don" src/types/database.types.ts` ≥ 1).
    - `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>0052 và 0053 đã nằm trên database thật, kiểu đã sinh lại, toàn bộ pgTAP xanh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|---|---|
| trình duyệt → PostgREST | Ai có token `authenticated` gọi `rpc/xac_nhan_don` thẳng được, không qua nút bấm |
| RPC `SECURITY DEFINER` → bảng | RLS không áp dụng bên trong, phải kiểm vai trò tường minh |
| client → `don_dat_hang` qua PostgREST | Sửa đơn bằng `PATCH /don_dat_hang?id=eq.…`, bỏ qua mọi kiểm tra ở giao diện |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-05 | Elevation of Privilege | `van_phong`/`thu_kho` gọi `xac_nhan_don` qua PostgREST | mitigate | Kiểm `vai_tro_hien_tai() <> 'quan_ly'` ngay dòng đầu hàm → `42501`; pgTAP 25 assert |
| T-04-06 | Elevation of Privilege | `van_phong` gọi `mo_khoa_don`/`dong_don_som` để né bước duyệt | mitigate | Cùng khối kiểm vai trò; pgTAP 25 assert cả ba hàm |
| T-04-07 | Tampering | Sửa dòng đơn đã `DA_XAC_NHAN` bằng `PATCH` thẳng lên `don_dat_hang_dong` | mitigate | Policy `for update`/`for insert`/`for delete` trên `don_dat_hang_dong` đều đòi cha `TAM`; pgTAP 25 assert 0 dòng bị sửa |
| T-04-08 | Repudiation | Mở khóa đơn rồi sửa số, không ai biết ai làm | mitigate | Trigger `ghi_nhat_ky_sua` trên `don_dat_hang` ghi `nguoi_sua_id = auth.uid()`; `mo_khoa_don` bắt buộc lý do ≥ 5 ký tự; pgTAP 25 assert có dòng nhật ký |
| T-04-09 | Tampering | Hai người tạo đơn đồng thời nhận cùng `so_dh` → một đơn ghi đè đơn kia | mitigate | `insert ... on conflict do update ... returning` trong một statement + `so_dh` unique ở 0006; pgTAP 26 assert hai lần gọi trả hai số |
| T-04-10 | Elevation of Privilege | `chi_xem` cấp số đơn để ép tạo bản ghi | mitigate | `sinh_so_dh` chặn `chi_xem` → `42501`; pgTAP 26 assert |
| T-04-11 | Denial of Service | Policy `don_dat_hang` ↔ `don_dat_hang_dong` hỏi vòng nhau → đệ quy vô hạn, mọi truy vấn đơn chết | mitigate | Task 1 mục (d) bắt buộc kiểm chiều hỏi; nếu có vòng thì cắt bằng hàm `SECURITY DEFINER` theo cách 0042 |
| T-04-SC | Tampering | Cài gói npm/pip/cargo | accept | Plan này không cài gói nào |
</threat_model>

<verification>
- `npm run db:push` · `npm run db:types` · `npm run db:test:linked` exit 0
- pgTAP 0 `not ok`, 0 `ERROR:`, tổng assert tăng so với plan 04-01
- Thử tay trên database (transaction + rollback): `vanphong` sửa đơn `DA_XAC_NHAN` → 0 dòng;
  `vanphong` gọi `xac_nhan_don` → 42501
- `npm run typecheck` exit 0
</verification>

<success_criteria>
- Ba RPC `xac_nhan_don` / `mo_khoa_don` / `dong_don_som` tồn tại, chỉ `quan_ly` gọi được
- Đơn `DA_XAC_NHAN` không sửa được bằng bất kỳ đường nào ngoài `mo_khoa_don`
- `don_dat_hang_dong` có đủ policy insert/update/delete, tất cả đòi đơn cha đang `TAM`
- Đổi trạng thái đơn để lại dòng trong `nhat_ky_sua`
- `sinh_so_dh` cấp số dạng `DH{YY}-{6 chữ số}`, không trùng khi gọi đồng thời
</success_criteria>

<output>
Create `.planning/phases/04-don-dat-hang-phieu-xuat/04-02-SUMMARY.md` when done.
Ghi lại: định nghĩa policy cũ lấy từ `pg_policies` (nguyên văn, để phase sau đối chiếu),
số assert trước/sau, và kết luận kiểm chiều hỏi giữa hai policy đơn.
</output>
</content>
