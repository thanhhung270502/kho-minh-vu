---
phase: 04-don-dat-hang-phieu-xuat
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - src/features/documents/types.ts
  - src/features/documents/lib/url-filter.ts
  - src/features/documents/schemas/document.schema.ts
  - src/features/documents/api/document.api.ts
  - src/features/documents/api/document.keys.ts
  - src/features/stock-in/types.ts
  - src/features/stock-in/api/receipt.api.ts
  - src/features/stock-in/api/receipt.keys.ts
  - src/features/stock-in/schemas/receipt.schema.ts
  - src/features/stock-in/hooks/useReceipts.ts
  - src/shared/components/product-search-input.tsx
  - src/features/stock-in/components/product-search-input.tsx
  - src/features/stock-in/components/receipt-line-table.tsx
autonomous: false
requirements: [XUAT-01]

must_haves:
  truths:
    - "Kiểu và mapper của chứng từ dùng chung nằm ở features/documents, không còn bản sao ở stock-in"
    - "Màn phiếu nhập chạy y như trước sau khi rút lớp dùng chung"
    - "Kiểu chứng từ thấy được các cột mới của plan 04-01: đơn gốc, chứng từ gốc, lý do xuất âm, tồn hiện tại"
    - "Ô tìm mã hàng nằm ở src/shared/components, mọi feature import từ đó"
  artifacts:
    - path: "src/features/documents/types.ts"
      provides: "DocumentRow/DocumentDetail/DocumentLine + mapper + nhãn trạng thái dùng chung"
      exports: ["DocumentRow", "DocumentDetail", "DocumentLine", "toDocumentRow", "toDocumentDetail", "toDocumentLine", "DOC_STATUS_LABELS", "DOC_STATUS_COLORS"]
      min_lines: 120
    - path: "src/features/documents/api/document.api.ts"
      provides: "fetchDocuments / fetchDocumentDetail / fetchDocumentLines + CRUD dòng + ghi sổ + hủy"
      exports: ["fetchDocuments", "fetchDocumentDetail", "fetchDocumentLines", "postDocument", "voidDocument"]
    - path: "src/features/documents/api/document.keys.ts"
      provides: "documentKeys — query key dùng chung, tách nhánh theo loại chứng từ"
      exports: ["documentKeys"]
    - path: "src/shared/components/product-search-input.tsx"
      provides: "Ô tìm mã hàng dùng chung — đã ưu tiên mã khớp tuyệt đối (bẫy 15)"
      exports: ["ProductSearchInput"]
  key_links:
    - from: "src/features/stock-in/types.ts"
      to: "@/features/documents/types"
      via: "re-export, không định nghĩa lại"
      pattern: "features/documents"
    - from: "src/features/stock-in/hooks/useReceipts.ts"
      to: "@/features/documents/api/document.api"
      via: "hook gọi hàm dùng chung"
      pattern: "features/documents"
---

<objective>
Rút lớp chứng từ dùng chung từ `src/features/stock-in/` lên `src/features/documents/`.

CLAUDE.md nói chỉ nâng lên dùng chung khi có **ít nhất 2** feature dùng thật. Phase 4 thêm
`stock-out` và `returns` — điều kiện đã đủ. `src/features/stock-in/types.ts` **đã đặt tên
tổng quát `Document*`** ngay từ Phase 3, nên đây là refactor "dời nhà", không phải viết mới.

⚠️ **Plan này cố ý sửa file ngoài phạm vi feature đang làm** (`src/features/stock-in/`).
CLAUDE.md yêu cầu nêu lý do trước: điều kiện "2 feature dùng thật" đã thỏa, và nhân bản
lần thứ hai sẽ tạo hai bản sao lệch nhau.

⚠️ Plan này cũng trả **nợ UAT trình duyệt của Phase 3** (`03-SUMMARY.md` mục "Chưa làm"):
màn phiếu nhập chưa ai mở bằng mắt. Phase 4 nhân bản đúng cơ chế đó, nên lỗi giao diện
chưa phát hiện sẽ bị nhân đôi. Task 3 là checkpoint mở màn phiếu nhập kiểm mắt.

Purpose: `stock-out` và `returns` mỏng trên `documents`, không copy-paste.
Output: feature `documents`; `stock-in` gọi lại từ đó; màn phiếu nhập không đổi hành vi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-01-SUMMARY.md
@.planning/phases/03-phieu-nhap/03-SUMMARY.md

**Ranh giới đặt tên (CLAUDE.md):** tên file/thư mục/hàm/type bằng **tiếng Anh**; lớp
`api/` + `types.ts` là chỗ DUY NHẤT chạm tên cột tiếng Việt. Component và hook không bao
giờ thấy `ma_hang`, `so_luong`, `doi_tac_id`.

**Cái gì đi, cái gì ở lại:**

| Chuyển sang `documents` (dùng chung mọi loại chứng từ) | Ở lại `stock-in` (chỉ NHẬP có) |
|---|---|
| `DocStatus`, `DocumentRow/Detail/Line`, ba hàm `toDocument*` | `ReceiptSource`, `RECEIPT_SOURCE_LABELS`, `RECEIPT_SOURCE_COLORS` |
| `DOC_STATUS_LABELS`, `DOC_STATUS_COLORS` | `createReceipt` (cấp số theo nguồn nhà máy) |
| `documentHeaderSchema`, `documentLineSchema`, `toDocumentUpdate`, `toDocumentLineUpdate` | `ReceiptFilter` + bộ lọc URL của riêng màn nhập |
| `fetchDocuments/Detail/Lines`, `addDocumentLine`, `updateDocumentLine`, `deleteDocumentLine`, `updateDocumentHeader`, `postDocument`, `voidDocument` | `ReceiptPermissions` |
| Hai hàm trợ giúp `readUuid` / `readDate` của bộ lọc URL | — |

**Một thứ nữa phải nâng lên, vì lý do khác:** `ProductSearchInput` hiện nằm ở
`src/features/stock-in/components/product-search-input.tsx`. Bảng dòng của đơn (plan 04-09),
của phiếu xuất (04-11) và của phiếu trả (04-14) đều cần nó. CLAUDE.md **cấm feature import
trực tiếp từ thư mục nội bộ của feature khác**, nên nó phải lên `src/shared/components/`.
Nâng ở plan này để ba plan sau chỉ việc import, không phải chờ nhau.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dựng features/documents — kiểu, mapper, schema, query key</name>
  <files>src/features/documents/types.ts, src/features/documents/lib/url-filter.ts, src/features/documents/schemas/document.schema.ts, src/features/documents/api/document.keys.ts</files>
  <read_first>
    - src/features/stock-in/types.ts (toàn bộ — nguồn để chép)
    - src/features/stock-in/schemas/receipt.schema.ts (toàn bộ)
    - src/features/stock-in/api/receipt.keys.ts
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-01-SUMMARY.md (danh sách cột mới mà `chi_tiet_chung_tu` và `dong_chung_tu` trả về)
    - src/types/database.types.ts — chỉ phần `Functions.chi_tiet_chung_tu.Returns` và `Functions.dong_chung_tu.Returns`; dùng Grep tìm dòng rồi Read đúng đoạn, đừng đọc cả file
  </read_first>
  <action>
Tạo `src/features/documents/types.ts`: chép nguyên khối `DocStatus`, `DocumentRow`,
`DocumentDetail`, `DocumentLine`, `DOC_STATUS_LABELS`, `DOC_STATUS_COLORS`,
`toDocumentRow`, `toDocumentDetail`, `toDocumentLine` từ `stock-in/types.ts`.

Bổ sung field ánh xạ các cột mà plan 04-01 vừa thêm vào hai RPC:
- `DocumentDetail`: `orderId` (`don_dat_hang_id`), `orderNo` (`so_dh`), `sourceDocId`
  (`chung_tu_goc_id`), `sourceDocNo` (`so_ct_goc`), `negativeReason` (`ly_do_xuat_am`),
  `negativeReasonNote` (`ghi_chu_ly_do`), `approvedById` (`nguoi_duyet_id`) — đều
  `string | null`.
- `DocumentLine`: `currentStock: number` (`ton_hien_tai`). Map bằng `Number(...)` — cột
  `numeric` về client dưới dạng chuỗi.

`ReceiptSource` và hai bảng nhãn nguồn nhập **không** chuyển sang. `DocumentRow.source`
vẫn cần kiểu enum `nguon_nhap`: khai lại trong `documents` dưới tên trung tính
`DocumentSource`, rồi `stock-in` re-export nó dưới tên cũ `ReceiptSource`.

Tạo `src/features/documents/lib/url-filter.ts` (file thuần, **không** `"use client"` —
bẫy 9): chuyển hai hàm trợ giúp `readUuid` và `readDate` từ `receipt.schema.ts` sang, kèm
hằng `DATE_PATTERN`. Cả màn nhập lẫn màn xuất đều dùng.

Tạo `src/features/documents/schemas/document.schema.ts`: chép `documentHeaderSchema`,
`documentLineSchema`, `DocumentHeaderInput`, `DocumentLineInput`, `toDocumentUpdate`,
`toDocumentLineUpdate`. **Không** chép `ReceiptFilter`, `DEFAULT_RECEIPT_FILTER`,
`readReceiptFilterFromUrl`, `writeReceiptFilterToUrl`, `toReceiptListRpcArgs`,
`countActiveReceiptFilters` — bộ lọc là chuyện riêng từng màn.

Tạo `src/shared/components/product-search-input.tsx`: chuyển nguyên văn
`src/features/stock-in/components/product-search-input.tsx` sang, **không sửa một dòng
logic nào** — đặc biệt giữ nguyên khối ưu tiên mã khớp tuyệt đối trước khi lấy kết quả đầu
tiên (bẫy 15: chọn nhầm mã ở màn nhập kho là nhập sai hàng vào sổ). Xóa file cũ. Sửa import
trong `src/features/stock-in/components/receipt-line-table.tsx` trỏ sang đường dẫn mới.
Component này gọi `searchProducts` ở `src/features/stock-in/api/product-search.api.ts` —
chuyển luôn file đó sang `src/shared/api/product-search.api.ts` để không còn import ngược
từ `shared` vào `features`.

Tạo `src/features/documents/api/document.keys.ts`: `documentKeys` cùng hình dạng
`receiptKeys` nhưng thêm nhánh phạm vi để hai màn không đụng cache nhau —
`all`, `list(scope, filter)`, `detail(id)`, `lines(id)`, với `scope` kiểu
`"nhap" | "xuat" | "tra"`.

Task này chỉ THÊM file, không xóa gì ở `stock-in`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Bốn file mới tồn tại đúng đường dẫn trong `files_modified`.
    - `grep -l "use client" src/features/documents/types.ts src/features/documents/lib/url-filter.ts`
      không in ra file nào — hai file này phải thuần để Server Component import được.
    - `src/features/documents/types.ts` export đủ tám tên: `DocumentRow`, `DocumentDetail`,
      `DocumentLine`, `toDocumentRow`, `toDocumentDetail`, `toDocumentLine`,
      `DOC_STATUS_LABELS`, `DOC_STATUS_COLORS`.
    - `grep -c RECEIPT_SOURCE src/features/documents/types.ts` trả `0`.
    - `DocumentLine` có `currentStock`; `DocumentDetail` có `orderNo`, `sourceDocNo`,
      `negativeReason`.
    - `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>Feature `documents` có kiểu, mapper, schema, query key; chưa ai gọi.</done>
</task>

<task type="auto">
  <name>Task 2: Chuyển lớp gọi dữ liệu sang documents và nối lại stock-in</name>
  <files>src/features/documents/api/document.api.ts, src/features/stock-in/types.ts, src/features/stock-in/api/receipt.api.ts, src/features/stock-in/api/receipt.keys.ts, src/features/stock-in/schemas/receipt.schema.ts, src/features/stock-in/hooks/useReceipts.ts</files>
  <read_first>
    - src/features/stock-in/api/receipt.api.ts (toàn bộ — nguồn để chép)
    - src/features/stock-in/hooks/useReceipts.ts (toàn bộ — chú ý `enabled: id !== ""` và `useRefreshAfterPosting`)
    - src/features/documents/types.ts (vừa tạo ở Task 1)
    - src/lib/supabase/client.ts
    - src/shared/lib/errors.ts (tên THẬT là `isPostgrestError` / `errorCode` / `explainError`; CLAUDE.md còn ghi tên cũ `laLoiPostgrest` / `maLoi` — đừng dùng tên cũ)
  </read_first>
  <action>
Tạo `src/features/documents/api/document.api.ts`, chuyển sang các hàm **không phụ thuộc
loại chứng từ**: `fetchDocuments`, `fetchDocumentDetail`, `fetchDocumentLines`,
`updateDocumentHeader`, `addDocumentLine`, `updateDocumentLine`, `deleteDocumentLine`,
`postDocument` (gọi `ghi_so_chung_tu`), `voidDocument` (gọi `huy_chung_tu`).
`fetchDocuments` nhận thẳng đối tượng tham số RPC
(`Database["public"]["Functions"]["danh_sach_chung_tu"]["Args"]`) để mỗi màn tự dựng args.

Giữ nguyên hai khuôn bắt buộc:
- `const { data, error } = await ...; if (error) throw error;` ở MỌI hàm — supabase-js
  không tự ném lỗi.
- `updateDocumentHeader` giữ `{ count: "exact" }` và khối `if (!count) throw new Error(...)`
  với câu tiếng Việt nói rõ phiếu đã ghi sổ hoặc thiếu quyền.

Sửa `src/features/stock-in/*` để dùng lại và **xóa hẳn bản cũ**, không để song song:
- `stock-in/types.ts`: xóa khai báo đã chuyển; re-export từ `@/features/documents/types`.
  Giữ lại `ReceiptSource` (alias của `DocumentSource`), `RECEIPT_SOURCE_LABELS`,
  `RECEIPT_SOURCE_COLORS`, `ReceiptPermissions`.
- `stock-in/api/receipt.api.ts`: giữ `createReceipt` (riêng của NHẬP vì chọn nguồn nhà
  máy) và `fetchReceipts(filter)` mỏng, gọi `fetchDocuments(toReceiptListRpcArgs(filter))`.
  Các hàm còn lại re-export từ `@/features/documents/api/document.api` dưới đúng tên cũ
  để component `stock-in` không phải đổi import.
- `stock-in/schemas/receipt.schema.ts`: import `documentHeaderSchema`, `documentLineSchema`,
  `toDocumentUpdate`, `toDocumentLineUpdate` từ `documents/schemas`, và `readUuid`,
  `readDate` từ `documents/lib/url-filter`. Giữ nguyên phần bộ lọc URL của màn nhập,
  **không đổi tên tham số URL** (`trang_thai`, `ncc`, `kho`, `nguon`, `tu_ngay`,
  `den_ngay`, `trang`) — bookmark của người dùng đang trỏ vào đó.
- `stock-in/api/receipt.keys.ts`: dựng `receiptKeys` trên `documentKeys` với `scope = "nhap"`.
- `stock-in/hooks/useReceipts.ts`: đổi import sang nguồn mới. **Giữ nguyên**
  `enabled: id !== ""` (bẫy 10) và `useRefreshAfterPosting` invalidate cả `productKeys.all`
  — ghi sổ/hủy đổi TỒN và GIÁ VỐN, không chỉ đổi cache phiếu.

`npm run check` **không** bắt được lỗi quên xóa bản cũ (hai định nghĩa cùng tên ở hai file
vẫn compile). Sau khi sửa phải tự rà bằng grep, xem `<acceptance_criteria>`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0 (typecheck + lint + build).
    - `grep -rln "export type DocumentRow" src/features/` chỉ in ra
      `src/features/documents/types.ts`.
    - `grep -rln "export function toDocumentRow" src/features/` chỉ in ra
      `src/features/documents/types.ts`.
    - `grep -rln "export const DOC_STATUS_LABELS" src/features/` chỉ in ra
      `src/features/documents/types.ts`.
    - `grep -c "features/documents" src/features/stock-in/hooks/useReceipts.ts` ≥ 1.
    - `grep -c 'enabled: id !== ""' src/features/stock-in/hooks/useReceipts.ts` ≥ 1.
    - `grep -c "productKeys" src/features/stock-in/hooks/useReceipts.ts` ≥ 1.
    - `grep -o 'params.get("[a-z_]*")' src/features/stock-in/schemas/receipt.schema.ts`
      vẫn chứa `trang_thai`, `ncc`, `kho`, `nguon`, `tu_ngay`, `den_ngay`, `trang`.
    - `git diff --stat src/features/stock-in/components/` chỉ được đụng tới
      `product-search-input.tsx` (xóa) và `receipt-line-table.tsx` (đổi đúng dòng import).
      Mọi component khác của `stock-in` phải **0 dòng đổi** — nếu phải sửa thì re-export ở
      bước trên chưa đầy đủ.
    - `src/shared/components/product-search-input.tsx` tồn tại và
      `grep -rln "features/stock-in/components/product-search-input" src/` không in ra file nào.
    - `git diff src/features/stock-in/components/receipt-line-table.tsx` chỉ có một dòng
      thêm và một dòng bớt.
  </acceptance_criteria>
  <done>`stock-in` chạy trên lớp `documents`; không còn bản sao kiểu/mapper nào.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Kiểm mắt màn phiếu nhập trên trình duyệt (trả nợ UAT Phase 3)</name>
  <files>—</files>
  <read_first>
    - .planning/phases/03-phieu-nhap/03-UAT.md (bài kiểm đã soạn cho phiếu nhập)
    - .planning/phases/03-phieu-nhap/03-SUMMARY.md mục "Chưa làm" và mục "Dữ liệu thử để lại"
  </read_first>
  <what-built>
Lớp chứng từ dùng chung đã rút sang `src/features/documents/`; `src/features/stock-in/`
gọi lại từ đó. `npm run check` xanh và grep xác nhận không còn bản sao. Nhưng `npm run check`
**không chứng minh giao diện chạy** — cả 5 lỗi UAT Phase 2 đều lọt qua check, và màn phiếu
nhập của Phase 3 tới giờ chưa ai mở bằng mắt.
  </what-built>
  <how-to-verify>
Mở một cửa sổ chạy `npm run dev`, đăng nhập bằng `vanphong@khominhvu.local`, rồi làm
đúng sáu bước sau. **Mở DevTools Console trước khi bắt đầu và để mở suốt.**

1. Vào `http://localhost:3000/nhap-kho`. Bảng danh sách hiện ra, panel lọc bên trái.
   Console **không** có dòng cảnh báo nào của antd (bẫy 11: antd v6 bỏ prop của v5, chỉ
   cảnh báo lúc chạy, build vẫn xanh).
2. Bật một điều kiện lọc bất kỳ → URL đổi theo (`?trang_thai=...`), tải lại trang thì bộ
   lọc giữ nguyên.
3. Thu cửa sổ xuống dưới 992px → panel lọc sập vào Drawer, bảng cuộn ngang **trong khung
   riêng**, cả trang không tràn ngang.
4. Mở một phiếu nhập bất kỳ. Lưu ý: dữ liệu thử Phase 3 còn trên database —
   phiếu `PNM26-000001` đang ở trạng thái **đã hủy**, hai mã `PN-UAT-A`/`PN-UAT-B` đã
   ngừng kinh doanh. Dùng chúng để xem, đừng ghi sổ lại.
5. Ở bảng dòng, gõ vào ô tìm mã → chọn bằng **Enter** → con trỏ nhảy sang ô số lượng →
   gõ số → **Enter** → dòng lưu và con trỏ quay về ô mã. Cả chuỗi không chạm chuột.
6. Bấm nút In → mở `/nhap-kho/{id}/in`, xem bản xem trước in (Ctrl+P): không có cột giá,
   đầu bảng lặp lại nếu sang trang thứ hai.

Báo lại: bước nào lệch, và **nguyên văn** mọi dòng console xuất hiện.
  </how-to-verify>
  <action>
Dừng thực thi tại đây. Đưa nguyên văn phần "how-to-verify" ở trên cho người dùng, chờ họ
tự chạy và trả lời. KHÔNG tự đánh giá thay, KHÔNG đi tiếp khi chưa có trả lời. Khi có phản
hồi: ghi nguyên văn vào SUMMARY (kể cả từng dòng console người dùng chép lại), và nếu có
bước lệch thì sửa rồi mời kiểm lại đúng bước đó trước khi khép task.
  </action>
  <verify>
    <human-check>Người dùng trả lời theo đúng resume-signal bên dưới</human-check>
  </verify>
  <acceptance_criteria>
    - Sáu bước trên đều chạy đúng mô tả.
    - DevTools Console không có `Warning:` nào của antd và không có lỗi đỏ nào.
    - Không có bước nào trả trang `/khong-du-quyen` hay trang lỗi.
  </acceptance_criteria>
  <resume-signal>Gõ "đạt" nếu cả sáu bước đúng, hoặc mô tả bước lệch kèm nguyên văn dòng console.</resume-signal>
  <done>Người dùng đã trả lời "đạt", hoặc mọi bước lệch đã được sửa và kiểm lại xong.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|---|---|
| trình duyệt → PostgREST | Lớp `api/` là nơi duy nhất dựng truy vấn; sai một cột là lộ hoặc 42501 |
| mapper `types.ts` → component | Ranh giới duy nhất chạm tên cột tiếng Việt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-24 | Information Disclosure | `fetchDocuments`/`fetchDocumentLines` vô tình dùng `select *` trên `san_pham` hoặc `kho_movement` | mitigate | Cả ba hàm đọc đi qua RPC (`danh_sach_chung_tu`, `chi_tiet_chung_tu`, `dong_chung_tu`), không truy vấn bảng trực tiếp; `npm run check` + grep xác nhận không có `.from("san_pham").select("*")` mới |
| T-04-25 | Information Disclosure | Refactor làm lộ `gia_von` qua `DocumentLine` | mitigate | `DocumentLine` chỉ nhận cột do `dong_chung_tu` trả; RPC đó không trả cột giá vốn nào (plan 04-01 đã khóa) |
| T-04-26 | Tampering | Rút lớp dùng chung làm mất khối `if (error) throw error` → lỗi bị nuốt, giao diện hiện dữ liệu cũ như thể thành công | mitigate | Acceptance criteria của Task 2 đòi `npm run check` xanh; Task 3 kiểm mắt màn nhập; khuôn bắt lỗi chép nguyên văn |
| T-04-27 | Tampering | Mất `{ count: "exact" }` ở `updateDocumentHeader` → sửa phiếu đã ghi sổ thất bại âm thầm, người dùng tưởng đã lưu | mitigate | Giữ nguyên khối `if (!count) throw new Error(...)`; nêu tường minh trong action |
| T-04-SC | Tampering | Cài gói npm/pip/cargo | accept | Plan này không cài gói mới nào; `package.json` không nằm trong `files_modified` |
</threat_model>

<verification>
- `npm run check` exit 0
- grep xác nhận `DocumentRow` / `toDocumentRow` / `DOC_STATUS_LABELS` chỉ còn một định
  nghĩa duy nhất, nằm trong `src/features/documents/`
- `git diff --stat src/features/stock-in/components/` = 0 file đổi
- Checkpoint Task 3: sáu bước màn phiếu nhập chạy đúng, console sạch
</verification>

<success_criteria>
- `src/features/documents/` có kiểu, mapper, schema, query key và lớp gọi dữ liệu dùng chung
- `src/features/stock-in/` không còn định nghĩa trùng, chỉ giữ phần riêng của chiều nhập
- Tham số URL của màn phiếu nhập không đổi
- Màn phiếu nhập mở trên trình duyệt chạy đúng sáu bước, console không cảnh báo
</success_criteria>

<output>
Create `.planning/phases/04-don-dat-hang-phieu-xuat/04-05-SUMMARY.md` when done.
Ghi lại: danh sách tên đã chuyển sang `documents` (để plan 04-06/04-07 khỏi đoán), lựa
chọn về `ReceiptPermissions` (ở lại hay chuyển), và kết quả kiểm mắt màn phiếu nhập kèm
mọi dòng console nếu có.
</output>
</content>
