---
phase: 04-don-dat-hang-phieu-xuat
plan: 07
type: execute
wave: 6
depends_on: ["04-05"]
files_modified:
  - src/features/stock-out/lib/negative-reasons.ts
  - src/features/stock-out/types.ts
  - src/features/stock-out/schemas/issue.schema.ts
  - src/features/stock-out/api/issue.keys.ts
  - src/features/stock-out/api/issue.api.ts
  - src/features/stock-out/hooks/useIssues.ts
  - src/features/returns/api/return.api.ts
  - src/features/returns/hooks/useReturns.ts
autonomous: true
requirements: [XUAT-01, XUAT-02, XUAT-04, XUAT-05, XUAT-09]

must_haves:
  truths:
    - "Màn phiếu xuất mỏng trên lớp documents, không copy lại mapper nào"
    - "Bốn lý do xuất âm là một danh sách cố định, khai đúng một chỗ"
    - "Lý do xuất âm được lưu vào đầu phiếu TRƯỚC khi gọi ghi sổ"
    - "Ghi sổ hoặc hủy phiếu xuất làm cache tồn và giá vốn hết hạn, không chỉ cache phiếu"
    - "Tạo phiếu trả gọi đúng một RPC và trả về id chứng từ mới"
  artifacts:
    - path: "src/features/stock-out/lib/negative-reasons.ts"
      provides: "Bốn mã lý do xuất âm + nhãn tiếng Việt, file thuần"
      exports: ["NEGATIVE_REASONS", "NEGATIVE_REASON_LABELS"]
    - path: "src/features/stock-out/api/issue.api.ts"
      provides: "Hàm gọi Supabase riêng của chiều xuất"
      exports: ["fetchIssues", "createIssue", "saveNegativeReason", "postIssue", "fetchSimilarCodes", "proposeMerge"]
    - path: "src/features/stock-out/hooks/useIssues.ts"
      provides: "Hook TanStack Query cho phiếu xuất"
      exports: ["useIssues", "useIssueDetail", "useIssueLines", "usePostIssue"]
    - path: "src/features/returns/api/return.api.ts"
      provides: "createReturn gọi tao_phieu_tra"
      exports: ["createReturn"]
  key_links:
    - from: "src/features/stock-out/api/issue.api.ts"
      to: "src/features/documents/api/document.api.ts"
      via: "dùng lại fetchDocuments/postDocument/voidDocument, không viết lại"
      pattern: "features/documents"
    - from: "postIssue"
      to: "saveNegativeReason"
      via: "cập nhật ly_do_xuat_am vào chung_tu trước khi gọi ghi_so_chung_tu"
      pattern: "ly_do_xuat_am"
---

<objective>
Lớp dữ liệu của phiếu xuất và phiếu trả hàng, mỏng trên `features/documents` của plan 04-05.

Phần riêng của chiều xuất chỉ có ba thứ:
1. **Lý do xuất âm** (D-11) — danh sách cố định bốn mục + ô ghi chú tự do. Mục đầu là
   nguyên nhân gốc người dùng chỉ ra: "mã bị tách / xuất nhầm mã".
2. **Gợi ý mã gần giống còn tồn** (D-14) — gọi `goi_y_ma_trung`, và nút "Đề nghị gộp hai
   mã" gọi `ghi_de_nghi_gop_ma`. Chỉ ghi lại đề nghị, không gộp gì.
3. **Thứ tự ghi sổ.** `ghi_so_chung_tu` đọc `ly_do_xuat_am` từ **đầu phiếu đã lưu**, nó
   không nhận lý do qua tham số. Nên lý do phải `update` vào `chung_tu` **trước** khi gọi
   ghi sổ. Sai thứ tự này thì ghi sổ sẽ trả 23514 "phải chọn lý do" dù người dùng đã chọn.

Phiếu trả (XUAT-09, D-15) dùng chung mọi thứ với `documents`, chỉ thêm đúng một hàm
`createReturn` gọi `tao_phieu_tra`.

Purpose: khóa hợp đồng dữ liệu cho bốn plan giao diện xuất/trả ở Wave 7–10.
Output: tám file, chưa có component nào.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-CONTEXT.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-04-SUMMARY.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-05-SUMMARY.md

**Bốn lý do xuất âm (D-11) — chốt mã và nhãn ở đây, mọi nơi khác import:**

| Mã lưu vào `chung_tu.ly_do_xuat_am` | Nhãn hiện cho người dùng |
|---|---|
| `MA_BI_TACH` | Mã bị tách / xuất nhầm mã |
| `HANG_VE_CHUA_NHAP` | Hàng đã về, chưa nhập phiếu |
| `LECH_TON_CHO_KIEM_KE` | Lệch tồn, chờ kiểm kê |
| `KHAC` | Khác |

Cột `chung_tu.ly_do_xuat_am` là `text` tự do ở tầng database và **cố ý để nguyên như vậy**:
`supabase/tests/20_chung_tu_test.sql` dòng 150 đang ghi một chuỗi tự do vào đó, và D-11 đã
chừa đường cho việc "quản lý sửa danh sách lý do ở Cài đặt" sau này. Hình dạng lý do do
Zod ở tầng này ép; chốt chặn *phải có lý do* thì đã nằm ở `ghi_so_chung_tu` từ Phase 1.

**Quy ước URL màn xuất:** `/xuat-kho` với `?q=`, `?trang_thai=`, `?doi_tac=`, `?kho=`,
`?tu_ngay=`, `?den_ngay=`, `?trang=`. Không có `?nguon=` — nguồn nhập chỉ NHẬP mới có.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lý do xuất âm, kiểu và bộ lọc URL của màn phiếu xuất</name>
  <files>src/features/stock-out/lib/negative-reasons.ts, src/features/stock-out/types.ts, src/features/stock-out/schemas/issue.schema.ts, src/features/stock-out/api/issue.keys.ts</files>
  <read_first>
    - src/features/documents/types.ts (kiểu và mapper dùng chung sau plan 04-05)
    - src/features/documents/lib/url-filter.ts
    - src/features/stock-in/schemas/receipt.schema.ts (khuôn bộ lọc URL và `toReceiptListRpcArgs`)
    - src/features/stock-in/types.ts (xem `ReceiptPermissions` để đặt `IssuePermissions` tương ứng)
    - src/features/documents/api/document.keys.ts
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-05-SUMMARY.md (danh sách tên đã chuyển sang `documents`)
  </read_first>
  <action>
**`src/features/stock-out/lib/negative-reasons.ts`** (file thuần, KHÔNG `"use client"` —
bẫy 9; màn in và Server Component cũng có thể cần nhãn):
`export const NEGATIVE_REASONS = ["MA_BI_TACH", "HANG_VE_CHUA_NHAP",
"LECH_TON_CHO_KIEM_KE", "KHAC"] as const;`
`export type NegativeReasonCode = (typeof NEGATIVE_REASONS)[number];`
`export const NEGATIVE_REASON_LABELS: Record<NegativeReasonCode, string>` theo đúng bảng
trong `<context>`. Giữ thứ tự — mục đầu là nguyên nhân phổ biến nhất, sẽ là lựa chọn nằm
trên cùng.
Thêm `export function negativeReasonLabel(code: string | null): string | null` trả nhãn
nếu mã nằm trong danh sách, ngược lại trả chính chuỗi đó — dữ liệu cũ hoặc dữ liệu test có
thể chứa chuỗi tự do, giao diện không được vỡ vì thế.

**`src/features/stock-out/types.ts`**: re-export `DocumentRow`, `DocumentDetail`,
`DocumentLine`, `DocStatus`, `toDocument*`, `DOC_STATUS_LABELS`, `DOC_STATUS_COLORS` từ
`@/features/documents/types` dưới tên miền của chiều xuất (`IssueRow`, `IssueDetail`,
`IssueLine`) — **alias, không định nghĩa lại**. Thêm:
- `IssuePermissions = { canEdit: boolean; canVoid: boolean }` (hủy phiếu xuất đã ghi sổ
  chỉ quản lý — plan 04-01 đã chặn ở database).
- `SimilarCode` — kiểu trả của `goi_y_ma_trung`: `productId`, `productCode`, `productName`,
  `warehouseId`, `warehouseName`, `stock` (`Number(...)`), `similarity`. Kèm mapper
  `toSimilarCode`.
- Hàm thuần `exceedsStock(line: IssueLine): boolean` → `line.quantity > line.currentStock`.
  Dùng cho việc tô màu dòng (D-12) và cho việc quyết định có phải chọn lý do hay không.
  Đặt ở `lib/`, không ở `types.ts`, nếu muốn giữ `types.ts` chỉ có kiểu — ghi lựa chọn vào SUMMARY.

**`src/features/stock-out/schemas/issue.schema.ts`**:
- `IssueFilter`, `DEFAULT_ISSUE_FILTER`, `ISSUE_PAGE_SIZE = 50`.
- `readIssueFilterFromUrl` / `writeIssueFilterToUrl` với tham số `q`, `trang_thai`,
  `doi_tac`, `kho`, `tu_ngay`, `den_ngay`, `trang`. Dùng `readUuid` / `readDate` từ
  `@/features/documents/lib/url-filter`.
- `countActiveIssueFilters`.
- `toIssueListRpcArgs(filter)` với `p_loai_ct: "XUAT"`. **Không** truyền `p_nguon_nhap`.
- `negativeReasonSchema = z.object({ code: z.enum(NEGATIVE_REASONS), note: z.string().nullable() })`
  với `superRefine`: khi `code === "KHAC"` thì `note` bắt buộc ≥ 5 ký tự, thông báo tiếng
  Việt "Chọn Khác thì phải ghi rõ lý do".
- `newIssueSchema` cho màn tạo phiếu xuất không cần đơn (XUAT-02): `partnerId` uuid bắt
  buộc, `warehouseId` uuid bắt buộc, `docDate` tùy chọn.

**`src/features/stock-out/api/issue.keys.ts`**: `issueKeys` dựng trên `documentKeys` với
`scope = "xuat"`, thêm nhánh `similar(productId, warehouseId)` cho gợi ý mã trùng.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Bốn file tồn tại đúng đường dẫn.
    - `grep -l "use client" src/features/stock-out/lib/negative-reasons.ts src/features/stock-out/types.ts`
      không in ra file nào.
    - `negative-reasons.ts` chứa đủ bốn mã `MA_BI_TACH`, `HANG_VE_CHUA_NHAP`,
      `LECH_TON_CHO_KIEM_KE`, `KHAC`, và `MA_BI_TACH` là phần tử đầu tiên của mảng.
    - `grep -c "export type DocumentRow =\|export function toDocumentRow" src/features/stock-out/types.ts`
      trả `0` — chỉ alias, không định nghĩa lại.
    - `grep -c "p_nguon_nhap" src/features/stock-out/schemas/issue.schema.ts` trả `0`.
    - `grep -c 'p_loai_ct: "XUAT"' src/features/stock-out/schemas/issue.schema.ts` ≥ 1.
    - `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>Lý do xuất âm, kiểu, bộ lọc URL, query key của màn xuất đã có.</done>
</task>

<task type="auto">
  <name>Task 2: Hàm gọi Supabase và hook cho phiếu xuất và phiếu trả</name>
  <files>src/features/stock-out/api/issue.api.ts, src/features/stock-out/hooks/useIssues.ts, src/features/returns/api/return.api.ts, src/features/returns/hooks/useReturns.ts</files>
  <read_first>
    - src/features/documents/api/document.api.ts (hàm dùng chung sau 04-05 — KHÔNG viết lại)
    - src/features/stock-in/api/receipt.api.ts (`createReceipt` — khuôn cấp số trên server rồi insert)
    - src/features/stock-in/hooks/useReceipts.ts (khuôn hook và `useRefreshAfterPosting`)
    - src/features/stock-out/schemas/issue.schema.ts + lib/negative-reasons.ts (vừa viết ở Task 1)
    - src/features/products/... — tìm `productKeys` bằng Grep để invalidate đúng key sau khi ghi sổ
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-03-SUMMARY.md và 04-04-SUMMARY.md (chữ ký `goi_y_ma_trung`, `ghi_de_nghi_gop_ma`, `tao_phieu_tra`)
  </read_first>
  <action>
**`src/features/stock-out/api/issue.api.ts`** — mọi hàm giữ khuôn
`const { data, error } = await ...; if (error) throw error;`.
- `fetchIssues(filter)` → `fetchDocuments(toIssueListRpcArgs(filter))` của `documents`.
- `fetchIssueDetail` / `fetchIssueLines` → re-export `fetchDocumentDetail` /
  `fetchDocumentLines`. Không viết lại.
- `createIssue(input: NewIssueInput): Promise<string>` (XUAT-02, phiếu không cần đơn):
  `rpc("sinh_so_ct", { p_loai: "XUAT" })` rồi
  `.from("chung_tu").insert({ so_ct, loai_ct: "XUAT", kho_id, doi_tac_id, ngay_ct? }).select("id").single()`.
  Liệt kê cột trong `.select("id")` — `.select()` trống sẽ 42501 (bẫy 5).
- `saveNegativeReason(id, reason: { code: NegativeReasonCode; note: string | null })`:
  `.from("chung_tu").update({ ly_do_xuat_am: reason.code, ghi_chu_ly_do: reason.note },
  { count: "exact" }).eq("id", id)`; `if (!count) throw new Error(...)` với câu tiếng Việt.
- `clearNegativeReason(id)`: đặt cả hai cột về `null` — người dùng bỏ chọn thì phải xóa
  được, không thì phiếu sau vẫn mang lý do cũ.
- `postIssue(id, reason?)`: **nếu có `reason` thì gọi `saveNegativeReason` TRƯỚC**, rồi mới
  `postDocument(id)` của `documents`. Ghi comment nêu rõ vì sao thứ tự này bắt buộc:
  `ghi_so_chung_tu` đọc `ly_do_xuat_am` từ đầu phiếu đã lưu, không nhận qua tham số.
- `voidIssue(id, reason)` → `voidDocument(id, reason)` của `documents`.
- `fetchSimilarCodes(productId, warehouseId): Promise<SimilarCode[]>` →
  `rpc("goi_y_ma_trung", { p_san_pham_id, p_kho_id })`, map qua `toSimilarCode`.
- `proposeMerge(productIdA, productIdB, docId)` → `rpc("ghi_de_nghi_gop_ma", ...)`.

**`src/features/stock-out/hooks/useIssues.ts`** — `"use client"` dòng đầu.
- `useIssues(filter)`, `useIssueDetail(id)`, `useIssueLines(id)` (hai hook sau có
  `enabled: id !== ""` — bẫy 10).
- `useCreateIssue`, `useUpdateIssueHeader(id)`, `useAddIssueLine(id)`,
  `useUpdateIssueLine(id)`, `useDeleteIssueLine(id)`, `useSaveNegativeReason(id)`,
  `usePostIssue(id)`, `useVoidIssue(id)`.
- `useSimilarCodes(productId, warehouseId)` với `enabled: productId !== ""`.
- `useProposeMerge()`.
- **`usePostIssue` và `useVoidIssue` phải invalidate cả `productKeys.all`** — ghi sổ và
  hủy đổi TỒN và GIÁ VỐN, y như `useRefreshAfterPosting` của `stock-in`. Bỏ sót là màn
  danh mục hiện số tồn cũ.
- `usePostIssue` cũng invalidate `orderKeys.all` của `sales-order` — ghi sổ phiếu xuất gắn
  đơn làm `so_luong_da_xuat` và trạng thái đơn đổi (XUAT-05).

**`src/features/returns/api/return.api.ts`**:
- `createReturn(sourceDocId: string): Promise<string>` → `rpc("tao_phieu_tra",
  { p_goc_id: sourceDocId })`, trả `id`. Loại phiếu trả do RPC suy từ chứng từ gốc —
  **không** truyền loại từ client.
- `fetchReturnDetail` / `fetchReturnLines` / `updateReturnLine` / `deleteReturnLine` /
  `postReturn` / `voidReturn`: re-export thẳng từ `@/features/documents/api/document.api`.
  Phiếu trả không có gì riêng ngoài cách sinh ra.
- `returnKeys` dựng trên `documentKeys` với `scope = "tra"`.

**`src/features/returns/hooks/useReturns.ts`** — `"use client"`:
`useCreateReturn()`, `useReturnDetail(id)`, `useReturnLines(id)`, `useUpdateReturnLine(id)`,
`useDeleteReturnLine(id)`, `usePostReturn(id)`. `usePostReturn` invalidate `productKeys.all`
và `documentKeys.all` — ghi sổ phiếu trả đổi tồn.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0.
    - `grep -c "features/documents" src/features/stock-out/api/issue.api.ts` ≥ 1 và
      `grep -c "features/documents" src/features/returns/api/return.api.ts` ≥ 1 — cả hai
      dùng lại lớp chung.
    - `grep -c "ghi_so_chung_tu" src/features/stock-out/api/issue.api.ts` trả `0` và
      `grep -c "ghi_so_chung_tu" src/features/returns/api/return.api.ts` trả `0` — ghi sổ
      đi qua `postDocument` của `documents`, không gọi RPC trực tiếp lần thứ hai.
    - Trong `issue.api.ts`, lệnh gọi `saveNegativeReason` bên trong `postIssue` xuất hiện
      **trước** lệnh gọi `postDocument`
      (`node -e "const s=require('fs').readFileSync('src/features/stock-out/api/issue.api.ts','utf8'); const b=s.slice(s.indexOf('function postIssue')); if (b.indexOf('saveNegativeReason') > b.indexOf('postDocument')) throw new Error('sai thu tu'); console.log('ok')"`).
    - `grep -c "productKeys" src/features/stock-out/hooks/useIssues.ts` ≥ 1 và
      `grep -c "productKeys" src/features/returns/hooks/useReturns.ts` ≥ 1.
    - `grep -c "orderKeys" src/features/stock-out/hooks/useIssues.ts` ≥ 1.
    - `grep -c 'enabled: ' src/features/stock-out/hooks/useIssues.ts` ≥ 3.
    - `grep -c "p_loai" src/features/returns/api/return.api.ts` trả `0`.
    - `grep -c ".select()" src/features/stock-out/api/issue.api.ts` trả `0`.
  </acceptance_criteria>
  <done>Lớp gọi dữ liệu và hook của phiếu xuất và phiếu trả đã xong.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|---|---|
| trình duyệt → PostgREST | `issue.api.ts` và `return.api.ts` dựng mọi truy vấn của chiều xuất |
| lý do xuất âm | Giá trị do người dùng chọn, đi vào `chung_tu.ly_do_xuat_am` rồi vào báo cáo Phase 5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-33 | Tampering | Ghi sổ phiếu xuất âm mà không có lý do, bằng cách gọi `postDocument` thẳng | mitigate | Chốt chặn thật ở `ghi_so_chung_tu` (0011/0041) đã ép `ly_do_xuat_am is null` + tồn âm → `23514`; tầng client chỉ thu thập lý do |
| T-04-34 | Tampering | Lý do lưu SAU khi gọi ghi sổ → ghi sổ đỏ dù người dùng đã chọn, hoặc phiếu ghi sổ rồi mới có lý do | mitigate | `postIssue` gọi `saveNegativeReason` trước `postDocument`; acceptance criteria kiểm thứ tự bằng script |
| T-04-35 | Tampering | Lý do tự do bậy lọt vào báo cáo xuất âm Phase 5, không nhóm được | mitigate | `negativeReasonSchema` ép `z.enum(NEGATIVE_REASONS)`; `KHAC` bắt buộc ghi chú ≥ 5 ký tự |
| T-04-36 | Information Disclosure | `fetchSimilarCodes` lộ giá vốn qua `goi_y_ma_trung` | mitigate | RPC đó không trả cột giá vốn nào (plan 04-03 đã khóa và có lệnh verify chặn chuỗi `gia_von`); mapper `toSimilarCode` chỉ nhận bảy trường đã biết |
| T-04-37 | Elevation of Privilege | `proposeMerge` bị dùng như đường gộp mã thật | mitigate | `ghi_de_nghi_gop_ma` chỉ `insert` một dòng đề nghị; pgTAP 28 của plan 04-03 assert tồn và `kho_movement` không đổi |
| T-04-38 | Information Disclosure | `.select()` trống sau insert `chung_tu` → 42501 hoặc lộ cột | mitigate | Acceptance criteria cấm `.select()` trống |
| T-04-SC | Tampering | Cài gói npm/pip/cargo | accept | Plan này không cài gói mới nào |
</threat_model>

<verification>
- `npm run check` exit 0
- Script kiểm thứ tự `saveNegativeReason` → `postDocument` trong `postIssue` exit 0
- grep xác nhận `stock-out` và `returns` không viết lại hàm nào của `documents`
- grep xác nhận `usePostIssue` invalidate cả `productKeys` lẫn `orderKeys`
</verification>

<success_criteria>
- `src/features/stock-out/` mỏng trên `documents`, chỉ thêm phần riêng của chiều xuất
- Bốn lý do xuất âm khai đúng một chỗ, `MA_BI_TACH` đứng đầu
- `postIssue` lưu lý do trước khi ghi sổ
- Ghi sổ/hủy làm hết hạn cache tồn, giá vốn và đơn liên quan
- `src/features/returns/` chỉ thêm `createReturn`, còn lại dùng chung
</success_criteria>

<output>
Create `.planning/phases/04-don-dat-hang-phieu-xuat/04-07-SUMMARY.md` when done.
Ghi lại: danh sách export của tám file, tên tham số URL màn xuất, và lựa chọn đặt
`exceedsStock` ở `types.ts` hay `lib/`.
</output>
</content>
