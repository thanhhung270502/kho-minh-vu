---
phase: 04-don-dat-hang-phieu-xuat
plan: 14
type: execute
wave: 11
depends_on: ["04-12", "04-13"]
files_modified:
  - src/app/(app)/xuat-kho/[id]/in/page.tsx
  - src/features/stock-out/components/delivery-print-page.tsx
  - src/features/stock-out/components/delivery-print-template.tsx
  - src/features/stock-out/components/issue-detail.tsx
  - src/features/returns/components/return-button.tsx
  - src/features/returns/components/return-detail.tsx
  - src/features/returns/components/return-line-table.tsx
  - src/app/(app)/tra-hang/[id]/page.tsx
  - src/features/stock-in/components/receipt-detail.tsx
autonomous: false
requirements: [XUAT-06, XUAT-09]

must_haves:
  truths:
    - "In được phiếu giao hàng cho khách từ một phiếu xuất"
    - "Trên phiếu xuất đã ghi sổ có nút Khách trả hàng; trên phiếu nhập đã ghi sổ có nút Trả NCC"
    - "Phiếu trả luôn mang tham chiếu tới chứng từ gốc và mở được chứng từ đó"
    - "Sửa số trả rồi ghi sổ làm tồn tăng với TRA_KHACH và giảm với TRA_NCC"
    - "Phiếu trả không thêm được mã ngoài chứng từ gốc"
  artifacts:
    - path: "src/features/stock-out/components/delivery-print-template.tsx"
      provides: "Mẫu in phiếu giao hàng cho khách (XUAT-06)"
      min_lines: 80
    - path: "src/features/returns/components/return-button.tsx"
      provides: "Nút sinh phiếu trả từ chứng từ gốc đã ghi sổ (D-15)"
    - path: "src/app/(app)/tra-hang/[id]/page.tsx"
      provides: "Route chi tiết phiếu trả, kiểm uuid, chặn quyền"
      contains: "requirePermission"
    - path: "src/features/returns/components/return-line-table.tsx"
      provides: "Bảng dòng phiếu trả: chỉ sửa số lượng, không thêm mã mới"
  key_links:
    - from: "src/features/returns/components/return-button.tsx"
      to: "createReturn"
      via: "gọi tao_phieu_tra, loại suy từ chứng từ gốc"
      pattern: "createReturn"
    - from: "src/features/returns/components/return-detail.tsx"
      to: "DocumentDetail.sourceDocNo"
      via: "link mở chứng từ gốc"
      pattern: "sourceDoc"
---

<objective>
Hai việc cuối của phần giao diện: in phiếu giao hàng, và trả hàng hai chiều.

**XUAT-06 — phiếu giao hàng.** Đây là mẫu in **thứ hai**, khác mục đích với tờ đi lấy hàng
của plan 04-12: tờ kia cho kho cầm đi nhặt hàng, tờ này đi cùng hàng tới người nhận.

> **Quyết định ở bước lập kế hoạch — phiếu giao hàng KHÔNG in giá.**
> 04-PATTERNS.md để ngỏ câu này. Chốt: không in. Ba lý do. (1) Chốt 19/09 câu 7 đã bỏ giá
> khỏi đơn, in giá ở khâu giao hàng là mở lại cửa đó. (2) `chung_tu_dong.don_gia` của phiếu
> xuất luôn bằng 0 — RPC `tao_phieu_xuat_tu_don` đặt 0, và màn tạo phiếu thủ công cũng
> không có ô giá; in ra toàn số 0 còn tệ hơn không in. (3) Giá vốn thì tuyệt đối không được
> lộ ra giấy đi ra ngoài công ty. Thay vào đó tờ này **có ô ký nhận** — khác tờ đi lấy hàng
> (D-09 cấm ô ký nhận ở tờ kia) vì đây là bằng chứng giao hàng.

**XUAT-09 / D-15 — trả hàng tạo từ nút trên chứng từ gốc.** Phiếu xuất đã ghi sổ →
"Khách trả hàng" (`TRA_KHACH`, tồn tăng). Phiếu nhập đã ghi sổ → "Trả NCC" (`TRA_NCC`, tồn
giảm). Dòng bê sang để sửa số trả. Ràng buộc `ck_tra_hang_co_goc` tự thỏa mãn vì RPC
`tao_phieu_tra` (plan 04-04) luôn set `chung_tu_goc_id`.

Purpose: khép phần giao diện của Phase 4.
Output: route in `/xuat-kho/{id}/in`, route `/tra-hang/{id}`, hai nút trả hàng.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-CONTEXT.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-07-SUMMARY.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-11-SUMMARY.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-13-SUMMARY.md

**Plan này sửa hai file của feature khác** (`src/features/stock-in/components/receipt-detail.tsx`
và `src/features/stock-out/components/issue-detail.tsx`) — chỉ để cắm thêm nút, không đổi
logic nào. CLAUDE.md yêu cầu nêu lý do trước: nút trả hàng **phải** nằm trên chứng từ gốc,
đó là chính quyết định D-15.

**Không có màn danh sách phiếu trả trong phase này** — chỉ có `/tra-hang/{id}`. Phiếu trả
xuất hiện trong danh sách chứng từ khi phase sau làm màn đó; hiện tại người dùng tới được
qua nút trên chứng từ gốc và qua link "Từ chứng từ …". Ghi điều này vào SUMMARY để người sau
không tưởng là sót.

**Bẫy 11 — antd v6:** `Alert` dùng `title`; `Modal` dùng `mask={{ closable: false }}`.
**Bẫy 12:** `/tra-hang/[id]` **phải** có dòng riêng trong ma trận quyền route (plan 04-15),
dù đây là route duy nhất dạng này và không có `/tra-hang` danh sách.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mẫu in phiếu giao hàng cho khách</name>
  <files>src/app/(app)/xuat-kho/[id]/in/page.tsx, src/features/stock-out/components/delivery-print-page.tsx, src/features/stock-out/components/delivery-print-template.tsx, src/features/stock-out/components/issue-detail.tsx</files>
  <read_first>
    - src/app/(app)/nhap-kho/[id]/in/page.tsx (khuôn route in riêng + comment giải thích)
    - src/features/stock-in/components/receipt-print-page.tsx
    - src/features/stock-in/components/receipt-print-template.tsx (toàn bộ — CSS `@page` / `@media print`, `thead { display: table-header-group }`, `tr { break-inside: avoid }`)
    - src/features/sales-order/components/picking-print-template.tsx (plan 04-12 — giữ hai mẫu in nhất quán về đầu tờ và CSS)
    - src/features/stock-out/components/issue-detail.tsx (plan 04-11 và 04-13 — khu vực actions còn comment chừa chỗ cho plan này)
    - src/features/stock-out/hooks/useIssues.ts
  </read_first>
  <action>
`src/app/(app)/xuat-kho/[id]/in/page.tsx` — Server Component theo khuôn
`nhap-kho/[id]/in/page.tsx`: `await params`, regex UUID → `notFound()`,
`await requirePermission("view-catalog")`, render `<DeliveryPrintPage id={id} />`.
Giữ comment giải thích vì sao là route riêng (trang chi tiết có ô nhập liệu, in ra sẽ dính
khung nhập).

`delivery-print-page.tsx` (`"use client"`): `useIssueDetail(id)` + `useIssueLines(id)`, bọc
`<QueryState>`, render `<DeliveryPrintTemplate>`.

`delivery-print-template.tsx` (`"use client"`) — chép **nguyên khối CSS in** từ
`receipt-print-template.tsx`. Nội dung:

**Đầu tờ:** tên công ty, tiêu đề "PHIẾU GIAO HÀNG", số phiếu, ngày phiếu, người nhận (mã +
tên + điện thoại nếu có), số đơn gốc nếu có, ghi chú.

**Bảng:** | STT | Mã hàng | Tên hàng | ĐVT | Số lượng |
**Không** cột đơn giá, **không** cột thành tiền, **không** cột giá vốn, **không** cột tồn.
Lý do đã ghi ở `<objective>`.
Dòng xếp theo thứ tự trên phiếu (`created_at, id`) — người nhận đối chiếu theo đúng thứ tự
văn phòng nhập.

**Cuối tờ:** tổng số dòng, tổng số lượng, và **hai ô ký nhận** cạnh nhau: "Người giao hàng"
và "Người nhận hàng", mỗi ô có dòng "(ký, ghi rõ họ tên)". Đây là điểm khác chính so với tờ
đi lấy hàng.

Nếu phiếu **chưa ghi sổ**, in ra kèm chữ "BẢN NHÁP — CHƯA GHI SỔ" mờ ở đầu tờ. Giấy giao
hàng của một phiếu chưa ghi sổ không phải bằng chứng gì cả, và người cầm giấy phải biết.

Sửa `issue-detail.tsx`: cắm nút "In phiếu giao hàng" (`Link` sang `/xuat-kho/{id}/in`, mở
tab mới) vào khu vực actions. Giữ lại comment chừa chỗ cho nút trả hàng của Task 2, hoặc
làm luôn ở Task 2 — ghi rõ lựa chọn.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0.
    - `grep -c "antd" "src/app/(app)/xuat-kho/[id]/in/page.tsx"` trả `0`; file có
      `requirePermission`, `notFound`, `await params`.
    - `delivery-print-template.tsx` chứa `table-header-group` và `break-inside`.
    - `grep -rc "Đơn giá\|Thành tiền\|don_gia\|unitPrice\|gia_von\|Tồn" src/features/stock-out/components/delivery-print-template.tsx`
      trả `0`.
    - File chứa chuỗi `Người nhận hàng` và `Người giao hàng`.
    - File chứa chuỗi `CHƯA GHI SỔ`.
    - `grep -c "xuat-kho/" src/features/stock-out/components/issue-detail.tsx` ≥ 1 — có nút in.
  </acceptance_criteria>
  <done>In được phiếu giao hàng: không giá, có ô ký nhận, đánh dấu bản nháp khi chưa ghi sổ.</done>
</task>

<task type="auto">
  <name>Task 2: Nút trả hàng trên chứng từ gốc</name>
  <files>src/features/returns/components/return-button.tsx, src/features/stock-out/components/issue-detail.tsx, src/features/stock-in/components/receipt-detail.tsx</files>
  <read_first>
    - src/features/stock-in/components/receipt-detail.tsx (toàn bộ — khu vực `Space wrap` chứa nút "In phiếu" và "Hủy phiếu", khoảng dòng 58–78)
    - src/features/stock-out/components/issue-detail.tsx (sau Task 1)
    - src/features/returns/api/return.api.ts và hooks/useReturns.ts (plan 04-07)
    - src/features/stock-in/components/post-receipt-button.tsx (khuôn `modal.confirm` + bắt lỗi theo mã)
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-04-SUMMARY.md (mọi mã lỗi `tao_phieu_tra` có thể ném)
    - src/shared/lib/errors.ts
  </read_first>
  <action>
Tạo `src/features/returns/components/return-button.tsx` (`"use client"`).

Props: `{ document: DocumentDetail; canEdit: boolean }`. Nhãn nút suy từ `document.docType`:
- `XUAT` → "Khách trả hàng"
- `NHAP` → "Trả hàng NCC"
- loại khác → không render.
Chỉ render khi `document.status === "HOAN_THANH"` và `canEdit` — D-15 nói tạo từ chứng từ
**đã ghi sổ**, và RPC cũng đòi vậy.

`modal.confirm` với tóm tắt: "Sinh phiếu {loại} từ {so_ct}: {n} dòng bê nguyên sang. Sửa lại
số trả ở từng dòng rồi ghi sổ. Tồn sẽ {tăng|giảm} sau khi ghi sổ." — nói đúng chiều tồn, vì
hai loại trả đi ngược nhau và nhầm chiều là lệch sổ.

`onOk` gọi `useCreateReturn()` với `document.id`, nhận `id` phiếu trả, rồi
`router.push(\`/tra-hang/${id}\`)`. **Không** truyền loại phiếu — RPC tự suy từ chứng từ gốc.

Bắt lỗi theo mã: `23514` → nguyên văn message RPC (chứng từ gốc chưa ghi sổ, hoặc gốc là
một phiếu trả); `42501` → câu cố định về quyền; còn lại → `explainError`. Nút `loading` khi
đang gọi.

Cắm `<ReturnButton>` vào khu vực actions của **cả hai** trang chi tiết:
`src/features/stock-out/components/issue-detail.tsx` và
`src/features/stock-in/components/receipt-detail.tsx`. Ở `receipt-detail.tsx` **chỉ thêm
đúng phần render nút**, không đổi gì khác — đây là file của feature `stock-in` và plan này
chỉ được chạm tới nó vì D-15 đặt nút ở đó.
Xóa comment chừa chỗ "plan 04-14" trong `issue-detail.tsx`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0; `return-button.tsx` mở bằng `"use client";`.
    - `grep -c "useCreateReturn" …` ≥ 1; `grep -c "tra-hang/" …` ≥ 1.
    - `grep -c "p_loai\|TRA_KHACH\|TRA_NCC" src/features/returns/components/return-button.tsx`
      — chỉ được khớp ở phần **nhãn hiển thị / tóm tắt**, không ở tham số gọi RPC; xác nhận
      bằng mắt khi đọc diff và ghi kết luận vào SUMMARY.
    - File xử lý riêng `23514` và `42501`, có `explainError`;
      `grep -c "instanceof PostgrestError" …` trả `0`.
    - `grep -c "ReturnButton" src/features/stock-out/components/issue-detail.tsx` ≥ 1 và
      `grep -c "ReturnButton" src/features/stock-in/components/receipt-detail.tsx` ≥ 1.
    - `git diff src/features/stock-in/components/receipt-detail.tsx` chỉ thêm dòng import và
      dòng render nút — không sửa dòng nào có sẵn.
    - `grep -c "04-14" src/features/stock-out/components/issue-detail.tsx` trả `0`.
  </acceptance_criteria>
  <done>Từ phiếu xuất và phiếu nhập đã ghi sổ, một nút là sang phiếu trả với dòng bê sẵn.</done>
</task>

<task type="auto">
  <name>Task 3: Trang chi tiết phiếu trả</name>
  <files>src/app/(app)/tra-hang/[id]/page.tsx, src/features/returns/components/return-detail.tsx, src/features/returns/components/return-line-table.tsx</files>
  <read_first>
    - src/app/(app)/xuat-kho/[id]/page.tsx (plan 04-11 — khuôn route `[id]`)
    - src/features/stock-out/components/issue-detail.tsx (khuôn khung trang; plan 04-11 đã ghi vào SUMMARY có dùng chung được hay không)
    - src/features/stock-out/components/issue-line-table.tsx (plan 04-11 — khuôn bảng dòng, kho theo dòng)
    - src/features/returns/hooks/useReturns.ts (plan 04-07)
    - src/features/documents/types.ts
    - src/shared/components/query-state.tsx
  </read_first>
  <action>
`src/app/(app)/tra-hang/[id]/page.tsx` — Server Component: `await params`, regex UUID →
`notFound()`, `const user = await requirePermission("view-catalog")`, render
`<ReturnDetailView id={id} permissions={{ canEdit, canVoid }} />` với `canVoid: user.role === "quan_ly"`.

`return-detail.tsx` (`"use client"`):
- `useReturnDetail(id)` bọc `<QueryState>` đủ bốn trạng thái.
- Tiêu đề: `{so_ct}` + `Tag` trạng thái + nhãn loại phiếu ("Khách trả hàng" cho `TRA_KHACH`,
  "Trả hàng NCC" cho `TRA_NCC`).
- **Link "Từ chứng từ {sourceDocNo}"** trỏ về `/xuat-kho/{sourceDocId}` hoặc
  `/nhap-kho/{sourceDocId}` tùy loại phiếu trả. Đây là đường duy nhất đi ngược về gốc, phải
  có.
- Một `Alert` (prop `title`) nói rõ chiều tồn trước khi người dùng ghi sổ: `TRA_KHACH` làm
  tồn **tăng**, `TRA_NCC` làm tồn **giảm**.
- Đầu phiếu ở dạng chỉ đọc — phiếu trả không cho đổi người nhận hay kho; chúng bê từ chứng
  từ gốc và đổi đi là mất liên hệ với gốc.
- `<ReturnLineTable>`, rồi nút ghi sổ và nút hủy: **dùng lại** `PostIssueButton` và
  `VoidIssueDialog` của `features/stock-out` nếu plan 04-11/04-13 đã để chúng đủ tổng quát;
  nếu không thì nâng chúng lên `features/documents/components/` và sửa import của
  `stock-out`. Chọn cách nào cũng được, **nhưng không chép lại** — ghi lựa chọn vào SUMMARY.
  Lưu ý phiếu trả **không** có khối lý do xuất âm cho `TRA_KHACH` (tồn tăng, không thể âm);
  `TRA_NCC` thì **có thể** làm tồn âm và `ghi_so_chung_tu` cũng ép lý do cho loại đó — nên
  khối `NegativeStockPanel` phải hiện với `TRA_NCC`.

`return-line-table.tsx` (`"use client"`) — chép khuôn `issue-line-table.tsx`, bỏ hai thứ:
- **Không có `ProductSearchInput` và không có hàng thêm dòng.** Trả hàng không thêm mã ngoài
  chứng từ gốc — mã nào không có trên phiếu gốc thì không phải hàng của lần giao đó.
- **Không có nút xóa dòng.** Không trả dòng nào thì đặt số lượng về 0; xóa hẳn làm mất đối
  chiếu với gốc. Ghi comment nêu lý do.
Giữ: cột kho của dòng (bê từ gốc, sửa được), tô màu dòng vượt tồn (chỉ có nghĩa với
`TRA_NCC`), luồng bàn phím cho ô số lượng (Tab/Enter di chuyển giữa các ô số lượng).
Số lượng cho phép bằng 0 nhưng **không âm**; validate ở schema và hiện lỗi dưới ô.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0.
    - `grep -c "antd" "src/app/(app)/tra-hang/[id]/page.tsx"` trả `0`; file có `await params`,
      `notFound`, `requirePermission`.
    - `return-detail.tsx` chứa `QueryState`, `sourceDoc`, và cả hai đường `/xuat-kho/` và
      `/nhap-kho/`.
    - `grep -c "TRA_NCC" src/features/returns/components/return-detail.tsx` ≥ 1 — khối lý do
      xuất âm chỉ bật cho loại đó.
    - `grep -c "ProductSearchInput" src/features/returns/components/return-line-table.tsx`
      trả `0`.
    - `grep -rc "deleteReturnLine\|onDelete" src/features/returns/components/return-line-table.tsx`
      trả `0` — không có nút xóa dòng.
    - `grep -rln "PostIssueButton\|PostingSummary" src/` cho thấy mỗi component chỉ có
      **một** nơi định nghĩa.
    - Không file nào trong `src/features/returns/components/` vượt 200 dòng.
  </acceptance_criteria>
  <done>`/tra-hang/{id}` mở được, sửa số trả rồi ghi sổ được, luôn có đường về chứng từ gốc.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Kiểm in phiếu giao hàng và hai chiều trả hàng</name>
  <files>—</files>
  <read_first>
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-13-SUMMARY.md (dữ liệu thử còn lại trên database)
  </read_first>
  <what-built>
Mẫu in phiếu giao hàng, nút trả hàng trên hai loại chứng từ gốc, và trang chi tiết phiếu
trả. Trả hàng **đụng tồn thật và đi hai chiều ngược nhau** — nhầm chiều là lệch sổ, và
`npm run check` không nói gì về chuyện đó.
  </what-built>
  <how-to-verify>
**Dùng mã test riêng.** Tạo `TRA-UAT-A`, nhập kho 100 cái ở Kho 1.

`npm run dev`, đăng nhập `vanphong@khominhvu.local`, **mở DevTools Console trước**.

**In phiếu giao hàng:**
1. Mở một phiếu xuất **chưa ghi sổ** → bấm "In phiếu giao hàng" → tờ in có chữ
   "BẢN NHÁP — CHƯA GHI SỔ".
2. Ghi sổ phiếu đó → in lại → chữ bản nháp biến mất.
3. Ctrl+P: tờ in **không** có cột giá nào, **có** hai ô ký nhận "Người giao hàng" và
   "Người nhận hàng"; nếu sang trang hai thì đầu bảng lặp lại.

**Khách trả hàng (tồn TĂNG):**
4. Trên phiếu xuất vừa ghi sổ (xuất `TRA-UAT-A` 30 cái, tồn còn 70) bấm **"Khách trả hàng"**
   → hộp xác nhận nói tồn sẽ **tăng** → đồng ý → sang `/tra-hang/{id}`.
5. Trang phiếu trả có link "Từ chứng từ {số phiếu xuất}" mở đúng phiếu gốc.
6. Dòng đã bê sang với số lượng 30. Sửa còn 10. **Không** có ô thêm mã mới, **không** có nút
   xóa dòng.
7. Ghi sổ → tồn `TRA-UAT-A` ở Kho 1 thành **80**.

**Trả NCC (tồn GIẢM):**
8. Mở phiếu nhập đã ghi sổ của `TRA-UAT-A` → bấm **"Trả hàng NCC"** → hộp xác nhận nói tồn
   sẽ **giảm** → đồng ý.
9. Sửa số trả thành 200 (lớn hơn tồn 80) → dòng đổi màu, khối lý do xuất âm hiện ra → chọn
   lý do → ghi sổ được → tồn thành **−120**.
10. Đăng nhập `quanly@khominhvu.local` → hủy phiếu trả đó → tồn về **80**.

11. Trên một phiếu xuất **chưa ghi sổ**, nút "Khách trả hàng" phải **không hiện**.

Dọn dẹp: ghi vào SUMMARY mọi mã và phiếu thử còn lại trên database.

Báo lại bước lệch và **nguyên văn** mọi dòng console.
  </how-to-verify>
  <action>
Dừng thực thi tại đây. Đưa nguyên văn phần "how-to-verify" ở trên cho người dùng, chờ họ
tự chạy và trả lời. KHÔNG tự đánh giá thay, KHÔNG đi tiếp khi chưa có trả lời. Khi có phản
hồi: ghi nguyên văn vào SUMMARY (kể cả từng dòng console người dùng chép lại), và nếu có
bước lệch thì sửa rồi mời kiểm lại đúng bước đó trước khi khép task.
  </action>
  <verify>
    <human-check>Người dùng trả lời theo đúng resume-signal bên dưới</human-check>
  </verify>
  <acceptance_criteria>
    - Bước 1–3: bản nháp được đánh dấu; tờ in không có cột giá; có hai ô ký nhận.
    - Bước 7: tồn **tăng** đúng số trả.
    - Bước 8–9: tồn **giảm**, và khối lý do xuất âm hiện cho `TRA_NCC`.
    - Bước 10: hủy làm tồn quay lại.
    - Bước 11: nút trả hàng ẩn khi chứng từ gốc chưa ghi sổ.
    - Console không có `Warning:` của antd và không lỗi đỏ ở mọi bước.
  </acceptance_criteria>
  <resume-signal>Gõ "đạt" nếu cả mười một bước đúng, hoặc mô tả bước lệch kèm nguyên văn console.</resume-signal>
  <done>Người dùng đã trả lời "đạt", hoặc mọi bước lệch đã được sửa và kiểm lại xong.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|---|---|
| tờ in → ngoài công ty | Phiếu giao hàng đi cùng hàng, ra khỏi tầm kiểm soát |
| nút trả hàng → `tao_phieu_tra` | Ẩn nút chỉ là trang trí; RPC gọi thẳng được |
| phiếu trả → `kho_movement` | Ghi sổ phiếu trả đổi tồn theo hai chiều ngược nhau |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-73 | Information Disclosure | Phiếu giao hàng in giá vốn hoặc giá bán ra giấy đi ra ngoài công ty | mitigate | Mẫu in không có cột giá nào; acceptance criteria chặn các chuỗi `don_gia`, `gia_von`, `Đơn giá`, `Thành tiền`; `dong_chung_tu` không trả cột giá vốn |
| T-04-74 | Spoofing | Phiếu giao hàng của một phiếu chưa ghi sổ bị dùng như bằng chứng giao hàng | mitigate | In kèm dấu "BẢN NHÁP — CHƯA GHI SỔ" khi `status !== "HOAN_THANH"` |
| T-04-75 | Tampering | Tạo phiếu trả từ chứng từ chưa ghi sổ, hoặc từ chính một phiếu trả | mitigate | `tao_phieu_tra` đòi gốc `HOAN_THANH` và `loai_ct in ('NHAP','XUAT')` → `23514` (plan 04-04); pgTAP 31 assert; nút cũng ẩn theo điều kiện |
| T-04-76 | Tampering | Phiếu trả bị thêm mã không có trên chứng từ gốc → tồn tăng từ hư không | mitigate | `return-line-table.tsx` không có `ProductSearchInput` và không có hàng thêm dòng; acceptance criteria kiểm bằng grep |
| T-04-77 | Tampering | Xóa dòng phiếu trả làm mất đối chiếu với chứng từ gốc | mitigate | Không có nút xóa dòng; không trả thì đặt số lượng 0 |
| T-04-78 | Tampering | `TRA_NCC` làm tồn âm mà không có lý do | mitigate | `ghi_so_chung_tu` ép `ly_do_xuat_am` cho cả `XUAT` và `TRA_NCC` (0011/0041); `NegativeStockPanel` hiện cho `TRA_NCC` |
| T-04-79 | Elevation of Privilege | `van_phong` hủy phiếu trả đã ghi sổ | mitigate | `huy_chung_tu` của plan 04-01 chặn cả `TRA_NCC` và `TRA_KHACH` đã ghi sổ → `42501`; pgTAP 24 assert |
| T-04-80 | Elevation of Privilege | Gõ tay `/tra-hang/{id}` khi chưa đăng nhập | mitigate | `requirePermission("view-catalog")` đầu Server Component; plan 04-15 thêm dòng riêng vào ma trận quyền (bẫy 12) |
| T-04-SC | Tampering | Cài gói npm/pip/cargo | accept | Plan này không cài gói mới nào |
</threat_model>

<verification>
- `npm run check` exit 0
- grep xác nhận: mẫu in giao hàng không có cột giá, có hai ô ký nhận và dấu bản nháp;
  bảng dòng phiếu trả không có ô thêm mã và không có nút xóa; `receipt-detail.tsx` chỉ thêm
  hai dòng
- Checkpoint Task 4: mười một bước chạy đúng trên database thật bằng mã test, console sạch
</verification>

<success_criteria>
- In được phiếu giao hàng: không giá, có ô ký nhận, đánh dấu rõ khi phiếu chưa ghi sổ
- Nút trả hàng hiện đúng trên phiếu xuất và phiếu nhập **đã ghi sổ**, ẩn khi chưa
- Phiếu trả luôn có đường về chứng từ gốc
- `TRA_KHACH` làm tồn tăng, `TRA_NCC` làm tồn giảm, cả hai ghi vào đúng kho của dòng
- Phiếu trả không thêm được mã mới và không xóa được dòng
</success_criteria>

<output>
Create `.planning/phases/04-don-dat-hang-phieu-xuat/04-14-SUMMARY.md` when done.
Ghi lại: hai route mới (`/xuat-kho/[id]/in`, `/tra-hang/[id]`) cho plan 04-15; lựa chọn về
việc dùng chung hay nâng `PostIssueButton`/`VoidIssueDialog`; việc **không có màn danh sách
phiếu trả** trong phase này; và dữ liệu thử còn lại trên database.
</output>
</content>
