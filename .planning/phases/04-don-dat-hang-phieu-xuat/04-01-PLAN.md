---
phase: 04-don-dat-hang-phieu-xuat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0050_trang_thai_don_duyet.sql
  - supabase/migrations/0051_chung_tu_rpc_mo_rong.sql
  - supabase/tests/23_don_dat_hang_test.sql
  - supabase/tests/24_chung_tu_mo_rong_test.sql
  - src/types/database.types.ts
autonomous: true
requirements: [DDH-02, DDH-03, XUAT-04, XUAT-05]

must_haves:
  truths:
    - "Trạng thái đơn chỉ còn trục duyệt TAM | DA_XAC_NHAN | HOAN_THANH | DA_HUY"
    - "Ghi sổ phiếu xuất gắn đơn làm so_luong_da_xuat của từng dòng đơn cộng lại đúng"
    - "Đơn DA_XAC_NHAN giao đủ mọi dòng thì tự đóng HOAN_THANH; đơn TAM không tự đổi trạng thái"
    - "Giao diện phiếu xuất đọc được tồn hiện tại của từng dòng để tô màu cảnh báo"
    - "Giao diện phiếu xuất đọc được đơn gốc, chứng từ gốc và lý do xuất âm của phiếu"
    - "Văn phòng không hủy được phiếu xuất đã ghi sổ"
    - "Ghi sổ phiếu xuất và phiếu trả trừ/cộng đúng kho của TỪNG DÒNG, không phải kho đầu phiếu"
  artifacts:
    - path: "supabase/migrations/0050_trang_thai_don_duyet.sql"
      provides: "Đổi trục enum trang_thai_ddh + viết lại _cap_nhat_tien_do_ddh + dựng lại idx_ddh_trang_thai"
      contains: "create type public.trang_thai_ddh as enum"
    - path: "supabase/migrations/0051_chung_tu_rpc_mo_rong.sql"
      provides: "chi_tiet_chung_tu/dong_chung_tu trả thêm cột phục vụ chiều xuất; huy_chung_tu chặn vai trò cho XUAT/TRA_*; _ghi_so_xuat/_ghi_so_tra_* ghi sổ theo kho của dòng"
      contains: "ton_hien_tai"
    - path: "supabase/tests/23_don_dat_hang_test.sql"
      provides: "pgTAP cho trục trạng thái đơn"
      contains: "select plan("
    - path: "supabase/tests/24_chung_tu_mo_rong_test.sql"
      provides: "pgTAP cho cột RPC mới và chốt chặn hủy phiếu xuất"
      contains: "42501"
  key_links:
    - from: "public.ghi_so_chung_tu"
      to: "public._cap_nhat_tien_do_ddh"
      via: "perform khi loai_ct = 'XUAT' và don_dat_hang_id not null"
      pattern: "_cap_nhat_tien_do_ddh"
    - from: "public.dong_chung_tu"
      to: "public.ton_kho"
      via: "left join theo coalesce(dong.kho_id, header.kho_id)"
      pattern: "ton_kho"
    - from: "public._ghi_so_xuat"
      to: "public.kho_movement"
      via: "insert với kho_id = coalesce(p_dong.kho_id, p_ct.kho_id)"
      pattern: "coalesce\(p_dong.kho_id, p_ct.kho_id\)"
---

<objective>
Sửa nền database đã có từ Phase 1/3 cho chiều xuất, trước khi dựng bất cứ thứ gì mới.

Hai việc:
1. `trang_thai_ddh` đang đo **trục giao** (`MOI | DA_XUAT_MOT_PHAN | DA_XUAT_DU | DA_HUY`).
   D-04 chốt nó chỉ mang **trục duyệt** (`TAM | DA_XAC_NHAN | HOAN_THANH | DA_HUY`); trục
   giao tính khi đọc từ `so_luong_da_xuat` so với `so_luong_dat`.
2. `chi_tiet_chung_tu` / `dong_chung_tu` (migration 0045) không trả đơn gốc, chứng từ gốc,
   lý do xuất âm hay tồn hiện tại — bốn thứ màn phiếu xuất bắt buộc phải có. Và
   `huy_chung_tu` mới chỉ chặn vai trò cho `NHAP`, phiếu xuất đã ghi sổ ai cũng hủy được.

Purpose: mọi plan sau của Phase 4 đứng trên nền này. Làm sau sẽ phải sửa lại RPC đã có
người gọi.
Output: hai migration (0050, 0051), hai file pgTAP (23, 24), `database.types.ts` sinh lại.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-CONTEXT.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md
@supabase/README.md

**Trạng thái database đo ngày 20/09 trên `kho-vu-tru` — dùng số này, không đo lại:**
- `don_dat_hang`: **0 dòng**. `chung_tu` có `loai_ct='XUAT'`: **0 dòng**. Không có dữ liệu
  phải chuyển đổi khi đổi enum.
- Migration mới nhất: **0049**. pgTAP hiện tại: **225 assert xanh**.

**Phụ thuộc của kiểu `trang_thai_ddh` phải gỡ theo đúng thứ tự** (04-PATTERNS.md mục WU-1
đã viết sẵn thứ tự — đọc và làm theo):
1. `don_dat_hang.trang_thai` default `'MOI'` (migration 0006)
2. partial index `idx_ddh_trang_thai` — mệnh đề `where` nhắc hai giá trị cũ (0006)
3. literal `'DA_XUAT_DU'`/`'DA_XUAT_MOT_PHAN'`/`'MOI'` trong `_cap_nhat_tien_do_ddh`
   (0011 dòng 139–176)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Đổi trục enum trang_thai_ddh và viết lại _cap_nhat_tien_do_ddh</name>
  <files>supabase/migrations/0050_trang_thai_don_duyet.sql, supabase/tests/23_don_dat_hang_test.sql</files>
  <read_first>
    - supabase/migrations/0002_enums.sql (định nghĩa `trang_thai_ddh` hiện tại)
    - supabase/migrations/0006_don_dat_hang.sql (cột default + `idx_ddh_trang_thai`)
    - supabase/migrations/0011_rpc_ghi_so.sql dòng 139–176 (`_cap_nhat_tien_do_ddh` bản gốc)
    - supabase/migrations/0041_kho_theo_dong.sql (khuôn "chép nguyên văn hàm cũ, sửa đúng một chỗ, ghi comment chỉ rõ chỗ đổi")
    - supabase/tests/22_kho_theo_dong_test.sql (khuôn file pgTAP)
    - supabase/tests/00_helper.sql.inc (khối helper phải chép vào đầu file test)
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md mục "WU-1" (thứ tự lệnh bắt buộc)
  </read_first>
  <action>
Viết `supabase/migrations/0050_trang_thai_don_duyet.sql` theo đúng thứ tự lệnh đã ghi
trong 04-PATTERNS.md mục WU-1:

1. `alter table public.don_dat_hang alter column trang_thai drop default;` rồi
   `drop index if exists public.idx_ddh_trang_thai;` — phải làm TRƯỚC khi đụng vào kiểu.
2. `alter type public.trang_thai_ddh rename to trang_thai_ddh_old;` →
   `create type public.trang_thai_ddh as enum ('TAM','DA_XAC_NHAN','HOAN_THANH','DA_HUY');`
   → `alter table public.don_dat_hang alter column trang_thai type public.trang_thai_ddh
   using (...)::public.trang_thai_ddh` với ánh xạ `MOI→TAM`, `DA_XUAT_MOT_PHAN→DA_XAC_NHAN`,
   `DA_XUAT_DU→HOAN_THANH`, `DA_HUY→DA_HUY` (bảng 0 dòng nên ánh xạ chỉ để migration
   không phụ thuộc giả định về dữ liệu) → `set default 'TAM'` → `drop type public.trang_thai_ddh_old;`
3. `create index idx_ddh_trang_thai on public.don_dat_hang (trang_thai) where trang_thai in ('TAM','DA_XAC_NHAN');`
   — hai trạng thái còn việc phải làm, giữ đúng ý đồ partial index gốc của 0006.
4. CUỐI CÙNG mới `create or replace function public._cap_nhat_tien_do_ddh(p_ddh_id uuid)`.
   Làm bước này trước bước 2 sẽ lỗi `invalid input value for enum`.

Nội dung `_cap_nhat_tien_do_ddh` bản mới (D-04, D-05):
- Khối `update public.don_dat_hang_dong ... set so_luong_da_xuat = coalesce((select sum(...)))`
  **giữ nguyên từng ký tự** so với bản 0011 — đây không phải lần sửa logic tính.
- Bỏ biến `v_da_xuat` (không còn dùng). Giữ `v_con_thieu`.
- Thay khối `update public.don_dat_hang set trang_thai = case ...` bằng: chỉ khi
  `v_con_thieu = 0` thì `update public.don_dat_hang set trang_thai = 'HOAN_THANH'
  where id = p_ddh_id and trang_thai = 'DA_XAC_NHAN'`. Đơn `TAM` và `DA_HUY` không bị
  đụng tới — chuyển `TAM → DA_XAC_NHAN` là việc của `xac_nhan_don` (plan 04-02), không
  phải của hàm này.
- Giữ nguyên `language plpgsql security definer set search_path = ''` và mọi tên
  schema-qualified.

Thêm `comment on type public.trang_thai_ddh` nói rõ đây là trục duyệt, trục giao tính
khi đọc.

Viết `supabase/tests/23_don_dat_hang_test.sql` theo khuôn `22_kho_theo_dong_test.sql`:
`begin; select plan(N);` → chép nguyên khối `00_helper.sql.inc` → dựng dữ liệu →
assert → `select * from finish(); rollback;`. Các assert bắt buộc:
- `has_type('trang_thai_ddh')` và bốn nhãn enum đúng bằng `enum_has_labels` hoặc so
  mảng `enum_range` đổ về text.
- Cột `don_dat_hang.trang_thai` có default `'TAM'`.
- Index `idx_ddh_trang_thai` tồn tại (`has_index('public','don_dat_hang','idx_ddh_trang_thai')`).
- Nghiệp vụ: tạo một đơn `DA_XAC_NHAN` hai dòng, tạo phiếu `XUAT` gắn `don_dat_hang_id`,
  ghi sổ đủ cả hai dòng → `so_luong_da_xuat` từng dòng bằng `so_luong_dat`, và trạng thái
  đơn thành `HOAN_THANH`.
- Nghiệp vụ: đơn `DA_XAC_NHAN` chỉ giao một phần → vẫn `DA_XAC_NHAN` (KHÔNG có trạng
  thái "giao một phần").
- Nghiệp vụ: đơn `TAM` mà có phiếu xuất ghi sổ đủ → `so_luong_da_xuat` cập nhật nhưng
  trạng thái vẫn `TAM`.
Dựng sản phẩm bằng `pg_temp.sp_test(...)`, kho bằng `pg_temp.kho_id('K1')`, đăng nhập
bằng `pg_temp.dang_nhap_nhu('vanphong@khominhvu.local')`. Đơn cần `doi_tac_id` — lấy một
`doi_tac` có sẵn bằng `select id from public.doi_tac limit 1`, không hard-code uuid.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0050_trang_thai_don_duyet.sql','utf8'); const i=(t)=>s.indexOf(t); if(i('drop index')<0||i('rename to trang_thai_ddh_old')<0||i('_cap_nhat_tien_do_ddh')<0) throw new Error('thieu buoc'); if(!(i('drop index')<i('rename to trang_thai_ddh_old') && i('rename to trang_thai_ddh_old')<i('_cap_nhat_tien_do_ddh'))) throw new Error('sai thu tu lenh'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0050_trang_thai_don_duyet.sql` tồn tại và lệnh `drop index` đứng
      TRƯỚC `alter type ... rename to trang_thai_ddh_old`, lệnh này đứng TRƯỚC
      `create or replace function public._cap_nhat_tien_do_ddh` (lệnh verify ở trên exit 0).
    - `grep -c "DA_XUAT_MOT_PHAN\|DA_XUAT_DU" supabase/migrations/0050_trang_thai_don_duyet.sql`
      chỉ đếm được các lần xuất hiện bên trong mệnh đề `using (...)` của `alter column ... type`
      — không có literal nào trong thân `_cap_nhat_tien_do_ddh`
      (`sed -n '/create or replace function public._cap_nhat_tien_do_ddh/,$p' supabase/migrations/0050_trang_thai_don_duyet.sql | grep -v '^\s*--' | grep -c 'DA_XUAT'` trả `0`).
    - Thân `_cap_nhat_tien_do_ddh` mới có đúng một lệnh `update public.don_dat_hang`, và
      lệnh đó chứa cả `trang_thai = 'DA_XAC_NHAN'` (điều kiện) lẫn `'HOAN_THANH'` (giá trị đặt).
    - `supabase/tests/23_don_dat_hang_test.sql` mở bằng `begin;`, đóng bằng `rollback;`,
      có `select plan(` và `select * from finish();`.
    - File test không chứa chuỗi `DH26-` hay bất kỳ số chứng từ cố định theo năm hiện tại nào.
  </acceptance_criteria>
  <done>Migration 0050 và test 23 đã viết, thứ tự lệnh kiểm được bằng script, chưa push.</done>
</task>

<task type="auto">
  <name>Task 2: Mở rộng RPC chứng từ, ghi sổ theo kho của dòng, siết quyền hủy</name>
  <files>supabase/migrations/0051_chung_tu_rpc_mo_rong.sql, supabase/tests/24_chung_tu_mo_rong_test.sql</files>
  <read_first>
    - supabase/migrations/0045_rpc_chung_tu.sql (toàn bộ — `danh_sach_chung_tu`, `chi_tiet_chung_tu`, `dong_chung_tu`; phải chép lại nguyên thân hàm rồi mới thêm cột)
    - supabase/migrations/0046_chi_quan_ly_huy_nhap.sql (bản `huy_chung_tu` ĐANG CHẠY — chép từ đây, không từ 0012)
    - supabase/migrations/0007_chung_tu.sql dòng 1–55 (cột `don_dat_hang_id`, `chung_tu_goc_id`, `ly_do_xuat_am`, `ghi_chu_ly_do`, ràng buộc `ck_tra_hang_co_goc`)
    - supabase/migrations/0011_rpc_ghi_so.sql (`_ghi_so_xuat`, `_ghi_so_tra_ncc`, `_ghi_so_tra_khach` — cả ba đang ghi `p_ct.kho_id`)
    - supabase/migrations/0041_kho_theo_dong.sql dòng 17–33 (`_ghi_so_nhap` bản đã sửa — khuôn `coalesce(p_dong.kho_id, p_ct.kho_id)` phải nhân bản)
    - supabase/migrations/0029_*.sql (vì sao không được `select *` trên `san_pham`/`kho_movement`)
    - supabase/tests/21_danh_sach_chung_tu_test.sql (khuôn test cho nhóm RPC này)
    - supabase/tests/00_helper.sql.inc
  </read_first>
  <action>
Viết `supabase/migrations/0051_chung_tu_rpc_mo_rong.sql`. Ba việc, đặt trong một file:

**(a) `chi_tiet_chung_tu(p_id uuid)` — thêm cột.** Đổi kiểu trả về nên bắt buộc
`drop function public.chi_tiet_chung_tu(uuid);` rồi `create function` lại (CREATE OR
REPLACE không cho đổi kiểu trả về — tiền lệ đã ghi ở 0029). Giữ nguyên toàn bộ danh sách
cột cũ theo đúng thứ tự, thêm vào cuối: `don_dat_hang_id uuid`, `so_dh text`,
`chung_tu_goc_id uuid`, `so_ct_goc text`, `ly_do_xuat_am text`, `ghi_chu_ly_do text`,
`nguoi_duyet_id uuid`. `so_dh` lấy bằng left join `public.don_dat_hang`; `so_ct_goc` lấy
bằng left join `public.chung_tu` tự tham chiếu (đặt alias khác, ví dụ `goc`). Giữ nguyên
`stable security definer set search_path = ''` và toàn bộ logic lọc phân quyền hiện có.

**(b) `dong_chung_tu(p_id uuid)` — thêm cột `ton_hien_tai numeric`.** Cũng phải
`drop function` rồi tạo lại. Cột mới = tồn hiện tại của `(san_pham_id, kho hiệu lực)`,
lấy bằng `left join public.ton_kho tk on tk.san_pham_id = d.san_pham_id and tk.kho_id =
coalesce(d.kho_id, ct.kho_id)`, trả `coalesce(tk.so_luong, 0)`. Đây là số để giao diện tô
màu dòng vượt tồn (D-12, XUAT-04). Kho hiệu lực phải dùng đúng `coalesce(dòng, header)`
giống `ghi_so_chung_tu` bản 0041 — sai chỗ này thì cảnh báo báo nhầm kho. Giữ nguyên
khối chống lặp logic phân quyền (`if not exists (select 1 from public.chi_tiet_chung_tu(p_id)) then return; end if;`).

**(c) Ghi sổ theo kho của DÒNG cho chiều xuất — lỗi ngầm phải vá trước khi dựng giao diện.**
Migration 0041 chỉ sửa `_ghi_so_nhap` và khối kiểm tồn âm trong `ghi_so_chung_tu`. Ba hàm
`_ghi_so_xuat`, `_ghi_so_tra_ncc`, `_ghi_so_tra_khach` **vẫn ghi `p_ct.kho_id`**. Hệ quả
nếu để nguyên: cảnh báo xuất âm đọc tồn kho của DÒNG (0041) nhưng `kho_movement` lại trừ
kho ĐẦU PHIẾU — kho này âm, kho kia dư, không ai thấy cho tới lúc kiểm kê. D-13 cho kho
sửa từng dòng trên phiếu xuất nên lỗi này sẽ nổ ngay ngày đầu.

`create or replace` cả ba hàm, chép nguyên văn thân hàm từ `0011_rpc_ghi_so.sql`, đổi
**đúng một chỗ** ở mỗi hàm: đối số `kho_id` của lệnh `insert into public.kho_movement`
từ `p_ct.kho_id` thành `coalesce(p_dong.kho_id, p_ct.kho_id)` — giống hệt cách 0041 đã
làm cho `_ghi_so_nhap`. Không đổi dấu `so_luong` (xuất và trả NCC vẫn âm, trả khách vẫn
dương), không đổi cách lấy `gia_von`. Kèm lại `revoke all on function ... from public,
anon, authenticated;` cho cả ba — chúng là hàm nội bộ, client không được gọi.
`_ghi_so_chuyen_kho` giữ nguyên: chuyển kho theo bản chất là chuyện của cả phiếu, đúng
comment đã ghi ở đầu file 0041.

**(d) `huy_chung_tu` — siết quyền cho chiều xuất.** `create or replace` từ bản ĐANG CHẠY
ở 0046, đổi đúng một điều kiện: thay `if v_ct.loai_ct = 'NHAP' and v_ct.trang_thai =
'HOAN_THANH' ...` thành `if v_ct.loai_ct in ('NHAP','XUAT','TRA_NCC','TRA_KHACH') and
v_ct.trang_thai = 'HOAN_THANH' and (select public.vai_tro_hien_tai()) <> 'quan_ly' then
raise exception ... using errcode = '42501';`. Đổi câu thông báo thành
`'Chỉ quản lý được hủy chứng từ đã ghi sổ'`. Ghi comment nói rõ: 0046 mới chặn `NHAP`;
hủy phiếu xuất đã ghi sổ cũng viết lại sổ cái và đảo tồn nên phải cùng một mức quyền.
Ghi thêm comment rằng `CHUYEN_KHO`/`KIEM_KE`/`DIEU_CHINH` chưa có giao diện nên để
nguyên, sẽ quyết ở phase của chúng.

Hai hàm đọc và `huy_chung_tu` phải kèm lại `revoke all on function ... from public, anon;` +
`grant execute on function ... to authenticated;` sau khi drop/create.

Viết `supabase/tests/24_chung_tu_mo_rong_test.sql` theo khuôn
`21_danh_sach_chung_tu_test.sql`. Assert bắt buộc:
- `chi_tiet_chung_tu` trả `so_dh` đúng khi phiếu gắn đơn, trả `null` khi không gắn.
- `chi_tiet_chung_tu` trả `so_ct_goc` đúng với một phiếu `TRA_KHACH` trỏ về phiếu xuất gốc.
- `dong_chung_tu` trả `ton_hien_tai` bằng tồn thật của kho DÒNG khi dòng có `kho_id` khác
  header (dựng một phiếu hai dòng hai kho, ghi sổ một phiếu nhập trước để có tồn).
- `van_phong` gọi `huy_chung_tu` trên một phiếu `XUAT` đã `HOAN_THANH` bị lỗi `42501`
  (`throws_ok`, so mã lỗi `42501`).
- `quan_ly` gọi `huy_chung_tu` trên chính phiếu đó thành công.
- **Kho theo dòng chiều xuất:** dựng tồn ở K1 và K2 bằng một phiếu nhập, rồi ghi sổ một
  phiếu `XUAT` có `kho_id` header = K1 nhưng một dòng đặt `kho_id` = K2. Assert dòng
  `kho_movement` của dòng đó có `kho_id` = K2, `ton_kho` của K2 giảm đúng số, và `ton_kho`
  của K1 không đổi vì dòng đó.
- Lặp assert tương tự cho một phiếu `TRA_KHACH` (tồn K2 **tăng**).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0051_chung_tu_rpc_mo_rong.sql','utf8'); for (const t of ['drop function public.chi_tiet_chung_tu','drop function public.dong_chung_tu','ton_hien_tai','so_ct_goc','ly_do_xuat_am','grant execute on function public.chi_tiet_chung_tu','grant execute on function public.dong_chung_tu','_ghi_so_xuat','_ghi_so_tra_ncc','_ghi_so_tra_khach']) { if (!s.includes(t)) throw new Error('thieu: '+t); } if (!/loai_ct in \('NHAP','XUAT','TRA_NCC','TRA_KHACH'\)/.test(s)) throw new Error('thieu chot chan huy'); const n=(s.match(/coalesce\(p_dong\.kho_id, p_ct\.kho_id\)/g)||[]).length; if (n<3) throw new Error('kho theo dong moi vao '+n+'/3 ham ghi so'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh verify ở trên exit 0: file có đủ drop+create cho hai RPC đọc, có `ton_hien_tai`,
      `so_ct_goc`, `ly_do_xuat_am`, có grant lại, có mệnh đề chặn hủy bốn loại chứng từ, và
      chuỗi `coalesce(p_dong.kho_id, p_ct.kho_id)` xuất hiện **ít nhất 3 lần** — một lần cho
      mỗi hàm `_ghi_so_xuat` / `_ghi_so_tra_ncc` / `_ghi_so_tra_khach`.
    - File KHÔNG chứa `_ghi_so_chuyen_kho` — hàm đó cố ý để nguyên.
    - `grep -c "select \*" supabase/migrations/0051_chung_tu_rpc_mo_rong.sql` không đếm được
      `select *` nào trên `public.san_pham` hoặc `public.kho_movement`.
    - Thân `dong_chung_tu` chứa chuỗi `coalesce(d.kho_id, ct.kho_id)` (hoặc alias tương đương)
      trong điều kiện join `ton_kho` — tồn phải đọc theo kho của DÒNG.
    - `supabase/tests/24_chung_tu_mo_rong_test.sql` chứa `42501` và `throws_ok`.
    - File test mở `begin;` đóng `rollback;`, có `select plan(` và `select * from finish();`.
  </acceptance_criteria>
  <done>Migration 0051 (mở rộng RPC đọc + kho theo dòng chiều xuất + siết quyền hủy) và test 24 đã viết, chưa push.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Đẩy migration lên database, sinh lại kiểu, chạy toàn bộ pgTAP</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - supabase/README.md (cách chạy migration và pgTAP trên cloud)
    - supabase/migrations/0050_trang_thai_don_duyet.sql (vừa viết ở Task 1)
    - supabase/migrations/0051_chung_tu_rpc_mo_rong.sql (vừa viết ở Task 2)
  </read_first>
  <action>
Chạy theo đúng thứ tự, dừng ngay khi có bước đỏ:

1. `npm run db:push` — đẩy 0050 và 0051 lên `kho-vu-tru`.
2. `npm run db:types` — sinh lại `src/types/database.types.ts`. KHÔNG sửa tay file này.
3. `npm run db:test:linked` — chạy **toàn bộ** bộ pgTAP, không chỉ hai file mới. 0050 sửa
   `_cap_nhat_tien_do_ddh` là hàm mọi test gọi `ghi_so_chung_tu` trên chứng từ `XUAT` có
   `don_dat_hang_id` đều chạm gián tiếp; 0051 sửa `huy_chung_tu` và hai RPC đọc mà test 20,
   21, 22 đang gọi.
4. Đếm kết quả bằng cách đếm CẢ dòng `ERROR` lẫn `not ok`. File pgTAP chết giữa chừng
   không sinh `not ok` nào — đếm mỗi `not ok` sẽ báo "0 lỗi" trong khi assert không hề
   chạy (bài học Phase 3, `03-SUMMARY.md` mục "Cách đếm assert cũ nói dối").

Nếu một assert cũ đỏ vì chốt chặn hủy mới (mục c của Task 2): ĐỌC assert đó, xác định nó
đang khẳng định hành vi cũ nào, rồi sửa assert cho khớp quyết định mới và ghi lại vào
SUMMARY — không nới lỏng migration để test cũ xanh.

Xác nhận `git diff --stat src/types/database.types.ts` cho thấy file đã đổi (enum mới +
kiểu trả về RPC mới). Nếu không đổi thì `db:push` chưa ăn — dừng lại và điều tra, đừng đi tiếp.
  </action>
  <verify>
    <automated>npm run db:test:linked 2>&1 | tee /tmp/pgtap-04-01.log; grep -c "^not ok\|^ERROR\|ERROR:" /tmp/pgtap-04-01.log</automated>
  </verify>
  <acceptance_criteria>
    - `npm run db:push` exit 0.
    - `npm run db:types` exit 0 và `git diff --stat src/types/database.types.ts` có thay đổi.
    - `npm run db:test:linked` exit 0; log không có dòng nào bắt đầu bằng `not ok` và không
      có dòng nào chứa `ERROR:`.
    - Tổng số assert chạy được **≥ 225** (số cuối Phase 3) cộng thêm số assert của file 23 và 24.
    - `npm run typecheck` exit 0 sau khi sinh lại kiểu.
  </acceptance_criteria>
  <done>0050 và 0051 đã nằm trên database thật, `database.types.ts` khớp, toàn bộ pgTAP xanh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|---|---|
| trình duyệt → PostgREST | Bất kỳ ai có token `authenticated` gọi thẳng RPC được, không qua giao diện |
| RPC `SECURITY DEFINER` → bảng | Hàm chạy dưới quyền owner, RLS **không** áp dụng bên trong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-01 | Elevation of Privilege | `huy_chung_tu` với `loai_ct='XUAT'` đã `HOAN_THANH` | mitigate | Thêm mệnh đề `loai_ct in ('NHAP','XUAT','TRA_NCC','TRA_KHACH') and vai_tro_hien_tai() <> 'quan_ly'` → `42501`; pgTAP 24 assert `van_phong` bị chặn, `quan_ly` qua |
| T-04-02 | Information Disclosure | `chi_tiet_chung_tu`/`dong_chung_tu` là `SECURITY DEFINER`, thêm cột = thêm bề mặt lộ | mitigate | Cột thêm vào không có `gia_von`/`gia_von_tai_thoi_diem`; giữ nguyên khối lọc phạm vi kho cho `thu_kho` đã có ở 0045; `dong_chung_tu` vẫn gọi `chi_tiet_chung_tu` để thừa hưởng lọc |
| T-04-03 | Tampering | `_cap_nhat_tien_do_ddh` có thể đóng nhầm đơn `TAM` thành `HOAN_THANH`, vượt bước duyệt | mitigate | Mệnh đề `where ... and trang_thai = 'DA_XAC_NHAN'`; pgTAP 23 assert đơn `TAM` giao đủ vẫn ở `TAM` |
| T-04-04 | Tampering | `ton_hien_tai` đọc sai kho → cảnh báo xuất âm im lặng sai | mitigate | Join `ton_kho` bằng `coalesce(dòng.kho_id, header.kho_id)`; pgTAP 24 assert bằng phiếu hai dòng hai kho |
| T-04-04b | Tampering | `_ghi_so_xuat`/`_ghi_so_tra_*` trừ kho đầu phiếu trong khi cảnh báo đọc kho dòng — sổ cái ghi sai kho, lệch tồn âm thầm | mitigate | Cả ba hàm đổi sang `coalesce(p_dong.kho_id, p_ct.kho_id)` giống `_ghi_so_nhap` (0041); pgTAP 24 assert `kho_movement.kho_id` và `ton_kho` của đúng kho dòng |
| T-04-SC | Tampering | Cài gói npm/pip/cargo | accept | Plan này không cài gói nào — không có `npm install` trong bất kỳ task nào |
</threat_model>

<verification>
- `npm run db:push` · `npm run db:types` · `npm run db:test:linked` đều exit 0
- pgTAP: ≥ 225 assert cũ + assert mới, 0 `not ok`, 0 `ERROR:`
- `npm run typecheck` exit 0
- `select unnest(enum_range(null::public.trang_thai_ddh))` trên database thật trả đúng
  bốn giá trị `TAM, DA_XAC_NHAN, HOAN_THANH, DA_HUY`
</verification>

<success_criteria>
- Enum `trang_thai_ddh` chỉ còn trục duyệt; `don_dat_hang.trang_thai` default `'TAM'`
- `_cap_nhat_tien_do_ddh` chỉ cập nhật `so_luong_da_xuat` và tự đóng `HOAN_THANH` cho đơn
  `DA_XAC_NHAN` giao đủ; không ghi bất kỳ trạng thái "giao một phần" nào
- `chi_tiet_chung_tu` trả `so_dh`, `so_ct_goc`, `ly_do_xuat_am`, `ghi_chu_ly_do`
- `dong_chung_tu` trả `ton_hien_tai` theo kho của dòng
- `van_phong` bị `42501` khi hủy chứng từ đã ghi sổ; `quan_ly` thì được
- `_ghi_so_xuat`, `_ghi_so_tra_ncc`, `_ghi_so_tra_khach` ghi `kho_movement` vào đúng kho
  của dòng, rơi về kho đầu phiếu khi dòng không chọn kho
- Toàn bộ pgTAP xanh sau khi push
</success_criteria>

<output>
Create `.planning/phases/04-don-dat-hang-phieu-xuat/04-01-SUMMARY.md` when done.
Ghi lại: số assert trước/sau, mọi assert cũ phải sửa vì chốt chặn hủy mới, và số hiệu
migration cuối cùng để plan sau đánh số tiếp.
</output>
</content>
</invoke>
