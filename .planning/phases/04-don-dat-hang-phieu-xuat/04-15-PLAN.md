---
phase: 04-don-dat-hang-phieu-xuat
plan: 15
type: execute
wave: 12
depends_on: ["04-14"]
files_modified:
  - src/shared/lib/navigation.ts
  - src/shared/components/nav-icons.tsx
  - scripts/test-route-permissions.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [DDH-01, DDH-03, XUAT-01, XUAT-02]

must_haves:
  truths:
    - "Menu trái và thanh tab đáy có mục Đặt hàng và Xuất kho, hiện đúng theo vai trò"
    - "Ma trận quyền route liệt kê đủ bảy route mới, kể cả route in và route trả hàng"
    - "Toàn bộ bộ kiểm của dự án xanh"
    - "REQUIREMENTS.md mô tả DDH-03 khớp với trục trạng thái đã chốt ở D-04"
  artifacts:
    - path: "src/shared/lib/navigation.ts"
      provides: "NAV_ITEMS có mục Đặt hàng và Xuất kho, mobilePriority tính lại"
      contains: "dat-hang"
    - path: "scripts/test-route-permissions.ts"
      provides: "Ma trận quyền phủ mọi route thật của Phase 4"
      contains: "tra-hang"
  key_links:
    - from: "src/shared/components/app-shell.tsx"
      to: "src/shared/lib/navigation.ts"
      via: "filterByPermission(user.role, NAV_ITEMS) — sửa mảng là mọi nơi tiêu thụ tự cập nhật"
      pattern: "NAV_ITEMS"
    - from: "scripts/test-route-permissions.ts"
      to: "danh_sach_don / danh_sach_chung_tu"
      via: "lấy id thật để kiểm route [id], không hard-code uuid"
      pattern: "danh_sach_don"
---

<objective>
Khép Phase 4: điều hướng, chốt chặn hồi quy quyền route, chạy toàn bộ bộ kiểm, và sửa hai
tài liệu đã lệch so với quyết định.

⚠️ **File đúng cần sửa để thêm mục menu là `src/shared/lib/navigation.ts` (mảng `NAV_ITEMS`),
KHÔNG PHẢI `src/shared/components/app-shell.tsx`.** `app-shell.tsx` chỉ tiêu thụ `NAV_ITEMS`
qua `filterByPermission(user.role, NAV_ITEMS)`; sửa ở component sẽ không có tác dụng, còn
sửa đúng `navigation.ts` thì cả `app-shell.tsx` lẫn `bottom-tab-bar.tsx` tự cập nhật.

⚠️ **Bẫy 12 — ma trận quyền route phải liệt kê MỌI route thật**, kể cả route chỉ redirect.
Lần trước thiếu đúng `/cai-dat` nên script báo 45/45 xanh trong khi trang đó crash. Hiện
65/65 ô; Phase 4 thêm **bảy route** nên đích là **100 ô**.

**Hai tài liệu đã lệch:**
1. `REQUIREMENTS.md` dòng DDH-03 còn ghi `MOI → DA_XUAT_MOT_PHAN → DA_XUAT_DU`. D-04 đã
   chốt trạng thái đơn chỉ mang **trục duyệt** và trục giao **tính khi đọc**; ROADMAP.md
   success criterion 1 đã phản ánh, REQUIREMENTS.md thì chưa.
2. `ROADMAP.md` mục Phase 4 còn `**Plans**: TBD`.

Purpose: không để Phase 5 bắt đầu trên một nền có chốt chặn thủng hoặc tài liệu nói sai.
Output: menu, ma trận quyền 100 ô, bộ kiểm xanh, hai tài liệu khớp thực tế.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-CONTEXT.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-08-SUMMARY.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-09-SUMMARY.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-10-SUMMARY.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-11-SUMMARY.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-12-SUMMARY.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-14-SUMMARY.md

**Bảy route Phase 4 tạo ra** (đối chiếu lại với SUMMARY của từng plan, đừng tin danh sách
này mà không kiểm — dùng `find "src/app/(app)" -name "page.tsx"`):

| Route | Plan | Quyền |
|---|---|---|
| `/dat-hang` | 04-08 | `view-catalog` |
| `/dat-hang/[id]` | 04-09 | `view-catalog` |
| `/dat-hang/[id]/in` | 04-12 | `view-catalog` |
| `/xuat-kho` | 04-10 | `view-catalog` |
| `/xuat-kho/[id]` | 04-11 | `view-catalog` |
| `/xuat-kho/[id]/in` | 04-14 | `view-catalog` |
| `/tra-hang/[id]` | 04-14 | `view-catalog` |

**Chặn quyền route KHÔNG nằm ở `src/proxy.ts`** — file đó chỉ lo phiên đăng nhập. Chặn theo
vai trò nằm ở `requirePermission()` gọi trong từng `page.tsx`. Comment ở đầu
`scripts/test-route-permissions.ts` nói "requirePermission() trong Server Component VÀ
proxy.ts" là hơi gây hiểu lầm — thực tế 100% role-gating nằm ở `requirePermission()`.
Sửa luôn câu comment đó cho khỏi dắt người sau đi sai.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thêm mục Đặt hàng và Xuất kho vào điều hướng</name>
  <files>src/shared/lib/navigation.ts, src/shared/components/nav-icons.tsx</files>
  <read_first>
    - src/shared/lib/navigation.ts (toàn bộ — `NAV_ITEMS`, `NavIconId`, `findActiveHref`, `filterByPermission`, `splitMobileItems`, `bottomTabColumns`)
    - src/shared/components/nav-icons.tsx (ánh xạ `NavIconId` sang element)
    - src/shared/components/app-shell.tsx (chỉ để xác nhận nó tiêu thụ `NAV_ITEMS`, KHÔNG sửa file này)
    - src/shared/components/bottom-tab-bar.tsx (cách dùng `splitMobileItems`)
  </read_first>
  <action>
Thêm hai phần tử vào `NAV_ITEMS`, đặt **sau** `/nhap-kho` — nghiệp vụ hằng ngày đứng trước
dữ liệu nền, và thứ tự trong mảng là thứ tự menu:

| trường | mục Đặt hàng | mục Xuất kho |
|---|---|---|
| `href` | `/dat-hang` | `/xuat-kho` |
| `label` | Đặt hàng | Xuất kho |
| `shortLabel` | Đặt hàng | Xuất |
| `icon` | `sales-order` | `stock-out` |
| `permission` | `view-catalog` | `view-catalog` |
| `mobilePriority` | tính lại, xem bên dưới | tính lại, xem bên dưới |


Thêm `"sales-order"` và `"stock-out"` vào union `NavIconId`, và ánh xạ sang element tương
ứng trong `nav-icons.tsx` (chọn icon đã có sẵn trong bộ đang dùng; không cài thư viện icon
mới).

**Tính lại `mobilePriority`.** `splitMobileItems` chỉ lấy **4 ô chính**; hiện đã có 4 mục
priority 1–4 (`/`, `/nhap-kho`, `/danh-muc`, `/doi-tac`). Thêm hai mục mà không tính lại
thì "Danh mục" và "Đối tác" bị đẩy vào "Khác" một cách vô tình.
Chọn có chủ đích cho thủ kho trên điện thoại — người dùng nhiều nhất của thanh tab đáy —
và ghi lý do vào comment ngay trên mảng. Đề xuất: `/` 1, `/xuat-kho` 2, `/nhap-kho` 3,
`/dat-hang` 4, `/danh-muc` và `/doi-tac` chuyển sang `null` (vào "Khác"). Lý do: kho làm
việc với phiếu xuất nhiều nhất (92 phiếu/ngày so với 8 phiếu nhập), còn danh mục và đối tác
là màn tra cứu thỉnh thoảng.
**Không** đổi `permission` của mục nào đang có.

Kiểm `findActiveHref`: `/dat-hang/{id}/in` phải khớp `/dat-hang` (hàm lọc theo
`startsWith` rồi lấy phần tử cuối). Xác nhận `/dat-hang` không vô tình khớp với route nào
khác.
  </action>
  <verify>
    <automated>npm run check && npx tsx scripts/test-pure-functions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0 và `npx tsx scripts/test-pure-functions.ts` exit 0.
    - `grep -c '"/dat-hang"\|"/xuat-kho"' src/shared/lib/navigation.ts` ≥ 2.
    - `grep -c "sales-order\|stock-out" src/shared/components/nav-icons.tsx` ≥ 2.
    - `git diff --stat src/shared/components/app-shell.tsx` cho thấy **0 dòng đổi**.
    - `grep -l "use client" src/shared/lib/navigation.ts` không in ra gì — file phải thuần.
    - Tổng số mục có `mobilePriority !== null` trong `NAV_ITEMS` **không vượt quá 4**
      (`node -e "..."` đếm, hoặc đọc bằng mắt và ghi kết luận vào SUMMARY).
    - Mảng có comment nêu lý do chọn thứ tự `mobilePriority`.
    - `npm run dev` + mở `/dat-hang` trên khung 375px: thanh tab đáy có 4 ô + nút "Khác",
      không tràn ngang (kiểm ở Task 4).
  </acceptance_criteria>
  <done>Menu trái và thanh tab đáy có Đặt hàng và Xuất kho, thứ tự ưu tiên có chủ đích.</done>
</task>

<task type="auto">
  <name>Task 2: Ma trận quyền route phủ bảy route mới</name>
  <files>scripts/test-route-permissions.ts</files>
  <read_first>
    - scripts/test-route-permissions.ts (TOÀN BỘ — mảng `MA_TRAN`, hằng `AI_CUNG_XEM`, hàm `layIdPhieuNhap()` khoảng dòng 89–115, và khối `if (idPhieu) {...} else { console.warn(...) }` khoảng dòng 164–172)
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-03-SUMMARY.md (chữ ký `danh_sach_don`)
  </read_first>
  <action>
**Hai route tĩnh** — thêm thẳng vào `MA_TRAN` với hằng `AI_CUNG_XEM` (bốn vai trò `200`,
khách `dangnhap`):
`/dat-hang` và `/xuat-kho`.

**Năm route có `[id]`** — cần id **thật**, không hard-code uuid. Viết ba hàm theo đúng khuôn
`layIdPhieuNhap()` (đăng nhập bằng `quan_ly`, gọi RPC với `p_kich_thuoc: 1`, trả
`data?.[0]?.id ?? null`):
- `layIdDon()` → `rpc("danh_sach_don", { p_trang: 1, p_kich_thuoc: 1 })`
- `layIdPhieuXuat()` → `rpc("danh_sach_chung_tu", { p_loai_ct: "XUAT", p_trang: 1, p_kich_thuoc: 1 })`
- `layIdPhieuTra()` → `rpc("danh_sach_chung_tu", { p_loai_ct: "TRA_KHACH", p_trang: 1, p_kich_thuoc: 1 })`;
  nếu rỗng thì thử tiếp `TRA_NCC` trước khi trả `null`.

Rồi push các dòng tương ứng:
`/dat-hang/{id}`, `/dat-hang/{id}/in`, `/xuat-kho/{id}`, `/xuat-kho/{id}/in`,
`/tra-hang/{id}` — tất cả dùng `AI_CUNG_XEM`.

**Không có dữ liệu thì phải nói rõ, không được giả vờ đã kiểm** — theo đúng khuôn
`else { console.warn("⚠ chưa có phiếu nhập nào — bỏ qua 2 route chi tiết") }` đang có.
Mỗi nhóm một cảnh báo riêng, nêu đúng số route bị bỏ qua. `don_dat_hang` và phiếu xuất sẽ
rỗng cho tới khi người dùng hoặc UAT tạo dữ liệu — script phải chạy được và báo trung thực
trong cả hai trường hợp.

**Bẫy 12:** `/tra-hang/{id}` **phải có dòng riêng** dù là route duy nhất dạng đó và không
có `/tra-hang` danh sách.

Sửa luôn câu comment ở đầu file: bỏ vế "VÀ proxy.ts" vì `proxy.ts` không có role check nào,
chỉ có auth check — câu hiện tại dắt người đọc đi sai chỗ.

Cuối script đã in tổng số ô — xác nhận con số mới. Đích: **65 + 35 = 100 ô** khi có đủ dữ
liệu cho cả năm route `[id]`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0.
    - `grep -c '"/dat-hang"' scripts/test-route-permissions.ts` ≥ 1 và
      `grep -c '"/xuat-kho"' …` ≥ 1.
    - `grep -c "tra-hang" scripts/test-route-permissions.ts` ≥ 1.
    - `grep -c "layIdDon\|layIdPhieuXuat\|layIdPhieuTra" scripts/test-route-permissions.ts` ≥ 6
      (mỗi hàm khai báo một lần và gọi một lần).
    - `grep -c "console.warn" scripts/test-route-permissions.ts` ≥ 4 — mỗi nhóm route thiếu
      dữ liệu có cảnh báo riêng.
    - `grep -c "proxy.ts" scripts/test-route-permissions.ts` trả `0` trong khối comment đầu file.
    - Mọi route tĩnh trong `find "src/app/(app)" -name "page.tsx"` đều có một dòng trong
      `MA_TRAN` — đối chiếu bằng tay và ghi bảng đối chiếu vào SUMMARY.
  </acceptance_criteria>
  <done>Ma trận quyền liệt kê đủ bảy route mới, báo trung thực khi thiếu dữ liệu thử.</done>
</task>

<task type="auto">
  <name>Task 3: Chạy toàn bộ bộ kiểm và sửa hai tài liệu đã lệch</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (mục "Đơn đặt hàng (DDH)" và "Phiếu xuất & trả hàng (XUAT)")
    - .planning/ROADMAP.md (mục "### Phase 4")
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-CONTEXT.md mục `<decisions>` D-04, D-05
    - package.json (danh sách script của bộ kiểm)
  </read_first>
  <action>
**Chạy toàn bộ bộ kiểm, theo thứ tự, dừng ở bước đỏ đầu tiên:** `npm run check` →
`npm run db:test:linked` (đếm **cả** dòng `ERROR:` lẫn `not ok`, xem bài học "Cách đếm
assert cũ nói dối" ở `03-SUMMARY.md`) → `npm run verify:hook` →
`npx tsx scripts/test-pure-functions.ts` → `npx tsx scripts/test-excel-reader.ts` → mở một
cửa sổ chạy `npm run dev` rồi `npx tsx scripts/test-route-permissions.ts`.
Ghi lại con số của từng bước vào SUMMARY theo đúng khuôn bảng "Bộ kiểm cuối" của
`03-SUMMARY.md`.

**Sửa `REQUIREMENTS.md`:**
- DDH-03: viết lại thành
  `**DDH-03**: Trạng thái đơn chạy TAM → DA_XAC_NHAN → HOAN_THANH theo bước duyệt; tiến độ
  giao (đã xuất / còn lại) tính khi đọc từ số đã xuất của từng dòng`
  và ghi chú ngắn `*(đổi trục theo D-04, chốt 20/09)*`. Đây không phải đổi phạm vi — là sửa
  mô tả cho khớp quyết định đã chốt; ROADMAP.md success criterion 1 đã viết đúng từ trước.
- Đánh dấu `[x]` cho đúng các requirement Phase 4 đã làm xong: DDH-01..04, XUAT-01, 02, 04,
  05, 06, 07, 09. **Không** đánh dấu XUAT-03 và XUAT-08 (đã dời sang Phase 6).

**Sửa `ROADMAP.md`** mục Phase 4:
- `**Plans**: 15 plans`
- Thêm danh sách plan với ô đánh dấu, mỗi dòng một câu mục tiêu ngắn:
  `- [x] 04-01-PLAN.md — đổi trục trạng thái đơn, mở rộng RPC chứng từ, kho theo dòng chiều xuất`
  … đủ 15 dòng, đánh dấu `[x]` cho plan đã chạy xong.
- Giữ nguyên `**Goal**` và năm success criteria — chúng đã đúng.

Commit theo Conventional Commits tiếng Việt, ví dụ
`docs(04): cap nhat requirements va roadmap sau phase 4`. Một commit làm một việc.
  </action>
  <verify>
    <automated>npm run check && npm run db:test:linked && npm run verify:hook && npx tsx scripts/test-pure-functions.ts</automated>
  </verify>
  <acceptance_criteria>
    - Cả bốn lệnh trong `<automated>` exit 0.
    - `npm run db:test:linked` không có dòng `not ok` và không có dòng chứa `ERROR:`; tổng
      assert ≥ 225 cộng assert của sáu file test mới (23, 24, 25, 26, 27, 28, 29, 31).
    - `npx tsx scripts/test-excel-reader.ts` exit 0.
    - `npx tsx scripts/test-route-permissions.ts` (với `npm run dev` đang chạy) báo **0 ô
      lệch**; số ô in ra được ghi vào SUMMARY kèm số route bị bỏ qua vì thiếu dữ liệu.
    - `grep -c "DA_XUAT_MOT_PHAN\|DA_XUAT_DU" .planning/REQUIREMENTS.md` trả `0`.
    - `grep -c "TBD" .planning/ROADMAP.md` trả `0` trong mục Phase 4.
    - `grep -c "04-15-PLAN.md" .planning/ROADMAP.md` ≥ 1 — danh sách plan đủ 15 dòng.
  </acceptance_criteria>
  <done>Bộ kiểm xanh, REQUIREMENTS.md và ROADMAP.md khớp với thực tế đã dựng.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Kiểm điều hướng, thanh tab đáy, và việc người dùng còn phải làm</name>
  <files>—</files>
  <read_first>
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-WORK-UNITS.md mục "WU-0 — Việc của người dùng, không phải của code"
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-13-SUMMARY.md và 04-14-SUMMARY.md (dữ liệu thử còn lại)
  </read_first>
  <what-built>
Điều hướng, ma trận quyền 100 ô, bộ kiểm xanh, tài liệu đã sửa. Phần code của Phase 4 khép
lại ở đây.
  </what-built>
  <how-to-verify>
`npm run dev`, **mở DevTools Console trước**.

**Điều hướng:**
1. Đăng nhập `quanly@` → menu trái có "Đặt hàng" và "Xuất kho", bấm vào mở đúng trang.
2. Thu cửa sổ xuống 375px → thanh tab đáy có **4 ô + nút "Khác"**, không tràn ngang; các
   mục bị đẩy vào "Khác" mở được từ đó.
3. Ở `/dat-hang/{id}/in`, mục "Đặt hàng" trên menu vẫn được đánh dấu đang hoạt động.
4. Đăng nhập `chixem@` → vẫn thấy "Đặt hàng" và "Xuất kho" (quyền `view-catalog`), nhưng
   vào trong **không** có nút tạo/sửa nào.

**Dọn dẹp và bàn giao:**
5. Liệt kê mọi mã và phiếu thử còn trên database thật (từ `04-13-SUMMARY.md`,
   `04-14-SUMMARY.md`, và `03-SUMMARY.md`: `PN-UAT-A/B`, `PNM26-000001`, `PX-UAT-A/B`,
   `TRA-UAT-A`, đối tác `UAT Phase 4`). Quyết định với người dùng: đánh dấu ngừng kinh
   doanh, hay xóa. **Không tự xóa** — dữ liệu sản xuất không khôi phục được.

**Ba việc chỉ người dùng làm được, phải xong trước khi UAT Phase 4 có nghĩa** (WU-0):
6. Chạy `/doi-tac/ra-ghi-chu`, rà **17 tên ≥ 10 hóa đơn** (NGỌC 58, TỐT 50, PHƯƠNG 48,
   OANH 45, QUỲNH 42, QUYÊN 39, NHUNG 32, DIỆU 31, MỸ 28, VI 27…) thành đối tác.
   `anh_xa_ghi_chu_kiotviet` hiện **0 dòng** — chưa ai rà tên nào. Không có danh sách người
   nhận thì đơn nào cũng phải tạo đối tác tại chỗ.
7. Đặt `kho_mac_dinh_id` cho **4 mã** còn thiếu (3.266/3.270 đã có). Bốn mã đó sẽ bị chặn
   khi thêm dòng và khi tạo phiếu xuất từ đơn.
8. Xóa ba bản ghi rác UAT Phase 2 trong `doi_tac` nếu còn: `Test UAT plan 13`,
   `UAT Khách plan 18`, `UAT Khách+Sale plan 18` — chúng sẽ hiện trong ô chọn người nhận.

Báo lại: bước nào lệch, quyết định về dữ liệu thử, và ba việc ở bước 6–8 đã làm hay chưa.
  </how-to-verify>
  <action>
Dừng thực thi tại đây. Đưa nguyên văn phần "how-to-verify" ở trên cho người dùng, chờ họ
tự chạy và trả lời. KHÔNG tự đánh giá thay, KHÔNG đi tiếp khi chưa có trả lời. Khi có phản
hồi: ghi nguyên văn vào SUMMARY (kể cả từng dòng console người dùng chép lại), và nếu có
bước lệch thì sửa rồi mời kiểm lại đúng bước đó trước khi khép task.
  </action>
  <verify>
    <human-check>Người dùng trả lời theo đúng resume-signal bên dưới</human-check>
  </verify>
  <acceptance_criteria>
    - Bốn bước điều hướng chạy đúng; console không có `Warning:` của antd, không lỗi đỏ.
    - Thanh tab đáy ở 375px có đúng 4 ô chính + "Khác", không tràn ngang.
    - Có quyết định rõ ràng cho từng mục dữ liệu thử ở bước 5.
    - Ba việc WU-0 ở bước 6–8 được ghi lại trạng thái (đã làm / chưa làm) vào SUMMARY.
  </acceptance_criteria>
  <resume-signal>Gõ "đạt" kèm trạng thái ba việc WU-0, hoặc mô tả bước lệch kèm nguyên văn console.</resume-signal>
  <done>Người dùng đã trả lời "đạt", hoặc mọi bước lệch đã được sửa và kiểm lại xong.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|---|---|
| gõ tay URL → Server Component | Người dùng vượt giao diện bằng thanh địa chỉ |
| menu → quyền | `filterByPermission` chỉ ẩn/hiện; không chặn gì |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-81 | Elevation of Privilege | Route Phase 4 quên gọi `requirePermission` → ai gõ URL cũng vào được | mitigate | `scripts/test-route-permissions.ts` kiểm bằng HTTP thật trên phiên thật cho cả 5 vai trò; bảy route mới đều có dòng riêng; đích 100 ô |
| T-04-82 | Elevation of Privilege | Route mới bị bỏ sót khỏi ma trận → script vẫn báo xanh trong khi trang đó thủng | mitigate | Acceptance criteria đòi đối chiếu `find "src/app/(app)" -name "page.tsx"` với `MA_TRAN` và ghi bảng đối chiếu vào SUMMARY (bẫy 12) |
| T-04-83 | Spoofing | Ma trận báo xanh vì thiếu dữ liệu nên bỏ qua route `[id]`, người đọc tưởng đã kiểm | mitigate | Mỗi nhóm route thiếu dữ liệu in `console.warn` riêng nêu đúng số route bị bỏ qua; SUMMARY ghi lại con số đó |
| T-04-84 | Information Disclosure | Menu hiện mục mà vai trò không có quyền, lộ cấu trúc hệ thống | accept | Cả hai mục mới dùng `view-catalog` — cả bốn vai trò đều xem được. Nút tạo/sửa bên trong ẩn theo `edit-catalog` |
| T-04-85 | Tampering | Dữ liệu thử UAT còn trên database thật, lọt vào báo cáo hoặc vào ô chọn của người dùng | mitigate | Checkpoint bước 5 và 8 liệt kê và quyết định từng mục; **không tự xóa** — dữ liệu sản xuất không khôi phục được |
| T-04-SC | Tampering | Cài gói npm/pip/cargo | accept | Plan này không cài gói mới nào — kể cả bộ icon; `package.json` không nằm trong `files_modified` |
</threat_model>

<verification>
- `npm run check` · `npm run db:test:linked` · `npm run verify:hook` ·
  `npx tsx scripts/test-pure-functions.ts` · `npx tsx scripts/test-excel-reader.ts` ·
  `npx tsx scripts/test-route-permissions.ts` — tất cả exit 0
- pgTAP: 0 `not ok`, 0 `ERROR:`, tổng assert ≥ 225 + assert của tám file test mới
- Ma trận quyền: 0 ô lệch; số ô và số route bị bỏ qua được ghi vào SUMMARY
- `git diff --stat src/shared/components/app-shell.tsx` = 0 dòng đổi
- Checkpoint Task 4: điều hướng đúng, console sạch, ba việc WU-0 được ghi trạng thái
</verification>

<success_criteria>
- `NAV_ITEMS` có "Đặt hàng" và "Xuất kho"; `mobilePriority` tính lại có chủ đích, tối đa 4 ô
- Ma trận quyền route phủ đủ bảy route mới, kể cả `/tra-hang/[id]`
- Toàn bộ bộ kiểm của dự án xanh
- `REQUIREMENTS.md` DDH-03 khớp D-04; requirement Phase 4 đánh dấu xong; XUAT-03/XUAT-08 vẫn để trống
- `ROADMAP.md` Phase 4 hết `TBD`, liệt kê đủ 15 plan
- Dữ liệu thử còn lại và ba việc WU-0 được bàn giao rõ ràng cho người dùng
</success_criteria>

<output>
Create `.planning/phases/04-don-dat-hang-phieu-xuat/04-15-SUMMARY.md` when done.
Ghi lại: bảng "Bộ kiểm cuối" theo khuôn `03-SUMMARY.md` (số assert, số ô quyền route, kết
quả từng script); bảng đối chiếu `page.tsx` với `MA_TRAN`; danh sách dữ liệu thử còn trên
database và quyết định với từng mục; trạng thái ba việc WU-0; và mục **"Chưa làm"** nêu rõ
những gì Phase 4 không đụng tới (XUAT-03 barcode, XUAT-08 màn xuất trên điện thoại, màn
danh sách phiếu trả, gộp mã thật).
</output>
</content>
