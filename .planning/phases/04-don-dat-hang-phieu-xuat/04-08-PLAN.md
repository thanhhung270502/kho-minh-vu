---
phase: 04-don-dat-hang-phieu-xuat
plan: 08
type: execute
wave: 7
depends_on: ["04-06"]
files_modified:
  - src/shared/components/partner-search-input.tsx
  - src/features/sales-order/components/order-table.tsx
  - src/features/sales-order/components/order-table-body.tsx
  - src/features/sales-order/components/order-filter-panel.tsx
  - src/features/sales-order/components/order-toolbar.tsx
  - src/features/sales-order/components/create-order-button.tsx
  - src/app/(app)/dat-hang/page.tsx
autonomous: true
requirements: [DDH-01]

must_haves:
  truths:
    - "Vào /dat-hang thấy danh sách đơn, lọc được, và bộ lọc nằm trên URL"
    - "Chưa có đơn nào thì thấy trạng thái rỗng riêng, không phải màn trắng"
    - "Ô người nhận tìm trên server theo từ khóa, không nạp toàn bộ đối tác"
    - "Gõ ra tên chưa có thì tạo đối tác mới ngay tại chỗ rồi chọn luôn"
    - "Tạo đơn xong nhảy thẳng sang trang chi tiết đơn vừa tạo"
  artifacts:
    - path: "src/app/(app)/dat-hang/page.tsx"
      provides: "Route danh sách đơn, chặn quyền bằng requirePermission"
      contains: "requirePermission"
    - path: "src/features/sales-order/components/order-table.tsx"
      provides: "Điều phối ListLayout + QueryState + đọc/ghi bộ lọc trên URL"
      min_lines: 80
    - path: "src/shared/components/partner-search-input.tsx"
      provides: "Ô tìm người nhận trên server + tạo đối tác tại chỗ (D-03)"
      min_lines: 80
    - path: "src/features/sales-order/components/create-order-button.tsx"
      provides: "Tạo đơn: chọn người nhận + ngày giao dự kiến, cấp số trên server"
  key_links:
    - from: "src/features/sales-order/components/order-table.tsx"
      to: "QueryState"
      via: "bọc query, không render thẳng từ query.data"
      pattern: "QueryState"
    - from: "src/shared/components/partner-search-input.tsx"
      to: "danh_sach_doi_tac"
      via: "tìm server-side theo từ khóa, giới hạn 20 dòng"
      pattern: "searchCustomers|danh_sach_doi_tac"
---

<objective>
Màn danh sách đơn đặt hàng và đường tạo đơn mới.

**D-03 là quyết định cốt lõi của plan này:** ô người nhận là **MỘT ô tìm kiếm duy nhất**
trên `doi_tac`. Gõ ra tên chưa có → nút "Thêm đối tác mới" tạo ngay tại chỗ.
**Không thêm cột text tự do**; `don_dat_hang.doi_tac_id` giữ NOT NULL. Lý do: giữ được
lịch sử giao dịch theo khách và báo cáo "sale nào bán nhiều nhất" ở Phase 5, mà vẫn không
kẹt khi gặp tên mới.

⚠️ **Đừng chép `Select` NCC của `receipt-header.tsx`.** Select đó nạp TOÀN BỘ nhà cung cấp
(~25 dòng, hợp lý cho NCC). Danh sách khách sẽ phình nhanh sau khi người dùng rà xong 17
tên lớn rồi 123 tên đuôi — phải tìm **server-side theo từ khóa**. Analog đúng là
`CustomerSelect` trong `src/features/partners/components/note-actions.tsx`.

Purpose: đây là cửa vào của toàn bộ luồng Phase 4.
Output: route `/dat-hang` chạy được, tạo được đơn rỗng và nhảy sang trang chi tiết.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-CONTEXT.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-06-SUMMARY.md

**Điều kiện dữ liệu — nói trước để khỏi tưởng là lỗi:** đo ngày 20/09, `doi_tac` chỉ có
**24 dòng = 23 NCC + 1 "Khách lẻ"**, và `don_dat_hang` có **0 dòng**. Ô tìm người nhận sẽ
gần như không ra gì cho tới khi người dùng chạy `/doi-tac/ra-ghi-chu` rà 17 tên lớn (việc
của người dùng, không phải của code). Vì vậy **nhánh "tạo đối tác mới tại chỗ" là đường
chính trong những ngày đầu**, không phải nhánh phụ — phải làm cho tử tế.

**Bốn bẫy áp dụng ở plan này:**
- **Bẫy 1:** file nào import từ `antd` phải có `"use client"` ở dòng đầu. Component dạng
  chấm (`Select.Option`, `Form.Item`, `Typography.Title`) gọi từ Server Component sẽ trả
  `undefined` và văng "Element type is invalid".
- **Bẫy 11:** antd v6 bỏ prop của v5 và **chỉ cảnh báo lúc chạy** — build vẫn xanh. Cụ thể
  đã vấp: `Alert message=` → `title`, `Modal maskClosable` → `mask={{ closable }}`,
  `Drawer width` → `size`, `Select options` có `value: null` → dùng `""` rồi đổi sang null
  khi gọi API, `Descriptions items` có `span` cố định trong lưới responsive.
- **Bẫy 4 (antd v6, Select):** mục "tất cả" trong bộ lọc dùng `value: ""`, không dùng `null`.
- **Bẫy 12:** route mới phải thêm dòng vào `scripts/test-route-permissions.ts` — việc đó
  làm ở plan 04-15, nhưng ghi lại route đã tạo vào SUMMARY để plan đó không sót.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ô tìm người nhận trên server, kèm tạo đối tác mới tại chỗ</name>
  <files>src/shared/components/partner-search-input.tsx</files>
  <read_first>
    - src/features/partners/components/note-actions.tsx (component `CustomerSelect` — khuôn tìm server-side: `showSearch`, `filterOption={false}`, `onSearch`, `notFoundContent`)
    - src/features/partners/hooks/useNoteReview.ts (`useCustomerSearch`)
    - src/features/partners/api/note-review.api.ts (`searchCustomers` — gọi `danh_sach_doi_tac` với `p_loai: "KHACH"`, `p_kich_thuoc: 20`)
    - src/features/partners/hooks/usePartners.ts (`useSavePartner`, `useSuggestedPartnerCode`)
    - src/features/partners/schemas/partner.schema.ts (`partnerSchema` — tái dùng, không viết schema mới)
    - src/features/stock-in/components/create-receipt-button.tsx (khuôn Modal có form nhỏ, `onOk` gọi mutation)
  </read_first>
  <action>
Tạo `src/shared/components/partner-search-input.tsx` với `"use client"` ở dòng đầu.
Đặt ở `shared/` chứ không trong `sales-order/` vì màn tạo phiếu xuất không cần đơn
(plan 04-10, XUAT-02) cũng phải chọn người nhận, và CLAUDE.md cấm feature import trực
tiếp từ thư mục nội bộ của feature khác.

Props: `{ value: string | undefined; onChange: (id: string) => void; disabled?: boolean;
autoFocus?: boolean }`.

Phần tìm: dùng lại `useCustomerSearch(query)` của `features/partners` — không viết hook
mới, không gọi RPC trực tiếp. `Select` với `showSearch`, `filterOption={false}`,
`loading={search.isFetching}`, `onSearch={setQuery}`, `options` dạng
`{ value: partner.id, label: \`${partner.code} — ${partner.name}\` }`.
`notFoundContent` hiện hai trạng thái: đang tìm thì "Đang tìm…", không thấy thì câu nhắc
kèm **nút "Thêm đối tác mới"** đặt ngay trong `notFoundContent` (hoặc trong
`dropdownRender` — chọn cách nào thì ghi lý do vào comment).

Phần tạo mới: `Modal` chứa form nhỏ gồm mã (điền sẵn từ `useSuggestedPartnerCode("KHACH", open)`),
tên (điền sẵn bằng từ khóa vừa gõ — đây là chỗ tiết kiệm thao tác nhiều nhất), điện thoại
tùy chọn. Validate bằng `partnerSchema` đã có của `features/partners`, **không viết schema
mới**. `onOk` gọi `useSavePartner()` với `id = null`, lấy `id` trả về rồi gọi `onChange(id)`
và đóng modal — người dùng không phải tìm lại.

Bốn yêu cầu bắt buộc của form (CLAUDE.md bước 5):
- lỗi hiện **theo từng field**, không đổ hết ra toast;
- nút OK `disabled`/`loading` khi đang gửi — bấm 5 lần không được tạo 5 đối tác;
- lỗi server map về đúng field: mã trùng trả PostgREST `23505` → `setError("code", ...)`
  với câu "Mã này đã có, chọn mã khác". Dùng `errorCode(e) === "23505"` từ
  `src/shared/lib/errors.ts` — **không** `instanceof PostgrestError` (bẫy 8);
- submit lỗi thì **giữ nguyên** dữ liệu đã nhập.

Props antd phải theo v6: `Modal` dùng `mask={{ closable: false }}` thay cho `maskClosable`,
`Alert` dùng `title` thay cho `message`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0.
    - Dòng đầu của file là `"use client";`.
    - File chứa `filterOption={false}` và `onSearch` — tìm server-side, không lọc ở client.
    - `grep -c "usePartners(" src/shared/components/partner-search-input.tsx`
      trả `0` — không nạp toàn bộ danh sách đối tác.
    - File dùng lại `partnerSchema` (`grep -c "partnerSchema" …` ≥ 1) và **không** khai
      `z.object(` nào mới cho đối tác.
    - `grep -c "instanceof PostgrestError" …` trả `0`; `grep -c "errorCode\|isPostgrestError" …` ≥ 1.
    - `grep -c "maskClosable" …` trả `0`.
  </acceptance_criteria>
  <done>Ô tìm người nhận dùng được, tạo đối tác mới tại chỗ và chọn luôn đối tác vừa tạo.</done>
</task>

<task type="auto">
  <name>Task 2: Màn danh sách đơn với bộ lọc trên URL</name>
  <files>src/app/(app)/dat-hang/page.tsx, src/features/sales-order/components/order-table.tsx, src/features/sales-order/components/order-table-body.tsx, src/features/sales-order/components/order-filter-panel.tsx, src/features/sales-order/components/order-toolbar.tsx</files>
  <read_first>
    - src/app/(app)/nhap-kho/page.tsx (khuôn route: `requirePermission`, `PageHeader`, ranh giới `Suspense` cho `useSearchParams`)
    - src/features/stock-in/components/receipt-table.tsx (khuôn điều phối `ListLayout` + `QueryState` + đọc/ghi URL)
    - src/features/stock-in/components/receipt-table-body.tsx (khuôn cấu hình cột + `Table` + `pagination`)
    - src/features/stock-in/components/receipt-filter-panel.tsx
    - src/features/stock-in/components/receipt-toolbar.tsx
    - src/shared/components/list-layout.tsx và src/shared/components/query-state.tsx
    - src/features/sales-order/schemas/order.schema.ts + hooks/useOrders.ts + lib/order-status.ts (plan 04-06)
  </read_first>
  <action>
`src/app/(app)/dat-hang/page.tsx` — Server Component theo khuôn `nhap-kho/page.tsx`:
`export const metadata = { title: "Đơn đặt hàng" }`, gọi
`const user = await requirePermission("view-catalog")`, render `PageHeader` với mô tả
ngắn bằng tiếng Việt, rồi `<Suspense fallback={null}><OrderTable canCreate={hasPermission(user.role, "edit-catalog")} /></Suspense>`.
Ranh giới `Suspense` là bắt buộc vì bảng dùng `useSearchParams()`.
Tuyệt đối **không** import `antd` trong file này (bẫy 1) và **không** gọi hàm nào export
từ file có `"use client"` (bẫy 9).

`order-table.tsx` (`"use client"`) — chép khuôn `receipt-table.tsx`:
đọc bộ lọc từ URL bằng `readOrderFilterFromUrl(useSearchParams())`, ghi ngược bằng
`writeOrderFilterToUrl` + `router.replace`, gọi `useOrders(filter)`, bọc kết quả bằng
`<QueryState>` với **đủ bốn trạng thái**:
- loading → skeleton, không phải màn trắng;
- error → thông báo đọc hiểu được **và** nút "Thử lại";
- empty → trạng thái rỗng riêng với câu "Chưa có đơn đặt hàng nào" + nút tạo đơn;
- success → bảng.
Bố cục dùng `ListLayout` (panel lọc trái ≥992px, sập vào `Drawer` dưới 992px) giống
`receipt-table.tsx`.

`order-table-body.tsx` (`"use client"`) — cấu hình cột và `Table`:
Số đơn (link sang `/dat-hang/{id}`) · Ngày đơn · Người nhận · Ngày giao dự kiến ·
Tiến độ (`${shippedQuantity}/${orderedQuantity}`, render từ `OrderRow`, tính khi render
chứ không lưu state) · Trạng thái (`Tag` màu theo `ORDER_STATUS_COLORS`, nhãn theo
`ORDER_STATUS_LABELS`) · Người tạo. **Không có cột tiền.**
`pagination` dùng `total` từ `tong_so_dong` và `ISSUE`-style page size của
`ORDER_PAGE_SIZE`; `showSizeChanger={false}`.
Bảng cuộn ngang **trong khung riêng** (`scroll={{ x: ... }}`), không để cả trang tràn ngang.

`order-filter-panel.tsx` (`"use client"`): trạng thái (`Select` bốn giá trị + mục "Tất cả"
dùng `value: ""`, **không** `null` — bẫy 11), người nhận (dùng lại `PartnerSearchInput` của
Task 1), khoảng ngày (`DatePicker.RangePicker`). Nút "Xóa lọc" hiện khi
`countActiveOrderFilters(filter) > 0`.

`order-toolbar.tsx` (`"use client"`): ô tìm (`q`, debounce theo cách `receipt-toolbar.tsx`
đang làm) + chỗ cắm nút tạo đơn qua prop `addButton?: ReactNode`.

Component nào vượt ~200 dòng thì tách tiếp, không xin phép.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0.
    - `grep -c "antd" "src/app/(app)/dat-hang/page.tsx"` trả `0`.
    - `grep -c "requirePermission" "src/app/(app)/dat-hang/page.tsx"` ≥ 1 và
      `grep -c "Suspense" "src/app/(app)/dat-hang/page.tsx"` ≥ 1.
    - Bốn file component đều mở bằng `"use client";`.
    - `grep -c "QueryState" src/features/sales-order/components/order-table.tsx` ≥ 1.
    - `grep -rc "don_gia\|thanh_tien\|Thành tiền\|Đơn giá" src/features/sales-order/components/`
      trả `0` cho mọi file — đơn không có giá.
    - `grep -c "value: null" src/features/sales-order/components/order-filter-panel.tsx` trả `0`.
    - Không file nào trong `src/features/sales-order/components/` vượt 200 dòng
      (`wc -l src/features/sales-order/components/*.tsx`).
  </acceptance_criteria>
  <done>`/dat-hang` render danh sách, lọc được, bộ lọc nằm trên URL, đủ bốn trạng thái.</done>
</task>

<task type="auto">
  <name>Task 3: Nút tạo đơn mới</name>
  <files>src/features/sales-order/components/create-order-button.tsx</files>
  <read_first>
    - src/features/stock-in/components/create-receipt-button.tsx (toàn bộ — khuôn Modal + form + `router.push` sau khi có id)
    - src/shared/components/partner-search-input.tsx (Task 1)
    - src/features/sales-order/hooks/useOrders.ts (`useCreateOrder`)
    - src/features/sales-order/schemas/order.schema.ts (`orderHeaderSchema`)
    - src/shared/lib/errors.ts
  </read_first>
  <action>
Tạo `src/features/sales-order/components/create-order-button.tsx` (`"use client"`).

`Button` "Tạo đơn" mở `Modal` gồm: ô người nhận (`PartnerSearchInput`, `autoFocus`) và
`DatePicker` ngày giao dự kiến (tùy chọn). **Không có ô số đơn** — số cấp trên server bởi
`sinh_so_dh`, người dùng không gõ.

Validate bằng `orderHeaderSchema`: thiếu người nhận thì lỗi hiện ngay dưới ô đó với câu
"Chọn người nhận trước khi tạo đơn", không phải toast.

`onOk` gọi `useCreateOrder()`, nhận `id`, rồi `router.push(\`/dat-hang/${id}\`)` — tạo đơn
là sinh ngay một đơn có số trên server, y như `createReceipt` của Phase 3, không giữ đơn
nháp ở client.

Bắt lỗi theo mã, đúng khuôn `post-receipt-button.tsx`:
- `errorCode(e) === "42501"` → câu cố định "Tài khoản không có quyền tạo đơn.";
- `isPostgrestError(e) && e.code === "23514"` → hiện **nguyên văn** message của RPC (RPC
  đã soạn sẵn câu tiếng Việt);
- còn lại → `explainError(e)`.
Nút OK `loading` khi đang gửi; đóng modal bị chặn khi đang gửi.

Cắm nút này vào `order-toolbar` qua prop `addButton` ở `order-table.tsx`, và vào trạng
thái rỗng của `QueryState` — người dùng vào màn trống phải thấy đường đi tiếp.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0.
    - Dòng đầu file là `"use client";`.
    - File chứa `useCreateOrder` và `router.push`.
    - `grep -c "so_dh\|sinh_so_dh" src/features/sales-order/components/create-order-button.tsx`
      trả `0` — cấp số là chuyện của lớp `api/`, component không chạm.
    - File xử lý riêng cả `42501` và `23514`, và có nhánh `explainError`.
    - `grep -c "maskClosable" …` trả `0`.
    - `order-table.tsx` truyền `CreateOrderButton` vào cả toolbar lẫn trạng thái rỗng
      (`grep -c "CreateOrderButton" src/features/sales-order/components/order-table.tsx` ≥ 2).
  </acceptance_criteria>
  <done>Tạo được đơn mới từ màn danh sách và từ trạng thái rỗng, xong thì nhảy sang chi tiết.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|---|---|
| Server Component → client | `requirePermission` chạy ở server; nút ẩn/hiện ở client chỉ là trang trí |
| form tạo đối tác → `doi_tac` | Dữ liệu người dùng gõ đi thẳng vào bảng đối tác |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-39 | Elevation of Privilege | Gõ tay `/dat-hang` khi chưa đăng nhập hoặc sai vai trò | mitigate | `await requirePermission("view-catalog")` đầu Server Component — `proxy.ts` chỉ lo phiên, không lo vai trò; plan 04-15 thêm dòng vào ma trận quyền route |
| T-04-40 | Elevation of Privilege | `chi_xem` thấy nút "Tạo đơn" rồi bấm | mitigate | `canCreate = hasPermission(user.role, "edit-catalog")` ẩn nút; chặn thật là policy `tao don dat hang tru chi xem` (0016) và `sinh_so_dh` chặn `chi_xem` (plan 04-02) |
| T-04-41 | Spoofing | Tạo đối tác trùng mã, hai khách gộp làm một trong báo cáo | mitigate | Mã điền sẵn từ `sinh_ma_doi_tac`; trùng mã trả `23505` và map về đúng field với câu tiếng Việt |
| T-04-42 | Denial of Service | Ô tìm người nhận gõ mỗi ký tự là một lượt RPC | mitigate | `useCustomerSearch` dùng lại nguyên của `features/partners` (đã giới hạn 20 dòng); ô tìm `q` của toolbar debounce theo cách `receipt-toolbar.tsx` |
| T-04-43 | Tampering | Người dùng sửa `?trang=` hoặc `?doi_tac=` thành giá trị bậy | mitigate | `readOrderFilterFromUrl` của plan 04-06 lọc mọi tham số; giá trị hỏng thành `null` chứ không vào truy vấn |
| T-04-SC | Tampering | Cài gói npm/pip/cargo | accept | Plan này không cài gói mới nào |
</threat_model>

<verification>
- `npm run check` exit 0
- grep xác nhận: route không import antd, có `requirePermission` + `Suspense`; component
  có `"use client"`; có `QueryState`; không có cột giá; không có `value: null` trong Select
- Không file component nào vượt 200 dòng
</verification>

<success_criteria>
- `/dat-hang` render được với đủ bốn trạng thái loading/error/empty/success
- Bộ lọc đọc và ghi trên URL tiếng Việt không dấu, tải lại trang giữ nguyên
- Ô người nhận tìm trên server, tạo đối tác mới tại chỗ rồi chọn luôn
- Tạo đơn cấp số trên server và chuyển sang `/dat-hang/{id}`
</success_criteria>

<output>
Create `.planning/phases/04-don-dat-hang-phieu-xuat/04-08-SUMMARY.md` when done.
Ghi lại: **route mới đã tạo** (`/dat-hang`) kèm quyền yêu cầu, để plan 04-15 thêm vào ma
trận quyền; và mọi prop antd v6 phải đổi so với khuôn Phase 3.
</output>
</content>
