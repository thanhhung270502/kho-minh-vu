---
phase: 04-don-dat-hang-phieu-xuat
plan: 09
type: execute
wave: 8
depends_on: ["04-08"]
files_modified:
  - src/app/(app)/dat-hang/[id]/page.tsx
  - src/features/sales-order/components/order-detail.tsx
  - src/features/sales-order/components/order-header.tsx
  - src/features/sales-order/components/order-line-table.tsx
autonomous: false
requirements: [DDH-01, DDH-02, XUAT-07]

must_haves:
  truths:
    - "Mở một đơn thấy đầu đơn, dòng đơn, và số đã xuất / còn lại của từng dòng"
    - "Đơn còn TAM thì sửa được đầu đơn và dòng; đơn đã xác nhận thì khóa ở giao diện"
    - "Thêm dòng hoàn toàn bằng bàn phím: gõ mã, Enter, gõ số lượng, Enter, sang dòng mới"
    - "Enter chọn đúng mã gõ đầy đủ, không chọn nhầm mã luân chuyển nhiều"
  artifacts:
    - path: "src/app/(app)/dat-hang/[id]/page.tsx"
      provides: "Route chi tiết đơn, kiểm uuid, chặn quyền"
      contains: "requirePermission"
    - path: "src/features/sales-order/components/order-detail.tsx"
      provides: "Khung trang chi tiết: đầu đơn, bảng dòng, khu vực nút hành động"
    - path: "src/features/sales-order/components/order-header.tsx"
      provides: "Đầu đơn sửa tại chỗ: người nhận, ngày giao dự kiến, ghi chú"
    - path: "src/features/sales-order/components/order-line-table.tsx"
      provides: "Bảng dòng gõ bàn phím, cột Đã xuất / Còn lại, không có cột giá"
      min_lines: 120
  key_links:
    - from: "src/features/sales-order/components/order-line-table.tsx"
      to: "ProductSearchInput"
      via: "dùng lại ô tìm mã dùng chung, đã xử lý bẫy mã khớp tuyệt đối"
      pattern: "ProductSearchInput"
    - from: "src/features/sales-order/components/order-line-table.tsx"
      to: "onKeyDownCapture"
      via: "bắt Enter ở pha capture của div bọc ngoài vì rc-select tự focus lại"
      pattern: "onKeyDownCapture"
---

<objective>
Trang chi tiết đơn: đầu đơn sửa tại chỗ và bảng dòng gõ bàn phím.

**DDH-02 — "đã xuất / còn lại" của từng dòng.** `dong_don` trả `so_luong_dat` và
`so_luong_da_xuat`; `toOrderLine` (plan 04-06) đã tính `remainingQuantity`. Bảng chỉ việc
render, **không lưu state**.

**XUAT-07 — nhập liệu hoàn toàn bằng bàn phím.** Đây là màn đầu tiên nhân bản bảng gõ bàn
phím của Phase 3, và ba bẫy của nó đã được ghi lại đầy đủ. Đọc kỹ `<context>` trước khi gõ.

**Không có cột Đơn giá và cột Thành tiền** — chốt 19/09 câu 7, đơn không mang giá. Hệ quả
đã chấp nhận: không có báo cáo doanh thu. Tồn và giá vốn vẫn đúng vì phiếu xuất lấy giá
vốn từ trigger.

Purpose: đơn nhập xong ở đây rồi mới đi tiếp sang bước duyệt.
Output: `/dat-hang/{id}` dùng được bằng bàn phím.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-CONTEXT.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-PATTERNS.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-06-SUMMARY.md
@.planning/phases/04-don-dat-hang-phieu-xuat/04-08-SUMMARY.md

**Ba bẫy bàn phím — đã tốn thời gian một lần rồi, chép đúng cách đã chữa:**

- **Bẫy 14a — rc-select tự focus lại ô tìm của nó** ngay sau khi Enter chọn option. Lệnh
  `focus()` sang ô kế tiếp đặt trong `onChange` sẽ bị nuốt. Cách chữa: bắt Enter ở
  `onKeyDownCapture` của div bọc ngoài — pha capture chạy TRƯỚC rc-select — rồi
  `preventDefault()` + `stopPropagation()`.
- **Bẫy 14b — `focus()` gọi ngay sau khi mutation resolve** bị vòng render kế tiếp dọn đi.
  Phải `setTimeout(..., 0)` thì con trỏ mới ở lại đúng ô.
- **Bẫy 15 — gợi ý tìm kiếm phải ưu tiên mã khớp tuyệt đối.** `tim_san_pham` xếp
  `lan_phat_sinh_cuoi desc` TRƯỚC `similarity`, nên mã luân chuyển nhiều đè lên mã vừa gõ
  đầy đủ. `ProductSearchInput` của `stock-in` **đã xử lý** chỗ này (tìm mã khớp tuyệt đối
  trước khi lấy kết quả đầu tiên) — **dùng lại nguyên component đó, không viết lại**.

**Bẫy 13 khi tự kiểm bằng công cụ điều khiển trình duyệt:** phím gửi dưới tên `Return` tới
trang với `event.key === ""`, mọi handler `if (e.key !== "Enter") return` đều trượt và màn
hình trông y hệt như đang hỏng. Trước khi kết luận "thư viện không xử lý phím X", đo chính
sự kiện đó bằng `document.addEventListener("keydown", e => console.log(e.key), true)`.

**Bẫy 11 — antd v6:** `Descriptions items` **bỏ `span` cố định** khi nằm trong lưới
responsive; `Alert` dùng `title` chứ không `message`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route chi tiết đơn và khung trang</name>
  <files>src/app/(app)/dat-hang/[id]/page.tsx, src/features/sales-order/components/order-detail.tsx</files>
  <read_first>
    - src/app/(app)/nhap-kho/[id]/page.tsx (khuôn route `[id]`: `await params`, regex UUID, `notFound()`, `requirePermission`, truyền `permissions` xuống)
    - src/features/stock-in/components/receipt-detail.tsx (toàn bộ — khuôn khung trang + khu vực `Space wrap` cho nút hành động)
    - src/shared/components/query-state.tsx
    - src/features/sales-order/hooks/useOrders.ts (plan 04-06)
    - src/features/sales-order/lib/order-status.ts
  </read_first>
  <action>
`src/app/(app)/dat-hang/[id]/page.tsx` — Server Component theo khuôn
`nhap-kho/[id]/page.tsx`: `const { id } = await params` (Next 16: `params` là Promise),
kiểm `id` bằng regex UUID rồi `notFound()` nếu sai, `const user = await
requirePermission("view-catalog")`, render `<OrderDetailView id={id} permissions={{...}} />`.

`permissions` gồm:
- `canEdit: hasPermission(user.role, "edit-catalog")` — tạo/sửa đơn tạm (D-06: văn phòng
  và quản lý).
- `canApprove: user.role === "quan_ly"` — xác nhận, mở khóa, đóng sớm (D-06, D-07). Chặn
  thật ở ba RPC của plan 04-02; đây chỉ để ẩn nút.
Không import `antd` trong file route (bẫy 1). Không gọi hàm export từ file `"use client"`
(bẫy 9) — nếu cần hằng số thì lấy từ `src/features/sales-order/lib/order-status.ts`.

`order-detail.tsx` (`"use client"`) theo khuôn `receipt-detail.tsx`:
- `useOrderDetail(id)` bọc `<QueryState>` đủ bốn trạng thái. Đơn không tồn tại → trạng
  thái rỗng có câu "Không tìm thấy đơn này" + link về `/dat-hang`, **không** màn trắng.
- Tiêu đề trang: `{so_dh}` + `Tag` trạng thái theo `ORDER_STATUS_LABELS`/`ORDER_STATUS_COLORS`.
- Một khu vực `<Space wrap>` cho nút hành động, để trống có chú thích ở plan này — plan
  04-12 sẽ cắm nút xác nhận / mở khóa / đóng sớm / in / tạo phiếu xuất vào đúng chỗ đó.
  Khai nó thành prop `actions?: ReactNode` hoặc một vùng rõ ràng, ghi comment nói plan nào
  sẽ điền.
- Dưới đó: `<OrderHeader>` rồi `<OrderLineTable>`.
- Truyền xuống một cờ duy nhất `editable = permissions.canEdit && order.status === "TAM"`
  — đơn đã xác nhận khóa sửa ở giao diện; database đã chặn bằng policy của plan 04-02, đây
  là lớp thứ hai cho đỡ bực mình chứ không phải lớp bảo vệ.
- Khi `order.status !== "TAM"` và `permissions.canEdit`, hiện `Alert` (prop `title`, không
  `message`) giải thích: đơn đã xác nhận nên khóa sửa, muốn sửa thì nhờ quản lý mở khóa.
  Đây là "nói rõ làm gì tiếp theo", không phải "Có lỗi xảy ra".
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0.
    - `grep -c "antd" "src/app/(app)/dat-hang/[id]/page.tsx"` trả `0`.
    - Route có `await params`, regex UUID, `notFound()`, `requirePermission`
      (`grep -c "await params\|notFound\|requirePermission" …` ≥ 3).
    - `order-detail.tsx` mở bằng `"use client";` và chứa `QueryState`.
    - `grep -c 'status === "TAM"' src/features/sales-order/components/order-detail.tsx` ≥ 1.
    - `grep -c "Alert" src/features/sales-order/components/order-detail.tsx` ≥ 1 và
      `grep -c "message=" …` trả `0` (antd v6 dùng `title`).
    - Có chỗ dành sẵn cho nút hành động, kèm comment nêu tên plan sẽ điền
      (`grep -c "04-12" src/features/sales-order/components/order-detail.tsx` ≥ 1).
  </acceptance_criteria>
  <done>`/dat-hang/{id}` mở được, hiện đầu đơn và chỗ trống cho nút hành động.</done>
</task>

<task type="auto">
  <name>Task 2: Đầu đơn sửa tại chỗ</name>
  <files>src/features/sales-order/components/order-header.tsx</files>
  <read_first>
    - src/features/stock-in/components/receipt-header.tsx (toàn bộ — khuôn `Descriptions` sửa tại chỗ, `editable = trạng thái cho phép && canEdit`, `save(field, values)` gọi mutation rồi hiện "đã lưu" 2 giây)
    - src/shared/components/partner-search-input.tsx (plan 04-08)
    - src/features/sales-order/hooks/useOrders.ts (`useUpdateOrderHeader`)
    - src/features/sales-order/schemas/order.schema.ts
    - src/shared/lib/errors.ts
  </read_first>
  <action>
Tạo `src/features/sales-order/components/order-header.tsx` (`"use client"`) theo khuôn
`receipt-header.tsx`: `Descriptions` với các mục sửa tại chỗ, mỗi mục lưu ngay khi rời ô
rồi hiện dấu "đã lưu" 2 giây.

Các mục:
- **Số đơn** — chỉ đọc.
- **Ngày đơn** — chỉ đọc (cấp lúc tạo).
- **Người nhận** — `PartnerSearchInput` (Task 1 của plan 04-08). **Không** chép `Select` NCC
  nạp toàn bộ của `receipt-header.tsx`.
- **Ngày giao dự kiến** — `DatePicker`, cho phép để trống.
- **Ghi chú** — `Input.TextArea`.
- **Người tạo** và **Trạng thái** — chỉ đọc.

Khi `editable === false`, mọi mục về dạng chỉ đọc, không chỉ `disabled`.

Lưu bằng `useUpdateOrderHeader(id)`. Lớp `api/` đã ném lỗi khi RLS lọc im lặng
(`count` rỗng) — bắt lỗi đó và hiện nguyên văn câu tiếng Việt kèm gợi ý nhờ quản lý mở
khóa. Bắt `42501` riêng. Dùng `errorCode` / `isPostgrestError` / `explainError` từ
`src/shared/lib/errors.ts`, **không** `instanceof PostgrestError` (bẫy 8).

**antd v6:** `Descriptions items` trong lưới responsive **không đặt `span` cố định**
(bẫy 11) — bỏ hẳn prop đó, để antd tự xếp.

Không có mục nào liên quan tới kho — đơn không có kho; kho chỉ xuất hiện khi tạo phiếu
xuất từ đơn (D-13).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0; dòng đầu file là `"use client";`.
    - File dùng `PartnerSearchInput` (`grep -c "PartnerSearchInput" …` ≥ 1) và **không**
      dùng `usePartners(` (`grep -c "usePartners(" …` trả `0`).
    - `grep -c "span:" src/features/sales-order/components/order-header.tsx` trả `0`.
    - `grep -c "kho\|warehouse" src/features/sales-order/components/order-header.tsx` trả `0`.
    - File xử lý riêng `42501` và có nhánh `explainError`.
    - File dưới 200 dòng.
  </acceptance_criteria>
  <done>Đầu đơn sửa được khi đơn còn TAM, khóa khi đã xác nhận, lỗi nói rõ đường phục hồi.</done>
</task>

<task type="auto">
  <name>Task 3: Bảng dòng đơn gõ bàn phím</name>
  <files>src/features/sales-order/components/order-line-table.tsx</files>
  <read_first>
    - src/features/stock-in/components/receipt-line-table.tsx (TOÀN BỘ 366 dòng — đây là analog gần như nguyên vẹn; đặc biệt luồng `onKeyDownCapture`, `setTimeout(..., 0)`, và ghi chú "tính khi render, không giữ state" ở dòng ~207)
    - src/shared/components/product-search-input.tsx (đã nâng lên ở plan 04-05 — cách ưu tiên mã khớp tuyệt đối; DÙNG LẠI, không viết lại)
    - src/features/sales-order/hooks/useOrders.ts (`useAddOrderLine`, `useUpdateOrderLine`, `useDeleteOrderLine`)
    - src/features/sales-order/types.ts (`OrderLine`, `remainingQuantity`)
    - src/features/sales-order/schemas/order.schema.ts (`orderLineSchema`)
  </read_first>
  <action>
Tạo `src/features/sales-order/components/order-line-table.tsx` (`"use client"`) bằng cách
chép khuôn `receipt-line-table.tsx`, rồi đổi ba chỗ:

**Bỏ:** cột "Đơn giá" và cột "Thành tiền", và hàng tổng tiền nếu có. Giữ hàng tổng số lượng.

**Thêm:** hai cột "Đã xuất" và "Còn lại", **chỉ hiện khi đơn đã có phiếu xuất**
(`lines.some(l => l.shippedQuantity > 0)`). `remainingQuantity` lấy thẳng từ `OrderLine`
(đã tính trong mapper của plan 04-06) — tính trong `render` hoặc đọc từ mapper, **tuyệt
đối không lưu vào state** (khuôn đã ghi ở dòng ~207 của `receipt-line-table.tsx`).
Dòng đã giao đủ (`remainingQuantity === 0` và `shippedQuantity > 0`) hiện nhạt đi hoặc có
`Tag` "đã giao đủ" — người nhập biết dòng nào còn phải làm.

**Giữ nguyên từng chi tiết của luồng bàn phím:**
1. `ProductSearchInput` (import từ `@/shared/components/product-search-input`)
   → chọn mã bằng Enter.
2. Enter bắt ở `onKeyDownCapture` của div bọc ngoài, gọi `preventDefault()` +
   `stopPropagation()` — pha capture chạy trước rc-select, nếu để `onChange` xử lý thì
   rc-select sẽ giành lại focus (bẫy 14a).
3. Con trỏ nhảy sang ô số lượng.
4. Enter ở ô số lượng → lưu dòng.
5. Sau khi mutation resolve, `setTimeout(() => productInputRef.current?.focus(), 0)` —
   gọi thẳng `focus()` sẽ bị vòng render kế tiếp dọn đi (bẫy 14b).
Toàn chuỗi không chạm chuột (XUAT-07).

Dòng đã có phiếu xuất thì **không cho xóa** — xóa đi thì `so_luong_da_xuat` treo không
tham chiếu. Nút xóa `disabled` kèm `Tooltip` giải thích.

Khi `editable === false`: ẩn hẳn hàng nhập liệu và nút xóa, bảng về chế độ đọc.

Component này sẽ dài; nếu vượt ~200 dòng thì tách phần cấu hình cột ra
`order-table-columns.ts` (file thuần) hoặc tách hàng nhập liệu ra component riêng — tách
theo trách nhiệm, không cắt ngẫu nhiên.

`ProductSearchInput` đã được nâng lên `src/shared/components/product-search-input.tsx` ở
plan 04-05 — import từ đó, **không** import xuyên feature vào `features/stock-in`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exit 0; dòng đầu file là `"use client";`.
    - `grep -c "onKeyDownCapture" src/features/sales-order/components/order-line-table.tsx` ≥ 1.
    - `grep -c "setTimeout" src/features/sales-order/components/order-line-table.tsx` ≥ 1.
    - `grep -rc "Đơn giá\|Thành tiền\|unitPrice" src/features/sales-order/components/order-line-table.tsx`
      trả `0`.
    - `grep -c "remainingQuantity" …` ≥ 1 và `grep -c "useState.*remaining" …` trả `0`.
    - `grep -c "shared/components/product-search-input" src/features/sales-order/components/order-line-table.tsx` ≥ 1.
    - `git diff --stat src/features/stock-in/` cho thấy **0 file đổi** — plan này không
      chạm `stock-in`.
    - Không file nào trong `src/features/sales-order/components/` vượt 200 dòng.
  </acceptance_criteria>
  <done>Bảng dòng đơn nhập được hoàn toàn bằng bàn phím, hiện đã xuất / còn lại, không có cột giá.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Kiểm luồng bàn phím và console trên trình duyệt</name>
  <files>—</files>
  <read_first>
    - .planning/phases/04-don-dat-hang-phieu-xuat/04-08-SUMMARY.md (route và prop antd đã ghi lại)
  </read_first>
  <what-built>
Trang `/dat-hang/{id}` với đầu đơn sửa tại chỗ và bảng dòng gõ bàn phím. `npm run check`
xanh, nhưng **`npm run check` không chứng minh giao diện chạy** — antd v6 bỏ prop của v5
và chỉ cảnh báo lúc chạy (bẫy 11), còn luồng focus bàn phím thì không có cách nào kiểm
bằng typecheck.
  </what-built>
  <how-to-verify>
`npm run dev`, đăng nhập `vanphong@khominhvu.local`, **mở DevTools Console trước**.

1. Vào `/dat-hang`, bấm "Tạo đơn". Nếu ô người nhận không ra kết quả nào: đúng như dự
   kiến (`doi_tac` mới có 1 khách) — dùng luôn nhánh "Thêm đối tác mới", tạo một đối tác
   thử tên `UAT Phase 4`. Tạo đơn.
2. Trang chi tiết mở ra với số đơn dạng `DH26-000001`, trạng thái "Đơn tạm".
3. Ở bảng dòng: gõ một phần mã hàng → danh sách gợi ý hiện ra → **Enter** → con trỏ nhảy
   sang ô số lượng → gõ `5` → **Enter** → dòng được lưu và con trỏ **quay về ô mã**.
   Lặp thêm hai dòng nữa. Cả chuỗi không chạm chuột.
4. Gõ một mã hàng **đầy đủ, chính xác** rồi Enter ngay. Dòng được thêm phải đúng mã vừa
   gõ, không phải một mã khác gần giống (bẫy 15).
5. Sửa ngày giao dự kiến ở đầu đơn → hiện dấu "đã lưu" → tải lại trang, giá trị còn đó.
6. Thu cửa sổ xuống dưới 992px: bảng cuộn ngang trong khung riêng, cả trang không tràn ngang.
7. Bảng **không** có cột Đơn giá và cột Thành tiền.

Nếu một phím Enter có vẻ không ăn, chạy trước trong Console:
`document.addEventListener("keydown", e => console.log("key=", JSON.stringify(e.key)), true)`
— thấy `key= ""` thì lỗi ở công cụ gõ phím, không phải ở code (bẫy 13).

Báo lại: bước nào lệch, và **nguyên văn** mọi dòng console.
  </how-to-verify>
  <action>
Dừng thực thi tại đây. Đưa nguyên văn phần "how-to-verify" ở trên cho người dùng, chờ họ
tự chạy và trả lời. KHÔNG tự đánh giá thay, KHÔNG đi tiếp khi chưa có trả lời. Khi có phản
hồi: ghi nguyên văn vào SUMMARY (kể cả từng dòng console người dùng chép lại), và nếu có
bước lệch thì sửa rồi mời kiểm lại đúng bước đó trước khi khép task.
  </action>
  <verify>
    <human-check>Người dùng trả lời theo đúng resume-signal bên dưới</human-check>
  </verify>
  <acceptance_criteria>
    - Bảy bước trên chạy đúng mô tả.
    - Console không có `Warning:` nào của antd và không có lỗi đỏ.
    - Bước 4 thêm đúng mã đã gõ đầy đủ.
    - Con trỏ quay về ô mã sau mỗi lần lưu dòng.
  </acceptance_criteria>
  <resume-signal>Gõ "đạt" nếu cả bảy bước đúng, hoặc mô tả bước lệch kèm nguyên văn console.</resume-signal>
  <done>Người dùng đã trả lời "đạt", hoặc mọi bước lệch đã được sửa và kiểm lại xong.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|---|---|
| Server Component → client | `requirePermission` ở server; `editable` ở client chỉ ẩn/hiện |
| ô nhập số lượng → `don_dat_hang_dong` | Người dùng gõ tự do, đi thẳng vào bảng qua PostgREST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-44 | Elevation of Privilege | Sửa dòng đơn đã `DA_XAC_NHAN` bằng cách bỏ qua giao diện | mitigate | Policy `sua dong don dat hang` và `xoa dong don dat hang khi tam` của plan 04-02 đòi cha đang `TAM`; `editable` ở client chỉ là lớp thứ hai |
| T-04-45 | Tampering | Số lượng âm hoặc bằng 0 lọt vào dòng đơn | mitigate | `orderLineSchema` ép `positive()`; ràng buộc `check (so_luong_dat > 0)` của migration 0006 là chốt chặn cuối |
| T-04-46 | Tampering | Xóa dòng đơn đã có phiếu xuất → `so_luong_da_xuat` treo, tiến độ đơn sai | mitigate | Nút xóa `disabled` khi `shippedQuantity > 0`, kèm `Tooltip` giải thích |
| T-04-47 | Spoofing | Enter chọn nhầm mã gần giống → nhập sai hàng vào đơn | mitigate | Dùng lại `ProductSearchInput` đã ưu tiên mã khớp tuyệt đối (bẫy 15); checkpoint Task 4 bước 4 kiểm tay |
| T-04-48 | Information Disclosure | Route `[id]` nhận chuỗi không phải uuid, đẩy lỗi database ra người dùng | mitigate | Regex UUID + `notFound()` trước khi gọi bất cứ gì |
| T-04-SC | Tampering | Cài gói npm/pip/cargo | accept | Plan này không cài gói mới nào |
</threat_model>

<verification>
- `npm run check` exit 0
- grep xác nhận `onKeyDownCapture`, `setTimeout`, không cột giá, không lưu `remaining` vào state
- `ProductSearchInput` đã nâng lên `src/shared/components/`, không còn import xuyên feature
- Checkpoint Task 4: bảy bước chạy đúng, console sạch
</verification>

<success_criteria>
- `/dat-hang/{id}` hiện đầu đơn sửa tại chỗ và bảng dòng, đủ bốn trạng thái
- Đơn `TAM` sửa được; đơn `DA_XAC_NHAN` khóa kèm lời giải thích đường phục hồi
- Thêm dòng hoàn toàn bằng bàn phím, con trỏ quay về ô mã sau mỗi dòng
- Cột "Đã xuất / Còn lại" hiện khi đơn đã có phiếu xuất, tính khi render
- Không có cột giá ở bất kỳ đâu trên màn đơn
</success_criteria>

<output>
Create `.planning/phases/04-don-dat-hang-phieu-xuat/04-09-SUMMARY.md` when done.
Ghi lại: route mới (`/dat-hang/[id]`) cho plan 04-15, và kết quả checkpoint kèm mọi dòng
console.
</output>
</content>
