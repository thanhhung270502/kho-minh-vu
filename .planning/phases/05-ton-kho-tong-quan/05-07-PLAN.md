---
phase: 05-ton-kho-tong-quan
plan: 07
type: execute
wave: 5
depends_on: [05-06]
files_modified:
  - src/app/(app)/ton-kho/page.tsx
  - src/features/inventory/components/stock-table.tsx
  - src/features/inventory/components/stock-filter-panel.tsx
autonomous: true
requirements: [TON-01, TQAN-02]

must_haves:
  truths:
    - "Mở /ton-kho thấy một dòng cho mỗi mã hàng, tồn của Kho 1 và Kho 2 là hai cột riêng cộng cột Tổng"
    - "Lọc theo nhóm hàng, công đoạn, kho, trạng thái tồn và ô tìm gõ không dấu đều đổi kết quả và đổi URL"
    - "Mở /ton-kho?ton=duoi_dinh_muc ra đúng danh sách mã đang dưới định mức"
    - "Màn có đủ bốn trạng thái: đang tải, lỗi kèm nút thử lại, rỗng, có dữ liệu"
    - "Không có cột nào hiện giá vốn hay giá trị tồn"
    - "Bấm mã hàng mở được trang chi tiết mã đó"
  artifacts:
    - path: "src/app/(app)/ton-kho/page.tsx"
      provides: "Route Server Component, chặn quyền, PageHeader, Suspense"
      contains: "requirePermission"
    - path: "src/features/inventory/components/stock-table.tsx"
      provides: "Bảng tồn kho, cột kho dựng động, đọc/ghi bộ lọc trên URL"
      contains: "QueryState"
    - path: "src/features/inventory/components/stock-filter-panel.tsx"
      provides: "Panel lọc nhóm hàng / công đoạn / kho / trạng thái tồn"
      contains: "duoi_dinh_muc"
  key_links:
    - from: "src/features/inventory/components/stock-table.tsx"
      to: "useInventory"
      via: "hook TanStack Query của feature inventory"
      pattern: "useInventory"
    - from: "src/features/inventory/components/stock-table.tsx"
      to: "URL"
      via: "readInventoryFilterFromUrl / writeInventoryFilterToUrl + router.replace"
      pattern: "writeInventoryFilterToUrl"
---

<objective>
Dựng màn tồn kho `/ton-kho` (TON-01, D-01, D-02) — và nhờ bộ lọc `duoi_dinh_muc` dùng chung,
cũng là màn "mã dưới định mức" của tiêu chí 4 (TQAN-02).

Purpose: đau số một người dùng tự nêu — *"không ai biết tồn thật, muốn biết phải hỏi thủ kho
hoặc mở KiotViet"*.

Output: ba file — một route, một bảng, một panel lọc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/features/products/components/product-table.tsx
@src/features/products/components/product-filter-panel.tsx
</context>

<design_decisions>
- **Không dựng route `/ton-kho/duoi-dinh-muc` riêng.** `danh_sach_ton_kho` nhận
  `p_trang_thai_ton = 'duoi_dinh_muc'` (plan 05-01) nên `/ton-kho?ton=duoi_dinh_muc` đã là màn
  đó; `/danh-muc?ton=duoi_dinh_muc` cũng đã chạy từ Phase 2. Thêm một route thứ ba hiển thị
  cùng dữ liệu là nợ kỹ thuật (05-PATTERNS.md Đính chính #2).
- **Cột kho dựng ĐỘNG từ `useLookups().warehouses`**, không gõ cứng hai cột. RPC trả
  `stockByWarehouse` là map theo `kho_id`. Import `useLookups` từ
  `@/features/products/hooks/useProducts` — đúng tiền lệ đã có trong `features/stock-in`
  (bốn file đang import như vậy). Nâng `useLookups` lên `src/shared/` là việc đáng làm nhưng
  KHÔNG làm ở phase này: nó chạm `features/products/api/product.api.ts` mà plan 05-08 đang sở hữu.
- **Không cột giá trị tồn, không cột giá vốn** (D-02).
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: stock-filter-panel.tsx</name>
  <files>src/features/inventory/components/stock-filter-panel.tsx</files>
  <read_first>
    - `src/features/products/components/product-filter-panel.tsx` toàn bộ — khuôn panel, cách
      đổ `lookups`, `Select` với `value: ""` làm "tất cả" rồi đổi sang null khi gọi API
      (antd v6 cảnh báo khi option có `value: null` — Bẫy 11).
    - `src/features/inventory/schemas/inventory.schema.ts` (plan 05-06) — tên trường của
      `InventoryFilter`.
  </read_first>
  <action>
    Tạo `src/features/inventory/components/stock-filter-panel.tsx` với `"use client"` ở dòng đầu
    (file import antd — Bẫy 1).

    Props: `{ filter: InventoryFilter; onChange: (next: InventoryFilter) => void }`.
    Bốn ô lọc: **Nhóm hàng** (`categoryId`), **Công đoạn** (`stageId`), **Kho** (`warehouseId`,
    đổ từ `useLookups().warehouses`), **Tồn** (`stockStatus`, bốn tùy chọn `con_hang` /
    `het_hang` / `am` / `duoi_dinh_muc` với nhãn tiếng Việt). Thêm một nút "Xóa lọc" đưa về
    `DEFAULT_INVENTORY_FILTER`. KHÔNG có ô "Đơn vị tính" (màn tồn không cần) và KHÔNG có ô
    "Kinh doanh" ở panel — giữ `tradingStatus` ở mặc định `active`.

    Mọi thay đổi gọi `onChange({ ...filter, <trường>: giá trị, page: 1 })` — đổi điều kiện
    phải về trang 1, nếu không người dùng đang ở trang 40 sẽ thấy màn rỗng và tưởng là lỗi.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Dòng đầu file đúng là `"use client";`.
    - File chứa cả bốn chuỗi `con_hang`, `het_hang`, `am`, `duoi_dinh_muc`.
    - Không option nào có `value: null` (`grep "value: null"` không ra dòng nào).
    - `npx tsc --noEmit` và `npm run lint` thoát mã 0.
    - File dưới 200 dòng.
  </acceptance_criteria>
  <done>Panel lọc bốn ô chạy được, không option nào `value: null`, file dưới 200 dòng.</done>
</task>

<task type="auto">
  <name>Task 2: stock-table.tsx</name>
  <files>src/features/inventory/components/stock-table.tsx</files>
  <read_first>
    - `src/features/products/components/product-table.tsx` dòng 1-120 — khuôn điều phối:
      `useSearchParams()` → `readFilterFromUrl` → hook → `ListLayout` + `QueryState`,
      `navigate()` ghi lại URL bằng `router.replace(..., { scroll: false })`.
    - `src/shared/components/list-layout.tsx` — props `filterPanel` / `toolbar` /
      `activeFilterCount` / `children`.
    - `src/shared/components/query-state.tsx` — props `query` / `isEmpty` / `emptyDescription`.
    - `src/features/products/components/stock-card.tsx` — cách bọc bảng bằng
      `<div className="overflow-x-auto">` và đặt `scroll={{ x: ... }}`.
    - `src/features/products/components/product-columns.tsx` — `formatNumber` đã có, dùng lại.
  </read_first>
  <action>
    Tạo `src/features/inventory/components/stock-table.tsx`, `"use client"`.

    - Đọc bộ lọc từ URL bằng `readInventoryFilterFromUrl(useSearchParams())`, ghi lại bằng
      `writeInventoryFilterToUrl` + `router.replace`. URL là nguồn sự thật, KHÔNG giữ bộ lọc
      trong `useState`.
    - Gọi `useInventory(filter)` và `useLookups()`.
    - Cột cố định: **Mã hàng** (`code`, bọc `<Link href={`/danh-muc/${row.id}`}>` — đây là
      đường sang thẻ kho của TON-02), **Tên hàng** (`name`, `ellipsis`), **Nhóm**
      (`categoryName`), **Công đoạn** (`stageName`, dùng `stageColor` cho `Tag` nếu có),
      **ĐVT** (`unitName`).
    - Cột động: với mỗi kho trong `lookups.data?.warehouses ?? []` (đã sắp theo `ma`), một cột
      tiêu đề là `warehouse.name`, `align: "right"`, `width: 110`, giá trị
      `row.stockByWarehouse[warehouse.id] ?? 0` qua `formatNumber`.
    - Cột **Tổng** (`totalStock`, in đậm) và cột **Định mức** (`minStock`).
    - Tô màu số: âm màu đỏ, bằng 0 màu xám nhạt, dương để mặc định. Dòng có
      `totalStock < minStock && minStock > 0` gắn thêm một `Tag` "Dưới định mức" ở cột Tổng.
    - **Không cột nào tính hay hiện giá trị tồn** (D-02).
    - Bọc toàn bộ trong `<QueryState>` với `isEmpty={(r) => r.rows.length === 0}` và
      `emptyDescription` nói rõ **chuyện gì xảy ra và làm gì tiếp**: nếu bộ lọc đang bật thì gợi
      ý xóa lọc; nếu không có bộ lọc nào thì nói "Chưa có biến động kho nào" kèm
      `<Link href="/ton-kho/nap-tam">` để quản lý nạp tồn tạm (route do plan 05-10 tạo, cùng
      wave). Cấm câu "Có lỗi xảy ra" — lỗi do `QueryState` lo, kèm nút Thử lại.
    - Phân trang server: `total` lấy từ `result.total`, `pageSize` từ `filter.pageSize`,
      `onChange` ghi lại URL.
    - Bảng bọc `<div className="overflow-x-auto">` và có `scroll={{ x: ... }}` — thủ kho mở trên
      điện thoại, cả trang không được tràn ngang.
    - Nếu file vượt ~200 dòng thì tách phần dựng cột ra `stock-columns.tsx` (thêm vào
      `files_modified` của SUMMARY).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` và `npm run lint` thoát mã 0.
    - File chứa `<QueryState` và `emptyDescription`.
    - `grep -E "gia_von|giaVon|costPrice|giá trị tồn" src/features/inventory/components/stock-table.tsx`
      không ra dòng nào.
    - File KHÔNG chứa `useState` cho bộ lọc (`grep "useState" ` chỉ được phép cho state giao
      diện thuần như hàng đang chọn, không cho điều kiện lọc).
    - File chứa `stockByWarehouse[` (cột kho dựng động) và KHÔNG chứa chuỗi `"K1"`/`"K2"`.
  </acceptance_criteria>
  <done>Bảng đọc/ghi bộ lọc trên URL, cột kho dựng động từ `useLookups`, bọc `QueryState` đủ bốn trạng thái, không cột giá vốn.</done>
</task>

<task type="auto">
  <name>Task 3: route /ton-kho và kiểm trên trình duyệt</name>
  <files>src/app/(app)/ton-kho/page.tsx</files>
  <read_first>
    - `src/app/(app)/danh-muc/page.tsx` toàn bộ (33 dòng) — khuôn route: `metadata`,
      `requirePermission`, `PageHeader`, `<Suspense fallback={null}>` bọc bảng vì bảng dùng
      `useSearchParams()`.
    - `src/features/auth/api/current-user.server.ts` — chữ ký `requirePermission`.
  </read_first>
  <action>
    Tạo `src/app/(app)/ton-kho/page.tsx` — Server Component, KHÔNG `"use client"`, KHÔNG import
    antd (Bẫy 1).

    - `export const metadata: Metadata = { title: "Tồn kho" };`
    - `const user = await requirePermission("view-catalog");` — mọi vai trò xem được; phạm vi
      kho của thủ kho đã bị `danh_sach_ton_kho` siết ở database, không siết lại ở giao diện.
    - `<PageHeader title="Tồn kho" description="Số tồn tính từ chứng từ đã ghi sổ — không sửa
      trực tiếp được." />`
    - `<Suspense fallback={null}><StockTable /></Suspense>`.

    Sau khi viết xong: chạy `npm run dev`, mở `http://localhost:3000/ton-kho`, **mở console
    trình duyệt và đọc** (Bẫy 11: antd v6 bỏ prop của v5, chỉ cảnh báo lúc chạy còn build vẫn
    xanh). Thử lần lượt: đổi từng ô lọc và xem URL đổi theo; refresh trang và xem bộ lọc giữ
    nguyên; mở thẳng `/ton-kho?ton=duoi_dinh_muc`; bấm một mã hàng để sang trang chi tiết.

    Lưu ý khi kiểm bằng công cụ điều khiển trình duyệt: phím giả lập có thể tới trang với
    `event.key === ""` (Bẫy 13) — nếu một tương tác bàn phím trông như hỏng, đo bằng
    `document.addEventListener("keydown", e => console.log(e.key), true)` trước khi kết luận.
  </action>
  <verify>
    <automated>npm run check</automated>
    <human-check>Mở http://localhost:3000/ton-kho: console KHÔNG có cảnh báo antd nào (dạng "Warning: [antd: X] ... deprecated"); đổi ô lọc thì URL đổi theo; refresh giữ nguyên bộ lọc; /ton-kho?ton=duoi_dinh_muc mở được; bấm mã hàng sang đúng trang chi tiết.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check` thoát mã 0.
    - `src/app/(app)/ton-kho/page.tsx` KHÔNG chứa `"use client"` và KHÔNG import từ `antd`.
    - File chứa `requirePermission("view-catalog")` và `<Suspense`.
    - SUMMARY ghi lại nguyên văn nội dung console sau khi mở trang (kể cả khi rỗng — ghi
      "console sạch").
  </acceptance_criteria>
  <done>`/ton-kho` mở được, `npm run check` xanh, console không cảnh báo antd, nội dung console ghi vào SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Người dùng gõ URL → Server Component | Vào thẳng `/ton-kho` bỏ qua menu |
| Giao diện → RPC | Ẩn/hiện ở giao diện không phải là chặn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-27 | Elevation of Privilege | Gõ thẳng `/ton-kho` khi chưa đăng nhập | mitigate | `requirePermission("view-catalog")` trong Server Component + `src/proxy.ts` đẩy về `/dang-nhap?tiep_tuc=`; phủ bởi ma trận quyền route ở plan 05-11 |
| T-05-28 | Information Disclosure | Thủ kho thấy tồn kho không được phân | transfer | Chặn nằm ở `danh_sach_ton_kho` (T-05-01); màn này KHÔNG tự lọc lại ở JS, nên không có đường lách bằng devtools |
| T-05-29 | Information Disclosure | Rò giá vốn qua màn tồn | mitigate | Không cột nào của `InventoryRow` mang giá vốn (D-02); acceptance criteria kiểm bằng grep |
| T-05-SC | Tampering | Cài gói npm mới | mitigate | Không thêm dependency; dùng antd/Tailwind/TanStack đã có |
</threat_model>

<verification>
- `npm run check` xanh.
- `/ton-kho` mở được bằng cả bốn vai trò mẫu, console sạch.
- Bộ lọc đọc/ghi URL đúng; `?ton=duoi_dinh_muc` lọc được.
- Không file nào vượt ~200 dòng.
</verification>

<success_criteria>
- Tiêu chí 1 của ROADMAP Phase 5 đạt: xem tồn cả danh mục, một dòng mỗi mã, kho là cột, lọc và
  phân trang chạy phía server.
- Tiêu chí 4 đạt qua `/ton-kho?ton=duoi_dinh_muc`.
- Commit: `feat(ton-kho): man ton kho theo ma va kho`.
</success_criteria>

<output>
Create `.planning/phases/05-ton-kho-tong-quan/05-07-SUMMARY.md` when done

**Một dòng BẮT BUỘC có trong SUMMARY:** việc bọc bảng bằng `overflow-x-auto` và đặt
`scroll={{ x }}` là **mức tối thiểu CLAUDE.md đòi cho MỌI bảng**, KHÔNG phải đã giao TON-05
(màn tồn kho dùng được trên điện thoại). TON-05 vẫn nằm ở Phase 6 và chưa ai làm — ghi rõ
để lúc lập kế hoạch Phase 6 không tưởng nó xong rồi.
</output>
