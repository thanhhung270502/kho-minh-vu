---
phase: 05-ton-kho-tong-quan
plan: 05
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - src/types/database.types.ts
autonomous: false
requirements: [TON-01, TON-02, TQAN-02]

must_haves:
  truths:
    - "Bốn migration 0058-0061 đã nằm trên database thật, gọi được bằng một phiên người dùng thật"
    - "src/types/database.types.ts biết đủ bốn RPC mới nên lớp dữ liệu client gõ đúng kiểu"
    - "Toàn bộ pgTAP xanh, và kết quả được đếm bằng cách bắt được cả dòng ERROR lẫn dòng not ok"
    - "Thẻ kho đang chạy ở trang chi tiết mã hàng không gãy sau khi drop/create lại hàm"
  artifacts:
    - path: "src/types/database.types.ts"
      provides: "Kiểu sinh lại từ database sau khi đẩy 0058-0061"
      contains: "danh_sach_ton_kho"
  key_links:
    - from: "supabase/migrations/0058..0061"
      to: "database cloud"
      via: "npm run db:push"
      pattern: "db:push"
    - from: "database cloud"
      to: "src/types/database.types.ts"
      via: "npm run db:types"
      pattern: "db:types"
---

<objective>
Đẩy bốn migration của Wave 1 lên database thật, sinh lại kiểu, chạy toàn bộ pgTAP.

Purpose: đây là **plan DUY NHẤT trong Phase 5 được chạy `npm run db:push`**. Chỉ có một
database cloud dùng chung; hai plan cùng đẩy là đè nhau. Mọi việc ở Wave 3 trở đi đều cần
`database.types.ts` đã biết bốn RPC mới.

Output: bốn migration nằm trên cloud, `database.types.ts` cập nhật, báo cáo pgTAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-ton-kho-tong-quan/05-CONTEXT.md
@supabase/README.md
</context>

<environment_warning>
**Bản làm việc hiện tại KHÔNG chạy được ba lệnh của plan này.** Đo ngày 20/09:
không có `node_modules`, không có `.env.local`, Supabase CLI chưa đăng nhập.
`npm run check` xanh cũng KHÔNG chứng minh gì về database, vì kiểu lấy từ một file đã sinh
sẵn chứ không hỏi database.

Việc trang bị máy là việc của con người, không phải việc executor tự làm được — nó cần khóa
bí mật và một phiên đăng nhập Supabase. Vì vậy Task 1 là một checkpoint chặn.
</environment_warning>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [CHẶN] Trang bị bản làm việc và gỡ hai việc ngoài phase</name>
  <files>node_modules/, .env.local, data/kiotviet/DanhSachSanPham_KV*.xlsx (việc của con người, không phải file trong repo)</files>
  <read_first>
    - `.planning/phases/05-ton-kho-tong-quan/05-CONTEXT.md` mục `<open_items>` — năm việc chặn.
    - `supabase/README.md` — cách link project và chạy test trên cloud.
    - `.env.example` — danh sách biến cần có.
    - `data/kiotviet/README.md` — bốn file export và quy ước tên.
  </read_first>
  <what-built>
    Bốn migration `0058`-`0061` và bốn file pgTAP `32`-`35` đã viết xong ở Wave 1, chưa đẩy đi đâu.
  </what-built>
  <how-to-verify>
    Người dùng làm năm việc sau rồi báo lại:

    1. `npm install` — đến khi `node_modules/` tồn tại.
    2. Tạo `.env.local` từ `.env.example` với khóa thật của project `kho-vu-tru`
       (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`,
       `SUPABASE_SECRET_KEY`, `SUPABASE_PROJECT_ID`). Kiểm: `npm run check` chạy tới cùng.
    3. `npx supabase login` rồi `npm run db:link`. Kiểm: `npx supabase migration list --linked`
       liệt kê tới `0057` ở cột remote.
    4. Đặt file `DanhSachSanPham_KV….xlsx` vào `data/kiotviet/` (cần cho plan 05-10; thư mục
       đang trống và nội dung bị gitignore).
    5. **Trả lời: ai đã đẩy migration 0050–0057 lên database thật?** Không phải từ thư mục này.
       Nếu còn một phiên khác đang ghi cùng database thì hai bên sẽ đè nhau — phải chốt xong
       trước khi Task 2 chạy.

    Ngoài ra xác nhận: giao diện Phase 4 (`/dat-hang`, `/xuat-kho`, `/tra-hang`) đã xong chưa?
    Nếu chưa, Phase 5 vẫn chạy được nhưng thẻ kho sẽ thưa dữ liệu thật và ma trận quyền route
    ở plan 05-11 vẫn thiếu dòng của Phase 4 — đó là việc của Phase 4, không phải của plan này.
  </how-to-verify>
  <action>
    Không tự chạy npm install hay tự tạo .env.local — việc này cần khóa bí mật và một phiên
    đăng nhập Supabase mà executor không có. Trình bày danh sách năm việc ở mục
    <how-to-verify> cho người dùng, DỪNG lại chờ trả lời, rồi chạy đúng năm lệnh kiểm ở
    <acceptance_criteria> để xác nhận trước khi sang Task 2.
  </action>
  <verify>
    <automated>test -d node_modules &amp;&amp; test -f .env.local &amp;&amp; ls data/kiotviet/DanhSachSanPham_KV*.xlsx >/dev/null 2>&amp;1 &amp;&amp; npx supabase migration list --linked | grep -q "0057" &amp;&amp; echo GATE-OK</automated>
    <human-check>Người dùng xác nhận bằng chữ ai đã đẩy migration 0050–0057 lên database thật, và cho biết giao diện Phase 4 đã xong chưa.</human-check>
  </verify>
  <acceptance_criteria>
    - `ls node_modules/.package-lock.json` có kết quả.
    - `test -f .env.local` đúng.
    - `npx supabase migration list --linked` in ra dòng `0057` ở cột remote.
    - `ls data/kiotviet/DanhSachSanPham_KV*.xlsx` có kết quả.
    - Người dùng đã trả lời câu hỏi về migration 0050–0057 bằng chữ.
  </acceptance_criteria>
  <resume-signal>Gõ "đã trang bị xong" kèm câu trả lời về 0050–0057, hoặc mô tả chỗ kẹt</resume-signal>
  <done>Bốn điều kiện môi trường xác nhận được bằng lệnh, và câu hỏi về migration 0050–0057 đã có câu trả lời bằng chữ.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] db:push rồi db:types</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - `supabase/README.md`.
    - Bốn file `supabase/migrations/0058_rpc_ton_kho.sql`, `0059_the_kho_ton_luy_ke.sql`,
      `0060_de_xuat_dinh_muc.sql`, `0061_nap_ton_tam.sql` — đọc lại một lượt trước khi đẩy,
      đặc biệt các khối header dán từ `pg_get_functiondef`/`pg_get_constraintdef`.
    - `src/types/database.types.ts` — file SINH RA, cấm sửa tay (CLAUDE.md Bước 3).
  </read_first>
  <action>
    Chạy `npx supabase migration list --linked` trước để chụp trạng thái, ghi vào SUMMARY.
    Nếu remote đã có bất kỳ migration nào ≥ 0058 mà không phải file trong repo này → DỪNG,
    báo người dùng: có phiên khác đang ghi cùng database.

    Chạy `npm run db:push`. Bốn migration phải lên theo đúng thứ tự 0058 → 0061.
    Nếu `0059` báo lỗi vì hàm đang có người phụ thuộc, KHÔNG thêm `cascade` — dừng và báo.

    Chạy `npm run db:types` để sinh lại `src/types/database.types.ts`. Không sửa tay file này
    dù chỉ một ký tự.

    Kiểm nhanh ngay sau đó, bằng một phiên người dùng thật (không phải service role):
    gọi `danh_sach_ton_kho` với `p_kich_thuoc := 1` và `the_kho_san_pham` với một
    `p_san_pham_id` có thật, xác nhận không nhận 42501 và `the_kho_san_pham` trả cột
    `ton_luy_ke`.
  </action>
  <verify>
    <automated>npx supabase migration list --linked | grep -q "0061" &amp;&amp; grep -q "danh_sach_ton_kho" src/types/database.types.ts &amp;&amp; grep -q "ton_luy_ke" src/types/database.types.ts &amp;&amp; grep -q "de_xuat_dinh_muc" src/types/database.types.ts &amp;&amp; grep -q "nap_ton_tam" src/types/database.types.ts &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - `git diff --stat src/types/database.types.ts` cho thấy file đã đổi.
    - `npm run typecheck` vẫn xanh sau khi sinh lại kiểu (hàm mới chưa ai gọi nên không được
      làm hỏng gì; nếu hỏng thì `the_kho_san_pham` đã đổi cột theo cách phá hợp đồng cũ).
    - SUMMARY ghi lại đầu ra `supabase migration list --linked` trước và sau khi đẩy.
  </acceptance_criteria>
  <done>Bốn migration 0058–0061 nằm trên cloud; `database.types.ts` sinh lại có đủ bốn RPC mới; `npm run typecheck` xanh.</done>
</task>

<task type="auto">
  <name>Task 3: Chạy toàn bộ pgTAP và đếm kết quả cho đúng</name>
  <files>supabase/tests/32_danh_sach_ton_kho_test.sql, supabase/tests/33_the_kho_luy_ke_test.sql, supabase/tests/34_dinh_muc_test.sql, supabase/tests/35_nap_ton_tam_test.sql</files>
  <read_first>
    - `supabase/tests/32_danh_sach_ton_kho_test.sql`, `33_the_kho_luy_ke_test.sql`,
      `34_dinh_muc_test.sql`, `35_nap_ton_tam_test.sql`.
    - `supabase/README.md` mục chạy test trên cloud.
  </read_first>
  <action>
    Chạy `npm run db:test:linked`.

    **Đếm kết quả phải bắt cả dòng `ERROR`, không chỉ dòng `not ok`.** Một file chết giữa chừng
    không phát ra `not ok` nào — chỉ đếm `not ok` sẽ báo "0 lỗi" trong khi assertion chưa từng
    chạy. Dùng `npm run db:test:linked 2>&amp;1 | tee /tmp/pgtap.log` rồi đếm riêng
    `grep -c "^not ok" /tmp/pgtap.log` và `grep -ci "error" /tmp/pgtap.log`; cả hai phải là 0.
    Ngoài ra kiểm số assertion thực chạy của bốn file mới ≥ tổng các `plan(n)` đã khai.

    Sửa test hoặc sửa migration cho tới khi xanh. Nếu phải sửa migration thì đẩy lại bằng
    `npm run db:push` (vẫn trong plan này — plan này là chủ sở hữu việc đẩy schema) rồi
    `npm run db:types` lại.

    Ghi vào SUMMARY: tổng số assertion toàn dự án trước và sau Phase 5, để lần sau không ai
    phải đoán.
  </action>
  <verify>
    <automated>npm run db:test:linked 2>&amp;1 | tee /tmp/pgtap-05.log; grep -c "^not ok" /tmp/pgtap-05.log | grep -q "^0$" &amp;&amp; grep -ci "error" /tmp/pgtap-05.log | grep -q "^0$" &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK` (không `not ok`, không `ERROR`).
    - Log chứa cả bốn tên file `32_`, `33_`, `34_`, `35_`.
    - SUMMARY ghi tổng số assertion trước/sau và ghi rõ đã đếm cả dòng `ERROR`.
  </acceptance_criteria>
  <done>`npm run db:test:linked` không có dòng `not ok` và không có dòng `ERROR`; số assertion chạy ≥ tổng `plan(n)` đã khai.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bản làm việc → database sản xuất | `db:push` sửa database thật, không khôi phục được |
| Phiên làm việc khác → cùng database | Hai bên đẩy migration chồng nhau |
| `.env.local` → bundle trình duyệt | Khóa `SUPABASE_SECRET_KEY` tuyệt đối không được mang tiền tố `NEXT_PUBLIC_` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-20 | Tampering | Hai phiên cùng đẩy migration lên một database | mitigate | Task 1 buộc trả lời ai đã đẩy 0050–0057; Task 2 chụp `migration list` trước khi đẩy và dừng nếu thấy ≥ 0058 lạ |
| T-05-21 | Denial of Service | `0059` drop hàm đang có người gọi rồi đẩy hỏng giữa chừng | mitigate | `0059` tự cấp lại quyền trong cùng file; Task 2 cấm thêm `cascade`, dừng và báo thay vì chữa cháy |
| T-05-22 | Information Disclosure | Khóa `service_role`/secret lọt vào biến `NEXT_PUBLIC_*` | mitigate | Task 1 dựng `.env.local` theo đúng `.env.example`; `src/lib/env.ts` đã tách biến server/client |
| T-05-23 | Repudiation | Báo "test xanh" trong khi file test chết giữa chừng | mitigate | Task 3 đếm cả `ERROR` lẫn `not ok`, và so số assertion chạy với `plan(n)` |
| T-05-SC | Tampering | `npm install` kéo về gói lạ | mitigate | Không thêm dependency mới trong Phase 5; `npm install` chỉ cài đúng `package-lock.json` đang có. Nếu `npm audit` báo `uuid@8` qua exceljs — đã phân tích ở CLAUDE.md bẫy 4, KHÔNG hạ cấp |
</threat_model>

<verification>
- `npx supabase migration list --linked` hiển thị tới `0061`.
- `src/types/database.types.ts` có bốn tên RPC mới.
- `npm run db:test:linked` không có `not ok` và không có `ERROR`.
- `npm run check` xanh.
</verification>

<success_criteria>
- Bốn migration của Wave 1 sống trên database thật và gọi được bằng phiên người dùng thật.
- Thẻ kho ở `/danh-muc/{id}` tab "Thẻ kho" vẫn mở được sau khi `the_kho_san_pham` bị drop/create
  lại (kiểm bằng cách mở trang — cột lũy kế chưa hiện vì giao diện là việc của plan 05-08).
- Commit: `chore(db): day migration 0058-0061 va sinh lai kieu`.
</success_criteria>

<output>
Create `.planning/phases/05-ton-kho-tong-quan/05-05-SUMMARY.md` when done
</output>
