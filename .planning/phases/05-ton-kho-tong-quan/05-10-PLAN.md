---
phase: 05-ton-kho-tong-quan
plan: 10
type: execute
wave: 4
depends_on: [05-06]
files_modified:
  - src/features/inventory/lib/read-stock-file.server.ts
  - src/app/api/ton-kho/nap-tam/route.ts
  - src/features/inventory/components/provisional-stock-preview.tsx
  - src/app/(app)/ton-kho/nap-tam/page.tsx
  - src/shared/lib/permissions.ts
autonomous: true
requirements: [TON-01]

must_haves:
  truths:
    - "Quản lý tải file danh mục KiotViet lên và thấy TRƯỚC khi ghi: bao nhiêu mã sẽ nạp, bao nhiêu bị bỏ qua, bao nhiêu lỗi và vì sao"
    - "Bấm nạp thật sinh đúng một chứng từ DIEU_CHINH đã ghi sổ, tồn kho đổi theo"
    - "Chạy lại lần hai không sinh thêm chứng từ nào"
    - "Chỉ quản lý vào được màn này và gọi được route"
    - "File Excel chỉ được đọc ở server"
  artifacts:
    - path: "src/features/inventory/lib/read-stock-file.server.ts"
      provides: "Đọc hai cột ma_hang và ton_kho từ file danh mục KiotViet"
      contains: "readFirstSheet"
    - path: "src/app/api/ton-kho/nap-tam/route.ts"
      provides: "Route handler nhận file, gọi RPC nap_ton_tam ở chế độ kiểm tra hoặc nạp"
      contains: "nap_ton_tam"
    - path: "src/features/inventory/components/provisional-stock-preview.tsx"
      provides: "Màn xem trước ba bước: chọn file → xem kết quả kiểm → nạp thật"
      contains: "che_do"
    - path: "src/app/(app)/ton-kho/nap-tam/page.tsx"
      provides: "Route chỉ quản lý"
      contains: "requirePermission"
  key_links:
    - from: "src/app/api/ton-kho/nap-tam/route.ts"
      to: "RPC nap_ton_tam"
      via: "supabase.rpc với p_chi_kiem_tra"
      pattern: "p_chi_kiem_tra"
    - from: "src/features/inventory/lib/read-stock-file.server.ts"
      to: "@/shared/lib/excel-cell"
      via: "readFirstSheet + readString + readNumber"
      pattern: "@/shared/lib/excel-cell"
---

<objective>
Dựng đường nạp tồn tạm từ file danh mục KiotViet (D-05): một lib đọc file, một route handler,
một màn xem trước, một route trang.

Purpose: `ton_kho` hiện có **0 dòng nào khác 0**. Không nạp thì ba màn của Phase 5 đều trắng và
không UAT được. RPC `nap_ton_tam` đã có từ plan 05-04; đây là phần người dùng chạm vào.

**Nhắc lại ranh giới:** số nạp ở đây là **số tạm chưa đếm thực tế**, KHÔNG phải tồn đầu kỳ
chính thức. Tồn đầu kỳ vẫn do kiểm kê Phase 6 đặt, bằng phiếu điều chỉnh đè lên chứng từ này.
Mọi chữ hiện trên màn phải nói đúng như vậy.

Output: bốn file mới + thêm một giá trị `Permission`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/app/api/danh-muc/gia-von-dau-ky/route.ts
@src/features/products/components/cost-import.tsx
</context>

<design_decisions>
- **Không dùng `readCatalogFile`.** `src/features/products/lib/read-catalog-file.server.ts` kéo
  theo cả bộ máy nhận dạng mẫu file, tách ĐVT–công đoạn, `ImportRowPayload` — nạp tồn tạm không
  phải nghiệp vụ "nhập danh mục". Viết một hàm đọc riêng chỉ lấy hai cột, đúng cách route
  `gia-von-dau-ky` chỉ đọc `ma_hang`/`gia_von`.
- **Vẫn đi qua `readFirstSheet`** của `@/shared/lib/excel-cell` — hàm đó đã tự thử reader stream
  rồi mới tới reader thường. Giữ cả hai đường là lý do file KiotViet đọc được (Bẫy 7; lưu ý
  đường dẫn `o-excel.ts` trong CLAUDE.md là SAI, file thật tên `excel-cell.ts`).
- **Tên cột tồn trong file KiotViet sau chuẩn hóa là `ton_kho`** — xác nhận bằng
  `scripts/import-kiotviet/analyze.ts` dòng 64 và `index.ts` dòng 160 đang đọc đúng khóa đó.
- **Chỉ vai trò `quan_ly`** — khớp với chặn trong RPC (plan 05-04) và khớp tiền lệ
  `gia-von-dau-ky`. Thêm một giá trị `Permission` mới thay vì mượn `manage-warehouses`
  (đúng vai trò nhưng sai ngữ nghĩa, người sau sẽ hiểu nhầm).
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: lib đọc file + route handler POST</name>
  <files>src/features/inventory/lib/read-stock-file.server.ts, src/app/api/ton-kho/nap-tam/route.ts</files>
  <read_first>
    - `src/app/api/danh-muc/gia-von-dau-ky/route.ts` toàn bộ — khuôn: `runtime = "nodejs"`,
      `errorResponse(title, action, status)`, guard vai trò, `formData()`, kiểm đuôi `.xlsx` và
      `MAX_FILE_MB`, `readFirstSheet`, gọi RPC bằng phiên người dùng
      (`createSupabaseServerClient()`), map lỗi bằng `explainError`.
    - `src/shared/lib/excel-cell.ts` — chữ ký `readFirstSheet(source): Promise<ParsedSheet>` với
      `{ headers, rawHeaders, rows }`, và `readString`/`readNumber`.
    - `scripts/import-kiotviet/analyze.ts` dòng 60-66 — bằng chứng khóa cột tồn là `ton_kho`.
    - `src/features/products/lib/excel-template.ts` — hằng `MAX_FILE_MB`.
  </read_first>
  <action>
    **`src/features/inventory/lib/read-stock-file.server.ts`** — CHỈ chạy ở server (dùng
    `node:stream` gián tiếp qua `readFirstSheet`). Không `import "server-only"` (theo đúng lý do
    ghi ở đầu `read-catalog-file.server.ts`: script kiểm phải import được).
    - Export `type ProvisionalStockRow = { ma_hang: string | null; so_luong: number | null }`.
      **Hai khóa này giữ snake_case tiếng Việt có chủ đích** — chúng là hợp đồng jsonb gửi
      thẳng cho RPC `nap_ton_tam`, giống khóa gửi cho `nhap_danh_muc`. Đặt comment nói rõ.
    - Export `async function readStockFile(buf: Buffer): Promise<ProvisionalStockRow[]>`:
      gọi `readFirstSheet`, kiểm `doc.headers` có cả `ma_hang` và `ton_kho`; thiếu thì `throw`
      một Error với câu tiếng Việt nêu tên cột cần có; map mỗi dòng thành
      `{ ma_hang: readString(cells["ma_hang"]), so_luong: readNumber(cells["ton_kho"]) }`.

    **`src/app/api/ton-kho/nap-tam/route.ts`**:
    - `export const runtime = "nodejs";`
    - `requireManager()` kiểu `gia-von-dau-ky`: không có user → 401 `"Phiên đăng nhập đã hết hạn"`;
      `user.role !== "quan_ly"` → 403 với câu giải thích vì sao chỉ quản lý.
    - `POST`: đọc `formData()`, lấy `file`, `che_do` (`"nap"` hay `"kiem_tra"`), và `kho_mac_dinh`
      (chuỗi uuid hoặc rỗng). Kiểm `file instanceof File`, đuôi `.xlsx`, kích thước `MAX_FILE_MB`
      — mỗi lỗi trả `errorResponse` với **tiêu đề + việc cần làm tiếp**, không phải câu chung chung.
    - Đọc file bằng `readStockFile`; lỗi đọc trả 422 kèm `e.message`.
    - Gọi `supabase.rpc("nap_ton_tam", { p_du_lieu: dong as unknown as Json, p_kho_mac_dinh:
      khoMacDinh || undefined, p_chi_kiem_tra: cheDo !== "nap" })` bằng
      `await createSupabaseServerClient()` — phiên của chính người dùng, để RPC kiểm vai trò lần
      nữa ở database.
    - Lỗi RPC: `explainError(error)` → `errorResponse(title, action, kind === "forbidden" ? 403 : 500)`.
    - Thành công: `Response.json({ result: data })`.
    - KHÔNG viết `GET` (không có file mẫu để tải — nguồn là file KiotViet xuất ra).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` và `npm run lint` thoát mã 0.
    - `read-stock-file.server.ts` import từ `@/shared/lib/excel-cell`, KHÔNG import `exceljs`
      trực tiếp và KHÔNG import từ `@/features/products/lib/read-catalog-file.server`.
    - `route.ts` chứa `p_chi_kiem_tra` và `runtime = "nodejs"`.
    - `route.ts` chứa nhánh `user.role !== "quan_ly"` trả 403.
    - Không file client nào import `read-stock-file.server` (`grep -rl "read-stock-file"
      src/features/inventory/components` không ra kết quả).
  </acceptance_criteria>
  <done>Lib đọc hai cột `ma_hang`/`ton_kho` qua `readFirstSheet`; route chặn vai trò khác `quan_ly` bằng 403 và gọi RPC bằng phiên người dùng.</done>
</task>

<task type="auto">
  <name>Task 2: provisional-stock-preview.tsx — xem trước ba bước</name>
  <files>src/features/inventory/components/provisional-stock-preview.tsx</files>
  <read_first>
    - `src/features/products/components/cost-import.tsx` toàn bộ — khuôn: `useReducer` với
      `step: 0 | 1 | 2`, `Upload.Dragger` tự gửi `che_do = "kiem_tra"` ngay khi chọn file,
      ba `<Statistic>` cho `applied`/`skipped`/`errorCount`, hai `<Table>` con cho "Bỏ qua" và
      "Lỗi", nút "Đóng" ở bước 2.
    - `src/features/inventory/hooks/useInventory.ts` — để `invalidateQueries` sau khi nạp xong.
    - `supabase/migrations/0061_nap_ton_tam.sql` — sáu khóa jsonb trả về: `da_nap`, `dat`,
      `bo_qua`, `so_loi`, `chi_tiet_bo_qua`, `loi` (và `chung_tu_id`/`so_ct` khi nạp thật).
  </read_first>
  <action>
    Tạo `src/features/inventory/components/provisional-stock-preview.tsx`, `"use client"`.
    Là một component TRONG THÂN TRANG, không bọc `Modal` — bỏ hẳn props `open`/`onClose` của analog.

    - **Khối cảnh báo trên cùng** (`<Alert type="warning" title=... />`, antd v6 dùng `title`):
      nói rõ số nạp vào đây là **số tạm từ KiotViet, chưa đếm thực tế**; kiểm kê Phase 6 sẽ đè
      lên bằng phiếu điều chỉnh; mã nào đã có chứng từ thật sẽ bị bỏ qua chứ không nạp đè.
    - **Chọn kho áp dụng**: một `Select` đổ từ `useLookups().warehouses` (import từ
      `@/features/products/hooks/useProducts`, đúng tiền lệ của `features/stock-in`), nhãn
      "Kho áp dụng cho mã chưa có kho mặc định". Giá trị `""` là "không chọn" (antd v6 cảnh báo
      với `value: null`), đổi sang `undefined` khi gửi.
    - **Bước 0**: `Upload.Dragger` nhận một file `.xlsx`, `beforeUpload` trả `false` để tự gửi
      bằng `fetch` tới `/api/ton-kho/nap-tam` với `che_do = "kiem_tra"`.
    - **Bước 1**: ba `<Statistic>` — "Sẽ nạp" (`dat`), "Bỏ qua (đã có chứng từ thật)" (`bo_qua`),
      "Lỗi" (`so_loi`); hai bảng con liệt kê `chi_tiet_bo_qua` và `loi` kèm cột lý do. Nút
      "Nạp thật" gửi lại cùng file với `che_do = "nap"`; `disabled` khi `dat === 0`;
      `loading` trong lúc bay.
    - **Bước 2**: hiện `so_ct` của chứng từ vừa tạo kèm link mở phiếu nếu route tồn tại, và một
      dòng nhắc chứng từ mang nhãn `[NAP_TON_TAM]`. Gọi `invalidateQueries` cho
      `inventoryKeys.all` và `["products"]` để màn tồn kho thấy số mới.
    - Lỗi HTTP: đọc `{ title, action }` route trả về và hiện đúng hai phần đó. Cấm nuốt lỗi,
      cấm câu "Có lỗi xảy ra".
    - Nếu file vượt ~200 dòng, tách hai bảng con ra một file phụ và ghi vào SUMMARY.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` và `npm run lint` thoát mã 0.
    - File chứa `che_do` với cả hai giá trị `"kiem_tra"` và `"nap"`.
    - File chứa `<Alert` dùng prop `title`, không dùng `message=`.
    - File chứa ba `<Statistic` và hai bảng con cho `chi_tiet_bo_qua` và `loi`.
    - File KHÔNG chứa chuỗi "Có lỗi xảy ra" và không có `catch {}` rỗng.
  </acceptance_criteria>
  <done>Màn ba bước chạy được, ba `Statistic` và hai bảng con đúng hình dạng jsonb của RPC, không câu lỗi chung chung.</done>
</task>

<task type="auto">
  <name>Task 3: quyền mới + route /ton-kho/nap-tam + kiểm trên trình duyệt</name>
  <files>src/shared/lib/permissions.ts, src/app/(app)/ton-kho/nap-tam/page.tsx</files>
  <read_first>
    - `src/shared/lib/permissions.ts` toàn bộ (42 dòng) — `Permission` union và `PERMISSION_MATRIX`.
    - `src/app/(app)/danh-muc/page.tsx` — khuôn route.
    - `data/kiotviet/README.md` — tên file cần đặt vào thư mục.
  </read_first>
  <action>
    **`src/shared/lib/permissions.ts`**: thêm đúng một giá trị `"load-provisional-stock"` vào
    union `Permission` và vào `PERMISSION_MATRIX` với `["quan_ly"]`. Không đụng giá trị nào khác.
    Comment đầu file đã nói rõ ma trận này chỉ ẩn/hiện giao diện — chặn thật ở RLS/RPC.

    **`src/app/(app)/ton-kho/nap-tam/page.tsx`** — Server Component:
    - `metadata.title = "Nạp tồn tạm"`.
    - `await requirePermission("load-provisional-stock");`
    - `<PageHeader title="Nạp tồn tạm từ KiotViet" description="Số tạm để các màn tồn kho có dữ
      liệu mà kiểm. Kiểm kê sẽ đè lên bằng phiếu điều chỉnh." />`
    - `<ProvisionalStockPreview />` (không cần `Suspense` nếu component không dùng
      `useSearchParams()`; nếu có thì bọc).

    **Kiểm trên trình duyệt**: `npm run dev`, đăng nhập `quanly`, mở `/ton-kho/nap-tam`,
    **mở console đọc cảnh báo antd**. Chọn file `DanhSachSanPham_KV….xlsx` trong
    `data/kiotviet/` (người dùng đã đặt vào ở checkpoint của plan 05-05) → xem kết quả kiểm.
    KHÔNG bấm "Nạp thật" ở đây — việc nạp thật nằm trong luồng UAT của plan 05-11, phải chạy
    đúng thứ tự (nạp trước, rồi mới soi các màn đọc).
    Đăng nhập `vanphong` gõ thẳng `/ton-kho/nap-tam` → phải bị đẩy sang `/khong-du-quyen`.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; npx tsx scripts/test-pure-functions.ts</automated>
    <human-check>Với quanly: /ton-kho/nap-tam mở được, chọn file KiotViet thật ra bảng kết quả kiểm với ba con số (sẽ nạp / bỏ qua / lỗi) và console sạch. Với vanphong: gõ thẳng URL bị đẩy sang /khong-du-quyen.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check` và `npx tsx scripts/test-pure-functions.ts` đều thoát mã 0
      (script này có assertion cho `hasPermission`, thêm giá trị mới không được làm nó đỏ).
    - `grep -c "load-provisional-stock" src/shared/lib/permissions.ts` = 2 (union + ma trận).
    - `git diff src/shared/lib/permissions.ts` chỉ thêm dòng, không sửa/xóa dòng nào có sẵn.
    - `page.tsx` không `"use client"`, không import antd, có
      `requirePermission("load-provisional-stock")`.
    - SUMMARY ghi: ba con số của lần chạy thử chế độ kiểm trên file thật, nội dung console, và
      ghi rõ CHƯA bấm nạp thật.
  </acceptance_criteria>
  <done>Quyền `load-provisional-stock` chỉ cấp cho `quan_ly`; `/ton-kho/nap-tam` chạy chế độ kiểm trên file thật và CHƯA nạp thật.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File Excel người dùng chọn → server | Nội dung và cấu trúc file hoàn toàn không tin được |
| Trình duyệt → `/api/ton-kho/nap-tam` | Route handler là bề mặt công khai, gọi được bằng curl |
| Route handler → RPC | Gọi bằng phiên người dùng, không phải service role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-38 | Elevation of Privilege | `van_phong`/`thu_kho`/`chi_xem` gọi thẳng `/api/ton-kho/nap-tam` bằng curl | mitigate | `requireManager()` trong route (403) + chặn `<> 'quan_ly'` trong RPC (42501). Hai lớp, lớp dưới là lớp thật |
| T-05-39 | Elevation of Privilege | Route handler dùng service role để lách RLS | mitigate | Dùng `createSupabaseServerClient()` (phiên người dùng), KHÔNG dùng `SUPABASE_SECRET_KEY` |
| T-05-40 | Denial of Service | Tải file khổng lồ để làm nghẽn server | mitigate | Chặn `MAX_FILE_MB` và đuôi `.xlsx` trước khi đọc; `readFirstSheet` đọc theo stream |
| T-05-41 | Tampering | File giả mạo nạp số tồn sai cho mã đã có chứng từ thật | mitigate | RPC bỏ qua mọi mã có `kho_movement` (T-05-15); màn xem trước liệt kê rõ mã bị bỏ qua trước khi ghi |
| T-05-42 | Information Disclosure | Đọc Excel lọt vào bundle trình duyệt | mitigate | Hàm đọc nằm trong file `.server.ts` chỉ được route handler import; acceptance criteria kiểm bằng grep |
| T-05-43 | Repudiation | Không biết số tồn hiện tại từ đâu ra | mitigate | Toàn bộ đi qua một chứng từ `DIEU_CHINH` có `so_ct` và nhãn `[NAP_TON_TAM]`; bước 2 hiện số chứng từ cho người nạp |
| T-05-SC | Tampering | Cài gói npm mới để đọc Excel | mitigate | Dùng `exceljs` đã có trong `package.json`, qua `@/shared/lib/excel-cell`. Không thêm dependency |
</threat_model>

<verification>
- `npm run check` và `npx tsx scripts/test-pure-functions.ts` xanh.
- `/ton-kho/nap-tam` mở được bằng `quanly`, bị chặn với ba vai trò còn lại.
- Chế độ kiểm chạy được trên file KiotViet thật, ra ba con số; chưa ghi gì vào database.
- Console sạch.
</verification>

<success_criteria>
- Tiêu chí 5 của ROADMAP Phase 5 có đủ đường đi: từ file KiotViet tới một chứng từ `DIEU_CHINH`
  gắn nhãn, qua một màn xem trước.
- Commit: `feat(ton-kho): man nap ton tam tu file danh muc kiotviet`.
</success_criteria>

<output>
Create `.planning/phases/05-ton-kho-tong-quan/05-10-SUMMARY.md` when done
</output>
