---
phase: 05-ton-kho-tong-quan
plan: 09
type: execute
wave: 5
depends_on: [05-06]
files_modified:
  - src/app/(app)/ton-kho/dinh-muc/page.tsx
  - src/features/inventory/components/reorder-level-table.tsx
autonomous: true
requirements: [TQAN-02]

must_haves:
  truths:
    - "Người duyệt thấy con số đề xuất CÙNG với căn cứ của nó: bao nhiêu ngày dữ liệu, bao nhiêu lần bán, suy theo mã hay theo nhóm"
    - "Màn nói rõ lịch sử chỉ trải 10 ngày và chỉ chạm 1.223/3.266 mã, ngay trước khi người ta bấm duyệt"
    - "Định mức chỉ đổi sau khi người duyệt bấm nút, không tự ghi đè"
    - "Thủ kho và chỉ xem không vào được màn này"
    - "Duyệt xong, các mã đó xuất hiện trong bộ lọc dưới định mức"
  artifacts:
    - path: "src/app/(app)/ton-kho/dinh-muc/page.tsx"
      provides: "Route duyệt định mức, chặn quyền edit-catalog"
      contains: "requirePermission(\"edit-catalog\")"
    - path: "src/features/inventory/components/reorder-level-table.tsx"
      provides: "Bảng đề xuất kèm cột căn cứ, chọn dòng, duyệt hàng loạt"
      contains: "SUGGESTION_BASIS_LABELS"
  key_links:
    - from: "src/features/inventory/components/reorder-level-table.tsx"
      to: "useApplyReorderLevels"
      via: "mutation gọi RPC dat_dinh_muc với danh sách id"
      pattern: "useApplyReorderLevels"
---

<objective>
Dựng màn duyệt đề xuất định mức `/ton-kho/dinh-muc` (TQAN-02, D-04).

Purpose: hiện **0/3.266 mã** có `ton_toi_thieu > 0`, nên cảnh báo "sắp hết" chưa bao giờ kêu.
Không ai ngồi gõ 3.266 dòng — hệ đề xuất, người duyệt.

Output: một route và một bảng duyệt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/features/products/components/stage-suggestions.tsx
@.planning/phases/05-ton-kho-tong-quan/05-CONTEXT.md
</context>

<design_decisions>
- **Là một TRANG riêng, không phải Modal.** `stage-suggestions.tsx` (analog gần nhất) mở trong
  `Modal` vì nó là việc phụ trong luồng CRUD danh mục. Duyệt định mức là một phiên làm việc dài
  trên 3.266 dòng, cần URL riêng để quay lại và cần chỗ cho cột căn cứ.
- **Cảnh báo chất lượng dữ liệu là bắt buộc, không phải trang trí.** 05-CONTEXT.md viết thẳng:
  *"Màn duyệt phải hiện rõ định mức này dựa trên bao nhiêu ngày và bao nhiêu lần bán, để người
  duyệt biết con số nào đáng tin"*. Không có phần này thì người ta duyệt mù 3.266 dòng.
- **Quyền `edit-catalog`** (quản lý + văn phòng) — khớp D-04 và khớp đúng vai trò mà
  `dat_dinh_muc` cho phép ở database. Không thêm giá trị `Permission` mới.
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: reorder-level-table.tsx</name>
  <files>src/features/inventory/components/reorder-level-table.tsx</files>
  <read_first>
    - `src/features/products/components/stage-suggestions.tsx` toàn bộ — khuôn "gợi ý rồi duyệt
      hàng loạt": `Collapse` nhóm theo tiêu chí, `Set<string>` giữ các dòng BỊ BỎ CHỌN (mặc định
      chọn hết), `rowSelection` với `preserveSelectedRowKeys: true`, nút xác nhận ghi rõ số
      lượng (`Áp dụng cho N mã đã chọn`), `App.useApp()` để lấy `modal`/`message`.
    - `src/features/inventory/types.ts` và `hooks/useInventory.ts` (plan 05-06) — `ReorderSuggestion`,
      `SUGGESTION_BASIS_LABELS`, `useReorderSuggestions`, `useApplyReorderLevels`.
    - `src/shared/components/query-state.tsx`.
    - `src/shared/lib/errors.ts` — `explainError`, `errorCode`, `isPostgrestError` (tên ĐÚNG;
      CLAUDE.md bẫy 8 in tên cũ).
  </read_first>
  <action>
    Tạo `src/features/inventory/components/reorder-level-table.tsx`, `"use client"`.

    **Khối cảnh báo trên cùng** — `<Alert type="warning" showIcon title=... description=.../>`.
    Chú ý antd v6 dùng prop `title`, KHÔNG phải `message` (Bẫy 11). Nội dung phải nêu bằng số:
    lịch sử bán dùng để suy định mức chỉ trải **10 ngày (03/09 → 12/09/2026)** và chỉ chạm
    **1.223 trong 3.266 mã**; một đơn lớn bất thường đủ thổi định mức lên gấp đôi; nên chạy lại
    sau vài tháng khi hệ mới có lịch sử của chính nó. Lấy `so_ngay_du_lieu` THẬT từ dòng đầu
    tiên của kết quả RPC thay vì gõ cứng số 10 — dữ liệu lưu trữ có thể được nạp thêm.

    **Bộ lọc nguồn**: một `Segmented`/`Select` chọn `basis` (tất cả · theo mã · trung bình nhóm
    · chưa có dữ liệu), đẩy vào `useReorderSuggestions(basis, page, true)`.

    **Bảng** bọc trong `<QueryState>` đủ bốn trạng thái:
    - Cột: **Mã hàng**, **Tên hàng** (ellipsis), **Nhóm**, **Định mức hiện tại** (`currentLevel`),
      **Đề xuất** (`suggestedLevel`, in đậm), và **cột Căn cứ**.
    - Cột "Căn cứ" là thứ phân biệt màn này với analog: hiện `SUGGESTION_BASIS_LABELS[basis]`
      dưới dạng `Tag` **có màu khác nhau theo độ tin cậy** — `theo_ma` màu xanh, `trung_binh_nhom`
      màu vàng, `khong_du_lieu` màu xám — kèm dòng chữ nhỏ bên dưới ghi bằng số:
      `{dataDays} ngày · {saleCount} lần bán · đã bán {totalSold}` cho `theo_ma`;
      `trung bình của {peersWithHistory} mã cùng nhóm` cho `trung_binh_nhom`;
      `nhóm chưa có mã nào từng bán` cho `khong_du_lieu`.
    - `rowSelection` kiểu `stage-suggestions.tsx`: giữ `Set` các dòng BỊ BỎ CHỌN, mặc định chọn
      hết **trừ** nhóm `khong_du_lieu` (đề xuất 0 thì duyệt cũng vô nghĩa) — đặt comment nói rõ.
      `preserveSelectedRowKeys: true` để đổi trang không mất lựa chọn.
    - Phân trang server theo `result.total`.

    **Nút duyệt**: hiện số lượng trong nhãn (`Duyệt ${selectedIds.length} mã`), mở `Modal.confirm`
    nhắc lại rằng thao tác này ghi thẳng vào danh mục và có ghi nhật ký. Khi chạy: `disabled` +
    `loading` trong lúc mutation bay (bấm năm lần không được gọi RPC năm lần). Thành công:
    `message.success` kèm số dòng thật RPC trả về, rồi để `invalidateQueries` của hook làm mới.
    Thất bại: dùng `explainError(error)` để hiện tiêu đề + việc cần làm; riêng
    `errorCode(error) === "42501"` hiện thông điệp về quyền, `errorCode(error) === "23514"` hiện
    nguyên văn message của RPC (ví dụ vượt 1000 mã mỗi lần). Cấm câu "Có lỗi xảy ra".

    Nếu file vượt ~200 dòng, tách cột ra `reorder-level-columns.tsx` và ghi vào SUMMARY.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` và `npm run lint` thoát mã 0.
    - File chứa `<Alert` với prop `title` và KHÔNG có prop `message=` trên `Alert` (antd v6).
    - File chứa cả ba nhãn nguồn qua `SUGGESTION_BASIS_LABELS` và hiện `dataDays`, `saleCount`.
    - File chứa `preserveSelectedRowKeys`.
    - File chứa `explainError` và `errorCode`; KHÔNG chứa `instanceof PostgrestError`.
    - File KHÔNG chứa chuỗi "Có lỗi xảy ra".
  </acceptance_criteria>
  <done>Bảng duyệt có cột Căn cứ bằng số, cảnh báo chất lượng dữ liệu bằng `Alert title`, xử lý lỗi bằng `explainError`/`errorCode`.</done>
</task>

<task type="auto">
  <name>Task 2: route /ton-kho/dinh-muc và kiểm trên trình duyệt</name>
  <files>src/app/(app)/ton-kho/dinh-muc/page.tsx</files>
  <read_first>
    - `src/app/(app)/danh-muc/page.tsx` — khuôn route (`metadata`, `requirePermission`,
      `PageHeader`, `Suspense`).
    - `src/shared/lib/permissions.ts` — `edit-catalog` = `["quan_ly", "van_phong"]`.
  </read_first>
  <action>
    Tạo `src/app/(app)/ton-kho/dinh-muc/page.tsx` — Server Component, không `"use client"`,
    không import antd.

    - `metadata.title = "Duyệt định mức tồn"`.
    - `await requirePermission("edit-catalog");`
    - `<PageHeader title="Duyệt định mức tồn tối thiểu" description="Hệ đề xuất từ lịch sử bán,
      người duyệt quyết. Định mức chỉ đổi khi bạn bấm duyệt." />`
    - `<Suspense fallback={null}><ReorderLevelTable /></Suspense>`.

    Kiểm trên trình duyệt: `npm run dev`, mở `/ton-kho/dinh-muc` bằng tài khoản `quanly` —
    **mở console đọc cảnh báo antd** (Bẫy 11). Duyệt thử một vài mã, rồi mở
    `/ton-kho?ton=duoi_dinh_muc` xem các mã đó có xuất hiện không (nối trọn vòng TQAN-02).
    Đăng nhập bằng `thukho1` gõ thẳng `/ton-kho/dinh-muc` → phải bị đẩy sang `/khong-du-quyen`.
  </action>
  <verify>
    <automated>npm run check</automated>
    <human-check>Với tài khoản quanly: /ton-kho/dinh-muc mở được, cảnh báo 10 ngày dữ liệu hiện rõ trên cùng, mỗi dòng có cột Căn cứ ghi số ngày và số lần bán, console sạch. Duyệt vài mã rồi mở /ton-kho?ton=duoi_dinh_muc thấy mã vừa duyệt nếu tồn dưới định mức. Với tài khoản thukho1: gõ thẳng URL bị đẩy sang /khong-du-quyen.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check` thoát mã 0.
    - `page.tsx` KHÔNG chứa `"use client"`, KHÔNG import `antd`.
    - `page.tsx` chứa `requirePermission("edit-catalog")`.
    - SUMMARY ghi: nội dung console sau khi mở trang, số mã đã duyệt thử, và kết quả kiểm với
      tài khoản `thukho1`.
  </acceptance_criteria>
  <done>`/ton-kho/dinh-muc` mở được với `quanly`/`vanphong`, bị chặn với `thukho1`, console sạch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Người dùng gõ URL → Server Component | `/ton-kho/dinh-muc` là màn ghi vào danh mục |
| Giao diện → `dat_dinh_muc` | Danh sách id do client gửi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-34 | Elevation of Privilege | Thủ kho/chỉ xem mở màn duyệt bằng cách gõ URL | mitigate | `requirePermission("edit-catalog")` ở Server Component; chặn thật thứ hai ở RPC (T-05-09); ma trận quyền route ở plan 05-11 kiểm bằng HTTP thật cho cả bốn vai trò |
| T-05-35 | Tampering | Bấm duyệt nhiều lần liên tiếp sinh nhiều lần ghi | mitigate | Nút `disabled`+`loading` trong lúc mutation bay; RPC ghi giá trị tính lại nên lần hai là no-op |
| T-05-36 | Repudiation | Định mức đổi mà không rõ ai duyệt và duyệt theo căn cứ nào | mitigate | RPC ghi `nhat_ky_sua` với `nguon = 'dinh_muc'` (T-05-12); màn hiện căn cứ trước khi duyệt |
| T-05-37 | Information Disclosure | Màn duyệt rò giá vốn | mitigate | `ReorderSuggestion` không có trường giá vốn; RPC không trả cột đó |
| T-05-SC | Tampering | Cài gói npm mới | mitigate | Không thêm dependency |
</threat_model>

<verification>
- `npm run check` xanh.
- `/ton-kho/dinh-muc` mở được bằng `quanly` và `vanphong`, bị chặn với `thukho1`/`chixem`.
- Cảnh báo chất lượng dữ liệu hiện trên cùng; mỗi dòng có căn cứ dạng số.
- Console sạch.
</verification>

<success_criteria>
- Tiêu chí 3 của ROADMAP Phase 5 đạt: hệ đề xuất định mức cho từng mã (mã chưa từng bán lấy
  trung bình nhóm), hiện rõ căn cứ, và chỉ ghi sau khi có người duyệt.
- Commit: `feat(dinh-muc): man duyet de xuat dinh muc ton toi thieu`.
</success_criteria>

<output>
Create `.planning/phases/05-ton-kho-tong-quan/05-09-SUMMARY.md` when done
</output>
