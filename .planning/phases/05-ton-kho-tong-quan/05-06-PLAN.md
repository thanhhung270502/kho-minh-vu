---
phase: 05-ton-kho-tong-quan
plan: 06
type: execute
wave: 3
depends_on: [05-05]
files_modified:
  - src/features/inventory/types.ts
  - src/features/inventory/schemas/inventory.schema.ts
  - src/features/inventory/api/inventory.keys.ts
  - src/features/inventory/api/inventory.api.ts
  - src/features/inventory/hooks/useInventory.ts
  - scripts/test-pure-functions.ts
autonomous: true
requirements: [TON-01, TQAN-02]

must_haves:
  truths:
    - "Component và hook của feature tồn kho không bao giờ nhìn thấy tên cột tiếng Việt"
    - "Bộ lọc màn tồn kho sống trên URL: refresh hay gửi link đều giữ nguyên điều kiện"
    - "Mọi cột numeric từ PostgREST được ép về number trước khi ra khỏi lớp api"
    - "Lỗi từ supabase-js được ném lên để TanStack Query bắt, không bị nuốt"
  artifacts:
    - path: "src/features/inventory/types.ts"
      provides: "InventoryRow, ReorderSuggestion và mapper Việt→Anh"
      contains: "export function toInventoryRow"
    - path: "src/features/inventory/schemas/inventory.schema.ts"
      provides: "InventoryFilter, đọc/ghi URL, map sang tham số p_*"
      contains: "export function readInventoryFilterFromUrl"
    - path: "src/features/inventory/api/inventory.api.ts"
      provides: "fetchInventory, fetchReorderSuggestions, applyReorderLevels"
      contains: "danh_sach_ton_kho"
    - path: "src/features/inventory/hooks/useInventory.ts"
      provides: "useInventory, useReorderSuggestions, useApplyReorderLevels"
      contains: "keepPreviousData"
  key_links:
    - from: "src/features/inventory/api/inventory.api.ts"
      to: "RPC danh_sach_ton_kho"
      via: "getSupabaseBrowserClient().rpc"
      pattern: "rpc\\(\"danh_sach_ton_kho\""
    - from: "src/features/inventory/hooks/useInventory.ts"
      to: "inventoryKeys"
      via: "queryKey tập trung"
      pattern: "inventoryKeys"
---

<objective>
Dựng lớp dữ liệu của feature `inventory`: kiểu + mapper, bộ lọc trên URL, query key, hàm gọi
Supabase, hook TanStack Query.

Purpose: đây là ranh giới DUY NHẤT được chạm vào tên cột tiếng Việt (`so_luong`,
`ton_toi_thieu`, `nguon_de_xuat`). Ba màn hình ở Wave 4 đều dựng trên lớp này; làm đúng ở đây
thì ba màn kia không phải nghĩ về database.

Output: năm file mới trong `src/features/inventory/`, cộng phần assertion mới trong
`scripts/test-pure-functions.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/features/products/schemas/filter.schema.ts
@src/features/products/api/product.keys.ts
</context>

<interfaces>
Hợp đồng đã có sẵn trong repo, executor dùng thẳng, không phải đi tìm:

- `src/shared/types.ts` xuất `Page<T> = { rows: T[]; total: number }` (khuôn trả về của
  `fetchProducts`).
- `src/lib/supabase/client.ts` xuất `getSupabaseBrowserClient()` — client DUY NHẤT cho
  Client Component/hook.
- `src/shared/lib/errors.ts` xuất `explainError`, `isPostgrestError`, `errorCode`.
  **Đây mới là tên đúng** — CLAUDE.md bẫy 8 còn in tên cũ `laLoiPostgrest`/`maLoi`, file đó lạc hậu.
- `src/types/database.types.ts` (đã sinh lại ở plan 05-05) có
  `Database["public"]["Functions"]["danh_sach_ton_kho"]`, `["de_xuat_dinh_muc"]`,
  `["dat_dinh_muc"]`, `["nap_ton_tam"]`.
- `src/features/products/hooks/useProducts.ts` xuất `useLookups()` trả `{ warehouses, categories,
  stages, units }` — màn tồn kho dùng lại để dựng cột theo kho và đổ dữ liệu cho bộ lọc.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: types.ts — kiểu miền và mapper Việt→Anh</name>
  <files>src/features/inventory/types.ts</files>
  <read_first>
    - `src/features/products/types.ts` — đặc biệt khối `StockCardRow` (dòng 52-67) và
      `toStockCardRow` (dòng 209-226): khuôn mapper chuẩn của dự án, và thói quen bọc
      `Number()` quanh mọi cột `numeric` (PostgREST trả `numeric` dưới dạng CHUỖI).
    - `src/types/database.types.ts` — đọc đúng hình dạng `Returns` của `danh_sach_ton_kho`
      và `de_xuat_dinh_muc` thay vì đoán.
    - `supabase/migrations/0058_rpc_ton_kho.sql` và `0060_de_xuat_dinh_muc.sql` — danh sách cột thật.
  </read_first>
  <action>
    Tạo `src/features/inventory/types.ts`.

    Khai `type Fn = Database["public"]["Functions"]`, rồi hai kiểu thô nội bộ (KHÔNG export):
    `InventoryRowDb = Fn["danh_sach_ton_kho"]["Returns"][number]` và
    `ReorderSuggestionDb = Fn["de_xuat_dinh_muc"]["Returns"][number]`.

    Export hai kiểu miền, khóa camelCase tiếng Anh:
    - `InventoryRow`: `id`, `code`, `name`, `categoryId`, `categoryName`, `stageId`, `stageCode`,
      `stageName`, `stageColor`, `unitName`, `stockByWarehouse: Record<string, number>`,
      `totalStock: number`, `minStock: number`, `isActive: boolean`, `totalRows: number`.
    - `ReorderSuggestion`: `id`, `code`, `name`, `categoryName`, `currentLevel: number`,
      `suggestedLevel: number`, `basis: SuggestionBasis`, `dataDays: number`, `saleCount: number`,
      `totalSold: number`, `peersWithHistory: number`, `totalRows: number`.

    Export `const SUGGESTION_BASES = ["theo_ma", "trung_binh_nhom", "khong_du_lieu"] as const` và
    `type SuggestionBasis = (typeof SUGGESTION_BASES)[number]`. **Giữ nguyên chuỗi tiếng Việt kèm
    comment nói rõ đây là hợp đồng với database** (cùng lý do như `STOCK_STATUSES` trong
    `filter.schema.ts`). Kèm `SUGGESTION_BASIS_LABELS: Record<SuggestionBasis, string>` với nhãn
    tiếng Việt cho giao diện ("Theo lịch sử bán của mã" · "Trung bình nhóm hàng" ·
    "Chưa có dữ liệu").

    Export `toInventoryRow(row: InventoryRowDb): InventoryRow` và
    `toReorderSuggestion(row: ReorderSuggestionDb): ReorderSuggestion`.
    - `ton_theo_kho` là `jsonb` nên kiểu sinh ra là `Json`: chuyển bằng một hàm nội bộ
      `toStockMap(value: unknown): Record<string, number>` — trả `{}` khi không phải object,
      và `Number(v)` cho từng giá trị. **Cấm `any`, cấm `!`** — dùng `unknown` rồi thu hẹp.
    - Mọi cột numeric (`tong_ton`, `ton_toi_thieu`, `dinh_muc_*`, `tong_da_ban`) bọc `Number()`.
    - `tong_so_dong` → `Number(row.tong_so_dong)`.
    - `nguon_de_xuat` thu hẹp về `SuggestionBasis` bằng cách kiểm tra thành viên của
      `SUGGESTION_BASES`, rơi về `"khong_du_lieu"` nếu database trả giá trị lạ.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` thoát mã 0.
    - `grep -c "any" src/features/inventory/types.ts` không khớp dạng `: any` hay `as any`
      (kiểm bằng `grep -E ":\s*any|as any" src/features/inventory/types.ts` không ra dòng nào).
    - File KHÔNG chứa ký tự `!` dùng làm non-null assertion (`grep -E "\w!\." `không ra dòng nào).
    - Export đủ: `InventoryRow`, `ReorderSuggestion`, `SuggestionBasis`, `SUGGESTION_BASES`,
      `SUGGESTION_BASIS_LABELS`, `toInventoryRow`, `toReorderSuggestion`.
  </acceptance_criteria>
  <done>`InventoryRow` và `ReorderSuggestion` cùng hai mapper biên dịch sạch, không `any`, không `!`, mọi cột numeric bọc `Number()`.</done>
</task>

<task type="auto">
  <name>Task 2: inventory.schema.ts + inventory.keys.ts + assertion hàm thuần</name>
  <files>src/features/inventory/schemas/inventory.schema.ts, src/features/inventory/api/inventory.keys.ts, scripts/test-pure-functions.ts</files>
  <read_first>
    - `src/features/products/schemas/filter.schema.ts` toàn bộ — khuôn `readFilterFromUrl` /
      `writeFilterToUrl` / `toListRpcArgs` / `countActiveFilters`, và cái bẫy `Number(null)`
      bằng 0 chứ không NaN (comment trong `readInt`).
    - `src/features/products/api/product.keys.ts`.
    - `scripts/test-pure-functions.ts` — khuôn assertion bằng `node:assert/strict`, và khối
      đã có cho `readFilterFromUrl` của products.
  </read_first>
  <action>
    **`src/features/inventory/schemas/inventory.schema.ts`:**
    - `InventoryFilter` = `{ q: string; categoryId: string | null; stageId: string | null;
      warehouseId: string | null; stockStatus: StockStatus | null; tradingStatus: TradingStatus;
      sortBy: InventorySortField | null; sortDir: "asc" | "desc"; page: number; pageSize: number }`.
    - **Dùng lại `STOCK_STATUSES` và `TradingStatus` bằng cách import từ
      `@/features/products/schemas/filter.schema`?** KHÔNG — CLAUDE.md cấm feature import trực
      tiếp từ thư mục nội bộ của feature khác. Khai lại `STOCK_STATUSES` trong file này (bốn
      chuỗi y hệt, là hợp đồng với `p_trang_thai_ton`) kèm comment trỏ tới file của products
      để người sau biết hai chỗ phải đi cùng nhau.
    - `InventorySortField = "code" | "name" | "totalStock"`, map sang tên cột database
      `ma_hang` / `ten_hang` / `tong_ton`.
    - `DEFAULT_INVENTORY_FILTER` với `pageSize: 50`, `tradingStatus: "active"`, còn lại null/"".
    - `readInventoryFilterFromUrl(params)` đọc tham số URL **tiếng Việt không dấu**: `q`,
      `nhom`, `cong_doan`, `kho`, `ton`, `kinh_doanh`, `sap_xep`, `sortDir`, `trang`,
      `kich_thuoc`. Chép nguyên hàm `readInt` (kể cả comment về `Number(null)`) và `readUuid`.
    - `writeInventoryFilterToUrl(filter)` chỉ ghi khóa khác mặc định.
    - `toInventoryRpcArgs(filter)` map sang `p_tu_khoa`, `p_nhom_hang_id`, `p_cong_doan_id`,
      `p_kho_id`, `p_trang_thai_ton`, `p_dang_kinh_doanh`, `p_sap_xep`, `p_huong`, `p_trang`,
      `p_kich_thuoc`. Giữ nguyên xử lý `tradingStatus === "all"` → gán `p_dang_kinh_doanh = null`
      TƯỜNG MINH (bỏ trường đi là RPC dùng mặc định `true` và lặng lẽ giấu mã ngừng kinh doanh).
    - `countActiveInventoryFilters(filter)` — không tính ô tìm kiếm.

    **`src/features/inventory/api/inventory.keys.ts`:** `inventoryKeys` với `all`,
    `list(filter)`, `reorderSuggestions(basis, page)`.

    **`scripts/test-pure-functions.ts`:** thêm một khối assertion cho bộ lọc tồn kho, đặt cạnh
    khối của products: đọc URL rỗng ra `DEFAULT_INVENTORY_FILTER`; `?ton=duoi_dinh_muc&kho=<uuid>`
    ra đúng giá trị; `kho` không phải uuid bị bỏ; vòng tròn `write(read(x)) === x` cho một bộ lọc
    đầy đủ; `toInventoryRpcArgs` với `tradingStatus: "all"` cho `p_dang_kinh_doanh === null`
    (chứ không phải `undefined`).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx tsx scripts/test-pure-functions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/test-pure-functions.ts` thoát mã 0 và in dòng kết luận như trước.
    - `grep -c "duoi_dinh_muc" scripts/test-pure-functions.ts` ≥ 1.
    - `src/features/inventory/schemas/inventory.schema.ts` KHÔNG import gì từ
      `@/features/products/...` (`grep "features/products" src/features/inventory/schemas/inventory.schema.ts`
      không ra dòng nào).
    - Tham số URL trong file chỉ gồm chuỗi tiếng Việt không dấu đã liệt kê — không có `category`,
      `warehouse`, `page`.
  </acceptance_criteria>
  <done>Bộ lọc đọc/ghi URL bằng tham số tiếng Việt không dấu, và `scripts/test-pure-functions.ts` xanh với khối assertion mới.</done>
</task>

<task type="auto">
  <name>Task 3: inventory.api.ts + useInventory.ts</name>
  <files>src/features/inventory/api/inventory.api.ts, src/features/inventory/hooks/useInventory.ts</files>
  <read_first>
    - `src/features/products/api/product.api.ts` dòng 1-75 — khuôn `fetchProducts`
      (`if (error) throw error`, lấy `total` từ `raw[0]?.tong_so_dong`).
    - `src/features/products/hooks/useProducts.ts` dòng 27-131 — `keepPreviousData`,
      `useRefreshProducts`, khuôn mutation `invalidateQueries`.
    - File `inventory.keys.ts` và `types.ts` vừa viết.
  </read_first>
  <action>
    **`src/features/inventory/api/inventory.api.ts`** — hàm thuần, không JSX, không hook:
    - `fetchInventory(filter: InventoryFilter): Promise<Page<InventoryRow>>` gọi
      `.rpc("danh_sach_ton_kho", toInventoryRpcArgs(filter))`, `if (error) throw error`,
      map bằng `toInventoryRow`, `total` từ `Number(raw[0]?.tong_so_dong ?? 0)`.
    - `fetchReorderSuggestions(basis: SuggestionBasis | null, page: number):
      Promise<Page<ReorderSuggestion>>` gọi `.rpc("de_xuat_dinh_muc", { p_nguon: basis ??
      undefined, p_chi_khac_hien_tai: true, p_trang: page, p_kich_thuoc: 200 })`.
    - `applyReorderLevels(ids: string[]): Promise<number>` gọi `.rpc("dat_dinh_muc",
      { p_ids: ids })`, `if (error) throw error`, trả `Number(data ?? 0)`.
    - Đầu file đặt một comment ngắn nhắc: đây là lớp DUY NHẤT được chạm tên cột tiếng Việt.

    **`src/features/inventory/hooks/useInventory.ts`** (`"use client"` KHÔNG cần ở đây vì file
    không import antd, nhưng hook chỉ chạy ở client — cứ để không có directive, giống
    `useProducts.ts`):
    - `useInventory(filter)` — `queryKey: inventoryKeys.list(filter)`, `placeholderData:
      keepPreviousData` (đổi trang không nháy trắng).
    - `useReorderSuggestions(basis, page, enabled)` — có `enabled` để không bắn query khi màn
      duyệt chưa mở (Bẫy 10 áp dụng cho mọi hook đọc có tham số có thể rỗng).
    - `useApplyReorderLevels()` — `useMutation` gọi `applyReorderLevels`, `onSuccess`
      `invalidateQueries` cho `inventoryKeys.all` **và** cho `["products"]` (định mức nằm trên
      `san_pham`, bảng danh mục đang hiển thị nó) **và** cho `["audit-log", "san_pham"]`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` và `npm run lint` đều thoát mã 0.
    - `grep -c "if (error) throw error" src/features/inventory/api/inventory.api.ts` = 3.
    - `grep -q "keepPreviousData" src/features/inventory/hooks/useInventory.ts` đúng.
    - `grep -q "enabled" src/features/inventory/hooks/useInventory.ts` đúng.
    - Không file nào trong `src/features/inventory/` import `antd` (lớp dữ liệu phải import được
      từ Server Component).
    - `grep -rE "so_luong|ma_hang|ton_toi_thieu|nguon_de_xuat" src/features/inventory/hooks/`
      không ra dòng nào — component/hook không thấy tên cột tiếng Việt.
  </acceptance_criteria>
  <done>Ba hàm gọi RPC đều `throw` lỗi supabase-js; hook có `keepPreviousData` và `enabled`; không file nào trong feature import antd.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL do người dùng gõ → bộ lọc | Tham số URL là đầu vào không tin được |
| Lớp api → PostgREST | Gọi bằng phiên người dùng, RLS và kiểm vai trò nằm ở database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Tampering | Tham số URL rác (`kho=<chuỗi bậy>`, `trang=-5`) | mitigate | `readUuid` bỏ giá trị không phải uuid; `readInt` kẹp khoảng; RPC kẹp lần nữa ở database |
| T-05-25 | Information Disclosure | Lớp client tự lọc kho thay vì để database lọc | mitigate | `fetchInventory` không lọc gì ở JS; phạm vi kho của thủ kho do `danh_sach_ton_kho` áp (T-05-01) |
| T-05-26 | Elevation of Privilege | Giao diện ẩn nút duyệt định mức nhưng vẫn gọi được `dat_dinh_muc` | transfer | Chặn thật nằm ở RPC (`42501`, plan 05-03); lớp này chỉ ẩn/hiện |
| T-05-SC | Tampering | Cài gói npm mới | mitigate | Plan này KHÔNG thêm dependency nào; toàn bộ dùng gói đã có trong `package.json` |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npx tsx scripts/test-pure-functions.ts` đều xanh.
- `src/features/inventory/` có đúng năm file, không file nào import `antd`.
- `scripts/test-pure-functions.ts` có thêm khối assertion cho bộ lọc tồn kho.
</verification>

<success_criteria>
- Ba màn ở Wave 4 chỉ cần import hook và kiểu từ `features/inventory`, không phải biết RPC nào.
- Commit: `feat(ton-kho): lop du lieu feature inventory`.
</success_criteria>

<output>
Create `.planning/phases/05-ton-kho-tong-quan/05-06-SUMMARY.md` when done
</output>
