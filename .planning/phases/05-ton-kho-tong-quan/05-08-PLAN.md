---
phase: 05-ton-kho-tong-quan
plan: 08
type: execute
wave: 4
depends_on: [05-05]
files_modified:
  - src/features/products/types.ts
  - src/features/products/api/product.api.ts
  - src/features/products/components/stock-card.tsx
autonomous: true
requirements: [TON-02]

must_haves:
  truths:
    - "Mỗi dòng thẻ kho của hệ mới hiện số tồn lũy kế tại đúng thời điểm dòng đó"
    - "Dòng dữ liệu KiotViet cũ hiện dấu gạch ở ô lũy kế, không hiện số 0 gây hiểu nhầm"
    - "Bấm số phiếu mở được đúng chứng từ sinh ra dòng đó"
    - "Thẻ kho vẫn chạy nguyên vẹn mọi thứ đã có: lọc kho, cột giá vốn theo quyền, bút toán đảo, phân trang"
  artifacts:
    - path: "src/features/products/types.ts"
      provides: "StockCardRow có thêm runningBalance"
      contains: "runningBalance"
    - path: "src/features/products/components/stock-card.tsx"
      provides: "Cột Tồn lũy kế + link số phiếu sang chứng từ"
      contains: "Tồn lũy kế"
  key_links:
    - from: "src/features/products/types.ts"
      to: "RPC the_kho_san_pham cột ton_luy_ke"
      via: "toStockCardRow map ton_luy_ke → runningBalance"
      pattern: "ton_luy_ke"
    - from: "src/features/products/components/stock-card.tsx"
      to: "trang chi tiết chứng từ"
      via: "Link theo documentId và docType"
      pattern: "DOC_TYPE_TO_ROUTE"
---

<objective>
Mở rộng thẻ kho ĐÃ CHẠY: thêm cột tồn lũy kế và link mở chứng từ (TON-02, D-03).

Purpose: phản hồi business 19/09 — *"Chi tiết: tồn tại thời điểm đó — chưa có"*. Cột lũy kế đã
có ở database từ plan 05-02; việc còn lại là đưa nó ra màn hình và nối mỗi dòng về đúng chứng
từ sinh ra nó.

**Đây là plan SỬA, không phải plan DỰNG MỚI.** 05-WORK-UNITS.md WU-8 liệt kê một component mới
trong `features/inventory` — sai. Thẻ kho đã tồn tại đầy đủ tại
`src/features/products/components/stock-card.tsx` và đã gắn vào tab "Thẻ kho" của
`product-detail.tsx` (dòng 202-214). Dựng bản thứ hai sẽ tạo một component không ai render,
đồng thời để bản đang chạy thiếu cột lũy kế (05-PATTERNS.md Đính chính #1).

Output: ba file sửa, KHÔNG file mới, KHÔNG đụng `src/app/(app)/danh-muc/[id]/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/features/products/components/stock-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: types.ts + product.api.ts — nhận cột ton_luy_ke</name>
  <files>src/features/products/types.ts, src/features/products/api/product.api.ts</files>
  <read_first>
    - `src/features/products/types.ts` dòng 52-67 (`StockCardRow`) và 209-226 (`toStockCardRow`).
    - `src/features/products/api/product.api.ts` dòng 56-74 (`fetchStockCard`) và khối comment
      dòng 24-27 (vì sao mọi truy vấn phải liệt kê cột — migration 0029).
    - `src/types/database.types.ts` — xác nhận `the_kho_san_pham` đã có `ton_luy_ke` sau plan 05-05.
      Nếu CHƯA có thì plan 05-05 chưa chạy xong: dừng, báo về orchestrator.
  </read_first>
  <action>
    `src/features/products/types.ts`:
    - Thêm `runningBalance: number | null` vào `StockCardRow`. Kiểu **nullable là có chủ đích**:
      dòng nguồn `KIOTVIET_NHAP`/`KIOTVIET_BAN` trả `null` vì chúng không nằm trong sổ cái hệ
      mới (lý do đầy đủ ở plan 05-02). Đặt một comment ngắn tại chỗ nói điều đó.
    - Trong `toStockCardRow`, map `runningBalance: row.ton_luy_ke === null ? null :
      Number(row.ton_luy_ke)`. KHÔNG bọc `Number()` trần — `Number(null)` ra 0 và sẽ hiện "0"
      ở mọi dòng KiotViet, đúng kiểu hiểu nhầm mà cột này sinh ra để tránh.

    `src/features/products/api/product.api.ts`: `fetchStockCard` gọi RPC nên không liệt kê cột,
    không phải sửa gì ngoài việc xác nhận nó vẫn biên dịch với kiểu mới. Nếu `tsc` báo lỗi ở
    đây thì sửa tại chỗ, không đổi chữ ký hàm.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` thoát mã 0.
    - `grep -n "runningBalance" src/features/products/types.ts` ra đúng hai dòng (khai kiểu + map).
    - `grep "Number(row.ton_luy_ke)" src/features/products/types.ts` chỉ xuất hiện bên trong một
      biểu thức có kiểm `=== null` trước đó.
    - Mười bốn trường cũ của `StockCardRow` giữ nguyên tên (`git diff` không xóa trường nào).
  </acceptance_criteria>
  <done>`StockCardRow.runningBalance` là `number | null` và map đúng, 14 trường cũ giữ nguyên, `tsc` xanh.</done>
</task>

<task type="auto">
  <name>Task 2: stock-card.tsx — cột Tồn lũy kế và link số phiếu</name>
  <files>src/features/products/components/stock-card.tsx</files>
  <read_first>
    - `src/features/products/components/stock-card.tsx` toàn bộ (~160 dòng) — mảng `columns`,
      cột "Số phiếu" ở dòng 51, cột "Bút toán đảo" ở dòng 88-95, `SOURCE_LABELS` ở dòng 15-19.
    - Kết quả của `ls "src/app/(app)"` — danh sách route chứng từ THẬT đang có trong repo.
  </read_first>
  <action>
    **Cột mới:** chèn một cột `{ title: "Tồn lũy kế", dataIndex: "runningBalance", width: 120,
    align: "right" }` NGAY TRƯỚC cột "Bút toán đảo" (sau cột giá vốn có điều kiện).
    `render` trả `value === null ? <span className="text-gray-400">—</span> : formatNumber(value)`.
    Thêm một `Tooltip` hoặc dòng chú thích dưới bảng giải thích: dấu gạch nghĩa là dòng lưu trữ
    KiotViet, không nằm trong sổ cái hệ mới nên không cộng vào lũy kế.

    **Link số phiếu:** khai một hằng ở đầu file:
    `const DOC_TYPE_TO_ROUTE: Record<string, string>` — khóa là giá trị `chung_tu.loai_ct`
    (`NHAP`, `XUAT`, `TRA_NCC`, `TRA_KHACH`, `CHUYEN_KHO`, `KIEM_KE`, `DIEU_CHINH`).
    **Chỉ đưa vào map những loại đã có route THẬT trong `src/app/(app)/`** — chạy
    `ls "src/app/(app)"` để biết; hôm nay mới có `nhap-kho`. Các loại còn lại để trong một
    comment kèm ghi chú "route đến cùng giao diện Phase 4/6", KHÔNG đưa vào map — link tới route
    chưa tồn tại là một trang 404 giữa luồng UAT.

    Đổi cột "Số phiếu" thành `render: (value: string, row: StockCardRow) => { const base =
    DOC_TYPE_TO_ROUTE[row.docType]; return row.documentId && base ? <Link href={`${base}/${row.documentId}`}>{value}</Link>
    : value; }`. Dòng KiotViet có `documentId` null nên tự động ra chữ thường, không link.

    Không đổi bất kỳ cột nào khác, không đổi props của component (`productId`, `canViewCost`),
    không đụng `product-detail.tsx`.

    Nếu file vượt ~200 dòng sau khi thêm, tách mảng `columns` ra
    `src/features/products/components/stock-card-columns.tsx` và ghi file mới vào SUMMARY.
  </action>
  <verify>
    <automated>npm run check</automated>
    <human-check>Mở http://localhost:3000/danh-muc rồi bấm một mã có biến động → tab "Thẻ kho": cột "Tồn lũy kế" hiện số ở dòng hệ thống và dấu "—" ở dòng KiotViet; số của dòng trên cùng khớp cột Tổng của mã đó ở /ton-kho; bấm số phiếu của một dòng NHAP mở đúng phiếu nhập; console không có cảnh báo antd.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check` thoát mã 0.
    - File chứa chuỗi `"Tồn lũy kế"` và hằng `DOC_TYPE_TO_ROUTE`.
    - Mọi giá trị trong `DOC_TYPE_TO_ROUTE` tương ứng một thư mục thật dưới `src/app/(app)/`
      — kiểm bằng cách đối chiếu với `ls "src/app/(app)"`, ghi kết quả đối chiếu vào SUMMARY.
    - `git diff --stat` chỉ chạm ba file trong `files_modified` (hoặc bốn nếu tách cột).
    - `src/app/(app)/danh-muc/[id]/page.tsx` và `src/features/products/components/product-detail.tsx`
      KHÔNG nằm trong diff.
  </acceptance_criteria>
  <done>Cột "Tồn lũy kế" hiện số ở dòng hệ thống và dấu "—" ở dòng KiotViet; số phiếu link được với loại chứng từ đã có route thật.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC → giao diện thẻ kho | `ton_luy_ke` và `gia_von_tai_thoi_diem` đều do database quyết định có trả hay không |
| Link chứng từ → route chi tiết | `documentId` đi thẳng vào URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-30 | Information Disclosure | Thủ kho thấy lũy kế gộp cả kho không được phân | transfer | Chặn nằm ở RPC (T-05-05); giao diện hiện đúng thứ RPC trả |
| T-05-31 | Information Disclosure | Cột giá vốn lộ cho vai trò không có quyền | mitigate | Giữ nguyên nhánh `canViewCost` đang có; plan này không chạm nhánh đó |
| T-05-32 | Elevation of Privilege | Link tới chứng từ của kho khác | transfer | Trang chi tiết chứng từ tự `requirePermission` và RLS chặn ở database; thẻ kho chỉ liệt kê dòng người dùng đã được phép thấy |
| T-05-33 | Denial of Service | Link tới route chưa tồn tại làm gãy luồng UAT | mitigate | `DOC_TYPE_TO_ROUTE` chỉ chứa loại có thư mục route thật; acceptance criteria bắt đối chiếu `ls` |
| T-05-SC | Tampering | Cài gói npm mới | mitigate | Không thêm dependency |
</threat_model>

<verification>
- `npm run check` xanh.
- Tab "Thẻ kho" mở được, có cột lũy kế, console sạch.
- Diff không chạm `product-detail.tsx` hay `danh-muc/[id]/page.tsx`.
</verification>

<success_criteria>
- Tiêu chí 2 của ROADMAP Phase 5 đạt: thẻ kho có tồn lũy kế tại thời điểm và link mở chứng từ.
- Commit: `feat(the-kho): hien ton luy ke va link mo chung tu`.
</success_criteria>

<output>
Create `.planning/phases/05-ton-kho-tong-quan/05-08-SUMMARY.md` when done
</output>
