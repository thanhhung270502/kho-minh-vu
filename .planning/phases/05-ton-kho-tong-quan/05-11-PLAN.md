---
phase: 05-ton-kho-tong-quan
plan: 11
type: execute
wave: 5
depends_on: [05-07, 05-08, 05-09, 05-10]
files_modified:
  - src/shared/lib/navigation.ts
  - src/shared/components/nav-icons.tsx
  - scripts/test-route-permissions.ts
autonomous: false
requirements: [TON-01, TON-02, TQAN-02]

must_haves:
  truths:
    - "Tồn kho có mục riêng trên menu trái và trên thanh tab đáy của điện thoại"
    - "Ba route mới của Phase 5 được kiểm quyền bằng HTTP thật cho cả bốn vai trò cộng khách vãng lai"
    - "Toàn bộ bộ kiểm của dự án xanh: check, pgTAP, verify:hook, hàm thuần, đọc Excel, quyền route"
    - "Năm tiêu chí của ROADMAP Phase 5 được người dùng xác nhận trên dữ liệu thật, theo đúng thứ tự nạp trước xem sau"
  artifacts:
    - path: "src/shared/lib/navigation.ts"
      provides: "Mục Tồn kho trong NAV_ITEMS"
      contains: "/ton-kho"
    - path: "scripts/test-route-permissions.ts"
      provides: "Ba dòng ma trận mới + phép thử POST cho route nạp tạm"
      contains: "/ton-kho/dinh-muc"
  key_links:
    - from: "src/shared/lib/navigation.ts"
      to: "src/shared/components/nav-icons.tsx"
      via: "NavIconId mới phải có case tương ứng"
      pattern: "NavIconId"
    - from: "scripts/test-route-permissions.ts"
      to: "requirePermission trong từng page.tsx"
      via: "HTTP thật trên phiên thật"
      pattern: "MA_TRAN"
---

<objective>
Nối Phase 5 vào ứng dụng: menu, ma trận quyền route, chạy toàn bộ bộ kiểm, rồi UAT.

Purpose: ba màn đã dựng xong nhưng chưa có lối vào, và chưa vai trò nào bị kiểm bằng HTTP thật.
Bẫy 12 đã cắn một lần: ma trận thiếu đúng `/cai-dat` nên script báo 45/45 xanh trong khi trang
đó crash.

Output: mục menu, ba dòng ma trận, một phiên UAT có biên bản.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/shared/lib/navigation.ts
@scripts/test-route-permissions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mục Tồn kho trên menu</name>
  <files>src/shared/lib/navigation.ts, src/shared/components/nav-icons.tsx</files>
  <read_first>
    - `src/shared/lib/navigation.ts` toàn bộ — `NavIconId`, `NAV_ITEMS`, `mobilePriority`,
      `splitMobileItems` (tối đa 4 ô ở thanh tab đáy, phần dư rơi vào "Khác").
    - `src/shared/components/nav-icons.tsx` — `NAV_ICONS` là `Record<NavIconId, ReactNode>`,
      thêm `NavIconId` mà quên thêm case ở đây là lỗi kiểu ngay lúc build.
  </read_first>
  <action>
    `src/shared/lib/navigation.ts`:
    - Thêm `"inventory"` vào union `NavIconId`.
    - Thêm một phần tử `NAV_ITEMS` cho `/ton-kho`: `label: "Tồn kho"`, `shortLabel: "Tồn"`,
      `icon: "inventory"`, `permission: "view-catalog"`. Đặt **ngay sau `/nhap-kho`** — nghiệp
      vụ hằng ngày đứng trước dữ liệu nền, và tồn kho là thứ thủ kho mở nhiều nhất.
    - `mobilePriority`: đặt `3` và đẩy `/danh-muc` thành `4`, `/doi-tac` thành `5`. Thanh tab đáy
      chỉ giữ 4 ô nên `/doi-tac` sẽ rơi vào "Khác" — đó là đánh đổi có chủ đích: thủ kho cầm
      điện thoại cần Tồn kho hơn cần Đối tác. Ghi lý do vào comment ngay tại chỗ.
    - KHÔNG thêm mục menu cho `/ton-kho/dinh-muc` và `/ton-kho/nap-tam`: một cái là việc định kỳ
      vài tháng một lần, một cái là việc một lần. Lối vào của chúng là link trong thân trang
      (`findActiveHref` dùng `startsWith` nên menu "Tồn kho" vẫn sáng khi đang ở route con).

    `src/shared/components/nav-icons.tsx`: thêm `inventory: <...Outlined />` — chọn một icon
    `@ant-design/icons` đã cài, hợp nghĩa kho hàng (ví dụ `DatabaseOutlined`,
    `InboxOutlined`, `ContainerOutlined`), khác với bốn icon đang dùng.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` thoát mã 0 (nếu quên case icon thì `tsc` đỏ ngay).
    - `grep -c "inventory" src/shared/lib/navigation.ts` ≥ 2 và
      `grep -c "inventory" src/shared/components/nav-icons.tsx` = 1.
    - `NAV_ITEMS` có đúng sáu phần tử, `mobilePriority` là dãy 1..5 không trùng nhau cộng một
      `null`.
    - Không thêm mục menu nào cho `/ton-kho/dinh-muc` hay `/ton-kho/nap-tam`.
  </acceptance_criteria>
  <done>Menu có mục "Tồn kho" với icon riêng; `mobilePriority` không trùng; `npm run check` xanh.</done>
</task>

<task type="auto">
  <name>Task 2: Ma trận quyền route + chạy toàn bộ bộ kiểm</name>
  <files>scripts/test-route-permissions.ts</files>
  <read_first>
    - `scripts/test-route-permissions.ts` toàn bộ — `MA_TRAN` (dòng 36-52), hằng `AI_CUNG_XEM`,
      kiểu `KyVong` (`"200" | "quyen" | "dangnhap" | "401" | "goc" | \`→${string}\``),
      hàm `layCookie` và `layIdPhieuNhap`.
    - `src/app/(app)/ton-kho/page.tsx`, `ton-kho/dinh-muc/page.tsx`, `ton-kho/nap-tam/page.tsx`
      — `requirePermission` thật của từng route.
  </read_first>
  <action>
    Thêm vào `MA_TRAN` đúng ba dòng:
    - `/ton-kho` → dùng hằng `AI_CUNG_XEM` (bốn vai trò `"200"`, khách `"dangnhap"`).
    - `/ton-kho/dinh-muc` → `quanly: "200"`, `vanphong: "200"`, `thukho1: "quyen"`,
      `chixem: "quyen"`, `khach: "dangnhap"` (khuôn `/cai-dat/nhom-hang`).
    - `/ton-kho/nap-tam` → `quanly: "200"`, `vanphong: "quyen"`, `thukho1: "quyen"`,
      `chixem: "quyen"`, `khach: "dangnhap"` (khuôn `/cai-dat/nguoi-dung`).

    Route `/api/ton-kho/nap-tam` **chỉ có POST** nên không kiểm được bằng `MA_TRAN` (script gửi
    GET; GET vào đó trả 405 cho mọi vai trò, không nói gì về quyền). Viết thêm một hàm
    `kiemNapTamPost()` chạy sau vòng lặp chính: với mỗi vai trò, POST một `FormData` RỖNG tới
    `/api/ton-kho/nap-tam` kèm cookie của vai trò đó, và kỳ vọng
    `quanly → 400` (qua được cửa quyền, dừng ở "Chưa chọn file"),
    `vanphong|thukho1|chixem → 403`, `khách (không cookie) → 401`.
    In kết quả theo đúng định dạng dòng của script hiện tại và cộng vào bộ đếm hỏng.

    Chạy toàn bộ bộ kiểm, ghi kết quả từng cái vào SUMMARY:
    `npm run check` · `npm run db:test:linked` (đếm CẢ dòng `ERROR` lẫn `not ok`) ·
    `npm run verify:hook` · `npx tsx scripts/test-pure-functions.ts` ·
    `npx tsx scripts/test-excel-reader.ts` · `npm run dev` ở một cửa sổ rồi
    `npx tsx scripts/test-route-permissions.ts`.

    **Dự kiến trước một kết quả không phải lỗi của Phase 5:** `MA_TRAN` hiện KHÔNG có dòng nào
    cho `/dat-hang`, `/xuat-kho`, `/tra-hang` của Phase 4 vì giao diện phase đó chưa xong. Không
    bổ khuyết hộ ở đây — ghi vào SUMMARY như một việc còn nợ của Phase 4.
  </action>
  <verify>
    <automated>npx tsx scripts/test-route-permissions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/test-route-permissions.ts` báo 0 dòng hỏng và số dòng kiểm tăng đúng
      3 × 5 = 15 so với lần chạy trước, cộng 5 phép thử POST.
    - `MA_TRAN` có cả ba chuỗi `"/ton-kho"`, `"/ton-kho/dinh-muc"`, `"/ton-kho/nap-tam"`.
    - `npm run db:test:linked` không có `not ok` và không có `ERROR`.
    - `npm run check`, `npm run verify:hook`, `scripts/test-pure-functions.ts`,
      `scripts/test-excel-reader.ts` đều thoát mã 0.
    - SUMMARY liệt kê kết quả của cả sáu bộ kiểm kèm con số, và ghi việc còn nợ của Phase 4.
  </acceptance_criteria>
  <done>Ba dòng ma trận mới cộng phép thử POST đều xanh; cả sáu bộ kiểm của dự án chạy xong và có con số trong SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT Phase 5 — nạp trước, soi sau</name>
  <files>không sửa file nào — đây là phiên nghiệm thu trên dữ liệu thật</files>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 5 — năm tiêu chí thành công.
    - `.planning/phases/05-ton-kho-tong-quan/05-CONTEXT.md` mục `<decisions>` — D-01..D-05.
  </read_first>
  <what-built>
    RPC đọc tồn theo mã × kho; thẻ kho có tồn lũy kế và link chứng từ; cặp RPC đề xuất/duyệt định
    mức cộng màn duyệt; đường nạp tồn tạm qua một chứng từ `DIEU_CHINH`; mục menu và ma trận quyền.
  </what-built>
  <how-to-verify>
    **Thứ tự quan trọng: nạp tồn tạm TRƯỚC, rồi mới soi các màn đọc.** `ton_kho` hiện có 0 dòng
    khác 0, nên nếu soi trước thì màn nào cũng trắng và mọi kết luận đều vô nghĩa.

    1. **Nạp tồn tạm** — đăng nhập `quanly`, mở `/ton-kho/nap-tam`, chọn file
       `DanhSachSanPham_KV….xlsx`, xem kết quả chế độ kiểm, rồi bấm **Nạp thật**.
       Ghi lại: số mã đã nạp, số bị bỏ qua, số lỗi, và `so_ct` của chứng từ sinh ra.
    2. **Kiểm chứng từ** — mở `/nhap-kho` (hoặc màn danh sách chứng từ tương ứng) tìm chứng từ
       `DIEU_CHINH` vừa tạo: trạng thái phải là `HOÀN THÀNH`, ghi chú bắt đầu bằng `[NAP_TON_TAM]`.
    3. **Tiêu chí 1** — mở `/ton-kho`: mỗi mã một dòng, hai cột kho cộng cột Tổng, số liệu khớp
       với file KiotViet ở vài mã bạn tự chọn. Lọc theo nhóm hàng và công đoạn. Gõ tên hàng
       **không dấu** vào ô tìm và xem có ra mã có dấu không. Đổi trang.
    4. **Tiêu chí 2** — bấm một mã có biến động → tab "Thẻ kho": mỗi dòng hệ thống có **tồn lũy
       kế**, dòng trên cùng khớp cột Tổng ở `/ton-kho`; dòng KiotViet cũ để dấu "—"; bấm số phiếu
       mở đúng chứng từ.
    5. **Tiêu chí 3** — mở `/ton-kho/dinh-muc`: cảnh báo "10 ngày dữ liệu, 1.223/3.266 mã" hiện
       trên cùng; mỗi dòng có căn cứ bằng số; duyệt thử một nhóm; xác nhận `ton_toi_thieu` của
       các mã đó đã đổi ở `/danh-muc`.
    6. **Tiêu chí 4** — mở `/ton-kho?ton=duoi_dinh_muc`: ra danh sách mã dưới định mức, không rỗng.
    7. **Phân quyền** — đăng nhập `thukho1`: `/ton-kho` chỉ hiện tồn của kho được phân (cột kho
       kia trống hoặc không có); gõ thẳng `/ton-kho/dinh-muc` và `/ton-kho/nap-tam` đều bị chặn.
    8. **Điện thoại** — thu hẹp cửa sổ xuống cỡ điện thoại: thanh tab đáy có ô "Tồn"; bảng tồn kho
       cuộn ngang trong khung riêng chứ không làm cả trang tràn.
    9. **Console** — mở console ở từng màn mới, không được có cảnh báo antd nào.

    Nếu có chỗ sai, mô tả cụ thể: màn nào, thao tác nào, thấy gì, mong đợi gì.
  </how-to-verify>
  <action>
    Không tự bấm "Nạp thật" và không tự kết luận đạt. Trình bày chín mục ở <how-to-verify>
    theo ĐÚNG THỨ TỰ đó cho người dùng, chạy `npm run dev`, DỪNG chờ người dùng thao tác và
    trả lời từng mục, rồi chép nguyên văn kết luận của họ vào SUMMARY.
  </action>
  <verify>
    <automated>npx tsx scripts/test-route-permissions.ts</automated>
    <human-check>Chín mục ở <how-to-verify> được người dùng xác nhận từng mục một, theo đúng thứ tự: nạp tồn tạm trước, soi các màn đọc sau.</human-check>
  </verify>
  <acceptance_criteria>
    - Chín mục trên được xác nhận bằng chữ, mỗi mục một dòng kết luận đạt/không đạt.
    - Số liệu của bước 1 (đã nạp / bỏ qua / lỗi / `so_ct`) được ghi vào SUMMARY.
    - Kết quả bước 7 (thủ kho) ghi rõ thấy tồn của kho nào.
    - Mọi mục "không đạt" được chuyển thành gap để `/gsd:plan-phase --gaps` xử lý, không tự sửa
      ngoài phạm vi plan.
  </acceptance_criteria>
  <resume-signal>Gõ "đạt" nếu cả chín mục đều ổn, hoặc liệt kê mục nào sai và sai thế nào</resume-signal>
  <done>Chín mục UAT được người dùng xác nhận bằng chữ; số liệu lần nạp tồn tạm ghi vào SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Người dùng gõ URL → route mới | Menu ẩn mục không có quyền, nhưng URL gõ tay thì không |
| Script kiểm quyền → ứng dụng đang chạy | Kiểm bằng HTTP thật trên phiên thật của bốn vai trò |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-44 | Elevation of Privilege | Route mới thiếu `requirePermission`, chỉ dựa vào menu ẩn | mitigate | Ba dòng `MA_TRAN` kiểm bằng HTTP thật cho cả bốn vai trò + khách; đây đúng là lỗ mà Bẫy 12 từng để lọt |
| T-05-45 | Elevation of Privilege | Route handler POST không bị ma trận GET phủ | mitigate | Hàm `kiemNapTamPost()` gửi POST thật với cookie từng vai trò, kỳ vọng 400/403/401 |
| T-05-46 | Repudiation | Báo "bộ kiểm xanh" trong khi một file test chết giữa chừng | mitigate | Đếm cả `ERROR` lẫn `not ok`; SUMMARY ghi con số từng bộ kiểm |
| T-05-47 | Information Disclosure | Thủ kho thấy tồn kho không được phân trên màn thật | mitigate | Bước 7 của UAT kiểm bằng mắt trên dữ liệu thật, bổ sung cho pgTAP |
| T-05-SC | Tampering | Cài gói npm mới | mitigate | Không thêm dependency; icon lấy từ `@ant-design/icons` đã có |
</threat_model>

<verification>
- `npm run check` · `npm run db:test:linked` · `npm run verify:hook` ·
  `scripts/test-pure-functions.ts` · `scripts/test-excel-reader.ts` ·
  `scripts/test-route-permissions.ts` — tất cả xanh, con số ghi vào SUMMARY.
- Menu có mục "Tồn kho"; thanh tab đáy có ô "Tồn".
- UAT chín mục được người dùng xác nhận.
</verification>

<success_criteria>
- Năm tiêu chí của ROADMAP Phase 5 được xác nhận trên dữ liệu thật.
- Commit: `feat(ton-kho): menu va ma tran quyen cho ba route moi`.
</success_criteria>

<output>
Create `.planning/phases/05-ton-kho-tong-quan/05-11-SUMMARY.md` when done
</output>
