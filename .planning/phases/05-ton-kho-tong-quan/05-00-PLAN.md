---
phase: 05-ton-kho-tong-quan
plan: 00
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [TON-01, TON-02, TQAN-02]

must_haves:
  truths:
    - "Bản làm việc chạy được npm run check, db:push, db:types và db:test:linked"
    - "Đọc được pg_get_functiondef trên database thật — điều kiện bắt buộc của ba plan sửa hàm đã có"
    - "File danh mục KiotViet nằm trong data/kiotviet/ để plan 05-10 có cái mà nạp"
    - "Biết ai đã đẩy migration 0050-0057, tức biết có phiên nào khác đang ghi cùng database không"
  artifacts:
    - path: "node_modules"
      provides: "Phụ thuộc đã cài, đủ để chạy mọi lệnh kiểm"
    - path: ".env.local"
      provides: "Khóa Supabase cho cả client lẫn server"
    - path: "data/kiotviet"
      provides: "File DanhSachSanPham_KV… — nguồn số tồn tạm của D-05"
  key_links:
    - from: ".env.local"
      to: "supabase CLI + src/lib/env.ts"
      via: "SUPABASE_PROJECT_ID và khóa publishable/secret"
      pattern: "SUPABASE_PROJECT_ID"
---

<objective>
Trang bị bản làm việc. Không viết dòng code nào.

Purpose: **plan này tồn tại để gỡ một khóa cứng.** Ba plan Wave 2 (05-02, 05-03, 05-04) đều sửa
hoặc dựa vào một đối tượng database ĐÃ CÓ, nên bắt buộc phải đọc `pg_get_functiondef` /
`pg_get_constraintdef` trên cloud trước khi viết — và đều tự dừng nếu không kết nối được. Nếu
việc trang bị máy nằm trong plan 05-05 (plan đẩy schema, vốn phải chờ ba plan kia xong) thì ba
plan đó dừng ngay task đầu, 05-05 không bao giờ đủ điều kiện chạy, và cả phase đứng im.

Tách ra đứng riêng ở wave 1, không phụ thuộc gì, là cách duy nhất để thứ tự chạy được.

Output: một môi trường làm việc đủ điều kiện, và câu trả lời cho một câu hỏi về an toàn dữ liệu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-ton-kho-tong-quan/05-CONTEXT.md
@supabase/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: npm install</name>
  <files>node_modules/ (không phải file trong repo)</files>
  <read_first>
    - `package.json` — danh sách script và dependency.
    - `package-lock.json` — bản khóa; `npm install` phải cài đúng bản này.
    - `CLAUDE.md` bẫy 4 — `npm audit` sẽ báo moderate ở `uuid@8` (phụ thuộc của exceljs).
      Đã phân tích một lần rồi: đường code bị ảnh hưởng không bao giờ chạy. **Không** chạy
      `npm audit fix`, nó hạ exceljs xuống 3.x và làm hỏng việc đọc file KiotViet.
  </read_first>
  <action>
    Chạy `npm install`. Không thêm, không bớt, không nâng cấp dependency nào — Phase 5 không cần
    gói mới nào cả.

    Nếu `npm audit` in cảnh báo, ghi nguyên văn vào SUMMARY kèm một dòng nói rõ đã cố ý bỏ qua
    theo bẫy 4, rồi đi tiếp.
  </action>
  <verify>
    <automated>test -f node_modules/.package-lock.json &amp;&amp; npx tsc --version &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - `git status --short` KHÔNG hiện thay đổi ở `package.json` hay `package-lock.json`.
    - SUMMARY ghi lại cảnh báo `npm audit` (nếu có) kèm lý do bỏ qua.
  </acceptance_criteria>
  <done>`node_modules/` tồn tại, `npx tsc --version` chạy được, `package-lock.json` không đổi.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [CHẶN] Khóa Supabase, file KiotViet, và câu hỏi về migration 0050-0057</name>
  <files>.env.local, data/kiotviet/DanhSachSanPham_KV*.xlsx (việc của con người — cần khóa bí mật và một phiên đăng nhập)</files>
  <read_first>
    - `.env.example` — danh sách biến cần có.
    - `CLAUDE.md` bẫy 17 — tên biến trên Vercel khác `.env.local`; `env.ts` đọc cả hai tên và
      nối bằng `||` chứ không `??`.
    - `supabase/README.md` — cách link project.
    - `data/kiotviet/README.md` — bốn file export và quy ước đặt tên theo tiền tố.
    - `.planning/phases/05-ton-kho-tong-quan/05-CONTEXT.md` mục `<open_items>`.
  </read_first>
  <what-built>
    `npm install` đã xong ở Task 1. Bốn việc còn lại đều cần khóa bí mật hoặc một phiên đăng nhập
    mà executor không có, nên phải do người dùng làm.
  </what-built>
  <action>
    Không tự tạo `.env.local`, không tự đăng nhập Supabase, không tự tải file KiotViet về.
    Trình bày bốn việc ở `<how-to-verify>` cho người dùng, DỪNG chờ họ làm và trả lời, rồi chạy
    đúng các lệnh kiểm ở `<acceptance_criteria>` để xác nhận trước khi báo plan xong.

    Nếu người dùng trả lời rằng CÓ một phiên khác đang làm việc trên cùng database
    `kho-vu-tru`, dừng cả phase và báo về orchestrator — `npm run db:push` ở plan 05-05 sẽ đè
    lên công việc của họ.
  </action>
  <how-to-verify>
    Bốn việc:

    1. **Tạo `.env.local`** từ `.env.example`, điền khóa thật của project `kho-vu-tru`:
       `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`,
       `SUPABASE_SECRET_KEY`, `SUPABASE_PROJECT_ID`.
       Khóa secret **tuyệt đối không** đặt vào biến có tiền tố `NEXT_PUBLIC_`.
    2. **Đăng nhập Supabase CLI**: `npx supabase login` (hoặc đặt biến môi trường
       `SUPABASE_ACCESS_TOKEN`), rồi `npm run db:link`.
    3. **Đặt file `DanhSachSanPham_KV….xlsx`** vào `data/kiotviet/`, giữ nguyên tên KiotViet xuất
       ra kể cả phần timestamp. Thư mục đang trống và nội dung bị gitignore.
    4. **Trả lời bằng chữ: ai đã đẩy migration 0050–0057 lên database thật?** Không phải từ thư
       mục này (xem commit `8fc8bfe` — các file đó được dựng lại TỪ database chứ không sinh ra
       ở đây). Nếu còn một phiên khác đang ghi cùng database thì hai bên sẽ đè nhau, và plan
       05-05 sẽ đẩy schema đè lên họ.

    Kèm theo, cho biết: giao diện Phase 4 (`/dat-hang`, `/xuat-kho`, `/tra-hang`) đã xong chưa?
    Chưa xong thì Phase 5 vẫn chạy được, chỉ là thẻ kho thưa dữ liệu thật và ma trận quyền route
    ở plan 05-11 vẫn thiếu dòng của Phase 4 — đó là nợ của Phase 4, không phải việc của phase này.
  </how-to-verify>
  <verify>
    <automated>test -f .env.local &amp;&amp; ls data/kiotviet/DanhSachSanPham_KV*.xlsx >/dev/null 2>&amp;1 &amp;&amp; npx supabase migration list --linked | grep -q "0057" &amp;&amp; echo GATE-OK</automated>
    <human-check>Người dùng trả lời bằng chữ ai đã đẩy migration 0050–0057 lên database thật, và cho biết giao diện Phase 4 đã xong chưa.</human-check>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - `npx supabase migration list --linked` in ra dòng `0057` ở cột remote và KHÔNG có dòng nào
      ≥ `0058` (nếu có thì một phiên khác đã đẩy trước — dừng và báo).
    - Đọc thử được một định nghĩa hàm trên cloud:
      `select pg_get_functiondef('public.the_kho_san_pham(uuid,uuid,int,int)'::regprocedure);`
      trả về nội dung — đây đúng là thứ mà 05-02/03/04 cần.
    - `grep -c "NEXT_PUBLIC_.*SECRET\|NEXT_PUBLIC_.*SERVICE_ROLE" .env.local` bằng 0.
    - Câu trả lời về migration 0050–0057 được chép nguyên văn vào SUMMARY.
  </acceptance_criteria>
  <resume-signal>Gõ "đã trang bị xong" kèm câu trả lời về 0050–0057, hoặc mô tả chỗ kẹt</resume-signal>
  <done>Bốn điều kiện môi trường xác nhận được bằng lệnh; đọc được `pg_get_functiondef` trên cloud; câu hỏi về 0050–0057 đã có lời đáp bằng chữ.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.local` → bundle trình duyệt | Chỉ biến `NEXT_PUBLIC_*` được nhúng vào bundle |
| Phiên làm việc khác → cùng database `kho-vu-tru` | Hai bên đẩy migration chồng nhau, không khôi phục được |
| `npm install` → cây phụ thuộc | Mã của bên thứ ba chạy lúc cài |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00a | Information Disclosure | Khóa secret đặt nhầm vào biến `NEXT_PUBLIC_*` → lộ ra trình duyệt, mất sạch quyền kiểm soát database | mitigate | Acceptance criteria grep `.env.local` tìm tiền tố `NEXT_PUBLIC_` đi kèm `SECRET`/`SERVICE_ROLE`, phải bằng 0 |
| T-05-00b | Tampering | Phiên khác đang ghi cùng database, `db:push` ở 05-05 đè lên họ | mitigate | Bắt buộc trả lời bằng chữ ai đã đẩy 0050–0057; kiểm remote KHÔNG có migration ≥ 0058 lạ; dừng cả phase nếu có |
| T-05-00c | Information Disclosure | File `DanhSachSanPham_KV….xlsx` là dữ liệu kinh doanh thật bị đẩy lên git | mitigate | `data/kiotviet/` đã gitignore trừ README; acceptance criteria kiểm `git status --short` sạch |
| T-05-SC | Tampering | `npm install` kéo về gói lạ hoặc `npm audit fix` hạ cấp exceljs | mitigate | Cài đúng `package-lock.json` đang có, không thêm dependency; cấm `npm audit fix` theo bẫy 4; kiểm `package-lock.json` không đổi |
</threat_model>

<verification>
- `node_modules/` tồn tại, `package.json`/`package-lock.json` không đổi.
- `.env.local` tồn tại, không có khóa bí mật nào mang tiền tố `NEXT_PUBLIC_`.
- `npx supabase migration list --linked` tới `0057`, không có gì ≥ `0058`.
- Đọc được `pg_get_functiondef` trên cloud.
- `ls data/kiotviet/DanhSachSanPham_KV*.xlsx` có kết quả, `git status --short` không hiện file đó.
- Câu trả lời về 0050–0057 nằm trong SUMMARY.
</verification>

<success_criteria>
- Ba plan của Wave 2 (05-02, 05-03, 05-04) có đủ điều kiện chạy bước "đọc định nghĩa đang chạy
  trên cloud" mà không phải dừng.
- Plan này KHÔNG tạo commit code nào — chỉ SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/05-ton-kho-tong-quan/05-00-SUMMARY.md` when done
</output>
