---
phase: 02-khung-ung-dung
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0028_cau_hinh_so_ct.sql
  - supabase/tests/80_cau_hinh_so_ct_test.sql
autonomous: true
requirements: [CDAT-04]
user_setup: []
must_haves:
  truths:
    - "sinh_so_ct đọc tiền tố và số chữ số từ bảng cau_hinh_so_ct; cấu hình mặc định cho ra đúng số như Phase 1 (PN26-000001)"
    - "Đổi tiền tố chỉ ảnh hưởng số phát sau; số đã phát giữ nguyên"
    - "Không lưu được tiền tố sai định dạng, trùng giữa hai loại, hoặc số chữ số ngoài 3–8"
    - "Không giảm được số chữ số xuống dưới độ dài số đang chạy năm nay — không bao giờ phát số bị cắt cụt"
    - "Chỉ quản lý sửa được cấu hình; mọi vai trò đọc được"
  artifacts:
    - path: "supabase/migrations/0028_cau_hinh_so_ct.sql"
      provides: "Bảng cau_hinh_so_ct, sinh_so_ct mới, RPC danh_sach_cau_hinh_so_ct"
      contains: "create table public.cau_hinh_so_ct"
  key_links:
    - from: "public.sinh_so_ct"
      to: "public.cau_hinh_so_ct"
      via: "select tien_to, so_chu_so into ... where loai_ct = p_loai"
---

<objective>
Cho quản lý sửa tiền tố và số chữ số đánh số chứng từ theo từng loại (CDAT-04, D-08), giữ nguyên
reset theo năm và tính an toàn đồng thời của `sinh_so_ct`.

Bẫy phải chặn: `lpad('1234567', 6, '0')` trả `'123456'` — cắt cụt. Giảm số chữ số khi số đang chạy
đã dài hơn sẽ phát ra số TRÙNG số cũ. Chặn ở cả trigger cấu hình lẫn trong `sinh_so_ct`.

Output: migration 0028, pgTAP 80. `20_chung_tu_test.sql` (assert `PN26-000001`) phải vẫn xanh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@supabase/migrations/0009_danh_so.sql
@supabase/tests/20_chung_tu_test.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test 80 đỏ, rồi migration 0028 cấu hình đánh số</name>
  <files>supabase/tests/80_cau_hinh_so_ct_test.sql, supabase/migrations/0028_cau_hinh_so_ct.sql</files>
  <read_first>supabase/migrations/0009_danh_so.sql, supabase/tests/20_chung_tu_test.sql, supabase/tests/00_helper.sql.inc, supabase/migrations/0016_rls_chung_tu.sql, supabase/migrations/0020_va_bao_mat.sql</read_first>
  <action>
**Bước A — `supabase/tests/80_cau_hinh_so_ct_test.sql`** (`begin; select plan(10); ... finish(); rollback;`),
chép khối helper từ `00_helper.sql.inc`. Dùng năm `2091` để không đụng chuỗi số thật.

1. `is((select count(*) from public.cau_hinh_so_ct), 7::bigint, 'đủ cấu hình cho 7 loại chứng từ')`
2. `is(public.sinh_so_ct('NHAP', 2091::smallint), 'PN91-000001', 'cấu hình mặc định giữ đúng định dạng Phase 1')`
3. Dưới postgres `update public.cau_hinh_so_ct set tien_to = 'NK', so_chu_so = 5 where loai_ct = 'NHAP'` → `is(public.sinh_so_ct('NHAP', 2091::smallint), 'NK91-00002', 'đổi tiền tố và số chữ số áp cho số kế tiếp')`
4. `throws_ok($$update public.cau_hinh_so_ct set tien_to = 'pn' where loai_ct = 'XUAT'$$, '23514', null, 'tiền tố phải là chữ hoa/số')`
5. `throws_ok($$update public.cau_hinh_so_ct set tien_to = 'NK' where loai_ct = 'XUAT'$$, '23505', null, 'tiền tố không trùng giữa hai loại')`
6. `throws_ok($$update public.cau_hinh_so_ct set so_chu_so = 9 where loai_ct = 'XUAT'$$, '23514', null, 'số chữ số tối đa 8')`
7. Dưới postgres: `insert into public.chuoi_so_ct (loai_ct, nam, so_hien_tai) values ('KIEM_KE', extract(year from current_date)::smallint, 12345) on conflict (loai_ct, nam) do update set so_hien_tai = 12345` → `throws_ok($$update public.cau_hinh_so_ct set so_chu_so = 4 where loai_ct = 'KIEM_KE'$$, '23514', null, 'không giảm số chữ số dưới độ dài số đang chạy')`
8. `dang_nhap_nhu('vanphong@khominhvu.local')` → `update public.cau_hinh_so_ct set tien_to = 'ZZ' where loai_ct = 'DIEU_CHINH'` rồi `pg_temp.dang_xuat()` → `is((select tien_to from public.cau_hinh_so_ct where loai_ct='DIEU_CHINH'), 'DC', 'văn phòng không sửa được cấu hình')` (RLS update 0 dòng, không ném lỗi — assert trạng thái).
9. `dang_nhap_nhu('thukho1@khominhvu.local')` → `is((select count(*) from public.danh_sach_cau_hinh_so_ct()), 7::bigint, 'mọi vai trò đọc được cấu hình')`
10. `pg_temp.dang_xuat()` → `is((select vi_du from public.danh_sach_cau_hinh_so_ct() where loai_ct = 'XUAT'), 'PX' || to_char(extract(year from current_date)::int % 100, 'FM00') || '-' || lpad(((select coalesce(max(so_hien_tai),0) from public.chuoi_so_ct where loai_ct='XUAT' and nam = extract(year from current_date)) + 1)::text, 6, '0'), 'ví dụ số kế tiếp đúng định dạng')`

**Bước B — `supabase/migrations/0028_cau_hinh_so_ct.sql`:**
```sql
create table public.cau_hinh_so_ct (
  loai_ct public.loai_ct primary key,
  tien_to text not null constraint ck_tien_to check (tien_to ~ '^[A-Z0-9]{1,5}$'),
  so_chu_so smallint not null default 6 constraint ck_so_chu_so check (so_chu_so between 3 and 8),
  updated_at timestamptz not null default now(),
  constraint uq_cau_hinh_tien_to unique (tien_to)
);
insert into public.cau_hinh_so_ct (loai_ct, tien_to) values
  ('NHAP','PN'), ('XUAT','PX'), ('TRA_NCC','TN'), ('TRA_KHACH','TK'),
  ('CHUYEN_KHO','CK'), ('KIEM_KE','KK'), ('DIEU_CHINH','DC')
on conflict (loai_ct) do nothing;

create trigger set_updated_at_cau_hinh_so_ct before update on public.cau_hinh_so_ct
  for each row execute function public.update_updated_at();

create or replace function public.kiem_so_chu_so_cau_hinh()
returns trigger language plpgsql set search_path = '' as $$
declare v_dai int;
begin
  select coalesce(max(length(so_hien_tai::text)), 0) into v_dai
  from public.chuoi_so_ct
  where loai_ct = new.loai_ct and nam = extract(year from current_date)::smallint;
  if new.so_chu_so < v_dai then
    raise exception 'Số chứng từ % năm nay đã tới % chữ số, không giảm xuống % được', new.loai_ct, v_dai, new.so_chu_so
      using errcode = '23514';
  end if;
  return new;
end $$;
revoke all on function public.kiem_so_chu_so_cau_hinh() from public, anon, authenticated;
create trigger kiem_so_chu_so before update on public.cau_hinh_so_ct
  for each row execute function public.kiem_so_chu_so_cau_hinh();

alter table public.cau_hinh_so_ct enable row level security;
create policy "doc cau hinh so ct" on public.cau_hinh_so_ct for select to authenticated using (true);
create policy "quan ly sua cau hinh so ct" on public.cau_hinh_so_ct for update to authenticated
  using ((select public.vai_tro_hien_tai()) = 'quan_ly')
  with check ((select public.vai_tro_hien_tai()) = 'quan_ly');
revoke insert, delete on public.cau_hinh_so_ct from anon, authenticated;
revoke update on public.cau_hinh_so_ct from anon, authenticated;
grant update (tien_to, so_chu_so) on public.cau_hinh_so_ct to authenticated;
```
Viết lại `sinh_so_ct` (giữ chữ ký `(public.loai_ct, smallint)`, giữ nguyên câu `insert ... on conflict ... returning`):
```sql
create or replace function public.sinh_so_ct(p_loai public.loai_ct, p_nam smallint default null)
returns text language plpgsql set search_path = '' as $$
declare
  v_so integer;
  v_nam smallint := coalesce(p_nam, extract(year from current_date)::smallint);
  v_tien_to text;
  v_so_chu_so smallint;
begin
  select tien_to, so_chu_so into v_tien_to, v_so_chu_so
  from public.cau_hinh_so_ct where loai_ct = p_loai;
  if v_tien_to is null then
    raise exception 'Chưa cấu hình đánh số cho loại chứng từ %', p_loai using errcode = '23514';
  end if;

  insert into public.chuoi_so_ct (loai_ct, nam, so_hien_tai)
  values (p_loai, v_nam, 1)
  on conflict (loai_ct, nam)
  do update set so_hien_tai = public.chuoi_so_ct.so_hien_tai + 1
  returning so_hien_tai into v_so;

  -- lpad CẮT CỤT chuỗi dài hơn độ rộng → phát số trùng. Chặn cứng.
  if length(v_so::text) > v_so_chu_so then
    raise exception 'Số chứng từ % đã vượt % chữ số — tăng số chữ số trong Cài đặt', p_loai, v_so_chu_so
      using errcode = '23514';
  end if;

  return format('%s%s-%s', v_tien_to, to_char(v_nam % 100, 'FM00'), lpad(v_so::text, v_so_chu_so, '0'));
end;
$$;
revoke all    on function public.sinh_so_ct(public.loai_ct, smallint) from public, anon;
grant execute on function public.sinh_so_ct(public.loai_ct, smallint) to authenticated;
```
KIỂM TRƯỚC KHI ĐỔI: `0009` không có `set search_path` — thêm `set search_path = ''` là thay đổi hành
vi; mọi tham chiếu trong thân hàm đã qualify `public.` nên an toàn. Nếu Supabase advisor từng chấp
nhận cảnh báo cho hàm này (xem `supabase/README.md`), cập nhật README.

RPC đọc cho màn Cài đặt:
```sql
create or replace function public.danh_sach_cau_hinh_so_ct()
returns table (loai_ct public.loai_ct, tien_to text, so_chu_so smallint, so_hien_tai integer, vi_du text)
language sql stable set search_path = '' as $$
  select c.loai_ct, c.tien_to, c.so_chu_so, coalesce(s.so_hien_tai, 0),
         format('%s%s-%s', c.tien_to, to_char(extract(year from current_date)::int % 100, 'FM00'),
                lpad((coalesce(s.so_hien_tai, 0) + 1)::text, c.so_chu_so, '0'))
  from public.cau_hinh_so_ct c
  left join public.chuoi_so_ct s on s.loai_ct = c.loai_ct and s.nam = extract(year from current_date)::smallint
  order by c.loai_ct;
$$;
revoke all    on function public.danh_sach_cau_hinh_so_ct() from public, anon;
grant execute on function public.danh_sach_cau_hinh_so_ct() to authenticated;
```
Cuối file: khối DO tự kiểm RLS.

**Bước C:** `npm run db:push` → test 80 xanh 10/10 → `npx supabase test db --linked supabase/tests/20_chung_tu_test.sql` vẫn xanh.
  </action>
  <verify>
    <automated>npm run db:push && npx supabase test db --linked supabase/tests/80_cau_hinh_so_ct_test.sql && npx supabase test db --linked supabase/tests/20_chung_tu_test.sql</automated>
  </verify>
  <acceptance_criteria>
    - Migration chứa `create table public.cau_hinh_so_ct`, `check (tien_to ~ '^[A-Z0-9]{1,5}$')`, `check (so_chu_so between 3 and 8)`, `unique (tien_to)`
    - Migration chứa `if length(v_so::text) > v_so_chu_so then` và KHÔNG còn `when 'NHAP'       then 'PN'`
    - Migration chứa `grant update (tien_to, so_chu_so) on public.cau_hinh_so_ct to authenticated`
    - Test 80 có `plan(10)` và báo 10/10 ok; test 20 vẫn xanh
  </acceptance_criteria>
  <done>Đánh số cấu hình được, không bao giờ phát số cắt cụt</done>
</task>

</tasks>

<verification>
- [ ] Test 80 và test 20 xanh
- [ ] Trên cloud `select * from public.danh_sach_cau_hinh_so_ct()` trả 7 dòng
</verification>

<success_criteria>
- CDAT-04 có nền database đầy đủ cho màn Cài đặt (plan 15)
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-03-SUMMARY.md`
</output>
