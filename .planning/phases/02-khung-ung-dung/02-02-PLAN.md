---
phase: 02-khung-ung-dung
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - supabase/migrations/0027_nhat_ky_sua.sql
  - supabase/tests/70_nhat_ky_sua_test.sql
autonomous: true
requirements: [DMUC-04, DTAC-02]
user_setup: []
must_haves:
  truths:
    - "Mỗi lần sửa san_pham/doi_tac/nguoi_dung sinh một dòng nhật ký cho MỖI trường đổi, kèm giá trị cũ → mới, người sửa, thời điểm, nguồn"
    - "Tạo mới sinh một dòng truong = '_tao_moi' chứa toàn bộ bản ghi"
    - "UPDATE không đổi giá trị nào không sinh nhật ký; updated_at, created_at, lan_phat_sinh_cuoi, gia_von không bao giờ vào nhật ký"
    - "Nhật ký không sửa, không xóa được bởi bất kỳ role nào kể cả postgres"
    - "Chỉ quản lý và văn phòng đọc được nhật ký, qua RPC lich_su_sua"
  artifacts:
    - path: "supabase/migrations/0027_nhat_ky_sua.sql"
      provides: "Bảng nhat_ky_sua, trigger ghi_nhat_ky_sua generic, RPC lich_su_sua"
      contains: "create table public.nhat_ky_sua"
    - path: "supabase/tests/70_nhat_ky_sua_test.sql"
      provides: "12 assertion nhật ký"
  key_links:
    - from: "RPC import/hàng loạt (plan 08), rà ghi chú (plan 07)"
      to: "trigger ghi_nhat_ky_sua"
      via: "perform set_config('app.nguon_sua', '<nguon>', true)"
---

<objective>
Dựng nhật ký sửa append-only (D-20) để trả lời "ai sửa gì, lúc nào, từ đâu" cho danh mục, đối tác
và tài khoản. Mọi đường ghi (form, sửa ô, hàng loạt, import, rà ghi chú, Cài đặt) đều bị trigger
bắt — không phụ thuộc code ứng dụng nhớ ghi log.

Purpose: đối ứng nỗi đau "không kiểm soát được ai sửa gì".
Output: migration 0027, pgTAP 70.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@.planning/phases/02-khung-ung-dung/02-RESEARCH.md
@supabase/migrations/0008_so_cai_ton_kho.sql
@supabase/migrations/0015_rls_danh_muc.sql
@supabase/migrations/0026_nguoi_dung_nhieu_kho.sql
@.memory/patterns/pgtap-va-test.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test 70 đỏ, rồi migration 0027 nhật ký sửa</name>
  <files>supabase/tests/70_nhat_ky_sua_test.sql, supabase/migrations/0027_nhat_ky_sua.sql</files>
  <read_first>supabase/tests/00_helper.sql.inc, supabase/tests/60_kho_mac_dinh_test.sql, supabase/migrations/0008_so_cai_ton_kho.sql, supabase/migrations/0015_rls_danh_muc.sql, supabase/migrations/0004_danh_muc.sql, supabase/migrations/0003_nguoi_dung_kho.sql</read_first>
  <action>
**Bước A — `supabase/tests/70_nhat_ky_sua_test.sql`** (`begin; select plan(12); ... select * from finish(); rollback;`).
Chép khối helper từ `supabase/tests/00_helper.sql.inc` (bản ĐÃ sửa ở plan 01 nếu plan 01 xong trước;
nếu chưa, chép bản hiện tại — plan 01 Task 2 sẽ đồng bộ sau). Mã test dùng tiền tố `NKS-ZQX-`.

Assertion:
1. Dưới postgres: `select pg_temp.sp_test('NKS-ZQX-001')` → `is((select count(*) from public.nhat_ky_sua where ban_ghi_id = <id> and truong = '_tao_moi'), 1::bigint, 'tạo mới sinh một dòng _tao_moi')`.
2. `dang_nhap_nhu('vanphong@khominhvu.local')`; `update public.san_pham set ten_hang = 'Tên mới', ghi_chu = 'gc' where ma_hang = 'NKS-ZQX-001'` → `is(count truong in ('ten_hang','ghi_chu'), 2::bigint, 'sửa hai trường sinh hai dòng')`.
3. `is((select gia_tri_cu #>> '{}' from ... truong='ten_hang'), 'Hàng test NKS-ZQX-001', 'lưu giá trị cũ')`.
4. `is((select nguoi_sua_id from ... truong='ten_hang'), (select id from auth.users where email='vanphong@khominhvu.local'), 'ghi đúng người sửa')`.
5. `is((select nguon ... truong='ten_hang'), 'form', 'nguồn mặc định là form khi có người dùng')`.
6. `update ... set ten_hang = 'Tên mới'` (không đổi) → số dòng nhật ký của bản ghi không tăng.
7. `select set_config('app.nguon_sua', 'import', true); update ... set ghi_chu = 'gc2'` → dòng mới có `nguon = 'import'`.
8. `throws_ok($$update public.nhat_ky_sua set nguon = 'x'$$, '42501', null, 'authenticated không sửa được nhật ký')`.
9. `pg_temp.dang_xuat()` (về postgres) → `throws_ok($$delete from public.nhat_ky_sua$$, '23514', null, 'kể cả postgres không xóa được nhật ký')`.
10. `dang_nhap_nhu('thukho1@khominhvu.local')` → `throws_ok($$select * from public.lich_su_sua('san_pham', (select id from public.san_pham where ma_hang='NKS-ZQX-001'))$$, '42501', null, 'thủ kho không đọc được lịch sử sửa')`.
11. `dang_nhap_nhu('quanly@khominhvu.local')` → `ok((select count(*) from public.lich_su_sua('san_pham', <id>)) >= 4, 'quản lý đọc được lịch sử kèm tên người sửa')` và cột `ho_ten_nguoi_sua` không null ở dòng `ten_hang`.
12. Dưới postgres: `update public.san_pham set gia_von = 5, lan_phat_sinh_cuoi = now() where ma_hang = 'NKS-ZQX-001'` → không sinh dòng nhật ký nào có `truong in ('gia_von','lan_phat_sinh_cuoi','updated_at')`.

Nhớ: mọi bảng tạm đọc dưới role authenticated phải `grant select ... to authenticated` (false pass 42501).
Chạy file trên cloud sau khi push migration — trước khi viết migration, chạy thử thấy đỏ là đủ.

**Bước B — `supabase/migrations/0027_nhat_ky_sua.sql`:**
```sql
create table public.nhat_ky_sua (
  id uuid primary key default uuid_generate_v4(),
  bang text not null check (bang in ('san_pham','doi_tac','nguoi_dung')),
  ban_ghi_id uuid not null,
  truong text not null,
  gia_tri_cu jsonb,
  gia_tri_moi jsonb,
  nguon text not null check (nguon in ('form','sua_o','hang_loat','goi_y_duoi','import','ra_ghi_chu','cai_dat','script')),
  nguoi_sua_id uuid references public.nguoi_dung(id),
  sua_luc timestamptz not null default now()
);
create index idx_nhat_ky_sua_tra_cuu on public.nhat_ky_sua (bang, ban_ghi_id, sua_luc desc);
alter table public.nhat_ky_sua enable row level security;
-- Không policy nào: client không đọc/ghi trực tiếp. Đọc qua RPC lich_su_sua.
revoke all on public.nhat_ky_sua from anon, authenticated;
revoke update, delete on public.nhat_ky_sua from service_role;

-- Hàm chặn sửa/xóa GENERIC (chan_sua_xoa_so_cai ở 0008 in cứng tên kho_movement trong thông báo).
create or replace function public.chan_sua_xoa_bat_bien()
returns trigger language plpgsql as $$
begin
  raise exception 'Bảng % là bất biến, không được sửa hoặc xóa (thao tác: %)', tg_table_name, tg_op
    using errcode = '23514';
end $$;
revoke all on function public.chan_sua_xoa_bat_bien() from public, anon, authenticated;
create trigger chan_sua_xoa_nhat_ky_sua
  before update or delete on public.nhat_ky_sua
  for each row execute function public.chan_sua_xoa_bat_bien();

create or replace function public.ghi_nhat_ky_sua()
returns trigger language plpgsql security definer set search_path = '' as $$
declare
  v_nguon text := coalesce(
    nullif(current_setting('app.nguon_sua', true), ''),
    case when auth.uid() is null then 'script' else 'form' end
  );
  v_bo_qua text[] := array['updated_at','created_at','lan_phat_sinh_cuoi','gia_von'];
  v_cu jsonb; v_moi jsonb; k text;
begin
  if tg_op = 'INSERT' then
    insert into public.nhat_ky_sua (bang, ban_ghi_id, truong, gia_tri_moi, nguon, nguoi_sua_id)
    values (tg_table_name, new.id, '_tao_moi', to_jsonb(new) - v_bo_qua, v_nguon, auth.uid());
    return new;
  end if;
  v_cu := to_jsonb(old); v_moi := to_jsonb(new);
  for k in select jsonb_object_keys(v_moi) loop
    if k = any(v_bo_qua) then continue; end if;
    if (v_cu -> k) is distinct from (v_moi -> k) then
      insert into public.nhat_ky_sua (bang, ban_ghi_id, truong, gia_tri_cu, gia_tri_moi, nguon, nguoi_sua_id)
      values (tg_table_name, new.id, k, v_cu -> k, v_moi -> k, v_nguon, auth.uid());
    end if;
  end loop;
  return new;
end $$;
revoke all on function public.ghi_nhat_ky_sua() from public, anon, authenticated;

create trigger ghi_nhat_ky_san_pham after insert or update on public.san_pham
  for each row execute function public.ghi_nhat_ky_sua();
create trigger ghi_nhat_ky_doi_tac after insert or update on public.doi_tac
  for each row execute function public.ghi_nhat_ky_sua();
create trigger ghi_nhat_ky_nguoi_dung after insert or update on public.nguoi_dung
  for each row execute function public.ghi_nhat_ky_sua();
```
Gán/gỡ kho của người dùng (bảng `nguoi_dung_kho`, tạo ở migration 0026 của plan 01) cũng phải vào nhật ký.
Tạo trigger TRỰC TIẾP, không bọc điều kiện — nếu 0026 chưa áp thì push 0027 phải hỏng ngay và rõ ràng
(một khối `if to_regclass(...)` sẽ âm thầm bỏ qua, và migration đã ghi nhận thì push lại không chạy lại nữa):
```sql
create or replace function public.ghi_nhat_ky_kho_nguoi_dung()
returns trigger language plpgsql security definer set search_path = '' as $$
declare v_nguon text := coalesce(nullif(current_setting('app.nguon_sua', true), ''),
                                 case when auth.uid() is null then 'script' else 'cai_dat' end);
begin
  if tg_op = 'INSERT' then
    insert into public.nhat_ky_sua (bang, ban_ghi_id, truong, gia_tri_moi, nguon, nguoi_sua_id)
    values ('nguoi_dung', new.nguoi_dung_id, 'kho', to_jsonb(new.kho_id), v_nguon, auth.uid());
    return new;
  end if;
  insert into public.nhat_ky_sua (bang, ban_ghi_id, truong, gia_tri_cu, nguon, nguoi_sua_id)
  values ('nguoi_dung', old.nguoi_dung_id, 'kho', to_jsonb(old.kho_id), v_nguon, auth.uid());
  return old;
end $$;
revoke all on function public.ghi_nhat_ky_kho_nguoi_dung() from public, anon, authenticated;
create trigger ghi_nhat_ky_nguoi_dung_kho after insert or delete on public.nguoi_dung_kho
  for each row execute function public.ghi_nhat_ky_kho_nguoi_dung();
```
Trước khi push: `npx supabase migration list --linked` phải thấy `0026` ở cột Remote.

RPC đọc:
```sql
create or replace function public.lich_su_sua(p_bang text, p_ban_ghi_id uuid, p_gioi_han int default 200)
returns table (id uuid, truong text, gia_tri_cu jsonb, gia_tri_moi jsonb, nguon text,
               nguoi_sua_id uuid, ho_ten_nguoi_sua text, sua_luc timestamptz)
language plpgsql stable security definer set search_path = '' as $$
begin
  if coalesce((select public.vai_tro_hien_tai())::text, '') not in ('quan_ly','van_phong') then
    raise exception 'Chỉ quản lý và văn phòng xem được lịch sử sửa' using errcode = '42501';
  end if;
  return query
  select n.id, n.truong, n.gia_tri_cu, n.gia_tri_moi, n.nguon, n.nguoi_sua_id, nd.ho_ten, n.sua_luc
  from public.nhat_ky_sua n
  left join public.nguoi_dung nd on nd.id = n.nguoi_sua_id
  where n.bang = p_bang and n.ban_ghi_id = p_ban_ghi_id
  order by n.sua_luc desc, n.truong
  limit least(greatest(p_gioi_han, 1), 1000);
end $$;
revoke all    on function public.lich_su_sua(text, uuid, int) from public, anon;
grant execute on function public.lich_su_sua(text, uuid, int) to authenticated;
```
Cuối file: khối DO tự kiểm RLS (chép từ 0016).

**Bước C:** `npm run db:push` → chạy test 70 → xanh 12/12. Chạy `npx supabase test db --linked supabase/tests/60_kho_mac_dinh_test.sql` để chắc trigger mới không làm vỡ test cũ ghi `san_pham`.
  </action>
  <verify>
    <automated>npm run db:push && npx supabase test db --linked supabase/tests/70_nhat_ky_sua_test.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0027_nhat_ky_sua.sql` chứa `create table public.nhat_ky_sua`, `create trigger ghi_nhat_ky_san_pham`, `create trigger ghi_nhat_ky_doi_tac`, `create trigger ghi_nhat_ky_nguoi_dung`
    - File chứa `current_setting('app.nguon_sua', true)` và `array['updated_at','created_at','lan_phat_sinh_cuoi','gia_von']`
    - File chứa `create trigger chan_sua_xoa_nhat_ky_sua` và `errcode = '23514'`
    - File chứa `raise exception 'Chỉ quản lý và văn phòng xem được lịch sử sửa' using errcode = '42501'`
    - `supabase/tests/70_nhat_ky_sua_test.sql` chứa `plan(12)`
    - Test 70 báo 12/12 ok; `select count(*) from pg_trigger where tgname = 'ghi_nhat_ky_nguoi_dung_kho'` = 1 trên cloud
  </acceptance_criteria>
  <done>Nhật ký sửa bắt mọi đường ghi, bất biến, đọc có kiểm vai trò</done>
</task>

</tasks>

<verification>
- [ ] Test 70 xanh 12/12
- [ ] Test 60 (ghi san_pham) vẫn xanh
- [ ] Trigger nhật ký trên nguoi_dung_kho tồn tại trên cloud
</verification>

<success_criteria>
- Mọi thay đổi danh mục/đối tác/tài khoản truy được về người + nguồn + thời điểm
- Nhật ký bất biến ở tầng database
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-02-SUMMARY.md`
</output>
