---
phase: 02-khung-ung-dung
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0029_an_gia_von.sql
  - supabase/tests/90_gia_von_test.sql
  - supabase/tests/40_tim_kiem_test.sql
autonomous: true
requirements: [DMUC-05]
user_setup: []
must_haves:
  truths:
    - "Không vai trò nào đọc được san_pham.gia_von hay kho_movement.gia_von_tai_thoi_diem bằng SELECT trực tiếp (PostgREST select=* cũng bị từ chối)"
    - "Mọi cột khác của san_pham và kho_movement vẫn đọc được như trước theo RLS"
    - "Quản lý và văn phòng đọc được giá vốn qua RPC gia_von_san_pham; thủ kho và chỉ xem nhận 42501"
    - "tim_san_pham vẫn chạy cho mọi vai trò và không trả cột gia_von"
    - "Migration tự kiểm: authenticated có SELECT trên mọi cột san_pham trừ gia_von — thêm cột mới mà quên grant thì push hỏng ngay"
  artifacts:
    - path: "supabase/migrations/0029_an_gia_von.sql"
      provides: "Thu quyền đọc bảng, cấp lại theo cột, RPC gia_von_san_pham, tim_san_pham không select *"
      contains: "revoke select on public.san_pham from authenticated"
  key_links:
    - from: "RPC danh_sach_san_pham / chi_tiet_san_pham / the_kho_san_pham (plan 06)"
      to: "public.co_quyen_xem_gia_von()"
      via: "case when (select public.co_quyen_xem_gia_von()) then sp.gia_von end"
---

<objective>
Chặn đọc giá vốn theo vai trò ở tầng database (D-16).

**Sửa sai trong research (đã kiểm trên cloud):** `authenticated` đang có quyền SELECT **cấp ở mức
bảng** trên `san_pham`. Postgres: REVOKE một cột KHÔNG gỡ được quyền cấp ở mức bảng ("the table-level
grant is unaffected by a column-level operation"). Vì vậy phải `revoke select on <bảng>` rồi
`grant select (<danh sách cột trừ cột giá>)` — đúng mẫu 0015 đã làm cho INSERT/UPDATE. Research đề
xuất `revoke select (gia_von_tai_thoi_diem) on kho_movement` sẽ **âm thầm không chặn gì**.

Hệ quả phải biết cho mọi plan giao diện sau:
- `.from('san_pham').select('*')` và `.select()` sau insert/update (return=representation) lỗi 42501
  cho MỌI vai trò. Luôn liệt kê cột tường minh, không có `gia_von`.
- Cột mới thêm vào `san_pham` sau này phải tự `grant select (<cột>)` — khối tự kiểm bắt lỗi quên.

Output: migration 0029, pgTAP 90, `40_tim_kiem_test.sql` cập nhật nếu cần.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@.planning/phases/02-khung-ung-dung/02-RESEARCH.md
@.memory/patterns/supabase-rls-bao-mat.md
@supabase/migrations/0015_rls_danh_muc.sql
@supabase/migrations/0022_tim_kiem_theo_chuoi_con.sql
@supabase/migrations/0025_kho_mac_dinh.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test 90 đỏ, rồi migration 0029 ẩn giá vốn</name>
  <files>supabase/tests/90_gia_von_test.sql, supabase/migrations/0029_an_gia_von.sql, supabase/tests/40_tim_kiem_test.sql</files>
  <read_first>supabase/migrations/0015_rls_danh_muc.sql, supabase/migrations/0022_tim_kiem_theo_chuoi_con.sql, supabase/migrations/0025_kho_mac_dinh.sql, supabase/migrations/0017_doi_chieu.sql, supabase/migrations/0020_va_bao_mat.sql, supabase/tests/40_tim_kiem_test.sql, supabase/tests/30_rls_test.sql, supabase/tests/00_helper.sql.inc</read_first>
  <action>
**Bước 0 — kiểm kê trước khi viết** (ghi kết quả vào SUMMARY):
```bash
set -a; source .env.local; set +a
psql "$DATABASE_URL" -X -A -c "select column_name from information_schema.columns where table_schema='public' and table_name='san_pham' order by ordinal_position"
psql "$DATABASE_URL" -X -A -c "select column_name from information_schema.columns where table_schema='public' and table_name='kho_movement' order by ordinal_position"
grep -rn "select \*\|sp\.\*\|setof public.san_pham\|setof public.kho_movement\|gia_von" supabase/migrations/ | grep -v "^supabase/migrations/0008"
```
Mọi hàm **SECURITY INVOKER** và view `security_invoker` đọc `san_pham.*`, `gia_von`, hoặc
`kho_movement.*`/`gia_von_tai_thoi_diem` đều sẽ vỡ sau migration này — liệt kê và sửa trong chính
migration 0029. Hàm SECURITY DEFINER (`cap_nhat_ton_va_gia_von`, `ghi_so_chung_tu`, `huy_chung_tu`,
`nap_danh_muc_kiotviet`, `_doi_chieu_ton_he_thong`) chạy dưới owner nên không ảnh hưởng.

**Bước A — `supabase/tests/90_gia_von_test.sql`** (`plan(11)`, chép helper, dữ liệu tiền tố `GV-ZQX-`):
Dựng dưới postgres: `pg_temp.sp_test('GV-ZQX-001')`, `insert into kho_movement (kho_id, san_pham_id, so_luong, gia_von_tai_thoi_diem) values (k1, sp, 5, 12345)`; `grant select on t_id to authenticated`.
1. thukho1: `throws_ok($$select gia_von from public.san_pham limit 1$$, '42501', null, 'thủ kho không SELECT được gia_von')`
2. thukho1: `throws_ok($$select gia_von_tai_thoi_diem from public.kho_movement limit 1$$, '42501', null, 'thủ kho không SELECT được gia_von_tai_thoi_diem')`
3. quanly: `throws_ok($$select * from public.san_pham limit 1$$, '42501', null, 'kể cả quản lý không select * trực tiếp — giá vốn chỉ qua RPC')`
4. thukho1: `lives_ok($$select id, ma_hang, ten_hang, gia_ban, kho_mac_dinh_id from public.san_pham limit 1$$, 'thủ kho vẫn đọc các cột khác')`
5. thukho1: `is((select count(*) from public.kho_movement where san_pham_id = (select sp from t_id)), 1::bigint, 'thủ kho vẫn đọc số lượng movement kho mình')`
6. quanly: `is((select gia_von from public.gia_von_san_pham(array[(select sp from t_id)])), <gia_von thật của sp dưới postgres>::numeric, 'quản lý đọc giá vốn qua RPC')` — lấy giá trị kỳ vọng bằng một `select gia_von` dưới postgres lưu vào bảng tạm trước.
7. vanphong: `lives_ok($$select * from public.gia_von_san_pham(array[(select sp from t_id)])$$, 'văn phòng đọc giá vốn qua RPC')`
8. thukho1: `throws_ok($$select * from public.gia_von_san_pham(array[(select sp from t_id)])$$, '42501', null, 'thủ kho gọi RPC giá vốn bị từ chối')`
9. chixem: `throws_ok($$select * from public.gia_von_san_pham(array[(select sp from t_id)])$$, '42501', null, 'chỉ xem gọi RPC giá vốn bị từ chối')`
10. thukho1: `lives_ok($$select * from public.tim_san_pham('GV-ZQX')$$, 'tìm sản phẩm vẫn chạy cho thủ kho')`
11. `ok(not exists (select 1 from pg_proc p where p.proname = 'tim_san_pham' and p.prorettype = 'public.san_pham'::regtype), 'tim_san_pham không còn trả nguyên hàng san_pham')`

**Bước B — `supabase/migrations/0029_an_gia_von.sql`:**
```sql
-- san_pham: thu quyền đọc mức bảng, cấp lại từng cột TRỪ gia_von.
revoke select on public.san_pham from anon, authenticated;
grant select (<mọi cột ở Bước 0 trừ gia_von, liệt kê tường minh>) on public.san_pham to authenticated;

-- kho_movement: tương tự, trừ gia_von_tai_thoi_diem.
revoke select on public.kho_movement from anon, authenticated;
grant select (<mọi cột ở Bước 0 trừ gia_von_tai_thoi_diem>) on public.kho_movement to authenticated;

create or replace function public.co_quyen_xem_gia_von()
returns boolean language sql stable security definer set search_path = '' as $$
  select coalesce((select public.vai_tro_hien_tai())::text in ('quan_ly','van_phong'), false);
$$;
revoke all    on function public.co_quyen_xem_gia_von() from public, anon;
grant execute on function public.co_quyen_xem_gia_von() to authenticated;

create or replace function public.gia_von_san_pham(p_ids uuid[])
returns table (san_pham_id uuid, gia_von numeric)
language plpgsql stable security definer set search_path = '' as $$
begin
  if not (select public.co_quyen_xem_gia_von()) then
    raise exception 'Chỉ quản lý và văn phòng xem được giá vốn' using errcode = '42501';
  end if;
  return query select sp.id, sp.gia_von from public.san_pham sp where sp.id = any(p_ids);
end $$;
revoke all    on function public.gia_von_san_pham(uuid[]) from public, anon;
grant execute on function public.gia_von_san_pham(uuid[]) to authenticated;

-- tim_san_pham: đổi kiểu trả về (setof san_pham → table) phải DROP rồi tạo lại.
drop function public.tim_san_pham(text, int);
create function public.tim_san_pham(p_tu_khoa text, p_gioi_han int default 20)
returns table (
  id uuid, ma_hang text, ten_hang text, barcode text, nhom_hang_id uuid, dvt_id uuid,
  cong_doan_id uuid, quy_doi numeric, gia_ban numeric, dang_kinh_doanh boolean,
  kho_mac_dinh_id uuid, lan_phat_sinh_cuoi timestamptz
)
language sql stable set search_path = '' as $$
  select sp.id, sp.ma_hang, sp.ten_hang, sp.barcode, sp.nhom_hang_id, sp.dvt_id,
         sp.cong_doan_id, sp.quy_doi, sp.gia_ban, sp.dang_kinh_doanh,
         sp.kho_mac_dinh_id, sp.lan_phat_sinh_cuoi
  from public.san_pham sp
  where <GIỮ NGUYÊN mệnh đề where của 0022, từng ký tự>
  order by <GIỮ NGUYÊN order by của 0022>
  limit p_gioi_han;
$$;
revoke all    on function public.tim_san_pham(text, int) from public, anon;
grant execute on function public.tim_san_pham(text, int) to authenticated;
comment on function public.tim_san_pham(text, int) is '<giữ comment 0022> Không trả gia_von (0029).';
```
Kiểu cột (`quy_doi numeric(18,4)` → khai `numeric` trong returns table là hợp lệ).
Sửa mọi hàm/view INVOKER khác tìm thấy ở Bước 0 trong cùng file.

Khối tự kiểm cuối file:
```sql
do $$
declare v_thieu text;
begin
  select string_agg(c.column_name, ', ') into v_thieu
  from information_schema.columns c
  where c.table_schema = 'public' and c.table_name = 'san_pham' and c.column_name <> 'gia_von'
    and not has_column_privilege('authenticated', 'public.san_pham', c.column_name, 'SELECT');
  if v_thieu is not null then
    raise exception 'authenticated thiếu quyền SELECT cột san_pham: %', v_thieu;
  end if;
  if has_column_privilege('authenticated', 'public.san_pham', 'gia_von', 'SELECT')
     or has_column_privilege('authenticated', 'public.kho_movement', 'gia_von_tai_thoi_diem', 'SELECT') then
    raise exception 'authenticated vẫn đọc được cột giá vốn';
  end if;
end $$;
```

**Bước C:** `npm run db:push` → test 90 xanh 11/11 → chạy lại `40_tim_kiem_test.sql`, `30_rls_test.sql`,
`60_kho_mac_dinh_test.sql`, `10_ton_kho_test.sql`. Test nào đỏ vì đọc `select *` / `gia_von` dưới role
authenticated → sửa assertion cho đọc cột tường minh hoặc đọc dưới postgres, KHÔNG nới quyền.
Nếu test 30 có assertion "văn phòng không UPDATE gia_von" dạng `update ... returning *` → bỏ `returning *`.
  </action>
  <verify>
    <automated>npm run db:push && npm run db:test:linked</automated>
  </verify>
  <acceptance_criteria>
    - Migration chứa `revoke select on public.san_pham from anon, authenticated` và `revoke select on public.kho_movement from anon, authenticated`
    - Migration chứa `grant select (` hai lần và KHÔNG có chuỗi `gia_von,` hay `gia_von)` bên trong hai danh sách grant đó
    - Migration chứa `drop function public.tim_san_pham(text, int)` và không chứa `returns setof public.san_pham`
    - Migration chứa `raise exception 'authenticated vẫn đọc được cột giá vốn'`
    - Test 90 có `plan(11)`; `npm run db:test:linked` exit 0
    - Trên cloud: `select has_column_privilege('authenticated','public.san_pham','gia_von','SELECT')` = false
  </acceptance_criteria>
  <done>Giá vốn chỉ đọc được qua RPC có kiểm vai trò; toàn bộ suite xanh</done>
</task>

</tasks>

<verification>
- [ ] Toàn bộ pgTAP xanh sau khi thu quyền
- [ ] `has_column_privilege` xác nhận trên cloud
</verification>

<success_criteria>
- D-16 chặn ở database, không đường lách qua PostgREST
- Ghi vào SUMMARY cảnh báo cho plan giao diện: không `select('*')`, không `.select()` trống sau insert/update trên san_pham
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-04-SUMMARY.md`
</output>
