---
phase: 02-khung-ung-dung
plan: 16
type: execute
wave: 6
depends_on: ["02-11", "02-13"]
files_modified:
  - src/app/(app)/danh-muc/page.tsx
  - src/features/danh-muc/components/bang-san-pham.tsx
  - src/features/danh-muc/components/thanh-loc-san-pham.tsx
  - src/features/danh-muc/components/cot-san-pham.tsx
  - src/features/danh-muc/schemas/san-pham.schema.ts
  - src/features/danh-muc/components/ngan-keo-san-pham.tsx
autonomous: true
requirements: [DMUC-01, DMUC-02, DMUC-03, DMUC-04]
user_setup: []
must_haves:
  truths:
    - "Trang /danh-muc hiện bảng 3.266 mã phân trang server (50/trang), tổng số dòng đúng"
    - "Một ô tìm theo mã + tên, gõ không dấu vẫn ra; gõ xong 300ms mới gọi"
    - "Lọc nhóm hàng, công đoạn, ĐVT, trạng thái tồn, trạng thái kinh doanh; bấm tiêu đề cột để sắp xếp; tất cả nằm trên URL"
    - "Đổi trang không nháy trắng bảng; trang vượt quá tự lùi về trang 1"
    - "Cột giá vốn chỉ hiện cho quản lý/văn phòng; cột tồn hiện 0 kèm gợi ý chưa có chứng từ"
    - "Quản lý/văn phòng tạo và sửa mã trong ngăn kéo: ĐVT và công đoạn là hai ô chọn độc lập, quy đổi > 0, kho mặc định"
    - "Văn phòng thấy ô giá bán bị khóa; quản lý sửa được"
    - "Mã trùng hiện lỗi dưới ô Mã; lưu lỗi giữ nguyên dữ liệu; lưu xong bảng cập nhật và vị trí cuộn/bộ lọc giữ nguyên"
  artifacts:
    - path: "src/features/danh-muc/components/bang-san-pham.tsx"
      provides: "Bảng danh mục server-side"
    - path: "src/features/danh-muc/components/ngan-keo-san-pham.tsx"
      provides: "Form tạo/sửa mã hàng"
  key_links:
    - from: "bang-san-pham.tsx"
      to: "useDanhSachSanPham(docBoLocTuUrl(searchParams))"
      via: "URL là nguồn sự thật của bộ lọc"
---

<objective>
Màn chính của văn phòng (DMUC-01..04; D-11..D-15, D-17). Đây là màn quyết định rủi ro "văn phòng chịu bỏ
KiotViet" — tốc độ tìm và độ dày bảng quan trọng hơn trang trí.

Công cụ rà hàng loạt (chọn nhiều, sửa ô, gợi ý đuôi) và nút Excel là plan 19/20; plan này chừa chỗ
(`rowSelection` chưa bật, thanh công cụ có khe `hanhDongPhu`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@.planning/phases/02-khung-ung-dung/02-11-SUMMARY.md
@src/features/danh-muc/types.ts
@src/features/danh-muc/schemas/bo-loc.schema.ts
@src/features/danh-muc/hooks/useSanPham.ts
@src/shared/components/ngan-keo-form.tsx
@src/features/doi-tac/components/bang-doi-tac.tsx
@src/features/doi-tac/components/ngan-keo-doi-tac.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bảng danh mục + thanh lọc trên URL</name>
  <files>src/app/(app)/danh-muc/page.tsx, src/features/danh-muc/components/bang-san-pham.tsx, src/features/danh-muc/components/thanh-loc-san-pham.tsx, src/features/danh-muc/components/cot-san-pham.tsx</files>
  <read_first>src/features/doi-tac/components/bang-doi-tac.tsx, src/features/danh-muc/hooks/useSanPham.ts, src/features/danh-muc/schemas/bo-loc.schema.ts, src/features/danh-muc/types.ts, src/shared/components/query-state.tsx, src/shared/lib/quyen.ts</read_first>
  <action>
**page** (Server): metadata `title: "Danh mục hàng"`; `const nd = await yeuCauQuyen("xem_danh_muc")`;
`PageHeader tieuDe="Danh mục hàng" moTa="Tìm theo mã hoặc tên, gõ không dấu cũng được"`;
`<BangSanPham quyen={{ sua: coQuyen(nd.vaiTro,"sua_danh_muc"), xemGiaVon: coQuyen(nd.vaiTro,"xem_gia_von"), suaGiaBan: coQuyen(nd.vaiTro,"sua_gia_ban") }} />`.
Bọc `<Suspense>` (dùng `useSearchParams`).

**`thanh-loc-san-pham.tsx`** (`"use client"`): props `{ boLoc; onDoi: (b: BoLocSanPham) => void; danhMucPhu: DanhMucPhu | undefined; hanhDongPhu?: ReactNode; nutThem?: ReactNode }`.
Hàng 1: `Input` tìm (allowClear, `prefix={<SearchOutlined />}`, `autoFocus`, placeholder "Mã hoặc tên hàng — vd: op po air blade"), giữ state cục bộ và `onDoi` sau 300ms (clearTimeout trong ref); phím Enter gọi ngay.
Hàng 2 (`flex flex-wrap gap-2`): `Select` Nhóm hàng (showSearch, `optionFilterProp="label"`, allowClear, rộng 220), Công đoạn (option có chấm màu `mau_hien_thi`), ĐVT, Tồn (`Còn hàng / Hết hàng / Âm / Dưới định mức`), Kinh doanh (`Đang kinh doanh / Ngừng / Tất cả`), nút "Xóa bộ lọc" khi khác mặc định; khe `hanhDongPhu` và `nutThem` bên phải.
Mọi thay đổi (trừ đổi trang/sắp xếp) đặt `trang: 1`.

**`cot-san-pham.tsx`**: hàm `taoCot({ xemGiaVon, sua, onSua, sapXep })` trả `TableColumnsType<DongSanPham>`:
Mã hàng (link `/danh-muc/[id]`, font mono, `sorter`, `sortOrder` theo bộ lọc) · Tên hàng (`sorter`, ellipsis, tooltip) ·
Nhóm (ellipsis) · ĐVT · Công đoạn (`Tag color={mau_cong_doan}`) · Tồn (`align right`, `sorter`, số định dạng `Intl.NumberFormat("vi-VN")`;
nếu = 0 hiện `Typography.Text type="secondary"` "0") · Giá bán (right) · Giá vốn (right, CHỈ khi `xemGiaVon`) ·
Trạng thái (`Tag` "Ngừng KD" khi `!dang_kinh_doanh`; `Tag orange` "Cần rà" khi `can_ra`) · Thao tác "Sửa" (khi `sua`).
Tiền: `numeric` về từ Postgres có thể là string/number — hiển thị bằng `Number(v).toLocaleString("vi-VN")` CHỈ để hiển thị (không tính toán, CLAUDE.md Phase 1 D-12).

**`bang-san-pham.tsx`** (`"use client"`):
- `const boLoc = useMemo(() => docBoLocTuUrl(searchParams), [searchParams])`; `doiBoLoc(b)` → `router.replace(\`${pathname}?${ghiBoLocRaUrl(b)}\`, { scroll: false })`.
- `useDanhSachSanPham(boLoc)`, `useDanhMucPhu()`.
- Rỗng & `boLoc.trang > 1` → `useEffect` gọi `doiBoLoc({ ...boLoc, trang: 1 })`.
- `QueryState` với `khungCho` = `Skeleton` dạng bảng; `laRong = (d) => d.dong.length === 0`; `moTaRong` phân biệt có từ khóa ("Không có mã khớp '<q>'. Thử gõ ít chữ hơn hoặc bỏ dấu.") / có bộ lọc / không có gì.
- Khi đang refetch (`isFetching && !isPending`) hiện `Table loading` mỏng thay vì skeleton (placeholderData giữ bảng cũ).
- `Table` `rowKey="id"` `size="small"` `sticky` (header dính) `scroll={{ x: 1100 }}`, `pagination={{ current: boLoc.trang, pageSize: boLoc.kichThuoc, total, showSizeChanger: true, pageSizeOptions: [20, 50, 100, 200], showTotal: (t) => \`${t.toLocaleString("vi-VN")} mã\` }}`,
  `onChange(pagination, _filters, sorter)`: đổi trang/kích thước/sắp xếp → `doiBoLoc` (sorter.field → `COT_SAP_XEP`, order `ascend/descend` → `asc/desc`, bỏ sắp xếp → `sapXep: null`).
- Nút "Thêm mã hàng" (primary) khi `quyen.sua`; state ngăn kéo `{ mo, id }`.
- Dòng chú thích dưới bảng khi mọi tồn = 0: `Typography.Text type="secondary"` "Tồn hiện bằng 0 cho mọi mã vì chưa có phiếu nhập — tồn thật được đặt khi kiểm kê đầu kỳ."
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - page chứa `yeuCauQuyen("xem_danh_muc")`, `<Suspense`, và không còn `ChuaTrienKhai`
    - `bang-san-pham.tsx` chứa `docBoLocTuUrl`, `ghiBoLocRaUrl`, `scroll: false`, `sticky`, `QueryState`
    - `cot-san-pham.tsx` chỉ thêm cột giá vốn trong nhánh `xemGiaVon`
    - `thanh-loc-san-pham.tsx` chứa `300`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>DMUC-01..03 dùng được trên 3.266 mã thật</done>
</task>

<task type="auto">
  <name>Task 2: Ngăn kéo tạo/sửa mã hàng</name>
  <files>src/features/danh-muc/schemas/san-pham.schema.ts, src/features/danh-muc/components/ngan-keo-san-pham.tsx, src/features/danh-muc/components/bang-san-pham.tsx</files>
  <read_first>src/features/doi-tac/components/ngan-keo-doi-tac.tsx, src/features/danh-muc/api/san-pham.api.ts, src/features/danh-muc/hooks/useSanPham.ts, src/features/danh-muc/types.ts, src/shared/components/ngan-keo-form.tsx</read_first>
  <action>
**`san-pham.schema.ts`**:
```ts
const soKhongAm = (nhan: string) => z.coerce.number({ message: `${nhan} phải là số` }).min(0, `${nhan} không được âm`);
export const sanPhamSchema = z.object({
  ma_hang: z.string().trim().min(1, "Nhập mã hàng").max(50).regex(/^\S+$/, "Mã hàng không chứa khoảng trắng"),
  ten_hang: z.string().trim().min(2, "Nhập tên hàng"),
  nhom_hang_id: z.string().uuid().nullable(),
  dvt_id: z.string({ message: "Chọn đơn vị tính" }).uuid("Chọn đơn vị tính"),
  cong_doan_id: z.string({ message: "Chọn công đoạn" }).uuid("Chọn công đoạn"),
  quy_doi: z.coerce.number({ message: "Quy đổi phải là số" }).gt(0, "Quy đổi phải lớn hơn 0"),
  kho_mac_dinh_id: z.string().uuid().nullable(),
  ton_toi_thieu: soKhongAm("Tồn tối thiểu"),
  ton_toi_da: soKhongAm("Tồn tối đa").nullable(),
  gia_ban: soKhongAm("Giá bán"),
  barcode: z.string().trim().max(64).nullable().transform((v) => v || null),
  ghi_chu: z.string().trim().nullable().transform((v) => v || null),
  dang_kinh_doanh: z.boolean(),
}).refine((v) => v.ton_toi_da === null || v.ton_toi_da >= v.ton_toi_thieu, { path: ["ton_toi_da"], message: "Tồn tối đa phải ≥ tồn tối thiểu" });
```
Mặc định tạo mới: `quy_doi: 1`, `gia_ban: 0`, `ton_toi_thieu: 0`, `ton_toi_da: null`, `dang_kinh_doanh: true`, `cong_doan_id` = id của `MUA_NGOAI`, `dvt_id` = id của `CAI` (lấy từ `useDanhMucPhu`).

**`ngan-keo-san-pham.tsx`** (`"use client"`): props `{ id: string | null; open; onDong; quyen: { suaGiaBan: boolean; xemGiaVon: boolean } }`.
- Sửa: `useChiTietSanPham(id)` → `reset(...)` ép `numeric` string về number cho các ô số.
- Bố cục 2 cột trên ≥ 640px (`grid grid-cols-1 sm:grid-cols-2 gap-x-4`): Mã hàng · Tên hàng (full) · Nhóm hàng · Kho mặc định · **Đơn vị tính · Công đoạn** (đặt cạnh nhau, mỗi ô có help: ĐVT — "Đếm hàng bằng gì: cái, cặp, bộ…"; Công đoạn — "Hàng qua xử lý gì: sơn, carbon, xi mạ… hoặc mua ngoài") · Quy đổi (help "Số đơn vị cơ bản trong 1 ĐVT. Để 1 nếu không chắc") · Tồn tối thiểu · Tồn tối đa · Giá bán (`disabled={!quyen.suaGiaBan}` + help "Chỉ quản lý đặt giá bán" khi khóa) · Giá vốn (chỉ đọc, chỉ khi `xemGiaVon`, help "Tính tự động từ phiếu nhập") · Barcode · Ghi chú (full) · Đang kinh doanh (`Switch`, khi sửa).
- Khi sửa mã có `can_ra_dvt` hoặc `can_ra`: `Alert type="warning"` đầu form "Mã này đang trong danh sách Cần rà — kiểm tra ĐVT và công đoạn." Lưu đổi công đoạn/ĐVT KHÔNG tự gỡ cờ (gỡ bằng "Xác nhận đã rà" ở plan 19) — ghi rõ trong comment.
- Lưu: `useLuuSanPham().mutateAsync({ id, giaTri, guiGiaBan: quyen.suaGiaBan })`. Lỗi: `23505` → `setError("ma_hang", "Mã hàng đã tồn tại")`; `42501` → `setError("root", "Tài khoản không có quyền sửa danh mục/giá bán")`; `23514` → root với message server. Thành công: `message.success`, `onDong()`; tạo mới hiện nút phụ trong message "Tạo tiếp" không cần — thay bằng checkbox "Tạo tiếp mã khác" ở footer: bật thì `reset` giữ Nhóm/ĐVT/Công đoạn/Kho, xóa Mã/Tên, focus ô Mã.
- `NganKeoForm dangLuu={isPending} tieuDe={id ? "Sửa mã hàng" : "Thêm mã hàng"}`.

Nối vào `bang-san-pham.tsx`. Thử tay: vanphong tạo mã `UAT-ZQX-001` ĐVT Cặp + công đoạn Sơn → tìm "uat zqx" thấy ngay; sửa quy đổi 0 → lỗi dưới ô; quanly sửa giá bán → được. Sau thử đặt mã ngừng kinh doanh. Ghi SUMMARY.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `san-pham.schema.ts` chứa `gt(0, "Quy đổi phải lớn hơn 0")` và `dvt_id` + `cong_doan_id` là hai trường riêng
    - `ngan-keo-san-pham.tsx` chứa `disabled={!quyen.suaGiaBan}`, `"23505"`, `reset(`, `guiGiaBan`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>DMUC-04 dùng được, đúng phân quyền giá</done>
</task>

</tasks>

<verification>
- [ ] `npm run check` xanh, thử tay ghi SUMMARY
</verification>

<success_criteria>
- Văn phòng tìm và sửa mã nhanh hơn KiotViet trên dữ liệu thật
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-16-SUMMARY.md`
</output>
