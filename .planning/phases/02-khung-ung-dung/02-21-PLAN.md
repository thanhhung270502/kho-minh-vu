---
phase: 02-khung-ung-dung
plan: 21
type: execute
wave: 9
depends_on: ["02-14", "02-15", "02-17", "02-18", "02-20"]
files_modified:
  - src/app/layout.tsx
  - src/providers/antd-theme.ts
  - src/providers/query-client.ts
  - src/features/README.md
  - CLAUDE.md
  - scripts/kiem-tra-quyen-route.ts
  - .memory/blockers/mo-sau-phase-1.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-07, DMUC-01, DMUC-02, DMUC-03, DMUC-04, DMUC-05, DMUC-06, DMUC-07, DTAC-01, DTAC-02, DTAC-03, DLIEU-04, CDAT-01, CDAT-02, CDAT-03, CDAT-04]
user_setup: []
must_haves:
  truths:
    - "Không còn chữ 'sản xuất', 'xưởng', 'lô' của phạm vi cũ ở bất kỳ file src/ nào"
    - "Mỗi route nội bộ trả đúng hành vi cho 4 vai trò: vào được / chuyển Không đủ quyền / chuyển đăng nhập"
    - "Toàn bộ pgTAP, verify:hook, npm run check xanh cùng lúc"
    - "Tài liệu feature và CLAUDE.md phản ánh cấu trúc thật sau Phase 2"
  artifacts:
    - path: "scripts/kiem-tra-quyen-route.ts"
      provides: "Kiểm HTTP mọi route × 4 vai trò bằng phiên thật"
  key_links:
    - from: "scripts/kiem-tra-quyen-route.ts"
      to: "npm run dev (localhost:3000)"
      via: "đăng nhập bằng anon client, gửi cookie phiên @supabase/ssr"
---

<objective>
Khép Phase 2: dọn dấu vết phạm vi cũ, kiểm quyền route tự động cho 4 vai trò, cập nhật tài liệu, chạy toàn
bộ bộ kiểm. Chuẩn bị cho `/spartan:phase verify 2`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/features/README.md
@src/app/layout.tsx
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@.planning/phases/02-khung-ung-dung/02-VALIDATION.md
@.memory/blockers/mo-sau-phase-1.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dọn phạm vi cũ + cập nhật tài liệu</name>
  <files>src/app/layout.tsx, src/providers/antd-theme.ts, src/providers/query-client.ts, src/features/README.md, CLAUDE.md, .memory/blockers/mo-sau-phase-1.md</files>
  <read_first>src/app/layout.tsx, src/providers/antd-theme.ts, src/providers/query-client.ts, src/features/README.md, CLAUDE.md, .memory/blockers/mo-sau-phase-1.md</read_first>
  <action>
1. `grep -rni "sản xuất\|san xuat\|xưởng\|xuong\|lô \|lệnh sản xuất\|phế phẩm\|FTY" src/` → sửa mọi kết quả:
   - `src/app/layout.tsx` metadata: `title: { default: "Kho Minh Vũ", template: "%s · Kho Minh Vũ" }`, `description: "Quản lý xuất nhập tồn phụ tùng xe máy — CTY TNHH SX-TM P.Tùng Xe Máy Minh Vũ"`.
   - Comment trong `antd-theme.ts`, `query-client.ts`: "sản xuất/xưởng/lô" → "kho/phiếu/mã hàng". Không đổi giá trị cấu hình.
2. `src/features/README.md`: bảng feature thật — `xac-thuc` (đăng nhập, đổi mật khẩu, người dùng hiện tại), `danh-muc` (mã hàng, bộ lọc, Excel, rà dữ liệu), `doi-tac` (NCC + khách, rà ghi chú, lịch sử giao dịch), `cai-dat` (người dùng, kho, danh mục phụ, số chứng từ). Ghi quy ước mới: file `.server.ts` chỉ import từ server; `src/shared/lib/quyen.ts` chỉ quyết ẩn/hiện, chặn thật ở RLS.
3. `CLAUDE.md` — thêm vào "Bẫy đã gặp":
   - "5. `san_pham` và `kho_movement` không có quyền SELECT mức bảng (giá vốn). Không `select('*')`, không `.select()` trống sau insert/update; cột mới phải `grant select (<cột>)`."
   - "6. Quyền mở rộng cho người dùng có hiệu lực khi token làm mới; quyền thu hẹp có hiệu lực ngay (helper RLS đối chiếu claim với bảng)."
   - "7. Đọc Excel chỉ ở server (`src/shared/lib/o-excel.ts`, stream reader `styles: "ignore"`)."
   Mục "Lệnh hay dùng": thêm `npx tsx scripts/kiem-tra-ham-thuan.ts`, `npx tsx scripts/kiem-tra-doc-excel.ts`, `npx tsx scripts/kiem-tra-quyen-route.ts`.
4. `.memory/blockers/mo-sau-phase-1.md`: đánh dấu đã xong các mục "Chuyển sang Phase 2" (DLIEU-04 có công cụ — việc RÀ THẬT do người văn phòng làm; gán công đoạn có công cụ; app-shell/errors đã sửa; thu hồi phiên đã giải). Giữ mục "Trước go-live" và thêm: "Người văn phòng rà ~150 giá trị ghi chú và 364 mã Cần rà trên dữ liệu thật", "Hỏi người dùng có xóa cột nguoi_dung.kho_id không".
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rni "sản xuất\|xưởng\|phế phẩm" src/` không trả kết quả
    - `src/app/layout.tsx` chứa `Kho Minh Vũ`
    - `CLAUDE.md` chứa `grant select (<cột>)` và `kiem-tra-quyen-route`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>Không còn dấu vết phạm vi cũ, tài liệu đúng thực tế</done>
</task>

<task type="auto">
  <name>Task 2: Kiểm quyền route × 4 vai trò bằng phiên thật + chạy toàn bộ bộ kiểm</name>
  <files>scripts/kiem-tra-quyen-route.ts</files>
  <read_first>scripts/_supabase-admin.ts, scripts/verify-hook.ts, src/lib/supabase/middleware.ts, node_modules/@supabase/ssr/dist/main/index.d.ts, src/proxy.ts</read_first>
  <action>
**`scripts/kiem-tra-quyen-route.ts`** — chạy khi `npm run dev` đang chạy (`BASE_URL` mặc định `http://localhost:3000`; không chạy được thì in hướng dẫn và exit 1):
- Đăng nhập từng tài khoản mẫu bằng `createServerClient` của `@supabase/ssr` với bộ nhớ cookie trong RAM (`getAll/setAll` vào một `Map`) → lấy cookie phiên đúng định dạng app đọc.
- `fetch(BASE_URL + route, { headers: { cookie }, redirect: "manual" })` và so với bảng kỳ vọng:

| Route | quanly | vanphong | thukho1 | chixem | không đăng nhập |
|---|---|---|---|---|---|
| `/` | 200 | 200 | 200 | 200 | 307→/dang-nhap |
| `/danh-muc` | 200 | 200 | 200 | 200 | 307→/dang-nhap?tiep_tuc=%2Fdanh-muc |
| `/doi-tac` | 200 | 200 | 200 | 200 | 307 |
| `/doi-tac/ra-ghi-chu` | 200 | 200 | →/khong-du-quyen | →/khong-du-quyen | 307 |
| `/cai-dat/nguoi-dung` | 200 | →/khong-du-quyen | →/khong-du-quyen | →/khong-du-quyen | 307 |
| `/cai-dat/nhom-hang` | 200 | 200 | →/khong-du-quyen | →/khong-du-quyen | 307 |
| `/cai-dat/so-chung-tu` | 200 | →/khong-du-quyen | →/khong-du-quyen | →/khong-du-quyen | 307 |
| `/api/danh-muc/mau-excel` | 200 | 200 | 200 | 200 | 401 |
| `/dang-nhap?tiep_tuc=//evil.com` (đã đăng nhập) | 307→/ | … | … | … | 200 |

  "→/khong-du-quyen" nghĩa là status 307/303 với `location` kết thúc bằng `/khong-du-quyen` (Server Component `redirect` trả 307 kèm header location; nếu Next trả 200 với RSC redirect payload thì kiểm body chứa `/khong-du-quyen` — ghi rõ cách nhận biết vào comment).
  Sai ô nào in bảng lệch và exit 1. Đúng hết in `✓ quyền route: <n>/<n> ô đúng`.
- Không in cookie hay token ra màn hình.

**Chạy toàn bộ** (ghi kết quả vào SUMMARY):
```bash
npm run db:test:linked
npm run verify:hook
npx tsx scripts/kiem-tra-ham-thuan.ts
npx tsx scripts/kiem-tra-doc-excel.ts
npm run check
npm run dev   # nền
npx tsx scripts/kiem-tra-quyen-route.ts
```
Cập nhật `02-VALIDATION.md`: cột Status các dòng đã xanh → ✅, `wave_0_complete: true`.
  </action>
  <verify>
    <automated>npm run db:test:linked && npm run verify:hook && npx tsx scripts/kiem-tra-ham-thuan.ts && npx tsx scripts/kiem-tra-doc-excel.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/kiem-tra-quyen-route.ts` chứa `/cai-dat/so-chung-tu`, `/khong-du-quyen`, `//evil.com`
    - Script in `✓ quyền route` khi chạy với dev server
    - Toàn bộ lệnh verify exit 0
    - `02-VALIDATION.md` có `wave_0_complete: true`
  </acceptance_criteria>
  <done>Phase 2 sẵn sàng cho UAT</done>
</task>

</tasks>

<verification>
- [ ] Mọi lệnh kiểm tự động xanh
- [ ] Ma trận quyền route đúng 45 ô
</verification>

<success_criteria>
- 5 success criteria của ROADMAP §Phase 2 có bằng chứng tự động hoặc thử tay trong các SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-21-SUMMARY.md`
</output>
