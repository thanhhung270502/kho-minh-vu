---
phase: 02-khung-ung-dung
plan: 17
type: execute
wave: 7
depends_on: ["02-16", "02-13"]
files_modified:
  - src/shared/components/lich-su-sua.tsx
  - src/app/(app)/danh-muc/[id]/page.tsx
  - src/features/danh-muc/components/chi-tiet-san-pham.tsx
  - src/features/danh-muc/components/the-kho.tsx
  - src/app/(app)/doi-tac/[id]/page.tsx
  - src/features/doi-tac/components/chi-tiet-doi-tac.tsx
  - src/features/doi-tac/components/lich-su-giao-dich.tsx
autonomous: true
requirements: [DMUC-05, DTAC-03]
user_setup: []
must_haves:
  truths:
    - "Trang /danh-muc/[id] hiện thông tin mã, tồn theo từng kho, thẻ kho và tab Lịch sử sửa"
    - "Thẻ kho gộp biến động hệ thống với dòng KiotViet cũ (nhãn KiotViet, chỉ đọc); lọc theo kho; phân trang"
    - "Thủ kho chỉ thấy biến động kho được phân, không thấy dòng KiotViet, không thấy giá vốn"
    - "Trang /doi-tac/[id] hiện thông tin đối tác, lịch sử giao dịch gom theo phiếu (mới + KiotViet) và tab Lịch sử sửa"
    - "Tab Lịch sử sửa chỉ hiện với quản lý/văn phòng; hiện ai, lúc nào, trường nào, cũ → mới, nguồn"
    - "Id không tồn tại hiện trạng thái Không tìm thấy có nút quay lại danh sách"
  artifacts:
    - path: "src/shared/components/lich-su-sua.tsx"
      provides: "LichSuSua dùng chung cho sản phẩm và đối tác"
    - path: "src/features/danh-muc/components/the-kho.tsx"
      provides: "TheKho — Phase 5 (TON-02) dùng lại"
  key_links:
    - from: "the-kho.tsx"
      to: "rpc the_kho_san_pham"
      via: "useTheKho(id, khoId, trang)"
---

<objective>
Hai trang chi tiết (DMUC-05, DTAC-03; D-20, D-21, D-27). Ngày đầu, thẻ kho và lịch sử đối tác đã có dữ
liệu thật từ KiotViet — đối ứng rủi ro "văn phòng không bỏ KiotViet".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@src/features/danh-muc/hooks/useSanPham.ts
@src/features/doi-tac/hooks/useDoiTac.ts
@src/shared/api/lich-su-sua.api.ts
@src/shared/components/query-state.tsx
@src/shared/lib/quyen.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Component Lịch sử sửa dùng chung</name>
  <files>src/shared/components/lich-su-sua.tsx</files>
  <read_first>src/shared/api/lich-su-sua.api.ts, src/types/database.types.ts, src/shared/components/query-state.tsx</read_first>
  <action>
`"use client"`. Props `{ bang: "san_pham" | "doi_tac" | "nguoi_dung"; id: string; nhanTruong: Record<string, string>; hienGiaTri?: (truong: string, v: unknown) => ReactNode }`.
- `useQuery({ queryKey: khoaLichSuSua(bang, id), queryFn: () => layLichSuSua(bang, id) })` trong `QueryState`.
- Gom các dòng cùng `sua_luc` + `nguoi_sua_id` + `nguon` thành một mục `Timeline` (một lần lưu sửa nhiều trường = một mục).
- Mỗi mục: tiêu đề `<họ tên hoặc "Hệ thống/Script">` · `dayjs(sua_luc).format("HH:mm DD/MM/YYYY")` · `Tag` nguồn
  (`form: "Sửa tay"`, `sua_o: "Sửa trên bảng"`, `hang_loat: "Sửa hàng loạt"`, `goi_y_duoi: "Gợi ý công đoạn"`, `import: "Nhập Excel"`, `ra_ghi_chu: "Rà ghi chú"`, `cai_dat: "Cài đặt"`, `script: "Nạp dữ liệu"`).
  Nội dung: danh sách `<nhãn trường>: <cũ> → <mới>` (cũ gạch ngang màu xám). `_tao_moi` hiện "Tạo mới".
- Giá trị: `hienGiaTri` nếu truyền (để đổi uuid nhóm/ĐVT/công đoạn thành tên), mặc định `null` → "(trống)", boolean → "Có/Không", còn lại `String(v)`.
- Rỗng: "Chưa có lần sửa nào kể từ khi bật nhật ký."
- Lỗi 42501 → QueryState đã hiện "không đủ quyền" không nút thử lại.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File bắt đầu `"use client"`, chứa `Timeline`, `khoaLichSuSua`, `"Nhập Excel"`, `"_tao_moi"`
    - `npm run typecheck` exit 0
  </acceptance_criteria>
  <done>Một component nhật ký cho mọi bản ghi</done>
</task>

<task type="auto">
  <name>Task 2: Trang chi tiết mã hàng + thẻ kho</name>
  <files>src/app/(app)/danh-muc/[id]/page.tsx, src/features/danh-muc/components/chi-tiet-san-pham.tsx, src/features/danh-muc/components/the-kho.tsx</files>
  <read_first>src/features/danh-muc/hooks/useSanPham.ts, src/features/danh-muc/types.ts, src/features/danh-muc/components/ngan-keo-san-pham.tsx, src/shared/components/lich-su-sua.tsx</read_first>
  <action>
**page** (Server): `params: Promise<{ id: string }>` (Next 16 — `await params`); id không phải uuid → `notFound()`.
`const nd = await yeuCauQuyen("xem_danh_muc")`; render `<ChiTietSanPham id={id} quyen={{ sua, xemGiaVon, suaGiaBan, xemLichSu: coQuyen(nd.vaiTro,"sua_danh_muc") }} />`.
metadata động không cần (tiêu đề đặt ở client bằng `PageHeader`).

**`chi-tiet-san-pham.tsx`** (`"use client"`):
- `useChiTietSanPham(id)` trong `QueryState`; `laRong = (d) => d === null` với `moTaRong` "Không tìm thấy mã hàng này — có thể đã bị đổi mã." + nút "Về danh mục".
- `PageHeader tieuDe={ma_hang} moTa={ten_hang}` + hành động: nút "Sửa" (mở `NganKeoSanPham`) khi `quyen.sua`; link "← Danh mục" dùng `router.back()` nếu `document.referrer` cùng origin, ngược lại `/danh-muc` (giữ bộ lọc cũ).
- `Descriptions` (bordered, `column={{ xs: 1, sm: 2, lg: 3 }}`): Nhóm hàng · ĐVT · Công đoạn (Tag màu) · Quy đổi · Kho mặc định · Tồn tối thiểu/tối đa · Giá bán · Giá vốn (chỉ `xemGiaVon`) · Barcode · Trạng thái (Ngừng KD / Cần rà) · Cập nhật lúc.
- Khối "Tồn theo kho": `useTonTheoKho(id)` → các `Statistic` nhỏ theo kho; rỗng → "Chưa có tồn — chưa có chứng từ nào cho mã này".
- `Tabs`: "Thẻ kho" → `<TheKho sanPhamId={id} xemGiaVon={...} />`; "Lịch sử sửa" (chỉ `xemLichSu`) → `<LichSuSua bang="san_pham" id={id} nhanTruong={{ ma_hang: "Mã hàng", ten_hang: "Tên hàng", nhom_hang_id: "Nhóm hàng", dvt_id: "Đơn vị tính", cong_doan_id: "Công đoạn", quy_doi: "Quy đổi", kho_mac_dinh_id: "Kho mặc định", ton_toi_thieu: "Tồn tối thiểu", ton_toi_da: "Tồn tối đa", gia_ban: "Giá bán", dang_kinh_doanh: "Đang kinh doanh", barcode: "Barcode", ghi_chu: "Ghi chú", can_ra_dvt: "Cờ ĐVT mâu thuẫn", da_xac_nhan_ra: "Đã xác nhận rà" }} hienGiaTri={...đổi uuid → tên qua useDanhMucPhu...} />`.

**`the-kho.tsx`** (`"use client"`): props `{ sanPhamId: string; xemGiaVon: boolean }`.
- State `khoId` (Select "Tất cả kho" + danh sách kho từ `useDanhMucPhu`) và `trang`.
- `useTheKho(sanPhamId, khoId, trang)` trong `QueryState`; rỗng: khoId null → "Chưa có biến động nào. Thẻ kho ghi nhận từ phiếu nhập/xuất đầu tiên."; có khoId → "Kho này chưa có biến động."
- Chọn một kho cụ thể → hiện `Alert type="info"` nhỏ "Dữ liệu KiotViet cũ không gắn kho nên chỉ hiện khi xem Tất cả kho."
- `Table` `size="small"`, cuộn ngang; cột: Ngày giờ (`HH:mm DD/MM/YYYY`) · Nguồn (`Tag`: HE_THONG "Hệ thống" xanh, KIOTVIET_NHAP "KiotViet · nhập" xám, KIOTVIET_BAN "KiotViet · bán" xám) · Số phiếu (hệ thống: text — link chứng từ để Phase 3; KiotViet: text) · Kho · Đối tác/Ghi chú · Nhập (xanh, right) · Xuất (đỏ, right) · Giá vốn lúc đó (chỉ `xemGiaVon`) · Đảo (Tag "Bút toán đảo").
- Phân trang server: `pagination={{ current: trang, pageSize: 50, total: data[0]?.tong_so_dong ?? 0 }}`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/danh-muc/[id]/page.tsx` chứa `await params` và `notFound()`
    - `the-kho.tsx` chứa `"KIOTVIET_BAN"`, `"KIOTVIET_NHAP"`, `useTheKho`
    - `chi-tiet-san-pham.tsx` chứa `<LichSuSua` trong nhánh `xemLichSu` và `useTonTheoKho`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>DMUC-05 dùng được với lịch sử KiotViet thật</done>
</task>

<task type="auto">
  <name>Task 3: Trang chi tiết đối tác + lịch sử giao dịch</name>
  <files>src/app/(app)/doi-tac/[id]/page.tsx, src/features/doi-tac/components/chi-tiet-doi-tac.tsx, src/features/doi-tac/components/lich-su-giao-dich.tsx</files>
  <read_first>src/features/doi-tac/hooks/useDoiTac.ts, src/features/doi-tac/components/ngan-keo-doi-tac.tsx, src/features/danh-muc/components/chi-tiet-san-pham.tsx, src/shared/components/lich-su-sua.tsx</read_first>
  <action>
**page**: giống trang mã hàng (await params, uuid → notFound, `yeuCauQuyen("xem_danh_muc")`).
**`chi-tiet-doi-tac.tsx`**: `useChiTietDoiTac(id)` (`QueryState`, không tìm thấy → nút về `/doi-tac`); `PageHeader tieuDe={ten} moTa={<Tag loai> · mã}` + nút Sửa (`NganKeoDoiTac`); `Descriptions`: Điện thoại (link `tel:`), Email, Địa chỉ, Khu vực, Mã số thuế, Ghi chú, Trạng thái.
Với khách hàng: dòng phụ "Tên trong ô Ghi chú KiotViet: <danh sách gia_tri ánh xạ>" — lấy bằng `from("anh_xa_ghi_chu_kiotviet").select("gia_tri, loai").eq("doi_tac_id", id)` (RLS chỉ QL/VP; lỗi 42501 thì ẩn dòng này, không hiện lỗi).
`Tabs`: "Giao dịch" → `<LichSuGiaoDich doiTacId={id} />`; "Lịch sử sửa" (QL/VP) → `<LichSuSua bang="doi_tac" ... nhanTruong={{ ma: "Mã", ten: "Tên", loai: "Loại", dien_thoai: "Điện thoại", email: "Email", dia_chi: "Địa chỉ", khu_vuc: "Khu vực", ma_so_thue: "Mã số thuế", ghi_chu: "Ghi chú", dang_hoat_dong: "Đang hoạt động" }} />`.
**`lich-su-giao-dich.tsx`**: `useLichSuGiaoDich(id, trang)` trong `QueryState`; cột: Ngày · Nguồn (Tag như thẻ kho) · Số phiếu · Loại (NHAP "Nhập", XUAT "Xuất") · Số dòng · Tổng số lượng (right) · Ghi chú. Phân trang server 50.
Rỗng: "Chưa có giao dịch. Với khách hàng tạo từ Rà ghi chú, hóa đơn KiotViet hiện ở đây sau khi gán ghi chú cho khách."
Thủ kho/chỉ xem: RPC chỉ trả chứng từ mới trong phạm vi (không KiotViet) — không cần xử lý thêm ở UI.

Thử tay (SUMMARY): mở NCC `NCC000001` → thấy ~289 phiếu nhập KiotViet gom theo phiếu (số trong `.memory/knowledge/du-lieu-kiotviet.md`); mở một mã `-CB` quen → thẻ kho có dòng KiotViet · bán.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `chi-tiet-doi-tac.tsx` chứa `anh_xa_ghi_chu_kiotviet` và `<LichSuSua`
    - `lich-su-giao-dich.tsx` chứa `useLichSuGiaoDich`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>DTAC-03 dùng được; lịch sử NCC thật hiện ngày đầu</done>
</task>

</tasks>

<verification>
- [ ] `npm run check` xanh; hai thử tay ghi SUMMARY
</verification>

<success_criteria>
- Chi tiết mã và đối tác trả lời được "hàng này đã nhập/bán thế nào" bằng dữ liệu thật
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-17-SUMMARY.md`
</output>
