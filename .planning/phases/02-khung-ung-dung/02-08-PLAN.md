---
phase: 02-khung-ung-dung
plan: 08
type: execute
wave: 3
depends_on: ["02-02", "02-04"]
files_modified:
  - supabase/migrations/0034_import_danh_muc.sql
  - supabase/migrations/0035_ra_hang_loat.sql
  - supabase/tests/61_import_danh_muc_test.sql
  - supabase/tests/62_ra_hang_loat_test.sql
autonomous: true
requirements: [DMUC-06, DMUC-04]
user_setup: []
must_haves:
  truths:
    - "nhap_danh_muc ở chế độ kiểm tra trả tóm tắt thêm/sửa/không đổi/lỗi theo dòng+cột và KHÔNG ghi gì"
    - "Có bất kỳ dòng lỗi nào thì chế độ nạp cũng không ghi gì (không nạp nửa vời)"
    - "Chế độ nạp tự validate lại từ đầu, ghi trong một transaction, mã có sẵn chỉ sửa ô có giá trị, ô trống giữ nguyên"
    - "Tên nhóm/ĐVT/công đoạn/kho không có trong hệ → lỗi dòng; khớp theo mã hoặc tên không phân biệt hoa thường và dấu"
    - "Văn phòng import giá bán khác giá hiện tại → lỗi dòng, không ném exception giữa chừng"
    - "gan_hang_loat đổi công đoạn/nhóm/ĐVT/kinh doanh cho tối đa 1000 mã trong một lệnh, ghi nhật ký với nguồn truyền vào"
    - "goi_y_cong_doan_theo_duoi liệt kê 145 mã MUA_NGOAI có đuôi kèm công đoạn đề xuất; ap_dung_goi_y_cong_doan chỉ đổi mã còn MUA_NGOAI và đúng đuôi"
    - "Thủ kho và chỉ xem không gọi được các RPC ghi (42501)"
  artifacts:
    - path: "supabase/migrations/0034_import_danh_muc.sql"
      provides: "nhap_danh_muc(jsonb, boolean)"
    - path: "supabase/migrations/0035_ra_hang_loat.sql"
      provides: "gan_hang_loat, goi_y_cong_doan_theo_duoi, ap_dung_goi_y_cong_doan"
  key_links:
    - from: "Route Handler /api/danh-muc/nhap-excel (plan 12)"
      to: "public.nhap_danh_muc"
      via: "supabase.rpc('nhap_danh_muc', { p_dong, p_chi_kiem_tra })"
---

<objective>
Backend cho import Excel (DMUC-06, D-24..D-26) và bốn công cụ rà hàng loạt (D-18).

**Hợp đồng dữ liệu import (plan 12 chuyển cả hai định dạng file về đúng shape này):**
```json
[{ "dong": 2, "ma_hang": "HVR23-75-35-CB", "ten_hang": "...", "nhom_hang": "Hàng Hãng - L5/6",
   "dvt": "Cái", "cong_doan": "CARBON", "quy_doi": 1, "kho_mac_dinh": "Kho 1",
   "ton_toi_thieu": 0, "ton_toi_da": null, "gia_ban": null, "dang_kinh_doanh": true,
   "barcode": null, "ghi_chu": null }]
```
Khóa vắng hoặc giá trị `null` = GIỮ NGUYÊN khi sửa. `dong` là số dòng thật trong Excel.
Khóa phụ `cong_doan_khi_tao_moi` (tùy chọn): CHỈ dùng khi mã chưa có và `cong_doan` null. Lý do: file
KiotViet ghi "CÁI" cho 1.571 mã — không suy được công đoạn. Nếu route handler gửi `cong_doan: "MUA_NGOAI"`
thì import lại file KiotViet sẽ XÓA công sức rà công đoạn của người dùng. Route handler gửi
`cong_doan: null, cong_doan_khi_tao_moi: "MUA_NGOAI"` cho các dòng không suy được.
Kết quả:
```json
{ "tong": 10, "them": 2, "sua": 3, "khong_doi": 5, "da_nap": false,
  "loi": [{ "dong": 7, "cot": "dvt", "thong_bao": "Không có đơn vị tính 'Thùng'" }],
  "thay_doi": [{ "dong": 3, "ma_hang": "X", "loai": "SUA", "truong": { "ten_hang": ["cũ", "mới"] } }] }
```
`thay_doi` cắt tối đa 500 phần tử (kèm `"thay_doi_bi_cat": true`) để response không phình.

**Quyết định:** RPC SECURITY INVOKER — RLS "them/sua san pham", quyền cột (không có gia_von) và trigger
giá bán tự áp; chỉ quản lý/văn phòng ghi được. Kiểm vai trò tường minh đầu hàm để trả 42501 rõ ràng
thay vì âm thầm update 0 dòng.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@.planning/phases/02-khung-ung-dung/02-RESEARCH.md
@supabase/migrations/0015_rls_danh_muc.sql
@supabase/migrations/0024_nap_danh_muc_day_du.sql
@supabase/migrations/0027_nhat_ky_sua.sql
@supabase/migrations/0029_an_gia_von.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test 61 đỏ, rồi 0034 nhap_danh_muc</name>
  <files>supabase/tests/61_import_danh_muc_test.sql, supabase/migrations/0034_import_danh_muc.sql</files>
  <read_first>supabase/migrations/0024_nap_danh_muc_day_du.sql, supabase/migrations/0015_rls_danh_muc.sql, supabase/migrations/0018_du_lieu_nen.sql, supabase/migrations/0025_kho_mac_dinh.sql, supabase/migrations/0027_nhat_ky_sua.sql, supabase/tests/00_helper.sql.inc</read_first>
  <action>
**Test `supabase/tests/61_import_danh_muc_test.sql`** (`plan(14)`). Fixture dưới postgres:
`pg_temp.sp_test('IM-ZQX-001')` (ten 'Hàng test IM-ZQX-001', ĐVT CAI, MUA_NGOAI).
Biến jsonb dựng sẵn trong bảng tạm `t_in(ten text, du_lieu jsonb)` và `grant select on t_in to authenticated`:
- `kv`: `{dong:2, ma_hang:'IM-ZQX-001', cong_doan:null, cong_doan_khi_tao_moi:'MUA_NGOAI'}` và `{dong:3, ma_hang:'IM-ZQX-006', ten_hang:'KV mới', dvt:'CAI', cong_doan:null, cong_doan_khi_tao_moi:'MUA_NGOAI'}` — dùng ở assertion 13–14
- `sach`: 2 dòng — `{dong:2, ma_hang:'IM-ZQX-002', ten_hang:'Mới', dvt:'cái', cong_doan:'carbon'}` (dvt/công đoạn gõ thường không dấu) và `{dong:3, ma_hang:'IM-ZQX-001', ten_hang:'Tên sửa'}`
- `loi`: 3 dòng — `{dong:2, ma_hang:'IM-ZQX-003', ten_hang:'A', dvt:'Thùng'}`, `{dong:3, ma_hang:'IM-ZQX-004'}` (thiếu tên khi tạo mới), `{dong:4, ma_hang:'IM-ZQX-001', quy_doi:0}`
- `trung`: 2 dòng cùng `ma_hang:'IM-ZQX-005'`
- `gia`: `{dong:2, ma_hang:'IM-ZQX-001', gia_ban: 50000}`

1. vanphong, `nhap_danh_muc(sach, true)` → `->>'them' = '1'`, `->>'sua' = '1'`, `->>'da_nap' = 'false'`.
2. Sau (1): `is((select count(*) from public.san_pham where ma_hang = 'IM-ZQX-002'), 0::bigint, 'chế độ kiểm tra không ghi gì')`
3. vanphong, `nhap_danh_muc(loi, false)` → `jsonb_array_length(->'loi') = 3` và `->>'da_nap' = 'false'`.
4. `ok((select r->'loi' @> '[{"dong":2,"cot":"dvt"}]' ...), 'lỗi chỉ đúng dòng và cột')`
5. Sau (3): `is((select ten_hang from public.san_pham where ma_hang='IM-ZQX-001'), 'Hàng test IM-ZQX-001', 'có lỗi thì không nạp dòng nào')`
6. vanphong, `nhap_danh_muc(trung, true)` → mọi lỗi có `cot = 'ma_hang'`, số lỗi = 2.
7. vanphong, `nhap_danh_muc(sach, false)` → `->>'da_nap' = 'true'`; `IM-ZQX-002` tồn tại với `cong_doan_id` = CARBON.
8. `is((select ghi_chu is not distinct from (select ghi_chu from ... trước) ...)` — đơn giản hơn: trước (7) đặt `ghi_chu='giữ'` cho IM-ZQX-001 dưới postgres; sau (7) `is(ghi_chu, 'giữ', 'ô trống giữ nguyên giá trị cũ')`.
9. `ok(exists(select 1 from public.nhat_ky_sua n join public.san_pham s on s.id = n.ban_ghi_id where s.ma_hang='IM-ZQX-001' and n.truong='ten_hang' and n.nguon='import'), 'sửa qua import vào nhật ký nguồn import')`
10. vanphong, `nhap_danh_muc(gia, false)` → có lỗi `cot = 'gia_ban'`, `da_nap = false` (không exception).
11. quanly, `nhap_danh_muc(gia, false)` → `da_nap = true`, `gia_ban = 50000`.
12. thukho1: `throws_ok($$select public.nhap_danh_muc('[]'::jsonb, true)$$, '42501', null, 'thủ kho không import được')`
13. Dưới postgres đặt `IM-ZQX-001` công đoạn CARBON; vanphong `nhap_danh_muc(kv, false)` → `IM-ZQX-001` VẪN là CARBON (`'import file KiotViet không ghi đè công đoạn đã rà'`).
14. `IM-ZQX-006` được tạo với công đoạn MUA_NGOAI (`'mã mới từ KiotViet nhận công đoạn mặc định'`).

**Migration `supabase/migrations/0034_import_danh_muc.sql`:**
```sql
create or replace function public.khop_danh_muc(p_bang text, p_gia_tri text)
returns uuid language plpgsql stable set search_path = '' as $$
-- Khớp theo mã hoặc tên, không phân biệt hoa thường/dấu. Không thấy → null. Nhiều hơn một → ném 'P0001' do hàm gọi bắt.
declare v_ids uuid[]; v_chuan text := upper(public.f_unaccent(trim(p_gia_tri)));
begin
  if nullif(trim(coalesce(p_gia_tri,'')),'') is null then return null; end if;
  if p_bang = 'nhom_hang' then
    select array_agg(id) into v_ids from public.nhom_hang where upper(public.f_unaccent(ma)) = v_chuan or upper(public.f_unaccent(ten)) = v_chuan;
  elsif p_bang = 'don_vi_tinh' then
    select array_agg(id) into v_ids from public.don_vi_tinh where upper(public.f_unaccent(ma)) = v_chuan or upper(public.f_unaccent(ten)) = v_chuan;
  elsif p_bang = 'cong_doan' then
    select array_agg(id) into v_ids from public.cong_doan where upper(public.f_unaccent(ma)) = v_chuan or upper(public.f_unaccent(ten)) = v_chuan
      or upper(public.f_unaccent(replace(ma,'_',' '))) = v_chuan;
  elsif p_bang = 'kho' then
    select array_agg(id) into v_ids from public.kho where upper(public.f_unaccent(ma)) = v_chuan or upper(public.f_unaccent(ten)) = v_chuan;
  else
    raise exception 'Bảng khớp không hợp lệ: %', p_bang;
  end if;
  if coalesce(cardinality(v_ids), 0) > 1 then
    raise exception 'NHIEU_KET_QUA';
  end if;
  return v_ids[1];
end $$;
```
`nhap_danh_muc(p_dong jsonb, p_chi_kiem_tra boolean default true) returns jsonb`,
`language plpgsql volatile set search_path = ''` (INVOKER). Thuật toán:
1. Vai trò ∉ {quan_ly, van_phong} → `raise ... using errcode = '42501'`.
2. `jsonb_typeof(p_dong) <> 'array'` hoặc > 10000 phần tử → 23514.
3. Bảng tạm làm việc: `drop table if exists pg_temp._nhap; create temp table _nhap (dong int, ma_hang text, du_lieu jsonb, sp_id uuid, nhom_id uuid, dvt_id uuid, cd_id uuid, kho_id uuid, loai text) on commit drop;` và tương tự `_loi (dong int, cot text, thong_bao text)`. PHẢI `drop table if exists` trước: pgTAP gọi hàm nhiều lần trong CÙNG một transaction, `on commit drop` chưa kịp chạy → lần gọi thứ hai lỗi "relation already exists".
4. Duyệt từng phần tử `e` (`jsonb_array_elements`): `v_ma := nullif(trim(e->>'ma_hang'),'')`.
   - Thiếu mã → lỗi `ma_hang` "Thiếu mã hàng".
   - Tra `sp_id` bằng `select id from public.san_pham where ma_hang = v_ma`.
   - Với mỗi khóa `nhom_hang`/`dvt`/`cong_doan`/`kho_mac_dinh` có mặt và khác null: gọi `khop_danh_muc` trong
     `begin ... exception when others then` → lỗi "Có nhiều '<giá trị>' trùng tên, dùng mã"; null → lỗi
     "Không có <nhãn> '<giá trị>'" (nhãn: nhóm hàng / đơn vị tính / công đoạn / kho). Cột lỗi đúng tên khóa.
   - Tạo mới (`sp_id is null`): thiếu `ten_hang` → lỗi `ten_hang` "Mã mới phải có tên hàng"; thiếu `dvt` → lỗi `dvt` "Mã mới phải có đơn vị tính"; thiếu cả `cong_doan` lẫn `cong_doan_khi_tao_moi` → lỗi `cong_doan` tương tự (khớp `cong_doan_khi_tao_moi` bằng `khop_danh_muc` như `cong_doan`).
   - `quy_doi` có mặt: không phải số hoặc ≤ 0 → lỗi `quy_doi` "Quy đổi phải lớn hơn 0". `ton_toi_thieu`/`ton_toi_da` < 0 → lỗi. `gia_ban` < 0 → lỗi.
   - `gia_ban` có mặt, khác giá hiện tại (hoặc ≠ 0 khi tạo mới) và vai trò ≠ quan_ly → lỗi `gia_ban` "Chỉ quản lý được đặt giá bán". (Kiểm TRƯỚC khi ghi để trigger 42501 không nổ giữa chừng.)
   - `dang_kinh_doanh` nhận `true/false` hoặc chuỗi `có/không/1/0` (chuẩn hóa không dấu).
   - Ép kiểu số bằng khối `begin (e->>'quy_doi')::numeric exception when others then <lỗi> end`.
5. Mã trùng trong file: `insert into _loi select dong, 'ma_hang', 'Mã hàng xuất hiện nhiều lần trong file' from _nhap where ma_hang in (select ma_hang from _nhap group by 1 having count(*) > 1)`.
6. Tính thay đổi: với mã có sẵn, so từng trường có mặt với giá trị hiện tại (so theo id cho khóa ngoại) → nếu không trường nào khác thì `khong_doi`, ngược lại `sua` + ghi `thay_doi` (dạng `{truong: [cũ, mới]}` — tên hiển thị cho khóa ngoại: tên nhóm/ĐVT/công đoạn/kho). Mã mới → `them`.
7. Có lỗi hoặc `p_chi_kiem_tra` → trả jsonb, KHÔNG ghi.
8. Nạp: `perform set_config('app.nguon_sua', 'import', true);` rồi insert mã mới (cột: `ma_hang, ten_hang, nhom_hang_id, dvt_id, cong_doan_id, quy_doi, kho_mac_dinh_id, ton_toi_thieu, ton_toi_da, gia_ban, dang_kinh_doanh, barcode, ghi_chu` — mặc định `quy_doi` 1, `gia_ban` 0, `dang_kinh_doanh` true) và update mã có sẵn với `col = coalesce(<giá trị mới>, col)` CHỈ cho dòng `sua`. KHÔNG `returning *` (plan 04 thu quyền đọc bảng). Trả jsonb với `da_nap: true`.

Grant: `revoke all ... from public, anon; grant execute ... to authenticated` cho `nhap_danh_muc(jsonb, boolean)`; `khop_danh_muc` grant execute to authenticated.

`npm run db:push` → test 61 xanh 12/12.
  </action>
  <verify>
    <automated>npm run db:push && npx supabase test db --linked supabase/tests/61_import_danh_muc_test.sql</automated>
  </verify>
  <acceptance_criteria>
    - 0034 chứa `create or replace function public.nhap_danh_muc(p_dong jsonb, p_chi_kiem_tra boolean default true)`
    - 0034 chứa `set_config('app.nguon_sua', 'import', true)`, `'Chỉ quản lý được đặt giá bán'`, `'Mã hàng xuất hiện nhiều lần trong file'`
    - 0034 KHÔNG chứa `security definer` trong định nghĩa nhap_danh_muc và không chứa `returning *`
    - Test 61 có `plan(14)` và xanh
  </acceptance_criteria>
  <done>Import danh mục validate đầy đủ, all-or-nothing, có nhật ký</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test 62 đỏ, rồi 0035 gán hàng loạt + gợi ý công đoạn theo đuôi</name>
  <files>supabase/tests/62_ra_hang_loat_test.sql, supabase/migrations/0035_ra_hang_loat.sql</files>
  <read_first>supabase/migrations/0027_nhat_ky_sua.sql, supabase/migrations/0018_du_lieu_nen.sql, .memory/knowledge/du-lieu-kiotviet.md, supabase/tests/00_helper.sql.inc</read_first>
  <action>
**Test `supabase/tests/62_ra_hang_loat_test.sql`** (`plan(8)`). Fixture dưới postgres: `HL-ZQX-1-CB`,
`HL-ZQX-2-X`, `HL-ZQX-3-SĐM`, `HL-ZQX-4-N`, `HL-ZQX-5` (đều MUA_NGOAI qua `sp_test`), `HL-ZQX-6-CB` với công đoạn SON.
1. vanphong: `is((select count(*) from public.goi_y_cong_doan_theo_duoi() where ma_hang like 'HL-ZQX-%'), 4::bigint, 'gợi ý đúng 4 mã MUA_NGOAI có đuôi')`
2. `is((select ma_cong_doan_de_xuat from public.goi_y_cong_doan_theo_duoi() where ma_hang = 'HL-ZQX-3-SĐM'), 'SON', 'đuôi -SĐM gợi ý SƠN')`
3. vanphong: `ap_dung_goi_y_cong_doan(array[id HL-ZQX-1-CB, id HL-ZQX-5, id HL-ZQX-6-CB])` → trả 1 (chỉ HL-ZQX-1-CB đổi; HL-ZQX-5 không đuôi, HL-ZQX-6-CB không còn MUA_NGOAI).
4. `is((select cd.ma from san_pham sp join cong_doan cd on cd.id = sp.cong_doan_id where ma_hang='HL-ZQX-1-CB'), 'CARBON', 'áp dụng gợi ý đổi đúng công đoạn')`
5. `ok(exists(select 1 from nhat_ky_sua n join san_pham s on s.id=n.ban_ghi_id where s.ma_hang='HL-ZQX-1-CB' and n.nguon='goi_y_duoi'), 'nhật ký nguồn goi_y_duoi')`
6. vanphong: `gan_hang_loat(array[id HL-ZQX-2-X, id HL-ZQX-4-N], jsonb_build_object('nhom_hang_id', <id một nhóm bất kỳ>), 'hang_loat')` → trả 2; cả hai có nhóm mới, công đoạn giữ nguyên MUA_NGOAI.
7. vanphong: `throws_ok($$select public.gan_hang_loat(array[uuid_generate_v4()], '{"gia_von": 1}'::jsonb, 'hang_loat')$$, '23514', null, 'không gán hàng loạt trường ngoài danh sách cho phép')`
8. thukho1: `throws_ok($$select public.gan_hang_loat(array[uuid_generate_v4()], '{"dang_kinh_doanh": false}'::jsonb, 'hang_loat')$$, '42501', null, 'thủ kho không gán hàng loạt')`

**Migration `supabase/migrations/0035_ra_hang_loat.sql`:**
```sql
create or replace function public.cong_doan_theo_duoi(p_ma_hang text)
returns text language sql immutable set search_path = '' as $$
  select case
    when p_ma_hang ~ '-CB$' then 'CARBON'
    when p_ma_hang ~ '-X$' then 'XI_MA'
    when p_ma_hang ~ '-S[A-ZĐ]*$' then 'SON'
    when p_ma_hang ~ '-N$' then 'NANO'
  end;
$$;

create or replace function public.goi_y_cong_doan_theo_duoi()
returns table (id uuid, ma_hang text, ten_hang text, ten_nhom_hang text,
               cong_doan_de_xuat_id uuid, ma_cong_doan_de_xuat text, ten_cong_doan_de_xuat text)
language sql stable set search_path = '' as $$
  select sp.id, sp.ma_hang, sp.ten_hang, nh.ten, cd2.id, cd2.ma, cd2.ten
  from public.san_pham sp
  join public.cong_doan cd on cd.id = sp.cong_doan_id and cd.ma = 'MUA_NGOAI'
  left join public.nhom_hang nh on nh.id = sp.nhom_hang_id
  join public.cong_doan cd2 on cd2.ma = public.cong_doan_theo_duoi(sp.ma_hang)
  order by cd2.ma, sp.ma_hang;
$$;

create or replace function public.ap_dung_goi_y_cong_doan(p_ids uuid[])
returns integer language plpgsql volatile set search_path = '' as $$
declare v_so int;
begin
  if coalesce((select public.vai_tro_hien_tai())::text,'') not in ('quan_ly','van_phong') then
    raise exception 'Chỉ quản lý và văn phòng sửa được danh mục' using errcode = '42501';
  end if;
  if coalesce(cardinality(p_ids),0) > 1000 then
    raise exception 'Tối đa 1000 mã mỗi lần' using errcode = '23514';
  end if;
  perform set_config('app.nguon_sua', 'goi_y_duoi', true);
  update public.san_pham sp
  set cong_doan_id = cd2.id
  from public.cong_doan cd, public.cong_doan cd2
  where sp.id = any(p_ids) and cd.id = sp.cong_doan_id and cd.ma = 'MUA_NGOAI'
    and cd2.ma = public.cong_doan_theo_duoi(sp.ma_hang);
  get diagnostics v_so = row_count;
  return v_so;
end $$;

create or replace function public.gan_hang_loat(p_ids uuid[], p_thay_doi jsonb, p_nguon text default 'hang_loat')
returns integer language plpgsql volatile set search_path = '' as $$
declare v_so int; k text;
begin
  if coalesce((select public.vai_tro_hien_tai())::text,'') not in ('quan_ly','van_phong') then
    raise exception 'Chỉ quản lý và văn phòng sửa được danh mục' using errcode = '42501';
  end if;
  if p_nguon not in ('hang_loat','sua_o') then
    raise exception 'Nguồn sửa không hợp lệ: %', p_nguon using errcode = '23514';
  end if;
  if coalesce(cardinality(p_ids),0) > 1000 then
    raise exception 'Tối đa 1000 mã mỗi lần' using errcode = '23514';
  end if;
  for k in select jsonb_object_keys(p_thay_doi) loop
    if k not in ('nhom_hang_id','dvt_id','cong_doan_id','dang_kinh_doanh') then
      raise exception 'Không gán hàng loạt được trường %', k using errcode = '23514';
    end if;
  end loop;
  perform set_config('app.nguon_sua', p_nguon, true);
  update public.san_pham set
    nhom_hang_id    = case when p_thay_doi ? 'nhom_hang_id'    then nullif(p_thay_doi->>'nhom_hang_id','')::uuid else nhom_hang_id end,
    dvt_id          = case when p_thay_doi ? 'dvt_id'          then (p_thay_doi->>'dvt_id')::uuid else dvt_id end,
    cong_doan_id    = case when p_thay_doi ? 'cong_doan_id'    then (p_thay_doi->>'cong_doan_id')::uuid else cong_doan_id end,
    dang_kinh_doanh = case when p_thay_doi ? 'dang_kinh_doanh' then (p_thay_doi->>'dang_kinh_doanh')::boolean else dang_kinh_doanh end
  where id = any(p_ids);
  get diagnostics v_so = row_count;
  return v_so;
end $$;
```
Grant chuẩn cho 3 hàm public + `cong_doan_theo_duoi` (execute to authenticated).
Plan 04 và 06: đảm bảo `update` không cần `returning`. `?` jsonb với search_path '' — nếu Postgres báo
không tìm thấy toán tử, viết `operator(pg_catalog.?)`.

`npm run db:push` → test 62 xanh 8/8 → kiểm dữ liệu thật:
`select count(*) from public.goi_y_cong_doan_theo_duoi()` (chạy dưới postgres, hàm invoker nhưng owner đọc được) kỳ vọng 145. Ghi vào SUMMARY.
  </action>
  <verify>
    <automated>npm run db:push && npx supabase test db --linked supabase/tests/62_ra_hang_loat_test.sql</automated>
  </verify>
  <acceptance_criteria>
    - 0035 chứa `'-S[A-ZĐ]*$'`, `create or replace function public.gan_hang_loat`, `create or replace function public.ap_dung_goi_y_cong_doan`, `set_config('app.nguon_sua', 'goi_y_duoi', true)`
    - 0035 chứa `k not in ('nhom_hang_id','dvt_id','cong_doan_id','dang_kinh_doanh')`
    - Test 62 có `plan(8)` và xanh; số gợi ý trên dữ liệu thật = 145 ghi trong SUMMARY
  </acceptance_criteria>
  <done>Bốn công cụ rà có backend an toàn, đúng số liệu thật</done>
</task>

</tasks>

<verification>
- [ ] Test 61, 62 xanh
- [ ] 145 gợi ý trên dữ liệu thật
</verification>

<success_criteria>
- DMUC-06 all-or-nothing chứng minh bằng pgTAP
- D-18 rà hàng loạt không cho ghi trường ngoài whitelist
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-08-SUMMARY.md`
</output>
