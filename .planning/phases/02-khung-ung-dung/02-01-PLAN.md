---
phase: 02-khung-ung-dung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0026_nguoi_dung_nhieu_kho.sql
  - supabase/tests/30_rls_test.sql
  - supabase/tests/00_helper.sql.inc
  - supabase/tests/10_ton_kho_test.sql
  - supabase/tests/20_chung_tu_test.sql
  - supabase/tests/40_tim_kiem_test.sql
  - supabase/tests/50_doi_chieu_test.sql
  - supabase/tests/60_kho_mac_dinh_test.sql
  - scripts/_supabase-admin.ts
  - scripts/seed-users.ts
  - scripts/verify-hook.ts
autonomous: true
requirements: [CDAT-01]
user_setup: []
must_haves:
  truths:
    - "Một người dùng gắn được nhiều kho qua bảng nguoi_dung_kho; thủ kho 2 kho đọc được tồn cả hai kho"
    - "Thủ kho 1 kho vẫn không đọc được tồn kho còn lại (AUTH-04 không thoái lui)"
    - "Người dùng bị vô hiệu hóa hoặc có claim vai_tro lệch bảng nguoi_dung mất quyền ngay câu lệnh kế tiếp, dù access token còn hạn"
    - "Kho bị gỡ khỏi người dùng mất hiệu lực ngay câu lệnh kế tiếp"
    - "Hàm thu_hoi_phien_nguoi_dung xóa được auth.sessions, chỉ service_role gọi được"
    - "JWT thật (qua hook) mang kho_id dạng mảng"
  artifacts:
    - path: "supabase/migrations/0026_nguoi_dung_nhieu_kho.sql"
      provides: "Bảng nối, helper đối chiếu live, hook mảng kho, RPC hồ sơ người dùng, thu hồi phiên"
      contains: "create table public.nguoi_dung_kho"
    - path: "supabase/tests/30_rls_test.sql"
      provides: "Ca thủ kho nhiều kho, vô hiệu hóa, claim lệch"
  key_links:
    - from: "policy doc ton kho theo pham vi"
      to: "public.kho_hien_tai() returns uuid[]"
      via: "kho_id = any((select public.kho_hien_tai()))"
---

<objective>
Chuyển mô hình "một người một kho" của Phase 1 sang "một người nhiều kho" (D-06) và làm cho mọi
thay đổi quyền có hiệu lực ngay câu lệnh kế tiếp (D-05).

**Quyết định kỹ thuật chốt ở plan này (research §Câu hỏi 1 + kiểm chứng trên cloud):**
- Không thu hồi được access token đã phát. `auth.admin.signOut` nhận JWT, không nhận userId.
- Vì vậy hai helper RLS **vẫn đọc claim** (AUTH-03) nhưng **đối chiếu với bảng `nguoi_dung` trong
  cùng một lượt tra**: claim `vai_tro` phải khớp cột `vai_tro` và `dang_hoat_dong = true`; kho trả về
  là **giao** của claim với `nguoi_dung_kho`. Bọc `(select ...)` → InitPlan, chạy một lần mỗi câu lệnh
  (research xác nhận chi phí ngang đọc JWT). Hệ quả: **hạ quyền, gỡ kho, vô hiệu hóa → hiệu lực ngay**;
  **nâng quyền, thêm kho → sau khi token làm mới** (≤ `jwt_expiry` 3600s, hoặc ngay khi client gọi
  `refreshSession()` — plan 10 làm việc đó).
- `thu_hoi_phien_nguoi_dung(uuid)` xóa `auth.sessions` để chặn làm mới token. Đã kiểm trên cloud:
  `has_table_privilege('postgres','auth.sessions','DELETE') = true`.

Purpose: CDAT-01 cần gán nhiều kho; D-05 cần thu hồi quyền nhanh mà không viết lại hàng chục policy.
Output: migration 0026, helper test cập nhật ở mọi file pgTAP, tài khoản mẫu `thukho2` gắn K1+K2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@.planning/phases/02-khung-ung-dung/02-RESEARCH.md
@.memory/patterns/supabase-rls-bao-mat.md
@.memory/patterns/pgtap-va-test.md
@supabase/migrations/0003_nguoi_dung_kho.sql
@supabase/migrations/0014_auth_hook.sql
@supabase/migrations/0016_rls_chung_tu.sql
@supabase/migrations/0020_va_bao_mat.sql
@supabase/migrations/0023_policy_cho_auth_admin.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test đỏ rồi migration 0026 — bảng nối, helper đối chiếu live, hook, RPC hồ sơ, thu hồi phiên</name>
  <files>supabase/tests/30_rls_test.sql, supabase/migrations/0026_nguoi_dung_nhieu_kho.sql</files>
  <read_first>supabase/tests/30_rls_test.sql, supabase/migrations/0003_nguoi_dung_kho.sql, supabase/migrations/0014_auth_hook.sql, supabase/migrations/0016_rls_chung_tu.sql, supabase/migrations/0015_rls_danh_muc.sql, supabase/migrations/0020_va_bao_mat.sql, supabase/migrations/0023_policy_cho_auth_admin.sql, .memory/patterns/pgtap-va-test.md</read_first>
  <action>
**Bước A — sửa test trước (phải đỏ trên schema hiện tại).** Trong `supabase/tests/30_rls_test.sql`:

1. Thay helper `pg_temp.dang_nhap_nhu` bằng bản dựng claim từ bảng nối (giữ nguyên chữ ký):
```sql
create or replace function pg_temp.dang_nhap_nhu(p_email text)
returns void language plpgsql as $helper$
declare v_id uuid; v_nd public.nguoi_dung; v_kho jsonb;
begin
  select id into v_id from auth.users where email = p_email;
  if v_id is null then
    raise exception 'Không có tài khoản mẫu %. Chạy `npm run seed:users` trước.', p_email;
  end if;
  select * into v_nd from public.nguoi_dung where id = v_id;
  select coalesce(jsonb_agg(kho_id), '[]'::jsonb) into v_kho
  from public.nguoi_dung_kho where nguoi_dung_id = v_id;
  perform set_config('request.jwt.claims', jsonb_build_object(
    'sub', v_id::text, 'role', 'authenticated',
    'vai_tro', v_nd.vai_tro::text, 'kho_id', v_kho
  )::text, true);
  perform set_config('role', 'authenticated', true);
end $helper$;
```
2. Ba assertion CŨ đang so scalar với helper (dòng ~85, 89, 93 của file hiện tại) sẽ lỗi `operator does not exist: uuid = uuid[]` khi helper trả mảng. Viết lại đúng dạng mảng, giữ nguyên thông điệp:
   - `tk.kho_id = (select public.kho_hien_tai())` → `tk.kho_id = any((select public.kho_hien_tai()))`
   - `tk.kho_id <> (select public.kho_hien_tai())` → `not (tk.kho_id = any((select public.kho_hien_tai())))`
   - `mv.kho_id <> (select public.kho_hien_tai())` → `not (mv.kho_id = any((select public.kho_hien_tai())))`
   Sau đó `grep -n "kho_hien_tai())" supabase/tests/*.sql` — mọi chỗ còn lại phải nằm trong `any(...)`.
3. Tăng `plan(17)` lên `plan(25)` và thêm 8 assertion (đặt SAU các assertion cũ, trước `finish()`):
   - (a) Thủ kho 2 kho: dưới quyền postgres `insert into public.nguoi_dung_kho (nguoi_dung_id, kho_id) select id, (select k2 from t_id) from auth.users where email = 'thukho1@khominhvu.local' on conflict do nothing;` rồi `dang_nhap_nhu('thukho1@...')` → `is((select count(*) from public.ton_kho where san_pham_id = (select sp from t_id)), 2::bigint, 'thủ kho gắn K1+K2 thấy tồn cả hai kho')`.
   - (b) Claim mang K1+K2 nhưng bảng nối bị gỡ K2 (postgres `delete from public.nguoi_dung_kho where ... kho_id = k2`, KHÔNG đăng nhập lại — giữ claim cũ) → count = 1, thông điệp `'gỡ kho có hiệu lực ngay dù claim cũ còn K2'`.
   - (c) `pg_temp.dang_xuat()`; postgres `update public.nguoi_dung set dang_hoat_dong = false where id = <thukho1>`; set lại claim cũ bằng `set_config` thủ công (không gọi dang_nhap_nhu, để mô phỏng token còn hạn) → `is((select public.vai_tro_hien_tai()), null, 'người bị vô hiệu hóa mất vai trò ngay')` và `is((select count(*) from public.ton_kho), 0::bigint, 'người bị vô hiệu hóa không đọc được tồn')`. Khôi phục `dang_hoat_dong = true` dưới postgres sau đó.
   - (d) Claim `vai_tro = 'quan_ly'` cho tài khoản vanphong (claim giả lệch bảng) → `is((select public.vai_tro_hien_tai()), null, 'claim vai_tro lệch bảng nguoi_dung bị bỏ qua')`.
   - (e) `ok(not has_function_privilege('authenticated', 'public.thu_hoi_phien_nguoi_dung(uuid)', 'execute'), 'authenticated không gọi được thu hồi phiên')`.
   - (f) `ok(has_function_privilege('service_role', 'public.thu_hoi_phien_nguoi_dung(uuid)', 'execute'), 'service_role gọi được thu hồi phiên')`.
   - (g) Đăng nhập thukho1, gọi `public.luu_ho_so_nguoi_dung(...)` → `throws_ok(..., '42501', null, 'thủ kho không lưu được hồ sơ người dùng')`.
   - (h) Đăng nhập quanly, `select throws_ok($$select public.luu_ho_so_nguoi_dung((select id from auth.users where email='thukho1@khominhvu.local'), 'Thủ kho K1', 'thukho1', 'thu_kho', '{}'::uuid[], false)$$, '23514', null, 'thủ kho phải có ít nhất một kho')`.
   Chạy file → phải ĐỎ (bảng/hàm chưa có). Nhớ bài học false pass: bảng tạm `t_id` đã `grant select ... to authenticated`.

**Bước B — migration `supabase/migrations/0026_nguoi_dung_nhieu_kho.sql`**, đúng thứ tự sau, một file:

```sql
-- 1. Bảng nối + backfill TRONG CÙNG migration (không có khoảng hở RLS rỗng)
create table public.nguoi_dung_kho (
  nguoi_dung_id uuid not null references public.nguoi_dung(id) on delete cascade,
  kho_id uuid not null references public.kho(id),
  created_at timestamptz not null default now(),
  primary key (nguoi_dung_id, kho_id)
);
create index idx_nguoi_dung_kho_theo_kho on public.nguoi_dung_kho (kho_id);
insert into public.nguoi_dung_kho (nguoi_dung_id, kho_id)
select id, kho_id from public.nguoi_dung where kho_id is not null
on conflict do nothing;
comment on column public.nguoi_dung.kho_id is
  'KHÔNG CÒN ĐỌC từ 0026. Kho của người dùng nằm ở nguoi_dung_kho. Giữ cột tới khi người dùng đồng ý xóa (02-CONTEXT D-06).';

-- 2. Cột mới cho Phase 2
alter table public.nguoi_dung add column phai_doi_mat_khau boolean not null default false;
alter table public.nguoi_dung add column ten_dang_nhap text;
update public.nguoi_dung nd set ten_dang_nhap = split_part(u.email, '@', 1)
from auth.users u where u.id = nd.id and nd.ten_dang_nhap is null;
alter table public.nguoi_dung add constraint uq_nguoi_dung_ten_dang_nhap unique (ten_dang_nhap);
alter table public.nguoi_dung add constraint ck_ten_dang_nhap
  check (ten_dang_nhap is null or ten_dang_nhap ~ '^[a-z0-9._-]{3,32}$');

-- 3. RLS bảng nối
alter table public.nguoi_dung_kho enable row level security;
create policy "xem kho cua nguoi dung" on public.nguoi_dung_kho
  for select to authenticated using (
    (select public.vai_tro_hien_tai()) = 'quan_ly' or nguoi_dung_id = (select auth.uid())
  );
create policy "quan ly gan kho" on public.nguoi_dung_kho
  for all to authenticated
  using ((select public.vai_tro_hien_tai()) = 'quan_ly')
  with check ((select public.vai_tro_hien_tai()) = 'quan_ly');
-- Bài học 0023: hook chạy dưới supabase_auth_admin, cần CẢ grant LẪN policy.
grant select on public.nguoi_dung_kho to supabase_auth_admin;
create policy "auth admin doc kho nguoi dung" on public.nguoi_dung_kho
  for select to supabase_auth_admin using (true);
```
LƯU Ý: policy ở mục 3 gọi `vai_tro_hien_tai()` phiên bản CŨ (vẫn tồn tại) — hợp lệ; mục 4 thay thân hàm.

```sql
-- 4. Helper vai trò: claim PHẢI khớp bảng và người dùng PHẢI đang hoạt động.
create or replace function public.vai_tro_hien_tai()
returns public.vai_tro language sql stable security definer set search_path = ''
as $$
  select nd.vai_tro
  from public.nguoi_dung nd
  where nd.id = auth.uid()
    and nd.dang_hoat_dong
    and nd.vai_tro::text = (auth.jwt() ->> 'vai_tro');
$$;

-- 5. Helper kho đổi kiểu trả về uuid → uuid[]: phải DROP policy phụ thuộc, DROP hàm, tạo lại.
drop policy "doc ton kho theo pham vi" on public.ton_kho;
drop policy "doc so cai theo pham vi" on public.kho_movement;
drop policy "doc chung tu theo pham vi" on public.chung_tu;
drop function public.kho_hien_tai();

-- Giao của claim với bảng nối. `?` trên jsonb khớp cả phần tử mảng lẫn chuỗi scalar,
-- nên token cũ còn claim kho_id dạng chuỗi vẫn chạy đúng tới lúc hết hạn.
create function public.kho_hien_tai()
returns uuid[] language sql stable security definer set search_path = ''
as $$
  select coalesce(array_agg(ndk.kho_id), '{}'::uuid[])
  from public.nguoi_dung_kho ndk
  join public.nguoi_dung nd on nd.id = ndk.nguoi_dung_id
  where ndk.nguoi_dung_id = auth.uid()
    and nd.dang_hoat_dong
    and (auth.jwt() -> 'kho_id') ? ndk.kho_id::text;
$$;
revoke all    on function public.kho_hien_tai() from public, anon;
grant execute on function public.kho_hien_tai() to authenticated;
comment on function public.kho_hien_tai() is
  'Mảng kho người dùng được phân = claim kho_id GIAO bảng nguoi_dung_kho, rỗng nếu người dùng bị vô hiệu hóa. Trong policy LUÔN viết kho_id = any((select public.kho_hien_tai())).';

create policy "doc ton kho theo pham vi" on public.ton_kho
  for select to authenticated using (
    (select public.vai_tro_hien_tai()) in ('quan_ly','van_phong','chi_xem')
    or ((select public.vai_tro_hien_tai()) = 'thu_kho'
        and kho_id = any((select public.kho_hien_tai())))
  );
create policy "doc so cai theo pham vi" on public.kho_movement
  for select to authenticated using (
    (select public.vai_tro_hien_tai()) in ('quan_ly','van_phong','chi_xem')
    or ((select public.vai_tro_hien_tai()) = 'thu_kho'
        and kho_id = any((select public.kho_hien_tai())))
  );
create policy "doc chung tu theo pham vi" on public.chung_tu
  for select to authenticated using (
    (select public.vai_tro_hien_tai()) in ('quan_ly','van_phong','chi_xem')
    or ((select public.vai_tro_hien_tai()) = 'thu_kho'
        and (kho_id = any((select public.kho_hien_tai()))
             or kho_den_id = any((select public.kho_hien_tai()))))
  );
```

```sql
-- 6. Hook: kho_id thành mảng.
create or replace function public.custom_access_token_hook(event jsonb)
returns jsonb language plpgsql stable as $$
declare
  claims jsonb;
  v_vai_tro public.vai_tro;
  v_hoat_dong boolean;
  v_kho jsonb;
begin
  select vai_tro, dang_hoat_dong into v_vai_tro, v_hoat_dong
  from public.nguoi_dung where id = (event->>'user_id')::uuid;

  select coalesce(jsonb_agg(kho_id), '[]'::jsonb) into v_kho
  from public.nguoi_dung_kho where nguoi_dung_id = (event->>'user_id')::uuid;

  claims := event->'claims';
  if v_vai_tro is not null and coalesce(v_hoat_dong, false) then
    claims := jsonb_set(claims, '{vai_tro}', to_jsonb(v_vai_tro));
    claims := jsonb_set(claims, '{kho_id}', v_kho);
  end if;
  return jsonb_build_object('claims', claims);
end;
$$;
grant execute on function public.custom_access_token_hook(jsonb) to supabase_auth_admin;
revoke execute on function public.custom_access_token_hook(jsonb) from authenticated, anon, public;
```

```sql
-- 7. RPC hồ sơ người dùng — một transaction cho nguoi_dung + nguoi_dung_kho.
--    SECURITY INVOKER: RLS "quan ly sua nguoi dung" và "quan ly gan kho" tự chặn người khác,
--    và trigger nhật ký (plan 02) ghi đúng auth.uid() của quản lý.
create or replace function public.luu_ho_so_nguoi_dung(
  p_id uuid, p_ho_ten text, p_ten_dang_nhap text, p_vai_tro public.vai_tro,
  p_kho_ids uuid[], p_phai_doi_mat_khau boolean
) returns void language plpgsql set search_path = '' as $$
begin
  if coalesce((select public.vai_tro_hien_tai())::text, '') <> 'quan_ly' then
    raise exception 'Chỉ quản lý được sửa tài khoản' using errcode = '42501';
  end if;
  if p_vai_tro = 'thu_kho' and coalesce(cardinality(p_kho_ids), 0) = 0 then
    raise exception 'Thủ kho phải được gán ít nhất một kho' using errcode = '23514';
  end if;
  insert into public.nguoi_dung (id, ho_ten, ten_dang_nhap, vai_tro, phai_doi_mat_khau)
  values (p_id, p_ho_ten, p_ten_dang_nhap, p_vai_tro, p_phai_doi_mat_khau)
  on conflict (id) do update set
    ho_ten = excluded.ho_ten, ten_dang_nhap = excluded.ten_dang_nhap,
    vai_tro = excluded.vai_tro, phai_doi_mat_khau = excluded.phai_doi_mat_khau;
  -- Chỉ thủ kho gắn kho; vai trò khác thấy mọi kho nên xóa gán kho.
  delete from public.nguoi_dung_kho
  where nguoi_dung_id = p_id
    and (p_vai_tro <> 'thu_kho' or not (kho_id = any(p_kho_ids)));
  if p_vai_tro = 'thu_kho' then
    insert into public.nguoi_dung_kho (nguoi_dung_id, kho_id)
    select p_id, unnest(p_kho_ids) on conflict do nothing;
  end if;
end $$;
revoke all    on function public.luu_ho_so_nguoi_dung(uuid, text, text, public.vai_tro, uuid[], boolean) from public, anon;
grant execute on function public.luu_ho_so_nguoi_dung(uuid, text, text, public.vai_tro, uuid[], boolean) to authenticated;

-- 8. Người dùng tự gỡ cờ sau khi đổi mật khẩu (RLS không cho họ sửa nguoi_dung).
create or replace function public.da_doi_mat_khau()
returns void language sql security definer set search_path = '' as $$
  update public.nguoi_dung set phai_doi_mat_khau = false where id = (select auth.uid());
$$;
revoke all    on function public.da_doi_mat_khau() from public, anon;
grant execute on function public.da_doi_mat_khau() to authenticated;

-- 9. Thu hồi phiên: chặn LÀM MỚI token. Access token đang cầm vẫn hợp lệ tới hết hạn —
--    lớp chặn tức thời là helper ở mục 4–5.
create or replace function public.thu_hoi_phien_nguoi_dung(p_nguoi_dung_id uuid)
returns integer language plpgsql security definer set search_path = '' as $$
declare v_so integer;
begin
  delete from auth.sessions where user_id = p_nguoi_dung_id;
  get diagnostics v_so = row_count;
  return v_so;
end $$;
revoke all    on function public.thu_hoi_phien_nguoi_dung(uuid) from public, anon, authenticated;
grant execute on function public.thu_hoi_phien_nguoi_dung(uuid) to service_role;
```
Cuối file: chép lại khối DO "Tự kiểm: không sót bảng nào" của 0016 (bảng mới phải bật RLS).

**Bước C:** `npm run db:push` (KHÔNG dùng MCP apply_migration — sinh version timestamp lệch).
Nếu push báo lỗi ở mục 5 (`drop function` còn phụ thuộc) → tìm phụ thuộc bằng
`select * from pg_depend d join pg_proc p on p.oid = d.refobjid where p.proname = 'kho_hien_tai'`
và DROP/tạo lại policy đó theo đúng mẫu `= any((select ...))`, không dùng `drop ... cascade`.
Chạy `npx supabase test db --linked supabase/tests/30_rls_test.sql` → phải XANH 25/25.
  </action>
  <verify>
    <automated>npm run db:push && npx supabase test db --linked supabase/tests/30_rls_test.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0026_nguoi_dung_nhieu_kho.sql` chứa `create table public.nguoi_dung_kho`
    - File chứa `returns uuid[]` và `kho_id = any((select public.kho_hien_tai()))` (≥ 3 lần)
    - File chứa `nd.vai_tro::text = (auth.jwt() ->> 'vai_tro')` và `nd.dang_hoat_dong`
    - File chứa `delete from auth.sessions where user_id = p_nguoi_dung_id` và `grant execute on function public.thu_hoi_phien_nguoi_dung(uuid) to service_role`
    - File chứa `grant select on public.nguoi_dung_kho to supabase_auth_admin` và policy `to supabase_auth_admin`
    - File KHÔNG chứa `cascade` trong lệnh drop
    - `supabase/tests/30_rls_test.sql` chứa `plan(25)` và chuỗi `'gỡ kho có hiệu lực ngay dù claim cũ còn K2'`
    - `grep -nE "kho_id (=|<>) \(select public.kho_hien_tai\(\)\)" supabase/tests/` không trả kết quả
    - `npx supabase test db --linked supabase/tests/30_rls_test.sql` báo 25/25 ok
  </acceptance_criteria>
  <done>Nhiều kho chạy, thu hồi quyền có hiệu lực ngay ở tầng database, test 30 xanh</done>
</task>

<task type="auto">
  <name>Task 2: Cập nhật helper đăng nhập trong mọi file pgTAP còn lại</name>
  <files>supabase/tests/00_helper.sql.inc, supabase/tests/10_ton_kho_test.sql, supabase/tests/20_chung_tu_test.sql, supabase/tests/40_tim_kiem_test.sql, supabase/tests/50_doi_chieu_test.sql, supabase/tests/60_kho_mac_dinh_test.sql</files>
  <read_first>supabase/tests/00_helper.sql.inc, supabase/tests/10_ton_kho_test.sql, supabase/tests/20_chung_tu_test.sql, supabase/tests/40_tim_kiem_test.sql, supabase/tests/50_doi_chieu_test.sql, supabase/tests/60_kho_mac_dinh_test.sql</read_first>
  <action>
Mỗi file (và bản gốc `00_helper.sql.inc`) có một bản chép của `pg_temp.dang_nhap_nhu` dùng
`'kho_id', coalesce(v_nd.kho_id::text, '')`. Thay TOÀN BỘ thân hàm bằng đúng bản ở Task 1 Bước A
(khai báo thêm `v_kho jsonb`, đọc `nguoi_dung_kho`, `jsonb_build_object(... 'kho_id', v_kho)`).

Chỉ file nào thật sự chứa helper này mới sửa (dùng `grep -l "dang_nhap_nhu" supabase/tests/*`).
Trong `00_helper.sql.inc` sửa luôn dòng comment "phải chép lại vào cả năm file test" thành "cả sáu file test".

Không đổi số `plan(N)` và không đổi assertion nào trong các file này.

Chạy toàn bộ suite: `npm run db:test:linked`. Tổng phải là 97 (89 cũ + 8 mới của Task 1), 0 fail.
Nếu file nào đỏ vì assertion dùng `nguoi_dung.kho_id` trực tiếp → sửa assertion đọc từ
`nguoi_dung_kho`, ghi lý do vào SUMMARY.
  </action>
  <verify>
    <automated>npm run db:test:linked</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "coalesce(v_nd.kho_id::text" supabase/tests/` không trả file nào
    - `grep -c "from public.nguoi_dung_kho where nguoi_dung_id = v_id" supabase/tests/00_helper.sql.inc` ≥ 1
    - `npm run db:test:linked` exit 0, không dòng `not ok`
  </acceptance_criteria>
  <done>Toàn bộ pgTAP dựng claim kho dạng mảng, suite xanh</done>
</task>

<task type="auto">
  <name>Task 3: Tài khoản mẫu nhiều kho và verify:hook đọc mảng kho</name>
  <files>scripts/_supabase-admin.ts, scripts/seed-users.ts, scripts/verify-hook.ts</files>
  <read_first>scripts/_supabase-admin.ts, scripts/seed-users.ts, scripts/verify-hook.ts, src/types/database.types.ts</read_first>
  <action>
1. `scripts/_supabase-admin.ts` — đổi `maKho: string | null` thành `maKho: string[]`, và danh sách:
```ts
export const TAI_KHOAN_MAU = [
  { email: "quanly@khominhvu.local",   hoTen: "Quản lý demo",    vaiTro: "quan_ly"   as const, maKho: [] as string[] },
  { email: "vanphong@khominhvu.local", hoTen: "Văn phòng demo",  vaiTro: "van_phong" as const, maKho: [] as string[] },
  { email: "thukho1@khominhvu.local",  hoTen: "Thủ kho K1",      vaiTro: "thu_kho"   as const, maKho: ["K1"] },
  { email: "thukho2@khominhvu.local",  hoTen: "Thủ kho K1 + K2", vaiTro: "thu_kho"   as const, maKho: ["K1", "K2"] },
  { email: "chixem@khominhvu.local",   hoTen: "Chỉ xem demo",    vaiTro: "chi_xem"   as const, maKho: [] as string[] },
];
```
2. `scripts/seed-users.ts` — sau khi có `userId`: upsert `nguoi_dung` với
`{ id, ho_ten, vai_tro, ten_dang_nhap: email.split("@")[0], dang_hoat_dong: true }` (bỏ `kho_id`),
rồi `delete().eq("nguoi_dung_id", userId)` trên `nguoi_dung_kho` và `insert` lại theo `maKho`
(báo lỗi rõ nếu mã kho không tồn tại). Kiểm `error` sau mỗi lệnh và `throw`.
Vì `database.types.ts` chưa sinh lại (plan 09 mới sinh), dùng ép kiểu tối thiểu
`supabase.from("nguoi_dung_kho" as never)` kèm comment `// TODO(plan 02-09): bỏ ép kiểu sau khi sinh lại types`
— KHÔNG sửa tay `database.types.ts`.
3. `scripts/verify-hook.ts` — `type Claims = { vai_tro?: string; kho_id?: string[] | string; sub?: string }`.
Với tài khoản có `maKho.length > 0`: claim phải là mảng; tập mã kho suy từ claim (qua `maTheoKhoId`)
phải bằng đúng tập `maKho` (so sau khi sort). Tài khoản không có kho: claim là mảng rỗng hoặc vắng.
Sai thì `loi.push("kho_id sai (nhận ..., cần ...)")`.

Chạy `npm run seed:users` rồi `npm run verify:hook` → 5/5 tài khoản ✓.
  </action>
  <verify>
    <automated>npm run seed:users && npm run verify:hook</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/_supabase-admin.ts` chứa `thukho2@khominhvu.local` và `maKho: ["K1", "K2"]`
    - `scripts/seed-users.ts` chứa `nguoi_dung_kho` và KHÔNG chứa `kho_id: khoId`
    - `scripts/verify-hook.ts` chứa `kho_id?: string[] | string`
    - `npm run verify:hook` exit 0 và in 5 dòng ✓
  </acceptance_criteria>
  <done>Hook thật phát JWT có mảng kho, tài khoản demo thukho2 dùng được cho UAT D-05</done>
</task>

</tasks>

<verification>
- [ ] `npm run db:test:linked` xanh (97 assertion)
- [ ] `npm run verify:hook` xanh 5/5
- [ ] Không còn chỗ nào so `kho_id = (select public.kho_hien_tai())` (không có `any`): `grep -rn "= (select public.kho_hien_tai())" supabase/migrations/0026*` rỗng
</verification>

<success_criteria>
- Thủ kho nhiều kho hoạt động end-to-end (hook → claim → RLS)
- Hạ quyền/gỡ kho/vô hiệu hóa có hiệu lực ngay ở database — ghi độ trễ vào SUMMARY: "hạ quyền: câu lệnh kế tiếp; nâng quyền: khi token làm mới"
- Cột `nguoi_dung.kho_id` còn nguyên, có comment không còn đọc
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-01-SUMMARY.md`
</output>
