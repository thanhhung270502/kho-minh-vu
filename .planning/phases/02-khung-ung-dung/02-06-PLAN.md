---
phase: 02-khung-ung-dung
plan: 06
type: execute
wave: 2
depends_on: ["02-01", "02-04"]
files_modified:
  - supabase/migrations/0030_danh_sach_san_pham.sql
  - supabase/migrations/0031_the_kho_san_pham.sql
  - supabase/tests/41_danh_sach_san_pham_test.sql
  - supabase/tests/42_the_kho_test.sql
autonomous: true
requirements: [DMUC-01, DMUC-02, DMUC-03, DMUC-05]
user_setup: []
must_haves:
  truths:
    - "Một lệnh gọi danh_sach_san_pham trả đúng trang dữ liệu và tổng số dòng sau lọc"
    - "Tìm không dấu theo mã + tên dùng đúng biểu thức của tim_san_pham"
    - "Lọc được theo nhóm hàng, công đoạn, ĐVT, trạng thái tồn, trạng thái kinh doanh, Cần rà"
    - "gia_von chỉ có giá trị cho quản lý/văn phòng; vai trò khác nhận null"
    - "tong_ton của thủ kho chỉ cộng các kho được phân"
    - "Cần rà trên dữ liệu thật = 356 mã MUA_NGOAI chưa rõ + 8 mã ĐVT mâu thuẫn = 364; xác nhận đã rà thì rời danh sách"
    - "Thẻ kho gộp movement hệ thống với dòng KiotViet cũ gắn nhãn; dòng KiotViet chỉ quản lý/văn phòng thấy"
    - "Tham số sắp xếp ngoài danh sách cho phép bị bỏ qua, không nối chuỗi vào SQL"
  artifacts:
    - path: "supabase/migrations/0030_danh_sach_san_pham.sql"
      provides: "can_ra_dvt, da_xac_nhan_ra, danh_sach_san_pham, chi_tiet_san_pham, the_kho_san_pham, xac_nhan_da_ra"
      contains: "create or replace function public.danh_sach_san_pham"
  key_links:
    - from: "public.danh_sach_san_pham"
      to: "public.co_quyen_xem_gia_von() (plan 04), public.kho_hien_tai() uuid[] (plan 01)"
      via: "case when ... then sp.gia_von end; tk.kho_id = any(...)"
---

<objective>
Lớp đọc cho màn danh mục: danh sách phân trang server (D-11, D-12), chi tiết mã, thẻ kho gộp dữ liệu
KiotViet cũ (D-21), và cơ chế "Cần rà" (D-18, D-19).

**Quyết định kỹ thuật:**
- RPC `SECURITY DEFINER` vì phải tự quyết trả `gia_von` (plan 04 đã thu quyền cột). Hàm tự kiểm vai
  trò đầu tiên (`vai_tro_hien_tai() is null` → 42501) và tự áp phạm vi kho cho thủ kho — RLS không
  chạy dưới definer.
- `count(*) over()` cho tổng số dòng. Khi trang vượt quá dữ liệu, RPC trả 0 dòng (không có tổng) —
  giao diện (plan 16) tự lùi về trang 1. Test khẳng định hành vi này để không ai "sửa" nhầm.
- Sắp xếp theo whitelist bằng `CASE`, không nối chuỗi.
- "Cần rà" = `not da_xac_nhan_ra and (quy tắc MUA_NGOAI chưa rõ or can_ra_dvt)`. Quy tắc đã chạy thật
  trên cloud ra đúng 356: công đoạn `MUA_NGOAI`, nhóm không phải `Hàng Hãng - L5/6`/`Hàng Ngoài - L5/6`,
  mã không khớp `-(CB|X|S[A-ZĐ]*|N)$`. Thêm cờ `da_xac_nhan_ra` để người dùng xác nhận mã thật sự
  là mua ngoài — nếu không có cờ này, mã đúng là mua ngoài sẽ nằm trong "Cần rà" mãi mãi.
- Dòng KiotViet trong thẻ kho chỉ cho quản lý/văn phòng, khớp policy lưu trữ ở 0016 (thủ kho vốn
  không đọc được bảng lưu trữ; dòng lưu trữ cũng không có kho).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@.planning/phases/02-khung-ung-dung/02-RESEARCH.md
@.planning/phases/01-nen-du-lieu/01-UAT.md
@supabase/migrations/0022_tim_kiem_theo_chuoi_con.sql
@supabase/migrations/0029_an_gia_von.sql
@supabase/migrations/0026_nguoi_dung_nhieu_kho.sql
@supabase/migrations/0010_luu_tru_kiotviet.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test 41 đỏ, rồi cột Cần rà + danh_sach_san_pham + chi_tiet_san_pham + xac_nhan_da_ra</name>
  <files>supabase/tests/41_danh_sach_san_pham_test.sql, supabase/migrations/0030_danh_sach_san_pham.sql</files>
  <read_first>supabase/migrations/0022_tim_kiem_theo_chuoi_con.sql, supabase/migrations/0029_an_gia_von.sql, supabase/migrations/0026_nguoi_dung_nhieu_kho.sql, supabase/migrations/0005_san_pham.sql, supabase/migrations/0018_du_lieu_nen.sql, supabase/tests/00_helper.sql.inc, supabase/tests/40_tim_kiem_test.sql, .planning/phases/01-nen-du-lieu/01-UAT.md</read_first>
  <action>
**Bước A — `supabase/tests/41_danh_sach_san_pham_test.sql`** (`plan(14)`, helper, fixture tiền tố `DS-ZQX-`).
Dưới postgres dựng 4 mã: `DS-ZQX-001` (MUA_NGOAI, không nhóm, không đuôi → Cần rà), `DS-ZQX-002-CB`
(MUA_NGOAI, đuôi -CB → không Cần rà), `DS-ZQX-003` (CARBON), `DS-ZQX-004` (MUA_NGOAI, `dang_kinh_doanh=false`).
Tồn: `DS-ZQX-003` 7 ở K1, 5 ở K2 (insert kho_movement). Đặt `ton_toi_thieu = 100` cho `DS-ZQX-003`.
Mọi truy vấn lọc thêm `p_tu_khoa => 'DS-ZQX'` để không phụ thuộc dữ liệu thật.

1. quanly: `is((select count(*) from public.danh_sach_san_pham(p_tu_khoa => 'DS-ZQX', p_dang_kinh_doanh => null)), 4::bigint, 'không lọc kinh doanh ra đủ 4 mã')`
2. quanly: mặc định `p_dang_kinh_doanh` = true → 3 dòng.
3. quanly: `is((select distinct tong_so_dong from public.danh_sach_san_pham(p_tu_khoa => 'DS-ZQX', p_kich_thuoc => 2)), 3::bigint, 'tổng số dòng đúng khi trang nhỏ hơn kết quả')` và số dòng trả về = 2.
4. quanly: `p_trang => 99` → `is(count, 0::bigint, 'trang vượt quá trả 0 dòng — giao diện tự lùi trang')`
5. quanly: `p_tu_khoa => 'ds zqx 003'` (không dấu, cách) → có `DS-ZQX-003`.
6. quanly: `p_can_ra => true, p_tu_khoa => 'DS-ZQX'` → đúng một mã `DS-ZQX-001`.
7. quanly: `select public.xac_nhan_da_ra(array[<id DS-ZQX-001>])` rồi (6) lặp lại → 0 dòng. Thông điệp `'xác nhận đã rà thì rời Cần rà'`.
8. quanly: `p_cong_doan_id => (select id from cong_doan where ma='CARBON')` → 1 dòng.
9. quanly: `p_trang_thai_ton => 'duoi_dinh_muc'` → `DS-ZQX-003` (tồn 12 < 100).
10. quanly: `is((select gia_von is not null from danh_sach_san_pham(p_tu_khoa => 'DS-ZQX-003')), true, 'quản lý thấy giá vốn')`
11. thukho1 (chỉ K1): `is((select tong_ton from ... 'DS-ZQX-003'), 7::numeric, 'thủ kho chỉ cộng tồn kho được phân')` và `gia_von is null`.
12. `pg_temp.dang_xuat()` + claims rỗng: `throws_ok($$select * from public.danh_sach_san_pham()$$, '42501', null, 'không đăng nhập bị từ chối')`
13. quanly: `p_sap_xep => 'ma_hang; drop table x', p_huong => 'desc'` → `lives_ok`, trả 3 dòng. Thông điệp `'tham số sắp xếp lạ bị bỏ qua'`.
14. thukho1: `throws_ok($$select public.xac_nhan_da_ra(array[uuid_generate_v4()])$$, '42501', null, 'thủ kho không xác nhận rà được')`

**Bước B — `supabase/migrations/0030_danh_sach_san_pham.sql`:**
```sql
alter table public.san_pham add column can_ra_dvt boolean not null default false;
alter table public.san_pham add column da_xac_nhan_ra boolean not null default false;
comment on column public.san_pham.can_ra_dvt is '8 mã ô ĐVT gốc mâu thuẫn tên/đuôi (01-UAT). Không tự sửa dữ liệu — người rành hàng quyết (02-CONTEXT D-19).';
comment on column public.san_pham.da_xac_nhan_ra is 'Người dùng đã xác nhận công đoạn/ĐVT hiện tại là đúng → rời bộ lọc Cần rà.';
update public.san_pham set can_ra_dvt = true where ma_hang in (
  'HVR23-75-35-CB','HVR23-75-36-CB','HVR23-75-37-CB',
  'YE19-46-9635-CB','YE19-46-9736-CB','YE19-46-9837-CB',
  'YE15-29-CB','HL10-16ATBAĐ-S'
);
-- Plan 04 thu quyền SELECT mức bảng: cột mới PHẢI grant tường minh.
grant select (can_ra_dvt, da_xac_nhan_ra) on public.san_pham to authenticated;
grant update (da_xac_nhan_ra) on public.san_pham to authenticated;

create or replace function public.la_can_ra(p_cong_doan_ma text, p_ten_nhom text, p_ma_hang text,
                                            p_can_ra_dvt boolean, p_da_xac_nhan boolean)
returns boolean language sql immutable set search_path = '' as $$
  select not p_da_xac_nhan and (
    p_can_ra_dvt
    or (p_cong_doan_ma = 'MUA_NGOAI'
        and coalesce(p_ten_nhom, '') not in ('Hàng Hãng - L5/6', 'Hàng Ngoài - L5/6')
        and p_ma_hang !~ '-(CB|X|S[A-ZĐ]*|N)$')
  );
$$;
```
`danh_sach_san_pham` — tham số (tên dùng named notation từ supabase-js):
`p_tu_khoa text default null, p_nhom_hang_id uuid default null, p_cong_doan_id uuid default null,
p_dvt_id uuid default null, p_trang_thai_ton text default null, p_dang_kinh_doanh boolean default true,
p_can_ra boolean default null, p_sap_xep text default null, p_huong text default 'asc',
p_trang int default 1, p_kich_thuoc int default 50`.
Trả `table (id uuid, ma_hang text, ten_hang text, nhom_hang_id uuid, ten_nhom_hang text, dvt_id uuid,
ten_dvt text, cong_doan_id uuid, ma_cong_doan text, ten_cong_doan text, mau_cong_doan text,
quy_doi numeric, gia_ban numeric, gia_von numeric, ton_toi_thieu numeric, ton_toi_da numeric,
kho_mac_dinh_id uuid, dang_kinh_doanh boolean, tong_ton numeric, can_ra boolean, can_ra_dvt boolean,
updated_at timestamptz, tong_so_dong bigint)`.
Thân (`language plpgsql stable security definer set search_path = ''`):
```sql
declare
  v_vai_tro public.vai_tro := (select public.vai_tro_hien_tai());
  v_kho uuid[] := (select public.kho_hien_tai());
  v_xem_gv boolean := (select public.co_quyen_xem_gia_von());
  v_tk text := nullif(trim(coalesce(p_tu_khoa, '')), '');
  v_kt int := least(greatest(coalesce(p_kich_thuoc, 50), 1), 5000);
  v_tr int := greatest(coalesce(p_trang, 1), 1);
begin
  if v_vai_tro is null then
    raise exception 'Phiên đăng nhập không hợp lệ hoặc tài khoản đã bị vô hiệu hóa' using errcode = '42501';
  end if;
  return query
  with ton as (
    select tk.san_pham_id, sum(tk.so_luong) as so_luong
    from public.ton_kho tk
    where v_vai_tro <> 'thu_kho' or tk.kho_id = any(v_kho)
    group by tk.san_pham_id
  ), loc as (
    select sp.*, nh.ten as ten_nhom, dv.ten as ten_dv, cd.ma as ma_cd, cd.ten as ten_cd, cd.mau_hien_thi,
           coalesce(ton.so_luong, 0) as tong,
           public.la_can_ra(cd.ma, nh.ten, sp.ma_hang, sp.can_ra_dvt, sp.da_xac_nhan_ra) as cr
    from public.san_pham sp
    left join public.nhom_hang nh on nh.id = sp.nhom_hang_id
    left join public.don_vi_tinh dv on dv.id = sp.dvt_id
    left join public.cong_doan cd on cd.id = sp.cong_doan_id
    left join ton on ton.san_pham_id = sp.id
    where (v_tk is null
           or public.f_unaccent(coalesce(sp.ma_hang,'') || ' ' || coalesce(sp.ten_hang,''))
                ilike '%' || public.f_unaccent(v_tk) || '%'
           or public.f_unaccent(v_tk) operator(extensions.<%)
                public.f_unaccent(coalesce(sp.ma_hang,'') || ' ' || coalesce(sp.ten_hang,'')))
      and (p_nhom_hang_id is null or sp.nhom_hang_id = p_nhom_hang_id)
      and (p_cong_doan_id is null or sp.cong_doan_id = p_cong_doan_id)
      and (p_dvt_id is null or sp.dvt_id = p_dvt_id)
      and (p_dang_kinh_doanh is null or sp.dang_kinh_doanh = p_dang_kinh_doanh)
  )
  select l.id, l.ma_hang, l.ten_hang, l.nhom_hang_id, l.ten_nhom, l.dvt_id, l.ten_dv, l.cong_doan_id,
         l.ma_cd, l.ten_cd, l.mau_hien_thi, l.quy_doi, l.gia_ban,
         case when v_xem_gv then l.gia_von end,
         l.ton_toi_thieu, l.ton_toi_da, l.kho_mac_dinh_id, l.dang_kinh_doanh, l.tong, l.cr, l.can_ra_dvt,
         l.updated_at, count(*) over ()
  from loc l
  where (p_can_ra is null or l.cr = p_can_ra)
    and (p_trang_thai_ton is null
         or (p_trang_thai_ton = 'con_hang' and l.tong > 0)
         or (p_trang_thai_ton = 'het_hang' and l.tong = 0)
         or (p_trang_thai_ton = 'am' and l.tong < 0)
         or (p_trang_thai_ton = 'duoi_dinh_muc' and l.tong < l.ton_toi_thieu))
  order by
    case when p_sap_xep is null and v_tk is not null then l.lan_phat_sinh_cuoi end desc nulls last,
    case when p_sap_xep is null and v_tk is not null then
      extensions.word_similarity(public.f_unaccent(v_tk),
        public.f_unaccent(coalesce(l.ma_hang,'') || ' ' || coalesce(l.ten_hang,''))) end desc,
    case when p_sap_xep = 'ten_hang' and p_huong = 'asc'  then l.ten_hang end asc,
    case when p_sap_xep = 'ten_hang' and p_huong = 'desc' then l.ten_hang end desc,
    case when p_sap_xep = 'tong_ton' and p_huong = 'asc'  then l.tong end asc,
    case when p_sap_xep = 'tong_ton' and p_huong = 'desc' then l.tong end desc,
    case when p_sap_xep = 'updated_at' and p_huong = 'asc'  then l.updated_at end asc,
    case when p_sap_xep = 'updated_at' and p_huong = 'desc' then l.updated_at end desc,
    case when p_sap_xep = 'ma_hang' and p_huong = 'desc' then l.ma_hang end desc,
    l.ma_hang asc
  limit v_kt offset (v_tr - 1) * v_kt;
end;
```
`select sp.*` Ở ĐÂY an toàn vì hàm là SECURITY DEFINER (owner đọc mọi cột); cột `gia_von` chỉ lọt ra
qua `case when v_xem_gv`. Ghi comment đó trong hàm.

`chi_tiet_san_pham(p_id uuid)` — cùng kiểm vai trò; trả một dòng: mọi cột của danh_sach (trừ
`tong_so_dong`) + `barcode, hinh_anh_url, vi_tri_ke, ghi_chu, ten_kho_mac_dinh text, created_at`.
Tái dùng: gọi lại logic join như trên với `where sp.id = p_id` (không lọc kinh doanh).

`xac_nhan_da_ra(p_ids uuid[]) returns integer` — `language plpgsql set search_path = ''` (INVOKER: RLS
"sua san pham" + grant update cột tự chặn thủ kho/chỉ xem). Kiểm `cardinality(p_ids) <= 1000`.
```sql
perform set_config('app.nguon_sua', 'hang_loat', true);
update public.san_pham set da_xac_nhan_ra = true, can_ra_dvt = false where id = any(p_ids);
```
`can_ra_dvt` cũng cần `grant update (can_ra_dvt)` — thêm vào lệnh grant update ở trên. Nếu RLS không cho
update (thủ kho) thì `update` cập nhật 0 dòng không ném lỗi → kiểm vai trò tường minh đầu hàm:
`if coalesce((select public.vai_tro_hien_tai())::text,'') not in ('quan_ly','van_phong') then raise ... '42501'`.
Trả `row_count`.

Grant: `revoke all ... from public, anon; grant execute ... to authenticated` cho 3 hàm public (không grant `la_can_ra` cho anon).

**Bước C:** `npm run db:push` → test 41 xanh 14/14. Rồi chạy kiểm trên dữ liệu thật (ghi số vào SUMMARY):
```bash
psql "$DATABASE_URL" -X -A -c "select count(*) from public.san_pham sp left join public.nhom_hang nh on nh.id=sp.nhom_hang_id left join public.cong_doan cd on cd.id=sp.cong_doan_id where public.la_can_ra(cd.ma, nh.ten, sp.ma_hang, sp.can_ra_dvt, sp.da_xac_nhan_ra)"
```
Kỳ vọng 364. Lệch → điều tra, không chỉnh test cho khớp.
  </action>
  <verify>
    <automated>npm run db:push && npx supabase test db --linked supabase/tests/41_danh_sach_san_pham_test.sql</automated>
  </verify>
  <acceptance_criteria>
    - Migration chứa `add column can_ra_dvt boolean`, `add column da_xac_nhan_ra boolean` và đủ 8 mã trong `update ... set can_ra_dvt = true`
    - Migration chứa `grant select (can_ra_dvt, da_xac_nhan_ra) on public.san_pham to authenticated`
    - Migration chứa `'-(CB|X|S[A-ZĐ]*|N)$'` và `count(*) over ()`
    - Migration chứa `case when v_xem_gv then l.gia_von end` và KHÔNG chứa `format(` hay `execute ` trong danh_sach_san_pham
    - Test 41 có `plan(14)` và xanh; truy vấn dữ liệu thật trả 364
  </acceptance_criteria>
  <done>Màn danh mục có RPC đọc đầy đủ, an toàn, đúng số Cần rà trên dữ liệu thật</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test 42 đỏ, rồi the_kho_san_pham gộp KiotViet</name>
  <files>supabase/tests/42_the_kho_test.sql, supabase/migrations/0031_the_kho_san_pham.sql</files>
  <read_first>supabase/migrations/0010_luu_tru_kiotviet.sql, supabase/migrations/0007_chung_tu.sql, supabase/migrations/0008_so_cai_ton_kho.sql, supabase/migrations/0016_rls_chung_tu.sql, supabase/tests/00_helper.sql.inc</read_first>
  <action>
Tạo migration RIÊNG `supabase/migrations/0031_the_kho_san_pham.sql` (0030 đã push ở Task 1 — không sửa migration đã áp).

**Test `supabase/tests/42_the_kho_test.sql`** (`plan(7)`, fixture `TK-ZQX-001`):
Dưới postgres: tạo mã; 1 movement K1 +5 (không chứng từ), 1 movement K2 -2; insert
`luu_tru_nhap_kiotviet (ma_phieu, ngay, nha_cung_cap, ma_hang, so_luong) values ('PN-ZQX', '2026-09-10T08:00:00+07:00', 'NCC000001 X', 'TK-ZQX-001', 3)`
và `luu_tru_hoa_don_kiotviet (ma_hoa_don, ngay, ma_hang, so_luong, ghi_chu) values ('HD-ZQX', '2026-09-11T09:00:00+07:00', 'TK-ZQX-001', 4, 'NGỌC')`.
1. quanly: tổng dòng = 4.
2. quanly: `nguon` gồm đúng tập `{HE_THONG, KIOTVIET_NHAP, KIOTVIET_BAN}`.
3. quanly: dòng `KIOTVIET_BAN` có `so_luong_xuat = 4` và `so_luong_nhap is null`.
4. thukho1 (K1): tổng dòng = 1 (chỉ movement K1, không dòng KiotViet).
5. thukho1: `gia_von_tai_thoi_diem is null` trên mọi dòng.
6. quanly: `p_kho_id => k2` → 1 dòng HE_THONG + 0 KiotViet (lọc kho loại dòng lưu trữ vì không có kho).
7. quanly: sắp xếp `ngay desc` — dòng đầu tiên là dòng mới nhất.

**Hàm:**
```sql
create or replace function public.the_kho_san_pham(
  p_san_pham_id uuid, p_kho_id uuid default null, p_trang int default 1, p_kich_thuoc int default 50
) returns table (
  nguon text, ngay timestamptz, kho_id uuid, ten_kho text, chung_tu_id uuid, so_ct text, loai_ct text,
  doi_tac text, so_luong_nhap numeric, so_luong_xuat numeric, gia_von_tai_thoi_diem numeric,
  la_but_toan_dao boolean, ghi_chu text, tong_so_dong bigint
) language plpgsql stable security definer set search_path = '' as $$
declare
  v_vai_tro public.vai_tro := (select public.vai_tro_hien_tai());
  v_kho uuid[] := (select public.kho_hien_tai());
  v_xem_gv boolean := (select public.co_quyen_xem_gia_von());
  v_xem_kv boolean := v_vai_tro in ('quan_ly','van_phong');
  v_ma text;
  v_kt int := least(greatest(coalesce(p_kich_thuoc, 50), 1), 500);
begin
  if v_vai_tro is null then raise exception 'Phiên đăng nhập không hợp lệ' using errcode = '42501'; end if;
  select ma_hang into v_ma from public.san_pham where id = p_san_pham_id;
  return query
  with tat_ca as (
    select 'HE_THONG'::text, m.ngay, m.kho_id, k.ten, m.chung_tu_id, ct.so_ct, ct.loai_ct::text,
           dt.ten, case when m.so_luong > 0 then m.so_luong end, case when m.so_luong < 0 then -m.so_luong end,
           case when v_xem_gv then m.gia_von_tai_thoi_diem end, m.la_but_toan_dao, ct.ghi_chu
    from public.kho_movement m
    join public.kho k on k.id = m.kho_id
    left join public.chung_tu ct on ct.id = m.chung_tu_id
    left join public.doi_tac dt on dt.id = ct.doi_tac_id
    where m.san_pham_id = p_san_pham_id
      and (v_vai_tro <> 'thu_kho' or m.kho_id = any(v_kho))
      and (p_kho_id is null or m.kho_id = p_kho_id)
    union all
    select 'KIOTVIET_NHAP', l.ngay::timestamptz, null::uuid, null::text, null::uuid, l.ma_phieu, 'NHAP',
           l.nha_cung_cap, l.so_luong, null::numeric, null::numeric, false, l.ghi_chu
    from public.luu_tru_nhap_kiotviet l
    where v_xem_kv and p_kho_id is null and l.ma_hang = v_ma
    union all
    select 'KIOTVIET_BAN', l.ngay::timestamptz, null, null, null, l.ma_hoa_don, 'XUAT',
           coalesce(nullif(l.ghi_chu, ''), l.khach_hang), null, l.so_luong, null, false, l.ghi_chu
    from public.luu_tru_hoa_don_kiotviet l
    where v_xem_kv and p_kho_id is null and l.ma_hang = v_ma
  )
  select t.*, count(*) over () from tat_ca t
  order by 2 desc
  limit v_kt offset (greatest(coalesce(p_trang,1),1) - 1) * v_kt;
end $$;
revoke all    on function public.the_kho_san_pham(uuid, uuid, int, int) from public, anon;
grant execute on function public.the_kho_san_pham(uuid, uuid, int, int) to authenticated;
```
Đặt tên cột trong CTE bằng alias tường minh nếu Postgres báo lỗi kiểu/tên cột ở `union all`
(`as nguon`, `as ngay`, ...). Ép `l.ngay::timestamptz` an toàn: dữ liệu thật dạng
`2026-09-12T15:43:10+07:00` (đã kiểm). Nếu gặp giá trị không parse được → bọc bằng hàm
`public.doc_ngay_an_toan(text) returns timestamptz` bắt exception trả null, KHÔNG để RPC nổ cả thẻ kho.

**Push + test:** `npm run db:push` → test 42 xanh 7/7 → chạy lại test 41.
  </action>
  <verify>
    <automated>npm run db:push && npx supabase test db --linked supabase/tests/42_the_kho_test.sql && npx supabase test db --linked supabase/tests/41_danh_sach_san_pham_test.sql</automated>
  </verify>
  <acceptance_criteria>
    - Có hàm `public.the_kho_san_pham(uuid, uuid, int, int)` trên cloud (`select to_regprocedure('public.the_kho_san_pham(uuid,uuid,int,int)') is not null`)
    - Thân hàm chứa `'KIOTVIET_NHAP'`, `'KIOTVIET_BAN'`, `v_xem_kv` và `m.kho_id = any(v_kho)`
    - Test 42 có `plan(7)` và xanh; test 41 vẫn xanh
  </acceptance_criteria>
  <done>Thẻ kho ngày đầu đã có lịch sử thật từ KiotViet cho người được xem</done>
</task>

</tasks>

<verification>
- [ ] Test 41, 42 xanh
- [ ] Số Cần rà trên dữ liệu thật = 364 (ghi trong SUMMARY)
</verification>

<success_criteria>
- DMUC-01..03, DMUC-05 có RPC đọc hoàn chỉnh cho plan 11/16/17
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-06-SUMMARY.md`
</output>
