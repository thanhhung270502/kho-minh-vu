---
phase: 02-khung-ung-dung
plan: 09
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05", "02-06", "02-07", "02-08"]
files_modified:
  - src/types/database.types.ts
  - supabase/README.md
  - scripts/seed-users.ts
  - .memory/patterns/supabase-rls-bao-mat.md
autonomous: true
requirements: [CDAT-01, DMUC-01, DMUC-06, DLIEU-04]
user_setup: []
must_haves:
  truths:
    - "Migration 0026–0035 đã áp trên cloud theo đúng thứ tự, lịch sử migration không có version timestamp lạ"
    - "Toàn bộ pgTAP (Phase 1 + Phase 2) xanh trên cloud, verify:hook xanh"
    - "src/types/database.types.ts có đủ bảng/hàm mới của Phase 2"
    - "npm run check xanh sau khi sinh lại types"
    - "Security advisor không có cảnh báo MỚI chưa được giải thích"
  artifacts:
    - path: "src/types/database.types.ts"
      provides: "Kiểu cho mọi RPC Phase 2"
      contains: "danh_sach_san_pham"
  key_links:
    - from: "Wave 5+ (giao diện)"
      to: "src/types/database.types.ts"
      via: "Database['public']['Functions'][...]"
---

<objective>
Cổng chặn giữa backend và giao diện: xác nhận mọi migration Phase 2 đã lên cloud, toàn bộ test xanh, sinh
lại kiểu TypeScript, ghi bài học mới vào bộ nhớ dự án. Không viết tính năng mới.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@supabase/README.md
@.memory/patterns/supabase-rls-bao-mat.md
@.planning/phases/02-khung-ung-dung/02-01-SUMMARY.md
@.planning/phases/02-khung-ung-dung/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Đối chiếu lịch sử migration, chạy full suite, kiểm advisor</name>
  <files>supabase/README.md</files>
  <read_first>supabase/README.md, supabase/migrations/</read_first>
  <action>
1. `npx supabase migration list --linked` — cột Remote phải có đủ `0026` → `0035`, không có version 14 chữ số.
   Nếu có (do ai đó dùng MCP apply_migration) → sửa `supabase_migrations.schema_migrations` theo mục
   "Sửa lịch sử migration" trong `supabase/README.md`, không áp lại migration.
2. `npm run db:push` (phải báo up to date).
3. `npm run seed:users` rồi `npm run db:test:linked` — ghi tổng số assertion vào SUMMARY (kỳ vọng ≈ 89 + 8 + 12 + 10 + 11 + 14 + 7 + 16 + 14 + 8 = 189).
4. `npm run verify:hook` — 5/5.
5. Kiểm advisor: dùng MCP `get_advisors` (type security và performance) cho project `phonzyruoalimgaovljm`.
   Cảnh báo `security_definer_view`/`function_search_path_mutable`/`rls_disabled` MỚI → sửa bằng migration
   `0036_...` trong plan này. Cảnh báo "authenticated/anon có thể gọi hàm SECURITY DEFINER" cho các RPC
   definer đã có kiểm vai trò đầu hàm (danh_sach_san_pham, chi_tiet_san_pham, the_kho_san_pham,
   lich_su_sua, gia_von_san_pham, co_quyen_xem_gia_von, danh_sach_ghi_chu_kiotviet, quyet_ghi_chu,
   bo_quyet_ghi_chu, lich_su_giao_dich_doi_tac, da_doi_mat_khau) → CHẤP NHẬN và thêm vào danh sách
   "cảnh báo advisor đã chấp nhận" trong `supabase/README.md`, mỗi hàm một dòng lý do.
6. Kiểm dữ liệu thật (psql, chỉ đọc), ghi vào SUMMARY:
```sql
select count(*) from public.san_pham sp left join public.nhom_hang nh on nh.id=sp.nhom_hang_id left join public.cong_doan cd on cd.id=sp.cong_doan_id where public.la_can_ra(cd.ma, nh.ten, sp.ma_hang, sp.can_ra_dvt, sp.da_xac_nhan_ra); -- 364
select count(*) from public.goi_y_cong_doan_theo_duoi(); -- 145
select count(distinct public.chuan_hoa_ghi_chu(ghi_chu)) from public.luu_tru_hoa_don_kiotviet; -- ~150
select count(*) from public.nguoi_dung_kho; -- ≥ 3 (thukho1 K1, thukho2 K1+K2)
select has_column_privilege('authenticated','public.san_pham','gia_von','SELECT'); -- f
```
7. Cập nhật `supabase/README.md`: mục "Migration Phase 2" liệt kê 0026–0035 một dòng mô tả mỗi file;
   ghi "Thứ tự push bắt buộc: 0026 trước 0027 (trigger nhật ký trên nguoi_dung_kho)".
  </action>
  <verify>
    <automated>npm run db:test:linked && npm run verify:hook</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase migration list --linked` hiện `0035` ở cả Local và Remote
    - `npm run db:test:linked` exit 0; `npm run verify:hook` exit 0
    - `supabase/README.md` chứa `0035` và chuỗi `Thứ tự push bắt buộc`
    - SUMMARY ghi 5 con số kiểm dữ liệu thật
  </acceptance_criteria>
  <done>Backend Phase 2 xác nhận trên cloud</done>
</task>

<task type="auto">
  <name>Task 2: Sinh lại types, bỏ ép kiểu tạm, ghi bài học</name>
  <files>src/types/database.types.ts, scripts/seed-users.ts, .memory/patterns/supabase-rls-bao-mat.md</files>
  <read_first>src/types/database.types.ts, scripts/seed-users.ts, .memory/patterns/supabase-rls-bao-mat.md, package.json</read_first>
  <action>
1. `npm run db:types` (cloud, cần `SUPABASE_PROJECT_ID` từ `.env.local`: `set -a; source .env.local; set +a`).
   Kiểm file chứa: `nguoi_dung_kho`, `nhat_ky_sua`, `cau_hinh_so_ct`, `anh_xa_ghi_chu_kiotviet`, và Functions
   `danh_sach_san_pham`, `chi_tiet_san_pham`, `the_kho_san_pham`, `xac_nhan_da_ra`, `nhap_danh_muc`,
   `gan_hang_loat`, `goi_y_cong_doan_theo_duoi`, `ap_dung_goi_y_cong_doan`, `danh_sach_doi_tac`,
   `sinh_ma_doi_tac`, `danh_sach_ghi_chu_kiotviet`, `quyet_ghi_chu`, `bo_quyet_ghi_chu`,
   `lich_su_giao_dich_doi_tac`, `lich_su_sua`, `gia_von_san_pham`, `luu_ho_so_nguoi_dung`,
   `da_doi_mat_khau`, `danh_sach_cau_hinh_so_ct`. Thiếu hàm nào → migration thiếu `grant execute ... to authenticated`;
   sửa bằng migration mới, không sửa tay file types. `thu_hoi_phien_nguoi_dung` KHÔNG có trong kiểu
   authenticated là đúng (chỉ service_role) — nếu generator vẫn sinh thì không sao.
2. `scripts/seed-users.ts`: xóa ép kiểu `as never` và comment `TODO(plan 02-09)` thêm ở plan 01.
3. `npm run check` — sửa lỗi kiểu phát sinh ở code hiện có (plan 05) do types đổi; KHÔNG dùng `any`.
4. `.memory/patterns/supabase-rls-bao-mat.md` thêm hai mục (ngắn, có ví dụ SQL):
   - "REVOKE một cột không gỡ được GRANT mức bảng" — phải revoke bảng rồi grant từng cột; kèm khối DO tự kiểm `has_column_privilege`; hệ quả `select('*')` lỗi cho mọi vai trò.
   - "Thu hồi quyền tức thời mà vẫn đọc claim" — helper đối chiếu claim với bảng trong `(select ...)`: hạ quyền hiệu lực ngay, nâng quyền chờ refresh token; không revoke được access token đã phát.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/database.types.ts` chứa `danh_sach_san_pham`, `nhap_danh_muc`, `quyet_ghi_chu`, `nguoi_dung_kho`, `phai_doi_mat_khau`
    - `grep -c "as never" scripts/seed-users.ts` = 0
    - `.memory/patterns/supabase-rls-bao-mat.md` chứa `has_column_privilege`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>Giao diện Wave 5+ có kiểu đúng cho mọi RPC Phase 2</done>
</task>

</tasks>

<verification>
- [ ] Full suite + verify:hook + check xanh
</verification>

<success_criteria>
- Không plan giao diện nào phải đoán shape dữ liệu
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-09-SUMMARY.md`
</output>
