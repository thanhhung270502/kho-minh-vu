---
phase: 02-khung-ung-dung
plan: 15
type: execute
wave: 6
depends_on: ["02-09", "02-13"]
files_modified:
  - supabase/migrations/0040_danh_muc_phu.sql
  - supabase/tests/63_danh_muc_phu_test.sql
  - src/app/(app)/cai-dat/layout.tsx
  - src/app/(app)/cai-dat/page.tsx
  - src/features/cai-dat/components/tab-cai-dat.tsx
  - src/features/cai-dat/api/danh-muc-phu.api.ts
  - src/features/cai-dat/components/danh-muc-phu.tsx
  - src/features/cai-dat/components/ngan-keo-danh-muc-phu.tsx
  - src/app/(app)/cai-dat/kho/page.tsx
  - src/app/(app)/cai-dat/nhom-hang/page.tsx
  - src/app/(app)/cai-dat/don-vi-tinh/page.tsx
  - src/app/(app)/cai-dat/cong-doan/page.tsx
  - src/app/(app)/cai-dat/so-chung-tu/page.tsx
  - src/features/cai-dat/components/cau-hinh-so-ct.tsx
autonomous: true
requirements: [CDAT-02, CDAT-03, CDAT-04]
user_setup: []
must_haves:
  truths:
    - "Khu Cài đặt có tab theo quyền: Người dùng, Kho, Số chứng từ (quản lý); Nhóm hàng, Đơn vị tính, Công đoạn (quản lý + văn phòng)"
    - "Quản lý tạo/sửa/ngừng hoạt động kho, không xóa"
    - "Quản lý/văn phòng tạo/sửa/xóa nhóm hàng, ĐVT, công đoạn; xóa mục đang dùng hiện 'Đang có N mã hàng dùng'"
    - "Mã của 6 công đoạn hệ thống (EP, SON, CARBON, XI_MA, NANO, MUA_NGOAI) và ĐVT CAI không đổi/xóa được"
    - "Nhóm hàng chọn được nhóm cha, không chọn chính nó"
    - "Màn số chứng từ hiện 7 loại với tiền tố, số chữ số, số đang chạy, ví dụ số kế tiếp; quản lý sửa tiền tố + số chữ số, lỗi hiện đúng ô"
  artifacts:
    - path: "src/features/cai-dat/components/danh-muc-phu.tsx"
      provides: "Một component bảng + ngăn kéo cấu hình được cho kho / nhóm hàng / ĐVT / công đoạn"
    - path: "supabase/migrations/0040_danh_muc_phu.sql"
      provides: "Policy xóa danh mục phụ + chặn đổi mã hệ thống"
  key_links:
    - from: "src/app/(app)/cai-dat/*/page.tsx"
      to: "yeuCauQuyen(...)"
      via: "guard server trước khi render"
---

<objective>
Khu Cài đặt phần dữ liệu nền (CDAT-02, CDAT-03, CDAT-04; D-08, D-09, D-10). Màn Người dùng là plan 14.

Phát hiện khi viết plan: 0015 KHÔNG có policy DELETE cho `nhom_hang`/`don_vi_tinh`/`cong_doan` → xóa sẽ
âm thầm 0 dòng. Và mã công đoạn/ĐVT được code dùng như hằng số (`cong_doan_theo_duoi`, `la_can_ra`,
`cong_doan_khi_tao_moi: "MUA_NGOAI"`, `sp_test` dùng `CAI`) → đổi mã sẽ âm thầm làm hỏng rà dữ liệu.
Migration 0040 xử lý cả hai. (Số 0040 để không đụng migration sửa advisor có thể sinh ở plan 09.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@supabase/migrations/0003_nguoi_dung_kho.sql
@supabase/migrations/0004_danh_muc.sql
@supabase/migrations/0015_rls_danh_muc.sql
@supabase/migrations/0028_cau_hinh_so_ct.sql
@src/shared/components/ngan-keo-form.tsx
@src/shared/lib/quyen.ts
@src/features/xac-thuc/api/nguoi-dung-hien-tai.server.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test 63 đỏ, rồi 0040 — policy xóa + khóa mã hệ thống</name>
  <files>supabase/tests/63_danh_muc_phu_test.sql, supabase/migrations/0040_danh_muc_phu.sql</files>
  <read_first>supabase/migrations/0004_danh_muc.sql, supabase/migrations/0015_rls_danh_muc.sql, supabase/tests/00_helper.sql.inc</read_first>
  <action>
**Test `63_danh_muc_phu_test.sql`** (`plan(6)`):
1. vanphong: `insert into don_vi_tinh (ma, ten) values ('ZQX','Zqx')` rồi `delete from don_vi_tinh where ma='ZQX'` → `is((select count(*) from don_vi_tinh where ma='ZQX'), 0::bigint, 'văn phòng xóa được ĐVT chưa dùng')`
2. vanphong: `throws_ok($$delete from public.don_vi_tinh where ma = 'CAI'$$, '23514', null, 'không xóa được ĐVT hệ thống')`
3. vanphong: `throws_ok($$update public.cong_doan set ma = 'MUA' where ma = 'MUA_NGOAI'$$, '23514', null, 'không đổi mã công đoạn hệ thống')`
4. vanphong: `lives_ok($$update public.cong_doan set ten = 'Mua ngoài (hàng hãng)' where ma = 'MUA_NGOAI'$$, 'đổi TÊN công đoạn hệ thống vẫn được')`
5. Dưới postgres tạo nhóm `NH-ZQX` + `pg_temp.sp_test('DMP-ZQX-1')` gán nhóm đó; vanphong `throws_ok($$delete from public.nhom_hang where ma = 'NH-ZQX'$$, '23503', null, 'xóa nhóm đang dùng bị khóa ngoại chặn')`
6. thukho1: `delete from nhom_hang where ma='NH-ZQX'` không lỗi nhưng 0 dòng → `is((select count(*) from nhom_hang where ma='NH-ZQX'), 1::bigint, 'thủ kho không xóa được')`

**Migration `0040_danh_muc_phu.sql`:**
```sql
create policy "xoa nhom hang" on public.nhom_hang for delete to authenticated
  using ((select public.vai_tro_hien_tai()) in ('quan_ly','van_phong'));
create policy "xoa don vi tinh" on public.don_vi_tinh for delete to authenticated
  using ((select public.vai_tro_hien_tai()) in ('quan_ly','van_phong'));
create policy "xoa cong doan" on public.cong_doan for delete to authenticated
  using ((select public.vai_tro_hien_tai()) in ('quan_ly','van_phong'));

create or replace function public.chan_doi_ma_he_thong()
returns trigger language plpgsql set search_path = '' as $$
declare v_he_thong text[] := case tg_table_name
  when 'cong_doan' then array['EP','SON','CARBON','XI_MA','NANO','MUA_NGOAI']
  when 'don_vi_tinh' then array['CAI']
  else array[]::text[] end;
begin
  if old.ma = any(v_he_thong) and (tg_op = 'DELETE' or new.ma is distinct from old.ma) then
    raise exception 'Mã % là mã hệ thống dùng trong quy tắc rà dữ liệu — chỉ đổi được tên', old.ma
      using errcode = '23514';
  end if;
  return coalesce(new, old);
end $$;
revoke all on function public.chan_doi_ma_he_thong() from public, anon, authenticated;
create trigger chan_doi_ma_cong_doan before update or delete on public.cong_doan
  for each row execute function public.chan_doi_ma_he_thong();
create trigger chan_doi_ma_don_vi_tinh before update or delete on public.don_vi_tinh
  for each row execute function public.chan_doi_ma_he_thong();

alter table public.nhom_hang add constraint ck_nhom_hang_khong_tu_lam_cha check (parent_id is null or parent_id <> id);
```
Kiểm `grant delete` của `authenticated` trên 3 bảng (research: role đã có DELETE mức bảng) — nếu thiếu, thêm `grant delete on ... to authenticated`.
`npm run db:push` → test 63 xanh.
  </action>
  <verify>
    <automated>npm run db:push && npx supabase test db --linked supabase/tests/63_danh_muc_phu_test.sql</automated>
  </verify>
  <acceptance_criteria>
    - 0040 chứa 3 lệnh `for delete to authenticated` và `array['EP','SON','CARBON','XI_MA','NANO','MUA_NGOAI']`
    - Test 63 có `plan(6)` và xanh
  </acceptance_criteria>
  <done>Xóa danh mục phụ chạy thật và an toàn với quy tắc rà dữ liệu</done>
</task>

<task type="auto">
  <name>Task 2: Khung Cài đặt + component danh mục phụ dùng chung cho 4 màn</name>
  <files>src/app/(app)/cai-dat/layout.tsx, src/app/(app)/cai-dat/page.tsx, src/features/cai-dat/components/tab-cai-dat.tsx, src/features/cai-dat/api/danh-muc-phu.api.ts, src/features/cai-dat/components/danh-muc-phu.tsx, src/features/cai-dat/components/ngan-keo-danh-muc-phu.tsx, src/app/(app)/cai-dat/kho/page.tsx, src/app/(app)/cai-dat/nhom-hang/page.tsx, src/app/(app)/cai-dat/don-vi-tinh/page.tsx, src/app/(app)/cai-dat/cong-doan/page.tsx</files>
  <read_first>src/app/(app)/cai-dat/page.tsx, src/shared/components/ngan-keo-form.tsx, src/shared/components/query-state.tsx, src/shared/lib/quyen.ts, src/shared/lib/errors.ts, src/types/database.types.ts, src/features/doi-tac/components/ngan-keo-doi-tac.tsx</read_first>
  <action>
**`cai-dat/layout.tsx`** (Server): `const nd = await yeuCauQuyen("cai_dat_danh_muc_phu")`; `PageHeader tieuDe="Cài đặt"`; render `<TabCaiDat vaiTro={nd.vaiTro} />` rồi `children`.
**`tab-cai-dat.tsx`** (`"use client"`): antd `Tabs` điều khiển bằng `usePathname()`, `onChange` → `router.push`. Mục:
`[{ key: "/cai-dat/nguoi-dung", nhan: "Người dùng", quyen: "cai_dat_nguoi_dung" }, { "/cai-dat/kho", "Kho", "cai_dat_kho" }, { "/cai-dat/nhom-hang", "Nhóm hàng", "cai_dat_danh_muc_phu" }, { "/cai-dat/don-vi-tinh", "Đơn vị tính", "cai_dat_danh_muc_phu" }, { "/cai-dat/cong-doan", "Công đoạn", "cai_dat_danh_muc_phu" }, { "/cai-dat/so-chung-tu", "Số chứng từ", "cai_dat_so_chung_tu" }]` — lọc `coQuyen`. Trên màn nhỏ Tabs tự cuộn ngang.
**`cai-dat/page.tsx`**: redirect tới tab đầu tiên người dùng có quyền (`/cai-dat/nguoi-dung` cho quản lý, `/cai-dat/nhom-hang` cho văn phòng).
**Mỗi page con** (Server): guard `yeuCauQuyen(<quyền của tab>)` rồi render `<DanhMucPhu cauHinh="kho" | "nhom_hang" | "don_vi_tinh" | "cong_doan" />`.
Trang `nguoi-dung` KHÔNG tạo ở plan này (plan 14).

**`danh-muc-phu.api.ts`** — kiểu `BangDanhMucPhu = "kho" | "nhom_hang" | "don_vi_tinh" | "cong_doan"`, cấu hình:
```ts
export const CAU_HINH_DANH_MUC_PHU = {
  kho:         { nhan: "kho", cot: "id, ma, ten, dia_chi, dang_hoat_dong, updated_at", xoaDuoc: false, coCha: false, coMau: false, coDiaChi: true },
  nhom_hang:   { nhan: "nhóm hàng", cot: "id, ma, ten, parent_id, thu_tu, updated_at", xoaDuoc: true, coCha: true, coMau: false, coDiaChi: false },
  don_vi_tinh: { nhan: "đơn vị tính", cot: "id, ma, ten, updated_at", xoaDuoc: true, coCha: false, coMau: false, coDiaChi: false },
  cong_doan:   { nhan: "công đoạn", cot: "id, ma, ten, mau_hien_thi, updated_at", xoaDuoc: true, coCha: false, coMau: true, coDiaChi: false },
} as const;
```
Hàm `layDanhMucPhu(bang)` (order `ma`; `nhom_hang` kèm số mã dùng: gọi thêm `from("san_pham").select("nhom_hang_id")` KHÔNG — tốn 3.266 dòng; thay bằng hiển thị lỗi khi xóa), `taoMucDanhMucPhu(bang, v)` (`.select("id").single()`), `capNhatMucDanhMucPhu(bang, id, v)`, `xoaMucDanhMucPhu(bang, id)` — sau `delete` kiểm `count` bằng `{ count: "exact" }`; `count === 0` → throw lỗi "Không xóa được — tài khoản không có quyền". Keys `["danh-muc-phu", bang]`; mutation invalidate cả `["danh-muc-phu"]` (danh mục hàng dùng chung key ở plan 11).
Kiểu từng bảng lấy từ `Database["public"]["Tables"][bang]["Row"]`, không viết tay.

**`danh-muc-phu.tsx`** (`"use client"`): `QueryState` + `Table` (Mã, Tên, cột theo cấu hình: Nhóm cha (tên tra từ chính danh sách), Màu (`Tag color={mau}`), Địa chỉ, Trạng thái kho), nút "Thêm <nhan>", nút Sửa mỗi dòng, nút Xóa (khi `xoaDuoc`) bọc `Popconfirm` "Xóa <nhan> '<ten>'?". Lỗi xóa: `23503` → `message.error("Đang có mã hàng dùng <nhan> này — đổi các mã đó sang <nhan> khác trước khi xóa")`; `23514` → message server (mã hệ thống). Mã hệ thống (danh sách giống migration) hiện `Tag` "Hệ thống" và không có nút Xóa; ô Mã khóa khi sửa.
Rỗng: "Chưa có <nhan> nào. Bấm Thêm <nhan> để tạo."

**`ngan-keo-danh-muc-phu.tsx`**: RHF + zod dựng theo cấu hình (`ma` `^[A-Z0-9_]{1,20}$` tự viết hoa, `ten` bắt buộc; `parent_id` Select các nhóm trừ chính nó; `mau_hien_thi` `ColorPicker` antd trả hex; `dia_chi`; `dang_hoat_dong` Switch chỉ cho kho). Lỗi `23505` → lỗi ô Mã "Mã đã tồn tại". Dùng `NganKeoForm`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - Tồn tại 4 page `cai-dat/{kho,nhom-hang,don-vi-tinh,cong-doan}/page.tsx`, mỗi file chứa `yeuCauQuyen(`
    - `danh-muc-phu.api.ts` chứa `CAU_HINH_DANH_MUC_PHU` và `count: "exact"`
    - `danh-muc-phu.tsx` chứa `"23503"` và `Popconfirm`
    - `tab-cai-dat.tsx` chứa `coQuyen(`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>CDAT-02, CDAT-03 dùng được</done>
</task>

<task type="auto">
  <name>Task 3: Màn cấu hình số chứng từ</name>
  <files>src/app/(app)/cai-dat/so-chung-tu/page.tsx, src/features/cai-dat/components/cau-hinh-so-ct.tsx</files>
  <read_first>supabase/migrations/0028_cau_hinh_so_ct.sql, src/types/database.types.ts, src/shared/components/query-state.tsx, src/shared/lib/errors.ts</read_first>
  <action>
**page** (Server): `yeuCauQuyen("cai_dat_so_chung_tu")`; render `<CauHinhSoCt />`.
**`cau-hinh-so-ct.tsx`** (`"use client"`):
- Query `rpc("danh_sach_cau_hinh_so_ct")`, key `["cau-hinh-so-ct"]`.
- Nhãn loại: `{ NHAP: "Phiếu nhập", XUAT: "Phiếu xuất", TRA_NCC: "Trả hàng NCC", TRA_KHACH: "Khách trả hàng", CHUYEN_KHO: "Chuyển kho", KIEM_KE: "Kiểm kê", DIEU_CHINH: "Điều chỉnh" }`.
- `Table` 7 dòng: Loại, Tiền tố (ô `Input` in hoa, maxLength 5), Số chữ số (`InputNumber` min 3 max 8), Số đã phát năm nay, Số kế tiếp (ví dụ, cập nhật trực tiếp khi gõ: tính ở client bằng cùng công thức `tien_to + yy + '-' + lpad(so_hien_tai+1, so_chu_so)`), nút "Lưu" mỗi dòng chỉ bật khi dòng đổi.
- Validate client trước khi gửi: tiền tố `^[A-Z0-9]{1,5}$`, không trùng tiền tố dòng khác (hiện lỗi dưới ô); số chữ số ≥ độ dài số đã phát (thông báo "Đã phát tới số X — cần ít nhất N chữ số").
- Lưu: `from("cau_hinh_so_ct").update({ tien_to, so_chu_so }).eq("loai_ct", loai)` với `{ count: "exact" }`; count 0 → lỗi quyền. Lỗi `23505` → ô tiền tố "Tiền tố đã dùng cho loại khác"; `23514` → thông báo server dưới ô tương ứng.
- `Alert type="info"` đầu màn: "Đổi tiền tố chỉ áp cho chứng từ tạo sau. Số đã phát giữ nguyên. Số thứ tự tự đặt lại về 1 vào đầu năm."
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `cau-hinh-so-ct.tsx` chứa `rpc("danh_sach_cau_hinh_so_ct")`, `"23505"`, `^[A-Z0-9]{1,5}$`
    - page chứa `yeuCauQuyen("cai_dat_so_chung_tu")`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>CDAT-04 dùng được</done>
</task>

</tasks>

<verification>
- [ ] Test 63 xanh, `npm run check` xanh
- [ ] Thử tay: văn phòng thấy 3 tab, quản lý thấy 6 tab (tab Người dùng 404 tới khi plan 14 (wave 7) xong — chấp nhận tạm)
</verification>

<success_criteria>
- Dữ liệu nền cấu hình được qua giao diện, không phá quy tắc rà dữ liệu
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-15-SUMMARY.md`
</output>
