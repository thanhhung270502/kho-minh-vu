---
phase: 02-khung-ung-dung
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/lib/chuan-hoa.ts
  - src/shared/lib/tiep-tuc.ts
  - src/shared/lib/quyen.ts
  - scripts/kiem-tra-ham-thuan.ts
  - src/proxy.ts
  - src/app/dang-nhap/page.tsx
  - src/features/xac-thuc/schemas/dang-nhap.schema.ts
  - src/features/xac-thuc/components/form-dang-nhap.tsx
  - src/features/xac-thuc/api/nguoi-dung-hien-tai.server.ts
  - src/app/(app)/layout.tsx
  - src/app/(app)/page.tsx
  - src/app/(app)/khong-du-quyen/page.tsx
  - src/shared/components/khong-du-quyen.tsx
  - src/app/(app)/danh-muc/page.tsx
  - src/app/(app)/doi-tac/page.tsx
  - src/app/(app)/cai-dat/page.tsx
  - src/shared/components/app-shell.tsx
  - src/shared/lib/errors.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-07]
user_setup: []
must_haves:
  truths:
    - "Người dùng gõ tên đăng nhập (không cần @khominhvu.local) và mật khẩu để đăng nhập; F5 vẫn giữ phiên"
    - "Vào route nội bộ khi chưa đăng nhập bị đẩy về /dang-nhap?tiep_tuc=<đường dẫn + query>; đăng nhập xong về đúng chỗ"
    - "tiep_tuc trỏ ra ngoài (//evil.com, https://evil.com, /\\evil.com) bị thay bằng /"
    - "Gọi /api/* khi chưa đăng nhập nhận JSON 401, không bị redirect HTML"
    - "Header mọi trang nội bộ hiện họ tên + vai trò và nút Đăng xuất"
    - "Menu chỉ hiện mục vai trò có quyền; route phạm vi cũ /san-xuat /bao-cao /kho không còn"
    - "Sai mật khẩu hiện 'Sai tên đăng nhập hoặc mật khẩu'; tài khoản bị ban hiện 'Tài khoản đã bị vô hiệu hóa'"
  artifacts:
    - path: "src/shared/lib/tiep-tuc.ts"
      provides: "tiepTucAnToan()"
      exports: ["tiepTucAnToan"]
    - path: "src/shared/lib/quyen.ts"
      provides: "Ma trận quyền D-07 dùng chung cho menu, nút, trang"
      exports: ["VaiTro", "coQuyen", "NHAN_VAI_TRO"]
    - path: "src/app/dang-nhap/page.tsx"
      provides: "Trang đăng nhập"
  key_links:
    - from: "src/proxy.ts"
      to: "src/shared/lib/tiep-tuc.ts"
      via: "import { tiepTucAnToan }"
    - from: "src/app/(app)/layout.tsx"
      to: "src/features/xac-thuc/api/nguoi-dung-hien-tai.server.ts"
      via: "await layNguoiDungHienTai()"
---

<objective>
Dựng khung ứng dụng thật đầu tiên: đăng nhập bằng tên, chặn route, header + đăng xuất, menu theo vai
trò (D-01, D-07, D-34, D-35, D-36). Xóa sạch dấu vết phạm vi "sản xuất/xưởng" cũ.

Plan này KHÔNG đụng cột mới của Phase 2 (`phai_doi_mat_khau`, `nguoi_dung_kho`) vì kiểu TypeScript
chưa sinh lại tới plan 09 (wave 4) — việc bắt đổi mật khẩu nằm ở plan 10.

Không cài thư viện mới. Hàm thuần kiểm bằng `tsx` + `node:assert`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@CLAUDE.md
@src/proxy.ts
@src/lib/supabase/middleware.ts
@src/lib/supabase/server.ts
@src/lib/supabase/client.ts
@src/shared/components/app-shell.tsx
@src/shared/lib/errors.ts
@src/providers/app-providers.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hàm thuần — bỏ dấu, tên đăng nhập, tiep_tuc an toàn, ma trận quyền (test trước)</name>
  <files>scripts/kiem-tra-ham-thuan.ts, src/shared/lib/chuan-hoa.ts, src/shared/lib/tiep-tuc.ts, src/shared/lib/quyen.ts</files>
  <read_first>scripts/import-kiotviet/tach-dvt-cong-doan.ts, src/types/database.types.ts, package.json</read_first>
  <action>
**Test trước** — `scripts/kiem-tra-ham-thuan.ts` dùng `import assert from "node:assert/strict"`, import từ
`../src/shared/lib/*` (đường dẫn tương đối, tsx không đọc alias `@/`). Viết đủ các assert rồi chạy thấy lỗi import:
```ts
assert.equal(boDau("Đặng Thị Ngọc"), "Dang Thi Ngoc");
assert.equal(chuanHoaTenDangNhap("  Kim.Chi "), "kim.chi");
assert.equal(chuanHoaTenDangNhap("Ngọc Ánh"), "ngocanh");
assert.equal(tenDangNhapThanhEmail("thukho1"), "thukho1@khominhvu.local");
assert.equal(tenDangNhapThanhEmail("thukho1@khominhvu.local"), "thukho1@khominhvu.local");
assert.equal(tiepTucAnToan("/danh-muc?nhom=a"), "/danh-muc?nhom=a");
for (const xau of [null, "", "danh-muc", "//evil.com", "/\\evil.com", "https://evil.com", "/x://y", "/dang-nhap"]) {
  assert.equal(tiepTucAnToan(xau), "/");
}
assert.equal(coQuyen("thu_kho", "xem_danh_muc"), true);
assert.equal(coQuyen("thu_kho", "sua_danh_muc"), false);
assert.equal(coQuyen("van_phong", "xem_gia_von"), true);
assert.equal(coQuyen("van_phong", "sua_gia_ban"), false);
assert.equal(coQuyen("van_phong", "cai_dat_danh_muc_phu"), true);
assert.equal(coQuyen("van_phong", "cai_dat_nguoi_dung"), false);
assert.equal(coQuyen("chi_xem", "xem_gia_von"), false);
console.log("✓ hàm thuần: tất cả assert đạt");
```

**`src/shared/lib/chuan-hoa.ts`**:
```ts
/** Bỏ dấu tiếng Việt, giữ hoa/thường. Nguồn dùng chung cho đăng nhập, Excel, tìm kiếm phía client. */
export function boDau(v: string): string {
  return v.normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/đ/g, "d").replace(/Đ/g, "D");
}
export const MIEN_EMAIL_NOI_BO = "khominhvu.local";
export function chuanHoaTenDangNhap(v: string): string {
  return boDau(v).toLowerCase().replace(/[^a-z0-9._-]/g, "");
}
/** D-01: người dùng gõ tên, Supabase Auth cần email. Đã gõ đủ email nội bộ thì giữ nguyên. */
export function tenDangNhapThanhEmail(v: string): string {
  const s = v.trim().toLowerCase();
  if (s.endsWith(`@${MIEN_EMAIL_NOI_BO}`)) return s;
  return `${chuanHoaTenDangNhap(s)}@${MIEN_EMAIL_NOI_BO}`;
}
```
**`src/shared/lib/tiep-tuc.ts`**:
```ts
/** D-35: chỉ nhận đường dẫn nội bộ. Chặn open redirect. Không quay về chính /dang-nhap. */
export function tiepTucAnToan(gt: string | null | undefined): string {
  if (!gt || !gt.startsWith("/") || gt.startsWith("//") || gt.startsWith("/\\") || gt.includes("://")) return "/";
  if (gt === "/dang-nhap" || gt.startsWith("/dang-nhap?")) return "/";
  return gt;
}
```
**`src/shared/lib/quyen.ts`** — ma trận D-07 (bảng trong 02-CONTEXT.md), kiểu vai trò suy từ types:
```ts
import type { Database } from "@/types/database.types";
export type VaiTro = Database["public"]["Enums"]["vai_tro"];
export type Quyen =
  | "xem_danh_muc" | "sua_danh_muc" | "xem_gia_von" | "sua_gia_ban"
  | "cai_dat_danh_muc_phu" | "cai_dat_nguoi_dung" | "cai_dat_kho" | "cai_dat_so_chung_tu";
const MA_TRAN: Record<Quyen, readonly VaiTro[]> = {
  xem_danh_muc: ["quan_ly", "van_phong", "thu_kho", "chi_xem"],
  sua_danh_muc: ["quan_ly", "van_phong"],
  xem_gia_von: ["quan_ly", "van_phong"],
  sua_gia_ban: ["quan_ly"],
  cai_dat_danh_muc_phu: ["quan_ly", "van_phong"],
  cai_dat_nguoi_dung: ["quan_ly"],
  cai_dat_kho: ["quan_ly"],
  cai_dat_so_chung_tu: ["quan_ly"],
};
export function coQuyen(vaiTro: VaiTro | null | undefined, quyen: Quyen): boolean {
  return !!vaiTro && MA_TRAN[quyen].includes(vaiTro);
}
export const NHAN_VAI_TRO: Record<VaiTro, string> = {
  quan_ly: "Quản lý", van_phong: "Văn phòng", thu_kho: "Thủ kho", chi_xem: "Chỉ xem",
};
```
Nếu `tsx` không phân giải `@/types/...` trong `quyen.ts`, đổi thành `import type ... from "../../types/database.types"` (import type bị xóa khi chạy, không ảnh hưởng runtime).

Ghi comment đầu `quyen.ts`: "Ma trận này CHỈ quyết định ẩn/hiện giao diện. Chặn thật nằm ở RLS — sửa quyền phải sửa migration trước."
  </action>
  <verify>
    <automated>npx tsx scripts/kiem-tra-ham-thuan.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/kiem-tra-ham-thuan.ts` in `✓ hàm thuần: tất cả assert đạt`
    - `src/shared/lib/tiep-tuc.ts` export `tiepTucAnToan` và chứa `gt.startsWith("//")`
    - `src/shared/lib/quyen.ts` export `coQuyen`, `NHAN_VAI_TRO`, `VaiTro`
    - `src/shared/lib/chuan-hoa.ts` chứa `MIEN_EMAIL_NOI_BO = "khominhvu.local"`
  </acceptance_criteria>
  <done>Logic nhạy cảm của đăng nhập và quyền giao diện là hàm thuần có kiểm</done>
</task>

<task type="auto">
  <name>Task 2: Proxy chặn route + trang đăng nhập</name>
  <files>src/proxy.ts, src/app/dang-nhap/page.tsx, src/features/xac-thuc/schemas/dang-nhap.schema.ts, src/features/xac-thuc/components/form-dang-nhap.tsx, src/shared/lib/errors.ts</files>
  <read_first>src/proxy.ts, src/lib/supabase/middleware.ts, src/lib/supabase/client.ts, src/shared/lib/errors.ts, src/app/layout.tsx, src/providers/app-providers.tsx, CLAUDE.md</read_first>
  <action>
**`src/proxy.ts`** — thay khối comment bằng logic thật, giữ `config.matcher`:
```ts
const DUONG_CONG_KHAI = ["/dang-nhap"];
export async function proxy(request: NextRequest) {
  const { response, user } = await updateSession(request);
  const { pathname, search } = request.nextUrl;
  const laCongKhai = DUONG_CONG_KHAI.some((p) => pathname === p || pathname.startsWith(`${p}/`));

  if (!user && pathname.startsWith("/api/")) {
    return NextResponse.json({ loai: "het-phien", thongBao: "Phiên đăng nhập đã hết hạn" }, { status: 401 });
  }
  if (!user && !laCongKhai) {
    const url = request.nextUrl.clone();
    url.pathname = "/dang-nhap";
    url.search = "";
    url.searchParams.set("tiep_tuc", tiepTucAnToan(`${pathname}${search}`));
    return NextResponse.redirect(url);
  }
  if (user && pathname === "/dang-nhap") {
    const url = request.nextUrl.clone();
    const dich = tiepTucAnToan(request.nextUrl.searchParams.get("tiep_tuc"));
    return NextResponse.redirect(new URL(dich, request.url));
  }
  return response;
}
```
Redirect PHẢI chép cookie đã làm mới từ `response` sang response redirect (nếu không phiên vừa refresh mất):
viết helper `chuyenHuong(url, response)` tạo `NextResponse.redirect(url)` rồi `response.cookies.getAll().forEach((c) => r.cookies.set(c))`.

**`src/features/xac-thuc/schemas/dang-nhap.schema.ts`**:
```ts
export const dangNhapSchema = z.object({
  tenDangNhap: z.string().trim().min(1, "Nhập tên đăng nhập"),
  matKhau: z.string().min(1, "Nhập mật khẩu"),
});
export type DangNhapInput = z.infer<typeof dangNhapSchema>;
```
**`src/features/xac-thuc/components/form-dang-nhap.tsx`** (`"use client"`): antd `Form` điều khiển bằng
React Hook Form `Controller` + `zodResolver`; `Input` (autoComplete "username", autoFocus) và
`Input.Password` (autoComplete "current-password"); nút `htmlType="submit"` `block` `size="large"`
`loading={isSubmitting}` (chặn bấm nhiều lần). Submit:
```ts
const { error } = await getSupabaseBrowserClient().auth.signInWithPassword({
  email: tenDangNhapThanhEmail(v.tenDangNhap), password: v.matKhau,
});
if (error) { const loi = dienGiaiLoi(error); setError("root", { message: `${loi.tieuDe}. ${loi.huongXuLy}` }); return; }
router.replace(tiepTucAnToan(searchParams.get("tiep_tuc")));
router.refresh();
```
Lỗi `root` hiện bằng `Alert type="error"` phía trên nút. Sai mật khẩu KHÔNG xóa ô tên đăng nhập.
Tham số `?loi=vo-hieu-hoa` (layout đẩy về khi tài khoản bị khóa) hiện `Alert type="warning"` "Tài khoản đã bị vô hiệu hóa. Liên hệ quản lý để mở lại."

**`src/app/dang-nhap/page.tsx`** (Server Component): metadata `title: "Đăng nhập"`; layout căn giữa
màn hình (`min-h-screen flex items-center justify-center p-4`), khung `w-full max-w-sm`, tiêu đề
"Kho Minh Vũ", phụ đề "Đăng nhập bằng tên đăng nhập được quản lý cấp". Bọc form trong
`<Suspense>` (form dùng `useSearchParams`). Trang ngoài group `(app)` nên không có AppShell.

**`src/shared/lib/errors.ts`** — trong nhánh `error instanceof AuthError`, TRƯỚC nhánh hết phiên:
- `error.code === "invalid_credentials"` → `{ loai: "du-lieu-khong-hop-le", tieuDe: "Sai tên đăng nhập hoặc mật khẩu", huongXuLy: "Kiểm tra lại, chú ý bộ gõ tiếng Việt và phím Caps Lock.", maGoc }`
- `error.code === "user_banned"` → `{ loai: "khong-du-quyen", tieuDe: "Tài khoản đã bị vô hiệu hóa", huongXuLy: "Liên hệ quản lý để mở lại tài khoản.", maGoc }`
- `error.code === "weak_password"` → `{ loai: "du-lieu-khong-hop-le", tieuDe: "Mật khẩu quá yếu", huongXuLy: "Dùng ít nhất 8 ký tự, có cả chữ và số.", maGoc }`
Đồng thời sửa chữ (D-36): mọi "xưởng" → "kho"; "Kiểm tra lại lô, xưởng và số lượng vừa nhập" → "Kiểm tra lại mã hàng, kho và số lượng vừa nhập". `grep -n "xưởng\|lô" src/shared/lib/errors.ts` phải rỗng.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/proxy.ts` chứa `tiepTucAnToan`, `status: 401` và `pathname.startsWith("/api/")`; không còn dòng comment `//   if (!user`
    - `src/features/xac-thuc/components/form-dang-nhap.tsx` bắt đầu bằng `"use client"`, chứa `signInWithPassword` và `tenDangNhapThanhEmail`
    - `src/app/dang-nhap/page.tsx` chứa `<Suspense`
    - `src/shared/lib/errors.ts` chứa `"invalid_credentials"` và `"user_banned"`; `grep -c "xưởng" src/shared/lib/errors.ts` = 0
    - `npm run typecheck` và `npm run lint` exit 0
  </acceptance_criteria>
  <done>Chưa đăng nhập bị chặn đúng cách, đăng nhập bằng tên chạy được</done>
</task>

<task type="auto">
  <name>Task 3: Khung app theo vai trò, đăng xuất, xóa route phạm vi cũ</name>
  <files>src/features/xac-thuc/api/nguoi-dung-hien-tai.server.ts, src/app/(app)/layout.tsx, src/shared/components/app-shell.tsx, src/app/(app)/page.tsx, src/app/(app)/khong-du-quyen/page.tsx, src/shared/components/khong-du-quyen.tsx, src/app/(app)/danh-muc/page.tsx, src/app/(app)/doi-tac/page.tsx, src/app/(app)/cai-dat/page.tsx</files>
  <read_first>src/app/(app)/layout.tsx, src/shared/components/app-shell.tsx, src/shared/components/page-header.tsx, src/shared/components/chua-trien-khai.tsx, src/lib/supabase/server.ts, src/app/(app)/page.tsx, src/app/(app)/danh-muc/page.tsx</read_first>
  <action>
**`src/features/xac-thuc/api/nguoi-dung-hien-tai.server.ts`** (`import "server-only"`):
```ts
export type NguoiDungHienTai = { id: string; hoTen: string; vaiTro: VaiTro };
/** Đọc vai trò từ BẢNG (không từ claim) để giao diện khớp RLS ngay sau khi quản lý đổi quyền. */
export async function layNguoiDungHienTai(): Promise<NguoiDungHienTai | null> {
  const supabase = await createSupabaseServerClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return null;
  const { data, error } = await supabase.from("nguoi_dung")
    .select("id, ho_ten, vai_tro, dang_hoat_dong").eq("id", user.id).maybeSingle();
  if (error) throw error;
  if (!data || !data.dang_hoat_dong) return null;
  return { id: data.id, hoTen: data.ho_ten, vaiTro: data.vai_tro };
}
export async function yeuCauQuyen(quyen: Quyen): Promise<NguoiDungHienTai> {
  const nd = await layNguoiDungHienTai();
  if (!nd) redirect("/dang-nhap");
  if (!coQuyen(nd.vaiTro, quyen)) redirect("/khong-du-quyen");
  return nd;
}
```
**`src/app/(app)/layout.tsx`** (Server Component, async): `const nd = await layNguoiDungHienTai();`
- `nd === null` nhưng `getUser()` có user (hồ sơ thiếu hoặc bị vô hiệu hóa) → gọi `supabase.auth.signOut()` rồi `redirect("/dang-nhap?loi=vo-hieu-hoa")`.
- `nd === null` và không có user → `redirect("/dang-nhap")` (proxy thường đã chặn trước).
- Có `nd` → `<AppShell nguoiDung={nd}>{children}</AppShell>`.

**`src/shared/components/app-shell.tsx`** — viết lại (giữ cấu trúc Sider/Header/Content, `breakpoint="lg"`, `collapsedWidth={0}`, nút thu gọn tự đặt trong Header):
- Prop `nguoiDung: { hoTen: string; vaiTro: VaiTro }`.
- Tiêu đề Sider: "Kho Minh Vũ".
- Menu:
```ts
const MUC_MENU = [
  { duongDan: "/", nhan: "Tổng quan", icon: <DashboardOutlined />, quyen: "xem_danh_muc" },
  { duongDan: "/danh-muc", nhan: "Danh mục hàng", icon: <AppstoreOutlined />, quyen: "xem_danh_muc" },
  { duongDan: "/doi-tac", nhan: "Đối tác", icon: <TeamOutlined />, quyen: "xem_danh_muc" },
  { duongDan: "/cai-dat", nhan: "Cài đặt", icon: <SettingOutlined />, quyen: "cai_dat_danh_muc_phu" },
] satisfies Array<{ duongDan: string; nhan: string; icon: ReactNode; quyen: Quyen }>;
```
  lọc bằng `coQuyen(nguoiDung.vaiTro, muc.quyen)` (D-07: ẩn hẳn).
- Header bên phải: `Dropdown` với trigger là `Avatar` + họ tên + `Tag` vai trò (`NHAN_VAI_TRO`), items: "Đổi mật khẩu" (`href="/doi-mat-khau"`, trang làm ở plan 10), "Đăng xuất" (danger). Đăng xuất:
```ts
await getSupabaseBrowserClient().auth.signOut();
queryClient.clear();
router.replace("/dang-nhap");
router.refresh();
```
  Trên màn < 640px chỉ hiện Avatar (ẩn họ tên bằng class `hidden sm:inline`).
- Bỏ dòng "Ép nhựa · Sơn · Carbon · Xi mạ · Đóng gói" và mọi chữ "xưởng".
- File vượt ~200 dòng thì tách `menu-tai-khoan.tsx` cạnh nó.

**Route:**
- Xóa `src/app/(app)/san-xuat/`, `src/app/(app)/bao-cao/`, `src/app/(app)/kho/` (git rm).
- `src/app/(app)/page.tsx`: tiêu đề "Tổng quan", mô tả "Tình hình kho trong ngày"; `ChuaTrienKhai` với `seCo` ["Tồn kho theo nhóm hàng và công đoạn", "Mã dưới định mức tồn tối thiểu", "Hàng không luân chuyển quá 30 ngày", "Biểu đồ nhập–xuất 30 ngày"], `phuThuoc="phiếu nhập và phiếu xuất (Phase 3–4)"`.
- `src/app/(app)/danh-muc/page.tsx`, `doi-tac/page.tsx`, `cai-dat/page.tsx`: placeholder `ChuaTrienKhai` với nội dung Phase 2 tương ứng (plan 16/13/15 sẽ thay). `cai-dat/page.tsx` gọi `await yeuCauQuyen("cai_dat_danh_muc_phu")`.
- `src/app/(app)/khong-du-quyen/page.tsx`: Server Component chỉ render `<KhongDuQuyen />` từ file client `src/shared/components/khong-du-quyen.tsx` (`"use client"` BẮT BUỘC — bẫy #1 CLAUDE.md: file import antd phải là Client Component). Component dùng antd `Result status="403"` title "Tài khoản không có quyền mở trang này", subTitle "Trang này dành cho vai trò khác. Liên hệ quản lý nếu bạn cần quyền.", nút "Về trang chính" `href="/"`.

Chạy `npm run check`, rồi `npm run dev` và thử tay: mở `/danh-muc` ẩn danh → bị đẩy về `/dang-nhap?tiep_tuc=%2Fdanh-muc`; đăng nhập `vanphong` (mật khẩu `SEED_USER_PASSWORD`) → về `/danh-muc`, thấy menu có Cài đặt; đăng nhập `thukho1` → không có Cài đặt, gõ `/cai-dat` → trang 403. Ghi kết quả vào SUMMARY.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/san-xuat`, `src/app/(app)/bao-cao`, `src/app/(app)/kho` không tồn tại
    - `grep -rn "xưởng\|san-xuat\|Theo dõi sản xuất" src/` không trả kết quả
    - `src/shared/components/app-shell.tsx` chứa `coQuyen(` và `auth.signOut()` (hoặc file tách `menu-tai-khoan.tsx` chứa `auth.signOut()`)
    - `src/app/(app)/layout.tsx` chứa `layNguoiDungHienTai` và `/dang-nhap?loi=vo-hieu-hoa`
    - `src/features/xac-thuc/api/nguoi-dung-hien-tai.server.ts` chứa `import "server-only"` và export `yeuCauQuyen`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>App có khung thật theo vai trò, đăng xuất được từ mọi trang, không còn phạm vi sản xuất cũ</done>
</task>

</tasks>

<verification>
- [ ] `npx tsx scripts/kiem-tra-ham-thuan.ts` xanh
- [ ] `npm run check` exit 0
- [ ] Thử tay 3 luồng ở Task 3 ghi trong SUMMARY
</verification>

<success_criteria>
- AUTH-01, AUTH-02, AUTH-07 dùng được bằng tài khoản demo
- Menu và trang tuân ma trận D-07
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-05-SUMMARY.md`
</output>
