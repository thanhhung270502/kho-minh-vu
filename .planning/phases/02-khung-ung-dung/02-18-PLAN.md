---
phase: 02-khung-ung-dung
plan: 18
type: execute
wave: 6
depends_on: ["02-13"]
files_modified:
  - src/app/(app)/doi-tac/ra-ghi-chu/page.tsx
  - src/features/doi-tac/api/ra-ghi-chu.api.ts
  - src/features/doi-tac/lib/ghi-chu.ts
  - scripts/kiem-tra-ham-thuan.ts
  - src/features/doi-tac/components/ra-ghi-chu.tsx
  - src/features/doi-tac/components/hanh-dong-ghi-chu.tsx
autonomous: true
requirements: [DLIEU-04]
user_setup: []
must_haves:
  truths:
    - "Màn Rà ghi chú liệt kê ~150 giá trị ghi chú KiotViet, sắp theo số hóa đơn giảm dần, mỗi dòng có số hóa đơn, khoảng ngày, hóa đơn mẫu"
    - "Bộ đếm 'Còn N giá trị chưa rà / tổng' luôn hiện và giảm sau mỗi quyết định"
    - "Mỗi giá trị có 5 hành động: Tạo khách mới, Gộp vào khách có sẵn, Là sale, Khách + sale, Bỏ qua — lưu ngay một lần bấm"
    - "Tab Đã rà xem lại quyết định và Hủy quyết định được"
    - "Tạo khách điền sẵn tên từ ghi chú (viết hoa đầu từ), sửa được trước khi lưu; mã tự gợi ý KH…"
    - "Thủ kho/chỉ xem mở trang bị chuyển Không đủ quyền"
  artifacts:
    - path: "src/features/doi-tac/components/ra-ghi-chu.tsx"
      provides: "Màn rà ghi chú"
  key_links:
    - from: "hanh-dong-ghi-chu.tsx"
      to: "rpc quyet_ghi_chu"
      via: "useQuyetGhiChu().mutateAsync"
---

<objective>
Công cụ để người văn phòng biến ~150 giá trị ô Ghi chú KiotViet thành danh sách khách hàng thật (DLIEU-04;
D-28..D-31). Hệ thống KHÔNG tự phân loại — người dùng xác nhận các tên ngắn lẫn cả khách và sale.

Nhịp dùng mong muốn: người văn phòng ngồi rà 10–20 phút, mỗi giá trị ≤ 2 lần bấm cho ca phổ biến
(Là sale / Bỏ qua / Tạo khách với tên đề xuất).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@.planning/phases/02-khung-ung-dung/02-07-SUMMARY.md
@src/features/doi-tac/api/doi-tac.api.ts
@src/features/doi-tac/hooks/useDoiTac.ts
@src/shared/components/query-state.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lớp dữ liệu rà ghi chú</name>
  <files>src/features/doi-tac/api/ra-ghi-chu.api.ts, src/features/doi-tac/lib/ghi-chu.ts, scripts/kiem-tra-ham-thuan.ts</files>
  <read_first>src/types/database.types.ts, src/features/doi-tac/api/doi-tac.api.ts, src/features/doi-tac/api/doi-tac.keys.ts</read_first>
  <action>
- Kiểu `DongGhiChu = Database["public"]["Functions"]["danh_sach_ghi_chu_kiotviet"]["Returns"][number]`; `LoaiQuyet = "KHACH" | "SALE" | "KHACH_VA_SALE" | "BO_QUA"`.
- `layGhiChu({ trangThai: "chua_ra" | "da_ra", q, trang })` → rpc `danh_sach_ghi_chu_kiotviet` (`p_kich_thuoc: 30`) → `{ dong, tong }`.
- `demGhiChu()` → hai lệnh song song `p_trang_thai: "chua_ra"` và `null` với `p_kich_thuoc: 1` → `{ chuaRa, tong }` (đọc `tong_so_dong`; 0 dòng → 0).
- `quyetGhiChu({ giaTri, loai, doiTacId?, taoKhach?: { ma?: string; ten: string }, tenSale? })` → rpc `quyet_ghi_chu` (tham số `p_tao_khach` là object jsonb).
- `boQuyetGhiChu(giaTri)` → rpc `bo_quyet_ghi_chu`.
- `timKhach(q)` → rpc `danh_sach_doi_tac` `{ p_tu_khoa: q, p_loai: "KHACH", p_kich_thuoc: 20 }` (cho Select gộp).
- `goiYTenKhach(giaTri)`: hàm thuần — viết hoa chữ đầu mỗi từ theo locale vi (`toLocaleLowerCase("vi")` rồi hoa ký tự đầu), bỏ phần sau xuống dòng đầu tiên (địa chỉ/SĐT hay nằm dòng 2).
- `tachSoDienThoai(giaTri)`: regex `/(0\d{9,10})/` lấy SĐT đầu tiên nếu có (điền sẵn cho form tạo khách).
Keys: `khoaRaGhiChu = { tatCa: ["ra-ghi-chu"], danhSach: (b) => ["ra-ghi-chu","ds",b], dem: ["ra-ghi-chu","dem"] }`.
Hook trong cùng file hoặc `hooks/useRaGhiChu.ts` (nếu tách thì thêm vào files_modified trong SUMMARY): `useGhiChu`, `useDemGhiChu`, `useQuyetGhiChu` (onSuccess invalidate `khoaRaGhiChu.tatCa` + `khoaDoiTac.tatCa`), `useBoQuyetGhiChu`.
Thêm assert cho `goiYTenKhach("TIẾN DŨNG 602 QUANG TRUNG\nSĐT 0909")` = "Tiến Dũng 602 Quang Trung" và `tachSoDienThoai("HUY HOÀNG 5 \nPHƯỚC HẬU 0966116224")` = "0966116224" vào `scripts/kiem-tra-ham-thuan.ts` — hai hàm đặt ở file thuần `src/features/doi-tac/lib/ghi-chu.ts` (không import supabase) để script kiểm được.
  </action>
  <verify>
    <automated>npx tsx scripts/kiem-tra-ham-thuan.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `ra-ghi-chu.api.ts` chứa `rpc("danh_sach_ghi_chu_kiotviet"`, `rpc("quyet_ghi_chu"`, `rpc("bo_quyet_ghi_chu"`
    - Script hàm thuần xanh với hai assert mới
  </acceptance_criteria>
  <done>Dữ liệu rà ghi chú có kiểu, có gợi ý tên/SĐT kiểm được</done>
</task>

<task type="auto">
  <name>Task 2: Màn Rà ghi chú + hành động từng giá trị</name>
  <files>src/app/(app)/doi-tac/ra-ghi-chu/page.tsx, src/features/doi-tac/components/ra-ghi-chu.tsx, src/features/doi-tac/components/hanh-dong-ghi-chu.tsx</files>
  <read_first>src/features/doi-tac/api/ra-ghi-chu.api.ts, src/features/doi-tac/components/bang-doi-tac.tsx, src/shared/components/query-state.tsx, src/shared/lib/errors.ts</read_first>
  <action>
**page** (Server): `yeuCauQuyen("sua_danh_muc")`; `PageHeader tieuDe="Rà ghi chú KiotViet" moTa="Biến tên trong ô Ghi chú hóa đơn cũ thành khách hàng thật. Hệ thống không tự đoán — bạn quyết từng giá trị."`; link "← Đối tác".

**`ra-ghi-chu.tsx`** (`"use client"`):
- Đầu màn: `Progress` + chữ "Còn **N** giá trị chưa rà / tổng T" (`useDemGhiChu`); N = 0 → `Result status="success"` "Đã rà xong mọi giá trị ghi chú" + link sang danh sách khách (`/doi-tac?loai=KHACH`).
- `Alert type="info"` có thể đóng (nhớ bằng `localStorage` bọc try/catch): "Hóa đơn không ghi tên đã tự gắn vào Khách lẻ. Tên ngắn như NGỌC, TỐT… có thể là khách hoặc người bán — hỏi người biết chuyện nếu chưa chắc, cứ để lại chưa rà."
- `Tabs` "Chưa rà" / "Đã rà"; ô tìm (`Input.Search`); danh sách dạng `Table` `size="small"`, phân trang server 30:
  cột Giá trị ghi chú (giữ xuống dòng: `whitespace-pre-line`, tối đa 3 dòng + nút "xem thêm") · Số hóa đơn (right, đậm) · Khoảng ngày (`DD/MM` → `DD/MM`) · Hóa đơn mẫu (`Tag` mã HD) · Hành động (`<HanhDongGhiChu />`).
  Tab Đã rà: cột Quyết định (`Tag`: Khách → tên khách link `/doi-tac/[id]`; Sale → "Sale: <tên>"; Khách+sale; Bỏ qua) · nút "Hủy quyết định" (`Popconfirm`: "Khách đã tạo không bị xóa").
- Trang hiện tại rỗng mà trang > 1 → về trang 1 (sau khi quyết xong phần tử cuối của trang).

**`hanh-dong-ghi-chu.tsx`** (`"use client"`): props `{ dong: DongGhiChu }`. Hàng nút `Space.Compact` nhỏ:
1. **Tạo khách** → `Popover` form nhỏ: Tên (điền `goiYTenKhach`), SĐT (điền `tachSoDienThoai`, không bắt buộc), nút Lưu → `quyetGhiChu({ loai: "KHACH", taoKhach: { ten } })`. Enter trong ô Tên = Lưu. (SĐT gửi kèm nếu RPC plan 07 nhận `dien_thoai` trong `p_tao_khach`; nếu chưa nhận thì bỏ ô SĐT và ghi vào SUMMARY.)
2. **Gộp vào khách** → `Popover` với `Select showSearch` gọi `timKhach` (debounce 300ms, `filterOption={false}`) → chọn = lưu `loai: "KHACH", doiTacId`.
3. **Là sale** → `Popover` ô Tên sale (điền `goiYTenKhach`) → `loai: "SALE", tenSale`.
4. **Khách + sale** → `Popover` gồm cả Select khách (hoặc chuyển "tạo khách mới") và ô tên sale.
5. **Bỏ qua** → lưu ngay `loai: "BO_QUA"` (không hỏi lại — hủy được ở tab Đã rà).
Mọi nút `loading` khi `isPending` và disable các nút khác của dòng. Lỗi → `message.error(dienGiaiLoi(e).tieuDe + ": " + huongXuLy)`. Thành công → `message.success("Đã lưu: <giá trị rút gọn 30 ký tự>")`.
Trên màn < 768px: hành động gom vào một `Dropdown` "Quyết định…".

Thử tay (SUMMARY): vanphong rà 5 giá trị gồm một tên dài (tạo khách), một tên ngắn (là sale), một "PHƯƠNG BÁN LẺ" (khách + sale), một bỏ qua, rồi hủy một quyết định. Mở khách vừa tạo → tab Giao dịch có hóa đơn KiotViet (khi plan 17 xong). Sau thử: HỦY toàn bộ quyết định thử để người văn phòng thật rà từ đầu — ghi rõ trong SUMMARY (khách đã tạo đặt ngừng hoạt động).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - page chứa `yeuCauQuyen("sua_danh_muc")`
    - `ra-ghi-chu.tsx` chứa `useDemGhiChu`, `Progress`, `"da_ra"`, `whitespace-pre-line`
    - `hanh-dong-ghi-chu.tsx` chứa `"KHACH_VA_SALE"`, `"BO_QUA"`, `goiYTenKhach`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>DLIEU-04 có công cụ cho người văn phòng tự rà</done>
</task>

</tasks>

<verification>
- [ ] `npm run check` xanh; dữ liệu thử đã hoàn tác
</verification>

<success_criteria>
- Người văn phòng rà được ~150 giá trị mà không cần kỹ thuật
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-18-SUMMARY.md`
</output>
