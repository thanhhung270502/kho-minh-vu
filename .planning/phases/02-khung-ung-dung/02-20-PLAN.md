---
phase: 02-khung-ung-dung
plan: 20
type: execute
wave: 8
depends_on: ["02-19", "02-12"]
files_modified:
  - src/features/danh-muc/api/nhap-excel.api.ts
  - src/features/danh-muc/components/nhap-excel.tsx
  - src/features/danh-muc/components/xem-truoc-nhap.tsx
  - src/features/danh-muc/lib/file-loi.ts
  - scripts/kiem-tra-ham-thuan.ts
  - src/features/danh-muc/components/bang-san-pham.tsx
autonomous: true
requirements: [DMUC-06]
user_setup: []
must_haves:
  truths:
    - "Luồng nhập 3 bước: chọn file → xem trước → nạp; không bước nào ghi dữ liệu trước khi bấm Nạp"
    - "Xem trước hiện định dạng nhận ra (Mẫu hệ mới / File KiotViet), số thêm / sửa / không đổi / lỗi"
    - "Dòng lỗi hiện số dòng Excel + cột + lý do; có lỗi thì nút Nạp bị khóa và có nút tải file lỗi"
    - "Dòng sửa hiện từng trường cũ → mới"
    - "Nạp gửi lại chính file, server validate lại; nếu dữ liệu đổi giữa hai bước và sinh lỗi, hiện lỗi mới thay vì nạp"
    - "Nạp xong bảng danh mục làm mới và hiện thông báo số mã đã thêm/sửa"
  artifacts:
    - path: "src/features/danh-muc/components/nhap-excel.tsx"
      provides: "Hộp thoại nhập Excel 3 bước"
  key_links:
    - from: "nhap-excel.api.ts"
      to: "POST /api/danh-muc/nhap-excel"
      via: "FormData { file, che_do }"
---

<objective>
Giao diện nhập danh mục từ Excel (DMUC-06; D-22, D-24..D-26) trên route handler plan 12 và RPC plan 08.
"Không nạp nửa vời" là cam kết người dùng thấy được: lỗi → không nạp gì, sửa file rồi thử lại.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@.planning/phases/02-khung-ung-dung/02-08-PLAN.md
@src/app/api/danh-muc/nhap-excel/route.ts
@src/features/danh-muc/lib/mau-excel.ts
@src/features/danh-muc/components/nut-excel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client nhập + tạo file lỗi phía client</name>
  <files>src/features/danh-muc/api/nhap-excel.api.ts, src/features/danh-muc/lib/file-loi.ts</files>
  <read_first>src/app/api/danh-muc/nhap-excel/route.ts, src/features/danh-muc/lib/mau-excel.ts, src/shared/lib/errors.ts</read_first>
  <action>
**`nhap-excel.api.ts`**: kiểu `KetQuaNhap = { tong: number; them: number; sua: number; khong_doi: number; da_nap: boolean; loi: { dong: number; cot: string; thong_bao: string }[]; thay_doi: { dong: number; ma_hang: string; loai: "THEM" | "SUA"; truong?: Record<string, [unknown, unknown]> }[]; thay_doi_bi_cat?: boolean }`
và `PhanHoiNhap = { dinhDang: "mau_moi" | "kiotviet"; ketQua: KetQuaNhap }`.
Kiểm runtime bằng zod schema tương ứng (dữ liệu từ server là `unknown` tới khi parse).
`guiFileNhap(file: File, cheDo: "kiem_tra" | "nap"): Promise<PhanHoiNhap>` — `fetch("/api/danh-muc/nhap-excel", { method: "POST", body: formData })`;
`!res.ok` → đọc `{ tieuDe, huongXuLy }` → `throw new LoiNhapExcel(tieuDe, huongXuLy, res.status)` (class nhỏ trong file); status 401 → `window.location.assign("/dang-nhap?tiep_tuc=/danh-muc")`.

**`lib/file-loi.ts`**: `taoCsvLoi(loi, tenFileGoc): Blob` — CSV UTF-8 có BOM (`\uFEFF`, Excel mở đúng tiếng Việt), cột `Dòng,Cột,Lỗi`, tên cột hiển thị lấy `COT_MAU` (khóa → tiêu đề), escape dấu phẩy/nháy kép. KHÔNG dùng exceljs ở client (research §Câu hỏi 3). Tên tải về: `<tên gốc bỏ .xlsx>-loi.csv`.
Thêm assert vào `scripts/kiem-tra-ham-thuan.ts` cho escape: lỗi `'Không có đơn vị tính "Thùng", kiểm tra'` → dòng CSV chứa `"Không có đơn vị tính ""Thùng"", kiểm tra"` (đọc Blob bằng `await blob.text()`).
  </action>
  <verify>
    <automated>npx tsx scripts/kiem-tra-ham-thuan.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `nhap-excel.api.ts` chứa `"/api/danh-muc/nhap-excel"`, `che_do`, và schema zod cho phản hồi
    - `file-loi.ts` chứa `\uFEFF` và không import `exceljs`
    - Script hàm thuần xanh
  </acceptance_criteria>
  <done>Client nhập có kiểu an toàn, file lỗi mở đúng bằng Excel</done>
</task>

<task type="auto">
  <name>Task 2: Hộp thoại nhập 3 bước + xem trước</name>
  <files>src/features/danh-muc/components/nhap-excel.tsx, src/features/danh-muc/components/xem-truoc-nhap.tsx, src/features/danh-muc/components/bang-san-pham.tsx</files>
  <read_first>src/features/danh-muc/components/nut-excel.tsx, src/features/danh-muc/components/bang-san-pham.tsx, src/features/danh-muc/api/nhap-excel.api.ts, src/features/danh-muc/api/san-pham.keys.ts, src/features/danh-muc/lib/mau-excel.ts</read_first>
  <action>
**`nhap-excel.tsx`** (`"use client"`): `Modal` rộng, `Steps` 3 bước: "Chọn file" · "Xem trước" · "Kết quả". State `useReducer` (`{ buoc; file; phanHoi; loi; dangGui }`) — nhiều trạng thái liên quan (CLAUDE.md Bước 6).
- Bước 1: `Upload.Dragger` `accept=".xlsx"` `maxCount={1}` `beforeUpload={() => false}` (không tự upload); kiểm client `.xlsx` và ≤ 5MB trước. Chữ hướng dẫn: "Kéo file vào đây — nhận file mẫu hệ mới hoặc file DanhSachSanPham xuất từ KiotViet"; link "Tải file mẫu trống". Chọn xong tự gửi `kiem_tra` (nút "Kiểm tra lại" nếu lỗi mạng).
- Bước 2: `<XemTruocNhap phanHoi={...} tenFile={...} />`; footer: "Chọn file khác" · "Nạp N thay đổi" (primary, `disabled` khi `loi.length > 0` hoặc `them + sua === 0`, `loading` khi gửi). Nạp → `guiFileNhap(file, "nap")`:
  - `ketQua.da_nap` → bước 3; `invalidateQueries({ queryKey: khoaSanPham.tatCa })` + `["lich-su-sua","san_pham"]`.
  - `!da_nap` (server phát hiện lỗi mới) → ở lại bước 2 với phản hồi mới + `Alert warning` "Dữ liệu đã thay đổi từ lúc xem trước — kiểm tra lại các lỗi dưới đây. Chưa có gì được nạp."
- Bước 3: `Result status="success"` "Đã nạp: X mã mới, Y mã sửa" + nút "Đóng" + nút "Xem các mã vừa sửa" (đóng hộp, đặt bộ lọc `sapXep: "updated_at", huong: "desc"`).
- Đang gửi thì không cho đóng modal (`maskClosable`/`closable` false).
- Lỗi `LoiNhapExcel` → `Alert type="error"` tiêu đề + hướng xử lý tại bước hiện tại.

**`xem-truoc-nhap.tsx`**: 
- `Tag` định dạng ("Mẫu hệ mới" xanh / "File KiotViet" cam) + với KiotViet thêm `Alert info` "File KiotViet: công đoạn chỉ cập nhật cho mã suy được từ ô ĐVT (Carbon, Sơn, Xi mạ, Ép, Nano). Mã 'CÁI/CẶP/BỘ' giữ nguyên công đoạn đã rà; mã mới nhận Mua ngoài."
- 4 `Statistic`: Thêm (xanh) · Sửa (xanh dương) · Không đổi (xám) · Lỗi (đỏ).
- `Tabs`: "Lỗi (Z)" (mặc định khi Z > 0) — `Table` Dòng · Cột (tiêu đề từ `COT_MAU`) · Lý do, nút "Tải danh sách lỗi (.csv)" → `taoCsvLoi`; "Sửa (Y)" — mỗi dòng: Dòng · Mã · danh sách `trường: cũ → mới`; "Thêm (X)" — Dòng · Mã. `thay_doi_bi_cat` → ghi "Chỉ hiện 500 thay đổi đầu — toàn bộ vẫn được nạp".
- Bảng cuộn trong khung cao tối đa `60vh`.

Nối: `bang-san-pham.tsx` truyền `onMoNhap` cho `NutExcel` (chỉ khi `quyen.sua`) và render `<NhapExcel open onDong />`.

Thử tay bắt buộc (SUMMARY, research Pitfall 3 — dùng file thật):
1. Nhập `data/kiotviet/DanhSachSanPham_*.xlsx` → xem trước không lỗi styles; ghi số thêm/sửa/không đổi. KHÔNG bấm Nạp nếu số "sửa" lớn bất thường — điều tra trước (có thể là khác biệt tên nhóm/kho giữa file và database).
2. Xuất Excel lọc CARBON (plan 19) → nhập lại chính file → "0 thêm, 0 sửa".
3. Sửa file đó: một ô ĐVT thành "Thùng", một ô Tên đổi → xem trước: 1 lỗi dòng đúng số, 1 sửa; Nạp bị khóa; tải CSV lỗi mở bằng Excel đúng dấu.
4. Xóa dòng lỗi → Nạp → lịch sử sửa của mã hiện nguồn "Nhập Excel". Hoàn tác tên đã đổi bằng form sau khi thử.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `nhap-excel.tsx` chứa `Upload.Dragger`, `beforeUpload`, `"kiem_tra"`, `"nap"`, `useReducer`, chuỗi `Chưa có gì được nạp`
    - `xem-truoc-nhap.tsx` chứa `taoCsvLoi` và `thay_doi_bi_cat`
    - `npm run check` exit 0
    - SUMMARY ghi kết quả 4 bước thử tay
  </acceptance_criteria>
  <done>DMUC-06 hoàn chỉnh, đã thử trên file KiotViet thật</done>
</task>

</tasks>

<verification>
- [ ] `npm run check` xanh; 4 bước thử tay ghi SUMMARY
</verification>

<success_criteria>
- Import không bao giờ nạp nửa vời, người dùng thấy trước mọi thay đổi
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-20-SUMMARY.md`
</output>
