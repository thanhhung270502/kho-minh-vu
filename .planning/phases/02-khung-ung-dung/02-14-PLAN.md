---
phase: 02-khung-ung-dung
plan: 14
type: execute
wave: 7
depends_on: ["02-10", "02-15"]
files_modified:
  - src/app/(app)/cai-dat/nguoi-dung/page.tsx
  - src/features/cai-dat/api/nguoi-dung.api.ts
  - src/features/cai-dat/components/bang-nguoi-dung.tsx
  - src/features/cai-dat/components/ngan-keo-nguoi-dung.tsx
  - src/features/cai-dat/components/hop-dat-lai-mat-khau.tsx
autonomous: true
requirements: [CDAT-01]
user_setup: []
must_haves:
  truths:
    - "Quản lý thấy danh sách tài khoản: họ tên, tên đăng nhập, vai trò, kho, trạng thái, cần đổi mật khẩu"
    - "Quản lý tạo tài khoản với mật khẩu tạm; thủ kho bắt buộc chọn ít nhất một kho, chọn được nhiều kho"
    - "Quản lý đổi vai trò/kho, vô hiệu hóa/mở lại, đặt lại mật khẩu; mỗi thao tác nhạy cảm có xác nhận"
    - "Sau khi đổi vai trò/kho/vô hiệu hóa, màn hình nói rõ độ trễ hiệu lực"
    - "Lỗi server (tên đăng nhập trùng, quản lý cuối cùng) hiện đúng ô hoặc đầu form"
    - "Văn phòng mở /cai-dat/nguoi-dung bị chuyển trang Không đủ quyền"
  artifacts:
    - path: "src/features/cai-dat/components/ngan-keo-nguoi-dung.tsx"
      provides: "Form tạo/sửa tài khoản gọi Server Actions plan 10"
  key_links:
    - from: "ngan-keo-nguoi-dung.tsx"
      to: "src/features/cai-dat/actions/nguoi-dung.actions.ts"
      via: "taoNguoiDung / capNhatNguoiDung"
---

<objective>
Màn Cài đặt → Người dùng (CDAT-01; D-02..D-06) trên lớp server của plan 10. Đối ứng rủi ro "người dùng
tự quản lý tài khoản": luồng ngắn, chữ rõ, không cần hiểu Supabase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@.planning/phases/02-khung-ung-dung/02-10-SUMMARY.md
@src/features/cai-dat/actions/nguoi-dung.actions.ts
@src/features/cai-dat/schemas/nguoi-dung.schema.ts
@src/shared/components/ngan-keo-form.tsx
@src/features/cai-dat/components/danh-muc-phu.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Đọc danh sách tài khoản + bảng</name>
  <files>src/features/cai-dat/api/nguoi-dung.api.ts, src/features/cai-dat/components/bang-nguoi-dung.tsx, src/app/(app)/cai-dat/nguoi-dung/page.tsx</files>
  <read_first>src/types/database.types.ts, src/shared/components/query-state.tsx, src/shared/lib/quyen.ts, src/features/cai-dat/components/danh-muc-phu.tsx</read_first>
  <action>
**`nguoi-dung.api.ts`**: `layDanhSachNguoiDung()` →
`from("nguoi_dung").select("id, ho_ten, ten_dang_nhap, vai_tro, dang_hoat_dong, phai_doi_mat_khau, updated_at, nguoi_dung_kho(kho_id, kho:kho_id(ma, ten))").order("ho_ten")`
(RLS: quản lý đọc mọi dòng). Kiểu suy từ kết quả query (`QueryData`/`Awaited<ReturnType>`), không viết tay.
Key `["nguoi-dung"]`. `layKhoHoatDong()` → `from("kho").select("id, ma, ten").eq("dang_hoat_dong", true)`.

**page** (Server): `yeuCauQuyen("cai_dat_nguoi_dung")` → `<BangNguoiDung nguoiDungHienTaiId={nd.id} />`.

**`bang-nguoi-dung.tsx`** (`"use client"`): `QueryState` + `Table` `rowKey="id"`, cuộn ngang trong khung.
Cột: Họ tên (kèm `Tag` "Bạn" nếu là chính mình), Tên đăng nhập (font mono), Vai trò (`NHAN_VAI_TRO`),
Kho (danh sách `Tag` tên kho; vai trò khác thủ kho hiện "Tất cả kho" màu xám), Trạng thái
(`Badge status="success"` "Đang hoạt động" / `"default"` "Đã vô hiệu hóa"; thêm `Tag orange` "Chờ đổi mật khẩu"
khi `phai_doi_mat_khau`), Thao tác (`Dropdown`: Sửa · Đặt lại mật khẩu · Vô hiệu hóa/Mở lại).
Nút "Thêm tài khoản" góc phải. Bộ lọc nhanh `Segmented`: Đang hoạt động / Đã vô hiệu hóa / Tất cả (lọc client — vài chục dòng).
Rỗng: "Chưa có tài khoản nào khác. Bấm Thêm tài khoản để cấp cho nhân viên."
Vô hiệu hóa/Mở lại: `Modal.confirm` (qua `App.useApp().modal`) nội dung "Nhân viên sẽ bị đăng xuất và không đăng nhập lại được cho tới khi mở lại. Thao tác đang dở của họ có thể bị từ chối." → gọi `doiTrangThaiNguoiDung` qua `useTransition`; kết quả `ok:false` → `message.error(thongBao)`; thành công → `invalidateQueries(["nguoi-dung"])`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - page chứa `yeuCauQuyen("cai_dat_nguoi_dung")`
    - `nguoi-dung.api.ts` chứa `nguoi_dung_kho(kho_id`
    - `bang-nguoi-dung.tsx` chứa `doiTrangThaiNguoiDung` và `modal.confirm` (hoặc `modal.confirm(`)
    - `npm run typecheck` exit 0
  </acceptance_criteria>
  <done>Quản lý xem được toàn bộ tài khoản và khóa/mở tài khoản</done>
</task>

<task type="auto">
  <name>Task 2: Ngăn kéo tạo/sửa tài khoản + hộp đặt lại mật khẩu</name>
  <files>src/features/cai-dat/components/ngan-keo-nguoi-dung.tsx, src/features/cai-dat/components/hop-dat-lai-mat-khau.tsx, src/features/cai-dat/components/bang-nguoi-dung.tsx</files>
  <read_first>src/features/cai-dat/actions/nguoi-dung.actions.ts, src/features/cai-dat/schemas/nguoi-dung.schema.ts, src/shared/components/ngan-keo-form.tsx, src/features/doi-tac/components/ngan-keo-doi-tac.tsx</read_first>
  <action>
**`ngan-keo-nguoi-dung.tsx`**: props `{ open; onDong; nguoiDung: DongNguoiDung | null }` (null = tạo mới).
RHF + `zodResolver(nguoiDung ? capNhatNguoiDungSchema : taoNguoiDungSchema)`.
Trường:
- Họ tên.
- Tên đăng nhập (chỉ khi tạo; khi sửa hiện chữ tĩnh). Dưới ô: `Typography.Text type="secondary"` "Nhân viên gõ đúng tên này khi đăng nhập" + xem trước giá trị sau chuẩn hóa (`chuanHoaTenDangNhap`).
- Vai trò: `Radio.Group` dạng dọc, mỗi lựa chọn kèm một dòng mô tả: Quản lý — "Toàn quyền, kể cả Cài đặt và giá bán"; Văn phòng — "Sửa danh mục, đối tác, xem giá vốn"; Thủ kho — "Chỉ kho được gán, không xem giá vốn"; Chỉ xem — "Xem, không tạo hay sửa gì".
- Kho: `Checkbox.Group` các kho hoạt động, CHỈ hiện khi vai trò = thủ kho.
- Mật khẩu tạm (chỉ khi tạo): `Input.Password` + nút "Tạo ngẫu nhiên" (10 ký tự chữ+số từ `crypto.getRandomValues`) + nút sao chép (`navigator.clipboard.writeText`). Ghi chú "Đưa mật khẩu này tận tay nhân viên. Họ phải đổi ở lần đăng nhập đầu."
Sửa: `reset({ id, hoTen, vaiTro, khoIds })` từ dòng bảng.
Lưu qua `useTransition` gọi Server Action; `ok:false` có `truong` → `setError(truong, ...)`, không có → `setError("root", ...)` (`Alert` đầu form). Thành công:
- Tạo: `message.success("Đã tạo tài khoản <ten_dang_nhap>")`.
- Sửa có đổi vai trò hoặc kho: `notification.info({ message: "Đã lưu", description: "Quyền bị thu hẹp có hiệu lực ngay. Quyền được mở rộng có hiệu lực khi nhân viên tải lại trang hoặc trong tối đa 60 phút." })`.
`invalidateQueries(["nguoi-dung"])`, `onDong()`. `NganKeoForm dangLuu={isPending}`.

**`hop-dat-lai-mat-khau.tsx`**: `Modal` với một ô mật khẩu tạm (nút tạo ngẫu nhiên + sao chép như trên) → `datLaiMatKhau`; thành công hiện lại mật khẩu vừa đặt trong `Typography.Paragraph copyable` và dòng "Nhân viên đã bị đăng xuất khỏi mọi thiết bị".

Nối vào `bang-nguoi-dung.tsx` (state mở ngăn kéo/hộp thoại). Mỗi file ≤ ~200 dòng — tách `o-mat-khau-tam.tsx` nếu cần (dùng chung cho ngăn kéo và hộp thoại).

Thử tay (ghi SUMMARY): quanly tạo `test.uat` thủ kho K2 → đăng nhập ẩn danh `test.uat` → bị buộc đổi mật khẩu → chỉ thấy tồn K2 (nếu có) → quản lý bỏ K2 thêm K1 → phiên ẩn danh tải lại thấy đúng K1 → vô hiệu hóa → phiên ẩn danh thao tác kế tiếp bị đẩy về đăng nhập với thông báo vô hiệu hóa. Cuối cùng để `test.uat` ở trạng thái vô hiệu hóa.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `ngan-keo-nguoi-dung.tsx` chứa `taoNguoiDung`, `capNhatNguoiDung`, `crypto.getRandomValues`, `Checkbox.Group`, chuỗi `tối đa 60 phút`
    - `hop-dat-lai-mat-khau.tsx` chứa `datLaiMatKhau` và `copyable`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>CDAT-01 hoàn chỉnh; luồng D-05 đã thử tay</done>
</task>

</tasks>

<verification>
- [ ] `npm run check` xanh; luồng thử tay ghi SUMMARY
</verification>

<success_criteria>
- Quản lý tự quản trị tài khoản không cần vào Supabase Dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-14-SUMMARY.md`
</output>
