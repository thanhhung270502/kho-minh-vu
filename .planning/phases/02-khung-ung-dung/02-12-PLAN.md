---
phase: 02-khung-ung-dung
plan: 12
type: execute
wave: 5
depends_on: ["02-09"]
files_modified:
  - src/shared/lib/o-excel.ts
  - src/shared/lib/tach-dvt-cong-doan.ts
  - scripts/import-kiotviet/doc-file.ts
  - scripts/import-kiotviet/tach-dvt-cong-doan.ts
  - src/features/danh-muc/lib/mau-excel.ts
  - src/features/danh-muc/lib/doc-file-danh-muc.server.ts
  - scripts/kiem-tra-doc-excel.ts
  - src/app/api/danh-muc/nhap-excel/route.ts
  - src/app/api/danh-muc/mau-excel/route.ts
autonomous: true
requirements: [DMUC-06, DMUC-07]
user_setup: []
must_haves:
  truths:
    - "Một nguồn duy nhất cho đọc ô Excel và tách ĐVT/công đoạn, dùng chung bởi script Phase 1 và ứng dụng"
    - "Đọc file DanhSachSanPham KiotViet thật không crash và nhận diện đúng định dạng KiotViet"
    - "File mẫu hệ mới xuất ra đọc lại được, nhận diện đúng định dạng mẫu mới, quay vòng không mất cột"
    - "Route nhập nhận multipart, giới hạn 5MB và .xlsx, parse ở server, gọi nhap_danh_muc bằng phiên người dùng"
    - "taoFileMau sinh file mẫu hệ mới (có/không cột Giá vốn) — plan 19 dùng cho route xuất theo bộ lọc"
    - "Import file KiotViet không ghi đè công đoạn đã rà (gửi cong_doan null + cong_doan_khi_tao_moi)"
  artifacts:
    - path: "src/features/danh-muc/lib/mau-excel.ts"
      provides: "COT_MAU — định nghĩa cột mẫu hệ mới dùng chung cho xuất và nhập"
    - path: "src/features/danh-muc/lib/doc-file-danh-muc.server.ts"
      provides: "docFileDanhMuc(buffer) → { dinhDang, dong: DongNhap[] }"
    - path: "src/app/api/danh-muc/nhap-excel/route.ts"
      provides: "POST kiểm tra / nạp"
  key_links:
    - from: "src/app/api/danh-muc/nhap-excel/route.ts"
      to: "public.nhap_danh_muc"
      via: "(await createSupabaseServerClient()).rpc('nhap_danh_muc', ...)"
---

<objective>
Excel hai chiều cho danh mục (D-22, D-23, D-25): đọc ở **server** (research §Câu hỏi 3 — stream reader
`styles: "ignore"` chỉ chạy trên Node; file KiotViet crash reader thường), dùng chung logic với script
Phase 1, xuất theo bộ lọc.

Quyết định: giới hạn upload 5MB (file danh mục thật ~vài trăm KB; nền tảng deploy chưa chốt — research
Open Question 1). Thông báo quá cỡ không phụ thuộc nền tảng: "File lớn hơn 5MB — chia nhỏ file rồi nhập
từng phần".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@.planning/phases/02-khung-ung-dung/02-08-PLAN.md
@scripts/import-kiotviet/doc-file.ts
@scripts/import-kiotviet/tach-dvt-cong-doan.ts
@scripts/import-kiotviet/kiem-tra.ts
@src/shared/lib/chuan-hoa.ts
@.memory/knowledge/du-lieu-kiotviet.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chuyển logic đọc ô + tách ĐVT về src/shared, script Phase 1 re-export</name>
  <files>src/shared/lib/o-excel.ts, src/shared/lib/tach-dvt-cong-doan.ts, scripts/import-kiotviet/doc-file.ts, scripts/import-kiotviet/tach-dvt-cong-doan.ts</files>
  <read_first>scripts/import-kiotviet/doc-file.ts, scripts/import-kiotviet/tach-dvt-cong-doan.ts, scripts/import-kiotviet/kiem-tra.ts, scripts/import-kiotviet/nap-du-lieu.ts, src/shared/lib/chuan-hoa.ts</read_first>
  <action>
1. Tạo `src/shared/lib/tach-dvt-cong-doan.ts`: chép NGUYÊN nội dung `scripts/import-kiotviet/tach-dvt-cong-doan.ts`
   (giữ comment, `QUY_DOI_CAP`, `chuanHoa`, `tachDvtCongDoan`, `KetQuaTach`). Trong `chuanHoa` thay đoạn
   bỏ dấu bằng `boDau(v)` từ `./chuan-hoa` (hành vi giống hệt). Import tương đối (`./chuan-hoa`) để `tsx` chạy được.
2. Tạo `src/shared/lib/o-excel.ts`: chuyển `doSo`, `doChuoi`, `doNgayExcel`, `chuanHoaTenCot`,
   `TU_KHOA_DONG_TONG`, kiểu `DongTho` từ `doc-file.ts`; thêm hàm đọc từ nguồn bất kỳ:
```ts
import ExcelJS from "exceljs";
import { Readable } from "node:stream";
/** Đọc sheet đầu. Nhận đường dẫn (script) hoặc Buffer (Route Handler). BẮT BUỘC stream + styles: "ignore" — xem bẫy trong comment gốc. */
export async function docSheetDau(nguon: string | Buffer): Promise<{ tenCotGoc: string[]; dong: DongTho[] }> {
  const reader = new ExcelJS.stream.xlsx.WorkbookReader(
    typeof nguon === "string" ? nguon : Readable.from(nguon),
    { sharedStrings: "cache", hyperlinks: "ignore", styles: "ignore", worksheets: "emit" },
  );
  // ... thân vòng lặp giữ nguyên docSheet cũ, thêm trả tenCotGoc (chuỗi header gốc chưa chuẩn hóa)
}
```
   File này dùng `node:stream` → CHỈ import từ code server (ghi comment đầu file: "Server/Node only. Không import vào Client Component.").
3. `scripts/import-kiotviet/tach-dvt-cong-doan.ts` → chỉ còn `export * from "../../src/shared/lib/tach-dvt-cong-doan";` + comment trỏ nguồn.
4. `scripts/import-kiotviet/doc-file.ts` → re-export `doSo, doChuoi, doNgayExcel, type DongTho` từ `../../src/shared/lib/o-excel`;
   giữ hàm `docSheet(duongDan)` (kiểm `existsSync` + thông báo cũ) gọi `docSheetDau(duongDan)` và trả `.dong`.
5. Không đổi hành vi script Phase 1: `npm run import:kiotviet` (mặc định `--dry-run`, KHÔNG ghi) phải in
   cùng số dòng hợp lệ như trước (3.266 sản phẩm). Ghi output tóm tắt vào SUMMARY.
  </action>
  <verify>
    <automated>npm run import:kiotviet && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/import-kiotviet/tach-dvt-cong-doan.ts` chứa `export * from "../../src/shared/lib/tach-dvt-cong-doan"` và không còn `const CONG_DOAN = new Map`
    - `src/shared/lib/o-excel.ts` chứa `styles: "ignore"` và `Readable.from(nguon)`
    - Dry-run in 3266 sản phẩm hợp lệ, không ghi database (không có cờ `--ghi`)
    - `npm run typecheck` exit 0
  </acceptance_criteria>
  <done>Một nguồn logic Excel, script cũ vẫn chạy y hệt</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mẫu cột + nhận diện hai định dạng (kiểm trên file KiotViet thật trước)</name>
  <files>scripts/kiem-tra-doc-excel.ts, src/features/danh-muc/lib/mau-excel.ts, src/features/danh-muc/lib/doc-file-danh-muc.server.ts</files>
  <read_first>scripts/import-kiotviet/kiem-tra.ts, src/shared/lib/o-excel.ts, src/shared/lib/tach-dvt-cong-doan.ts, .planning/phases/02-khung-ung-dung/02-08-PLAN.md</read_first>
  <action>
**Script kiểm trước — `scripts/kiem-tra-doc-excel.ts`** (`node:assert`, import tương đối):
```ts
const file = readdirSync("data/kiotviet").find((f) => f.startsWith("DanhSachSanPham"));
assert.ok(file, "Cần data/kiotviet/DanhSachSanPham*.xlsx (dữ liệu thật, không commit)");
const kv = await docFileDanhMuc(readFileSync(join("data/kiotviet", file)));
assert.equal(kv.dinhDang, "kiotviet");
assert.equal(kv.dong.length, 3266);
const carbon = kv.dong.find((d) => d.ma_hang?.endsWith("-CB") && d.cong_doan === "CARBON");
assert.ok(carbon, "có dòng suy được CARBON");
const cai = kv.dong.find((d) => d.cong_doan === null);
assert.ok(cai && cai.cong_doan_khi_tao_moi === "MUA_NGOAI", "dòng CÁI không suy được gửi cong_doan null");
assert.ok(kv.dong.every((d) => d.gia_ban === null), "giá bán 0 của KiotViet không gửi");
assert.ok(kv.dong.every((d) => d.dong >= 2), "số dòng Excel thật");
// Quay vòng mẫu mới
const buf = await taoFileMau(kv.dong.slice(0, 5).map(dongNhapThanhDongXuat), { coGiaVon: false });
const mm = await docFileDanhMuc(buf);
assert.equal(mm.dinhDang, "mau_moi");
assert.deepEqual(mm.dong.map((d) => d.ma_hang), kv.dong.slice(0, 5).map((d) => d.ma_hang));
await assert.rejects(docFileDanhMuc(Buffer.from("khong phai xlsx")), /không đọc được/i);
console.log("✓ đọc Excel: KiotViet thật + mẫu mới quay vòng");
```

**`src/features/danh-muc/lib/mau-excel.ts`** (không import Node — dùng được cả client để hiện tên cột):
```ts
export type KhoaCot = "ma_hang" | "ten_hang" | "nhom_hang" | "dvt" | "cong_doan" | "quy_doi" | "kho_mac_dinh"
  | "ton_toi_thieu" | "ton_toi_da" | "gia_ban" | "dang_kinh_doanh" | "barcode" | "ghi_chu" | "gia_von" | "tong_ton";
export const COT_MAU: ReadonlyArray<{ khoa: KhoaCot; tieuDe: string; rong: number; chiXuat?: boolean }> = [
  { khoa: "ma_hang", tieuDe: "Mã hàng", rong: 22 },
  { khoa: "ten_hang", tieuDe: "Tên hàng", rong: 48 },
  { khoa: "nhom_hang", tieuDe: "Nhóm hàng", rong: 28 },
  { khoa: "dvt", tieuDe: "Đơn vị tính", rong: 12 },
  { khoa: "cong_doan", tieuDe: "Công đoạn", rong: 14 },
  { khoa: "quy_doi", tieuDe: "Quy đổi", rong: 10 },
  { khoa: "kho_mac_dinh", tieuDe: "Kho mặc định", rong: 14 },
  { khoa: "ton_toi_thieu", tieuDe: "Tồn tối thiểu", rong: 14 },
  { khoa: "ton_toi_da", tieuDe: "Tồn tối đa", rong: 12 },
  { khoa: "gia_ban", tieuDe: "Giá bán", rong: 14 },
  { khoa: "dang_kinh_doanh", tieuDe: "Đang kinh doanh", rong: 16 },
  { khoa: "barcode", tieuDe: "Barcode", rong: 16 },
  { khoa: "ghi_chu", tieuDe: "Ghi chú", rong: 30 },
  { khoa: "tong_ton", tieuDe: "Tồn hiện tại", rong: 12, chiXuat: true },
  { khoa: "gia_von", tieuDe: "Giá vốn", rong: 14, chiXuat: true },
];
export type DongNhap = { dong: number; ma_hang: string | null; ten_hang: string | null; nhom_hang: string | null;
  dvt: string | null; cong_doan: string | null; cong_doan_khi_tao_moi?: string | null; quy_doi: number | null;
  kho_mac_dinh: string | null; ton_toi_thieu: number | null; ton_toi_da: number | null; gia_ban: number | null;
  dang_kinh_doanh: boolean | null; barcode: string | null; ghi_chu: string | null };
```
`DongNhap` khớp đúng hợp đồng jsonb của `nhap_danh_muc` (plan 08) — tên khóa snake_case.

**`src/features/danh-muc/lib/doc-file-danh-muc.server.ts`** — KHÔNG `import "server-only"`: gói đó ném lỗi khi chạy ngoài điều kiện `react-server`, nên script `tsx` kiểm ở trên sẽ không import được. Hậu tố `.server.ts` + import `node:stream` (qua `o-excel.ts`) đã đủ làm build client hỏng nếu ai import nhầm; ghi comment đầu file nói rõ lý do:
- `docFileDanhMuc(buf: Buffer): Promise<{ dinhDang: "mau_moi" | "kiotviet"; dong: DongNhap[] }>`.
- Bọc `docSheetDau` trong try/catch → `throw new Error("Không đọc được file Excel. Kiểm tra file .xlsx còn mở được bằng Excel.")`.
- Nhận diện: header chuẩn hóa (`chuanHoaTenCot`) chứa `ma_hang` + `don_vi_tinh` + `cong_doan` → `mau_moi`;
  chứa `ma_hang` + `dvt` + `nhom_hang_3_cap` → `kiotviet`; khác → `throw new Error("Không nhận ra mẫu file. Cần cột: Mã hàng, Tên hàng, Đơn vị tính, Công đoạn — hoặc file DanhSachSanPham xuất từ KiotViet.")`.
- Mẫu mới: map theo `COT_MAU` (bỏ `chiXuat`), ô trống → `null`; `dang_kinh_doanh`: `có/co/1/x/true` → true, `không/khong/0/false` → false, trống → null.
- KiotViet (ánh xạ cột từ `scripts/import-kiotviet/kiem-tra.ts`): `ma_hang`←`ma_hang`, `ten_hang`←`ten_hang`,
  `nhom_hang`←`nhom_hang_3_cap`, `tach = tachDvtCongDoan(dvt)`: `dvt`←`tach.maDvt`,
  `cong_doan`←`tach.suyDuoc ? tach.maCongDoan : null`, `cong_doan_khi_tao_moi`←`"MUA_NGOAI"`,
  `quy_doi`←`doSo(quy_doi) ?? 1`, `kho_mac_dinh`←`vi_tri` (cột Vị trí là TÊN KHO), `ton_toi_thieu`←`ton_nho_nhat`,
  `ton_toi_da`← `ton_lon_nhat` (≥ 999999999 → null), `gia_ban`← `null` nếu 0, `dang_kinh_doanh`←`dang_kinh_doanh !== "0"`,
  `ghi_chu`←`mo_ta`, `barcode`← null. KHÔNG đọc cột giá vốn, tồn kho.
- Export thêm `taoFileMau(dong: DongXuat[], { coGiaVon }): Promise<Buffer>` (exceljs `Workbook` thường — ghi file không gặp lỗi styles):
  sheet "Danh mục", header in đậm + freeze hàng 1 + autoFilter, độ rộng cột từ `COT_MAU`, bỏ cột `gia_von` khi `!coGiaVon`,
  sheet thứ hai "Hướng dẫn" liệt kê: ô trống = giữ nguyên; tên nhóm/ĐVT/công đoạn/kho phải đã có trong Cài đặt;
  cột Tồn hiện tại và Giá vốn chỉ để xem, không nhập.
- `dongNhapThanhDongXuat` và kiểu `DongXuat` (DongNhap + `tong_ton`, `gia_von`) để script quay vòng.
  </action>
  <verify>
    <automated>npx tsx scripts/kiem-tra-doc-excel.ts</automated>
  </verify>
  <acceptance_criteria>
    - Script in `✓ đọc Excel: KiotViet thật + mẫu mới quay vòng`
    - `doc-file-danh-muc.server.ts` chứa `"kiotviet"`, `"mau_moi"`, `cong_doan_khi_tao_moi` và KHÔNG chứa `import "server-only"`
    - `mau-excel.ts` không import `node:` hay `exceljs`
  </acceptance_criteria>
  <done>Hai định dạng đọc đúng trên dữ liệu thật, không ghi đè công đoạn đã rà</done>
</task>

<task type="auto">
  <name>Task 3: Route handler nhập (kiểm tra/nạp) và tải mẫu trống</name>
  <files>src/app/api/danh-muc/nhap-excel/route.ts, src/app/api/danh-muc/mau-excel/route.ts</files>
  <read_first>src/lib/supabase/server.ts, src/features/xac-thuc/api/nguoi-dung-hien-tai.server.ts, src/features/danh-muc/lib/doc-file-danh-muc.server.ts, src/shared/lib/errors.ts, src/shared/lib/quyen.ts</read_first>
  <action>
Mọi route: `export const runtime = "nodejs";` Không `console.log`. Lỗi trả JSON `{ loai, tieuDe, huongXuLy }` từ `dienGiaiLoi`.

**`POST /api/danh-muc/nhap-excel`** — `formData()`: `file` (File), `che_do` (`kiem_tra` | `nap`).
1. `layNguoiDungHienTai()`; null → 401; `!coQuyen(vaiTro, "sua_danh_muc")` → 403 JSON "Chỉ quản lý và văn phòng nhập được danh mục".
2. Không có file / không đuôi `.xlsx` → 400; `file.size > 5 * 1024 * 1024` → 413 "File lớn hơn 5MB — chia nhỏ file rồi nhập từng phần".
3. `docFileDanhMuc(Buffer.from(await file.arrayBuffer()))`; lỗi đọc → 422 với message.
4. `const { data, error } = await (await createSupabaseServerClient()).rpc("nhap_danh_muc", { p_dong: dong as unknown as Json, p_chi_kiem_tra: che_do !== "nap" })` (kiểu `Json` từ database.types). `error` → status theo `dienGiaiLoi(error).loai` (khong-du-quyen 403, du-lieu-khong-hop-le 422, còn lại 500).
5. Trả `{ dinhDang, ketQua: data }`. Nạp lại gửi lại CHÍNH file — server parse và validate lại từ đầu (D-25), giao diện không gửi JSON đã xem trước.

Route xuất theo bộ lọc (`/api/danh-muc/xuat-excel`) nằm ở plan 19 vì cần `docBoLocTuUrl` của plan 11 (cùng wave).

**`GET /api/danh-muc/mau-excel`** — cần đăng nhập; trả `taoFileMau([], { coGiaVon: false })` tên `mau-nhap-danh-muc.xlsx`.

Thử tay bằng curl có cookie phiên không thực tế → kiểm bằng `npm run dev` + trình duyệt ở plan 19/20. Ở plan
này verify bằng `npm run check` và gọi `curl -i localhost:3000/api/danh-muc/mau-excel` KHÔNG cookie → phải 401 JSON (proxy plan 05).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - Hai file route tồn tại, mỗi file chứa `export const runtime = "nodejs"`
    - `nhap-excel/route.ts` chứa `5 * 1024 * 1024`, `rpc("nhap_danh_muc"`, `coQuyen(`
    - `mau-excel/route.ts` chứa `Content-Disposition`
    - `npm run check` exit 0; curl không cookie tới `/api/danh-muc/mau-excel` trả `401`
  </acceptance_criteria>
  <done>Server nhập Excel hoàn chỉnh cho plan 20; taoFileMau sẵn cho plan 19</done>
</task>

</tasks>

<verification>
- [ ] Dry-run script Phase 1 không đổi số liệu
- [ ] Script đọc Excel xanh trên file thật
- [ ] `npm run check` xanh
</verification>

<success_criteria>
- DMUC-06/07 có server hoàn chỉnh, đã thử trên dữ liệu thật
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-12-SUMMARY.md`
</output>
