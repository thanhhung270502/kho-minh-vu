---
phase: 02-khung-ung-dung
plan: 19
type: execute
wave: 7
depends_on: ["02-16", "02-12"]
files_modified:
  - src/features/danh-muc/components/o-sua-nhanh.tsx
  - src/features/danh-muc/components/thanh-gan-hang-loat.tsx
  - src/features/danh-muc/components/bang-san-pham.tsx
  - src/features/danh-muc/components/cot-san-pham.tsx
  - src/features/danh-muc/components/goi-y-cong-doan.tsx
  - src/app/api/danh-muc/xuat-excel/route.ts
  - src/features/danh-muc/components/nut-excel.tsx
autonomous: true
requirements: [DMUC-04, DMUC-07]
user_setup: []
must_haves:
  truths:
    - "Bấm ô Công đoạn / Nhóm hàng / ĐVT trên bảng mở Select sửa tại chỗ, chọn là lưu (nguồn sua_o), lỗi hoàn tác giá trị hiển thị"
    - "Tick chọn nhiều dòng (kể cả chọn cả trang) rồi gán cùng công đoạn / nhóm / ĐVT / trạng thái kinh doanh, hoặc Xác nhận đã rà"
    - "Bộ lọc nhanh 'Cần rà' có số đếm và giảm dần sau khi gán/xác nhận"
    - "Hộp Gợi ý công đoạn theo đuôi mã liệt kê 145 mã nhóm theo công đoạn đề xuất, bỏ tick dòng sai, áp dụng một lần"
    - "Nút Xuất Excel tải toàn bộ kết quả đang lọc; nút Tải mẫu tải file trống"
    - "Thủ kho/chỉ xem không thấy checkbox, sửa ô, gợi ý; vẫn xuất Excel được (không cột giá vốn)"
  artifacts:
    - path: "src/app/api/danh-muc/xuat-excel/route.ts"
      provides: "GET xuất theo bộ lọc"
    - path: "src/features/danh-muc/components/goi-y-cong-doan.tsx"
      provides: "Hộp gợi ý công đoạn"
  key_links:
    - from: "o-sua-nhanh.tsx"
      to: "rpc gan_hang_loat"
      via: "ganHangLoat([id], { cong_doan_id }, 'sua_o')"
---

<objective>
Bốn công cụ rà dữ liệu (D-18, D-19) và Excel xuất (DMUC-07, D-23) trên bảng danh mục. Đối ứng rủi ro
"rà 356 mã chỉ bằng form từng mã".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-khung-ung-dung/02-CONTEXT.md
@.planning/phases/02-khung-ung-dung/02-RESEARCH.md
@src/features/danh-muc/components/bang-san-pham.tsx
@src/features/danh-muc/components/cot-san-pham.tsx
@src/features/danh-muc/hooks/useSanPham.ts
@src/features/danh-muc/lib/doc-file-danh-muc.server.ts
@src/features/danh-muc/schemas/bo-loc.schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sửa ô tại chỗ + chọn nhiều gán hàng loạt + lọc nhanh Cần rà</name>
  <files>src/features/danh-muc/components/o-sua-nhanh.tsx, src/features/danh-muc/components/thanh-gan-hang-loat.tsx, src/features/danh-muc/components/bang-san-pham.tsx, src/features/danh-muc/components/cot-san-pham.tsx</files>
  <read_first>src/features/danh-muc/components/bang-san-pham.tsx, src/features/danh-muc/components/cot-san-pham.tsx, src/features/danh-muc/components/thanh-loc-san-pham.tsx, src/features/danh-muc/hooks/useSanPham.ts, node_modules/antd/es/table/interface.d.ts</read_first>
  <action>
**`o-sua-nhanh.tsx`**: props `{ sanPhamId; truong: "cong_doan_id" | "nhom_hang_id" | "dvt_id"; giaTri: string | null; nhan: ReactNode; tuyChon: { value: string; label: ReactNode }[]; choPhep: boolean }`.
Không `choPhep` → chỉ hiện `nhan`. Có: hiện `nhan` với con trỏ tay + viền đứt khi hover; click → `Select` `open autoFocus size="small" showSearch` rộng 180; `onChange` → `useGanHangLoat().mutate({ ids: [sanPhamId], thayDoi: { [truong]: v }, nguon: "sua_o" })`; `onBlur`/Esc → đóng không lưu. Cập nhật lạc quan (TRANSACTION_RULES §13): `onMutate` sửa cache `khoaSanPham.danhSach(*)` tại dòng đó (lưu snapshot), `onError` khôi phục snapshot + `message.error`, `onSettled` invalidate im lặng. Nhóm hàng cho phép chọn "(không nhóm)" gửi `""` → RPC `nullif` về null.

**`cot-san-pham.tsx`**: cột Nhóm/ĐVT/Công đoạn render bằng `OSuaNhanh` (choPhep = quyền sửa).

**`thanh-gan-hang-loat.tsx`**: props `{ ids: string[]; onXong: () => void; danhMucPhu }`. Hiện khi `ids.length > 0` dạng thanh dính dưới thanh lọc (`sticky top-0 z-10` nền xanh nhạt): "Đã chọn **N** mã" · `Select` "Gán công đoạn" · "Gán nhóm hàng" · "Gán ĐVT" · `Dropdown` "Trạng thái" (Đang kinh doanh / Ngừng kinh doanh) · nút "Xác nhận đã rà" · nút "Bỏ chọn".
Mỗi hành động: `modal.confirm` "Gán công đoạn <tên> cho N mã?" (N > 20 thì thêm dòng "Thao tác ghi vào nhật ký sửa của từng mã") → `ganHangLoat(ids, ..., "hang_loat")` hoặc `xacNhanDaRa(ids)` → `message.success("Đã cập nhật N mã")` → `onXong()` (bỏ chọn) → invalidate.
N > 1000 → chặn với thông báo "Chọn tối đa 1000 mã mỗi lần".

**`bang-san-pham.tsx`**:
- Khi `quyen.sua`: `rowSelection={{ selectedRowKeys, onChange: (keys) => setChon(keys as string[]), preserveSelectedRowKeys: true }}` (antd v6: dùng `onChange`, không `onSelectAll` — research §antd v6). Đổi bộ lọc → bỏ chọn.
- Thanh lọc: thêm nút bật/tắt `Button` "Cần rà" (type primary khi bật) kèm `Badge count` = số mã cần rà — query riêng `useDanhSachSanPham({ ...BO_LOC_MAC_DINH, canRa: true, kichThuoc: 10 })` lấy `tong` (không thêm RPC mới).
- Khi `boLoc.canRa`: `Alert type="warning"` gọn trên bảng: "Mã mua ngoài chưa rõ công đoạn và 8 mã ô ĐVT mâu thuẫn. Gán lại công đoạn, hoặc chọn rồi bấm Xác nhận đã rà nếu hiện tại đã đúng." + nút "Gợi ý theo đuôi mã" (Task 2).
- Cột Trạng thái: mã `can_ra_dvt` hiện `Tag` "ĐVT mâu thuẫn" + tooltip "Ô ĐVT gốc KiotViet khác tên/đuôi mã — kiểm tra ĐVT và công đoạn".
File vượt ~200 dòng → tách hook `useChonDong` hoặc component `bang-san-pham-cong-cu.tsx` (ghi SUMMARY).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `o-sua-nhanh.tsx` chứa `"sua_o"`, `onMutate`, `onError`
    - `thanh-gan-hang-loat.tsx` chứa `"hang_loat"`, `xacNhanDaRa`, `1000`
    - `bang-san-pham.tsx` (hoặc file tách) chứa `rowSelection` và `preserveSelectedRowKeys`, không chứa `onSelectAll`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>Rà hàng loạt trên bảng dùng được</done>
</task>

<task type="auto">
  <name>Task 2: Hộp gợi ý công đoạn theo đuôi mã</name>
  <files>src/features/danh-muc/components/goi-y-cong-doan.tsx, src/features/danh-muc/components/bang-san-pham.tsx</files>
  <read_first>src/features/danh-muc/hooks/useSanPham.ts, src/features/danh-muc/types.ts, .memory/knowledge/du-lieu-kiotviet.md</read_first>
  <action>
`"use client"`. `Modal` rộng (antd v6 prop kích thước — kiểm type), mở từ nút "Gợi ý theo đuôi mã" (hiện với quyền sửa, ở thanh công cụ và trong Alert Cần rà).
- `useGoiYCongDoan(open)` trong `QueryState`; rỗng: "Không còn mã mua ngoài nào có đuôi -CB / -X / -S / -N."
- Đầu hộp: "Quy ước đuôi mã đã kiểm trên 1.441 mã: -CB → Carbon (97%), -X → Xi mạ (95%), -S… → Sơn (94%), -N → Nano. Bỏ tick những mã bạn thấy sai."
- Nhóm theo công đoạn đề xuất bằng `Collapse` (mặc định mở), tiêu đề "Carbon · 64 mã" kèm checkbox chọn/bỏ cả nhóm.
- Mỗi nhóm một `Table size="small"` với `rowSelection` (mặc định chọn hết): Mã · Tên · Nhóm hàng. Tên chứa từ gợi ý trái ngược (ví dụ đề xuất Carbon mà tên có "sơn"/"xi" không dấu) → `Tag orange` "Kiểm tra" và mặc định BỎ tick.
- Footer: "Áp dụng cho **K** mã đã chọn" → `apDungGoiYCongDoan(ids)` → `message.success("Đã gán công đoạn cho <số trả về> mã")` (số trả về có thể < K nếu ai đó đã sửa trong lúc mở — hiện cả hai số khi khác nhau) → đóng + invalidate.
Nối nút mở vào `bang-san-pham.tsx`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `goi-y-cong-doan.tsx` chứa `useGoiYCongDoan`, `apDungGoiYCongDoan`, `Collapse`, chuỗi `"Kiểm tra"`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>145 mã gán tự động có người xác nhận</done>
</task>

<task type="auto">
  <name>Task 3: Route xuất Excel theo bộ lọc + nút Excel</name>
  <files>src/app/api/danh-muc/xuat-excel/route.ts, src/features/danh-muc/components/nut-excel.tsx, src/features/danh-muc/components/bang-san-pham.tsx</files>
  <read_first>src/app/api/danh-muc/mau-excel/route.ts, src/features/danh-muc/lib/doc-file-danh-muc.server.ts, src/features/danh-muc/schemas/bo-loc.schema.ts, src/features/xac-thuc/api/nguoi-dung-hien-tai.server.ts, src/lib/supabase/server.ts, src/shared/lib/quyen.ts</read_first>
  <action>
**`GET /api/danh-muc/xuat-excel`** (`runtime = "nodejs"`):
1. `layNguoiDungHienTai()` null → 401 JSON.
2. `const boLoc = docBoLocTuUrl(request.nextUrl.searchParams)`; `const args = { ...thamSoRpc(boLoc), p_trang: 1, p_kich_thuoc: 5000 }`.
3. `rpc("danh_sach_san_pham", args)` bằng server client; lỗi → JSON theo `dienGiaiLoi`.
4. `(data[0]?.tong_so_dong ?? 0) > 5000` → 422 "Kết quả hơn 5000 mã — lọc hẹp lại rồi xuất".
5. Map dòng → `DongXuat` (tên nhóm/ĐVT/công đoạn/kho lấy từ cột tên RPC trả; `kho_mac_dinh` cần tên kho → một lệnh `from("kho").select("id, ten")` rồi tra).
6. `taoFileMau(dong, { coGiaVon: coQuyen(nd.vaiTro, "xem_gia_von") })`.
7. `Content-Disposition: attachment; filename="danh-muc-<yyyyMMdd-HHmm theo Asia/Ho_Chi_Minh>.xlsx"`; `Cache-Control: no-store`.

**`nut-excel.tsx`** (`"use client"`): `Dropdown.Button` hoặc hai nút:
- "Xuất Excel" → tải bằng `fetch(\`/api/danh-muc/xuat-excel?${ghiBoLocRaUrl(boLoc)}\`)`; `!res.ok` → đọc JSON lỗi → `message.error`; ok → `blob()` → tạo `<a download>` tạm để tải (trong app thật, không phải artifact) → thu hồi object URL. Nút `loading` trong lúc chờ. Hiện số mã sẽ xuất trong tooltip.
- Mục "Tải file mẫu trống" → `/api/danh-muc/mau-excel` tương tự.
- Mục "Nhập từ Excel…" chừa chỗ gọi `onMoNhap` (plan 20 nối) — chỉ hiện khi có quyền sửa và prop được truyền.
Đặt `NutExcel` vào khe `hanhDongPhu` của thanh lọc; hiện cho mọi vai trò (xuất), mục nhập theo quyền.

Thử tay (SUMMARY): lọc công đoạn CARBON → Xuất → mở file: số dòng = tổng trên bảng, có cột Giá vốn khi vanphong, không có khi thukho1; import lại chính file đó ở plan 20 ra "0 thêm, 0 sửa".
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `xuat-excel/route.ts` chứa `runtime = "nodejs"`, `docBoLocTuUrl`, `p_kich_thuoc: 5000`, `xem_gia_von`, `Content-Disposition`
    - `nut-excel.tsx` chứa `/api/danh-muc/xuat-excel` và `/api/danh-muc/mau-excel`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>DMUC-07 dùng được, đúng quyền giá vốn</done>
</task>

</tasks>

<verification>
- [ ] `npm run check` xanh; thử tay xuất Excel 2 vai trò
- [ ] Thử tay: gán công đoạn cho 3 mã Cần rà → số Cần rà giảm 3; lịch sử sửa của mã hiện nguồn "Sửa hàng loạt"
</verification>

<success_criteria>
- Người văn phòng rà 356 + 8 mã bằng lọc → chọn → gán, không cần mở từng mã
</success_criteria>

<output>
After completion, create `.planning/phases/02-khung-ung-dung/02-19-SUMMARY.md`
</output>
