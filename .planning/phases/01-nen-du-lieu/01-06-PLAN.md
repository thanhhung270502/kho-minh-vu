---
phase: 01-nen-du-lieu
plan: 06
type: execute
wave: 6
depends_on: ["01-05"]
files_modified: [supabase/migrations/0011_rpc_ghi_so.sql, supabase/migrations/0012_rpc_huy.sql]
autonomous: true
requirements: [DATA-05, DATA-06]
user_setup: []
must_haves:
  truths:
    - "ghi_so_chung_tu xử lý đủ 7 loại chứng từ qua các hàm nội bộ riêng cho từng loại"
    - "Lỗi ở dòng thứ n của chứng từ không để lại movement nào của n-1 dòng trước"
    - "Xuất làm tồn xuống dưới 0 mà thiếu ly_do_xuat_am bị từ chối với SQLSTATE 23514"
    - "huy_chung_tu sinh bút toán đảo, giữ nguyên bản ghi gốc, tồn về đúng số trước khi ghi sổ"
  artifacts:
    - path: "supabase/migrations/0011_rpc_ghi_so.sql"
      provides: "RPC ghi sổ atomic + 7 hàm nội bộ theo loại chứng từ"
      min_lines: 150
      contains: "security definer"
    - path: "supabase/migrations/0012_rpc_huy.sql"
      provides: "RPC hủy chứng từ sinh bút toán đảo"
      contains: "la_but_toan_dao"
  key_links:
    - from: "supabase/migrations/0011_rpc_ghi_so.sql"
      to: "public.kho_movement"
      via: "insert trong vòng lặp qua chung_tu_dong"
      pattern: "insert into public\\.kho_movement"
    - from: "supabase/migrations/0012_rpc_huy.sql"
      to: "public.kho_movement"
      via: "insert bút toán đảo với so_luong nghịch dấu"
      pattern: "la_but_toan_dao"
---

<objective>
Viết hai RPC là cửa duy nhất để tồn kho thay đổi: ghi sổ chứng từ và hủy chứng từ.

Purpose: Ghi sổ phải atomic. Server Action gọi bốn câu lệnh rời rạc KHÔNG phải một
transaction — mất mạng giữa chừng là lệch tồn vĩnh viễn. Đây là lý do toàn bộ logic nằm
trong Postgres.
Output: migration 0011 và 0012.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-nen-du-lieu/01-CONTEXT.md
@.planning/phases/01-nen-du-lieu/01-RESEARCH.md
@src/shared/lib/errors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bảy hàm nội bộ sinh movement theo từng loại chứng từ</name>
  <files>supabase/migrations/0011_rpc_ghi_so.sql</files>
  <read_first>supabase/migrations/0008_so_cai_ton_kho.sql, supabase/migrations/0007_chung_tu.sql, .planning/phases/01-nen-du-lieu/01-RESEARCH.md</read_first>
  <action>
Tạo `supabase/migrations/0011_rpc_ghi_so.sql`. Phần đầu: bảy hàm nội bộ, mỗi hàm nhận một
dòng `chung_tu_dong` cùng header và sinh movement tương ứng.

Tách theo loại thay vì một khối `case` khổng lồ vì mỗi loại test độc lập được ở plan 11,
và vì "xử lý đủ 7 loại trong một hàm" là khối lượng logic lớn hơn nhiều so với vẻ ngoài.

Quy tắc dấu và số movement mỗi dòng:

| loai_ct | Movement sinh ra |
|---|---|
| `NHAP` | +1 movement: `+so_luong` tại `kho_id`, `gia_von_tai_thoi_diem = dong.don_gia` |
| `TRA_KHACH` | +1 movement: `+so_luong` tại `kho_id`, `gia_von_tai_thoi_diem = san_pham.gia_von` hiện tại |
| `XUAT` | +1 movement: `-so_luong` tại `kho_id`, `gia_von_tai_thoi_diem = san_pham.gia_von` hiện tại |
| `TRA_NCC` | +1 movement: `-so_luong` tại `kho_id`, `gia_von_tai_thoi_diem = san_pham.gia_von` hiện tại |
| `CHUYEN_KHO` | **2 movement**: `-so_luong` tại `kho_id` và `+so_luong` tại `kho_den_id`, cùng `gia_von_tai_thoi_diem` (giá vốn toàn công ty nên chuyển kho không đổi giá — D-11) |
| `KIEM_KE` | +1 movement: `so_luong = dong.so_luong - dong.so_luong_he_thong` (chênh lệch, có thể âm hoặc dương). Nếu chênh lệch = 0 thì **không sinh movement** (ràng buộc `ck_so_luong_khac_khong`) |
| `DIEU_CHINH` | +1 movement: `so_luong = dong.so_luong` giữ nguyên dấu (cho phép âm) |

Mỗi hàm đặt tên `public._ghi_so_<loai>` (`_ghi_so_nhap`, `_ghi_so_xuat`, `_ghi_so_tra_ncc`,
`_ghi_so_tra_khach`, `_ghi_so_chuyen_kho`, `_ghi_so_kiem_ke`, `_ghi_so_dieu_chinh`), đều
`language plpgsql`, `security definer`, `set search_path = ''`.

Tiền tố `_` báo hiệu hàm nội bộ. `revoke all on function public._ghi_so_* from public, anon,
authenticated;` — chỉ `ghi_so_chung_tu` được gọi chúng.

Chữ ký chung:
```sql
create or replace function public._ghi_so_nhap(
  p_ct public.chung_tu,
  p_dong public.chung_tu_dong
) returns void
language plpgsql
security definer
set search_path = ''
as $$
begin
  insert into public.kho_movement (
    ngay, kho_id, san_pham_id, so_luong, gia_von_tai_thoi_diem,
    chung_tu_id, chung_tu_dong_id
  ) values (
    p_ct.ngay_ct, p_ct.kho_id, p_dong.san_pham_id, p_dong.so_luong, p_dong.don_gia,
    p_ct.id, p_dong.id
  );
end;
$$;
```
  </action>
  <verify>npm run db:reset</verify>
  <acceptance_criteria>
    - File chứa đủ 7 chuỗi: `_ghi_so_nhap`, `_ghi_so_xuat`, `_ghi_so_tra_ncc`, `_ghi_so_tra_khach`, `_ghi_so_chuyen_kho`, `_ghi_so_kiem_ke`, `_ghi_so_dieu_chinh`
    - `grep -c "set search_path = ''" supabase/migrations/0011_rpc_ghi_so.sql` >= 7
    - Hàm `_ghi_so_chuyen_kho` chứa hai lệnh `insert into public.kho_movement`
    - Hàm `_ghi_so_kiem_ke` chứa `so_luong_he_thong`
    - `npm run db:reset` exit 0
  </acceptance_criteria>
  <done>Bảy hàm nội bộ tồn tại, mỗi loại chứng từ có logic sinh movement riêng</done>
</task>

<task type="auto">
  <name>Task 2: RPC ghi_so_chung_tu — điều phối, kiểm tra, atomic</name>
  <files>supabase/migrations/0011_rpc_ghi_so.sql</files>
  <read_first>supabase/migrations/0011_rpc_ghi_so.sql, src/shared/lib/errors.ts</read_first>
  <action>
Thêm vào `supabase/migrations/0011_rpc_ghi_so.sql`:

```sql
create or replace function public.ghi_so_chung_tu(p_chung_tu_id uuid)
returns public.chung_tu
language plpgsql
security definer
set search_path = ''
as $$
declare
  v_ct public.chung_tu;
  v_dong public.chung_tu_dong;
  v_so_dong integer;
  v_ton_sau numeric(18,4);
begin
  select * into v_ct from public.chung_tu where id = p_chung_tu_id for update;

  if v_ct.id is null then
    raise exception 'Không tìm thấy chứng từ %', p_chung_tu_id using errcode = '23514';
  end if;

  if v_ct.trang_thai <> 'NHAP_LIEU' then
    raise exception 'Chứng từ % đã ở trạng thái %, không ghi sổ lại được', v_ct.so_ct, v_ct.trang_thai
      using errcode = '23514';
  end if;

  -- SECURITY DEFINER bỏ qua RLS nên phải kiểm quyền nghiệp vụ TƯỜNG MINH tại đây.
  if (select public.vai_tro_hien_tai()) = 'chi_xem' then
    raise exception 'Vai trò chỉ xem không được ghi sổ chứng từ' using errcode = '42501';
  end if;

  select count(*) into v_so_dong from public.chung_tu_dong where chung_tu_id = p_chung_tu_id;
  if v_so_dong = 0 then
    raise exception 'Chứng từ % không có dòng nào, không ghi sổ được', v_ct.so_ct
      using errcode = '23514';
  end if;

  for v_dong in
    select * from public.chung_tu_dong where chung_tu_id = p_chung_tu_id order by created_at
  loop
    -- Chặn xuất âm khi chưa chọn lý do. Cho xuất âm là quyết định có chủ đích
    -- (42 mã đang bị xuất khi tồn <= 0 trên hệ cũ; chặn cứng sẽ làm kho kẹt ngay
    -- ngày đầu và quay lại KiotViet) — nhưng bắt buộc có lý do để vào báo cáo.
    if v_ct.loai_ct in ('XUAT','TRA_NCC') and v_ct.ly_do_xuat_am is null then
      select coalesce(so_luong, 0) - v_dong.so_luong into v_ton_sau
      from public.ton_kho
      where kho_id = v_ct.kho_id and san_pham_id = v_dong.san_pham_id;

      if coalesce(v_ton_sau, -v_dong.so_luong) < 0 then
        raise exception
          'Xuất quá tồn cho sản phẩm %, phải chọn lý do xuất âm trước khi ghi sổ', v_dong.san_pham_id
          using errcode = '23514';
      end if;
    end if;

    case v_ct.loai_ct
      when 'NHAP'       then perform public._ghi_so_nhap(v_ct, v_dong);
      when 'XUAT'       then perform public._ghi_so_xuat(v_ct, v_dong);
      when 'TRA_NCC'    then perform public._ghi_so_tra_ncc(v_ct, v_dong);
      when 'TRA_KHACH'  then perform public._ghi_so_tra_khach(v_ct, v_dong);
      when 'CHUYEN_KHO' then perform public._ghi_so_chuyen_kho(v_ct, v_dong);
      when 'KIEM_KE'    then perform public._ghi_so_kiem_ke(v_ct, v_dong);
      when 'DIEU_CHINH' then perform public._ghi_so_dieu_chinh(v_ct, v_dong);
    end case;
  end loop;

  -- Cập nhật tiến độ đơn đặt hàng nếu phiếu xuất gắn với một đơn.
  if v_ct.loai_ct = 'XUAT' and v_ct.don_dat_hang_id is not null then
    perform public._cap_nhat_tien_do_ddh(v_ct.don_dat_hang_id);
  end if;

  update public.chung_tu
  set trang_thai = 'HOAN_THANH',
      ngay_ghi_so = now(),
      nguoi_duyet_id = auth.uid(),
      tong_so_luong = (select coalesce(sum(so_luong),0) from public.chung_tu_dong where chung_tu_id = p_chung_tu_id),
      tong_tien = (select coalesce(sum(thanh_tien),0) from public.chung_tu_dong where chung_tu_id = p_chung_tu_id)
  where id = p_chung_tu_id
  returning * into v_ct;

  return v_ct;
end;
$$;

revoke all on function public.ghi_so_chung_tu(uuid) from public, anon;
grant execute on function public.ghi_so_chung_tu(uuid) to authenticated;
```

Viết thêm `public._cap_nhat_tien_do_ddh(p_ddh_id uuid)`: cộng dồn `so_luong` đã xuất từ mọi
`chung_tu_dong` thuộc chứng từ XUAT đã HOAN_THANH gắn với đơn, ghi vào
`don_dat_hang_dong.so_luong_da_xuat`, rồi đặt `don_dat_hang.trang_thai` thành `DA_XUAT_DU`
nếu mọi dòng đủ, `DA_XUAT_MOT_PHAN` nếu có dòng đã xuất một phần, ngược lại giữ `MOI`.

**Tính atomic đến từ đâu:** toàn bộ hàm chạy trong transaction ngầm định của một lời gọi RPC.
Lỗi ở dòng thứ n làm rollback cả n-1 dòng trước — không cần `begin/exception` thủ công, và
KHÔNG được bọc thân vòng lặp trong `exception when others` (làm vậy sẽ nuốt lỗi và phá đúng
tính chất cần có).
  </action>
  <verify>npm run db:reset và chạy kịch bản ghi sổ một phiếu nhập 2 dòng</verify>
  <acceptance_criteria>
    - File chứa `create or replace function public.ghi_so_chung_tu(p_chung_tu_id uuid)`
    - File chứa `security definer` và `set search_path = ''` cho hàm ghi_so_chung_tu
    - File chứa `using errcode = '42501'` cho nhánh vai trò chỉ xem
    - File chứa `ly_do_xuat_am is null`
    - File chứa `grant execute on function public.ghi_so_chung_tu(uuid) to authenticated`
    - File chứa `public._cap_nhat_tien_do_ddh`
    - Thân vòng lặp KHÔNG chứa `exception when others`
    - `npm run db:reset` exit 0
  </acceptance_criteria>
  <done>Ghi sổ atomic, kiểm quyền tường minh, chặn xuất âm thiếu lý do</done>
</task>

<task type="auto">
  <name>Task 3: RPC huy_chung_tu — bút toán đảo</name>
  <files>supabase/migrations/0012_rpc_huy.sql</files>
  <read_first>supabase/migrations/0011_rpc_ghi_so.sql, supabase/migrations/0008_so_cai_ton_kho.sql</read_first>
  <action>
Tạo `supabase/migrations/0012_rpc_huy.sql`:

```sql
create or replace function public.huy_chung_tu(p_chung_tu_id uuid, p_ly_do text)
returns public.chung_tu
language plpgsql
security definer
set search_path = ''
as $$
declare
  v_ct public.chung_tu;
  v_mv public.kho_movement;
begin
  select * into v_ct from public.chung_tu where id = p_chung_tu_id for update;

  if v_ct.id is null then
    raise exception 'Không tìm thấy chứng từ %', p_chung_tu_id using errcode = '23514';
  end if;
  if v_ct.trang_thai = 'DA_HUY' then
    raise exception 'Chứng từ % đã hủy rồi', v_ct.so_ct using errcode = '23514';
  end if;
  if coalesce(trim(p_ly_do), '') = '' then
    raise exception 'Phải nhập lý do khi hủy chứng từ' using errcode = '23514';
  end if;
  if (select public.vai_tro_hien_tai()) = 'chi_xem' then
    raise exception 'Vai trò chỉ xem không được hủy chứng từ' using errcode = '42501';
  end if;

  -- Chứng từ mới nhập liệu chưa đụng tồn — hủy thẳng, không sinh bút toán đảo.
  if v_ct.trang_thai = 'NHAP_LIEU' then
    update public.chung_tu
    set trang_thai = 'DA_HUY',
        ghi_chu = coalesce(ghi_chu || E'\n', '') || 'Hủy: ' || p_ly_do
    where id = p_chung_tu_id
    returning * into v_ct;
    return v_ct;
  end if;

  -- Chứng từ đã ghi sổ: sinh bút toán đảo cho TỪNG movement, không xóa bản ghi nào.
  -- Sổ cái là append-only nên đây là cách duy nhất đưa tồn về số cũ.
  for v_mv in
    select * from public.kho_movement
    where chung_tu_id = p_chung_tu_id and la_but_toan_dao = false
  loop
    insert into public.kho_movement (
      ngay, kho_id, san_pham_id, so_luong, gia_von_tai_thoi_diem,
      chung_tu_id, chung_tu_dong_id, la_but_toan_dao
    ) values (
      now(), v_mv.kho_id, v_mv.san_pham_id, -v_mv.so_luong, v_mv.gia_von_tai_thoi_diem,
      v_mv.chung_tu_id, v_mv.chung_tu_dong_id, true
    );
  end loop;

  update public.chung_tu
  set trang_thai = 'DA_HUY',
      ghi_chu = coalesce(ghi_chu || E'\n', '') || 'Hủy: ' || p_ly_do
  where id = p_chung_tu_id
  returning * into v_ct;

  if v_ct.loai_ct = 'XUAT' and v_ct.don_dat_hang_id is not null then
    perform public._cap_nhat_tien_do_ddh(v_ct.don_dat_hang_id);
  end if;

  return v_ct;
end;
$$;

revoke all on function public.huy_chung_tu(uuid, text) from public, anon;
grant execute on function public.huy_chung_tu(uuid, text) to authenticated;
```

Lưu ý về giá vốn khi đảo: bút toán đảo của một phiếu **nhập** có `so_luong` âm nên trigger
giá vốn KHÔNG tính lại (chỉ tính khi `so_luong > 0`). Nghĩa là giá vốn không tự quay về số
trước khi nhập. Đây là hành vi đúng của bình quân gia quyền di động — giá vốn là trung bình
lịch sử, không phải giá trị hoàn tác được. Ghi rõ điều này vào `supabase/README.md` mục
"Giá vốn khi hủy phiếu nhập" để không ai tưởng là lỗi.
  </action>
  <verify>npm run db:reset và chạy kịch bản ghi sổ rồi hủy, kiểm ton_kho về số cũ</verify>
  <acceptance_criteria>
    - File chứa `la_but_toan_dao` và `-v_mv.so_luong`
    - File chứa nhánh xử lý riêng cho `trang_thai = 'NHAP_LIEU'` (hủy không sinh bút toán)
    - File chứa `and la_but_toan_dao = false` trong câu select vòng lặp (không đảo lại bút toán đảo)
    - File KHÔNG chứa `delete from public.kho_movement`
    - supabase/README.md chứa mục `Giá vốn khi hủy phiếu nhập`
    - Kịch bản: nhập 10, ghi sổ → ton_kho = 10; hủy → ton_kho = 0, còn 2 dòng kho_movement
  </acceptance_criteria>
  <done>Hủy chứng từ đưa tồn về số cũ bằng bút toán đảo, không xóa bản ghi</done>
</task>

</tasks>

<verification>
- [ ] `npm run db:reset` exit 0
- [ ] Ghi sổ phiếu nhập 2 dòng làm tồn tăng đúng cả 2 mã
- [ ] Ghi sổ phiếu có dòng lỗi không để lại movement nào
- [ ] Hủy chứng từ đã ghi sổ đưa ton_kho về số trước đó, kho_movement tăng thêm dòng đảo
- [ ] Mọi hàm SECURITY DEFINER đều có `set search_path = ''`
</verification>

<success_criteria>
- Tất cả task hoàn thành
- Ghi sổ là cửa duy nhất làm tồn thay đổi
- Tính atomic đến từ transaction của RPC, không bị nuốt lỗi
</success_criteria>

<output>
After completion, create `.planning/phases/01-nen-du-lieu/01-06-SUMMARY.md`
</output>
