---
phase: 01-nen-du-lieu
plan: 14
type: execute
wave: 12
depends_on: ["01-08", "01-15"]
files_modified: [src/types/database.types.ts, package.json, supabase/README.md]
autonomous: true
requirements: [DATA-01]
user_setup: []
must_haves:
  truths:
    - "src/types/database.types.ts sinh từ schema thật, chứa đủ 13 bảng"
    - "Kiểu của các RPC (ghi_so_chung_tu, huy_chung_tu, tim_san_pham, doi_chieu_ton) có mặt trong Functions"
    - "npm run check đi qua typecheck, lint và build"
  artifacts:
    - path: "src/types/database.types.ts"
      provides: "Kiểu TypeScript sinh tự động cho toàn bộ schema"
      min_lines: 300
      contains: "kho_movement"
---

<objective>
Sinh lại `database.types.ts` từ schema thật và xác nhận ứng dụng vẫn build. Đây là plan CUỐI CÙNG của Phase 1.

Purpose: File này là điểm nối duy nhất giữa database và ứng dụng. Từ Phase 2 trở đi mọi
feature đọc kiểu từ đây, không ai tự viết type bảng. Hiện nó còn là stub rỗng.
Output: `src/types/database.types.ts` thật.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@src/types/database.types.ts
@src/lib/supabase/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sinh database.types.ts từ schema local</name>
  <files>src/types/database.types.ts</files>
  <read_first>src/types/database.types.ts, package.json, src/lib/supabase/client.ts</read_first>
  <action>
Chạy `npm run db:reset` rồi `npm run db:types:local`.

Kiểm kết quả có đủ:
- 13 bảng trong `Tables`: kho, nhom_hang, don_vi_tinh, cong_doan, doi_tac, nguoi_dung,
  san_pham, don_dat_hang, don_dat_hang_dong, chung_tu, chung_tu_dong, kho_movement, ton_kho
- hai bảng lưu trữ: luu_tru_nhap_kiotviet, luu_tru_hoa_don_kiotviet
- `chuoi_so_ct`
- 5 enum trong `Enums`
- View `v_doi_chieu_ton` trong `Views`
- Các RPC trong `Functions`: `ghi_so_chung_tu`, `huy_chung_tu`, `tim_san_pham`,
  `doi_chieu_ton`, `sinh_so_ct`, `vai_tro_hien_tai`, `kho_hien_tai`,
  `nap_danh_muc_kiotviet` — hàm cuối đến từ migration 0016 của plan 15. Đó là lý do
  plan này chạy SAU plan 15 chứ không song song: sinh types trước khi có 0016 sẽ thiếu
  kiểu của RPC đó và Phase 2 sẽ không gọi được nó với kiểu đúng.

Nếu thiếu RPC nào: nguyên nhân thường là hàm không được `grant execute to authenticated`
(Supabase chỉ sinh kiểu cho hàm client gọi được). Sửa migration tương ứng chứ **không sửa
tay file types** — CLAUDE.md ghi rõ sửa tay sẽ mất ở lần sinh sau.

Giữ nguyên comment cảnh báo "ĐƯỢC SINH TỰ ĐỘNG — không sửa tay" ở đầu file nếu generator
ghi đè mất thì thêm lại bằng một bước hậu xử lý trong npm script.
  </action>
  <verify>npm run db:types:local && npm run typecheck</verify>
  <acceptance_criteria>
    - src/types/database.types.ts chứa `kho_movement`, `ton_kho`, `chung_tu_dong`, `san_pham`
    - File chứa `ghi_so_chung_tu` và `tim_san_pham` trong phần Functions
    - File chứa `loai_ct` và `vai_tro` trong phần Enums
    - File chứa `v_doi_chieu_ton`
    - File dài hơn 300 dòng
    - File chứa dòng cảnh báo `ĐƯỢC SINH TỰ ĐỘNG`
    - `npm run typecheck` exit 0
  </acceptance_criteria>
  <done>Kiểu TypeScript phản ánh đúng schema thật, ứng dụng type-check được</done>
</task>

<task type="auto">
  <name>Task 2: Chốt quy ước và ghi tài liệu vận hành</name>
  <files>supabase/README.md, package.json</files>
  <read_first>supabase/README.md, package.json, CLAUDE.md</read_first>
  <action>
Hoàn thiện `supabase/README.md` thành tài liệu vận hành database của dự án. Phải có đủ các
mục đã tích lũy qua các plan trước, cộng thêm:

**"Quy ước tên migration"** — `NNNN_ten_khong_dau.sql`, bốn chữ số tăng dần, khóa cho toàn
dự án. Phase 2 trở đi tiếp tục từ `0016`. Không dùng `supabase migration new` (sinh timestamp
14 chữ số) để tránh trộn hai kiểu.

**"Vòng đời lệnh"** — bảng tra nhanh:
| Việc | Lệnh |
|---|---|
| Bật stack local | `npm run db:start` |
| Dựng lại DB từ đầu + seed | `npm run db:reset` |
| Chạy toàn bộ pgTAP | `npm run db:test` |
| Sinh lại kiểu TS | `npm run db:types:local` |
| Kiểm hook chạy thật | `npm run verify:hook` |
| Thử nạp dữ liệu KiotViet | `npm run import:kiotviet -- --mau` |

**"Ba mục phải kiểm thủ công"** — chép từ `01-VALIDATION.md` §Manual-Only: hook chạy thật,
đăng ký cron trên cloud, TTL claim sau khi đổi vai trò. Nói rõ `npm run db:test` xanh không
phủ ba mục này.

**"Khi lên cloud cần làm gì"** — danh sách việc Dashboard mà CLI không làm được: bật
extension, đăng ký custom access token hook, đăng ký `cron.schedule`, tạo tài khoản thật.
Nhấn: **không chạy `seed.sql` trên cloud**.

Cập nhật `CLAUDE.md` mục "Lệnh hay dùng": thêm các lệnh db mới vào khối bash đã có.
  </action>
  <verify>npm run check</verify>
  <acceptance_criteria>
    - supabase/README.md chứa mục `Quy ước tên migration` và chuỗi `NNNN_ten_khong_dau.sql`
    - supabase/README.md chứa bảng `Vòng đời lệnh` với cả 6 lệnh
    - supabase/README.md chứa mục `Ba mục phải kiểm thủ công`
    - supabase/README.md chứa mục `Khi lên cloud cần làm gì` và cảnh báo không chạy seed.sql trên cloud
    - CLAUDE.md khối "Lệnh hay dùng" chứa `npm run db:test`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>Người tiếp theo mở repo biết chạy gì và biết cái gì chưa được tự động kiểm</done>
</task>

</tasks>

<verification>
- [ ] `npm run check` exit 0 (typecheck + lint + build)
- [ ] database.types.ts có đủ 13 bảng và 4 RPC chính
- [ ] supabase/README.md đủ 4 mục mới
</verification>

<success_criteria>
- Tất cả task hoàn thành
- Ứng dụng build được với kiểu sinh từ schema thật
- Tài liệu vận hành đủ để Phase 2 bắt đầu mà không phải đọc lại toàn bộ migration
</success_criteria>

<output>
After completion, create `.planning/phases/01-nen-du-lieu/01-14-SUMMARY.md`
</output>
