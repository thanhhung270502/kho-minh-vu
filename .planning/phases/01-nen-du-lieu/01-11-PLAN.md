---
phase: 01-nen-du-lieu
plan: 11
type: execute
wave: 9
depends_on: ["01-09"]
files_modified: [supabase/tests/chung_tu_test.sql]
autonomous: true
requirements: [DATA-10]
user_setup: []
must_haves:
  truths:
    - "Test chứng minh lỗi ở dòng thứ n của chứng từ không để lại movement nào"
    - "Test chứng minh hủy chứng từ đã ghi sổ đưa tồn về đúng số cũ và giữ bản ghi gốc"
    - "Test chứng minh hai phiên gọi sinh_so_ct đồng thời không ra số trùng"
    - "Test chứng minh xuất quá tồn mà thiếu lý do bị từ chối"
  artifacts:
    - path: "supabase/tests/chung_tu_test.sql"
      provides: "pgTAP cho DATA-05, DATA-06, DATA-08"
      min_lines: 90
      contains: "throws_ok"
---

<objective>
Viết pgTAP chứng minh ghi sổ atomic, hủy sinh bút toán đảo, và đánh số không trùng.

Purpose: Ba hành vi này là hợp đồng của RPC ghi sổ. Không test được chúng thì không biết
chứng từ có đáng tin hay không.
Output: `supabase/tests/chung_tu_test.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-nen-du-lieu/01-VALIDATION.md
@.planning/phases/01-nen-du-lieu/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test ghi sổ atomic (DATA-05)</name>
  <files>supabase/tests/chung_tu_test.sql</files>
  <read_first>supabase/migrations/0011_rpc_ghi_so.sql, supabase/tests/_helper.sql</read_first>
  <action>
Tạo `supabase/tests/chung_tu_test.sql` theo khung pgTAP chuẩn.

Đăng nhập như `vanphong@khominhvu.local` qua `public.dang_nhap_nhu()` trước các assertion ghi.

Assertion bắt buộc:

1. **Happy path** — `lives_ok()`: tạo chứng từ NHAP 2 dòng ở trạng thái `NHAP_LIEU`, gọi
   `ghi_so_chung_tu()`. Sau đó `is()`:
   - `chung_tu.trang_thai` = `HOAN_THANH`
   - `chung_tu.ngay_ghi_so` không NULL
   - đếm `kho_movement` của chứng từ đó = `2`
   - `chung_tu.tong_so_luong` khớp tổng dòng

2. **Atomic** — dựng một chứng từ XUAT 3 dòng, trong đó dòng thứ 2 trỏ tới `san_pham_id`
   không tồn tại (vi phạm FK). `throws_ok()` khi gọi `ghi_so_chung_tu()`, rồi `is()`:
   - đếm `kho_movement` của chứng từ đó = `0` — **không có movement mồ côi của dòng 1**
   - `chung_tu.trang_thai` vẫn = `NHAP_LIEU`

   Đây là assertion quan trọng nhất của plan. Nếu nó trượt, nghĩa là ai đó đã bọc vòng lặp
   trong `exception when others` và nuốt mất tính atomic.

3. **Không ghi sổ lại** — `throws_ok()` gọi `ghi_so_chung_tu()` lần hai trên chứng từ đã
   `HOAN_THANH`, mã lỗi `23514`

4. **Chứng từ rỗng** — `throws_ok()` ghi sổ chứng từ không có dòng nào, mã `23514`

5. **Xuất âm thiếu lý do** — tạo phiếu XUAT số lượng lớn hơn tồn, `ly_do_xuat_am` NULL,
   `throws_ok()` mã `23514`. Rồi đặt `ly_do_xuat_am = 'Hàng đang về'` và `lives_ok()` —
   chứng minh cho xuất âm khi đã chọn lý do (quyết định có chủ đích, không phải lỗ hổng)

6. **CHUYEN_KHO sinh 2 movement** — ghi sổ một phiếu chuyển kho 1 dòng, `is()` đếm movement
   = `2`, một âm ở K1 và một dương ở K2, cùng `gia_von_tai_thoi_diem`
  </action>
  <verify>npm run db:test</verify>
  <acceptance_criteria>
    - File chứa assertion đếm movement = 0 sau khi ghi sổ thất bại
    - File chứa `ly_do_xuat_am` trong cả một `throws_ok` và một `lives_ok`
    - File chứa assertion CHUYEN_KHO sinh đúng 2 movement
    - File chứa `'23514'` ít nhất 3 lần
    - `npm run db:test` exit 0
  </acceptance_criteria>
  <done>Tính atomic của ghi sổ được chứng minh bằng một kịch bản lỗi thật</done>
</task>

<task type="auto">
  <name>Task 2: Test hủy chứng từ sinh bút toán đảo (DATA-06)</name>
  <files>supabase/tests/chung_tu_test.sql</files>
  <read_first>supabase/tests/chung_tu_test.sql, supabase/migrations/0012_rpc_huy.sql</read_first>
  <action>
Thêm assertion:

1. Ghi sổ phiếu NHAP `+10`, `is()` `ton_kho.so_luong` = `10`
2. Gọi `huy_chung_tu(id, 'Nhập nhầm nhà cung cấp')`, rồi `is()`:
   - `ton_kho.so_luong` = `0` — tồn về đúng số trước khi ghi sổ
   - đếm `kho_movement` của chứng từ = `2` — **bản ghi gốc còn nguyên, không bị xóa**
   - đếm movement có `la_but_toan_dao = true` = `1`
   - `chung_tu.trang_thai` = `DA_HUY`
   - `chung_tu.ghi_chu` chứa chuỗi `Nhập nhầm nhà cung cấp`
3. `throws_ok()` — hủy lần hai cùng chứng từ, mã `23514`
4. `throws_ok()` — hủy với `p_ly_do` là chuỗi rỗng, mã `23514`
5. Hủy chứng từ đang `NHAP_LIEU`: `lives_ok()`, rồi `is()` đếm `kho_movement` = `0`
   — chưa ghi sổ thì không sinh bút toán đảo
6. `is()` — không có movement nào của bút toán đảo lại bị đảo tiếp (đếm
   `la_but_toan_dao = true` vẫn = 1 sau mọi thao tác)

Thêm một assertion ghi nhận hành vi đã biết, kèm comment giải thích:
`san_pham.gia_von` **không** quay về giá trước khi nhập sau khi hủy — bình quân gia quyền là
trung bình lịch sử, không hoàn tác được. Assert giá trị hiện tại để nếu sau này ai đó "sửa"
thành hoàn tác thì test sẽ đỏ và buộc phải bàn lại.
  </action>
  <verify>npm run db:test</verify>
  <acceptance_criteria>
    - File chứa `la_but_toan_dao` trong ít nhất 2 assertion
    - File chứa assertion đếm kho_movement = 2 sau khi hủy (gốc + đảo)
    - File chứa assertion cho trường hợp hủy chứng từ đang NHAP_LIEU
    - File chứa comment giải thích vì sao gia_von không quay lại sau khi hủy
    - `npm run db:test` exit 0
  </acceptance_criteria>
  <done>Hủy chứng từ đưa tồn về đúng số mà không xóa lịch sử</done>
</task>

<task type="auto">
  <name>Task 3: Test đánh số không trùng khi đồng thời (DATA-08)</name>
  <files>supabase/tests/chung_tu_test.sql</files>
  <read_first>supabase/tests/chung_tu_test.sql, supabase/migrations/0009_danh_so.sql</read_first>
  <action>
Assertion tuần tự (rẻ, bắt lỗi định dạng):
1. `is(public.sinh_so_ct('NHAP', 2026::smallint), 'PN26-000001')`
2. `is(public.sinh_so_ct('NHAP', 2026::smallint), 'PN26-000002')`
3. `is(public.sinh_so_ct('XUAT', 2026::smallint), 'PX26-000001')` — chuỗi độc lập theo loại
4. `is(public.sinh_so_ct('NHAP', 2027::smallint), 'PN27-000001')` — reset theo năm
5. Kiểm đủ 7 tiền tố: gọi `sinh_so_ct` cho cả 7 loại, assert tiền tố lần lượt là
   PN, PX, TN, TK, CK, KK, DC

Assertion đồng thời (bắt lỗi race):
6. Dùng cùng cơ chế hai kết nối như plan 10 (dblink hoặc script Node). Gọi
   `sinh_so_ct('NHAP', 2026)` **50 lần từ hai kết nối song song**, thu kết quả vào một bảng
   tạm, rồi assert:
   - `count(*) = 50`
   - `count(distinct so) = 50` — **không số nào trùng**

   Nếu ai đó thay `INSERT ... ON CONFLICT DO UPDATE ... RETURNING` bằng `SELECT MAX+1` thì
   assertion này sẽ đỏ. Đó là toàn bộ lý do nó tồn tại.

Nếu dùng đường script Node (khi dblink không khả dụng), gộp vào cùng file
`scripts/test-race-gia-von.ts` của plan 10, đổi tên thành `scripts/test-dong-thoi.ts` và
cập nhật npm script tương ứng.
  </action>
  <verify>npm run db:test (và npm run test:race nếu dùng đường thay thế)</verify>
  <acceptance_criteria>
    - File chứa `'PN26-000001'` và `'PN26-000002'` và `'PX26-000001'`
    - File chứa assertion reset theo năm với `'PN27-000001'`
    - File chứa cả 7 tiền tố PN, PX, TN, TK, CK, KK, DC
    - Có assertion `count(distinct` cho kịch bản đồng thời với kỳ vọng 50
    - Test chạy exit 0
  </acceptance_criteria>
  <done>Đánh số chứng từ chứng minh được là không trùng dưới đồng thời thật</done>
</task>

</tasks>

<verification>
- [ ] `npm run db:test` exit 0, không có `not ok`
- [ ] Assertion "0 movement sau khi ghi sổ lỗi" xanh
- [ ] Assertion "50 số, 50 giá trị phân biệt" xanh
</verification>

<success_criteria>
- Tất cả task hoàn thành
- DATA-05, DATA-06, DATA-08 chứng minh bằng hành vi
</success_criteria>

<output>
After completion, create `.planning/phases/01-nen-du-lieu/01-11-SUMMARY.md`
</output>
