---
phase: 01-nen-du-lieu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/config.toml, supabase/migrations/0001_extensions.sql, supabase/migrations/0002_enums.sql, package.json, .gitignore]
autonomous: true
requirements: [DATA-01]
user_setup: []
must_haves:
  truths:
    - "npx supabase start dựng được stack local, npx supabase db reset chạy sạch không lỗi"
    - "f_unaccent('Bạc đạn') trả về 'Bac dan' và hàm được đánh dấu IMMUTABLE nên index được"
    - "5 kiểu enum tồn tại với đúng số giá trị: loai_ct 7, trang_thai_ct 3, vai_tro 4, loai_doi_tac 3, trang_thai_ddh 4"
  artifacts:
    - path: "supabase/config.toml"
      provides: "Cấu hình stack local + đăng ký custom access token hook"
      contains: "[auth.hook.custom_access_token]"
    - path: "supabase/migrations/0001_extensions.sql"
      provides: "Extension, f_unaccent(), update_updated_at()"
      contains: "regdictionary"
    - path: "supabase/migrations/0002_enums.sql"
      provides: "5 kiểu enum của miền nghiệp vụ"
      contains: "create type public.loai_ct"
  key_links:
    - from: "supabase/migrations/0001_extensions.sql"
      to: "extensions.unaccent"
      via: "wrapper IMMUTABLE 2 tham số"
      pattern: "unaccent\\('extensions\\.unaccent'::regdictionary"
---

<objective>
Dựng nền Postgres cho toàn bộ Phase 1: stack Supabase local, các extension, hai hàm dùng
chung và năm kiểu enum của miền nghiệp vụ.

Purpose: Mọi migration sau đều phụ thuộc plan này. `f_unaccent()` phải tồn tại trước khi
tạo index tìm kiếm ở plan 03 — quên thứ tự này sẽ lỗi "function does not exist".
Output: `supabase/config.toml`, hai migration đầu tiên, npm script để chạy DB local.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-nen-du-lieu/01-CONTEXT.md
@.planning/phases/01-nen-du-lieu/01-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Khởi tạo stack Supabase local</name>
  <files>supabase/config.toml, .gitignore</files>
  <read_first>.gitignore, package.json, .planning/phases/01-nen-du-lieu/01-RESEARCH.md</read_first>
  <action>
Chạy `npx supabase init` tại gốc repo để sinh `supabase/config.toml`.

Sửa `config.toml`:
- `project_id = "kho-minh-vu"`
- Thêm khối đăng ký hook (RESEARCH.md §Code Examples 1):

```toml
[auth.hook.custom_access_token]
enabled = true
uri = "pg-functions://postgres/public/custom_access_token_hook"
```

Hook function chưa tồn tại ở plan này — nó được tạo ở plan 08. Đăng ký trước là cố ý:
`supabase start` chỉ đọc config, không validate hàm có thật; đặt sẵn tránh quên sau.
Nếu `supabase start` báo lỗi vì hàm chưa có, tạm comment khối này và ghi TODO trỏ tới plan 08.

Đọc giá trị `jwt_expiry` mà `supabase init` sinh ra trong `[auth]` và ghi lại vào
`supabase/README.md` (tạo mới) dưới mục "TTL access token local" — Open Question 1 của
RESEARCH.md cần con số này để Phase 2 biết claim cũ sống bao lâu sau khi đổi vai trò.

Thêm vào `.gitignore` (nếu `supabase init` chưa tự thêm):
```
/supabase/.branches
/supabase/.temp
```
  </action>
  <verify>npx supabase start && npx supabase status</verify>
  <acceptance_criteria>
    - supabase/config.toml tồn tại và chứa `project_id = "kho-minh-vu"`
    - supabase/config.toml chứa chuỗi `[auth.hook.custom_access_token]`
    - supabase/README.md tồn tại và chứa chuỗi `jwt_expiry`
    - `npx supabase status` exit 0 và in ra API URL localhost
  </acceptance_criteria>
  <done>Stack local chạy, `npx supabase status` trả về API URL và DB URL</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0001 — extension và hàm dùng chung</name>
  <files>supabase/migrations/0001_extensions.sql</files>
  <read_first>supabase/config.toml, .planning/phases/01-nen-du-lieu/01-RESEARCH.md</read_first>
  <action>
Tạo `supabase/migrations/0001_extensions.sql`.

**Quy ước tên file migration khóa cho TOÀN DỰ ÁN:** `NNNN_ten_khong_dau.sql`, bốn chữ số,
tăng dần. Supabase CLI parse phần số làm version nên hợp lệ. Ghi quy ước này vào
`supabase/README.md`.

Nội dung:

```sql
create extension if not exists "uuid-ossp" with schema extensions;
create extension if not exists unaccent with schema extensions;
create extension if not exists pg_trgm with schema extensions;

-- Wrapper IMMUTABLE bắt buộc dạng 2 tham số: dạng 1 tham số phụ thuộc search_path
-- để tìm dictionary nên không thật sự immutable, index dựng trên nó có thể sai.
create or replace function public.f_unaccent(text)
returns text
language sql
immutable
parallel safe
strict
as $$
  select extensions.unaccent('extensions.unaccent'::regdictionary, $1);
$$;

-- Hàm trigger dùng chung cho mọi bảng có cột updated_at.
create or replace function public.update_updated_at()
returns trigger
language plpgsql
as $$
begin
  new.updated_at := now();
  return new;
end;
$$;
```

**KHÔNG** tạo extension `pg_cron` ở đây — pg_cron trên Supabase local Docker không đáng tin
(RESEARCH.md Pitfall 6). Nó được xử lý riêng ở plan 07 với fallback.

Dùng `uuid_generate_v4()` (từ uuid-ossp) làm default cho PK ở các migration sau, KHÔNG dùng
`gen_random_uuid()` — theo D-20.
  </action>
  <verify>npx supabase db reset && npx supabase db execute --stdin <<< "select public.f_unaccent('Bạc đạn xe máy');"</verify>
  <acceptance_criteria>
    - supabase/migrations/0001_extensions.sql chứa chuỗi `'extensions.unaccent'::regdictionary`
    - supabase/migrations/0001_extensions.sql chứa `create or replace function public.update_updated_at()`
    - supabase/migrations/0001_extensions.sql KHÔNG chứa chuỗi `pg_cron`
    - supabase/migrations/0001_extensions.sql KHÔNG chứa chuỗi `gen_random_uuid`
    - `npx supabase db reset` exit 0
    - Truy vấn `select public.f_unaccent('Bạc đạn')` trả về `Bac dan`
  </acceptance_criteria>
  <done>f_unaccent bỏ dấu đúng và khai báo IMMUTABLE (index được)</done>
</task>

<task type="auto">
  <name>Task 3: Migration 0002 — năm kiểu enum của miền nghiệp vụ</name>
  <files>supabase/migrations/0002_enums.sql</files>
  <read_first>supabase/migrations/0001_extensions.sql, .planning/phases/01-nen-du-lieu/01-CONTEXT.md</read_first>
  <action>
Tạo `supabase/migrations/0002_enums.sql` với đúng năm kiểu, đúng các giá trị sau (không
thêm, không bớt — giá trị lấy từ PROJECT.md và tài liệu thiết kế gốc):

```sql
create type public.loai_ct as enum (
  'NHAP', 'XUAT', 'TRA_NCC', 'TRA_KHACH', 'CHUYEN_KHO', 'KIEM_KE', 'DIEU_CHINH'
);

create type public.trang_thai_ct as enum ('NHAP_LIEU', 'HOAN_THANH', 'DA_HUY');

create type public.vai_tro as enum ('quan_ly', 'van_phong', 'thu_kho', 'chi_xem');

create type public.loai_doi_tac as enum ('NCC', 'KHACH', 'CA_HAI');

create type public.trang_thai_ddh as enum (
  'MOI', 'DA_XUAT_MOT_PHAN', 'DA_XUAT_DU', 'DA_HUY'
);
```

Quy ước đặt tên giá trị: `loai_ct`/`trang_thai_ct`/`loai_doi_tac`/`trang_thai_ddh` dùng
CHỮ HOA (là mã nghiệp vụ, xuất hiện trên chứng từ); `vai_tro` dùng chữ thường (là khóa kỹ
thuật, đi vào JWT claim). Giữ nguyên sự khác biệt này, đừng "chuẩn hóa" lại.
  </action>
  <verify>npx supabase db reset</verify>
  <acceptance_criteria>
    - supabase/migrations/0002_enums.sql chứa `create type public.loai_ct as enum`
    - File chứa đủ 7 giá trị loai_ct: NHAP, XUAT, TRA_NCC, TRA_KHACH, CHUYEN_KHO, KIEM_KE, DIEU_CHINH
    - File chứa `'quan_ly', 'van_phong', 'thu_kho', 'chi_xem'`
    - `npx supabase db reset` exit 0
    - `select count(*) from pg_type where typname in ('loai_ct','trang_thai_ct','vai_tro','loai_doi_tac','trang_thai_ddh')` trả về 5
  </acceptance_criteria>
  <done>Năm enum tồn tại với đúng bộ giá trị</done>
</task>

<task type="auto">
  <name>Task 4: npm script cho vòng đời DB local</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
Thêm vào `scripts` của `package.json` (giữ nguyên các script đang có):

```json
"db:start": "supabase start",
"db:stop": "supabase stop",
"db:reset": "supabase db reset",
"db:test": "supabase db reset && supabase test db",
"db:types:local": "supabase gen types typescript --local --schema public > src/types/database.types.ts"
```

Giữ nguyên script `db:types` đang có (trỏ tới cloud qua `SUPABASE_PROJECT_ID`) — hai script
tồn tại song song có chủ đích: `db:types:local` dùng lúc phát triển, `db:types` dùng khi đã
push lên cloud.

Thêm `supabase` vào `devDependencies` để khỏi phụ thuộc `npx` tải lại mỗi lần:
`npm install --save-dev supabase@2.117.0`
  </action>
  <verify>npm run db:reset</verify>
  <acceptance_criteria>
    - package.json chứa `"db:reset": "supabase db reset"`
    - package.json chứa `"db:test": "supabase db reset && supabase test db"`
    - package.json chứa `"db:types:local"` VÀ vẫn còn `"db:types"` cũ
    - package.json devDependencies chứa `"supabase"`
    - `npm run db:reset` exit 0
  </acceptance_criteria>
  <done>Chạy được toàn bộ vòng đời DB bằng npm script, không cần nhớ lệnh supabase</done>
</task>

</tasks>

<verification>
- [ ] `npm run db:reset` exit 0
- [ ] `select public.f_unaccent('Bạc đạn')` trả `Bac dan`
- [ ] 5 enum tồn tại trong pg_type
- [ ] supabase/config.toml có khối `[auth.hook.custom_access_token]`
- [ ] supabase/README.md ghi lại jwt_expiry và quy ước tên migration
</verification>

<success_criteria>
- Tất cả task hoàn thành
- Stack Supabase local chạy được và reset sạch
- f_unaccent dùng được trong biểu thức index (IMMUTABLE, dạng 2 tham số)
- Không có pg_cron và không có gen_random_uuid trong migration nào
</success_criteria>

<output>
After completion, create `.planning/phases/01-nen-du-lieu/01-01-SUMMARY.md`
</output>
