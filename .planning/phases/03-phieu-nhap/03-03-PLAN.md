---
phase: 03-phieu-nhap
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0043_gia_von_dau_ky.sql
  - supabase/tests/91_gia_von_dau_ky_test.sql
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Quản lý đặt được giá vốn đầu kỳ cho mã đang có giá vốn 0"
    - "Mã đã có giá vốn khác 0 thì KHÔNG đè được — bình quân gia quyền không bị ghi đè"
    - "Văn phòng, thủ kho, chỉ xem gọi đều bị 42501"
    - "Mỗi lần đặt ghi vào nhat_ky_sua với nguồn gia_von_dau_ky"
  artifacts:
    - path: "supabase/migrations/0043_gia_von_dau_ky.sql"
      provides: "RPC dat_gia_von_dau_ky"
  key_links:
    - from: "dat_gia_von_dau_ky"
      to: "san_pham.gia_von"
      via: "SECURITY DEFINER, chỉ khi gia_von = 0"
---

<objective>
D-02. **Đây là việc ngoài NHAP-01..08** — người dùng muốn nạp giá vốn đầu kỳ bằng Excel.
Không ghi thẳng `san_pham.gia_von` (Phase 1 đã thu quyền đó của client, cố ý). Cửa duy nhất
là RPC có ba lớp chặn: vai trò, giá vốn phải đang 0, và ghi nhật ký.

Cắt được: nếu cần gọn phạm vi roadmap thì bỏ plan 03 và 12, dồn sang kiểm kê đầu kỳ Phase 6.
</objective>

<context>
@.planning/phases/03-phieu-nhap/03-CONTEXT.md
@supabase/migrations/0029_an_gia_von.sql
@supabase/migrations/0027_nhat_ky_sua.sql
@supabase/tests/90_gia_von_test.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test 91 đỏ trước</name>
  <files>supabase/tests/91_gia_von_dau_ky_test.sql</files>
  <action>
`plan(6)`:
1. quanly đặt giá vốn cho mã đang 0 → `is(gia_von, giá đặt)`.
2. Đặt lần hai cho chính mã đó (giờ đã khác 0) → `throws_ok(…, '23514')`.
3. vanphong gọi → `throws_ok(…, '42501')`.
4. thukho1 gọi → `throws_ok(…, '42501')`.
5. Mã không tồn tại → `throws_ok(…, '23514')`.
6. `is(count nhat_ky_sua nguồn 'gia_von_dau_ky' của mã đó, 1)`.
  </action>
  <verify><automated>psql "$DATABASE_URL" -f supabase/tests/91_gia_von_dau_ky_test.sql</automated></verify>
  <acceptance_criteria>
    - `plan(6)`, đỏ trước migration
  </acceptance_criteria>
  <done>Hợp đồng giá vốn đầu kỳ viết trước</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0043</name>
  <files>supabase/migrations/0043_gia_von_dau_ky.sql</files>
  <read_first>supabase/migrations/0029_an_gia_von.sql, supabase/migrations/0027_nhat_ky_sua.sql</read_first>
  <action>
```sql
create or replace function public.dat_gia_von_dau_ky(p_du_lieu jsonb)
returns jsonb language plpgsql security definer set search_path = '' as $$
```
Nhận mảng `[{ma_hang, gia_von}]`. Với mỗi phần tử:
- vai trò khác `quan_ly` → 42501 ngay dòng đầu, trước khi đụng dữ liệu.
- mã không có → gom vào danh sách lỗi.
- `gia_von <> 0` → gom vào danh sách bỏ qua, **không** đè.
- `gia_von <= 0` trong dữ liệu vào → lỗi (giá vốn đầu kỳ phải dương).
Trả `{dat: n, bo_qua: n, loi: [{ma_hang, ly_do}]}` — cùng hình dạng với `nhap_danh_muc` để
giao diện xem trước (plan 12) dùng lại được khuôn.

`set_config('app.nguon_sua','gia_von_dau_ky', true)` trước khi update để trigger nhật ký ghi
đúng nguồn. `revoke all … from public, anon;` `grant execute … to authenticated;`
(kiểm vai trò nằm trong thân hàm).
  </action>
  <verify><automated>npm run db:push && psql "$DATABASE_URL" -f supabase/tests/91_gia_von_dau_ky_test.sql</automated></verify>
  <acceptance_criteria>
    - Test 91: 6/6 xanh
    - `grep -c "gia_von" supabase/migrations/0043*.sql` > 0 và hàm có `security definer`
    - Toàn bộ pgTAP không đỏ bài nào
  </acceptance_criteria>
  <done>Có cửa hợp lệ để nạp giá vốn đầu kỳ</done>
</task>

</tasks>

<verification>
- [ ] pgTAP toàn bộ xanh; không đường nào ghi `san_pham.gia_von` ngoài trigger và RPC này
</verification>

<output>
Tạo `.planning/phases/03-phieu-nhap/03-03-SUMMARY.md`
</output>
