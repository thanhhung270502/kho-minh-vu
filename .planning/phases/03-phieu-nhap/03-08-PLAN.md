---
phase: 03-phieu-nhap
plan: 08
type: execute
wave: 5
depends_on: ["03-07"]
files_modified:
  - src/features/nhap-kho/components/bang-dong-nhap.tsx
  - src/features/nhap-kho/components/o-tim-ma-hang.tsx
autonomous: true
requirements: [NHAP-02, NHAP-08]
must_haves:
  truths:
    - "Gõ mã → Enter → nhảy vào ô số lượng → đơn giá → Enter sang dòng mới, không rời bàn phím"
    - "Mỗi dòng lưu lên server ngay khi đủ mã + số lượng"
    - "Chọn được kho riêng cho từng dòng, mặc định là kho của phiếu"
    - "Bảng 48 dòng vẫn gõ mượt"
  artifacts:
    - path: "src/features/nhap-kho/components/bang-dong-nhap.tsx"
      provides: "bảng dòng nhập bằng bàn phím"
  key_links:
    - from: "o-tim-ma-hang"
      to: "rpc tim_san_pham"
      via: "debounce 250ms, filterOption=false"
---

<objective>
D-07 + D-05. Đây là màn quyết định tốc độ nhập liệu: 7,6 dòng mỗi phiếu, phiếu lớn nhất
48 dòng. Bàn phím là chính, chuột là phụ.
</objective>

<context>
@.planning/phases/03-phieu-nhap/03-RESEARCH.md
@src/features/danh-muc/components/o-sua-nhanh.tsx
@src/features/danh-muc/components/cot-san-pham.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ô tìm mã hàng</name>
  <files>src/features/nhap-kho/components/o-tim-ma-hang.tsx</files>
  <read_first>src/features/danh-muc/api/san-pham.api.ts</read_first>
  <action>
`Select showSearch filterOption={false}` gọi `tim_san_pham` (debounce 250ms, giới hạn 20).
Mỗi lựa chọn hiện: mã (mono) · tên · ĐVT · "lần cuối nhập/xuất" nếu `lan_phat_sinh_cuoi` có.
Chọn xong `onChon(sanPham)` rồi tự xóa ô để gõ mã kế tiếp.
`notFoundContent`: đang tìm → "Đang tìm…"; không thấy → "Không có mã nào khớp. Kiểm tra lại
hoặc tạo mã mới ở Danh mục." (không tạo mã tắt trong phiếu nhập — ngoài phạm vi).
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria><![CDATA[- Chứa `tim_san_pham` và `filterOption={false}`]]></acceptance_criteria>
  <done>Ô tìm mã sẵn sàng</done>
</task>

<task type="auto">
  <name>Task 2: Bảng dòng nhập</name>
  <files>src/features/nhap-kho/components/bang-dong-nhap.tsx</files>
  <action>
`Table` các dòng đã lưu + một **dòng nhập mới** luôn ở cuối. Cột: STT · Mã hàng · Tên hàng ·
ĐVT · Kho (`Select`, mặc định kho phiếu, chỉ hiện khi có ≥ 2 kho) · Số lượng (`InputNumber`) ·
Đơn giá (`InputNumber`, định dạng nghìn) · Thành tiền (tính khi render, **không** giữ state) ·
nút Xóa dòng.

Luồng bàn phím: chọn mã ở dòng mới → focus ô Số lượng → Enter → focus Đơn giá → Enter →
lưu dòng (`useThemDong`) → dòng mới trống, focus về ô tìm mã. Escape ở ô nào thì bỏ dòng đang gõ.
Dùng `useRef` cho từng ô để `focus()`; không dùng thư viện mới.

Sửa dòng đã lưu: sửa tại chỗ như `o-sua-nhanh.tsx`, `onBlur` là lưu, lỗi thì hoàn lại giá trị cũ.
Phiếu `HOAN_THANH` → bảng chỉ đọc, không có dòng nhập mới, không nút Xóa.

Chân bảng: tổng số dòng, tổng số lượng, tổng tiền. Cảnh báo `Alert warning` khi có dòng
`don_gia = 0` — nói rõ "Ghi sổ sẽ bị chặn cho tới khi điền đơn giá" (D-04).
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - Chứa `useRef`, `"Enter"`, `kho_id`
    - Không tính thành tiền bằng `useState`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>NHAP-02 dùng được, gõ không rời bàn phím</done>
</task>

</tasks>

<verification>
- [ ] Thử tay: nhập 10 dòng liên tiếp chỉ bằng bàn phím, ghi thời gian vào SUMMARY
</verification>

<output>
Tạo `.planning/phases/03-phieu-nhap/03-08-SUMMARY.md`
</output>
