---
phase: 03-phieu-nhap
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - supabase/migrations/0044_rpc_chung_tu.sql
  - supabase/tests/21_danh_sach_chung_tu_test.sql
autonomous: true
requirements: [NHAP-01]
user_setup: []
must_haves:
  truths:
    - "Danh sách phiếu lọc theo loại, trạng thái, NCC, kho, khoảng ngày; phân trang server"
    - "Chi tiết phiếu trả header + dòng kèm tên mã, ĐVT, tên kho của từng dòng"
    - "Thủ kho chỉ thấy phiếu thuộc kho mình (theo header HOẶC theo dòng)"
    - "Vai trò không được xem giá vốn nhận null, RPC không lộ qua đường vòng"
  artifacts:
    - path: "supabase/migrations/0044_rpc_chung_tu.sql"
      provides: "danh_sach_chung_tu, chi_tiet_chung_tu"
  key_links:
    - from: "chi_tiet_chung_tu"
      to: "chung_tu_dong.kho_id"
      via: "coalesce(dòng, header) khi trả tên kho"
---

<objective>
Hai RPC đọc cho màn phiếu nhập, cùng khuôn với `danh_sach_san_pham` của Phase 2: lọc + phân
trang chạy ở server, trả `tong_so_dong` ở mỗi dòng.
</objective>

<context>
@.planning/phases/03-phieu-nhap/03-RESEARCH.md
@supabase/migrations/0030_danh_sach_san_pham.sql
@supabase/migrations/0016_rls_chung_tu.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test 21 đỏ trước</name>
  <files>supabase/tests/21_danh_sach_chung_tu_test.sql</files>
  <action>
`plan(8)`. Dựng 3 phiếu nhập (2 hoàn thành, 1 nhập liệu) ở K1 và K2:
1. Lọc `p_loai_ct = 'NHAP'` trả đúng 3.
2. Lọc `p_trang_thai = 'NHAP_LIEU'` trả đúng 1.
3. Lọc theo `p_doi_tac_id` trả đúng số phiếu của NCC đó.
4. Lọc khoảng ngày cắt đúng.
5. `tong_so_dong` khớp tổng thật khi `p_kich_thuoc` nhỏ hơn tổng.
6. `chi_tiet_chung_tu` trả đủ số dòng và **tên kho theo từng dòng**.
7. thukho2 gọi `danh_sach_chung_tu` chỉ thấy phiếu có liên quan K2.
8. thukho1 gọi `chi_tiet_chung_tu` của phiếu K2 → 0 dòng hoặc 42501 (chốt hành vi nào rồi assert).
  </action>
  <verify><automated>psql "$DATABASE_URL" -f supabase/tests/21_danh_sach_chung_tu_test.sql</automated></verify>
  <acceptance_criteria><![CDATA[- `plan(8)`, đỏ trước migration]]></acceptance_criteria>
  <done>Hợp đồng RPC đọc viết trước</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0044</name>
  <files>supabase/migrations/0044_rpc_chung_tu.sql</files>
  <read_first>supabase/migrations/0030_danh_sach_san_pham.sql</read_first>
  <action>
`danh_sach_chung_tu(p_loai_ct, p_trang_thai, p_doi_tac_id, p_kho_id, p_tu_ngay, p_den_ngay,
p_nguon_nhap, p_tu_khoa, p_trang, p_kich_thuoc)` → `so_ct, ngay_ct, loai_ct, nguon_nhap,
trang_thai, ten_doi_tac, ten_kho, so_dong, tong_so_luong, tong_tien, nguoi_tao, tong_so_dong`.
`p_tu_khoa` tìm theo `so_ct` và tên đối tác, bỏ dấu (dùng `f_unaccent` như `tim_san_pham`).

`chi_tiet_chung_tu(p_id)` → header + mảng dòng: `san_pham_id, ma_hang, ten_hang, ten_dvt,
so_luong, don_gia, thanh_tien, kho_id, ten_kho` với `ten_kho` lấy theo
`coalesce(dong.kho_id, ct.kho_id)`.

Cả hai `security definer` + `set search_path = ''` (bắt buộc vì `san_pham` đã thu quyền cột),
và **tự kiểm vai trò + phạm vi kho của thủ kho** ngay trong thân hàm — SECURITY DEFINER bỏ
qua RLS nên không kiểm là lộ dữ liệu kho khác.
Không trả `gia_von` ở cả hai hàm — màn phiếu nhập không cần.
  </action>
  <verify><automated>npm run db:push && psql "$DATABASE_URL" -f supabase/tests/21_danh_sach_chung_tu_test.sql && npm run db:types</automated></verify>
  <acceptance_criteria>
    - Test 21: 8/8 xanh, toàn bộ pgTAP không đỏ
    - Hai hàm đều có `security definer` và khối kiểm vai trò ở đầu
    - `npm run db:types` sinh lại, `npm run check` exit 0
  </acceptance_criteria>
  <done>Màn phiếu nhập có nguồn dữ liệu</done>
</task>

</tasks>

<output>
Tạo `.planning/phases/03-phieu-nhap/03-04-SUMMARY.md`
</output>
