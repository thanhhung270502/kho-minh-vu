---
phase: 03-phieu-nhap
plan: 05
type: execute
wave: 3
depends_on: ["03-04"]
files_modified:
  - src/features/nhap-kho/types.ts
  - src/features/nhap-kho/schemas/phieu-nhap.schema.ts
  - src/features/nhap-kho/api/phieu-nhap.api.ts
  - src/features/nhap-kho/api/phieu-nhap.keys.ts
  - src/features/nhap-kho/hooks/usePhieuNhap.ts
autonomous: true
requirements: [NHAP-01, NHAP-02, NHAP-03, NHAP-05]
user_setup: []
must_haves:
  truths:
    - "Mọi hàm gọi Supabase kiểm error rồi throw; không hàm nào select('*')"
    - "Bộ lọc danh sách phiếu đọc/ghi được từ URL, quay vòng không mất giá trị"
    - "Mutation ghi/hủy invalidate đúng khóa, kể cả tồn kho và thẻ kho của mã liên quan"
  artifacts:
    - path: "src/features/nhap-kho/api/phieu-nhap.api.ts"
      provides: "lớp gọi dữ liệu thuần cho phiếu nhập"
  key_links:
    - from: "useGhiSo"
      to: "rpc ghi_so_chung_tu"
      via: "mutateAsync, onSuccess invalidate san-pham + nhap-kho"
---

<objective>
Lớp dữ liệu cho phiếu nhập, đúng khuôn `features/danh-muc` của Phase 2 (CLAUDE.md Bước 2–4).
Không JSX, không hook trong file api.
</objective>

<context>
@CLAUDE.md
@src/features/danh-muc/api/san-pham.api.ts
@src/features/danh-muc/schemas/bo-loc.schema.ts
@src/features/doi-tac/hooks/useDoiTac.ts
@.memory/patterns/nextjs-antd-supabase-ui.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kiểu, schema, bộ lọc URL</name>
  <files>src/features/nhap-kho/types.ts, src/features/nhap-kho/schemas/phieu-nhap.schema.ts</files>
  <action>
`types.ts`: `DongDanhSachPhieu`, `ChiTietPhieu`, `DongPhieu` suy từ `Database["public"]["Functions"]`.
`NguonNhap = Database["public"]["Enums"]["nguon_nhap"]`, nhãn `NHAN_NGUON_NHAP`.

`phieu-nhap.schema.ts`:
- `dauPhieuSchema`: `doi_tac_id` uuid bắt buộc, `kho_id` uuid bắt buộc, `ngay_ct` date,
  `nguon_nhap` enum, `ghi_chu` optional → null.
- `dongPhieuSchema`: `san_pham_id` uuid, `so_luong` > 0, `don_gia` ≥ 0, `kho_id` nullable.
  **Không** `z.coerce.number()` (bẫy 11 — antd InputNumber đã trả number).
- `BoLocPhieu` + `docBoLocPhieu` / `ghiBoLocPhieu` cùng khuôn `bo-loc.schema.ts`, nhớ bẫy
  `Number(null) === 0` và "tất cả" phải gửi null tường minh.
- Thêm assert quay vòng bộ lọc vào `scripts/kiem-tra-ham-thuan.ts`.
  </action>
  <verify><automated>npx tsx scripts/kiem-tra-ham-thuan.ts && npm run typecheck</automated></verify>
  <acceptance_criteria>
    - Không `z.coerce` trong schema; hàm thuần xanh
  </acceptance_criteria>
  <done>Kiểu và bộ lọc sẵn sàng</done>
</task>

<task type="auto">
  <name>Task 2: api + keys + hooks</name>
  <files>src/features/nhap-kho/api/phieu-nhap.api.ts, src/features/nhap-kho/api/phieu-nhap.keys.ts, src/features/nhap-kho/hooks/usePhieuNhap.ts</files>
  <action>
api: `layDanhSachPhieu(boLoc)`, `layChiTietPhieu(id)`, `taoPhieuNhap(dau)` (insert `chung_tu`
kèm `so_ct` từ `sinh_so_ct` — gọi RPC chứ không tự sinh), `capNhatDauPhieu(id, dau)`,
`themDong(chungTuId, dong)`, `capNhatDong(id, dong)`, `xoaDong(id)`, `ghiSo(id)`, `huyPhieu(id, lyDo)`.
Mọi hàm `if (error) throw error`. `update`/`delete` kiểm `count` (bẫy 6).

keys: `khoaPhieuNhap = { tatCa, danhSach(b), chiTiet(id) }`.

hooks: `useDanhSachPhieu` (`keepPreviousData`), `useChiTietPhieu(id)` (**`enabled: id !== ""`** —
bẫy 10), `useTaoPhieu`, `useSuaDauPhieu`, `useThemDong`, `useSuaDong`, `useXoaDong`,
`useGhiSo`, `useHuyPhieu`.
Ghi sổ và hủy invalidate: `khoaPhieuNhap.tatCa` **và** `khoaSanPham.tatCa` (tồn + giá vốn đổi)
**và** `["san-pham","the-kho"]`.
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - `grep -c "select('\*')" src/features/nhap-kho/` = 0
    - `useChiTietPhieu` có `enabled`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>Giao diện có thể bắt đầu</done>
</task>

</tasks>

<output>
Tạo `.planning/phases/03-phieu-nhap/03-05-SUMMARY.md`
</output>
