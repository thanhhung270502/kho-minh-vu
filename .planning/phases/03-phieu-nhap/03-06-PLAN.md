---
phase: 03-phieu-nhap
plan: 06
type: execute
wave: 4
depends_on: ["03-05"]
files_modified:
  - src/app/(app)/nhap-kho/page.tsx
  - src/features/nhap-kho/components/bang-phieu-nhap.tsx
  - src/features/nhap-kho/components/panel-loc-phieu.tsx
  - src/features/nhap-kho/components/thanh-cong-cu-phieu.tsx
  - src/features/nhap-kho/components/noi-dung-bang-phieu.tsx
autonomous: true
requirements: [NHAP-01]
must_haves:
  truths:
    - "Danh sách phiếu nhập lọc theo trạng thái, NCC, kho, khoảng ngày; bộ lọc nằm trên URL"
    - "Đủ bốn trạng thái: skeleton, lỗi có nút Thử lại, rỗng có hướng dẫn, dữ liệu"
    - "Chỉ xem không thấy nút Tạo phiếu"
  artifacts:
    - path: "src/features/nhap-kho/components/bang-phieu-nhap.tsx"
      provides: "bảng phiếu nhập"
---

<objective>
Màn danh sách `/nhap-kho`.

⚠️ **Khuôn đã đổi sau khi lập kế hoạch.** Một phiên khác đã dựng lại bố cục danh sách theo
giao diện KiotViet (commit `39da902`, `4006200`): panel lọc cố định bên trái 264px, bảng
trong card trắng, dưới 992px panel sập vào ngăn kéo đáy. Plan này bám khuôn MỚI:

- `BoCucDanhSach` (`src/shared/components/bo-cuc-danh-sach.tsx`) — bố cục ngoài
- `panel-loc-*.tsx` — điều kiện lọc; `thanh-cong-cu-*.tsx` — ô tìm + nút; `noi-dung-bang-*.tsx` — bảng
- `demDieuKien*()` trong schema bộ lọc → số hiện trên nút "Bộ lọc (n)" ở mobile
- Token: `rounded-the`, `bg-nen-the`, `shadow-the`, `bg-nen-tong`
</objective>

<context>
@CLAUDE.md
@src/features/danh-muc/components/bang-san-pham.tsx
@src/features/danh-muc/components/panel-loc-san-pham.tsx
@src/features/danh-muc/components/thanh-cong-cu-san-pham.tsx
@src/features/danh-muc/components/noi-dung-bang-san-pham.tsx
@src/shared/components/bo-cuc-danh-sach.tsx
@src/shared/components/query-state.tsx
@.memory/patterns/nextjs-antd-supabase-ui.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trang + panel lọc + thanh công cụ</name>
  <files>src/app/(app)/nhap-kho/page.tsx, src/features/nhap-kho/components/panel-loc-phieu.tsx, src/features/nhap-kho/components/thanh-cong-cu-phieu.tsx</files>
  <action>
page (Server): `yeuCauQuyen("xem_danh_muc")`, `PageHeader tieuDe="Phiếu nhập"`,
`<Suspense>` bọc bảng (bẫy: `useSearchParams` cần ranh giới Suspense).
Truyền `coQuyenTao = coQuyen(nd.vaiTro, "sua_danh_muc")`.

`panel-loc-phieu.tsx` (trong panel trái): trạng thái, NCC (`danh_sach_doi_tac` lọc NCC),
kho, nguồn nhập, `DatePicker.RangePicker` mặc định 30 ngày gần nhất, nút "Xóa bộ lọc".
`thanh-cong-cu-phieu.tsx`: ô tìm số phiếu debounce 300ms + khe `nutThem`.
Thêm `demDieuKienPhieu(b)` vào schema (plan 05) — đếm điều kiện đang bật, KHÔNG tính ô tìm.
Đồng bộ ô tìm với URL bằng cách chỉnh state **trong lúc render**, không `useEffect`
(lint `react-hooks/set-state-in-effect` — đã vấp ở Phase 2).
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - page chứa `yeuCauQuyen(` và `<Suspense`
    - thanh lọc chứa `300`
  </acceptance_criteria>
  <done>Khung màn danh sách xong</done>
</task>

<task type="auto">
  <name>Task 2: Bảng phiếu</name>
  <files>src/features/nhap-kho/components/bang-phieu-nhap.tsx, src/features/nhap-kho/components/noi-dung-bang-phieu.tsx</files>
  <action>
`bang-phieu-nhap.tsx` giữ state + bộ lọc URL rồi bọc `BoCucDanhSach`;
`noi-dung-bang-phieu.tsx` chỉ nhận dữ liệu và vẽ `Table`.
`QueryState` + `Table` `rowKey="id"` `size="small"` `sticky` `scroll={{ x: 1000 }}`
(KHÔNG tự bọc `overflow-x-auto` — `BoCucDanhSach` đã bọc).
Cột: Số phiếu (link `/nhap-kho/[id]`, mono) · Ngày · Nguồn (`Tag`: NCC xanh dương,
Nhà máy tím) · Nhà cung cấp (ellipsis) · Kho · Số dòng (right) · Tổng số lượng (right) ·
Trạng thái (`Tag`: Nhập liệu vàng, Hoàn thành xanh, Đã hủy xám) · Người tạo.
Phân trang server 50, `showTotal: t => `${t} phiếu``.
Trang > 1 mà rỗng → `router.replace` về trang 1 (**không** setState trong effect).
Rỗng: có bộ lọc → "Không có phiếu nào khớp bộ lọc" + nút Xóa bộ lọc; không lọc →
"Chưa có phiếu nhập nào. Bấm Tạo phiếu nhập để ghi chuyến hàng đầu tiên."
Nút "Tạo phiếu nhập" chỉ khi `coQuyenTao`.
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - Chứa `QueryState`, `docBoLocPhieu`, `scroll: false`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>NHAP-01 phần danh sách xong</done>
</task>

</tasks>

<output>
Tạo `.planning/phases/03-phieu-nhap/03-06-SUMMARY.md`
</output>
