---
phase: 03-phieu-nhap
plan: 06
type: execute
wave: 4
depends_on: ["03-05"]
files_modified:
  - src/app/(app)/nhap-kho/page.tsx
  - src/features/nhap-kho/components/bang-phieu-nhap.tsx
  - src/features/nhap-kho/components/thanh-loc-phieu.tsx
autonomous: true
requirements: [NHAP-01]
must_haves:
  truths:
    - "Danh sách phiếu nhập lọc theo trạng thái, NCC, kho, khoảng ngày; bộ lọc nằm trên URL"
    - "Đủ bốn trạng thái: skeleton, lỗi có nút Thử lại, rỗng có hướng dẫn, dữ liệu"
    - "Chỉ xem không thấy nút Tạo phiếu"
  artifacts:
    - path: "src/features/nhap-kho/components/bang-phieu-nhap.tsx"
      provides: "bảng phiếu nhập"
---

<objective>
Màn danh sách `/nhap-kho`, đúng khuôn `bang-san-pham.tsx` của Phase 2.
</objective>

<context>
@CLAUDE.md
@src/features/danh-muc/components/bang-san-pham.tsx
@src/features/danh-muc/components/thanh-loc-san-pham.tsx
@src/shared/components/query-state.tsx
@.memory/patterns/nextjs-antd-supabase-ui.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trang + thanh lọc</name>
  <files>src/app/(app)/nhap-kho/page.tsx, src/features/nhap-kho/components/thanh-loc-phieu.tsx</files>
  <action>
page (Server): `yeuCauQuyen("xem_danh_muc")`, `PageHeader tieuDe="Phiếu nhập"`,
`<Suspense>` bọc bảng (bẫy: `useSearchParams` cần ranh giới Suspense).
Truyền `coQuyenTao = coQuyen(nd.vaiTro, "sua_danh_muc")`.

thanh-loc: `Segmented` trạng thái (Tất cả / Nhập liệu / Hoàn thành / Đã hủy),
`Select` NCC (dùng `danh_sach_doi_tac` lọc NCC), `Select` kho, `DatePicker.RangePicker`
mặc định 30 ngày gần nhất, ô tìm số phiếu debounce 300ms.
Đồng bộ ô tìm với URL bằng cách chỉnh state **trong lúc render**, không `useEffect`
(lint `react-hooks/set-state-in-effect` — đã vấp ở Phase 2).
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - page chứa `yeuCauQuyen(` và `<Suspense`
    - thanh lọc chứa `300`
  </acceptance_criteria>
  <done>Khung màn danh sách xong</done>
</task>

<task type="auto">
  <name>Task 2: Bảng phiếu</name>
  <files>src/features/nhap-kho/components/bang-phieu-nhap.tsx</files>
  <action>
`QueryState` + `Table` `rowKey="id"` `size="small"` `sticky` `scroll={{ x: 1000 }}`.
Cột: Số phiếu (link `/nhap-kho/[id]`, mono) · Ngày · Nguồn (`Tag`: NCC xanh dương,
Nhà máy tím) · Nhà cung cấp (ellipsis) · Kho · Số dòng (right) · Tổng số lượng (right) ·
Trạng thái (`Tag`: Nhập liệu vàng, Hoàn thành xanh, Đã hủy xám) · Người tạo.
Phân trang server 50, `showTotal: t => `${t} phiếu``.
Trang > 1 mà rỗng → `router.replace` về trang 1 (**không** setState trong effect).
Rỗng: có bộ lọc → "Không có phiếu nào khớp bộ lọc" + nút Xóa bộ lọc; không lọc →
"Chưa có phiếu nhập nào. Bấm Tạo phiếu nhập để ghi chuyến hàng đầu tiên."
Nút "Tạo phiếu nhập" chỉ khi `coQuyenTao`.
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - Chứa `QueryState`, `docBoLocPhieu`, `scroll: false`
    - `npm run check` exit 0
  </acceptance_criteria>
  <done>NHAP-01 phần danh sách xong</done>
</task>

</tasks>

<output>
Tạo `.planning/phases/03-phieu-nhap/03-06-SUMMARY.md`
</output>
