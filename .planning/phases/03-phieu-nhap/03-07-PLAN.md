---
phase: 03-phieu-nhap
plan: 07
type: execute
wave: 4
depends_on: ["03-05", "03-02"]
files_modified:
  - src/app/(app)/nhap-kho/[id]/page.tsx
  - src/features/nhap-kho/components/dau-phieu-nhap.tsx
  - src/features/nhap-kho/components/nut-tao-phieu.tsx
autonomous: true
requirements: [NHAP-01, NHAP-07, NHAP-08]
must_haves:
  truths:
    - "Bấm Tạo phiếu là sinh ngay chứng từ NHAP_LIEU có số thật trên server"
    - "Chọn nguồn Nhà máy thì số phiếu mang tiền tố riêng"
    - "Phiếu HOAN_THANH mở ra chỉ đọc, không ô nào sửa được"
    - "Đổi đầu phiếu lưu ngay, không cần bấm Lưu"
  artifacts:
    - path: "src/features/nhap-kho/components/dau-phieu-nhap.tsx"
      provides: "đầu phiếu nhập"
---

<objective>
D-09: lưu thật trên server từ dòng đầu. Bấm "Tạo phiếu nhập" → chứng từ `NHAP_LIEU` có số,
chuyển sang `/nhap-kho/[id]`. Chấp nhận số phiếu nhảy khi có phiếu bỏ dở.
</objective>

<context>
@.planning/phases/03-phieu-nhap/03-CONTEXT.md
@src/features/danh-muc/components/ngan-keo-san-pham.tsx
@src/features/doi-tac/api/doi-tac.api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nút tạo phiếu</name>
  <files>src/features/nhap-kho/components/nut-tao-phieu.tsx</files>
  <action>
`Modal` nhỏ hỏi ba thứ trước khi sinh số: Nhà cung cấp (`Select` tìm server, mặc định để
trống), Kho mặc định, Nguồn (`Radio.Group`: NCC ngoài / Nhà máy — chọn NCC000001 thì tự
gợi ý Nhà máy nhưng **không ép**). Bấm Tạo → `useTaoPhieu` → `router.push('/nhap-kho/' + id)`.
Lỗi: `maLoi(e)` (bẫy 8), không `instanceof`.
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria><![CDATA[- Chứa `maLoi(` và không chứa `instanceof PostgrestError`]]></acceptance_criteria>
  <done>Tạo phiếu sinh số ngay</done>
</task>

<task type="auto">
  <name>Task 2: Trang chi tiết + đầu phiếu</name>
  <files>src/app/(app)/nhap-kho/[id]/page.tsx, src/features/nhap-kho/components/dau-phieu-nhap.tsx</files>
  <action>
page (Server): `await params` (Next 16), id không phải uuid → `notFound()`,
`yeuCauQuyen("xem_danh_muc")`, truyền quyền `{ sua, ghiSo, huy: vaiTro === "quan_ly" }`.

dau-phieu: `Descriptions` khi `HOAN_THANH`/`DA_HUY` (chỉ đọc, kèm `Tag` trạng thái và ngày
ghi sổ); khi `NHAP_LIEU` thì các ô sửa được — đổi ô nào lưu ô đó ngay (`onBlur` hoặc
`onChange` cho Select), hiện "Đã lưu" mờ bên cạnh. Ô: Nhà cung cấp, Kho mặc định, Ngày,
Nguồn nhập, Ghi chú.
Không cho đổi Nguồn nhập sau khi đã có số phiếu — số đã sinh theo nguồn; hiện chữ giải thích.
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - page chứa `await params` và `notFound()`
    - Phiếu HOAN_THANH render bằng `Descriptions`, không có `Input` nào bật
  </acceptance_criteria>
  <done>NHAP-01, NHAP-07 phần giao diện xong</done>
</task>

</tasks>

<output>
Tạo `.planning/phases/03-phieu-nhap/03-07-SUMMARY.md`
</output>
