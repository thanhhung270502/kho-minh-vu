---
phase: 03-phieu-nhap
plan: 09
type: execute
wave: 6
depends_on: ["03-08"]
files_modified:
  - src/features/nhap-kho/components/nut-ghi-so.tsx
  - src/features/nhap-kho/components/tom-tat-ghi-so.tsx
autonomous: true
requirements: [NHAP-03, NHAP-04]
must_haves:
  truths:
    - "Ghi sổ làm tồn tăng đúng từng kho và giá vốn tính lại — thấy được ngay trên màn mã hàng"
    - "Có dòng đơn giá 0 thì nút Ghi sổ khóa, nói rõ dòng nào"
    - "Phiếu đã ghi sổ không sửa được — chặn ở database, giao diện chỉ phản ánh"
    - "Ghi sổ hai lần liên tiếp không tạo hai lần movement"
  artifacts:
    - path: "src/features/nhap-kho/components/nut-ghi-so.tsx"
      provides: "nút ghi sổ + hộp xác nhận"
---

<objective>
NHAP-03, NHAP-04, D-04. `ghi_so_chung_tu` đã atomic và đã test ở Phase 1 — plan này chỉ nối
giao diện và chặn sớm những ca người dùng sẽ gặp.
</objective>

<context>
@supabase/migrations/0011_rpc_ghi_so.sql
@.planning/phases/03-phieu-nhap/03-RESEARCH.md
@src/shared/lib/errors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tóm tắt trước khi ghi sổ</name>
  <files>src/features/nhap-kho/components/tom-tat-ghi-so.tsx</files>
  <action>
Khối tóm tắt cạnh nút: số dòng, tổng số lượng, tổng tiền, danh sách kho sẽ bị ảnh hưởng
(gom từ `coalesce(dòng.kho_id, phiếu.kho_id)`).
Câu cảnh báo **trước** khi bấm: "Ghi sổ xong không sửa được. Sai thì phải hủy phiếu và lập lại,
và giá vốn đã tính sẽ không tự quay lại." — lấy từ comment của `huy_chung_tu` (RESEARCH §3).
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria><![CDATA[- Chứa chuỗi "không sửa được"]]></acceptance_criteria>
  <done>Người dùng biết hậu quả trước khi bấm</done>
</task>

<task type="auto">
  <name>Task 2: Nút ghi sổ</name>
  <files>src/features/nhap-kho/components/nut-ghi-so.tsx</files>
  <action>
- Ẩn khi phiếu không ở `NHAP_LIEU` hoặc không có quyền sửa.
- `disabled` kèm `Tooltip` giải thích khi: 0 dòng · có dòng `don_gia <= 0` (liệt kê tối đa 3
  mã đầu) · đang lưu dòng dở.
- Bấm → `Modal.confirm` chứa `<TomTatGhiSo />` → `useGhiSo().mutateAsync`.
- Thành công: `message.success("Đã ghi sổ phiếu " + so_ct)`, phiếu chuyển chỉ đọc; cache
  `san-pham` và `the-kho` đã được invalidate ở hook (plan 05).
- Lỗi: `maLoi(e) === "23514"` → hiện `e.message` nguyên văn (câu tiếng Việt do RPC soạn:
  thiếu dòng, sai trạng thái, xuất quá tồn); `42501` → "Tài khoản không có quyền ghi sổ";
  còn lại → `dienGiaiLoi`.
- Nút `loading` và `disabled` trong lúc chạy — bấm 5 lần không được ghi sổ 5 lần.
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - Chứa `"23514"`, `maLoi(`, `loading`
    - Không `instanceof PostgrestError`
  </acceptance_criteria>
  <done>NHAP-03, NHAP-04 dùng được</done>
</task>

</tasks>

<verification>
- [ ] Thử tay trên dữ liệu thật: ghi sổ một phiếu 2 dòng 2 kho, mở màn mã hàng xem tồn và
      giá vốn đổi đúng; ghi số liệu trước/sau vào SUMMARY
</verification>

<output>
Tạo `.planning/phases/03-phieu-nhap/03-09-SUMMARY.md`
</output>
