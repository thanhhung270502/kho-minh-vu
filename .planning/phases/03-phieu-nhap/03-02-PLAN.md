---
phase: 03-phieu-nhap
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0042_so_phieu_nha_may.sql
  - supabase/tests/80_cau_hinh_so_ct_test.sql
autonomous: true
requirements: [NHAP-07]
user_setup: []
must_haves:
  truths:
    - "Phiếu nhập từ nhà máy mang số riêng (PNM26-000001), phiếu NCC ngoài giữ PN26-000001"
    - "Hai chuỗi số chạy độc lập, không tranh số của nhau"
    - "Bảy dòng cấu hình đánh số cũ và màn Cài đặt hiện có không vỡ"
  artifacts:
    - path: "supabase/migrations/0042_so_phieu_nha_may.sql"
      provides: "chung_tu.nguon_nhap + đánh số theo nguồn"
  key_links:
    - from: "sinh_so_ct"
      to: "cau_hinh_so_ct"
      via: "khóa (loai_ct, nguon)"
---

<objective>
D-10: phân biệt nhập nhà máy bằng **số phiếu riêng**. Chọn đường "thêm cột nguồn", KHÔNG thêm
giá trị vào enum `loai_ct` — nhà máy là một *nhà cung cấp*, không phải một *loại chứng từ*
(`03-RESEARCH.md` §5).
</objective>

<context>
@.planning/phases/03-phieu-nhap/03-RESEARCH.md
@supabase/migrations/0009_danh_so.sql
@supabase/migrations/0028_cau_hinh_so_ct.sql
@supabase/tests/80_cau_hinh_so_ct_test.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mở rộng test 80 (đỏ trước)</name>
  <files>supabase/tests/80_cau_hinh_so_ct_test.sql</files>
  <action>
`plan(10)` → `plan(14)`. Thêm 4 assertion:
1. `sinh_so_ct('NHAP', nguồn NHA_MAY)` trả số bắt đầu `PNM`.
2. `sinh_so_ct('NHAP', nguồn NCC)` trả số bắt đầu `PN` và KHÔNG phải `PNM`.
3. Hai chuỗi đếm độc lập: gọi mỗi bên hai lần, số thứ tự của bên này không nhảy theo bên kia.
4. `is((select count(*) from public.cau_hinh_so_ct), 8::bigint)` — thêm đúng một dòng.
  </action>
  <verify><automated>psql "$DATABASE_URL" -f supabase/tests/80_cau_hinh_so_ct_test.sql</automated></verify>
  <acceptance_criteria>
    - `plan(14)`, 4 bài mới đỏ trước migration
  </acceptance_criteria>
  <done>Hợp đồng đánh số riêng viết trước</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0042</name>
  <files>supabase/migrations/0042_so_phieu_nha_may.sql</files>
  <read_first>supabase/migrations/0028_cau_hinh_so_ct.sql, supabase/migrations/0009_danh_so.sql</read_first>
  <action>
- `create type public.nguon_nhap as enum ('NCC','NHA_MAY');`
- `alter table public.chung_tu add column nguon_nhap public.nguon_nhap;` (null cho loại khác NHAP)
- `cau_hinh_so_ct` và `chuoi_so_ct` đang khóa theo `loai_ct`. Thêm cột `nguon` (nullable, mặc định
  null = dòng gốc) và đổi khóa chính/unique thành `(loai_ct, coalesce(nguon,''))` — hoặc cách
  tương đương giữ được 7 dòng cũ nguyên vẹn. Ghi rõ cách chọn trong SUMMARY.
- Chèn dòng cấu hình thứ 8: `('NHAP', 'NHA_MAY', 'PNM', 6)`.
- `create or replace sinh_so_ct(p_loai, p_nam, p_nguon default null)` — **giữ nguyên chữ ký cũ
  gọi được** (tham số mới có default) để không phá chỗ gọi hiện tại.
- Màn Cài đặt → Số chứng từ (Phase 2) đọc `danh_sach_cau_hinh_so_ct()`: cập nhật RPC để trả
  thêm cột nguồn và nhãn "Phiếu nhập · nhà máy", **không** làm vỡ 7 dòng cũ.
  </action>
  <verify><automated>npm run db:push && psql "$DATABASE_URL" -f supabase/tests/80_cau_hinh_so_ct_test.sql</automated></verify>
  <acceptance_criteria>
    - Test 80: 14/14 xanh; toàn bộ pgTAP không đỏ bài nào
    - Màn `/cai-dat/so-chung-tu` mở được, hiện 8 dòng, ví dụ số kế tiếp đúng
  </acceptance_criteria>
  <done>NHAP-07 có nền database</done>
</task>

</tasks>

<verification>
- [ ] pgTAP toàn bộ xanh; màn Số chứng từ vẫn chạy với 8 dòng
</verification>

<output>
Tạo `.planning/phases/03-phieu-nhap/03-02-SUMMARY.md`
</output>
