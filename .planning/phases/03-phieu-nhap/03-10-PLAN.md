---
phase: 03-phieu-nhap
plan: 10
type: execute
wave: 7
depends_on: ["03-09"]
files_modified:
  - supabase/migrations/0045_chi_quan_ly_huy_nhap.sql
  - supabase/tests/20_chung_tu_test.sql
  - src/features/nhap-kho/components/hop-huy-phieu.tsx
autonomous: true
requirements: [NHAP-05]
must_haves:
  truths:
    - "Chỉ quản lý hủy được phiếu nhập đã ghi sổ; văn phòng bị chặn ở DATABASE, không chỉ ở nút"
    - "Hủy sinh bút toán đảo, giữ nguyên bản ghi gốc"
    - "Người hủy được cảnh báo rằng giá vốn KHÔNG tự quay lại"
    - "Phiếu còn NHAP_LIEU thì người nhập vẫn tự hủy được"
  artifacts:
    - path: "supabase/migrations/0045_chi_quan_ly_huy_nhap.sql"
      provides: "ràng buộc D-11 ở database"
---

<objective>
NHAP-05 + D-11..D-13.

⚠️ **Cảnh báo từ nghiên cứu:** `supabase/tests/20_chung_tu_test.sql` đăng nhập `vanphong`
ở dòng 87 rồi gọi `huy_chung_tu` cho phiếu nhập đã ghi sổ ở **dòng 180**. Thêm ràng buộc này
sẽ làm bài đó ĐỎ. Plan phải sửa test, không phải nới ràng buộc.
</objective>

<context>
@supabase/migrations/0012_rpc_huy.sql
@supabase/tests/20_chung_tu_test.sql
@.planning/phases/03-phieu-nhap/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sửa test 20 cho đúng luật mới</name>
  <files>supabase/tests/20_chung_tu_test.sql</files>
  <action>
- Dòng 180: trước khi gọi `huy_chung_tu` cho `PN-TEST-1`, chuyển sang phiên `quanly`
  (`pg_temp.dang_xuat()` rồi `dang_nhap_nhu('quanly@khominhvu.local')`).
- `plan(18)` → `plan(20)`, thêm hai assertion **mới**:
  - dưới `vanphong`: `throws_ok(huy_chung_tu(phiếu nhập HOAN_THANH), '42501')`.
  - dưới `vanphong`: `lives_ok(huy_chung_tu(phiếu nhập còn NHAP_LIEU))` — D-11 chỉ siết phiếu
    đã ghi sổ.
- Các assertion sau đó nếu cần vai trò cũ thì đăng nhập lại `vanphong` cho rõ ràng.
  </action>
  <verify><automated>psql "$DATABASE_URL" -f supabase/tests/20_chung_tu_test.sql</automated></verify>
  <acceptance_criteria>
    - `plan(20)`; hai bài mới đỏ trước migration
  </acceptance_criteria>
  <done>Luật mới có test bảo vệ</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0045 + hộp hủy</name>
  <files>supabase/migrations/0045_chi_quan_ly_huy_nhap.sql, src/features/nhap-kho/components/hop-huy-phieu.tsx</files>
  <action>
`create or replace huy_chung_tu` — chép nguyên văn 0012, chèn thêm sau khối kiểm `chi_xem`:

```sql
if v_ct.loai_ct = 'NHAP' and v_ct.trang_thai = 'HOAN_THANH'
   and (select public.vai_tro_hien_tai()) <> 'quan_ly' then
  raise exception 'Chỉ quản lý được hủy phiếu nhập đã ghi sổ' using errcode = '42501';
end if;
```
Giữ nguyên comment cũ của hàm (giá vốn không tự quay lại) và thêm một dòng về luật mới.

`hop-huy-phieu.tsx`: `Modal` bắt buộc gõ lý do (`Input.TextArea`, tối thiểu 5 ký tự, chặn ở
client trước khi gửi). Trong hộp nêu rõ ba điều:
1. Tồn sẽ giảm lại bằng bút toán đảo, bản ghi gốc giữ nguyên.
2. **Giá vốn KHÔNG tự quay về số trước khi nhập** — đúng bản chất bình quân gia quyền.
3. Tồn có thể xuống âm nếu hàng đã xuất đi (D-13 chọn không chặn).
Nút Hủy phiếu chỉ hiện với `quan_ly` — nhưng vẫn xử lý lỗi 42501 tử tế vì quyền có thể vừa
bị thu (CLAUDE.md §6).
  </action>
  <verify><automated>npm run db:push && psql "$DATABASE_URL" -f supabase/tests/20_chung_tu_test.sql && npm run check</automated></verify>
  <acceptance_criteria>
    - Test 20: 20/20 xanh; toàn bộ pgTAP không đỏ bài nào
    - `hop-huy-phieu.tsx` chứa chuỗi "giá vốn" và `maLoi(`
  </acceptance_criteria>
  <done>NHAP-05 dùng được, D-11 chặn ở database</done>
</task>

</tasks>

<output>
Tạo `.planning/phases/03-phieu-nhap/03-10-SUMMARY.md`
</output>
