---
phase: 03-phieu-nhap
plan: 12
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - src/app/api/danh-muc/gia-von-dau-ky/route.ts
  - src/features/danh-muc/components/nap-gia-von.tsx
  - src/features/danh-muc/lib/mau-gia-von.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Quản lý tải file mẫu, điền giá vốn, nạp lên và XEM TRƯỚC rồi mới xác nhận"
    - "Mã đã có giá vốn khác 0 hiện ở nhóm bỏ qua, nêu rõ lý do, không bị đè"
    - "Chỉ quản lý thấy chức năng này; vai trò khác gọi thẳng route cũng bị 403"
  artifacts:
    - path: "src/app/api/danh-muc/gia-von-dau-ky/route.ts"
      provides: "route đọc file giá vốn đầu kỳ"
---

<objective>
D-02 — **việc ngoài NHAP-01..08**. Nếu cần cắt phạm vi cho đúng roadmap thì cắt plan này
(và 03) trước tiên, dồn sang kiểm kê đầu kỳ Phase 6.

Dùng lại nguyên khuôn nhập Excel danh mục của Phase 2: đọc file ở server, xem trước, rồi nạp.
</objective>

<context>
@src/app/api/danh-muc/nhap-excel/route.ts
@src/features/danh-muc/components/nhap-excel.tsx
@src/features/danh-muc/components/xem-truoc-nhap.tsx
@src/shared/lib/o-excel.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route đọc file</name>
  <files>src/app/api/danh-muc/gia-von-dau-ky/route.ts, src/features/danh-muc/lib/mau-gia-von.ts</files>
  <action>
`runtime = "nodejs"`. POST multipart giống `nhap-excel`: kiểm phiên, kiểm
`coQuyen(nd.vaiTro, "xem_gia_von") && vaiTro === "quan_ly"` → 403 nếu không phải quản lý;
giới hạn 5MB, đuôi `.xlsx`; `docSheetDau` (đã có đường dự phòng reader — bẫy đã sửa ở plan 19
Phase 2); hai cột: Mã hàng, Giá vốn.
Gọi `dat_gia_von_dau_ky(p_du_lieu, p_chi_kiem_tra)` — **plan 03 phải có tham số chỉ kiểm tra**;
nếu chưa có thì bổ sung ở plan này và ghi vào SUMMARY.
GET trả file mẫu hai cột (dùng `taoFileMau` hoặc hàm riêng gọn hơn).
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - Chứa `runtime = "nodejs"`, `dat_gia_von_dau_ky`, kiểm vai trò quản lý
  </acceptance_criteria>
  <done>Có cửa nạp file</done>
</task>

<task type="auto">
  <name>Task 2: Màn xem trước và nạp</name>
  <files>src/features/danh-muc/components/nap-gia-von.tsx</files>
  <action>
`Modal` 3 bước như `nhap-excel.tsx` (`useReducer`). Xem trước hiện ba con số: **Sẽ đặt** ·
**Bỏ qua (đã có giá vốn)** · **Lỗi**. Bảng bỏ qua ghi rõ giá vốn hiện tại để người dùng hiểu
vì sao không đè.
Nút nạp khóa khi có lỗi. Nạp xong invalidate `khoaSanPham.tatCa`.
Nối vào menu "…" cạnh nút Xuất Excel ở `/danh-muc`, **chỉ hiện với quản lý**.
Alert đầu hộp: "Chỉ đặt được cho mã đang có giá vốn 0. Sau khi mã đã có phiếu nhập thật,
giá vốn do hệ thống tính và không nạp đè được nữa."
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - Chứa `useReducer` và chuỗi "giá vốn 0"
    - Mục menu chỉ render khi vai trò là quản lý
  </acceptance_criteria>
  <done>Người dùng tự nạp được giá vốn đầu kỳ</done>
</task>

</tasks>

<verification>
- [ ] Thử tay: nạp 5 mã (3 mã giá vốn 0, 2 mã đã có giá) → 3 đặt, 2 bỏ qua; hoàn tác bằng
      cách nào ghi rõ trong SUMMARY (không có đường hoàn tác thì nói thẳng)
</verification>

<output>
Tạo `.planning/phases/03-phieu-nhap/03-12-SUMMARY.md`
</output>
