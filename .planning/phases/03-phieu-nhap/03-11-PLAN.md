---
phase: 03-phieu-nhap
plan: 11
type: execute
wave: 5
depends_on: ["03-07"]
files_modified:
  - src/app/(app)/nhap-kho/[id]/in/page.tsx
  - src/features/nhap-kho/components/mau-in-phieu-nhap.tsx
autonomous: true
requirements: [NHAP-06]
must_haves:
  truths:
    - "In ra một trang A4 sạch: không menu, không nút, không màu nền"
    - "KHÔNG có đơn giá và thành tiền trên bản in"
    - "Có đủ chỗ ký: người giao, thủ kho, người lập"
    - "Phiếu 48 dòng in sang trang thứ hai vẫn có đầu bảng"
  artifacts:
    - path: "src/features/nhap-kho/components/mau-in-phieu-nhap.tsx"
      provides: "mẫu in phiếu nhập"
---

<objective>
NHAP-06 + D-14: bản in **không hiện giá**. Nó là giấy ký nhận hàng, không phải chứng từ kế toán.
</objective>

<context>
@.planning/phases/03-phieu-nhap/03-CONTEXT.md
@src/app/(app)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route in</name>
  <files>src/app/(app)/nhap-kho/[id]/in/page.tsx</files>
  <action>
Route riêng thay vì ẩn/hiện bằng CSS trên trang chi tiết — trang chi tiết có bảng sửa được,
in ra sẽ dính ô nhập. `await params`, kiểm uuid, `yeuCauQuyen("xem_danh_muc")`.
Trang này nằm trong `(app)` nhưng CSS in ẩn app-shell: `@media print { [data-app-shell] { display: none } }`.
Nếu cách đó vướng layout thì tách sang route group riêng — ghi lý do vào SUMMARY.
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria><![CDATA[- Chứa `await params`, `notFound()`]]></acceptance_criteria>
  <done>Có đường in</done>
</task>

<task type="auto">
  <name>Task 2: Mẫu in</name>
  <files>src/features/nhap-kho/components/mau-in-phieu-nhap.tsx</files>
  <action>
Đầu trang: tên công ty (CTY TNHH SX-TM P.Tùng Xe Máy Minh Vũ), "PHIẾU NHẬP KHO", số phiếu,
ngày. Khối thông tin: nhà cung cấp, kho, nguồn nhập, ghi chú.
Bảng: STT · Mã hàng · Tên hàng · ĐVT · Kho · Số lượng. **Không cột giá.**
Chân: tổng số dòng, tổng số lượng; ba ô ký: Người giao hàng · Thủ kho · Người lập phiếu.
CSS: `@page { size: A4; margin: 12mm }`, `thead { display: table-header-group }` để lặp đầu
bảng sang trang sau, `tr { break-inside: avoid }`.
Nút "In" gọi `window.print()`, ẩn khi in.
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - Không chứa `don_gia` hay `thanh_tien`
    - Chứa `@page` và `table-header-group`
  </acceptance_criteria>
  <done>NHAP-06 dùng được</done>
</task>

</tasks>

<verification>
- [ ] Thử tay: in ra PDF một phiếu ≥ 20 dòng, kiểm đầu bảng lặp và không lộ giá
</verification>

<output>
Tạo `.planning/phases/03-phieu-nhap/03-11-SUMMARY.md`
</output>
