---
phase: 03-phieu-nhap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0041_kho_theo_dong.sql
  - supabase/tests/22_kho_theo_dong_test.sql
autonomous: true
requirements: [NHAP-01, NHAP-03]
user_setup: []
must_haves:
  truths:
    - "Một phiếu nhập có dòng về Kho 1 và dòng về Kho 2; ghi sổ đẩy tồn đúng từng kho"
    - "Dòng không chọn kho thì rơi về kho của phiếu — mọi chứng từ cũ giữ nguyên hành vi"
    - "Thủ kho thấy phiếu có dòng thuộc kho mình, kể cả khi header là kho khác"
    - "Kiểm xuất âm tính theo kho của DÒNG, không phải kho của phiếu"
  artifacts:
    - path: "supabase/migrations/0041_kho_theo_dong.sql"
      provides: "chung_tu_dong.kho_id + _ghi_so_nhap theo dòng + policy đọc mở rộng"
  key_links:
    - from: "_ghi_so_nhap"
      to: "kho_movement.kho_id"
      via: "coalesce(p_dong.kho_id, p_ct.kho_id)"
---

<objective>
D-05: một chuyến hàng chia về hai kho. Làm bằng cột **nullable** + `coalesce` để không
phải viết lại bất kỳ test nào của Phase 1 (xem `03-RESEARCH.md` §1).
</objective>

<context>
@CLAUDE.md
@.planning/phases/03-phieu-nhap/03-CONTEXT.md
@.planning/phases/03-phieu-nhap/03-RESEARCH.md
@supabase/migrations/0011_rpc_ghi_so.sql
@supabase/migrations/0016_rls_chung_tu.sql
@supabase/tests/00_helper.sql.inc
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test 22 đỏ trước</name>
  <files>supabase/tests/22_kho_theo_dong_test.sql</files>
  <read_first>supabase/tests/20_chung_tu_test.sql, supabase/tests/00_helper.sql.inc</read_first>
  <action>
`plan(6)`, chép khối helper như mọi file test khác. Dưới `vanphong`:

1. Tạo phiếu NHAP header kho K1, hai dòng: dòng A không chọn kho, dòng B chọn K2.
2. Ghi sổ. `is(tồn K1 của spA tăng đúng)` — dòng không chọn kho rơi về header.
3. `is(tồn K2 của spB tăng đúng)` — dòng chọn kho đi đúng kho đó.
4. `is(count kho_movement của phiếu, 2)` — mỗi dòng đúng một movement.
5. Dưới `thukho2` (chỉ có K2): `isnt((select count(*) from chung_tu where id = phiếu), 0)`
   — thấy được phiếu vì có dòng thuộc K2, dù header là K1.
6. Dưới `thukho1`: giá vốn spB ở K2 — `is(kho_movement.kho_id của dòng B, K2)`.

Chạy `psql -f` → phải ĐỎ ít nhất 4 bài (cột chưa có nên lỗi cú pháp là hợp lệ ở bước này;
ghi lại kết quả đỏ vào SUMMARY).
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -f supabase/tests/22_kho_theo_dong_test.sql</automated>
  </verify>
  <acceptance_criteria>
    - File có `plan(6)` và kết thúc `select * from finish(); rollback;`
    - Chạy trước migration → đỏ
  </acceptance_criteria>
  <done>Hợp đồng kho-theo-dòng viết xong trước khi có code</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0041</name>
  <files>supabase/migrations/0041_kho_theo_dong.sql</files>
  <read_first>supabase/migrations/0011_rpc_ghi_so.sql, supabase/migrations/0016_rls_chung_tu.sql</read_first>
  <action>
```sql
alter table public.chung_tu_dong add column kho_id uuid references public.kho(id);
comment on column public.chung_tu_dong.kho_id is
  'Kho của riêng dòng này. NULL = dùng chung_tu.kho_id. Nullable có chủ đích: mọi dòng cũ giữ nguyên hành vi.';
create index idx_ct_dong_kho on public.chung_tu_dong (kho_id) where kho_id is not null;
```

`create or replace` **chỉ** `_ghi_so_nhap`: đổi `p_ct.kho_id` → `coalesce(p_dong.kho_id, p_ct.kho_id)`.
KHÔNG đụng sáu hàm còn lại; `_ghi_so_chuyen_kho` giữ nguyên (chuyển kho là chuyện của cả phiếu).

`create or replace` `ghi_so_chung_tu`: khối kiểm xuất âm đổi
`where kho_id = v_ct.kho_id` → `where kho_id = coalesce(v_dong.kho_id, v_ct.kho_id)`.
Giữ nguyên phần còn lại **nguyên văn** (chép từ 0011, đừng viết lại từ trí nhớ).

Policy đọc (`03-RESEARCH.md` §1): drop + tạo lại `"doc chung tu theo pham vi"`, nhánh thủ kho
thêm `or exists (select 1 from public.chung_tu_dong d where d.chung_tu_id = chung_tu.id
and d.kho_id = (select public.kho_hien_tai()))`.

`npm run db:push` rồi chạy test 22 và **toàn bộ** pgTAP.
  </action>
  <verify>
    <automated>npm run db:push && psql "$DATABASE_URL" -f supabase/tests/22_kho_theo_dong_test.sql</automated>
  </verify>
  <acceptance_criteria>
    - Test 22: 6/6 xanh
    - Toàn bộ pgTAP: 199 + 6 = 205 assert, 0 lỗi — không test cũ nào đỏ
    - `npm run db:types` chạy lại, `npm run check` exit 0
  </acceptance_criteria>
  <done>D-05 chạy thật, Phase 1 không vỡ</done>
</task>

</tasks>

<verification>
- [ ] 205 assert xanh; `chung_tu_dong.kho_id` nullable; thủ kho thấy phiếu theo dòng
</verification>

<output>
Tạo `.planning/phases/03-phieu-nhap/03-01-SUMMARY.md`
</output>
