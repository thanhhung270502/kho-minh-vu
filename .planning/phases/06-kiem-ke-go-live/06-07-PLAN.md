---
phase: 06-kiem-ke-go-live
plan: 07
type: execute
wave: 4
depends_on: [06-05]
files_modified:
  - src/features/kiotviet-history/types.ts
  - src/features/kiotviet-history/schemas/history-filter.schema.ts
  - src/features/kiotviet-history/api/kiotviet-history.api.ts
  - src/features/kiotviet-history/api/kiotviet-history.keys.ts
  - src/features/kiotviet-history/hooks/useKiotVietHistory.ts
  - src/features/kiotviet-history/components/history-table.tsx
  - src/features/kiotviet-history/components/voucher-drawer.tsx
autonomous: true
requirements: [DLIEU-07]

must_haves:
  truths:
    - "Một lớp dữ liệu duy nhất gọi tra_cuu_lich_su_kiotviet, dùng chung cho màn riêng và tab chi tiết mã hàng (D-11)"
    - "Bảng lịch sử hiện ngày giờ Việt Nam, loại Nhập/Bán, số phiếu, khách/NCC, mã, tên, số lượng, ghi chú — không cột giá (D-10, D-17)"
    - "Bấm số phiếu mở lại nguyên phiếu cũ với đủ mọi dòng (D-10 mục 4)"
    - "Component và hook không thấy tên cột tiếng Việt — mapper ở types.ts"
  artifacts:
    - path: "src/features/kiotviet-history/api/kiotviet-history.api.ts"
      provides: "fetchKiotVietHistory, fetchKiotVietVoucher"
      contains: "tra_cuu_lich_su_kiotviet"
    - path: "src/features/kiotviet-history/components/history-table.tsx"
      provides: "Bảng dùng chung, QueryState bốn trạng thái"
      contains: "QueryState"
    - path: "src/features/kiotviet-history/components/voucher-drawer.tsx"
      provides: "Mở lại nguyên phiếu"
      contains: "Drawer"
  key_links:
    - from: "hooks/useKiotVietHistory.ts"
      to: "api/kiotviet-history.api.ts"
      via: "useQuery + keys tập trung"
      pattern: "kiotVietHistoryKeys"
    - from: "useKiotVietVoucher"
      to: "enabled"
      via: "voucherNo !== '' (bẫy 10)"
      pattern: "enabled"
---

<objective>
Lớp dữ liệu và hai component hiển thị cho tra cứu lịch sử KiotViet (feature mới `src/features/kiotviet-history/`).

Purpose: DLIEU-07 hiện ở hai nơi dùng chung một bảng (D-11). Dựng bảng + drawer phiếu trước, để 06-08 chỉ còn ghép
bộ lọc/route/tab.

Output: types + filter schema (thuần) + api + keys + hooks + history-table + voucher-drawer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/06-kiem-ke-go-live/06-PATTERNS.md

<interfaces>
RPC (0064): tra_cuu_lich_su_kiotviet(p_loai text, p_tu_khoa text, p_ma_hang text, p_san_pham_id uuid, p_so_phieu text,
  p_tu_ngay date, p_den_ngay date, p_trang int, p_kich_thuoc int) returns table(nguon text 'NHAP'|'XUAT', ma_phieu text,
  ngay timestamptz, doi_tac text, ma_hang text, ten_hang text, so_luong numeric, ghi_chu text, tong_so_dong bigint,
  tong_nhap numeric, tong_xuat numeric). 42501 khi không có quyền; kích thước tối đa 500.
Khuôn: src/features/products/api/product.api.ts (fetchProducts đọc tong_so_dong), products/api/product.keys.ts,
  products/hooks/useProducts.ts (keepPreviousData), products/types.ts (Row Db → mapper), products/schemas/filter.schema.ts
  (readFilterFromUrl/writeFilterToUrl/toListRpcArgs — thuần), products/components/stock-card.tsx (bảng phân trang server + QueryState).
Client: getSupabaseBrowserClient() từ src/lib/supabase/client.ts. Lỗi: explainError trong src/shared/lib/errors.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: types, filter schema thuần, api, keys, hooks</name>
  <files>src/features/kiotviet-history/types.ts, src/features/kiotviet-history/schemas/history-filter.schema.ts, src/features/kiotviet-history/api/kiotviet-history.api.ts, src/features/kiotviet-history/api/kiotviet-history.keys.ts, src/features/kiotviet-history/hooks/useKiotVietHistory.ts</files>
  <read_first>
    - `src/features/products/types.ts` (mapper `toStockCardRow`), `src/features/products/schemas/filter.schema.ts`, `src/features/products/api/product.api.ts`, `src/features/products/api/product.keys.ts`, `src/features/products/hooks/useProducts.ts`.
    - `src/features/inventory/schemas/inventory.schema.ts` — khuôn URL filter mới nhất (Phase 5).
    - `src/types/database.types.ts` mục `tra_cuu_lich_su_kiotviet`.
    - CLAUDE.md bẫy 9 (hàm thuần không để trong file "use client"), bẫy 10 (`enabled`).
  </read_first>
  <behavior>
    - readHistoryFilterFromUrl(new URLSearchParams("loai=XUAT&tim=quynh&trang=2")) → { type: "XUAT", keyword: "quynh", page: 2, các field khác mặc định }.
    - loai lạ ("abc") → type "" (tất cả); tu_ngay không đúng YYYY-MM-DD → bỏ qua (""); trang ≤ 0 hoặc chữ → 1.
    - writeHistoryFilterToUrl(DEFAULT) → chuỗi rỗng (không ghi tham số mặc định).
    - toHistoryRpcArgs({ type: "", keyword: "  ", … }) → không có p_loai, không có p_tu_khoa (chuỗi rỗng/khoảng trắng thành undefined).
    (06-08 viết các case này vào scripts/test-pure-functions.ts; task này viết hàm đúng hành vi.)
  </behavior>
  <action>
    `types.ts`: `type KiotVietHistoryRowDb = Database["public"]["Functions"]["tra_cuu_lich_su_kiotviet"]["Returns"][number]`;
    `export type KiotVietHistoryRow = { source: "NHAP" | "XUAT"; voucherNo: string; date: string | null; partner: string | null;
    productCode: string | null; productName: string | null; quantity: number; note: string | null }`; `KiotVietHistoryPage = { rows, total,
    totalIn: number, totalOut: number }`; mapper `toKiotVietHistoryRow(row)` (Number() cho numeric; nguon ngoài hai giá trị → coi "XUAT" thì SAI —
    thay vào đó ném Error rõ nghĩa "Nguồn lịch sử lạ: …"). Chỉ file này và api/ thấy snake_case.

    `schemas/history-filter.schema.ts` (KHÔNG "use client"): `KiotVietHistoryFilter = { type: "" | "NHAP" | "XUAT"; from: string; to: string;
    keyword: string; voucherNo: string; productCode: string; page: number; pageSize: number }`, `DEFAULT_HISTORY_FILTER` (pageSize 50),
    `HISTORY_URL_PARAMS` ánh xạ sang tham số URL tiếng Việt không dấu: `loai`, `tu_ngay`, `den_ngay`, `tim`, `so_phieu`, `ma_hang`, `trang`;
    `readHistoryFilterFromUrl(params)`, `writeHistoryFilterToUrl(filter): string`, `countActiveHistoryFilters(filter)`,
    `toHistoryRpcArgs(filter, extra?: { productId?: string })` trả Args của RPC (chuỗi rỗng → undefined; `productId` → `p_san_pham_id`).
    Dùng zod cho định dạng ngày `YYYY-MM-DD`.

    `api/kiotviet-history.keys.ts`: `kiotVietHistoryKeys = { all, list(args), voucher(type, voucherNo) }`.
    `api/kiotviet-history.api.ts`: `fetchKiotVietHistory(args): Promise<KiotVietHistoryPage>` (rpc, `if (error) throw error`, total/totalIn/totalOut
    từ dòng đầu, rỗng → 0); `fetchKiotVietVoucher(type: "NHAP" | "XUAT", voucherNo: string): Promise<KiotVietHistoryRow[]>` gọi cùng RPC với
    `p_loai`, `p_so_phieu`, `p_kich_thuoc: 500`.
    `hooks/useKiotVietHistory.ts` ("use client"): `useKiotVietHistory(args)` với `placeholderData: keepPreviousData`;
    `useKiotVietVoucher(type, voucherNo)` với `enabled: voucherNo !== ""` (bẫy 10).
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; grep -q "enabled" src/features/kiotviet-history/hooks/useKiotVietHistory.ts &amp;&amp; ! grep -q "use client" src/features/kiotviet-history/schemas/history-filter.schema.ts &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - `grep -rn "ma_phieu\|doi_tac\|so_luong\|ghi_chu" src/features/kiotviet-history --include=*.ts* | grep -v "types.ts\|api/\|schemas/"` rỗng (snake_case chỉ ở mapper/api/schema-args).
    - Không `any`, không `!` non-null (`grep -rn ": any\|as any" src/features/kiotviet-history` rỗng).
  </acceptance_criteria>
  <done>Lớp dữ liệu gõ đúng kiểu, bộ lọc URL thuần, hook phiếu có enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Bảng lịch sử dùng chung + drawer mở lại nguyên phiếu</name>
  <files>src/features/kiotviet-history/components/history-table.tsx, src/features/kiotviet-history/components/voucher-drawer.tsx</files>
  <read_first>
    - `src/features/products/components/stock-card.tsx` và `stock-card-columns.tsx` — bảng phân trang server + `QueryState`, cách hiển thị ngày.
    - `src/shared/components/query-state.tsx` — props `query`, `isEmpty`, `emptyDescription`, `skeleton`.
    - CLAUDE.md bẫy 1 ("use client" khi import antd), bẫy 11 (`Drawer` dùng `size`, `Alert` dùng `title`), Bước 5 (bảng cuộn ngang trong khung riêng).
    - `design/danh-muc-chi-tiet.html` — nhịp tab thẻ kho để tab KiotViet trông cùng họ.
  </read_first>
  <action>
    `history-table.tsx` ("use client"): props `{ query: UseQueryResult<KiotVietHistoryPage>; page: number; pageSize: number;
    onPageChange(page, pageSize): void; onOpenVoucher(type, voucherNo): void; hideProductColumns?: boolean }`. Bọc `QueryState` với
    `isEmpty = rows.length === 0`, `emptyDescription` "Không có dòng lịch sử KiotViet nào khớp bộ lọc." Cột: Ngày (định dạng
    `dd/MM/yyyy HH:mm` theo `Asia/Ho_Chi_Minh` bằng `Intl.DateTimeFormat`), Loại (`Tag` "Nhập" / "Bán"), Số phiếu (nút link gọi
    `onOpenVoucher`), Khách / NCC, Mã hàng + Tên hàng (ẩn khi `hideProductColumns` — dùng ở tab chi tiết mã), Số lượng (căn phải,
    `toLocaleString("vi-VN")`), Ghi chú (ellipsis + title). Không cột giá (D-17). Phân trang server (`total`), `scroll={{ x: "max-content" }}`
    trong khung `overflow-x-auto`. Dưới bảng một dòng tóm tắt "Tổng nhập: … · Tổng bán: …" từ `totalIn/totalOut` (D-10 mục 3: mã đã nhập/bán
    bao nhiêu). Khóa dòng = `${voucherNo}-${productCode}-${index}`.

    `voucher-drawer.tsx` ("use client"): props `{ voucher: { type: "NHAP" | "XUAT"; voucherNo: string } | null; onClose(): void }`; dùng
    `useKiotVietVoucher(voucher?.type ?? "NHAP", voucher?.voucherNo ?? "")`; `Drawer` (prop `size`, không `width`) tiêu đề
    "Phiếu nhập KiotViet {số}" / "Hóa đơn KiotViet {số}"; phần đầu: ngày, khách/NCC, ghi chú (lấy dòng đầu); bảng mọi dòng (mã, tên, số lượng)
    + tổng số lượng; `QueryState` đủ bốn trạng thái. Câu phụ: "Dữ liệu lưu trữ từ KiotViet — chỉ để tra cứu, không nằm trong sổ kho."

    Mỗi file giữ dưới ~200 dòng; vượt thì tách cột ra `history-columns.tsx` trong cùng feature.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` thoát mã 0.
    - `head -1` của hai file là `"use client";`.
    - `grep -c "width=" src/features/kiotviet-history/components/voucher-drawer.tsx` = 0 (bẫy 11).
    - `grep -ci "đơn giá\|thành tiền\|giá vốn" src/features/kiotviet-history/components/*.tsx` = 0 (D-17).
    - `wc -l` mỗi component ≤ 220.
  </acceptance_criteria>
  <done>Bảng lịch sử và drawer phiếu sẵn sàng để 06-08 ghép vào màn và tab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trình duyệt → RPC tra_cuu_lich_su_kiotviet | Tham số lọc do người dùng gõ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-37 | Information Disclosure | Người bị tắt công tắc gọi thẳng RPC từ console | mitigate | RPC 42501 (0064); hook để lỗi nổi lên `QueryState` → `explainError` kind `forbidden` |
| T-06-38 | Tampering | Tham số lọc lạ (SQL injection qua từ khóa) | mitigate | Tham số truyền qua PostgREST RPC có kiểu; RPC dùng tham số, không ghép chuỗi động |
| T-06-39 | Denial of Service | pageSize cực lớn từ URL | mitigate | Schema kẹp pageSize cố định 50; RPC kẹp 500 |
</threat_model>

<verification>
- `npm run check` xanh.
</verification>

<success_criteria>
- Commit mỗi task: `feat(lich-su-kiotviet): lop du lieu tra cuu` và `feat(lich-su-kiotviet): bang lich su va mo lai nguyen phieu`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-07-SUMMARY.md` when done
</output>
