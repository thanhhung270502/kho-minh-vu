---
phase: 06-kiem-ke-go-live
plan: 13
type: execute
wave: 4
depends_on: [06-05]
files_modified:
  - src/features/stocktake/lib/count-template.server.ts
  - src/features/stocktake/lib/read-count-file.server.ts
  - scripts/test-excel-reader.ts
  - src/app/api/kiem-ke/mau-excel/route.ts
  - src/app/api/kiem-ke/nhap-excel/route.ts
autonomous: true
requirements: [KKE-02, DLIEU-06]

must_haves:
  truths:
    - "Hệ xuất file mẫu đếm theo nhóm hàng của một phiên: cột Mã hàng, Tên hàng, ĐVT, Số đếm (trống) — KHÔNG có cột tồn nào (D-08)"
    - "File đã điền nhập lại vào đúng phiên qua route server; chế độ kiểm tra trước, chỉ nạp khi không còn dòng lỗi (D-04, không nạp nửa vời)"
    - "Ô Số đếm để trống = mã vẫn CHƯA ĐẾM, không phải 0 (D-07)"
    - "File mẫu hệ xuất đọc lại được bằng chính reader — kiểm quay vòng trong scripts/test-excel-reader.ts"
    - "Đọc Excel chỉ ở server (bẫy 7)"
  artifacts:
    - path: "src/features/stocktake/lib/count-template.server.ts"
      provides: "STOCKTAKE_TEMPLATE_COLUMNS, buildCountTemplate"
      contains: "STOCKTAKE_TEMPLATE_COLUMNS"
    - path: "src/features/stocktake/lib/read-count-file.server.ts"
      provides: "readCountFile → { ma_hang, so_dem }[]"
      contains: "readFirstSheet"
    - path: "src/app/api/kiem-ke/nhap-excel/route.ts"
      provides: "POST kiểm tra / nạp số đếm"
      contains: "nhap_so_dem_kiem_ke"
  key_links:
    - from: "src/app/api/kiem-ke/mau-excel/route.ts"
      to: "rpc bang_dem_kiem_ke"
      via: "server client của chính người dùng; chỉ ghi mã/tên/ĐVT vào file"
      pattern: "bang_dem_kiem_ke"
    - from: "src/app/api/kiem-ke/nhap-excel/route.ts"
      to: "rpc nhap_so_dem_kiem_ke"
      via: "p_chi_kiem_tra = che_do !== 'nap'"
      pattern: "p_chi_kiem_tra"
---

<objective>
Phía server của đường nhập thứ ba (D-04): xuất file mẫu đếm theo nhóm (D-08) và nhận file đã điền.

Purpose: đợt đếm đầu kỳ làm ngoài hệ trên giấy/Excel (D-01) — hệ chỉ cần phát file mẫu và nhận lại. Xuất MỖI NHÓM MỘT FILE
(06-RESEARCH A4, Pitfall 4): `readFirstSheet` chỉ đọc sheet đầu, file nhiều sheet sẽ lặng lẽ mất dữ liệu nhóm sau.

Output: hai lib server, hai route handler, case quay vòng trong test-excel-reader.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/06-kiem-ke-go-live/06-RESEARCH.md

<interfaces>
src/shared/lib/excel-cell.ts: readFirstSheet(source: string | Buffer): Promise<ParsedSheet> (headers đã chuẩn hóa: "Mã hàng" → ma_hang,
  "Số đếm" → so_dem), readString, readNumber (hiểu "1.234,5" và "1,234.5"), RawRow.
src/features/inventory/lib/read-stock-file.server.ts: khuôn reader server (không "server-only" để script import được; hàng rào là node:stream).
src/features/products/lib/read-catalog-file.server.ts buildTemplateWorkbook: khuôn exceljs (header đậm, freeze, autoFilter, sheet Hướng dẫn).
src/features/products/lib/excel-template.ts: MAX_FILE_MB.
src/app/api/ton-kho/nap-tam/route.ts: khuôn POST formData (file .xlsx, giới hạn MB, che_do "nap"|"kiem_tra", uuid check, explainError → 401/403/500,
  trả `{ result }`). src/app/api/danh-muc/xuat-excel/route.ts: khuôn GET trả file + tên file giờ Việt Nam.
RPC: bang_dem_kiem_ke(p_chung_tu_id, p_nhom_hang_id?) → (ma_hang, ten_hang, ten_dvt, ten_nhom, …); nhap_so_dem_kiem_ke(p_chung_tu_id,
  p_du_lieu jsonb [{ma_hang, so_dem}], p_chi_kiem_tra) → jsonb { da_nap, dat, cap_nhat, bo_qua, so_loi, chi_tiet_*, loi, ly_do? }.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lib file mẫu + reader, kiểm quay vòng</name>
  <files>src/features/stocktake/lib/count-template.server.ts, src/features/stocktake/lib/read-count-file.server.ts, scripts/test-excel-reader.ts</files>
  <read_first>
    - `src/shared/lib/excel-cell.ts` toàn bộ — `normalizeHeader`, hai đường đọc (stream rồi reader thường — bẫy 7).
    - `src/features/inventory/lib/read-stock-file.server.ts` toàn bộ.
    - `src/features/products/lib/read-catalog-file.server.ts` dòng 120-175 — `buildTemplateWorkbook`.
    - `scripts/test-excel-reader.ts` — case quay vòng hiện có (ghi bằng exceljs rồi đọc lại, cỡ 100-1200 dòng).
  </read_first>
  <behavior>
    - buildCountTemplate(3 dòng) → đọc lại bằng readCountFile: 3 dòng, `so_dem` null cả ba, headers có đúng 4 cột, KHÔNG có header chứa "ton".
    - Điền `so_dem` 5, "1.234,5", để trống → đọc ra 5, 1234.5, null.
    - Quay vòng 1.200 dòng (cỡ hỏng reader stream đã biết) vẫn đọc đủ 1.200.
    - File thiếu cột "Số đếm" → readCountFile ném Error câu tiếng Việt nói cần hai cột "Mã hàng" và "Số đếm".
  </behavior>
  <action>
    Viết case ở `<behavior>` vào `scripts/test-excel-reader.ts` trước (RED), rồi:

    `count-template.server.ts`: `STOCKTAKE_TEMPLATE_COLUMNS = [{ key: "ma_hang", title: "Mã hàng", width: 22 }, { key: "ten_hang", title: "Tên hàng",
    width: 40 }, { key: "dvt", title: "ĐVT", width: 10 }, { key: "so_dem", title: "Số đếm", width: 12 }] as const` — comment: KHÔNG thêm cột tồn (D-08).
    `buildCountTemplate(rows: { code: string; name: string; unit: string | null }[], meta: { sessionNo: string; warehouseName: string;
    categoryName: string | null }): Promise<Buffer>` — sheet 1 tên = `categoryName ?? "Toàn kho"` cắt 31 ký tự và bỏ ký tự cấm của Excel (`[]:*?/\`),
    hàng 1 là header (bắt buộc để `readFirstSheet` nhận cột), freeze hàng 1, cột Số đếm tô nền nhạt; sheet 2 "Hướng dẫn": phiên, kho, nhóm, và các câu
    "Chỉ điền cột Số đếm", "Để trống = chưa đếm (KHÔNG phải 0)", "Không đổi tên cột, không thêm sheet phía trước", "Mỗi mã một dòng".
    `readCountFile(buf: Buffer): Promise<{ ma_hang: string | null; so_dem: number | null }[]>` — `readFirstSheet`, kiểm có `ma_hang` và `so_dem`,
    map bằng `readString`/`readNumber`; ô `so_dem` có chữ không phải số → giữ nguyên chuỗi để RPC báo lỗi đúng dòng (kiểu `number | string | null`,
    ghi chú lý do) — hoặc trả `so_dem: null` + cờ lỗi; chọn cách GIỮ CHUỖI vì RPC đã có nhánh `invalid_text_representation` báo "không phải số".
    Khóa `ma_hang`/`so_dem` là hợp đồng jsonb với RPC — comment lý do snake_case (CLAUDE.md ngoại lệ có chủ đích).
    Không `import "server-only"` (script phải import được — cùng lý do read-stock-file).
  </action>
  <verify>
    <automated>npx tsx scripts/test-excel-reader.ts</automated>
  </verify>
  <acceptance_criteria>
    - Script thoát mã 0 với các case mới.
    - `grep -ci "ton\b\|tồn" src/features/stocktake/lib/count-template.server.ts` chỉ xuất hiện trong comment (lọc `//` còn 0).
    - `grep -c "\.worksheets\[1\]\|addWorksheet" src/features/stocktake/lib/count-template.server.ts` = 2 (một sheet dữ liệu + một Hướng dẫn, dữ liệu ở sheet đầu).
  </acceptance_criteria>
  <done>File mẫu không lộ tồn, đọc lại được, ô trống giữ nghĩa chưa đếm.</done>
</task>

<task type="auto">
  <name>Task 2: Route GET file mẫu và POST nhập số đếm</name>
  <files>src/app/api/kiem-ke/mau-excel/route.ts, src/app/api/kiem-ke/nhap-excel/route.ts</files>
  <read_first>
    - `src/app/api/ton-kho/nap-tam/route.ts` toàn bộ; `src/app/api/danh-muc/xuat-excel/route.ts` dòng 1-80; `src/app/api/danh-muc/mau-excel/route.ts`.
    - `src/features/auth/api/current-user.server.ts`; `src/lib/supabase/server.ts` (tạo mới mỗi request).
    - CLAUDE.md bẫy 7, bẫy 17 (biến môi trường), Bước 4 (401 ≠ 403).
  </read_first>
  <action>
    `mau-excel/route.ts`: `runtime = "nodejs"`; `GET(request)` đọc `?phien=<uuid>&nhom=<uuid|trống>`; chưa đăng nhập → 401 JSON `{title, action}`;
    uuid sai → 400; gọi `bang_dem_kiem_ke` bằng `createSupabaseServerClient()` (phiên người dùng — RPC tự chặn thủ kho kho khác, 42501 → 403);
    lấy thêm `so_ct`, tên kho từ `danh_sach_phien_kiem_ke` hoặc select `chung_tu` cột tường minh; 0 dòng → 404 JSON "Nhóm này không có mã nào trong
    phạm vi phiên". Ghi file bằng `buildCountTemplate` CHỈ với `ma_hang`, `ten_hang`, `ten_dvt` (không đọc/ghi cột tồn nào — D-08). Tên file
    `dem-<so_ct>-<ten-nhom-bo-dau-kebab | toan-kho>.xlsx` (bỏ dấu bằng `removeDiacritics`). `Cache-Control: no-store`.

    `nhap-excel/route.ts`: `runtime = "nodejs"`; `POST` formData `file`, `phien`, `che_do` ("nap" mới ghi, mặc định kiểm tra). Chưa đăng nhập → 401;
    `role === "chi_xem"` → 403 "Vai trò chỉ xem không nhập số đếm được"; thiếu file / không .xlsx / quá `MAX_FILE_MB` / uuid sai → 400/413 như khuôn
    nap-tam; `readCountFile` lỗi → 422 với message của reader; 0 dòng → 422. Gọi `nhap_so_dem_kiem_ke({ p_chung_tu_id, p_du_lieu: rows as unknown as Json,
    p_chi_kiem_tra: mode !== "nap" })` bằng server client của người dùng. Lỗi: `forbidden` → 403, `session-expired` → 401, `23514` → 409 với câu database,
    còn lại 500 — luôn `{ title, action }`. Thành công → `{ result: data }` (giữ khóa `result` như nap-tam — client 06-14 đọc đúng khóa này, bài học `c51391d`).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` thoát mã 0.
    - `grep -c "runtime = \"nodejs\"" src/app/api/kiem-ke/*/route.ts` = 1 mỗi file.
    - `grep -c "ton_so\|ton_hien_tai\|ton_kiotviet" src/app/api/kiem-ke/mau-excel/route.ts` = 0.
    - Kiểm bằng curl có cookie phiên (hoặc trình duyệt): GET không đăng nhập → 401; POST FormData rỗng khi đã đăng nhập `vanphong` → 400 "Chưa chọn file"; ghi vào SUMMARY.
  </acceptance_criteria>
  <done>Hai route chạy, phân biệt 401/403, file mẫu không lộ tồn, nhập có kiểm trước.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file tải lên → route server | Nội dung Excel không tin được |
| route → RPC | Route dùng phiên người dùng, không dùng secret key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-56 | Information Disclosure | File mẫu lộ tồn sổ → người đếm chép theo | mitigate | Template chỉ 4 cột, route không đọc cột tồn; acceptance grep |
| T-06-57 | Elevation of Privilege | Route dùng secret key bỏ qua RLS | mitigate | `createSupabaseServerClient()` phiên người dùng; RPC tự kiểm vai trò/kho |
| T-06-58 | Denial of Service | File cực lớn / zip bomb | mitigate | Giới hạn `MAX_FILE_MB`, chỉ nhận `.xlsx` |
| T-06-59 | Tampering | File lặp mã / số sai → nạp nửa vời | mitigate | RPC phân loại + không ghi khi còn lỗi (0065, pgTAP C11-C13) |
| T-06-60 | Spoofing | Gộp 401 và 403 khiến người dùng bị đẩy về đăng nhập sai lúc | mitigate | Route trả riêng 401 (hết phiên) và 403 (không quyền) theo `explainError` |
</threat_model>

<verification>
- `scripts/test-excel-reader.ts` + `npm run check` xanh.
</verification>

<success_criteria>
- Commit: `feat(kiem-ke): file mau dem theo nhom va doc lai`, `feat(kiem-ke): route xuat mau va nhap so dem Excel`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-13-SUMMARY.md` when done
</output>
