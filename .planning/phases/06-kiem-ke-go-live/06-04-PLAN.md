---
phase: 06-kiem-ke-go-live
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-03]
files_modified:
  - supabase/tests/39_kiem_ke_duyet_test.sql
  - supabase/migrations/0066_kiem_ke_duyet.sql
autonomous: true
requirements: [KKE-03, KKE-04, DLIEU-06]

must_haves:
  truths:
    - "Một RPC trả bảng đếm của phiên: mọi mã trong phạm vi (đã đếm và chưa đếm) kèm số đếm, tồn sổ đã chốt, lệch, tồn hiện tại, tồn KiotViet tạm (KKE-03, D-07, D-09)"
    - "Người duyệt trả được từng dòng về 'đếm lại' và bỏ yêu cầu đó; không chặn duyệt vì lệch lớn (D-16)"
    - "Chỉ người được bật 'Duyệt kiểm kê' (hoặc quản lý) duyệt được; duyệt sinh chứng từ KIEM_KE HOAN_THANH với movement = lệch ĐÃ CHỐT, không tính lại theo tồn lúc duyệt (KKE-04, D-03, D-14)"
    - "Không duyệt được khi còn mã chưa đếm mà người duyệt chưa chọn 'chấp nhận 0', hoặc còn dòng chờ đếm lại — không mã nào bị bỏ sót mà không ai biết (D-07)"
    - "Mã chưa đếm được chấp nhận 0 sinh dòng đếm 0 và đảo đúng phần tồn tạm KiotViet của mã đó (D-06, DLIEU-06)"
    - "ghi_so_chung_tu gọi thẳng cho phiếu KIEM_KE bị từ chối — chỉ đi được qua duyet_phien_kiem_ke; hủy phiếu KIEM_KE đã duyệt chỉ quản lý"
  artifacts:
    - path: "supabase/migrations/0066_kiem_ke_duyet.sql"
      provides: "danh_sach_phien_kiem_ke, bang_dem_kiem_ke, dat_dem_lai, duyet_phien_kiem_ke, ghi_so_chung_tu + huy_chung_tu đã siết"
      contains: "duyet_phien_kiem_ke"
    - path: "supabase/tests/39_kiem_ke_duyet_test.sql"
      provides: "pgTAP bảng lệch, chưa đếm, đếm lại, duyệt, chặn cửa phụ"
      contains: "ghi_so_chung_tu"
  key_links:
    - from: "public.duyet_phien_kiem_ke"
      to: "public.ghi_so_chung_tu"
      via: "set_config('kho_minh_vu.duyet_kiem_ke','on', true) rồi gọi trong cùng transaction"
      pattern: "kho_minh_vu.duyet_kiem_ke"
    - from: "public.bang_dem_kiem_ke"
      to: "public._pham_vi_kiem_ke"
      via: "phạm vi dùng chung với 0065"
      pattern: "_pham_vi_kiem_ke"
---

<objective>
Lớp database cho XEM LỆCH và DUYỆT: danh sách phiên có tiến độ, bảng đếm/lệch/chưa đếm của một phiên,
cờ "đếm lại" từng dòng, và RPC duyệt gọi `ghi_so_chung_tu` có sẵn.

Purpose: 06-RESEARCH §Security Domain — `ghi_so_chung_tu` được `grant execute` cho mọi `authenticated`, nên
kiểm quyền chỉ trong wrapper là chưa đủ. Plan này thêm cửa chặn ngay trong `ghi_so_chung_tu` cho KIEM_KE.
Trạng thái hiển thị (mới mở / đang đếm / chờ duyệt) KHÔNG lưu DB — tính từ tiến độ (nguyên tắc kiến trúc số 1).

Output: migration 0066 + pgTAP 39. Chưa đẩy lên cloud.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/06-kiem-ke-go-live/06-RESEARCH.md
@.planning/phases/06-kiem-ke-go-live/06-03-PLAN.md
@.planning/phases/06-kiem-ke-go-live/06-01-PLAN.md
@.memory/patterns/pgtap-va-test.md

<interfaces>
Từ 06-01 (0063): public.duyet_duoc_kiem_ke() returns boolean (quan_ly luôn true).
Từ 06-03 (0065): chung_tu.pham_vi_nhom_hang uuid[]; chung_tu_dong.dem_luc, nguoi_dem_id, dem_lai boolean;
  unique (chung_tu_id, san_pham_id) where so_luong_he_thong is not null;
  public._pham_vi_kiem_ke(p_chung_tu_id uuid) returns table(san_pham_id uuid);
  public.mo_phien_kiem_ke(p_kho_id, p_nhom_hang_ids, p_ghi_chu); public.luu_dong_kiem_ke(p_chung_tu_id, p_san_pham_id, p_so_luong).
0051 ghi_so_chung_tu(p_chung_tu_id uuid) returns public.chung_tu — for update header, kiểm NHAP_LIEU, kiểm chi_xem (42501),
  vòng dòng gọi _ghi_so_<loai>, update HOAN_THANH + ngay_ghi_so + nguoi_duyet_id + tong_so_luong. Bản ĐANG CHẠY có thể mới hơn 0051 —
  đọc bằng pg_get_functiondef.
0051 huy_chung_tu(p_chung_tu_id uuid, p_ly_do text) — HOAN_THANH của NHAP/XUAT/TRA_NCC/TRA_KHACH chỉ quan_ly; NHAP_LIEU → DA_HUY không bút toán.
0061 nap_ton_tam: chung_tu DIEU_CHINH ghi_chu bắt đầu '[NAP_TON_TAM]' — tồn tạm KiotViet (D-06); dòng có kho_id riêng.
Khuôn phân trang: danh_sach_ghi_chu_kiotviet (0033) / danh_sach_chung_tu (0045) — count(*) over ().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pgTAP 39 — bảng lệch, chưa đếm, đếm lại, duyệt, chặn cửa phụ (RED)</name>
  <files>supabase/tests/39_kiem_ke_duyet_test.sql</files>
  <read_first>
    - `supabase/tests/38_kiem_ke_dem_test.sql` (do 06-03 viết) — dựng dữ liệu, mở phiên, lưu đếm; chép khuôn dữ liệu.
    - `supabase/tests/35_nap_ton_tam_test.sql` — dựng một chứng từ `[NAP_TON_TAM]` DIEU_CHINH để có "tồn KiotViet" (có thể gọi thẳng `nap_ton_tam` dưới `quanly` với jsonb mã test).
    - `.memory/patterns/pgtap-va-test.md` mục 8 (`now()` hằng trong transaction — không ORDER BY thời gian), mục 11.
  </read_first>
  <behavior>
    Dữ liệu: mã test `KD-ZQX-A/B/C` nhóm N1 kho mặc định K1; tồn tạm qua `nap_ton_tam` dưới quanly: A=10, B=4, C=3.
    Phiên P (N1) mở dưới vanphong.
    - E1: `bang_dem_kiem_ke(P)` trả đúng 3 dòng cho A/B/C (cộng mã thật nếu thuộc N1 — N1 là nhóm test tạo mới nên không có mã thật), `dong_id is null` cả ba (chưa đếm), `ton_hien_tai` = 10/4/3, `ton_kiotviet` = 10/4/3 (D-09).
    - E2: đếm A = 8 → dòng A có `so_dem = 8`, `ton_so = 10`, `lech = -2`; XUAT 1 cái A và ghi sổ SAU khi đếm → `bang_dem_kiem_ke` vẫn `ton_so = 10`, `lech = -2`, còn `ton_hien_tai = 9`.
    - E3: `p_nhom_hang_id` lọc đúng nhóm; `danh_sach_phien_kiem_ke()` có P với `so_da_dem = 1`, `so_trong_pham_vi = 3`, `so_dem_lai = 0`; `danh_sach_phien_kiem_ke(p_chung_tu_id := P)` trả đúng một dòng.
    - E4: dưới `thukho1` (kho không phải K1 nếu thukho1 thuộc K1 thì dùng K2 cho phiên này — đọc nguoi_dung_kho trước): `bang_dem_kiem_ke` phiên kho khác → 42501; `danh_sach_phien_kiem_ke` không trả phiên kho khác.
    - E5: dưới `vanphong` (cột duyet_kiem_ke false) `dat_dem_lai(dòng A, true)` → 42501; bật cột (postgres) → `dat_dem_lai` được, `dem_lai = true`; `luu_dong_kiem_ke` lại dòng A → `dem_lai` về false.
    - E6: còn B, C chưa đếm, gọi `duyet_phien_kiem_ke(P, '{}')` dưới người có quyền → 23514 (còn mã chưa đếm chưa xử lý); đặt `dem_lai = true` cho A rồi duyệt với đủ danh sách chấp nhận 0 → 23514 (còn dòng chờ đếm lại).
    - E7: dưới `vanphong` cột duyet false: `duyet_phien_kiem_ke` → 42501; `ghi_so_chung_tu(P)` gọi thẳng → 42501 dù có hay không có quyền duyệt (chỉ qua wrapper).
    - E8: dưới `quanly` (cột false — D-15) duyệt với `p_chap_nhan_khong_dem := array[B, C]` (A không cờ đếm lại) → trả chung_tu `HOAN_THANH`, `ngay_ct` = ngày hôm nay giờ Việt Nam; `kho_movement` của P: A = -2 (lệch đã chốt, KHÔNG phải 8 - 9), B = -4, C = -3; `ton_kho` K1: A = 7 (9 - 2), B = 0, C = 0 (D-06: đảo đúng tồn tạm).
    - E9: sau duyệt: `luu_dong_kiem_ke(P, …)` → 23514; `dat_dem_lai` → 23514; `duyet_phien_kiem_ke(P)` lần hai → 23514.
    - E10: `huy_chung_tu(P, 'test')` dưới `vanphong` → 42501; phiên NHAP_LIEU khác hủy được dưới vanphong (DA_HUY, không movement).
    - E11: chấp nhận 0 một `san_pham_id` KHÔNG thuộc danh sách chưa đếm của phiên → 23514.
    - E12: `ghi_so_chung_tu` cho phiếu NHAP thường dưới vanphong vẫn `lives_ok` (không phá luồng cũ); gọi dưới postgres (không phiên — script) cho KIEM_KE không bị chặn bởi cửa mới.
  </behavior>
  <action>
    Tạo `supabase/tests/39_kiem_ke_duyet_test.sql` khuôn 35 với E1-E12. Kiểm movement bằng tổng `so_luong` theo `(chung_tu_id, san_pham_id)`,
    không ORDER BY thời gian. Không assert `so_ct` cụ thể. Chạy RED qua MCP, ghi SUMMARY.
  </action>
  <verify>
    <automated>test -f supabase/tests/39_kiem_ke_duyet_test.sql &amp;&amp; grep -q "duyet_phien_kiem_ke" supabase/tests/39_kiem_ke_duyet_test.sql &amp;&amp; grep -q "bang_dem_kiem_ke" supabase/tests/39_kiem_ke_duyet_test.sql &amp;&amp; grep -q "dat_dem_lai" supabase/tests/39_kiem_ke_duyet_test.sql &amp;&amp; grep -q "huy_chung_tu" supabase/tests/39_kiem_ke_duyet_test.sql &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`; `plan(N)` khớp số assertion.
    - Có assertion movement của A bằng lệch đã chốt khi có XUAT xen giữa đếm và duyệt (E2 + E8).
    - SUMMARY ghi lỗi RED.
  </acceptance_criteria>
  <done>pgTAP 39 phủ E1-E12, đỏ trước migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0066 — đọc phiên/bảng đếm, đếm lại, duyệt, siết ghi sổ và hủy (GREEN)</name>
  <files>supabase/migrations/0066_kiem_ke_duyet.sql</files>
  <read_first>
    - `supabase/migrations/0065_kiem_ke_dem.sql` (06-03) — helper phạm vi, cột mới, cách kiểm vai trò/kho.
    - `supabase/migrations/0051_chung_tu_rpc_mo_rong.sql` dòng 20-120 và 276-356 — `ghi_so_chung_tu`, `huy_chung_tu`.
    - `supabase/migrations/0061_nap_ton_tam.sql` dòng 223-258 — dựng dòng rồi `perform ghi_so_chung_tu`, không `exception when others`.
    - `supabase/migrations/0033_ra_ghi_chu_lich_su.sql` dòng 52-95 — khuôn phân trang.
    - `.memory/patterns/pgtap-va-test.md` mục 10 — tiền tố bảng ở SELECT cuối của mọi `RETURNS TABLE`.
    - Bản ĐANG CHẠY qua MCP: `select pg_get_functiondef('public.ghi_so_chung_tu(uuid)'::regprocedure);` và
      `select pg_get_functiondef('public.huy_chung_tu(uuid,text)'::regprocedure);` — dán vào comment đầu migration; sửa trên BẢN CLOUD.
  </read_first>
  <action>
    Tạo `supabase/migrations/0066_kiem_ke_duyet.sql`, header nêu KKE-03/04, D-06, D-07, D-09, D-14, D-16 và lý do cửa chặn trong ghi_so. Thứ tự:

    1. `public.danh_sach_phien_kiem_ke(p_kho_id uuid default null, p_trang_thai public.trang_thai_ct default null, p_trang integer default 1,
       p_kich_thuoc integer default 20, p_chung_tu_id uuid default null)` (`p_chung_tu_id` lọc đúng một phiên — trang chi tiết dùng để lấy đầu phiên kèm tiến độ) returns table(id uuid, so_ct text, ngay_ct date, kho_id uuid, ten_kho text, pham_vi_nhom_hang uuid[],
       ten_nhom_pham_vi text, trang_thai public.trang_thai_ct, so_da_dem bigint, so_trong_pham_vi bigint, so_dem_lai bigint,
       nguoi_tao text, ngay_ghi_so timestamptz, created_at timestamptz, tong_so_dong bigint)` — plpgsql stable SECURITY DEFINER:
       chỉ `loai_ct = 'KIEM_KE'`; thủ kho chỉ thấy `kho_id = any(kho_hien_tai())`; `so_trong_pham_vi` = số phần tử của hợp
       (`_pham_vi_kiem_ke(ct.id)` ∪ mã đã có dòng); `ten_nhom_pham_vi` = `string_agg(nhom_hang.ten, ', ')` hoặc null; sắp `created_at desc`;
       phân trang kẹp 1..100. Mọi cột SELECT cuối có tiền tố.
    2. `public.bang_dem_kiem_ke(p_chung_tu_id uuid, p_nhom_hang_id uuid default null)` returns table(san_pham_id uuid, ma_hang text, ten_hang text,
       ten_dvt text, nhom_hang_id uuid, ten_nhom text, dong_id uuid, so_dem numeric, ton_so numeric, lech numeric, ton_hien_tai numeric,
       ton_kiotviet numeric, dem_luc timestamptz, nguoi_dem text, dem_lai boolean)` — stable SECURITY DEFINER: phiên không tồn tại/không
       KIEM_KE → 23514; `chi_xem` được đọc; thủ kho ngoài kho → 42501. Tập mã = `_pham_vi_kiem_ke(p)` ∪ mã có dòng trong phiên; left join dòng
       (`so_luong` → `so_dem`, `so_luong_he_thong` → `ton_so`, `lech = so_dem - ton_so` khi có dòng), `ton_kho` tại kho phiên (`ton_hien_tai`,
       coalesce 0), `ton_kiotviet` = tổng `chung_tu_dong.so_luong` của chứng từ `DIEU_CHINH` `HOAN_THANH` có `ghi_chu like '[NAP_TON_TAM]%'`
       với `coalesce(dong.kho_id, ct.kho_id)` = kho phiên (null khi không có). Lọc `p_nhom_hang_id` nếu có. Sắp `ma_hang`. Không phân trang —
       một kho tối đa ~3.300 mã (comment ghi rõ). Không trả giá (D-17).
    3. `public.dat_dem_lai(p_dong_id uuid, p_dem_lai boolean) returns void` — SECURITY DEFINER: `not duyet_duoc_kiem_ke()` → 42501; dòng thuộc
       phiên KIEM_KE còn NHAP_LIEU (khóa header `for update`) nếu không → 23514; update `dem_lai`.
    4. `public.duyet_phien_kiem_ke(p_chung_tu_id uuid, p_chap_nhan_khong_dem uuid[] default '{}'::uuid[]) returns public.chung_tu` — SECURITY DEFINER:
       a) `not duyet_duoc_kiem_ke()` → 42501; b) header `for update`, phải KIEM_KE + NHAP_LIEU (23514);
       c) còn dòng `dem_lai = true` → 23514 "Còn N dòng chờ đếm lại";
       d) tập chưa đếm = `_pham_vi_kiem_ke(p)` trừ mã có dòng; phần tử của `p_chap_nhan_khong_dem` không thuộc tập chưa đếm → 23514;
          tập chưa đếm trừ danh sách chấp nhận còn phần tử → 23514 "Còn N mã chưa đếm chưa được xử lý — đếm bù hoặc chấp nhận 0" (D-07);
       e) với mỗi mã chấp nhận: insert dòng `so_luong = 0`, `so_luong_he_thong` = tồn tại kho phiên NGAY LÚC DUYỆT (coalesce 0), `kho_id`,
          `dem_luc = now()`, `nguoi_dem_id = auth.uid()`, `ghi_chu = '[CHAP_NHAN_0]'`;
       f) `update chung_tu set ngay_ct = (now() at time zone 'Asia/Ho_Chi_Minh')::date where id = p` — movement mang ngày duyệt (comment lý do:
          thẻ kho sắp theo ngày; ngày mở phiên có thể cách nhiều ngày);
       g) `perform set_config('kho_minh_vu.duyet_kiem_ke', 'on', true);` rồi `return public.ghi_so_chung_tu(p_chung_tu_id);` — không bọc
          `exception when others`. KHÔNG đổi `_ghi_so_kiem_ke`.
    5. `ghi_so_chung_tu`: `create or replace` từ bản cloud, thêm DUY NHẤT một khối ngay sau khối kiểm `chi_xem`:
       nếu `v_ct.loai_ct = 'KIEM_KE' and auth.uid() is not null` và (`coalesce(current_setting('kho_minh_vu.duyet_kiem_ke', true), '') <> 'on'`
       hoặc `not (select public.duyet_duoc_kiem_ke())`) → raise 42501 "Phiếu kiểm kê chỉ ghi sổ qua nút Duyệt phiên". `auth.uid() is null`
       (script/postgres) không bị chặn. Comment: cờ transaction-local do `duyet_phien_kiem_ke` đặt; client PostgREST không gọi được `set_config`
       nên không giả được cờ; giữ kiểm quyền lần hai làm phòng thủ nhiều lớp. Giữ nguyên grant.
    6. `huy_chung_tu`: `create or replace` từ bản cloud, đổi danh sách `in ('NHAP','XUAT','TRA_NCC','TRA_KHACH')` thành thêm `'KIEM_KE'`
       (hủy phiên đã duyệt = đảo sổ cái → chỉ quản lý); thêm: `KIEM_KE` + thủ kho + kho không được phân → 42501. Cập nhật `comment on function`.
    7. `revoke all … from public, anon; grant execute … to authenticated;` cho bốn RPC mới; khối tự kiểm RLS.

    GREEN: dry-run MCP một lệnh `begin;` + 0063 + 0065 + 0066 + thân test 39 (finish pattern 9) + `rollback;` → `DAT`; lặp lại cho test 38 và
    cho một file test cũ có ghi sổ (ví dụ `29_phieu_xuat_tu_don_test.sql`) để chứng minh không phá luồng cũ. Không `db:push`.
  </action>
  <verify>
    <automated>grep -q "function public.duyet_phien_kiem_ke" supabase/migrations/0066_kiem_ke_duyet.sql &amp;&amp; grep -q "function public.bang_dem_kiem_ke" supabase/migrations/0066_kiem_ke_duyet.sql &amp;&amp; grep -q "kho_minh_vu.duyet_kiem_ke" supabase/migrations/0066_kiem_ke_duyet.sql &amp;&amp; grep -q "NAP_TON_TAM" supabase/migrations/0066_kiem_ke_duyet.sql &amp;&amp; ! grep -q "function public._ghi_so_kiem_ke" supabase/migrations/0066_kiem_ke_duyet.sql &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - Dry-run MCP trả `DAT` cho 39, 38 và 29; SUMMARY ghi kết quả + bản `pg_get_functiondef` đã dùng.
    - `grep -v '^\s*--' supabase/migrations/0066_kiem_ke_duyet.sql | grep -c "exception when others"` = 0.
    - Diff giữa bản cloud `ghi_so_chung_tu` và bản mới chỉ là khối kiểm KIEM_KE (SUMMARY dán diff).
    - `grep -c "gia_von\|don_gia" ` trong định nghĩa `bang_dem_kiem_ke` = 0 (D-17).
  </acceptance_criteria>
  <done>0066 viết xong; 39, 38, 29 dry-run DAT; ghi sổ KIEM_KE chỉ qua wrapper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → RPC ghi_so_chung_tu | Được grant cho mọi authenticated từ Phase 1 |
| client → RPC đọc bảng đếm | SECURITY DEFINER, bỏ qua RLS kho |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-22 | Elevation of Privilege | Gọi thẳng `ghi_so_chung_tu(phiên)` bỏ qua quyền duyệt và kiểm D-07 (06-RESEARCH Security Domain) | mitigate | Cờ transaction-local chỉ `duyet_phien_kiem_ke` đặt + kiểm `duyet_duoc_kiem_ke()` trong ghi_so; pgTAP E7 |
| T-06-23 | Elevation of Privilege | Người không bật công tắc duyệt phiên | mitigate | Kiểm đầu `duyet_phien_kiem_ke`; pgTAP E7, E8 |
| T-06-24 | Tampering | Duyệt bỏ sót mã chưa đếm | mitigate | Chặn khi tập chưa đếm trừ danh sách chấp nhận khác rỗng; pgTAP E6, E11 |
| T-06-25 | Tampering | Hủy phiếu KIEM_KE đã duyệt để đảo sổ cái mà không phải quản lý | mitigate | Thêm KIEM_KE vào nhánh chỉ quản lý của `huy_chung_tu`; pgTAP E10 |
| T-06-26 | Information Disclosure | Thủ kho đọc bảng đếm kho khác qua RPC definer | mitigate | Kiểm `kho_hien_tai()` trong bang_dem/danh_sach; pgTAP E4 |
| T-06-27 | Tampering | Sửa số đếm sau khi duyệt | mitigate | `luu_dong_kiem_ke`/`dat_dem_lai` kiểm NHAP_LIEU dưới khóa header; pgTAP E9 |
| T-06-28 | Elevation of Privilege | Client giả cờ `kho_minh_vu.duyet_kiem_ke` | mitigate | PostgREST chỉ expose schema public; `set_config` là pg_catalog, không gọi được qua RPC; cờ `is_local = true` chết cùng transaction |
</threat_model>

<verification>
- Dry-run DAT cho 39, 38, 29.
- `_ghi_so_kiem_ke` không bị đụng (Pitfall 1).
</verification>

<success_criteria>
- Duyệt phiên đưa tồn về đúng số đã đếm (KKE-04).
- Commit: `test(06-04): pgTAP duyet phien kiem ke` và `feat(db): bang lech va duyet phien kiem ke`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-04-SUMMARY.md` when done
</output>
