---
phase: 06-kiem-ke-go-live
plan: 16
type: execute
wave: 7
depends_on: [06-08, 06-13, 06-15]
files_modified:
  - src/shared/lib/navigation.ts
  - src/shared/components/nav-icons.tsx
  - src/shared/components/app-shell.tsx
  - scripts/test-pure-functions.ts
  - scripts/test-route-permissions.ts
autonomous: false
requirements: [KKE-01, KKE-02, KKE-03, KKE-04, DLIEU-05, DLIEU-06, DLIEU-07]

must_haves:
  truths:
    - "Menu có 'Kiểm kê' (mọi vai trò) và 'Lịch sử KiotViet' (chỉ người có công tắc — ẩn hẳn, không chỉ disable)"
    - "Năm route/endpoint mới của Phase 6 được kiểm quyền bằng HTTP thật cho bốn vai trò + khách (bẫy 12)"
    - "Toàn bộ bộ kiểm xanh: check, pgTAP, verify:hook, hàm thuần, đọc Excel, quyền route"
    - "DLIEU-05 đóng là không cần (D-17): phase này KHÔNG làm gì về giá vốn; màn nạp giá vốn đầu kỳ 0044 (cost-import) giữ nguyên, không file nào của nó bị sửa"
    - "Người dùng xác nhận năm tiêu chí ROADMAP Phase 6 trên trình duyệt, gồm quy trình đầu kỳ D-01 (đếm ngoài hệ bằng file mẫu, nhập lại, duyệt) và bước vận hành nạp tồn tạm D-06 do CHÍNH người dùng thực hiện"
  artifacts:
    - path: "src/shared/lib/navigation.ts"
      provides: "Mục /kiem-ke, /lich-su-kiotviet; filterNavItems theo vai trò + công tắc"
      contains: "/lich-su-kiotviet"
    - path: "scripts/test-route-permissions.ts"
      provides: "Dòng ma trận cho /kiem-ke, /kiem-ke/<id>, /lich-su-kiotviet, /api/kiem-ke/mau-excel + phép thử POST /api/kiem-ke/nhap-excel"
      contains: "/lich-su-kiotviet"
  key_links:
    - from: "src/shared/components/app-shell.tsx"
      to: "filterNavItems(user, NAV_ITEMS)"
      via: "user.canViewKiotVietHistory từ layout (CurrentUser)"
      pattern: "canViewKiotVietHistory"
    - from: "scripts/test-route-permissions.ts"
      to: "requirePermission / requireKiotVietHistoryAccess trong page.tsx"
      via: "HTTP thật trên phiên thật"
      pattern: "MA_TRAN"
---

<objective>
Nối Phase 6 vào ứng dụng: menu (có lọc theo công tắc), ma trận quyền route, chạy toàn bộ bộ kiểm, rồi UAT.

Purpose: các màn đã dựng xong nhưng chưa có lối vào và chưa route nào bị kiểm bằng HTTP thật. Bẫy 12 đã cắn một lần.
UAT gồm bước vận hành đầu kỳ — nạp tồn tạm (D-06) CHƯA làm trên database thật (24/09: 0 DIEU_CHINH); đó là việc của người
dùng, agent KHÔNG tự ghi dữ liệu sản xuất.

Output: menu, dòng ma trận + phép thử POST, biên bản UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/05-ton-kho-tong-quan/05-11-PLAN.md

<interfaces>
navigation.ts: NavIconId union (dashboard, stock-in, sales-order, stock-out, inventory, catalog, partners, settings); NavItem { href, label,
  shortLabel, icon, permission, mobilePriority }; NAV_ITEMS; filterByPermission(role, items) — chỉ app-shell.tsx gọi.
nav-icons.tsx: NAV_ICONS: Record<NavIconId, ReactNode> — thiếu case là lỗi kiểu.
app-shell.tsx: props user { fullName; role } — layout (src/app/(app)/layout.tsx) đã truyền nguyên CurrentUser (có canViewKiotVietHistory sau 06-06).
test-route-permissions.ts: MA_TRAN, AI_CUNG_XEM, KyVong ("200"|"quyen"|"dangnhap"|"401"|"goc"|`→…`), layCookie, kiemNapTamPost (khuôn phép thử POST),
  các hàm layId… đẩy thêm dòng động vào MA_TRAN.
Tài khoản mẫu: quanly, vanphong (sau 0063 có xem_lich_su_kiotviet = true do backfill), thukho1, chixem — duyet_kiem_ke false cho cả ba người không phải quản lý.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Menu Kiểm kê + Lịch sử KiotViet, lọc theo công tắc</name>
  <files>src/shared/lib/navigation.ts, src/shared/components/nav-icons.tsx, src/shared/components/app-shell.tsx, scripts/test-pure-functions.ts</files>
  <read_first>
    - `src/shared/lib/navigation.ts` toàn bộ (comment về mobilePriority từ Phase 4/5).
    - `src/shared/components/nav-icons.tsx`, `app-shell.tsx`, `src/app/(app)/layout.tsx`.
    - `scripts/test-pure-functions.ts` — có test `splitMobileItems`/`filterByPermission` chưa; thêm case mới cạnh đó.
    - CLAUDE.md bẫy 9 (navigation.ts là file thuần, cả server lẫn client import).
  </read_first>
  <behavior>
    - filterNavItems({ role: "thu_kho", canViewKiotVietHistory: false }, NAV_ITEMS) có "/kiem-ke", KHÔNG có "/lich-su-kiotviet".
    - filterNavItems({ role: "van_phong", canViewKiotVietHistory: true }, …) có cả hai; `{ role: "quan_ly", canViewKiotVietHistory: true }` có cả hai và có "/cai-dat".
    - filterNavItems({ role: "chi_xem", canViewKiotVietHistory: false }, …) có "/kiem-ke", không có "/cai-dat".
    - splitMobileItems(filterNavItems(thủ kho)) — 4 ô chính giữ nguyên như Phase 5 (Tổng quan, Xuất, Nhập, Tồn); "Kiểm kê" rơi vào "Khác".
  </behavior>
  <action>
    `navigation.ts`: thêm `"stocktake"` và `"kiotviet-history"` vào `NavIconId`; thêm field tùy chọn `requires?: "kiotviet-history"` vào `NavItem`
    (comment: quyền theo người, không theo PERMISSION_MATRIX). Hai mục mới sau "/ton-kho": `{ href: "/kiem-ke", label: "Kiểm kê", shortLabel: "Kiểm kê",
    icon: "stocktake", permission: "view-catalog", mobilePriority: null }` và `{ href: "/lich-su-kiotviet", label: "Lịch sử KiotViet", shortLabel:
    "LS KiotViet", icon: "kiotviet-history", permission: "view-catalog", requires: "kiotviet-history", mobilePriority: null }`. Comment mobilePriority:
    thanh tab đáy giữ bốn ô của Phase 5; Kiểm kê định kỳ không phải việc hằng giờ nên ở "Khác" (màn đếm vẫn dùng tốt trên điện thoại — D-04).
    Thêm `export function filterNavItems(user: { role: Role; canViewKiotVietHistory: boolean }, items: NavItem[]): NavItem[]` = lọc
    `hasPermission` rồi bỏ mục `requires === "kiotviet-history"` khi `!canViewKiotVietHistory`. Giữ `filterByPermission` nếu còn nơi dùng; nếu app-shell là
    nơi duy nhất thì thay hẳn và xóa hàm cũ (không để code chết).
    `nav-icons.tsx`: `stocktake: <AuditOutlined />` (hoặc `CheckSquareOutlined`), `"kiotviet-history": <HistoryOutlined />` từ `@ant-design/icons` đã cài.
    `app-shell.tsx`: kiểu `user` thêm `canViewKiotVietHistory: boolean`; dùng `filterNavItems(user, NAV_ITEMS)`.
    Case ở `<behavior>` viết vào `scripts/test-pure-functions.ts` trước khi sửa (RED), rồi GREEN.
  </action>
  <verify>
    <automated>npx tsx scripts/test-pure-functions.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - Cả hai lệnh thoát mã 0.
    - `grep -c "/kiem-ke\|/lich-su-kiotviet" src/shared/lib/navigation.ts` ≥ 2; `grep -c "stocktake\|kiotviet-history" src/shared/components/nav-icons.tsx` ≥ 2.
    - `grep -c "filterByPermission" src/shared/components/app-shell.tsx` = 0 (đã chuyển sang `filterNavItems`).
  </acceptance_criteria>
  <done>Menu có hai mục mới, Lịch sử KiotViet ẩn hẳn với người không có công tắc, thanh tab đáy không đổi.</done>
</task>

<task type="auto">
  <name>Task 2: Ma trận quyền route + phép thử POST + chạy toàn bộ bộ kiểm</name>
  <files>scripts/test-route-permissions.ts</files>
  <read_first>
    - `scripts/test-route-permissions.ts` toàn bộ — `MA_TRAN`, `kiemNapTamPost`, các hàm `layId…`, cách đếm hỏng.
    - `src/app/(app)/kiem-ke/page.tsx`, `kiem-ke/[id]/page.tsx`, `lich-su-kiotviet/page.tsx`, `src/app/api/kiem-ke/mau-excel/route.ts`, `nhap-excel/route.ts` — chặn thật của từng route.
  </read_first>
  <action>
    Thêm vào `MA_TRAN`:
    - `/kiem-ke` → `AI_CUNG_XEM`.
    - `/kiem-ke/00000000-0000-4000-8000-000000000000` (uuid hợp lệ không tồn tại) → `AI_CUNG_XEM` (trang render trạng thái rỗng; phạm vi kho siết ở RPC).
      Nếu có phiên thật, thêm hàm `layIdPhienKiemKe()` theo khuôn `layIdPhieuNhap` và đẩy thêm dòng với id thật.
    - `/lich-su-kiotviet` → `quanly: "200", vanphong: "200"` (backfill 0063), `thukho1: "quyen", chixem: "quyen", khach: "dangnhap"`. Comment: kỳ vọng của
      vanphong phụ thuộc công tắc trong database — nếu quản lý tắt công tắc của tài khoản mẫu, dòng này đổi thành "quyen".
    - `/api/kiem-ke/mau-excel` (không tham số) → bốn vai trò `"400"` là KHÔNG hợp lệ với `KyVong` hiện có; thay vào đó viết hàm `kiemKiemKeExcel(cookie)`
      theo khuôn `kiemNapTamPost`: GET `/api/kiem-ke/mau-excel` không tham số → khách 401, mọi vai trò 400; POST FormData rỗng tới
      `/api/kiem-ke/nhap-excel` → khách 401, `chixem` 403, `quanly`/`vanphong`/`thukho1` 400 ("Chưa chọn file"). In theo định dạng dòng của script, cộng vào
      bộ đếm hỏng.

    Chạy và ghi con số vào SUMMARY: `npm run check` · `npm run db:test:linked` (hoặc từng file qua MCP theo 06-05; đếm cả ERROR) · `npm run verify:hook` ·
    `npx tsx scripts/test-pure-functions.ts` · `npx tsx scripts/test-excel-reader.ts` · `npm run dev` rồi `npx tsx scripts/test-route-permissions.ts`.
    Việc còn nợ của phase trước (dòng ma trận Phase 4 nếu vẫn thiếu) ghi vào SUMMARY, không bổ khuyết ở đây.

    DLIEU-05 (D-17): chạy `git diff --stat <commit đầu phase 6>..HEAD -- src/features/products/components/cost-import.tsx src/app/api/danh-muc/gia-von-dau-ky
    supabase/migrations/0044_gia_von_dau_ky.sql` — phải rỗng; ghi vào SUMMARY "DLIEU-05 đóng — không cần, không file giá vốn nào bị sửa".
  </action>
  <verify>
    <automated>npx tsx scripts/test-route-permissions.ts</automated>
  </verify>
  <acceptance_criteria>
    - Script báo 0 dòng hỏng; số dòng kiểm tăng đúng 3 × 5 = 15 (cộng dòng id thật nếu có) + 10 phép thử của `kiemKiemKeExcel`.
    - `MA_TRAN` chứa `"/kiem-ke"`, `"/lich-su-kiotviet"`; script chứa `/api/kiem-ke/nhap-excel`.
    - Sáu bộ kiểm đều xanh, con số từng bộ trong SUMMARY; pgTAP không `not ok`, không `ERROR`.
    - Lệnh `git diff --stat` cho DLIEU-05 rỗng.
  </acceptance_criteria>
  <done>Route mới kiểm bằng HTTP thật; toàn bộ bộ kiểm xanh; DLIEU-05 xác nhận không đụng.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT Phase 6 — công tắc, tra cứu, kiểm kê định kỳ, quy trình đầu kỳ</name>
  <files>không sửa file nào — nghiệm thu trên dữ liệu thật</files>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 6 — năm tiêu chí thành công.
    - `.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md` — D-01..D-17 và `<specifics>` (thứ tự vận hành trước go-live).
  </read_first>
  <what-built>
    Công tắc quyền theo người; tra cứu lịch sử KiotViet (màn riêng + tab chi tiết mã; thẻ kho không còn trộn dòng cũ); phiên kiểm kê một kho với ba đường
    đếm (điện thoại, bảng, Excel), chốt tồn sổ theo dòng lúc lưu, bảng lệch tô nổi + đếm lại, danh sách chưa đếm, duyệt theo công tắc; menu và ma trận quyền.
  </what-built>
  <how-to-verify>
    CẢNH BÁO TRƯỚC: database là database thật, sổ cái append-only. Duyệt một phiên = ghi sổ thật, không xóa được (chỉ hủy bằng bút toán đảo, chỉ quản lý).
    Các bước 1-6 KHÔNG duyệt; phiên thử được HỦY ở cuối.

    1. **Công tắc** — `quanly` vào Cài đặt → Người dùng: tài khoản văn phòng có "Xem lịch sử KiotViet" đã bật sẵn (giữ quyền cũ — bạn muốn giữ hay tắt?),
       "Duyệt kiểm kê" tắt. Quản lý: hai ô tick + khóa. Tắt "Xem lịch sử KiotViet" của `vanphong` → đăng nhập `vanphong` ở cửa sổ khác, tải lại: menu mất
       "Lịch sử KiotViet", gõ `/lich-su-kiotviet` bị chặn, tab ở chi tiết mã và dòng KiotViet ở lịch sử đối tác cũng mất — KHÔNG cần đăng nhập lại. Bật lại → có ngay.
    2. **Tra cứu (tiêu chí 5)** — `/lich-su-kiotviet`: gõ tên khách không dấu (vd "quynh") ra hóa đơn có ghi chú QUỲNH; lọc Nhập + khoảng ngày; tìm theo mã
       hàng; bấm một số hóa đơn mở đủ các dòng. Mở một mã hàng → tab "Lịch sử KiotViet" cạnh "Thẻ kho"; Thẻ kho không còn dòng KiotViet (D-11 — bạn có đồng ý
       bỏ dòng cũ khỏi thẻ kho không?).
    3. **Mở phiên thử (tiêu chí 1)** — `vanphong` mở phiên cho MỘT kho, MỘT nhóm hàng nhỏ. Màn nói kho không phải đóng, tồn chốt lúc lưu.
    4. **Đếm ba đường (tiêu chí 2)** — điện thoại (thu cửa sổ 375px): gõ vài ký tự mã không dấu → Enter → số → Enter, ba mã liên tiếp chỉ bằng bàn phím, không
       thấy số tồn; máy tính: tab Bảng, gõ số hai dòng; Excel: tải file mẫu nhóm đó (mở ra: 4 cột, không có tồn), điền vài số + một ô trống + một mã sai, tải lên:
       thấy mới/ghi đè/bỏ qua/lỗi, nút Nạp bị khóa; sửa mã sai, tải lại, Nạp.
    5. **D-03 chốt theo dòng** — ghi sổ một phiếu xuất cho một mã ĐÃ đếm → Bảng lệch: tồn sổ của mã đó KHÔNG đổi. Đếm lại mã đó → tồn sổ cập nhật theo lúc lưu mới.
    6. **Bảng lệch (tiêu chí 3)** — dòng lệch lớn tô nổi (ngưỡng hiện tại: từ 5 cái hoặc 10% — bạn muốn ngưỡng khác không?); `quanly` trả một dòng về "đếm lại",
       nút Duyệt khóa kèm lý do; danh sách "chưa đếm" hiện trước nút duyệt; `vanphong` (chưa bật Duyệt) thấy nút Duyệt khóa kèm hướng dẫn. Không có cột tiền nào (D-17).
       Sau đó **Hủy phiên thử** với lý do "UAT".
    7. **Bước vận hành đầu kỳ (tiêu chí 4, D-01, D-06) — DO BẠN THỰC HIỆN, agent không tự làm:** tồn tạm KiotViet hiện CHƯA nạp (0 phiếu DIEU_CHINH). Thứ tự
       go-live: (1) `quanly` nạp tồn tạm ở `/ton-kho/nap-tam` bằng file KiotViet mới nhất; (2) mở phiên TOÀN KHO cho từng kho, tải file mẫu từng nhóm; (3) đếm
       trên giấy/Excel sát ngày chuyển (tối/chủ nhật, không bán xen giữa — D-01); (4) nhập file; (5) xem chưa đếm + lệch (cột "Tồn KiotViet tạm" để so), đếm lại
       nếu cần; (6) người có quyền duyệt → tồn đầu kỳ. Nếu bạn làm bước này trong phiên UAT, xác nhận: tồn sau duyệt = đúng số đếm, mã chấp nhận 0 về 0,
       phiếu KIEM_KE mở được từ thẻ kho. Nếu để tới ngày go-live, trả lời "đầu kỳ để sau" — không tính là lỗi.
    8. **Phân quyền** — `thukho1`: `/kiem-ke` chỉ thấy kho mình, không mở được phiên kho khác; `chixem`: xem được, không mở/đếm.
    9. **Console** — mở console ở mỗi màn mới: không cảnh báo antd.

    Sai chỗ nào: màn nào, thao tác gì, thấy gì, mong đợi gì.
  </how-to-verify>
  <action>
    Không tự nạp tồn tạm, không tự duyệt phiên nào trên database thật, không tự kết luận đạt. Chạy `npm run dev`, trình bày chín mục ở <how-to-verify> theo đúng
    thứ tự, DỪNG chờ người dùng trả lời từng mục, rồi chép nguyên văn kết luận (kèm ba câu hỏi: giữ công tắc văn phòng, bỏ dòng KiotViet khỏi thẻ kho, ngưỡng lệch
    lớn) vào SUMMARY.
  </action>
  <verify>
    <automated>npx tsx scripts/test-route-permissions.ts</automated>
    <human-check>Chín mục được người dùng xác nhận từng mục, kèm trả lời ba câu hỏi (công tắc văn phòng mặc định, bỏ dòng KiotViet khỏi thẻ kho, ngưỡng lệch lớn).</human-check>
  </verify>
  <acceptance_criteria>
    - Chín mục có kết luận đạt/không đạt bằng chữ trong SUMMARY; mục 7 ghi "đã làm" (kèm số phiếu DIEU_CHINH/KIEM_KE) hoặc "đầu kỳ để sau".
    - Ba câu trả lời về giả định (backfill công tắc văn phòng, D-11 thẻ kho, ngưỡng A1) chép nguyên văn.
    - Phiên thử của UAT ở trạng thái DA_HUY (không để phiên rác NHAP_LIEU).
    - Mục "không đạt" chuyển thành gap cho `/gsd:plan-phase --gaps`, không tự sửa ngoài phạm vi.
  </acceptance_criteria>
  <resume-signal>Gõ "đạt" kèm trả lời ba câu hỏi, hoặc liệt kê mục sai và sai thế nào</resume-signal>
  <done>UAT có biên bản; ba giả định được người dùng chốt; bước vận hành đầu kỳ được ghi rõ đã làm hay để ngày go-live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL gõ tay → route mới | Menu ẩn không chặn URL |
| Agent → database sản xuất trong UAT | Nạp tồn tạm/duyệt phiên là ghi sổ thật |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-67 | Elevation of Privilege | Route mới thiếu chặn | mitigate | Ma trận HTTP thật cho /kiem-ke, /kiem-ke/<id>, /lich-su-kiotviet + phép thử GET/POST hai endpoint Excel |
| T-06-68 | Information Disclosure | Menu hiện "Lịch sử KiotViet" cho người không có công tắc | mitigate | `filterNavItems` ẩn hẳn; test hàm thuần; chặn thật ở page + RLS |
| T-06-69 | Tampering | Agent tự nạp tồn tạm / duyệt phiên trên database thật trong UAT | mitigate | Task 3 cấm rõ; bước đầu kỳ là thao tác của người dùng; phiên thử kết thúc bằng Hủy |
| T-06-70 | Repudiation | Báo bộ kiểm xanh khi file pgTAP chết giữa chừng | mitigate | Đếm cả ERROR; con số từng bộ vào SUMMARY |
| T-06-SC | Tampering | Cài gói npm mới | mitigate | Không thêm dependency trong Phase 6; icon từ `@ant-design/icons` đã có (06-RESEARCH: không package mới) |
</threat_model>

<verification>
- Sáu bộ kiểm xanh; ma trận route 0 hỏng; UAT chín mục có biên bản.
</verification>

<success_criteria>
- Năm tiêu chí ROADMAP Phase 6 được xác nhận (tiêu chí 4 có thể "để ngày go-live" — ghi rõ).
- Commit: `feat(nav): menu kiem ke va lich su KiotViet theo cong tac`, `test(quyen): ma tran route Phase 6`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-16-SUMMARY.md` when done
</output>
