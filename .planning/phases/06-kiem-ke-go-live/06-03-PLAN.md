---
phase: 06-kiem-ke-go-live
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/38_kiem_ke_dem_test.sql
  - supabase/migrations/0065_kiem_ke_dem.sql
autonomous: true
requirements: [KKE-01, KKE-02, DLIEU-06]

must_haves:
  truths:
    - "Mở được phiên kiểm kê = MỘT chứng từ KIEM_KE cho MỘT kho, phạm vi nhóm hàng tùy chọn (NULL = toàn kho, dùng cho đợt đầu kỳ) (KKE-01)"
    - "Tồn sổ chốt theo TỪNG DÒNG tại lúc lưu số đếm; sửa đếm lại thì chốt lại tại lúc sửa — phiếu xuất ghi sổ giữa lúc mở phiên và lúc lưu không làm sai lệch (D-02, D-03)"
    - "Mỗi mã chỉ một dòng trong một phiên — ràng buộc ở database, không dựa vào giao diện (D-05)"
    - "Ba đường nhập số đếm (điện thoại, máy tính, Excel) đi chung một RPC luu_dong_kiem_ke; Excel đi qua RPC gộp có chế độ xem trước và không nạp nửa vời (D-04, D-08)"
    - "Client KHÔNG ghi thẳng được chung_tu / chung_tu_dong của phiếu KIEM_KE qua PostgREST — mọi ghi đi qua RPC"
    - "Thủ kho chỉ mở/đếm được phiên của kho mình; chỉ xem không đếm được"
  artifacts:
    - path: "supabase/migrations/0065_kiem_ke_dem.sql"
      provides: "Cột phạm vi/đếm, unique một mã một dòng, helper phạm vi, policy chặn ghi thẳng, mo_phien_kiem_ke, luu_dong_kiem_ke, xoa_dong_kiem_ke, nhap_so_dem_kiem_ke"
      contains: "luu_dong_kiem_ke"
    - path: "supabase/tests/38_kiem_ke_dem_test.sql"
      provides: "pgTAP mở phiên, chốt tồn theo dòng, chặn ghi thẳng, import Excel"
      contains: "so_luong_he_thong"
  key_links:
    - from: "public.luu_dong_kiem_ke"
      to: "public.ton_kho"
      via: "đọc so_luong NGAY LÚC GỌI rồi ghi so_luong_he_thong"
      pattern: "so_luong_he_thong"
    - from: "policy chung_tu_dong insert/update/delete"
      to: "public.la_chung_tu_kiem_ke(chung_tu_id)"
      via: "SECURITY DEFINER helper, không bị RLS che"
      pattern: "la_chung_tu_kiem_ke"
---

<objective>
Lớp database cho việc MỞ PHIÊN và ĐẾM: một phiên kiểm kê là một `chung_tu` loại `KIEM_KE` của một kho;
số đếm lưu qua RPC chốt tồn sổ theo từng dòng tại lúc lưu.

Purpose: 06-RESEARCH Pitfall 1 — KHÔNG sửa `_ghi_so_kiem_ke` (công thức `so_luong - so_luong_he_thong` đã đúng D-03).
Việc thật là ghi `so_luong_he_thong` đúng LÚC, và khóa mọi đường ghi thẳng để không ai bỏ qua bước chốt đó.
Phạm vi "mã thuộc phiên" dựng thành MỘT helper dùng chung cho đếm, danh sách chưa đếm, file mẫu và duyệt.

Output: migration 0065 + pgTAP 38. Chưa đẩy lên cloud.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/06-kiem-ke-go-live/06-RESEARCH.md
@.planning/phases/06-kiem-ke-go-live/06-PATTERNS.md
@.memory/patterns/pgtap-va-test.md

<interfaces>
0007 chung_tu: id, so_ct unique, loai_ct public.loai_ct, ngay_ct date, kho_id uuid NOT NULL, trang_thai public.trang_thai_ct
  ('NHAP_LIEU','HOAN_THANH','DA_HUY'), ghi_chu, nguoi_tao_id, nguoi_duyet_id, ngay_ghi_so, created_at, updated_at.
0007 chung_tu_dong: id, chung_tu_id, san_pham_id, so_luong numeric(18,4) NOT NULL (không check > 0), don_gia, thanh_tien,
  so_luong_he_thong numeric(18,4) (CHỈ KIEM_KE, hiện 0 dòng khác null), ghi_chu, created_at; 0041 thêm kho_id uuid.
0011 _ghi_so_kiem_ke(p_ct, p_dong): lệch = so_luong - coalesce(so_luong_he_thong,0); = 0 thì không ghi; movement kho = p_ct.kho_id,
  ngày = p_ct.ngay_ct. KHÔNG ĐỔI.
sinh_so_ct(p_loai public.loai_ct, p_nam smallint default null, p_nguon text default '') — SECURITY DEFINER (0047/0048); KIEM_KE → tiền tố 'KK'.
vai_tro_hien_tai() returns public.vai_tro; kho_hien_tai() returns uuid[] — trong điều kiện luôn viết `= any((select public.kho_hien_tai())::uuid[])`.
san_pham: id, ma_hang unique, ten_hang, nhom_hang_id, dvt_id, dang_kinh_doanh boolean, kho_mac_dinh_id.
ton_kho(kho_id, san_pham_id, so_luong) — có thể KHÔNG có dòng cho cặp (kho, mã) → coi là 0.
Policy 0016 cần sửa: chung_tu "tao chung tu tru chi xem" (insert), "chi sua chung tu dang nhap lieu" (update);
  chung_tu_dong "tao dong chung tu tru chi xem" (insert), "chi sua dong cua chung tu nhap lieu" (update),
  "xoa dong cua chung tu nhap lieu" (delete). Policy đọc chung_tu (0042) dùng helper SECURITY DEFINER để cắt đệ quy — học theo.
Khuôn RPC nhập hàng loạt: nap_ton_tam (0061) — jsonb vào, vòng phân loại không ghi, trả dat/bo_qua/so_loi/chi_tiet_*/loi.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pgTAP 38 — mở phiên, chốt tồn theo dòng, chặn ghi thẳng, import (RED)</name>
  <files>supabase/tests/38_kiem_ke_dem_test.sql</files>
  <read_first>
    - `supabase/tests/00_helper.sql.inc`, `supabase/tests/35_nap_ton_tam_test.sql` (khuôn dữ liệu hai kho K1/K2, `pg_temp.sp_test`, `pg_temp.kho_id`).
    - `supabase/tests/29_phieu_xuat_tu_don_test.sql` hoặc `30_rls_test.sql` — cách dựng phiếu NHAP/XUAT rồi `ghi_so_chung_tu` để có tồn.
    - `supabase/tests/30_rls_test.sql` — cách test một policy INSERT bị từ chối dưới `authenticated` (pattern 1: bảng tạm phải `grant select … to authenticated` trước, nếu không 42501 là false pass).
  </read_first>
  <behavior>
    Dữ liệu (dưới postgres): mã test `KK-ZQX-A`, `KK-ZQX-B` (nhóm N1, kho mặc định K1), `KK-ZQX-C` (nhóm N2, K1), `KK-ZQX-D` (kho mặc định K2);
    một phiếu NHAP đã ghi sổ đưa tồn K1 của A = 10, B = 5.
    - C1: dưới `vanphong`, `mo_phien_kiem_ke(p_kho_id := K1, p_nhom_hang_ids := array[N1])` trả chung_tu `loai_ct = 'KIEM_KE'`, `trang_thai = 'NHAP_LIEU'`, `kho_id = K1`, `pham_vi_nhom_hang = {N1}`, `so_ct like 'KK%'`.
    - C2: `mo_phien_kiem_ke(K1, null)` và `(K1, '{}')` đều cho `pham_vi_nhom_hang is null` (toàn kho).
    - C3: dưới `thukho1` mở phiên cho kho KHÔNG được phân → throws 42501; dưới `chixem` → 42501.
    - C4 (D-03 cốt lõi): mở phiên P1 (N1) → XUAT 3 cái A và ghi sổ (tồn A = 7) → `luu_dong_kiem_ke(P1, A, 6)` → dòng có `so_luong = 6`, `so_luong_he_thong = 7`, `dem_luc is not null`, `nguoi_dem_id = id vanphong`, `kho_id = K1`.
    - C5 (đếm lại): NHAP thêm 2 A (tồn 9) → `luu_dong_kiem_ke(P1, A, 8)` → VẪN một dòng cho A, `so_luong = 8`, `so_luong_he_thong = 9`.
    - C6: mã không có dòng ton_kho tại K1 (`KK-ZQX-B` ở kho khác không áp dụng — dùng mã mới chưa từng nhập) → `so_luong_he_thong = 0` (không null).
    - C7: `luu_dong_kiem_ke(P1, C, 1)` (C thuộc N2, ngoài phạm vi) → 23514; số đếm âm → 23514; `p_so_luong` null → 23514.
    - C8: dưới `vanphong`, `insert into chung_tu_dong (chung_tu_id, san_pham_id, so_luong) values (P1, B, 5)` → throws 42501 (RLS); `update chung_tu_dong set so_luong = 99 where chung_tu_id = P1` → 0 dòng bị đổi (kiểm lại bằng postgres); `delete` tương tự 0 dòng; `update chung_tu set kho_id = K2 where id = P1` → 0 dòng; `insert into chung_tu (so_ct, loai_ct, kho_id) values ('ZQX-KK', 'KIEM_KE', K1)` → 42501.
    - C9 (không phá luồng cũ): dưới `vanphong` insert dòng vào một phiếu NHAP còn NHAP_LIEU vẫn `lives_ok`.
    - C10: `xoa_dong_kiem_ke(<dòng A>)` xóa dòng; gọi trên phiên đã ghi sổ (đặt `trang_thai='HOAN_THANH'` bằng postgres) → 23514.
    - C11: `nhap_so_dem_kiem_ke(P1, jsonb, true)` với các dòng: A=4 (đã có dòng → cap_nhat), B=5 (dat), `so_dem` trống (bo_qua), mã không tồn tại (loi), mã C (loi — ngoài phạm vi), `so_dem = 'abc'` (loi), mã B lặp lần hai (loi "lặp") → trả `cap_nhat=1, dat=1, bo_qua=1, so_loi` = số dòng lỗi, và KHÔNG ghi gì (đếm dòng P1 không đổi).
    - C12: gọi lại với `p_chi_kiem_tra := false` trên dữ liệu CÓ lỗi → `da_nap = false`, không ghi dòng nào (không nạp nửa vời).
    - C13: `p_chi_kiem_tra := false` trên dữ liệu sạch (A=4, B=5) → `da_nap = true`, dòng A `so_luong = 4`, dòng B có `so_luong_he_thong = 5`.
    - C14: unique `(chung_tu_id, san_pham_id) where so_luong_he_thong is not null` tồn tại (`has_index` hoặc truy vấn `pg_indexes`).
    - C15: `has_function_privilege('anon', 'public.luu_dong_kiem_ke(uuid,uuid,numeric)', 'execute')` = false.
  </behavior>
  <action>
    Tạo `supabase/tests/38_kiem_ke_dem_test.sql` theo khuôn 35 với C1-C15. Nhóm hàng test N1/N2 tạo dưới postgres với mã chứa `ZQX`.
    Bảng tạm chứa id dùng dưới `authenticated` phải `grant select … to authenticated` (pattern 1). Kiểm "0 dòng bị đổi" (C8) bằng
    cách về `dang_xuat()` rồi đọc lại giá trị, không dựa vào ngoại lệ (UPDATE/DELETE bị RLS lọc không ném lỗi).
    Không assert số phiếu cụ thể (`KK26-000001`) — bộ đếm đang sống (bẫy 16); chỉ `like 'KK%'`.
    Chạy RED qua MCP (finish theo pattern 9), ghi lỗi vào SUMMARY.
  </action>
  <verify>
    <automated>test -f supabase/tests/38_kiem_ke_dem_test.sql &amp;&amp; grep -q "luu_dong_kiem_ke" supabase/tests/38_kiem_ke_dem_test.sql &amp;&amp; grep -q "nhap_so_dem_kiem_ke" supabase/tests/38_kiem_ke_dem_test.sql &amp;&amp; grep -q "mo_phien_kiem_ke" supabase/tests/38_kiem_ke_dem_test.sql &amp;&amp; grep -q "grant select" supabase/tests/38_kiem_ke_dem_test.sql &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - `plan(N)` khớp số assertion; không assertion nào so `so_ct` bằng chuỗi cụ thể.
    - Có ít nhất một assertion kiểm `so_luong_he_thong` = tồn SAU phiếu xuất ghi sổ giữa lúc mở phiên và lúc lưu (C4).
    - SUMMARY ghi lỗi RED.
  </acceptance_criteria>
  <done>pgTAP 38 phủ C1-C15 và đỏ vì chưa có migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0065 — phiên, đếm, chặn ghi thẳng, nhập Excel (GREEN)</name>
  <files>supabase/migrations/0065_kiem_ke_dem.sql</files>
  <read_first>
    - `supabase/migrations/0061_nap_ton_tam.sql` toàn bộ — khuôn kiểm quyền tường minh, vòng phân loại không ghi, `invalid_text_representation`, hình jsonb trả về.
    - `supabase/migrations/0042_sua_de_quy_policy_chung_tu.sql` — helper SECURITY DEFINER dùng trong policy để cắt đệ quy.
    - `supabase/migrations/0016_rls_chung_tu.sql` dòng 55-100 — năm policy ghi cần sửa.
    - `supabase/migrations/0051_chung_tu_rpc_mo_rong.sql` dòng 33-60 — `for update` khóa header, kiểm `chi_xem`.
    - Bản ĐANG CHẠY qua MCP: `select tablename, policyname, cmd, qual, with_check from pg_policies where tablename in ('chung_tu','chung_tu_dong');`
      và `select count(*) from chung_tu_dong where so_luong_he_thong is not null;` (phải = 0 để unique index mới tạo được). Dán vào comment đầu migration.
  </read_first>
  <action>
    Tạo `supabase/migrations/0065_kiem_ke_dem.sql`, header nêu KKE-01/02, D-02..D-05, D-08 và "không đổi `_ghi_so_kiem_ke`". Theo thứ tự:

    1. Cột: `chung_tu.pham_vi_nhom_hang uuid[]` (comment: chỉ KIEM_KE; NULL = toàn kho). `chung_tu_dong.dem_luc timestamptz`,
       `chung_tu_dong.nguoi_dem_id uuid references public.nguoi_dung(id)`, `chung_tu_dong.dem_lai boolean not null default false`
       (comment từng cột: chỉ KIEM_KE; `dem_lai` = người duyệt trả về đếm lại, D-16).
    2. `create unique index uq_ct_dong_kiem_ke_ma on public.chung_tu_dong (chung_tu_id, san_pham_id) where so_luong_he_thong is not null;`
       — chỉ dòng KIEM_KE mang `so_luong_he_thong`, nên NHAP/XUAT (nhiều dòng cùng mã, 0041) không bị ảnh hưởng. Comment lý do (D-05).
    3. Helper `public.la_chung_tu_kiem_ke(p_chung_tu_id uuid) returns boolean` — `language sql stable security definer set search_path = ''`,
       `select exists (select 1 from public.chung_tu where id = p_chung_tu_id and loai_ct = 'KIEM_KE')`. SECURITY DEFINER để phép thử
       không bị RLS đọc chung_tu che (thủ kho không thấy phiếu kho khác thì `exists` thường trả false → lọt). grant execute authenticated.
    4. Helper nội bộ `public._pham_vi_kiem_ke(p_chung_tu_id uuid) returns table(san_pham_id uuid)` — `language sql stable security definer
       set search_path = ''`: sản phẩm `sp` với `ct.pham_vi_nhom_hang is null or sp.nhom_hang_id = any(ct.pham_vi_nhom_hang)` VÀ
       (`sp.dang_kinh_doanh and sp.kho_mac_dinh_id = ct.kho_id` HOẶC có `ton_kho` tại `ct.kho_id` với `so_luong <> 0`). Comment ghi
       Pitfall 5 (06-RESEARCH) + A3: vế "có tồn" bắt mã ngừng kinh doanh còn hàng và mã sau này chuyển kho (Phase 8). Mã chưa gán
       kho mặc định và tồn 0 nằm ngoài phạm vi — ghi rõ. `revoke all … from public, anon, authenticated` (chỉ RPC khác gọi).
    5. Sửa policy (drop + create cùng tên, điều kiện gốc lấy từ `pg_policies` ĐANG CHẠY rồi THÊM):
       chung_tu insert: `and loai_ct <> 'KIEM_KE'`; chung_tu update: thêm `and loai_ct <> 'KIEM_KE'` vào USING;
       chung_tu_dong insert/update(USING)/delete: thêm `and not public.la_chung_tu_kiem_ke(chung_tu_id)`.
       Comment: phiếu KIEM_KE chỉ ghi qua RPC SECURITY DEFINER (bỏ qua RLS) — khuôn "sổ cái không có policy ghi cho client" (0016).
    6. `public.mo_phien_kiem_ke(p_kho_id uuid, p_nhom_hang_ids uuid[] default null, p_ghi_chu text default null) returns public.chung_tu`,
       plpgsql SECURITY DEFINER `set search_path = ''`: `chi_xem` → 42501; `thu_kho` và `p_kho_id <> all((select public.kho_hien_tai())::uuid[])`
       → 42501; kho không tồn tại/không hoạt động → 23514; mảng rỗng → NULL; mọi phần tử phải là `nhom_hang` có thật (23514).
       Insert `so_ct = public.sinh_so_ct('KIEM_KE'::public.loai_ct)`, `loai_ct = 'KIEM_KE'`, `kho_id`, `pham_vi_nhom_hang`,
       `ghi_chu = nullif(trim(p_ghi_chu),'')`, `nguoi_tao_id = auth.uid()`.
    7. `public.luu_dong_kiem_ke(p_chung_tu_id uuid, p_san_pham_id uuid, p_so_luong numeric) returns public.chung_tu_dong`, SECURITY DEFINER:
       `select … from chung_tu where id = p for update` (khóa header — tuần tự hóa với người đếm khác và với duyệt);
       không phải KIEM_KE → 23514; khác NHAP_LIEU → 23514 "Phiên đã duyệt hoặc đã hủy, không sửa số đếm được";
       `chi_xem` → 42501; thủ kho ngoài kho → 42501; `p_so_luong is null or p_so_luong < 0` → 23514;
       sản phẩm không tồn tại → 23514; phiên có phạm vi mà `nhom_hang_id` của mã không thuộc → 23514 "Mã không thuộc nhóm hàng của phiên".
       Đọc `v_ton := coalesce((select so_luong from public.ton_kho where kho_id = v_ct.kho_id and san_pham_id = p_san_pham_id), 0)` NGAY LÚC GỌI (D-03).
       `insert into chung_tu_dong (chung_tu_id, san_pham_id, so_luong, so_luong_he_thong, don_gia, thanh_tien, kho_id, dem_luc, nguoi_dem_id, dem_lai)
       values (…, v_ton, 0, 0, v_ct.kho_id, now(), auth.uid(), false) on conflict (chung_tu_id, san_pham_id) where so_luong_he_thong is not null
       do update set so_luong = excluded.so_luong, so_luong_he_thong = excluded.so_luong_he_thong, dem_luc = excluded.dem_luc,
       nguoi_dem_id = excluded.nguoi_dem_id, dem_lai = false returning *`. `comment on function` giải thích D-03 (khuôn 0061).
    8. `public.xoa_dong_kiem_ke(p_dong_id uuid) returns void`, SECURITY DEFINER: tìm dòng + header `for update`; cùng các kiểm như mục 7
       (KIEM_KE, NHAP_LIEU, chi_xem, thủ kho); delete.
    9. `public.nhap_so_dem_kiem_ke(p_chung_tu_id uuid, p_du_lieu jsonb, p_chi_kiem_tra boolean default true) returns jsonb`, SECURITY DEFINER:
       kiểm header như mục 7 (không cần khóa ở vòng phân loại); vòng 1 phân loại từng phần tử `{ma_hang, so_dem}` (khóa jsonb tiếng Việt =
       hợp đồng với route Excel): thiếu mã → loi; mã xuất hiện lần thứ hai trong file → loi "Mã lặp trong file"; `so_dem` rỗng/null → bo_qua
       "Ô Số đếm trống — mã vẫn tính là CHƯA ĐẾM" (trống ≠ 0, D-07); không phải số (`invalid_text_representation`) hoặc âm → loi;
       mã không có trong danh mục → loi; phiên có phạm vi nhóm mà nhóm của mã không thuộc → loi (CÙNG điều kiện với mục 7 — chỉ xét nhóm, không xét kho: hàng tìm thấy trong kho dù kho mặc định khác vẫn đếm được); đã có dòng trong phiên → cap_nhat;
       còn lại → dat. Chế độ xem trước trả `{da_nap:false, dat, cap_nhat, bo_qua, so_loi, chi_tiet_dat, chi_tiet_cap_nhat, chi_tiet_bo_qua, loi}`.
       Không xem trước mà `so_loi > 0` → trả `da_nap:false` + `ly_do` "Sửa hết dòng lỗi rồi nạp lại — không nạp nửa vời", KHÔNG ghi.
       Sạch → với mỗi dat/cap_nhat `perform public.luu_dong_kiem_ke(p_chung_tu_id, san_pham_id, so_dem)`; không bọc `exception when others`;
       trả `da_nap:true` + các số đếm.
    10. `revoke all … from public, anon; grant execute … to authenticated;` cho mo_phien/luu_dong/xoa_dong/nhap_so_dem; khối tự kiểm RLS.

    GREEN: dry-run MCP `begin;` + 0065 + thân test 38 (finish pattern 9) + `rollback;` → `DAT`. Không `db:push`.
  </action>
  <verify>
    <automated>grep -q "uq_ct_dong_kiem_ke_ma" supabase/migrations/0065_kiem_ke_dem.sql &amp;&amp; grep -q "function public.luu_dong_kiem_ke" supabase/migrations/0065_kiem_ke_dem.sql &amp;&amp; grep -q "function public.la_chung_tu_kiem_ke" supabase/migrations/0065_kiem_ke_dem.sql &amp;&amp; grep -q "function public._pham_vi_kiem_ke" supabase/migrations/0065_kiem_ke_dem.sql &amp;&amp; grep -q "function public.nhap_so_dem_kiem_ke" supabase/migrations/0065_kiem_ke_dem.sql &amp;&amp; ! grep -q "function public._ghi_so_kiem_ke" supabase/migrations/0065_kiem_ke_dem.sql &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK` (có đủ RPC, KHÔNG định nghĩa lại `_ghi_so_kiem_ke`).
    - Dry-run MCP trả `DAT` cho pgTAP 38; SUMMARY ghi nguyên kết quả và bản `pg_policies` đã đọc.
    - `grep -c "for update" supabase/migrations/0065_kiem_ke_dem.sql` ≥ 2.
    - `grep -v '^\s*--' supabase/migrations/0065_kiem_ke_dem.sql | grep -c "exception when others"` = 0.
    - Truy vấn đếm `so_luong_he_thong is not null` trên cloud trước khi viết = 0, ghi vào SUMMARY.
  </acceptance_criteria>
  <done>0065 viết xong, pgTAP 38 dry-run DAT trên cloud rồi rollback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PostgREST chung_tu / chung_tu_dong | Policy chung cho mọi loại chứng từ cho phép ghi thẳng |
| client → RPC SECURITY DEFINER đếm | Hàm bỏ qua RLS, phải tự kiểm vai trò và kho |
| file Excel → RPC nhap_so_dem_kiem_ke | Dữ liệu người dùng điền tay |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering | Insert/update thẳng `chung_tu_dong` của KIEM_KE để đặt `so_luong_he_thong` tùy ý (06-RESEARCH Pitfall 2) | mitigate | Ba policy thêm `not public.la_chung_tu_kiem_ke(chung_tu_id)`; pgTAP C8 |
| T-06-15 | Tampering | Update header KIEM_KE (đổi `kho_id`) sau khi đã đếm → lệch tính trên kho sai | mitigate | Policy update chung_tu thêm `loai_ct <> 'KIEM_KE'`; pgTAP C8 |
| T-06-16 | Elevation of Privilege | Helper trong policy dùng `exists` thường bị RLS che với thủ kho → lọt | mitigate | `la_chung_tu_kiem_ke` SECURITY DEFINER (khuôn 0042) |
| T-06-17 | Elevation of Privilege | Thủ kho mở/đếm phiên kho khác qua RPC | mitigate | Kiểm `kho_hien_tai()` trong mo_phien/luu_dong/xoa_dong; pgTAP C3 |
| T-06-18 | Tampering | Hai người lưu cùng mã cùng lúc tạo hai dòng | mitigate | `for update` header + unique partial index + `on conflict … do update`; pgTAP C5, C14 |
| T-06-19 | Tampering | File Excel lặp mã / số âm / chữ → nạp nửa vời | mitigate | Phân loại lỗi đầy đủ; không ghi gì khi `so_loi > 0`; pgTAP C11-C13 |
| T-06-20 | Repudiation | Không biết ai đếm, đếm lúc nào | mitigate | `nguoi_dem_id = auth.uid()`, `dem_luc = now()` ghi trong RPC, client không truyền |
| T-06-21 | Denial of Service | Phiên toàn kho lớn làm `_pham_vi_kiem_ke` chậm | accept | Tối đa 3.266 mã/kho, một lần quét; không có đường vào cho anon |
</threat_model>

<verification>
- pgTAP 38 dry-run DAT; `_ghi_so_kiem_ke` không bị đụng.
</verification>

<success_criteria>
- Mọi đường ghi số đếm đi qua `luu_dong_kiem_ke`.
- Commit: `test(06-03): pgTAP mo phien va dem kiem ke` và `feat(db): phien kiem ke chot ton so theo dong`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-03-SUMMARY.md` when done
</output>
