---
phase: 06-kiem-ke-go-live
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - supabase/tests/37_lich_su_kiotviet_test.sql
  - supabase/tests/42_the_kho_test.sql
  - supabase/tests/33_the_kho_luy_ke_test.sql
  - supabase/migrations/0064_lich_su_kiotviet.sql
autonomous: true
requirements: [DLIEU-07]

must_haves:
  truths:
    - "594 dòng nhập + 4.732 dòng hóa đơn tra được qua MỘT RPC phân trang, lọc theo loại / khoảng ngày / số phiếu / mã hàng / mã sản phẩm (D-10, D-11)"
    - "Tìm tự do không dấu khớp cả cột khách/NCC lẫn ghi chú — gõ 'quynh' ra dòng ghi chú 'QUỲNH' (D-12)"
    - "Khoảng ngày lọc theo NGÀY GIỜ VIỆT NAM, không theo UTC của máy chủ"
    - "Công tắc xem_lich_su_kiotviet chặn ở CẢ BA cửa: RLS hai bảng luu_tru_*, RPC tra cứu mới, và lich_su_giao_dich_doi_tac (0033) (D-13)"
    - "Thẻ kho (the_kho_san_pham) chỉ còn dòng sổ cái hệ thống — dòng KiotViet cũ chuyển sang tab riêng, không trộn (D-11)"
  artifacts:
    - path: "supabase/migrations/0064_lich_su_kiotviet.sql"
      provides: "tra_cuu_lich_su_kiotviet, hai policy đọc mới, lich_su_giao_dich_doi_tac + the_kho_san_pham đã sửa"
      contains: "tra_cuu_lich_su_kiotviet"
    - path: "supabase/tests/37_lich_su_kiotviet_test.sql"
      provides: "pgTAP lọc, tìm không dấu, múi giờ, ba cửa quyền"
      contains: "xem_lich_su_kiotviet"
  key_links:
    - from: "policy doc luu tru nhap / doc luu tru hoa don"
      to: "public.xem_duoc_lich_su_kiotviet()"
      via: "using ((select public.xem_duoc_lich_su_kiotviet()))"
      pattern: "xem_duoc_lich_su_kiotviet"
    - from: "public.lich_su_giao_dich_doi_tac"
      to: "public.xem_duoc_lich_su_kiotviet()"
      via: "v_xem_kv := (select public.xem_duoc_lich_su_kiotviet())"
      pattern: "v_xem_kv :="
---

<objective>
Lớp database cho DLIEU-07: RPC tra cứu lịch sử KiotViet, và chuyển quyền đọc hai bảng lưu trữ từ "theo vai
trò" sang công tắc theo người ở MỌI chỗ đang đọc chúng.

Purpose: nỗi đau chính của Phase 6 là mất lịch sử khi tắt KiotViet. Khi lập kế hoạch đã grep thấy BA chỗ đang
gate bằng `vai_tro in ('quan_ly','van_phong')`: policy 0016, `lich_su_giao_dich_doi_tac` (0033) và
`the_kho_san_pham` (0062) — nghiên cứu chỉ nêu hai. Sửa thiếu một chỗ là để hở một cửa song song.

Về thẻ kho: D-11 chốt "KHÔNG trộn vào thẻ kho — dòng cũ không có tồn lũy kế đúng, trộn vào sẽ bị hiểu nhầm là
sổ cái". Thẻ kho hiện (Phase 5) đang trộn dòng KiotViet. Plan này BỎ hai nhánh KiotViet khỏi `the_kho_san_pham`;
dòng cũ từ nay xem ở tab "Lịch sử KiotViet" (plan 06-08). Đây là cách đọc D-11 của planner — ghi vào SUMMARY.

Output: migration 0064, pgTAP 37 mới, sửa assertion KiotViet trong pgTAP 42 và 33.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/06-kiem-ke-go-live/06-RESEARCH.md
@.planning/phases/06-kiem-ke-go-live/06-01-PLAN.md
@.memory/patterns/pgtap-va-test.md

<interfaces>
Từ 06-01 (0063): public.xem_duoc_lich_su_kiotviet() returns boolean — quan_ly luôn true; cột nguoi_dung.xem_lich_su_kiotviet.
0010: luu_tru_nhap_kiotviet(id, ma_phieu, ngay text ISO "2026-09-12T15:43:00+07:00", nha_cung_cap, ma_hang, ten_hang,
      so_luong numeric, don_gia, thanh_tien, ghi_chu, du_lieu_goc jsonb, nap_luc)
      luu_tru_hoa_don_kiotviet(id, ma_hoa_don, ngay text, khach_hang, ma_hang, ten_hang, so_luong, don_gia, thanh_tien, ghi_chu, …)
0016 (đang chạy): policy "doc luu tru nhap" / "doc luu tru hoa don" for select to authenticated
      using ((select public.vai_tro_hien_tai()) in ('quan_ly','van_phong')).
0033: lich_su_giao_dich_doi_tac(p_doi_tac_id uuid, p_trang int default 1, p_kich_thuoc int default 50)
      — dòng gate: `v_xem_kv := v_vai_tro in ('quan_ly','van_phong');` (giữ nguyên biến v_vai_tro cho nhánh thủ kho).
0062: the_kho_san_pham(...) — có `v_xem_kv boolean` + hai nhánh union đọc luu_tru_nhap_kiotviet / luu_tru_hoa_don_kiotviet
      (nguồn 'KIOTVIET_NHAP' / 'KIOTVIET_BAN'), gate `v_xem_kv := v_vai_tro in ('quan_ly','van_phong')`.
Khuôn phân trang: danh_sach_ghi_chu_kiotviet (0033) — STABLE SECURITY DEFINER, count(*) over (), f_unaccent + ilike.
0060 de_xuat_dinh_muc đọc luu_tru_* để TÍNH tốc độ bán (chỉ trả số tổng hợp theo mã) — KHÔNG đổi (xem threat T-06-12).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pgTAP 37 mới + đổi kỳ vọng KiotViet trong 42 và 33 (RED)</name>
  <files>supabase/tests/37_lich_su_kiotviet_test.sql, supabase/tests/42_the_kho_test.sql, supabase/tests/33_the_kho_luy_ke_test.sql</files>
  <read_first>
    - `supabase/tests/00_helper.sql.inc`, `supabase/tests/35_nap_ton_tam_test.sql` — khuôn file.
    - `supabase/tests/42_the_kho_test.sql` toàn bộ — dòng 62-84 chèn luu_tru_* và assert chuỗi nguồn `'HE_THONG,KIOTVIET_BAN,KIOTVIET_NHAP'`.
    - `supabase/tests/33_the_kho_luy_ke_test.sql` toàn bộ — assertion 4 (dòng ~116) kiểm `ton_luy_ke is null` ở dòng `KIOTVIET_BAN`.
    - `supabase/tests/51_doi_tac_ghi_chu_test.sql` — cách test hiện có dựng đối tác + ánh xạ ghi chú cho `lich_su_giao_dich_doi_tac` (nếu có); nếu không có thì đọc `0033` nhánh KIOTVIET để biết điều kiện khớp `nha_cung_cap like dt.ma || ' %'`.
    - `.memory/patterns/pgtap-va-test.md` mục 2 (từ khóa không khớp dữ liệu thật — dùng `ZQX`), mục 11.
  </read_first>
  <behavior>
    pgTAP 37 (dữ liệu test mã phiếu/mã hàng chứa `ZQX`, ngày năm 2091 để không đụng 5.326 dòng thật):
    - B1: `p_loai := 'NHAP'` chỉ trả nguồn NHAP; `'XUAT'` chỉ XUAT; `p_loai` khác hai giá trị trên → throws 23514.
    - B2: `p_so_phieu` khớp tuyệt đối trả đủ MỌI dòng của phiếu đó (D-10 mở lại nguyên phiếu).
    - B3: `p_ma_hang` khớp tuyệt đối; `p_san_pham_id` của một sản phẩm test có `ma_hang` = mã trong lưu trữ trả đúng các dòng đó.
    - B4: `p_tu_khoa := 'quynh'` khớp dòng hóa đơn có `ghi_chu` = 'KHÁCH QUỲNH ZQX'; `p_tu_khoa := 'zqx ncc'` khớp dòng nhập có `nha_cung_cap` chứa chuỗi đó (D-12).
    - B5: dòng `ngay = '2091-03-01T23:30:00+07:00'` NẰM TRONG `p_tu_ngay = p_den_ngay = '2091-03-01'` (ngày Việt Nam), dòng `'2091-03-02T00:30:00+07:00'` KHÔNG nằm trong.
    - B6: `tong_so_dong` bằng tổng số dòng khớp dù `p_kich_thuoc := 1`; `tong_nhap`/`tong_xuat` = tổng so_luong theo nguồn trên toàn bộ tập khớp.
    - B7: dưới `vanphong` với cột `xem_lich_su_kiotviet = false` (đặt dưới postgres trong transaction): RPC throws 42501, `select count(*) from luu_tru_nhap_kiotviet` = 0 (RLS), và `lich_su_giao_dich_doi_tac` của đối tác test KHÔNG trả dòng nguồn `KIOTVIET_%`.
    - B8: bật lại cột → cả ba cửa trả dữ liệu ngay trong cùng transaction.
    - B9: dưới `chixem` (cột false): RPC 42501; dưới `quanly` với cột false: RPC trả dữ liệu (D-15).
    - B10: `the_kho_san_pham` của sản phẩm test có dòng lưu trữ trùng `ma_hang` KHÔNG trả dòng nào có `nguon like 'KIOTVIET%'`, kể cả dưới `quanly` (D-11).
    Sửa 42 và 33: assertion nào đang kỳ vọng có dòng `KIOTVIET_*` trong thẻ kho đổi thành kỳ vọng KHÔNG có; giữ nguyên `plan(n)` nếu số assertion không đổi, sửa `plan(n)` nếu thêm/bớt.
  </behavior>
  <action>
    Tạo `supabase/tests/37_lich_su_kiotviet_test.sql` theo khuôn 35 với các assertion B1-B10; chèn dữ liệu lưu trữ dưới
    postgres (bảng không có policy ghi). Đối tác test cho B7: tạo `doi_tac` loại NCC có `ma` = tiền tố của
    `nha_cung_cap` trong dòng nhập test (điều kiện `like dt.ma || ' %'` của 0033).

    Sửa `42_the_kho_test.sql`: assertion so chuỗi nguồn đổi thành chỉ `'HE_THONG'`; assertion đọc dòng `KIOTVIET_BAN` đổi
    thành `is((select count(*) … where nguon like 'KIOTVIET%'), 0::bigint, …)`. Sửa `33_the_kho_luy_ke_test.sql` assertion 4
    tương tự (không còn dòng KIOTVIET nào). Mô tả assertion ghi rõ "D-11: thẻ kho không trộn lịch sử KiotViet".

    Chạy RED qua MCP (dòng finish đổi theo pattern 9): 37 phải lỗi vì chưa có RPC; 42/33 phải đỏ vì thẻ kho vẫn trộn.
    Ghi kết quả RED vào SUMMARY.
  </action>
  <verify>
    <automated>test -f supabase/tests/37_lich_su_kiotviet_test.sql &amp;&amp; grep -q "tra_cuu_lich_su_kiotviet" supabase/tests/37_lich_su_kiotviet_test.sql &amp;&amp; grep -q "lich_su_giao_dich_doi_tac" supabase/tests/37_lich_su_kiotviet_test.sql &amp;&amp; grep -q "2091-03-01T23:30:00+07:00" supabase/tests/37_lich_su_kiotviet_test.sql &amp;&amp; ! grep -q "HE_THONG,KIOTVIET_BAN,KIOTVIET_NHAP" supabase/tests/42_the_kho_test.sql &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - `plan(N)` của cả ba file khớp đúng số assertion trong file.
    - File 37 dùng `ZQX` cho mọi mã/tên test và năm 2091 cho mọi ngày test.
    - SUMMARY ghi kết quả RED của cả ba file.
  </acceptance_criteria>
  <done>pgTAP 37 viết xong; 42 và 33 kỳ vọng thẻ kho không còn dòng KiotViet; cả ba đỏ trước migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0064 — RPC tra cứu, ba cửa quyền, bỏ KiotViet khỏi thẻ kho (GREEN)</name>
  <files>supabase/migrations/0064_lich_su_kiotviet.sql</files>
  <read_first>
    - `supabase/migrations/0033_ra_ghi_chu_lich_su.sql` toàn bộ — `danh_sach_ghi_chu_kiotviet` (khuôn), `lich_su_giao_dich_doi_tac`.
    - `supabase/migrations/0062_sua_the_kho_cot_mo_ho.sql` toàn bộ — `the_kho_san_pham` hiện hành.
    - `supabase/migrations/0016_rls_chung_tu.sql` dòng 113-122 — hai policy đọc lưu trữ.
    - `.memory/patterns/pgtap-va-test.md` mục 10 — cột trơn trong `RETURNS TABLE` gây 42702 lúc CHẠY; mọi SELECT/ORDER BY cuối phải có tiền tố bảng.
    - `.memory/patterns/supabase-rls-bao-mat.md` — qualify toán tử/hàm khi `search_path = ''` (`public.f_unaccent`).
    - Định nghĩa ĐANG CHẠY trên cloud qua MCP `execute_sql`:
      `select pg_get_functiondef('public.lich_su_giao_dich_doi_tac(uuid,integer,integer)'::regprocedure);`,
      `select pg_get_functiondef(p.oid) from pg_proc p where p.proname = 'the_kho_san_pham';`,
      `select policyname, cmd, qual, with_check from pg_policies where tablename in ('luu_tru_nhap_kiotviet','luu_tru_hoa_don_kiotviet');`.
      Dán vào comment đầu migration. Nếu bản cloud khác file 0033/0062 thì lấy BẢN CLOUD làm gốc.
    - Kiểm giả định A5 (06-RESEARCH): `select count(*) from luu_tru_nhap_kiotviet where ngay is not null and ngay !~ '^\d{4}-\d{2}-\d{2}T'` và tương tự bảng hóa đơn. Khác 0 → DỪNG, báo lại (ép `::timestamptz` sẽ lỗi trên dòng thật).
  </read_first>
  <action>
    Tạo `supabase/migrations/0064_lich_su_kiotviet.sql`, header nêu D-10..D-13 và "ba cửa" (0016, 0033, 0062):

    1. Policy: `drop policy "doc luu tru nhap" on public.luu_tru_nhap_kiotviet;` rồi `create policy` cùng tên
       `for select to authenticated using ((select public.xem_duoc_lich_su_kiotviet()))`. Tương tự "doc luu tru hoa don".
    2. `public.tra_cuu_lich_su_kiotviet(p_loai text default null, p_tu_khoa text default null, p_ma_hang text default null,
       p_san_pham_id uuid default null, p_so_phieu text default null, p_tu_ngay date default null, p_den_ngay date default null,
       p_trang integer default 1, p_kich_thuoc integer default 50)` returns table(nguon text, ma_phieu text, ngay timestamptz,
       doi_tac text, ma_hang text, ten_hang text, so_luong numeric, ghi_chu text, tong_so_dong bigint, tong_nhap numeric,
       tong_xuat numeric)`, `language plpgsql stable security definer set search_path = ''`. Thân:
       - đầu hàm: `if not (select public.xem_duoc_lich_su_kiotviet()) then raise … using errcode = '42501'`;
         `p_loai` không null và không thuộc ('NHAP','XUAT') → raise 23514 "Loại phải là NHAP hoặc XUAT".
       - CTE `tat_ca` union all hai bảng như 06-RESEARCH §Data Model mục 2 (nguồn 'NHAP' từ nhập với `nha_cung_cap` làm
         `doi_tac`, 'XUAT' từ hóa đơn với `khach_hang`), `ngay::timestamptz`.
       - `p_san_pham_id` → so `ma_hang = (select sp.ma_hang from public.san_pham sp where sp.id = p_san_pham_id)`.
       - Khoảng ngày theo giờ Việt Nam: `(t.ngay at time zone 'Asia/Ho_Chi_Minh')::date between coalesce(p_tu_ngay, …) and …`
         (không so thẳng timestamptz với date — máy chủ chạy UTC).
       - `p_so_phieu` và `p_ma_hang` so tuyệt đối sau `trim`; `p_tu_khoa` (sau `nullif(trim(...),'')`) so bằng
         `public.f_unaccent(coalesce(ma_hang,'')||' '||coalesce(ten_hang,'')||' '||coalesce(doi_tac,'')||' '||coalesce(ghi_chu,''))
         ilike '%' || public.f_unaccent(trim(p_tu_khoa)) || '%'` (D-12).
       - SELECT cuối có tiền tố bảng cho mọi cột, `count(*) over ()`, `sum(so_luong) filter (where nguon='NHAP') over ()`,
         `… 'XUAT' …`, `order by t.ngay desc nulls last, t.ma_phieu, t.ma_hang`, `limit least(greatest(coalesce(p_kich_thuoc,50),1),500)`
         `offset (greatest(coalesce(p_trang,1),1)-1) * <cùng limit>`.
       - Không trả `don_gia`/`thanh_tien` (D-17: không dùng giá).
       `revoke all … from public, anon; grant execute … to authenticated;` + `comment on function`.
    3. `lich_su_giao_dich_doi_tac`: `create or replace` từ bản cloud, đổi DUY NHẤT dòng gán thành
       `v_xem_kv := (select public.xem_duoc_lich_su_kiotviet());`. Giữ `v_vai_tro` (nhánh thủ kho vẫn dùng).
    4. `the_kho_san_pham`: `create or replace` từ bản cloud (cùng chữ ký, cùng kiểu trả — không drop), bỏ hai nhánh
       `union all` đọc `luu_tru_*` và biến `v_xem_kv`; mọi phần khác (tồn lũy kế, lọc kho thủ kho, giấu giá vốn) giữ nguyên
       từng ký tự. Comment: "D-11 Phase 6: lịch sử KiotViet xem ở tra_cuu_lich_su_kiotviet, không trộn vào thẻ kho".
       Cấp lại grant y như bản cloud nếu `create or replace` cần.
    5. Khối tự kiểm "Còn bảng chưa bật RLS".

    GREEN bằng dry-run MCP một lệnh: `begin;` + nguyên văn 0063 (chưa lên cloud) + 0064 + thân test 36, 37, 42, 33
    (mỗi file bỏ `begin;`/`rollback;` riêng, finish theo pattern 9, `plan()` từng file chạy tuần tự hoặc chạy bốn lệnh riêng,
    mỗi lệnh đều bắt đầu bằng 0063+0064) + `rollback;`. Mọi file phải trả `DAT`. Không `db:push`.
  </action>
  <verify>
    <automated>grep -q "function public.tra_cuu_lich_su_kiotviet" supabase/migrations/0064_lich_su_kiotviet.sql &amp;&amp; grep -q "v_xem_kv := (select public.xem_duoc_lich_su_kiotviet())" supabase/migrations/0064_lich_su_kiotviet.sql &amp;&amp; grep -q "Asia/Ho_Chi_Minh" supabase/migrations/0064_lich_su_kiotviet.sql &amp;&amp; grep -c "xem_duoc_lich_su_kiotviet" supabase/migrations/0064_lich_su_kiotviet.sql | awk '$1&gt;=4{print "GATE-OK"}'</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - Dry-run MCP trả `DAT` cho 36, 37, 42 và 33; SUMMARY ghi nguyên kết quả.
    - `grep -v '^\s*--' supabase/migrations/0064_lich_su_kiotviet.sql | grep -c "in ('quan_ly','van_phong')"` = 0.
    - Trong thân `the_kho_san_pham` mới: `grep -c "luu_tru_" ` chỉ xuất hiện trong comment (lọc `--` còn 0 ở phần định nghĩa hàm này).
    - Kết quả kiểm A5 (số dòng `ngay` sai định dạng, phải = 0) ghi vào SUMMARY.
    - `grep -c "don_gia\|thanh_tien" ` trong định nghĩa `tra_cuu_lich_su_kiotviet` = 0.
  </acceptance_criteria>
  <done>0064 viết xong; bốn file pgTAP liên quan dry-run DAT trên cloud rồi rollback; ba cửa quyền đều đọc công tắc; thẻ kho hết dòng KiotViet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PostgREST SELECT luu_tru_* | Đọc thẳng bảng, bỏ qua RPC |
| client → RPC SECURITY DEFINER đọc luu_tru_* | Hàm bỏ qua RLS, tự gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information Disclosure | SELECT thẳng `luu_tru_*` khi công tắc tắt | mitigate | Hai policy mới gọi `(select public.xem_duoc_lich_su_kiotviet())`; pgTAP B7 đếm 0 dòng |
| T-06-08 | Elevation of Privilege | `lich_su_giao_dich_doi_tac` (0033, SECURITY DEFINER) vẫn gate theo vai trò cũ | mitigate | Đổi dòng `v_xem_kv`; pgTAP B7/B8 kiểm không/có dòng KIOTVIET_% |
| T-06-09 | Elevation of Privilege | `the_kho_san_pham` (0062, SECURITY DEFINER) — cửa thứ ba không có trong nghiên cứu | mitigate | Bỏ hẳn hai nhánh lưu trữ (D-11); pgTAP B10 + 42 + 33 |
| T-06-10 | Information Disclosure | RPC tra cứu mới không tự gate | mitigate | Kiểm helper đầu hàm, 42501; pgTAP B7, B9 |
| T-06-11 | Denial of Service | `p_kich_thuoc` cực lớn quét 5.326 dòng + unaccent | mitigate | Kẹp `least(greatest(…,1),500)` |
| T-06-12 | Information Disclosure | `de_xuat_dinh_muc` (0060) đọc luu_tru_* để tính tốc độ bán | accept | Chỉ trả số tổng hợp theo mã cho quản lý/văn phòng duyệt định mức, không lộ dòng/khách/ghi chú; không phải "xem lịch sử" |
| T-06-13 | Tampering | Ép `ngay::timestamptz` trên dòng sai định dạng làm RPC lỗi cho mọi người | mitigate | Kiểm A5 trên dữ liệu thật trước khi viết; dừng nếu có dòng sai |
</threat_model>

<verification>
- Dry-run DAT cho pgTAP 36, 37, 42, 33.
- `grep` không còn gate `in ('quan_ly','van_phong')` cho dữ liệu lưu trữ trong 0064.
</verification>

<success_criteria>
- Một RPC phục vụ cả màn `/lich-su-kiotviet` lẫn tab chi tiết mã hàng (D-11).
- Commit: `test(06-02): pgTAP tra cuu lich su KiotViet` và `feat(db): tra cuu lich su KiotViet theo cong tac quyen`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-02-SUMMARY.md` when done
</output>
