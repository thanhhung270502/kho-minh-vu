---
phase: 06-kiem-ke-go-live
plan: 14
type: execute
wave: 5
depends_on: [06-09, 06-13]
files_modified:
  - src/features/stocktake/api/count-import.api.ts
  - src/features/stocktake/hooks/useCountImport.ts
  - src/features/stocktake/components/count-excel-import.tsx
autonomous: true
requirements: [KKE-02, DLIEU-06]

must_haves:
  truths:
    - "Người đếm tải file mẫu cho từng nhóm hàng của phiên, không thấy số tồn trong file (D-08)"
    - "Chọn file đã điền → hệ tự kiểm tra và hiện số dòng mới / ghi đè / bỏ qua (ô trống) / lỗi kèm lý do từng dòng, CHƯA ghi gì"
    - "Chỉ bấm được 'Nạp số đếm' khi 0 dòng lỗi; nạp xong bảng đếm và bảng lệch tự cập nhật (D-04)"
    - "Hết phiên đưa về đăng nhập có ?tiep_tuc=; không quyền báo liên hệ quản lý — hai luồng khác nhau"
  artifacts:
    - path: "src/features/stocktake/components/count-excel-import.tsx"
      provides: "Luồng tải mẫu → kiểm tra → nạp"
      contains: "Upload.Dragger"
    - path: "src/features/stocktake/api/count-import.api.ts"
      provides: "Gọi route /api/kiem-ke/nhap-excel, đọc khóa result"
      contains: "/api/kiem-ke/nhap-excel"
  key_links:
    - from: "useCountImport onSuccess (che_do nap)"
      to: "stocktakeKeys.sheet + stocktakeKeys.sessions"
      via: "invalidateQueries"
      pattern: "invalidateQueries"
---

<objective>
Phía giao diện của đường nhập Excel: chọn nhóm → tải file mẫu → tải file đã điền → xem kiểm tra → nạp.

Purpose: đợt đếm đầu kỳ D-01 đi đường này (giấy/Excel ngoài hệ rồi nhập lại). Khuôn 3 bước giống nạp tồn tạm Phase 5.

Output: api gọi route, hook mutation, component 3 bước. Ghép vào trang chi tiết ở 06-15.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-13-PLAN.md
@.planning/phases/06-kiem-ke-go-live/06-09-PLAN.md

<interfaces>
Route (06-13): GET /api/kiem-ke/mau-excel?phien=<uuid>&nhom=<uuid|trống> → file; POST /api/kiem-ke/nhap-excel FormData {file, phien, che_do}
  → 200 { result: { da_nap, dat, cap_nhat, bo_qua, so_loi, chi_tiet_dat, chi_tiet_cap_nhat, chi_tiet_bo_qua, loi, ly_do? } } | 4xx/5xx { title, action }.
Từ 06-09: stocktakeKeys, useCountSheet (lấy danh sách nhóm có trong phiên), StocktakeLookups.
Khuôn: src/features/inventory/api/provisional-stock.api.ts (fetch route, 401 → window.location.assign("/dang-nhap?tiep_tuc=…")),
  src/features/inventory/hooks/useProvisionalStockFlow.ts (useReducer bước), src/features/inventory/components/provisional-stock-preview.tsx,
  provisional-stock-issues.tsx (Statistic + bảng lỗi).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api gọi route + hook luồng nhập</name>
  <files>src/features/stocktake/api/count-import.api.ts, src/features/stocktake/hooks/useCountImport.ts</files>
  <read_first>
    - `src/features/inventory/api/provisional-stock.api.ts` toàn bộ (mapper kết quả route → kiểu tiếng Anh, 401 redirect).
    - `src/features/inventory/hooks/useProvisionalStockFlow.ts` toàn bộ.
    - `src/features/stocktake/api/stocktake.keys.ts` (06-09).
  </read_first>
  <action>
    `count-import.api.ts`: kiểu `CountImportResult { loaded: boolean; newCount; overwriteCount; skippedCount; errorCount; newRows; overwriteRows;
    skippedRows: { code, reason }[]; errors: { code, reason }[]; reason: string | null }` + mapper từ khóa snake_case của `result` (chỉ file này thấy
    khóa đó). `postCountFile({ sessionId, file, mode: "check" | "load" })` gửi FormData (`phien`, `che_do` = "nap" | "kiem_tra"), đọc `json.result`;
    `res.status === 401` → `window.location.assign("/dang-nhap?tiep_tuc=" + encodeURIComponent(location.pathname))` rồi ném; 4xx/5xx khác → ném
    `Error` mang `title` + `action` của route (để component hiện nguyên). `countTemplateUrl(sessionId, categoryId)` trả chuỗi URL GET.

    `useCountImport.ts` ("use client"): `useReducer` bước `idle → checked → loaded` giữ file đã chọn + kết quả kiểm tra; `check(file)` và `load()`
    dùng `useMutation`; `load` thành công → `invalidateQueries` tiền tố `stocktakeKeys.sheet(sessionId…)` và `stocktakeKeys.sessions`; `reset()`.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; grep -q "/api/kiem-ke/nhap-excel" src/features/stocktake/api/count-import.api.ts &amp;&amp; grep -q "json.result\|\.result" src/features/stocktake/api/count-import.api.ts &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - `grep -c "tiep_tuc" src/features/stocktake/api/count-import.api.ts` ≥ 1.
    - `grep -rn "chi_tiet_\|so_loi\|da_nap" src/features/stocktake --include=*.tsx` rỗng (snake_case chỉ trong api).
  </acceptance_criteria>
  <done>Client gọi route đúng hợp đồng `{ result }`, tách 401 khỏi lỗi khác, hook quản bước.</done>
</task>

<task type="auto">
  <name>Task 2: Component nhập số đếm từ Excel</name>
  <files>src/features/stocktake/components/count-excel-import.tsx</files>
  <read_first>
    - `src/features/inventory/components/provisional-stock-preview.tsx`, `provisional-stock-issues.tsx`; `src/features/products/components/cost-import.tsx` (nếu còn).
    - CLAUDE.md bẫy 11 (`Alert title`), Bước 5.
  </read_first>
  <action>
    `count-excel-import.tsx` ("use client"): props `{ sessionId: string; editable: boolean }`. Ba khối:
    1) "Tải file mẫu": `Select` nhóm (các nhóm có mã trong phiên lấy từ `useCountSheet(sessionId, "")`, tính khi render; thêm "Toàn phạm vi") +
       nút `<a href={countTemplateUrl(...)} download>` "Tải file mẫu nhóm này". Câu: "Mỗi nhóm một file. File không có số tồn — người đếm điền cột
       Số đếm. Để trống = chưa đếm." (D-08, D-07).
    2) "Nhập file đã điền": `Upload.Dragger` (accept .xlsx, `beforeUpload` trả false và gọi `check(file)`), khi đang kiểm: loading.
    3) Kết quả kiểm tra: bốn `Statistic` (Mới, Ghi đè số cũ — kèm chú thích "chốt lại tồn sổ lúc nạp", Bỏ qua ô trống, Lỗi); bảng lỗi (mã, lý do);
       bảng bỏ qua thu gọn; nút "Nạp số đếm" `type="primary"` disabled khi `errorCount > 0` (Tooltip "Sửa hết dòng lỗi trong file rồi tải lại — không
       nạp nửa vời") hoặc đang nạp; nạp xong `message.success("Đã nạp {n} số đếm")` + reset. Lỗi route: hiện `title` + `action` trong `Alert` error có nút
       "Thử lại". `!editable` → chỉ hiện khối 1 (tải mẫu vẫn hữu ích cho in giấy) và câu "Phiên đã duyệt/hủy — không nhập được".
    Giữ ≤ 200 dòng; vượt thì tách khối kết quả ra `count-import-result.tsx` (thêm vào commit).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` thoát mã 0.
    - `grep -c "Upload.Dragger" src/features/stocktake/components/count-excel-import.tsx` = 1.
    - `grep -c "errorCount > 0\|errorCount>0" src/features/stocktake/components/count-excel-import.tsx src/features/stocktake/components/count-import-result.tsx 2>/dev/null` tổng ≥ 1.
  </acceptance_criteria>
  <done>Luồng tải mẫu → kiểm tra → nạp chạy trong component, chặn nạp khi còn lỗi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trình duyệt → route nhập Excel | File người dùng |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-61 | Tampering | Bấm "Nạp" khi còn lỗi bằng cách gửi thẳng che_do=nap | mitigate | RPC không ghi khi `so_loi > 0` (0065) — nút disable chỉ là trang trí |
| T-06-62 | Spoofing | 401 bị hiểu thành lỗi chung, người dùng mất dữ liệu đang làm | mitigate | Redirect có `?tiep_tuc=` về đúng trang phiên |
| T-06-63 | Tampering | Nạp hai lần do bấm lặp | mitigate | Nút loading khi đang nạp; nạp lần hai chỉ ghi đè cùng số (upsert, idempotent) |
</threat_model>

<verification>
- `npm run check` xanh.
</verification>

<success_criteria>
- Commit: `feat(kiem-ke): nhap so dem tu Excel`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-14-SUMMARY.md` when done
</output>
