---
phase: 06-kiem-ke-go-live
plan: 06
type: execute
wave: 4
depends_on: [06-05]
files_modified:
  - src/features/auth/api/current-user.server.ts
  - src/features/settings/schemas/user.schema.ts
  - src/features/settings/actions/user.actions.ts
  - src/features/settings/api/user.api.ts
  - src/features/settings/components/user-drawer.tsx
  - src/features/settings/components/user-table.tsx
autonomous: true
requirements: [DLIEU-07, KKE-04]

must_haves:
  truths:
    - "Quản lý bật/tắt 'Xem lịch sử KiotViet' và 'Duyệt kiểm kê' cho từng người trong Cài đặt → Người dùng (D-13, D-14)"
    - "Với người vai trò quan_ly hai ô luôn tick và bị khóa — giao diện không cho tưởng là tắt được (D-15)"
    - "Giao diện nói đúng: bật hay tắt đều có hiệu lực ở lần tải trang kế tiếp, không phải chờ đăng nhập lại (D-15, bẫy 6)"
    - "Server component đọc được canViewKiotVietHistory / canApproveStocktake của người đang đăng nhập từ bảng, không từ JWT"
  artifacts:
    - path: "src/features/auth/api/current-user.server.ts"
      provides: "CurrentUser mở rộng + requireKiotVietHistoryAccess()"
      contains: "canApproveStocktake"
    - path: "src/features/settings/components/user-drawer.tsx"
      provides: "Hai Checkbox công tắc"
      contains: "Duyệt kiểm kê"
  key_links:
    - from: "src/features/settings/actions/user.actions.ts"
      to: "rpc luu_ho_so_nguoi_dung"
      via: "p_xem_lich_su_kiotviet, p_duyet_kiem_ke ở cả ba lời gọi"
      pattern: "p_duyet_kiem_ke"
    - from: "src/features/auth/api/current-user.server.ts"
      to: "nguoi_dung.xem_lich_su_kiotviet / duyet_kiem_ke"
      via: "cùng một select đang có"
      pattern: "xem_lich_su_kiotviet"
---

<objective>
Giao diện và lớp server cho hai công tắc quyền theo người.

Purpose: 06-01 đã chặn thật ở database; plan này cho quản lý thao tác được và cho các route mới (06-08, 06-15)
biết người đang đăng nhập có quyền gì để ẩn/hiện và redirect. Hai field KHÔNG vào `PERMISSION_MATRIX` — ma trận
đó chủ đích chỉ ánh xạ vai trò → quyền (06-PATTERNS).

Output: `CurrentUser` mở rộng + helper redirect; Server Action truyền hai tham số; drawer có hai ô; bảng người dùng hiện nhãn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/06-kiem-ke-go-live/06-PATTERNS.md
@design/cai-dat.html

<interfaces>
current-user.server.ts hiện có: CurrentUser { id, fullName, role: Role, mustChangePassword }, getCurrentUser() select
"id, ho_ten, vai_tro, dang_hoat_dong, phai_doi_mat_khau" từ nguoi_dung; requirePermission(permission) redirect /dang-nhap | /khong-du-quyen.
RPC sau 0063: luu_ho_so_nguoi_dung(p_id, p_ho_ten, p_ten_dang_nhap, p_vai_tro, p_kho_ids, p_phai_doi_mat_khau,
  p_xem_lich_su_kiotviet boolean default null, p_duyet_kiem_ke boolean default null) — null = giữ nguyên.
user.actions.ts: createUser, updateUser, resetPassword gọi rpc("luu_ho_so_nguoi_dung", {...}); readProfile select
  "id, ho_ten, ten_dang_nhap, vai_tro, dang_hoat_dong, phai_doi_mat_khau".
user.schema.ts: userProfileSchema { fullName, role, warehouseIds } .refine thủ kho có kho; createUserSchema, updateUserSchema, editUserFormSchema.
user-drawer.tsx: UserFormValues { fullName, username, role, warehouseIds, tempPassword }; Controller + antd Checkbox đã import.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CurrentUser, schema, Server Action truyền hai công tắc</name>
  <files>src/features/auth/api/current-user.server.ts, src/features/settings/schemas/user.schema.ts, src/features/settings/actions/user.actions.ts</files>
  <read_first>
    - `src/features/auth/api/current-user.server.ts` toàn bộ (49 dòng).
    - `src/features/settings/schemas/user.schema.ts` toàn bộ.
    - `src/features/settings/actions/user.actions.ts` toàn bộ — ba lời gọi RPC ở createUser, updateUser, resetPassword; `readProfile`.
    - `src/types/database.types.ts` — mục `luu_ho_so_nguoi_dung` Args và `nguoi_dung` Row sau 06-05.
  </read_first>
  <action>
    `current-user.server.ts`: thêm vào `CurrentUser` hai field `canViewKiotVietHistory: boolean` và `canApproveStocktake: boolean`;
    nối `xem_lich_su_kiotviet, duyet_kiem_ke` vào CHUỖI select đang có (không thêm round-trip); tính
    `canViewKiotVietHistory = vai_tro === "quan_ly" || xem_lich_su_kiotviet`, tương tự `canApproveStocktake` (D-15, khớp helper SQL).
    Thêm `export async function requireKiotVietHistoryAccess(): Promise<CurrentUser>` — không đăng nhập → `redirect("/dang-nhap")`,
    `!canViewKiotVietHistory` → `redirect("/khong-du-quyen")`. Comment: quyền theo người, không nằm trong PERMISSION_MATRIX; chặn thật ở RLS 0064.

    `user.schema.ts`: thêm vào `userProfileSchema` hai field `viewKiotVietHistory: z.boolean().default(false)` và
    `approveStocktake: z.boolean().default(false)` (trước `.refine`). Không đổi luật thủ kho.

    `user.actions.ts`: `readProfile` select thêm hai cột; createUser và updateUser truyền `p_xem_lich_su_kiotviet: values.viewKiotVietHistory`,
    `p_duyet_kiem_ke: values.approveStocktake`; resetPassword truyền lại giá trị CŨ đọc từ `previous` (không để null rơi vào ý nghĩa ngầm —
    ghi rõ ý định). KHÔNG gọi `revokeSessions` khi chỉ đổi công tắc: helper đọc bảng nên có hiệu lực ngay, thu hồi phiên là thừa
    (comment lý do). Giữ nguyên kiểm `getAdminSession()` (quản lý, đọc bảng).
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; grep -c "p_duyet_kiem_ke" src/features/settings/actions/user.actions.ts | awk '$1&gt;=3{print "GATE-OK"}'</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK` (typecheck xanh, `p_duyet_kiem_ke` xuất hiện ở cả ba lời gọi).
    - `grep -c "p_xem_lich_su_kiotviet" src/features/settings/actions/user.actions.ts` ≥ 3.
    - `grep -c "canViewKiotVietHistory\|canApproveStocktake" src/shared/lib/permissions.ts` = 0 (không nhét vào ma trận vai trò).
    - `grep -c "export async function requireKiotVietHistoryAccess" src/features/auth/api/current-user.server.ts` = 1.
  </acceptance_criteria>
  <done>CurrentUser có hai quyền theo người; Server Action ghi đúng hai công tắc ở mọi đường.</done>
</task>

<task type="auto">
  <name>Task 2: Hai ô công tắc trong UserDrawer + nhãn trong bảng người dùng</name>
  <files>src/features/settings/api/user.api.ts, src/features/settings/components/user-drawer.tsx, src/features/settings/components/user-table.tsx</files>
  <read_first>
    - `src/features/settings/api/user.api.ts` toàn bộ — `userQuery` là nguồn kiểu `UserRow` (QueryData).
    - `src/features/settings/components/user-drawer.tsx` toàn bộ (274 dòng) — `UserFormValues`, `reset(...)`, `onSave`, khối JSX chọn vai trò/kho.
    - `src/features/settings/components/user-table.tsx` toàn bộ — cột vai trò.
    - `design/cai-dat.html` phần `renderUsers` — vị trí/bố cục tham khảo.
    - CLAUDE.md bẫy 11 (prop antd v6 đã bỏ) và Bước 5 (form).
  </read_first>
  <action>
    `user.api.ts`: nối `xem_lich_su_kiotviet, duyet_kiem_ke` vào select của `userQuery` (kiểu tự suy).

    `user-drawer.tsx`: thêm `viewKiotVietHistory: boolean`, `approveStocktake: boolean` vào `UserFormValues`, `defaultValues` (false/false),
    cả hai nhánh `reset(...)` (sửa: lấy từ `user.xem_lich_su_kiotviet` / `user.duyet_kiem_ke`), và `onSave` truyền xuống `updateUser`/`createUser`.
    Dưới khối chọn kho thêm nhóm "Quyền riêng" với hai `Controller` + `Checkbox`: "Xem lịch sử KiotViet" (mô tả: "Tra 594 phiếu nhập và 4.732
    hóa đơn cũ") và "Duyệt kiểm kê" (mô tả: "Bấm duyệt phiên kiểm kê — sinh phiếu điều chỉnh tồn"). Khi `role === "quan_ly"`: cả hai
    `checked` và `disabled`, dòng phụ "Quản lý luôn có quyền này" (D-15). Dòng ghi chú cho cả nhóm: "Bật/tắt có hiệu lực ở lần tải trang kế
    tiếp của người đó, không cần đăng nhập lại." (D-15 — nói đúng, khác cảnh báo 60 phút của vai trò/kho). Nút lưu vẫn disabled khi đang chạy
    (đã có `useTransition`). Nếu file vượt ~200 dòng sau khi thêm, tách nhóm này ra `components/user-special-permissions.tsx` (CLAUDE.md Bước 6)
    và thêm file đó vào commit.

    `user-table.tsx`: trong cột vai trò (hoặc cột mới "Quyền riêng") hiện `Tag` "LS KiotViet" / "Duyệt KK" khi cột tương ứng true hoặc vai trò
    `quan_ly`. Không đổi luồng khác.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` thoát mã 0.
    - `grep -c "Duyệt kiểm kê" src/features/settings/components/user-drawer.tsx src/features/settings/components/user-special-permissions.tsx 2>/dev/null` tổng ≥ 1 và tương tự "Xem lịch sử KiotViet".
    - Không có `console.log`, `any`, `@ts-ignore` mới (`git diff | grep -E "^\+.*(console\.log|: any|@ts-ignore)"` rỗng).
    - Mở `/cai-dat/nguoi-dung` bằng `quanly` trên trình duyệt, mở drawer một người văn phòng, tick/untick "Duyệt kiểm kê", lưu, mở lại thấy giá trị giữ; mở drawer quản lý thấy hai ô tick + khóa; console không cảnh báo antd. Ghi kết quả vào SUMMARY.
  </acceptance_criteria>
  <done>Quản lý bật/tắt được hai công tắc; quản lý luôn tick khóa; build xanh; đã kiểm trên trình duyệt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trình duyệt → Server Action updateUser | Dữ liệu form do client gửi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-34 | Elevation of Privilege | Người không phải quản lý gọi Server Action để tự bật công tắc | mitigate | `getAdminSession()` đọc bảng kiểm `quan_ly`; RPC tự kiểm 42501 + RLS (06-01) |
| T-06-35 | Tampering | Client gửi field lạ qua Server Action | mitigate | `updateUserSchema.safeParse` zod — chỉ nhận field khai báo |
| T-06-36 | Information Disclosure | Ẩn nút/route theo `canViewKiotVietHistory` bị coi là lớp bảo vệ | accept | Chỉ là trang trí; chặn thật ở RLS/RPC 0064 và `requireKiotVietHistoryAccess` ở page |
</threat_model>

<verification>
- `npm run check` xanh; kiểm drawer trên trình duyệt + console.
</verification>

<success_criteria>
- Commit: `feat(cai-dat): cong tac xem lich su KiotViet va duyet kiem ke theo nguoi`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-06-SUMMARY.md` when done
</output>
