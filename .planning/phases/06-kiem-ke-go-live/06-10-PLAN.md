---
phase: 06-kiem-ke-go-live
plan: 10
type: execute
wave: 5
depends_on: [06-09]
files_modified:
  - src/features/stocktake/components/session-list.tsx
  - src/features/stocktake/components/open-session-drawer.tsx
  - src/app/(app)/kiem-ke/page.tsx
autonomous: true
requirements: [KKE-01]

must_haves:
  truths:
    - "/kiem-ke liệt kê phiên theo kho/trạng thái với tiến độ đã đếm / trong phạm vi và nhãn trạng thái tính từ tiến độ"
    - "Người không phải chỉ xem mở được phiên: chọn kho (thủ kho chỉ thấy kho mình) + nhóm hàng tùy chọn — bỏ trống = toàn kho cho đợt đầu kỳ (KKE-01, D-01)"
    - "Màn nói rõ: kho không phải đóng khi kiểm kê, tồn sổ của mỗi mã chốt lúc lưu số đếm mã đó (D-02, D-03)"
    - "Bấm Mở phiên năm lần không tạo năm phiên — nút loading/disabled khi đang gửi"
  artifacts:
    - path: "src/app/(app)/kiem-ke/page.tsx"
      provides: "Route danh sách phiên"
      contains: "requirePermission"
    - path: "src/features/stocktake/components/open-session-drawer.tsx"
      provides: "Form mở phiên RHF + zod"
      contains: "openSessionSchema"
  key_links:
    - from: "open-session-drawer.tsx"
      to: "useOpenSession → rpc mo_phien_kiem_ke"
      via: "onSubmit rồi router.push(/kiem-ke/<id>)"
      pattern: "/kiem-ke/"
---

<objective>
Màn danh sách phiên kiểm kê và form mở phiên.

Purpose: điểm vào của KKE-01. Phiên = một kho, nhóm hàng là bộ lọc chia việc (06-RESEARCH A2, Claude's Discretion);
phân công người ↔ nhóm KHÔNG lưu DB — người đếm tự lọc nhóm của mình trên màn đếm (Open Question 2, chọn cách đơn giản).

Output: session-list, open-session-drawer, route `/kiem-ke`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/06-kiem-ke-go-live/06-09-PLAN.md
@design/kiem-ke.html

<interfaces>
Từ 06-09: useStocktakeSessions(args), useStocktakeLookups(), useOpenSession(); StocktakeSession; sessionStatus(), SESSION_STATUS_LABELS,
  SESSION_STATUS_COLORS; openSessionSchema / OpenSessionInput.
Khuôn màn danh sách: src/app/(app)/nhap-kho/page.tsx + src/features/stock-in/components/receipt-table.tsx, create-receipt-button.tsx;
  src/shared/components/form-drawer.tsx, list-layout.tsx, page-header.tsx, query-state.tsx.
design/kiem-ke.html: bố cục "Danh sách phiên" (cột Số phiên, Ngày, Kho, Trạng thái, Mở). CẢNH BÁO: câu "chốt tồn sổ tại thời điểm mở phiên"
  và phần quét mã trong demo là SAI so với D-03 và phạm vi đã chốt — không chép logic, chỉ chép bố cục.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Form mở phiên (drawer)</name>
  <files>src/features/stocktake/components/open-session-drawer.tsx</files>
  <read_first>
    - `src/shared/components/form-drawer.tsx` — props và cách nút lưu loading.
    - `src/features/stock-in/components/create-receipt-button.tsx` — khuôn tạo chứng từ rồi điều hướng.
    - `src/features/settings/components/user-drawer.tsx` — RHF + zodResolver + `setError` map lỗi server.
    - `src/shared/lib/errors.ts` — `explainError`, `errorCode`.
    - CLAUDE.md Bước 5 (form), bẫy 11 (`Select` không dùng `value: null`; `Drawer` dùng `size`).
  </read_first>
  <action>
    `open-session-drawer.tsx` ("use client"): props `{ open: boolean; onClose(): void; isStorekeeper: boolean }`. RHF + `zodResolver(openSessionSchema)`.
    Trường: Kho (`Select` từ `useStocktakeLookups().warehouses`; thủ kho chỉ thấy kho trong `assignedWarehouseIds`; có đúng một kho thì chọn sẵn),
    Nhóm hàng (`Select mode="multiple"` tìm được, không dấu nhờ `optionFilterProp` trên nhãn đã bỏ dấu bằng `removeDiacritics` từ
    `@/shared/lib/text`), Ghi chú. Dưới ô nhóm: "Bỏ trống = đếm toàn kho (dùng cho đợt đếm đầu kỳ trước go-live)".
    `Alert` info (prop `title`): "Kho KHÔNG cần đóng khi kiểm kê — phiếu nhập/xuất vẫn ghi sổ bình thường. Tồn sổ của mỗi mã được chốt đúng lúc
    lưu số đếm mã đó, nên đếm xong mã nào nhập ngay mã đó." (D-02, D-03).
    Submit: `useOpenSession().mutateAsync` → `router.push("/kiem-ke/" + id)`; nút loading/disabled khi `isPending`. Lỗi: `explainError`; mã `42501`
    → lỗi gốc form "Bạn không được mở phiên cho kho này — liên hệ quản lý"; `23514` → hiện `message` của database ở field `warehouseId` nếu nói về
    kho, ngược lại lỗi gốc; dữ liệu đã nhập giữ nguyên. Không `catch {}` rỗng.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; grep -q "openSessionSchema" src/features/stocktake/components/open-session-drawer.tsx &amp;&amp; grep -q "isPending\|loading" src/features/stocktake/components/open-session-drawer.tsx &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`; dòng đầu file là `"use client";`.
    - `grep -c "width=" src/features/stocktake/components/open-session-drawer.tsx` = 0; `grep -c "value: null" …` = 0.
    - `wc -l` ≤ 200.
  </acceptance_criteria>
  <done>Form mở phiên validate zod, chống bấm lặp, map lỗi server, điều hướng sang phiên mới.</done>
</task>

<task type="auto">
  <name>Task 2: Danh sách phiên + route /kiem-ke</name>
  <files>src/features/stocktake/components/session-list.tsx, src/app/(app)/kiem-ke/page.tsx</files>
  <read_first>
    - `src/features/stock-in/components/receipt-table.tsx`, `receipt-toolbar.tsx`; `src/app/(app)/nhap-kho/page.tsx`.
    - `src/shared/components/list-layout.tsx`, `query-state.tsx`.
    - `design/kiem-ke.html` phần `renderList` (bố cục).
    - CLAUDE.md Bước 5 (bảng cuộn ngang trong khung riêng trên điện thoại), bẫy 9.
  </read_first>
  <action>
    `session-list.tsx` ("use client"): props `{ canOpen: boolean; isStorekeeper: boolean }`. State lọc cục bộ: kho ("" = tất cả), trạng thái
    ("" / NHAP_LIEU / HOAN_THANH / DA_HUY), trang. `useStocktakeSessions` → `QueryState` (empty: "Chưa có phiên kiểm kê nào. Bấm “Mở phiên kiểm kê”
    để bắt đầu."). Cột: Số phiên (`Link` tới `/kiem-ke/{id}`), Ngày, Kho, Phạm vi ("Toàn kho" hoặc `categoryNames`), Tiến độ (`Progress` nhỏ +
    "{countedCount}/{scopeCount}"), Chờ đếm lại (số, ẩn khi 0), Trạng thái (`Tag` màu theo `sessionStatus(...)`), Người mở. Bảng
    `scroll={{ x: "max-content" }}` trong khung `overflow-x-auto`. Nút "Mở phiên kiểm kê" (khi `canOpen`) mở `OpenSessionDrawer`. Không cột giá (D-17).

    `src/app/(app)/kiem-ke/page.tsx` (Server Component, không antd): `metadata.title = "Kiểm kê"`; `const user = await requirePermission("view-catalog")`;
    `PageHeader` "Kiểm kê" + mô tả "Đếm thực tế, xem lệch, duyệt để đưa tồn về đúng số đã đếm"; render `<SessionList canOpen={user.role !== "chi_xem"}
    isStorekeeper={user.role === "thu_kho"} />` trong `Suspense`. Phạm vi kho của thủ kho siết trong RPC (0066), không ở route — cùng cách `/ton-kho`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` thoát mã 0.
    - `grep -c "use client" "src/app/(app)/kiem-ke/page.tsx"` = 0; `grep -c "requirePermission" "src/app/(app)/kiem-ke/page.tsx"` = 1.
    - Trên trình duyệt: `vanphong` mở `/kiem-ke`, mở phiên cho một kho + một nhóm hàng nhỏ, được đưa sang `/kiem-ke/<id>` (trang chi tiết có thể 404 cho tới khi 06-15 xong — ghi nhận, không phải lỗi); quay lại danh sách thấy phiên "Mới mở" 0/N; `thukho1` chỉ thấy kho mình trong ô Kho; `chixem` không thấy nút mở; thu hẹp cỡ điện thoại bảng cuộn ngang trong khung; console sạch cảnh báo antd. Ghi vào SUMMARY. Nếu đã tạo phiên thật trên cloud, hủy nó ở 06-15 hoặc ghi số phiên vào SUMMARY để UAT dọn.
  </acceptance_criteria>
  <done>/kiem-ke hiển thị và mở được phiên; build xanh; kiểm trên trình duyệt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → /kiem-ke | Gõ tay bỏ qua menu |
| form → RPC mo_phien_kiem_ke | Kho/nhóm do client chọn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-46 | Elevation of Privilege | Thủ kho sửa request mở phiên kho khác | mitigate | RPC kiểm `kho_hien_tai()` (0065, pgTAP C3); UI lọc chỉ là trang trí |
| T-06-47 | Elevation of Privilege | Chỉ xem mở phiên bằng console | mitigate | RPC 42501 cho chi_xem; nút ẩn |
| T-06-48 | Tampering | Bấm lặp tạo nhiều phiên | mitigate | Nút disabled khi `isPending` |
</threat_model>

<verification>
- `npm run check` xanh; kiểm trình duyệt ba vai trò.
</verification>

<success_criteria>
- Commit: `feat(kiem-ke): danh sach phien va mo phien`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-10-SUMMARY.md` when done
</output>
