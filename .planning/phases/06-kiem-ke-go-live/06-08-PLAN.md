---
phase: 06-kiem-ke-go-live
plan: 08
type: execute
wave: 5
depends_on: [06-06, 06-07]
files_modified:
  - src/features/kiotviet-history/components/history-filter-panel.tsx
  - src/features/kiotviet-history/components/history-screen.tsx
  - src/app/(app)/lich-su-kiotviet/page.tsx
  - scripts/test-pure-functions.ts
  - src/features/kiotviet-history/components/product-history-tab.tsx
  - src/features/products/components/product-detail.tsx
  - src/app/(app)/danh-muc/[id]/page.tsx
autonomous: true
requirements: [DLIEU-07]

must_haves:
  truths:
    - "Màn /lich-su-kiotviet lọc được theo loại nhập/bán, khoảng ngày, ô tìm tự do không dấu (khách/NCC + ghi chú), mã hàng, số phiếu (D-10, D-11, D-12)"
    - "Bộ lọc nằm trên URL — gửi link cho đồng nghiệp mở ra đúng kết quả; F5 không mất"
    - "Chi tiết mã hàng có tab 'Lịch sử KiotViet' cạnh 'Thẻ kho', chỉ hiện cho người có công tắc (D-11, D-13)"
    - "Người không có công tắc gõ thẳng /lich-su-kiotviet bị đẩy sang /khong-du-quyen; chưa đăng nhập về /dang-nhap?tiep_tuc="
  artifacts:
    - path: "src/app/(app)/lich-su-kiotviet/page.tsx"
      provides: "Route mỏng: metadata, PageHeader, requireKiotVietHistoryAccess"
      contains: "requireKiotVietHistoryAccess"
    - path: "src/features/kiotviet-history/components/product-history-tab.tsx"
      provides: "Tab lịch sử theo một mã hàng"
      contains: "productId"
  key_links:
    - from: "src/app/(app)/danh-muc/[id]/page.tsx"
      to: "ProductDetailView prop kiotVietHistoryTab"
      via: "route ghép hai feature — products không import kiotviet-history"
      pattern: "kiotVietHistoryTab"
    - from: "history-screen.tsx"
      to: "URL search params"
      via: "readHistoryFilterFromUrl / writeHistoryFilterToUrl"
      pattern: "readHistoryFilterFromUrl"
---

<objective>
Ghép hai nơi hiển thị lịch sử KiotViet (D-11): màn riêng `/lich-su-kiotviet` và tab trong chi tiết mã hàng.

Purpose: lớp dữ liệu và bảng đã có (06-07). Plan này thêm bộ lọc URL, route, tab, và test hàm thuần của bộ lọc.
Menu và ma trận quyền route để plan cuối (06-16) làm cùng mọi route mới của phase — theo tiền lệ 05-11.

Output: filter panel + screen + page + tab + sửa product-detail + page chi tiết + test hàm thuần.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/06-kiem-ke-go-live/06-07-PLAN.md
@.planning/phases/06-kiem-ke-go-live/06-06-PLAN.md

<interfaces>
Từ 06-07: KiotVietHistoryFilter, DEFAULT_HISTORY_FILTER, readHistoryFilterFromUrl, writeHistoryFilterToUrl, countActiveHistoryFilters,
  toHistoryRpcArgs(filter, { productId? }) (schemas/history-filter.schema.ts); useKiotVietHistory(args), useKiotVietVoucher;
  <HistoryTable query page pageSize onPageChange onOpenVoucher hideProductColumns? />; <VoucherDrawer voucher onClose />.
Từ 06-06: requireKiotVietHistoryAccess(): Promise<CurrentUser>; CurrentUser.canViewKiotVietHistory.
product-detail.tsx: ProductDetailPermissions { canEdit, canViewCost, canEditSalePrice, canViewHistory }; <Tabs items=[stock-card,
  ...(canViewHistory ? [audit-log] : [])]> (dòng ~202-231).
danh-muc/[id]/page.tsx: requirePermission("view-catalog") rồi <ProductDetailView id permissions={...hasPermission(...)} />.
Khuôn màn danh sách: src/app/(app)/ton-kho/page.tsx + src/features/inventory/components/stock-filter-panel.tsx, stock-toolbar.tsx; ListLayout.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bộ lọc, màn tra cứu, route /lich-su-kiotviet, test hàm thuần bộ lọc</name>
  <files>src/features/kiotviet-history/components/history-filter-panel.tsx, src/features/kiotviet-history/components/history-screen.tsx, src/app/(app)/lich-su-kiotviet/page.tsx, scripts/test-pure-functions.ts</files>
  <read_first>
    - `src/features/inventory/components/stock-filter-panel.tsx`, `stock-toolbar.tsx`, `stock-table.tsx` và `src/app/(app)/ton-kho/page.tsx` — khuôn màn lọc URL gần nhất.
    - `src/shared/components/list-layout.tsx`, `page-header.tsx`.
    - `scripts/test-pure-functions.ts` — khối test bộ lọc tồn kho (Phase 5) để chép cách viết case.
    - `src/proxy.ts` — xác nhận route mới dưới `(app)` được chặn chưa đăng nhập và giữ `?tiep_tuc=` (không cần sửa nếu proxy khớp mọi route không công khai).
    - CLAUDE.md bẫy 9 (page Server Component không gọi hàm từ file "use client"), bẫy 11 (antd v6: `Select` không dùng `value: null` làm "tất cả").
  </read_first>
  <behavior>
    Viết vào `scripts/test-pure-functions.ts` (khối "lịch sử KiotViet"):
    - đọc `?loai=XUAT&tim=quynh&trang=2` → type XUAT, keyword "quynh", page 2;
    - `loai=abc` → type ""; `tu_ngay=2026-13-45` → from ""; `trang=-3` → 1;
    - ghi DEFAULT → ""; ghi { type: "NHAP", from: "2026-01-01" } → chứa `loai=NHAP` và `tu_ngay=2026-01-01`, không chứa `trang=`;
    - quay vòng write → read cho một filter đủ field ra đúng filter đó;
    - `toHistoryRpcArgs` với keyword "  " không có `p_tu_khoa`; với `{ productId }` có `p_san_pham_id`;
    - `countActiveHistoryFilters(DEFAULT)` = 0.
  </behavior>
  <action>
    Viết case ở `<behavior>` trước, chạy `npx tsx scripts/test-pure-functions.ts` (RED nếu hàm 06-07 sai, GREEN nếu đúng — sửa schema của 06-07
    nếu đỏ, ghi lại trong SUMMARY).

    `history-filter-panel.tsx` ("use client"): props `{ value: KiotVietHistoryFilter; onChange(next): void }`. Phần tử: `Segmented`/`Select`
    loại ("Tất cả" = "", "Nhập", "Bán"); `DatePicker.RangePicker` (định dạng dd/MM/yyyy, ghi ra YYYY-MM-DD); `Input.Search` "Tìm khách, NCC,
    ghi chú, mã (không cần dấu)" (D-12); `Input` mã hàng; `Input` số phiếu/số hóa đơn; nút "Xóa lọc" khi `countActiveHistoryFilters > 0`.
    Đổi lọc thì `page` về 1. Trên điện thoại xếp dọc, không tràn ngang.

    `history-screen.tsx` ("use client"): đọc filter bằng `useSearchParams` + `readHistoryFilterFromUrl`, ghi bằng `router.replace(pathname + "?" +
    writeHistoryFilterToUrl(next))`; gọi `useKiotVietHistory(toHistoryRpcArgs(filter))`; ghép `HistoryFilterPanel` + `HistoryTable` + `VoucherDrawer`
    (state phiếu đang mở là `useState` cục bộ). Alert info (antd `title`): "594 phiếu nhập và 4.732 hóa đơn từ KiotViet — chỉ để tra cứu, không nằm
    trong sổ kho, không có tồn lũy kế."

    `src/app/(app)/lich-su-kiotviet/page.tsx` (Server Component, không import antd): `export const metadata = { title: "Lịch sử KiotViet" }`,
    `await requireKiotVietHistoryAccess()`, `PageHeader` tiêu đề "Lịch sử KiotViet", bọc `<Suspense>` (useSearchParams) quanh `<HistoryScreen />`.
  </action>
  <verify>
    <automated>npx tsx scripts/test-pure-functions.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/test-pure-functions.ts` thoát mã 0 và có các case mới (grep `readHistoryFilterFromUrl` trong script ≥ 1).
    - `grep -c "requireKiotVietHistoryAccess" "src/app/(app)/lich-su-kiotviet/page.tsx"` = 1; file page không có `"use client"` và không import từ `antd`.
    - `grep -c "tu_ngay\|den_ngay\|so_phieu\|ma_hang" src/features/kiotviet-history/schemas/history-filter.schema.ts` ≥ 4 (URL tiếng Việt không dấu).
  </acceptance_criteria>
  <done>Màn /lich-su-kiotviet hoạt động với bộ lọc trên URL; test hàm thuần xanh.</done>
</task>

<task type="auto">
  <name>Task 2: Tab "Lịch sử KiotViet" trong chi tiết mã hàng</name>
  <files>src/features/kiotviet-history/components/product-history-tab.tsx, src/features/products/components/product-detail.tsx, src/app/(app)/danh-muc/[id]/page.tsx</files>
  <read_first>
    - `src/features/products/components/product-detail.tsx` toàn bộ — `ProductDetailPermissions`, khối `<Tabs items=…>`.
    - `src/app/(app)/danh-muc/[id]/page.tsx` toàn bộ (31 dòng).
    - `src/features/README.md` — luật import giữa feature (không import nội bộ feature khác).
  </read_first>
  <action>
    `product-history-tab.tsx` ("use client"): props `{ productId: string }`; `useState` trang (1) + loại ("" | "NHAP" | "XUAT" qua `Segmented`);
    gọi `useKiotVietHistory(toHistoryRpcArgs({ ...DEFAULT_HISTORY_FILTER, type, page }, { productId }))`; render `HistoryTable` với
    `hideProductColumns` + `VoucherDrawer`. Câu phụ: "Dòng cũ từ KiotViet, tách khỏi Thẻ kho vì không có tồn lũy kế."

    `product-detail.tsx`: KHÔNG import từ `features/kiotviet-history` (luật feature). Thêm prop tùy chọn `kiotVietHistoryTab?: ReactNode` cho
    `ProductDetailView`; trong `items` của `Tabs` thêm nhánh `...(kiotVietHistoryTab ? [{ key: "kiotviet-history", label: "Lịch sử KiotViet",
    children: kiotVietHistoryTab }] : [])` NGAY SAU tab "Thẻ kho". Không đổi `ProductDetailPermissions`.

    `danh-muc/[id]/page.tsx`: route là nơi ghép hai feature — truyền `kiotVietHistoryTab={user.canViewKiotVietHistory ? <ProductHistoryTab productId={id} />
    : undefined}` (field từ `CurrentUser`, KHÔNG qua `hasPermission`).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` thoát mã 0.
    - `grep -c "kiotviet-history" src/features/products/components/product-detail.tsx` = 0.
    - `grep -c "canViewKiotVietHistory" "src/app/(app)/danh-muc/[id]/page.tsx"` = 1.
    - Trên trình duyệt (`npm run dev`): `quanly` mở `/lich-su-kiotviet`, gõ "quynh" ra dòng ghi chú QUỲNH, lọc Bán + khoảng ngày, bấm số hóa đơn mở drawer đủ dòng; mở một mã hàng có lịch sử thấy tab "Lịch sử KiotViet" cạnh "Thẻ kho" và Thẻ kho không còn dòng KiotViet; `thukho1` không thấy tab và gõ `/lich-su-kiotviet` bị đẩy sang `/khong-du-quyen`; console không cảnh báo antd. Ghi vào SUMMARY.
  </acceptance_criteria>
  <done>Tab lịch sử KiotViet hiện đúng người; màn tra cứu chạy trên dữ liệu thật; build xanh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL gõ tay → route /lich-su-kiotviet | Menu ẩn không chặn được URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-40 | Elevation of Privilege | Route mới thiếu chặn, chỉ dựa menu | mitigate | `requireKiotVietHistoryAccess()` ở page; RLS/RPC 0064 là lớp thật; ma trận route ở 06-16 kiểm bằng HTTP |
| T-06-41 | Information Disclosure | Tab trong chi tiết mã hàng hiện cho người không có quyền | mitigate | Page chỉ truyền tab khi `canViewKiotVietHistory`; RPC vẫn 42501 nếu ai gọi thẳng |
| T-06-42 | Tampering | Tham số URL độc hại | mitigate | `readHistoryFilterFromUrl` chuẩn hóa bằng zod, giá trị lạ về mặc định |
</threat_model>

<verification>
- `npm run check` + `scripts/test-pure-functions.ts` xanh; kiểm trên trình duyệt + console.
</verification>

<success_criteria>
- DLIEU-07 dùng được ở hai nơi (tiêu chí 5 của ROADMAP Phase 6, phần hiển thị).
- Commit: `feat(lich-su-kiotviet): man tra cuu va bo loc URL`, `feat(danh-muc): tab lich su KiotViet trong chi tiet ma hang`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-08-SUMMARY.md` when done
</output>
