---
phase: 06-kiem-ke-go-live
plan: 09
type: execute
wave: 4
depends_on: [06-05]
files_modified:
  - src/features/stocktake/lib/discrepancy.ts
  - src/features/stocktake/lib/session-status.ts
  - scripts/test-pure-functions.ts
  - src/features/stocktake/types.ts
  - src/features/stocktake/schemas/stocktake.schema.ts
  - src/features/stocktake/api/stocktake.api.ts
  - src/features/stocktake/api/stocktake.keys.ts
  - src/features/stocktake/hooks/useStocktake.ts
autonomous: true
requirements: [KKE-01, KKE-02, KKE-03, KKE-04]

must_haves:
  truths:
    - "Ngưỡng 'lệch lớn' là MỘT hằng số dễ sửa: |lệch| ≥ 5 HOẶC |lệch| ≥ 10% tồn sổ (A1 — UAT hỏi lại người dùng) (D-16)"
    - "Nhãn trạng thái phiên (Mới mở / Đang đếm / Chờ duyệt / Đã duyệt / Đã hủy) tính từ tiến độ, không lưu database"
    - "Mọi RPC kiểm kê có một hàm api có kiểu và một hook TanStack Query; mutation invalidate đúng key"
    - "Component không thấy tên cột tiếng Việt — mapper ở types.ts"
  artifacts:
    - path: "src/features/stocktake/lib/discrepancy.ts"
      provides: "LARGE_DISCREPANCY_THRESHOLD, isLargeDiscrepancy, discrepancyOf"
      contains: "isLargeDiscrepancy"
    - path: "src/features/stocktake/lib/session-status.ts"
      provides: "sessionStatus, SESSION_STATUS_LABELS"
      contains: "sessionStatus"
    - path: "src/features/stocktake/api/stocktake.api.ts"
      provides: "Hàm gọi 9 RPC kiểm kê + lookups"
      contains: "duyet_phien_kiem_ke"
    - path: "src/features/stocktake/hooks/useStocktake.ts"
      provides: "Hook đọc/ghi phiên"
      contains: "invalidateQueries"
  key_links:
    - from: "useSaveCount / useApproveSession / useSetRecount / useDeleteCount"
      to: "stocktakeKeys.sheet(sessionId) + stocktakeKeys.sessions"
      via: "invalidateQueries onSuccess"
      pattern: "invalidateQueries"
---

<objective>
Lớp dữ liệu cho feature kiểm kê (`src/features/stocktake/`) và hai hàm thuần quyết định hiển thị.

Purpose: bốn plan giao diện wave 5 (danh sách phiên, đếm, bảng lệch, nhập Excel) đều dựng trên lớp này — viết một lần,
test hàm thuần trước.

Output: lib thuần + test; types/mapper; zod schema; api; keys; hooks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/06-kiem-ke-go-live/06-RESEARCH.md
@.planning/phases/06-kiem-ke-go-live/06-04-PLAN.md

<interfaces>
RPC sau 06-05 (xem src/types/database.types.ts):
  mo_phien_kiem_ke(p_kho_id uuid, p_nhom_hang_ids uuid[]?, p_ghi_chu text?) → chung_tu
  luu_dong_kiem_ke(p_chung_tu_id, p_san_pham_id, p_so_luong numeric) → chung_tu_dong
  xoa_dong_kiem_ke(p_dong_id uuid) → void
  nhap_so_dem_kiem_ke(p_chung_tu_id, p_du_lieu jsonb, p_chi_kiem_tra boolean) → jsonb   (gọi từ route server ở 06-13, không từ client)
  danh_sach_phien_kiem_ke(p_kho_id?, p_trang_thai?, p_trang?, p_kich_thuoc?, p_chung_tu_id?) → table(id, so_ct, ngay_ct, kho_id, ten_kho, pham_vi_nhom_hang,
    ten_nhom_pham_vi, trang_thai, so_da_dem, so_trong_pham_vi, so_dem_lai, nguoi_tao, ngay_ghi_so, created_at, tong_so_dong)
  bang_dem_kiem_ke(p_chung_tu_id, p_nhom_hang_id?) → table(san_pham_id, ma_hang, ten_hang, ten_dvt, nhom_hang_id, ten_nhom, dong_id,
    so_dem, ton_so, lech, ton_hien_tai, ton_kiotviet, dem_luc, nguoi_dem, dem_lai)
  dat_dem_lai(p_dong_id, p_dem_lai boolean) → void
  duyet_phien_kiem_ke(p_chung_tu_id, p_chap_nhan_khong_dem uuid[]) → chung_tu
  huy_chung_tu(p_chung_tu_id, p_ly_do text) → chung_tu   (hủy phiên chưa duyệt)
Khuôn: src/features/documents/api/document.api.ts (fetchDocuments/Page), src/features/inventory/api/inventory.api.ts
  (fetchAssignedWarehouseIds đọc nguoi_dung_kho), src/features/products/api/product.api.ts fetchLookups (kho, nhom_hang),
  src/features/inventory/hooks/useInventory.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hàm thuần ngưỡng lệch và nhãn trạng thái phiên (test trước)</name>
  <files>src/features/stocktake/lib/discrepancy.ts, src/features/stocktake/lib/session-status.ts, scripts/test-pure-functions.ts</files>
  <read_first>
    - `scripts/test-pure-functions.ts` — cách import và viết `assert` hiện có.
    - `.planning/phases/06-kiem-ke-go-live/06-RESEARCH.md` §Pattern 4 và Assumptions A1.
    - CLAUDE.md bẫy 9 — hàm thuần để `lib/*.ts`, không "use client".
  </read_first>
  <behavior>
    - discrepancyOf(8, 10) = -2; discrepancyOf(10, 10) = 0.
    - isLargeDiscrepancy(counted, book): (10,10) false; (15,10) true (|5| ≥ 5); (11,10) true (10%); (104,100) false (4 < 5 và 4% < 10%);
      (2,0) false (|2| < 5, tồn sổ 0 không tính %); (5,0) true; (0,3) true (−3 là 100%); số âm ở tồn sổ (tồn âm −4, đếm 0) → |4| và 4/|−4| = 100% → true.
    - sessionStatus({ state: "HOAN_THANH", counted: 3, scope: 5, recount: 0 }) = "approved"; "DA_HUY" → "voided";
      NHAP_LIEU + counted 0 → "new"; NHAP_LIEU + 0 < counted < scope → "counting"; NHAP_LIEU + counted ≥ scope + recount 0 → "ready";
      NHAP_LIEU + counted ≥ scope + recount > 0 → "counting".
    - SESSION_STATUS_LABELS: new "Mới mở", counting "Đang đếm", ready "Chờ duyệt", approved "Đã duyệt", voided "Đã hủy".
  </behavior>
  <action>
    Viết case ở `<behavior>` vào `scripts/test-pure-functions.ts` (khối "kiểm kê") TRƯỚC, chạy thấy đỏ (chưa có file), rồi viết:

    `lib/discrepancy.ts`: `export const LARGE_DISCREPANCY_THRESHOLD = { absolute: 5, ratio: 0.1 } as const;` kèm comment: giá trị khởi điểm
    (Claude's Discretion D-16, A1 trong 06-RESEARCH) — người dùng chưa cho con số, UAT hỏi lại; sửa đúng chỗ này là đổi toàn hệ.
    `discrepancyOf(counted: number, book: number): number`; `isLargeDiscrepancy(counted, book): boolean` — lệch 0 → false; `|lệch| ≥ absolute`
    → true; `book !== 0 && |lệch| / |book| ≥ ratio` → true.

    `lib/session-status.ts`: `type SessionStatus = "new" | "counting" | "ready" | "approved" | "voided"`; `sessionStatus(input: { state:
    "NHAP_LIEU" | "HOAN_THANH" | "DA_HUY"; counted: number; scope: number; recount: number }): SessionStatus`; `SESSION_STATUS_LABELS`
    và `SESSION_STATUS_COLORS` (màu Tag antd). Comment: trạng thái hiển thị tính từ tiến độ — không cột DB (nguyên tắc kiến trúc số 1).
  </action>
  <verify>
    <automated>npx tsx scripts/test-pure-functions.ts</automated>
  </verify>
  <acceptance_criteria>
    - Script thoát mã 0; `grep -c "isLargeDiscrepancy\|sessionStatus" scripts/test-pure-functions.ts` ≥ 2.
    - Hai file lib không có `"use client"` và không import `antd`/`react`.
    - `grep -c "LARGE_DISCREPANCY_THRESHOLD" src/features/stocktake/lib/discrepancy.ts` ≥ 2 (khai báo + dùng).
  </acceptance_criteria>
  <done>Hai hàm thuần đúng hành vi, test xanh.</done>
</task>

<task type="auto">
  <name>Task 2: types, zod schema, api, keys, hooks</name>
  <files>src/features/stocktake/types.ts, src/features/stocktake/schemas/stocktake.schema.ts, src/features/stocktake/api/stocktake.api.ts, src/features/stocktake/api/stocktake.keys.ts, src/features/stocktake/hooks/useStocktake.ts</files>
  <read_first>
    - `src/types/database.types.ts` — mục các RPC ở `<interfaces>`.
    - `src/features/documents/api/document.api.ts`, `src/features/inventory/api/inventory.api.ts`, `src/features/inventory/hooks/useInventory.ts`, `src/features/stock-in/api/receipt.keys.ts`.
    - `src/features/products/api/product.api.ts` dòng 90-115 — select lookup `kho` (lọc `dang_hoat_dong`) và `nhom_hang`.
    - CLAUDE.md Bước 3-4 (không tự viết type bảng; `if (error) throw error`; query key tập trung), bẫy 5 (không `select("*")` trên san_pham), bẫy 10.
  </read_first>
  <action>
    `types.ts`: kiểu suy từ `database.types.ts`; kiểu miền tiếng Anh + mapper:
    `StocktakeSession { id, docNo, date, warehouseId, warehouseName, categoryIds: string[] | null, categoryNames: string | null,
    state: "NHAP_LIEU" | "HOAN_THANH" | "DA_HUY", countedCount, scopeCount, recountCount, createdBy, postedAt, createdAt }` + `toStocktakeSession`;
    `CountSheetRow { productId, code, name, unitName, categoryId, categoryName, lineId: string | null, counted: number | null,
    bookQuantity: number | null, discrepancy: number | null, currentStock: number, kiotVietStock: number | null, countedAt: string | null,
    countedBy: string | null, needsRecount: boolean }` + `toCountSheetRow` (numeric → Number, null giữ null); `StocktakeLookups { warehouses,
    categories, assignedWarehouseIds }`.

    `schemas/stocktake.schema.ts`: `openSessionSchema = z.object({ warehouseId: z.string().uuid("Chọn kho"), categoryIds: z.array(z.string().uuid()),
    note: z.string().trim().max(500).optional() })` (categoryIds rỗng = toàn kho); `countQuantitySchema = z.number({ message: "Nhập số đếm" })
    .min(0, "Số đếm không được âm")`; `voidSessionSchema = z.object({ reason: z.string().trim().min(3, "Nhập lý do hủy phiên") })`. Type bằng `z.infer`.

    `api/stocktake.keys.ts`: `stocktakeKeys = { all, sessions(args), session(id), sheet(sessionId, categoryId), lookups }`.
    `api/stocktake.api.ts` (browser client, mỗi hàm `if (error) throw error`): `fetchSessions(args)` → `{ rows, total }`;
    `fetchSession(sessionId)` (gọi `danh_sach_phien_kiem_ke` với `p_chung_tu_id`, 0 dòng → `null`); `fetchCountSheet(sessionId, categoryId?)`; `fetchStocktakeLookups()` (kho hoạt động, nhóm hàng, `nguoi_dung_kho` của chính mình);
    `openSession(input)` → id; `saveCount({ sessionId, productId, quantity })`; `deleteCount(lineId)`; `setRecount(lineId, value)`;
    `approveSession(sessionId, acceptZeroProductIds: string[])`; `voidSession(sessionId, reason)` (gọi `huy_chung_tu`). Khóa jsonb/tham số
    `p_*` chỉ xuất hiện ở file này.

    `hooks/useStocktake.ts` ("use client"): `useStocktakeSessions(args)` (keepPreviousData), `useStocktakeSession(sessionId)` và `useCountSheet(sessionId, categoryId)` — cả hai
    `enabled: sessionId !== ""` (bẫy 10), `useStocktakeLookups()`, và mutation `useOpenSession`, `useSaveCount`, `useDeleteCount`, `useSetRecount`,
    `useApproveSession`, `useVoidSession` — mỗi cái `onSuccess` invalidate `stocktakeKeys.sheet(sessionId, …)` (tiền tố) và `stocktakeKeys.sessions`
    (tiền tố); `useApproveSession` invalidate thêm key thẻ kho/tồn kho bằng `queryClient.invalidateQueries({ queryKey: ["inventory"] })` nếu key
    tồn kho đang dùng tiền tố đó (đọc `inventory.keys.ts` để lấy đúng tiền tố, không đoán). Hook KHÔNG bắt lỗi — để component xử lý bằng `explainError`.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; grep -c "invalidateQueries" src/features/stocktake/hooks/useStocktake.ts | awk '$1&gt;=6{print "GATE-OK"}'</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - `grep -rn "so_luong_he_thong\|ton_so\|ma_hang\|dong_id" src/features/stocktake --include=*.ts* | grep -v "types.ts\|api/"` rỗng.
    - `grep -c "if (error) throw error" src/features/stocktake/api/stocktake.api.ts` ≥ 8.
    - `grep -rn "select(\"\\*\")\|\.select()" src/features/stocktake` rỗng (bẫy 5).
    - Không `any`/`@ts-ignore`.
  </acceptance_criteria>
  <done>Lớp dữ liệu kiểm kê gõ đúng kiểu, mutation invalidate đúng key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trình duyệt → RPC kiểm kê | Mọi ghi đi qua RPC SECURITY DEFINER đã tự kiểm quyền (06-03, 06-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-43 | Tampering | Client tự tính lệch rồi gửi lên | mitigate | API không có hàm gửi tồn sổ/lệch; chỉ gửi số đếm, database chốt tồn (D-03) |
| T-06-44 | Tampering | Client ghi thẳng `chung_tu_dong` bằng `.from()` | mitigate | api chỉ dùng `.rpc()` cho ghi; policy 0065 chặn đường còn lại |
| T-06-45 | Information Disclosure | Hook bắn RPC với id rỗng (bẫy 10) → lỗi 400 lặp | mitigate | `enabled: sessionId !== ""` |
</threat_model>

<verification>
- `scripts/test-pure-functions.ts` + `npm run typecheck` xanh.
</verification>

<success_criteria>
- Commit: `feat(kiem-ke): ham thuan nguong lech va trang thai phien`, `feat(kiem-ke): lop du lieu phien kiem ke`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-09-SUMMARY.md` when done
</output>
