---
phase: 06-kiem-ke-go-live
plan: 11
type: execute
wave: 5
depends_on: [06-09]
files_modified:
  - src/features/stocktake/components/count-mobile.tsx
  - src/features/stocktake/components/count-desk-table.tsx
  - src/features/stocktake/components/count-desk-columns.tsx
autonomous: true
requirements: [KKE-02]

must_haves:
  truths:
    - "Trên điện thoại: gõ vài ký tự mã không dấu → Enter chọn (ưu tiên mã khớp tuyệt đối) → gõ số → Enter lưu → con trỏ quay về ô mã, lặp liên tục không cần chạm chuột (D-04, bẫy 14, bẫy 15)"
    - "Trên máy tính: bảng dày mọi mã trong phạm vi, lọc theo nhóm hàng của người đếm, gõ số từng dòng + Enter lưu và nhảy dòng kế (D-04, D-05)"
    - "Màn đếm KHÔNG hiện tồn sổ/tồn KiotViet — người đếm đếm thật, không chép theo số (tinh thần D-08)"
    - "Lưu lại một mã đã đếm = ghi đè và chốt lại tồn sổ tại lúc lưu; giao diện nói rõ điều đó (D-03)"
    - "Dòng bị trả về 'đếm lại' được tô nổi trên cả hai màn đếm (D-16)"
    - "Không quét mã vạch (KKE-02 chốt 24/09)"
  artifacts:
    - path: "src/features/stocktake/components/count-mobile.tsx"
      provides: "Màn đếm điện thoại"
      contains: "ProductSearchInput"
    - path: "src/features/stocktake/components/count-desk-table.tsx"
      provides: "Bảng đếm văn phòng"
      contains: "useCountSheet"
  key_links:
    - from: "count-mobile.tsx / count-desk-table.tsx"
      to: "useSaveCount → rpc luu_dong_kiem_ke"
      via: "mutateAsync rồi setTimeout(focus, 0) (bẫy 14)"
      pattern: "setTimeout"
---

<objective>
Hai đường nhập số đếm trong trình duyệt: màn điện thoại (thủ kho đứng ở kệ) và bảng dày văn phòng — cùng về một phiên
qua `luu_dong_kiem_ke` (D-04). Đường thứ ba (Excel) ở 06-13/06-14.

Purpose: KKE-02. Nhịp bàn phím phải giống phiếu xuất Phase 4 — dùng lại `ProductSearchInput` đã chữa bẫy 14/15.

Output: count-mobile, count-desk-table (+ file cột tách riêng để giữ dưới 200 dòng). Ghép vào trang chi tiết ở 06-15.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/06-kiem-ke-go-live/06-09-PLAN.md
@design/kiem-ke.html

<interfaces>
Từ 06-09: useCountSheet(sessionId, categoryId) → CountSheetRow[] (productId, code, name, unitName, categoryId, categoryName, lineId, counted,
  countedAt, countedBy, needsRecount, …); useSaveCount(), useDeleteCount(); countQuantitySchema; useStocktakeLookups().categories.
src/shared/components/product-search-input.tsx: <ProductSearchInput onSelect inputRef disabled /> — ProductSearchResult { id, code, name,
  unitId, conversion, defaultWarehouseId }; đã bắt Enter ở onKeyDownCapture và ưu tiên mã khớp tuyệt đối (bẫy 14a, 15). tim_san_pham chỉ ra mã
  đang kinh doanh — mã ngừng kinh doanh còn tồn đếm qua bảng máy tính (có trong phạm vi) hoặc Excel.
Khuôn nhịp bàn phím: src/features/stock-out/components/issue-line-entry-row.tsx + issue-line-table.tsx (focus bằng setTimeout 0 sau mutation).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Màn đếm điện thoại</name>
  <files>src/features/stocktake/components/count-mobile.tsx</files>
  <read_first>
    - `src/shared/components/product-search-input.tsx` toàn bộ.
    - `src/features/stock-out/components/issue-line-entry-row.tsx`, `issue-line-table.tsx` dòng 60-140 (chọn mã → focus ô số → lưu → focus ô mã).
    - `design/kiem-ke.html` `renderCount`/`showScanCard` — bố cục thẻ mã + ô số + nút to. BỎ phần quét mã vạch.
    - CLAUDE.md bẫy 13, 14, 15, 19 (kiểm thử phím/tab ẩn), Bước 5 (điện thoại: nút đủ to).
  </read_first>
  <action>
    `count-mobile.tsx` ("use client"): props `{ sessionId: string; editable: boolean }`. Bố cục một cột, rộng 100%, không tràn ngang:
    1) dòng tiến độ "Đã đếm {n}/{tổng}" (từ `useCountSheet(sessionId, "")`); 2) `ProductSearchInput` (ref ô mã); 3) khi đã chọn mã: thẻ hiện mã
    (chữ đậm), tên, ĐVT; nếu mã đã có dòng trong phiên: "Đã đếm {counted} lúc {HH:mm} — lưu sẽ ghi đè và chốt lại tồn sổ lúc lưu" (D-03);
    nếu `needsRecount`: `Tag` đỏ "Cần đếm lại"; mã không thuộc phạm vi phiên (không có trong sheet) vẫn cho lưu — database quyết (23514 nếu ngoài nhóm);
    4) `InputNumber` cỡ lớn (`size="large"`, `min={0}`, `inputMode="decimal"`, `className="w-full"`), Enter = lưu; 5) nút "Lưu" `size="large"`
    `block`, cao ≥ 48px. KHÔNG hiện tồn sổ, tồn hiện tại, tồn KiotViet (đếm mù).
    Lưu: validate `countQuantitySchema` (lỗi hiện dưới ô số, không toast); `useSaveCount().mutateAsync`; thành công → `message.success("Đã lưu {mã}: {số}")`,
    xóa mã đang chọn, `setTimeout(() => codeRef.current?.focus(), 0)` (bẫy 14b); lỗi → `explainError`: 23514 hiện đúng câu database dưới ô số
    (ví dụ "Mã không thuộc nhóm hàng của phiên"), 42501 "Bạn không được đếm phiên này", còn lại title + action; số đã gõ giữ nguyên. Nút/ô disabled
    khi `!editable` hoặc đang lưu. Không `catch {}` rỗng.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; grep -q "ProductSearchInput" src/features/stocktake/components/count-mobile.tsx &amp;&amp; grep -q "setTimeout" src/features/stocktake/components/count-mobile.tsx &amp;&amp; ! grep -qi "bookQuantity\|kiotVietStock\|currentStock" src/features/stocktake/components/count-mobile.tsx &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK` (có ô tìm dùng chung, có focus trễ, KHÔNG đụng tới số tồn).
    - `grep -ci "barcode\|quét\|scan" src/features/stocktake/components/count-mobile.tsx` = 0.
    - `wc -l` ≤ 200.
  </acceptance_criteria>
  <done>Màn đếm điện thoại lưu được liên tục bằng bàn phím, đếm mù, không quét mã.</done>
</task>

<task type="auto">
  <name>Task 2: Bảng đếm văn phòng</name>
  <files>src/features/stocktake/components/count-desk-table.tsx, src/features/stocktake/components/count-desk-columns.tsx</files>
  <read_first>
    - `src/features/stock-out/components/issue-line-table.tsx` và file cột của nó — ô số sửa tại chỗ, `pendingQuantities`, Enter.
    - `src/features/sales-order/components/order-line-table.tsx` — bảng gõ bàn phím dày.
    - `src/shared/components/query-state.tsx`.
    - CLAUDE.md bẫy 14b (focus sau mutation), Bước 6 (tách file > 200 dòng).
  </read_first>
  <action>
    `count-desk-columns.tsx` ("use client"): `buildCountDeskColumns({ editable, inputRefs, pendingValue, onInput, onCommit, onDelete })` trả cột:
    Mã, Tên, ĐVT, Nhóm, Số đếm (`InputNumber` `min={0}` gắn ref theo `productId` vào `inputRefs.current`, Enter → `onCommit(row)`, blur có giá trị đổi →
    `onCommit(row)`), Đếm lúc (HH:mm dd/MM), Người đếm, Trạng thái (`Tag` "Cần đếm lại" đỏ khi `needsRecount`; "Chưa đếm" xám khi `lineId` null),
    thao tác "Xóa số đếm" (`Popconfirm` "Mã sẽ quay về chưa đếm", chỉ khi `lineId` và `editable`). KHÔNG cột tồn sổ / tồn KiotViet / lệch (đếm mù —
    lệch ở tab Bảng lệch).

    `count-desk-table.tsx` ("use client"): props `{ sessionId: string; editable: boolean }`. Thanh công cụ: `Select` nhóm hàng (chỉ các nhóm có trong
    sheet; "" = tất cả — chia việc theo nhóm D-05), `Input` lọc mã/tên (tính khi render bằng `removeDiacritics`, không lưu state dẫn xuất),
    `Segmented` "Tất cả / Chưa đếm / Đã đếm / Cần đếm lại". Dữ liệu `useCountSheet(sessionId, categoryId)` bọc `QueryState`. State ô đang gõ:
    `useState<Record<string, number | null>>`. Commit: `countQuantitySchema` → `useSaveCount().mutateAsync` → xóa pending của dòng →
    `setTimeout(() => inputRefs.current[<productId dòng kế trong danh sách đang hiển thị>]?.focus(), 0)`. Lỗi → `message.error(title + action)`
    (bảng không có field form), giữ giá trị đang gõ. Phân trang client 100 dòng/trang, `scroll={{ x: "max-content" }}`, `size="small"`. Câu phụ trên
    bảng: "Gõ số → Enter là lưu và xuống dòng. Lưu lại mã đã đếm sẽ ghi đè và chốt lại tồn sổ tại lúc lưu."
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` thoát mã 0.
    - `grep -ci "bookQuantity\|kiotVietStock\|discrepancy" src/features/stocktake/components/count-desk-*.tsx` = 0 (đếm mù).
    - `wc -l` mỗi file ≤ 200.
    - Kiểm trên trình duyệt (ghép tạm KHÔNG commit, hoặc đợi 06-15) được phép bỏ qua ở plan này; bắt buộc ở 06-15.
  </acceptance_criteria>
  <done>Bảng đếm văn phòng gõ bàn phím liên tục, lọc theo nhóm, đếm mù.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ô số trên trình duyệt → RPC luu_dong_kiem_ke | Số người dùng gõ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-49 | Tampering | Số âm/chữ gửi lên | mitigate | zod `min(0)` ở client; RPC 23514 cho âm/null (0065) |
| T-06-50 | Tampering | Chọn nhầm mã gần giống khi Enter (bẫy 15) → đếm sai hàng | mitigate | Dùng `ProductSearchInput` đã ưu tiên khớp tuyệt đối; thẻ mã hiện rõ mã + tên trước khi lưu |
| T-06-51 | Information Disclosure | Người đếm thấy tồn sổ rồi chép theo | mitigate | Hai màn đếm không render tồn sổ/KiotViet (D-08 tinh thần); bảng lệch ở tab riêng |
</threat_model>

<verification>
- `npm run check` xanh.
</verification>

<success_criteria>
- Commit: `feat(kiem-ke): man dem dien thoai`, `feat(kiem-ke): bang dem van phong`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-11-SUMMARY.md` when done
</output>
