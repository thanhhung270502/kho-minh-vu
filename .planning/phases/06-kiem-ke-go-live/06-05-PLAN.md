---
phase: 06-kiem-ke-go-live
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - src/types/database.types.ts
autonomous: true
requirements: [KKE-01, KKE-02, KKE-03, KKE-04, DLIEU-06, DLIEU-07]

must_haves:
  truths:
    - "Bốn migration 0063-0066 nằm trên database cloud theo đúng thứ tự, history khớp từng ký tự với file trong git"
    - "src/types/database.types.ts biết mọi RPC và cột mới của Phase 6 — lớp dữ liệu client gõ đúng kiểu"
    - "Toàn bộ pgTAP xanh trên cloud, đếm cả dòng ERROR lẫn not ok"
    - "App production đang chạy không gãy: lưu hồ sơ người dùng bằng 6 tham số cũ vẫn chạy, thẻ kho vẫn mở được"
  artifacts:
    - path: "src/types/database.types.ts"
      provides: "Kiểu sinh lại sau 0063-0066"
      contains: "duyet_phien_kiem_ke"
  key_links:
    - from: "supabase/migrations/0063..0066"
      to: "database cloud kho-vu-tru"
      via: "npm run db:push hoặc MCP execute_sql + ghi schema_migrations (pattern 12)"
      pattern: "schema_migrations"
    - from: "database cloud"
      to: "src/types/database.types.ts"
      via: "npm run db:types hoặc MCP generate_typescript_types"
      pattern: "db:types"
---

<objective>
[BLOCKING] Đẩy 0063 → 0066 lên database thật, sinh lại kiểu, chạy toàn bộ pgTAP.

Purpose: plan DUY NHẤT của Phase 6 được ghi schema lên cloud. Chỉ có một database dùng chung; mọi plan giao diện
(wave 4 trở đi) cần `database.types.ts` biết RPC mới. `npm run check` xanh KHÔNG chứng minh gì về database.

Output: bốn migration trên cloud, `database.types.ts` mới, báo cáo pgTAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@supabase/README.md
@.memory/patterns/pgtap-va-test.md
@.planning/phases/05-ton-kho-tong-quan/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Đẩy 0063-0066 rồi sinh lại kiểu</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - `supabase/README.md`; `.memory/patterns/pgtap-va-test.md` mục 7 (apply_migration ghi version timestamp — KHÔNG dùng) và mục 12 (đẩy qua MCP execute_sql + md5).
    - `.planning/phases/05-ton-kho-tong-quan/05-05-SUMMARY.md` — bảng thay thế CLI bằng MCP đã dùng thật ở Phase 5.
    - Bốn file `supabase/migrations/0063_cong_tac_quyen.sql`, `0064_lich_su_kiotviet.sql`, `0065_kiem_ke_dem.sql`, `0066_kiem_ke_duyet.sql` — đọc lại một lượt, đặc biệt khối comment chép từ `pg_get_functiondef`.
    - `src/types/database.types.ts` — FILE SINH RA, cấm sửa tay (CLAUDE.md Bước 3).
  </read_first>
  <action>
    1. Chụp trạng thái: `npx supabase migration list --linked` (hoặc MCP `list_migrations`). Remote phải dừng ở `0062`. Có bất kỳ
       version ≥ 0063 không phải file của repo → DỪNG, báo người dùng (phiên khác đang ghi cùng database).
    2. Kiểm lại định nghĩa đang chạy chưa đổi kể từ lúc 06-02/06-04 chép: so `md5(pg_get_functiondef(...))` của
       `ghi_so_chung_tu`, `huy_chung_tu`, `lich_su_giao_dich_doi_tac`, `the_kho_san_pham`, `luu_ho_so_nguoi_dung` với bản dán trong
       comment migration. Lệch → DỪNG, báo (có người sửa database giữa chừng).
    3. Đẩy theo thứ tự 0063 → 0064 → 0065 → 0066. Ưu tiên `npm run db:push` (cần `SUPABASE_ACCESS_TOKEN` cho môi trường không TTY). Không có
       CLI đăng nhập thì theo pattern 12: mỗi file một lệnh MCP `execute_sql` gồm nguyên văn file + dòng `-- @@ghi-lich-su@@` + insert
       `supabase_migrations.schema_migrations (version, name, statements)`; sau đó so `md5(statements[1])` với md5 file đã chuẩn hóa LF.
       Không dùng `apply_migration`. Lỗi ở file nào → dừng tại đó, không sửa bằng `cascade`, báo lại.
    4. Sinh kiểu: `npm run db:types` hoặc MCP `generate_typescript_types` ghi nguyên trường `types` vào `src/types/database.types.ts`.
       Không sửa tay dù một ký tự.
    5. Gọi thật bằng phiên người dùng (không service role) — pattern 10: `create or replace` thành công chưa đủ:
       dưới quanly gọi `danh_sach_phien_kiem_ke()`, `tra_cuu_lich_su_kiotviet(p_kich_thuoc := 1)`, `the_kho_san_pham(<một mã thật>)`,
       `lich_su_giao_dich_doi_tac(<một đối tác thật>)` — không lỗi 42702/42501. Kiểm `select count(*) from nguoi_dung where vai_tro='van_phong'
       and not xem_lich_su_kiotviet` = 0 (backfill chạy).
    6. `npm run typecheck` — lời gọi `luu_ho_so_nguoi_dung` 6 tham số trong `user.actions.ts` phải vẫn hợp lệ (hai tham số mới có default).
  </action>
  <verify>
    <automated>grep -q "duyet_phien_kiem_ke" src/types/database.types.ts &amp;&amp; grep -q "tra_cuu_lich_su_kiotviet" src/types/database.types.ts &amp;&amp; grep -q "bang_dem_kiem_ke" src/types/database.types.ts &amp;&amp; grep -q "xem_lich_su_kiotviet" src/types/database.types.ts &amp;&amp; grep -q "pham_vi_nhom_hang" src/types/database.types.ts &amp;&amp; npm run typecheck &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - `list_migrations` sau khi đẩy có 0063, 0064, 0065, 0066 với version đúng dạng `00NN` (không timestamp); md5 từng file khớp.
    - Năm lời gọi thật ở bước 5 đều trả kết quả, ghi vào SUMMARY.
    - SUMMARY ghi `migration list` trước/sau và md5 so sánh.
  </acceptance_criteria>
  <done>0063-0066 trên cloud, kiểu sinh lại có đủ RPC/cột mới, typecheck xanh.</done>
</task>

<task type="auto">
  <name>Task 2: Chạy toàn bộ pgTAP + verify:hook, đếm cho đúng</name>
  <files>supabase/tests/36_cong_tac_quyen_test.sql, supabase/tests/37_lich_su_kiotviet_test.sql, supabase/tests/38_kiem_ke_dem_test.sql, supabase/tests/39_kiem_ke_duyet_test.sql</files>
  <read_first>
    - Bốn file pgTAP mới 36-39 và hai file đã sửa 42, 33.
    - `.memory/patterns/pgtap-va-test.md` mục 9 (`finish(true)` không bắt thiếu assert) và "Đếm assert phải đếm cả dòng ERROR" (`.memory/index.md`).
  </read_first>
  <action>
    Chạy `npm run db:test:linked 2>&amp;1 | tee <scratchpad>/pgtap-06.log`. Không có Docker/CLI thì chạy TỪNG file `supabase/tests/*.sql` qua MCP
    `execute_sql` với dòng finish đổi theo pattern 9 (không commit bản đổi) — file ĐẠT khi không lỗi VÀ trả `DAT`.

    Đếm: `grep -c "^not ok"` = 0 VÀ `grep -ci "error"` = 0 (CLI), hoặc mọi file `DAT` (MCP). Sửa test/migration cho tới khi xanh; sửa migration
    thì đẩy lại trong CHÍNH plan này (plan này là chủ sở hữu việc đẩy) bằng migration vá mới số tiếp theo (0067…), không sửa file đã lên cloud,
    rồi sinh kiểu lại.

    Chạy `npm run verify:hook` — Phase 6 thêm cột vào `nguoi_dung`, hook đọc bảng này.
    Ghi SUMMARY: tổng số assertion toàn dự án trước (380, theo `.memory/index.md`) và sau Phase 6, danh sách file và kết quả.
  </action>
  <verify>
    <automated>npm run verify:hook</automated>
  </verify>
  <acceptance_criteria>
    - Mọi file pgTAP (kể cả 33, 42 đã sửa và 36-39 mới) ĐẠT; không `not ok`, không `ERROR`.
    - `npm run verify:hook` thoát mã 0.
    - SUMMARY có tổng assertion trước/sau và ghi rõ đã đếm cả dòng ERROR.
  </acceptance_criteria>
  <done>Toàn bộ pgTAP xanh trên cloud, verify:hook xanh, con số ghi vào SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bản làm việc → database sản xuất | Đẩy migration sửa database thật, không khôi phục được |
| Database → app production đang chạy | App production chưa deploy code Phase 6 khi migration lên |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-29 | Tampering | Phiên khác đã ghi migration ≥ 0063 hoặc sửa hàm giữa chừng | mitigate | Chụp `migration list` + so md5 `pg_get_functiondef` với bản dán trong comment trước khi đẩy; dừng nếu lệch |
| T-06-30 | Tampering | `apply_migration` ghi version timestamp → `db:push` sau chạy lại | mitigate | Cấm `apply_migration`; pattern 12 + md5 |
| T-06-31 | Denial of Service | App production gọi `luu_ho_so_nguoi_dung` 6 tham số sau khi signature đổi | mitigate | Hai tham số mới `default null` (0063); typecheck + gọi thật bước 5 |
| T-06-32 | Repudiation | Báo "pgTAP xanh" khi file chết giữa chừng | mitigate | Đếm cả ERROR; MCP dùng `finish(true)` + `DAT` |
| T-06-33 | Information Disclosure | Khóa secret dùng cho CLI lọt vào biến `NEXT_PUBLIC_*` | mitigate | Không tạo/sửa `.env*` trong plan này; dùng `SUPABASE_ACCESS_TOKEN` từ môi trường hiện có |
</threat_model>

<verification>
- `list_migrations` có 0063-0066; `database.types.ts` có RPC mới; pgTAP toàn bộ xanh; verify:hook xanh.
</verification>

<success_criteria>
- Wave 4 (giao diện) có kiểu đúng để gõ.
- Commit: `chore(db): day migration 0063-0066 va sinh lai kieu`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-05-SUMMARY.md` when done
</output>
