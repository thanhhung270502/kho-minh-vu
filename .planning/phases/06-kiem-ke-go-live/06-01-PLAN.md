---
phase: 06-kiem-ke-go-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/36_cong_tac_quyen_test.sql
  - supabase/migrations/0063_cong_tac_quyen.sql
autonomous: true
requirements: [DLIEU-07, KKE-04]

must_haves:
  truths:
    - "Hai công tắc theo từng người tồn tại trên nguoi_dung: xem_lich_su_kiotviet (D-13) và duyet_kiem_ke (D-14)"
    - "Người vai trò quan_ly luôn có cả hai quyền dù cột bằng false — không tự khóa được mình (D-15)"
    - "Helper đọc thẳng bảng nguoi_dung, không đọc JWT claim — bật hay tắt đều có hiệu lực ở câu lệnh kế tiếp (D-15, né bẫy 6)"
    - "Chỉ quản lý đổi được công tắc, qua đúng RPC luu_ho_so_nguoi_dung; lời gọi 6 tham số cũ của app đang chạy không làm mất giá trị công tắc"
  artifacts:
    - path: "supabase/migrations/0063_cong_tac_quyen.sql"
      provides: "Hai cột công tắc, hai helper SECURITY DEFINER, luu_ho_so_nguoi_dung 8 tham số"
      contains: "xem_duoc_lich_su_kiotviet"
    - path: "supabase/tests/36_cong_tac_quyen_test.sql"
      provides: "pgTAP cho helper + RPC lưu hồ sơ"
      contains: "duyet_duoc_kiem_ke"
  key_links:
    - from: "public.xem_duoc_lich_su_kiotviet()"
      to: "public.nguoi_dung"
      via: "select theo id = auth.uid() and dang_hoat_dong"
      pattern: "auth.uid"
    - from: "public.luu_ho_so_nguoi_dung"
      to: "nguoi_dung.xem_lich_su_kiotviet / duyet_kiem_ke"
      via: "coalesce(tham số, giá trị cũ)"
      pattern: "p_duyet_kiem_ke"
---

<objective>
Dựng nền quyền theo từng người cho Phase 6: hai cột công tắc trên `nguoi_dung`, hai helper SQL, và mở
rộng RPC `luu_ho_so_nguoi_dung` để quản lý bật/tắt được.

Purpose: D-13 (xem lịch sử KiotViet) và D-14 (duyệt kiểm kê) đều là công tắc theo NGƯỜI, không theo
vai trò. Mọi plan DB khác của phase (0064 đổi RLS lưu trữ, 0066 duyệt phiên) gọi hai helper này — nên
plan này đứng đầu.

Output: migration 0063 + file pgTAP 36. Chưa đẩy lên cloud — việc đẩy thuộc plan 06-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/06-kiem-ke-go-live/06-RESEARCH.md
@.memory/patterns/pgtap-va-test.md
@.memory/patterns/supabase-rls-bao-mat.md

<interfaces>
Bản 0026 (cần thay) — chữ ký hiện hành, KHÔNG phải SECURITY DEFINER (chạy dưới RLS "quan ly sua nguoi dung"):
  luu_ho_so_nguoi_dung(p_id uuid, p_ho_ten text, p_ten_dang_nhap text, p_vai_tro public.vai_tro,
                       p_kho_ids uuid[], p_phai_doi_mat_khau boolean) returns void
  Thân: kiểm vai_tro_hien_tai() = 'quan_ly' (42501), thủ kho phải có kho (23514), insert ... on conflict (id)
  do update, rồi đồng bộ nguoi_dung_kho.
Ba nơi gọi trong src/features/settings/actions/user.actions.ts: createUser, updateUser, resetPassword —
đều gọi bằng tham số ĐẶT TÊN qua PostgREST (supabase.rpc).
Bảng nguoi_dung (0003): id, ho_ten, vai_tro public.vai_tro, kho_id, dang_hoat_dong boolean, created_at,
updated_at (+ ten_dang_nhap, phai_doi_mat_khau từ các migration sau).
Helper pgTAP: supabase/tests/00_helper.sql.inc (dang_nhap_nhu, dang_xuat). Tài khoản mẫu:
quanly@khominhvu.local, vanphong@khominhvu.local, thukho1@khominhvu.local, chixem@khominhvu.local.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Viết pgTAP 36 trước (RED)</name>
  <files>supabase/tests/36_cong_tac_quyen_test.sql</files>
  <read_first>
    - `supabase/tests/00_helper.sql.inc` — chép nguyên khối helper vào đầu file (không `\i`).
    - `supabase/tests/35_nap_ton_tam_test.sql` — khuôn file: `begin; select plan(n);` … `rollback;`, bảng tạm + `grant select … to authenticated` (pattern 1 trong pgtap-va-test.md).
    - `.memory/patterns/pgtap-va-test.md` mục 1, 9, 11, 12 — false pass do trùng mã lỗi, `finish(true)` không bắt thiếu assert, chạy qua MCP.
    - `supabase/migrations/0026_nguoi_dung_nhieu_kho.sql` dòng 161-187 — `luu_ho_so_nguoi_dung` hiện hành.
  </read_first>
  <behavior>
    - A1: dưới `quanly`, `xem_duoc_lich_su_kiotviet()` = true và `duyet_duoc_kiem_ke()` = true, kể cả sau khi (dưới postgres) đặt cả hai cột của quanly = false (D-15).
    - A2: sau migration, mọi `nguoi_dung` vai trò `van_phong` có `xem_lich_su_kiotviet = true` (backfill giữ quyền đang có), `duyet_kiem_ke = false`.
    - A3: dưới `thukho1` cả hai helper = false khi cột false; đặt cột `duyet_kiem_ke = true` cho thukho1 (dưới postgres) rồi gọi lại trong CÙNG transaction → true ngay (không cần token mới).
    - A4: người dùng `dang_hoat_dong = false` → cả hai helper = false dù cột true.
    - A5: không phiên (dang_xuat, auth.uid() null) → cả hai helper = false.
    - A6: dưới `quanly`, gọi `luu_ho_so_nguoi_dung` với 8 tham số đặt tên, bật `p_xem_lich_su_kiotviet := true` cho `thukho1` → cột đổi thành true.
    - A7: gọi `luu_ho_so_nguoi_dung` KHÔNG truyền hai tham số mới (6 tham số đặt tên như app cũ) → hai cột giữ nguyên giá trị trước đó (không bị reset về false).
    - A8: dưới `vanphong` gọi `luu_ho_so_nguoi_dung` → throws 42501.
    - A9: `has_function_privilege('anon', 'public.xem_duoc_lich_su_kiotviet()', 'execute')` = false; tương tự `duyet_duoc_kiem_ke()`.
  </behavior>
  <action>
    Tạo `supabase/tests/36_cong_tac_quyen_test.sql` theo đúng khuôn file 35: header comment nêu D-13/D-14/D-15,
    `begin; select plan(N);` (N = đúng số assertion viết ra, tối thiểu 11), chép khối helper từ `00_helper.sql.inc`,
    rồi các assertion ở `<behavior>` bằng `is()`, `ok()`, `throws_ok(..., '42501')`.

    Đổi dữ liệu người dùng (đặt cột công tắc, `dang_hoat_dong`) luôn làm dưới `pg_temp.dang_xuat()` (postgres) rồi mới
    `dang_nhap_nhu(...)` lại để gọi helper — mọi thứ nằm trong `begin … rollback` nên không để lại gì trên cloud.
    Với A4, KHÔNG vô hiệu hóa tài khoản mẫu thật ngoài transaction; chỉ `update` trong transaction test.

    Dòng cuối trước `rollback;` là `select * from finish();` (runner CLI). Khi chạy qua MCP thì đổi tạm dòng đó
    thành khuôn `coalesce((select string_agg(f,' | ') from finish(true) f),'DAT')` như pattern 9 — KHÔNG commit bản đổi tạm.

    Chạy thử (RED): qua MCP `execute_sql` gửi nội dung file (đã đổi dòng finish) — phải LỖI vì hai helper và
    cột chưa tồn tại. Ghi lỗi đó vào SUMMARY làm bằng chứng RED.
  </action>
  <verify>
    <automated>test -f supabase/tests/36_cong_tac_quyen_test.sql &amp;&amp; grep -q "select plan(" supabase/tests/36_cong_tac_quyen_test.sql &amp;&amp; grep -q "duyet_duoc_kiem_ke" supabase/tests/36_cong_tac_quyen_test.sql &amp;&amp; grep -q "xem_duoc_lich_su_kiotviet" supabase/tests/36_cong_tac_quyen_test.sql &amp;&amp; grep -q "rollback;" supabase/tests/36_cong_tac_quyen_test.sql &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - Số trong `plan(N)` bằng đúng số lời gọi `is(`/`ok(`/`throws_ok(`/`lives_ok(` trong file (đếm bằng `grep -cE "^\s*select (is|ok|throws_ok|lives_ok|isnt)\("`).
    - File không chứa `\i` và bắt đầu bằng `begin;`, kết thúc bằng `rollback;`.
    - SUMMARY có lỗi RED thật khi chạy trước khi migration tồn tại.
  </acceptance_criteria>
  <done>File pgTAP 36 tồn tại, đủ 9 nhóm hành vi, chạy thử báo lỗi vì chưa có migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0063 — cột công tắc, hai helper, luu_ho_so_nguoi_dung 8 tham số (GREEN)</name>
  <files>supabase/migrations/0063_cong_tac_quyen.sql</files>
  <read_first>
    - `supabase/migrations/0026_nguoi_dung_nhieu_kho.sql` — toàn bộ: `vai_tro_hien_tai`, `kho_hien_tai`, `luu_ho_so_nguoi_dung`, khối tự kiểm RLS cuối file.
    - `supabase/migrations/0003_nguoi_dung_kho.sql` dòng 1-80 — bảng `nguoi_dung`, lý do helper cũ đọc JWT.
    - `.memory/patterns/supabase-rls-bao-mat.md` mục 7 — bọc `(select fn())` trong policy.
    - Định nghĩa ĐANG CHẠY trên cloud (bắt buộc — file migration có thể lệch cloud): qua MCP `execute_sql` chạy
      `select pg_get_functiondef('public.luu_ho_so_nguoi_dung(uuid,text,text,public.vai_tro,uuid[],boolean)'::regprocedure);`
      và `select column_name from information_schema.columns where table_schema='public' and table_name='nguoi_dung';`.
      Dán kết quả vào khối comment đầu migration (khuôn `05-LIVE-DEFS.md` của Phase 5).
  </read_first>
  <action>
    Tạo `supabase/migrations/0063_cong_tac_quyen.sql`, header comment tiếng Việt nêu D-13/D-14/D-15 và lý do đọc bảng
    thay vì JWT (06-RESEARCH.md §Data Model mục 3). Nội dung theo thứ tự:

    1. `alter table public.nguoi_dung add column xem_lich_su_kiotviet boolean not null default false,
       add column duyet_kiem_ke boolean not null default false;` kèm `comment on column` cho từng cột.
    2. Backfill có chủ đích (Claude's Discretion — cách lưu công tắc): `update public.nguoi_dung set xem_lich_su_kiotviet = true
       where vai_tro = 'van_phong';` — giữ nguyên quyền văn phòng đang có theo policy 0016 để go-live không mất tra cứu;
       từ nay quản lý tắt được từng người. Comment ghi rõ đây là lựa chọn giữ hành vi cũ, UAT sẽ hỏi lại người dùng.
    3. Helper `public.xem_duoc_lich_su_kiotviet()` và `public.duyet_duoc_kiem_ke()`: `returns boolean language sql stable
       security definer set search_path = ''`, thân `select coalesce((select vai_tro = 'quan_ly' or <cột> from public.nguoi_dung
       where id = (select auth.uid()) and dang_hoat_dong), false)`. `revoke all … from public, anon;` `grant execute … to authenticated;`
       và `comment on function` giải thích: đọc bảng nên cả nâng lẫn hạ quyền có hiệu lực ngay; `quan_ly` luôn true (D-15).
    4. `drop function public.luu_ho_so_nguoi_dung(uuid, text, text, public.vai_tro, uuid[], boolean);` rồi tạo lại với 8 tham số:
       sáu tham số cũ giữ nguyên tên/thứ tự, thêm `p_xem_lich_su_kiotviet boolean default null, p_duyet_kiem_ke boolean default null`.
       Thân chép từ định nghĩa ĐANG CHẠY (không phải từ file 0026 nếu hai bản lệch nhau), chỉ thêm: trong `insert … values`
       ghi `coalesce(p_xem_lich_su_kiotviet, false)` / `coalesce(p_duyet_kiem_ke, false)`; trong `on conflict do update set`
       ghi `xem_lich_su_kiotviet = coalesce(p_xem_lich_su_kiotviet, public.nguoi_dung.xem_lich_su_kiotviet)` và tương tự cho
       `duyet_kiem_ke`. Lý do NULL = giữ nguyên: app production đang gọi 6 tham số đặt tên; sau khi đẩy migration mà
       frontend chưa deploy thì lời gọi cũ vẫn chạy và không reset công tắc. Giữ nguyên kiểu KHÔNG SECURITY DEFINER như 0026.
       `revoke all … from public, anon;` `grant execute … to authenticated;` với chữ ký 8 tham số mới.
    5. Không đổi `custom_access_token_hook`, không thêm claim JWT (D-15: không cần chờ token).
    6. Chép khối tự kiểm "Còn bảng chưa bật RLS" từ cuối 0026.

    Chạy pgTAP 36 (GREEN) bằng dry-run không để lại dấu vết: một lệnh MCP `execute_sql` gồm `begin;` + nguyên văn 0063 +
    phần thân file 36 (giữa `begin;` và `rollback;` của nó, dòng finish đổi theo pattern 9) + `rollback;`. Kết quả phải là `DAT`.
    KHÔNG chạy `npm run db:push` và KHÔNG ghi `supabase_migrations.schema_migrations` — đẩy thật là việc của 06-05.
  </action>
  <verify>
    <automated>grep -q "add column xem_lich_su_kiotviet" supabase/migrations/0063_cong_tac_quyen.sql &amp;&amp; grep -q "add column duyet_kiem_ke" supabase/migrations/0063_cong_tac_quyen.sql &amp;&amp; grep -q "function public.duyet_duoc_kiem_ke()" supabase/migrations/0063_cong_tac_quyen.sql &amp;&amp; grep -q "p_duyet_kiem_ke boolean default null" supabase/migrations/0063_cong_tac_quyen.sql &amp;&amp; grep -q "Còn bảng chưa bật RLS" supabase/migrations/0063_cong_tac_quyen.sql &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - Dry-run MCP (begin + 0063 + thân test 36 + rollback) trả `DAT`; ghi nguyên kết quả vào SUMMARY.
    - `grep -c "security definer" supabase/migrations/0063_cong_tac_quyen.sql` ≥ 2 (hai helper) và thân `luu_ho_so_nguoi_dung` KHÔNG có `security definer`.
    - `grep -c "auth.jwt" supabase/migrations/0063_cong_tac_quyen.sql` = 0 (không đọc claim).
    - Khối comment đầu file chứa định nghĩa đang chạy của `luu_ho_so_nguoi_dung` lấy từ `pg_get_functiondef`.
    - `npx supabase migration list --linked` (hoặc MCP `list_migrations`) sau plan KHÔNG có 0063 — chưa đẩy.
  </acceptance_criteria>
  <done>Migration 0063 viết xong, dry-run pgTAP 36 trên cloud trả DAT rồi rollback, chưa có gì ghi xuống database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → RPC luu_ho_so_nguoi_dung | Người dùng thường có thể gọi thẳng PostgREST để tự bật công tắc cho mình |
| JWT claim ↔ bảng nguoi_dung | Claim có thể cũ hơn bảng tới 60 phút |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | `luu_ho_so_nguoi_dung` — không phải quản lý tự bật `duyet_kiem_ke` | mitigate | Thân RPC giữ kiểm `vai_tro_hien_tai() = 'quan_ly'` (42501) + RLS "quan ly sua nguoi dung" vì hàm KHÔNG security definer; pgTAP A8 |
| T-06-02 | Elevation of Privilege | Update trực tiếp `nguoi_dung.duyet_kiem_ke` qua PostgREST | mitigate | Policy "quan ly sua nguoi dung" (0015) chỉ cho `quan_ly` ghi bảng; không thêm policy ghi nào mới |
| T-06-03 | Elevation of Privilege | Quyền bị thu hồi nhưng token cũ vẫn mang quyền | mitigate | Helper đọc bảng theo `auth.uid()` mỗi câu lệnh, không đọc claim; pgTAP A3 chứng minh đổi cột có hiệu lực trong cùng transaction |
| T-06-04 | Denial of Service | Quản lý tự tắt công tắc của mình rồi mất quyền duyệt | mitigate | `vai_tro = 'quan_ly' or <cột>` trong cả hai helper (D-15); pgTAP A1 |
| T-06-05 | Tampering | Đẩy migration làm lời gọi 6 tham số của production reset công tắc về false | mitigate | Tham số mới `default null` + `coalesce(p, giá trị cũ)`; pgTAP A7 |
| T-06-06 | Information Disclosure | anon gọi helper dò quyền | mitigate | `revoke all … from public, anon`; pgTAP A9 |
</threat_model>

<verification>
- Dry-run pgTAP 36 trên cloud trả `DAT`.
- Migration chưa nằm trên cloud (06-05 đẩy).
</verification>

<success_criteria>
- Hai helper và RPC 8 tham số sẵn sàng cho 0064/0066 dùng.
- Commit riêng từng task: `test(06-01): pgTAP cong tac quyen theo nguoi` và `feat(db): cong tac xem lich su KiotViet va duyet kiem ke`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-01-SUMMARY.md` when done
</output>
