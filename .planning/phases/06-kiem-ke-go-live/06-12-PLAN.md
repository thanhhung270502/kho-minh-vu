---
phase: 06-kiem-ke-go-live
plan: 12
type: execute
wave: 5
depends_on: [06-09]
files_modified:
  - src/features/stocktake/components/discrepancy-table.tsx
  - src/features/stocktake/components/uncounted-panel.tsx
  - src/features/stocktake/components/approve-session-button.tsx
autonomous: true
requirements: [KKE-03, KKE-04, DLIEU-06]

must_haves:
  truths:
    - "Bảng lệch hiện số đếm, tồn sổ lúc đếm, lệch, và tồn KiotViet tạm để tham khảo; dòng lệch lớn tô nổi (KKE-03, D-09, D-16)"
    - "Người có quyền duyệt trả từng dòng về 'đếm lại' và bỏ yêu cầu; không chặn duyệt vì lệch lớn, không bắt nhập lý do (D-16)"
    - "Danh sách mã chưa đếm hiện TRƯỚC khi duyệt; mặc định chấp nhận 0, bỏ chọn = trả về đếm bù (D-07)"
    - "Nút duyệt chỉ bấm được với người có công tắc 'Duyệt kiểm kê'; hộp xác nhận nói rõ hậu quả: sinh phiếu KIEM_KE, tồn về đúng số đã đếm, không sửa được nữa (KKE-04, D-14)"
    - "Không hiện giá trị lệch bằng tiền, không cảnh báo giá vốn bằng 0 (D-17)"
  artifacts:
    - path: "src/features/stocktake/components/discrepancy-table.tsx"
      provides: "Bảng lệch + đếm lại"
      contains: "isLargeDiscrepancy"
    - path: "src/features/stocktake/components/uncounted-panel.tsx"
      provides: "Danh sách chưa đếm + chọn chấp nhận 0"
      contains: "rowSelection"
    - path: "src/features/stocktake/components/approve-session-button.tsx"
      provides: "Nút duyệt + xác nhận"
      contains: "useApproveSession"
  key_links:
    - from: "uncounted-panel.tsx"
      to: "approve-session-button.tsx"
      via: "acceptZeroProductIds tính từ tập bỏ chọn"
      pattern: "acceptZeroProductIds"
    - from: "discrepancy-table.tsx"
      to: "useSetRecount → rpc dat_dem_lai"
      via: "nút Trả về đếm lại / Bỏ yêu cầu"
      pattern: "useSetRecount"
---

<objective>
Màn xem lệch và duyệt phiên: bảng lệch, cờ đếm lại, danh sách chưa đếm, nút duyệt.

Purpose: KKE-03 + KKE-04 + đợt đầu kỳ DLIEU-06 (mã chưa đếm chấp nhận 0 đảo phần tồn tạm KiotViet, D-06/D-07). Database đã chặn thật
(0066); giao diện phải giải thích đúng để người duyệt quyết.

Output: discrepancy-table, uncounted-panel, approve-session-button. Ghép vào trang chi tiết ở 06-15.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/06-kiem-ke-go-live/06-09-PLAN.md
@.planning/phases/06-kiem-ke-go-live/06-04-PLAN.md
@design/kiem-ke.html

<interfaces>
Từ 06-09: useCountSheet(sessionId, "") → CountSheetRow[] (lineId, counted, bookQuantity, discrepancy, currentStock, kiotVietStock, countedAt,
  countedBy, needsRecount, code, name, unitName, categoryName, productId); useSetRecount(), useApproveSession(); isLargeDiscrepancy,
  discrepancyOf, LARGE_DISCREPANCY_THRESHOLD.
RPC duyet_phien_kiem_ke lỗi 23514 khi: còn dòng dem_lai, còn mã chưa đếm chưa chấp nhận, mã chấp nhận không thuộc tập chưa đếm, phiên không còn
  NHAP_LIEU; 42501 khi không có quyền.
Khuôn "hệ đề xuất — người duyệt": src/features/inventory/components/reorder-level-table.tsx (Phase 5 — Set dòng bỏ chọn, rowSelection
  preserveSelectedRowKeys, nút ghi rõ số lượng).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bảng lệch với tô nổi và trả về đếm lại</name>
  <files>src/features/stocktake/components/discrepancy-table.tsx</files>
  <read_first>
    - `src/features/inventory/components/reorder-level-table.tsx`, `reorder-level-columns.tsx`.
    - `src/features/stocktake/lib/discrepancy.ts` (06-09).
    - `design/kiem-ke.html` `renderDiscrepancy` — bố cục.
    - CLAUDE.md bẫy 11.
  </read_first>
  <action>
    `discrepancy-table.tsx` ("use client"): props `{ sessionId: string; editable: boolean; canApprove: boolean }`. Dữ liệu `useCountSheet(sessionId, "")`
    lọc dòng có `lineId` (tính khi render). Hàng tóm tắt (`Statistic`): Đã đếm, Có lệch, Lệch lớn, Chờ đếm lại. `Segmented` lọc: "Tất cả / Có lệch /
    Lệch lớn / Chờ đếm lại". Cột: Mã, Tên, ĐVT, Số đếm, Tồn sổ lúc đếm, Lệch (có dấu +/−, xanh khi dương, đỏ khi âm), Tồn KiotViet tạm (chỉ hiện cột khi
    có ít nhất một dòng `kiotVietStock !== null`; tiêu đề có `Tooltip` "Số tạm nạp từ KiotViet trước đợt đếm — lệch lớn so với số này thường là đếm sót",
    D-09), Đếm lúc, Người đếm, Trạng thái (`Tag` đỏ "Lệch lớn" khi `isLargeDiscrepancy(counted, bookQuantity)`; `Tag` cam "Chờ đếm lại").
    Hàng lệch lớn có nền nhạt (class Tailwind qua `rowClassName`, không `!`). Cột thao tác chỉ khi `editable && canApprove`: "Trả về đếm lại" /
    "Bỏ yêu cầu" gọi `useSetRecount`. Không chặn gì theo lệch (D-16). Câu phụ: "Ngưỡng lệch lớn: từ {absolute} cái hoặc {ratio×100}% tồn sổ" lấy từ
    `LARGE_DISCREPANCY_THRESHOLD`. Không cột tiền/giá (D-17). `QueryState` bốn trạng thái; empty "Chưa có mã nào được đếm trong phiên này."
    Bảng cuộn ngang trong khung. Tách cột ra `discrepancy-columns.tsx` nếu vượt 200 dòng (thêm vào commit).
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; grep -q "isLargeDiscrepancy" src/features/stocktake/components/discrepancy-table.tsx &amp;&amp; grep -q "useSetRecount" src/features/stocktake/components/discrepancy-table.tsx &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - `grep -ci "giá trị lệch\|thành tiền\|giá vốn\|₫\|VND" src/features/stocktake/components/discrepancy-*.tsx` = 0 (D-17).
    - Không `!` đứng trước class Tailwind (`grep -c 'className="[^"]*![a-z]' …` = 0 — bẫy 2).
  </acceptance_criteria>
  <done>Bảng lệch tô nổi lệch lớn, trả về/bỏ đếm lại được, không hiện tiền.</done>
</task>

<task type="auto">
  <name>Task 2: Danh sách chưa đếm + nút duyệt</name>
  <files>src/features/stocktake/components/uncounted-panel.tsx, src/features/stocktake/components/approve-session-button.tsx</files>
  <read_first>
    - `src/features/inventory/components/reorder-level-table.tsx` — `Set` dòng bỏ chọn + `rowSelection`.
    - `src/features/documents/components/post-document-button.tsx` — khuôn nút ghi sổ + hộp xác nhận + xử lý lỗi.
    - `src/shared/components/posting-summary.tsx` — tóm tắt hậu quả trước khi ghi sổ (Phase 4).
    - `src/shared/lib/errors.ts`.
  </read_first>
  <action>
    `uncounted-panel.tsx` ("use client"): props `{ sessionId: string; editable: boolean; canApprove: boolean }`. Dòng chưa đếm = sheet có `lineId === null`
    (tính khi render). Không có dòng nào → `Alert` success "Mọi mã trong phạm vi đã được đếm". Có → `Alert` warning (prop `title`) "Còn {n} mã chưa đếm —
    duyệt sẽ tính tồn 0 cho mã được chọn" (D-07) + bảng: Mã, Tên, ĐVT, Nhóm, Tồn sổ hiện tại, Tồn KiotViet tạm (cột này là thông tin người duyệt cần:
    chấp nhận 0 sẽ đảo đúng số này — D-06). `rowSelection` mặc định chọn tất cả (lưu `Set<string>` các mã BỎ chọn, `preserveSelectedRowKeys`);
    bỏ chọn = "trả về đếm bù", chú thích ngay trên bảng. `acceptZeroProductIds` = mọi mã chưa đếm trừ tập bỏ chọn (tính khi render). Cuối panel đặt
    `<ApproveSessionButton sessionId acceptZeroProductIds blockers={...} canApprove editable />`, với `blockers` = danh sách câu lý do không duyệt được:
    "{k} mã chưa đếm đang chờ đếm bù" khi tập bỏ chọn khác rỗng; "{m} dòng chờ đếm lại" khi có `needsRecount`.

    `approve-session-button.tsx` ("use client"): props `{ sessionId: string; acceptZeroProductIds: string[]; blockers: string[]; canApprove: boolean; editable: boolean }`.
    Ẩn khi `!editable`. `disabled` khi `!canApprove` (Tooltip "Cần quyền “Duyệt kiểm kê” — nhờ quản lý bật trong Cài đặt → Người dùng") hoặc
    `blockers.length > 0` (Tooltip liệt kê blockers). Bấm → `Modal.confirm` (qua `App.useApp().modal`) tóm tắt: số mã đã đếm, số mã có lệch, số mã chấp
    nhận 0, và câu "Duyệt sẽ ghi sổ phiếu kiểm kê: tồn của từng mã về đúng số đã đếm (lệch tính theo tồn sổ lúc đếm). Phiếu không sửa được nữa."
    OK → `useApproveSession().mutateAsync(sessionId, acceptZeroProductIds)`; nút loading suốt lúc gửi (chống bấm lặp). Thành công → `message.success`
    "Đã duyệt phiên {số} — tồn đã cập nhật". Lỗi: `42501` → "Bạn chưa được bật quyền Duyệt kiểm kê. Liên hệ quản lý."; `23514` → hiện nguyên câu
    database (đã viết tiếng Việt ở 0066) kèm "Tải lại bảng rồi thử lại"; còn lại `explainError`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` thoát mã 0.
    - `grep -c "acceptZeroProductIds" src/features/stocktake/components/uncounted-panel.tsx` ≥ 2.
    - `grep -c "42501" src/features/stocktake/components/approve-session-button.tsx` ≥ 1 và `grep -c "23514"` ≥ 1.
    - `wc -l` mỗi file ≤ 200.
  </acceptance_criteria>
  <done>Người duyệt thấy mã chưa đếm trước khi duyệt, chọn chấp nhận 0 hoặc trả đếm bù; nút duyệt theo công tắc và giải thích hậu quả.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trình duyệt → RPC duyet_phien_kiem_ke / dat_dem_lai | Danh sách chấp nhận 0 do client gửi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-52 | Elevation of Privilege | Nút duyệt ẩn/disable bị vượt bằng console | mitigate | RPC 42501 + cửa trong ghi_so_chung_tu (0066); UI chỉ trang trí |
| T-06-53 | Tampering | Client gửi danh sách chấp nhận 0 chứa mã đã đếm/ngoài phạm vi để ép tồn về 0 | mitigate | RPC 23514 khi mã chấp nhận không thuộc tập chưa đếm (pgTAP E11) |
| T-06-54 | Repudiation | Duyệt mà không biết còn mã chưa đếm | mitigate | Panel hiện danh sách trước nút duyệt; RPC chặn nếu còn mã chưa xử lý (D-07) |
| T-06-55 | Tampering | Bấm duyệt hai lần | mitigate | Nút loading khi gửi; RPC khóa header + chặn phiên không còn NHAP_LIEU |
</threat_model>

<verification>
- `npm run check` xanh.
</verification>

<success_criteria>
- Commit: `feat(kiem-ke): bang lech va tra ve dem lai`, `feat(kiem-ke): danh sach chua dem va duyet phien`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-12-SUMMARY.md` when done
</output>
