---
phase: 06-kiem-ke-go-live
plan: 15
type: execute
wave: 6
depends_on: [06-06, 06-10, 06-11, 06-12, 06-14]
files_modified:
  - src/features/stocktake/components/session-header.tsx
  - src/features/stocktake/components/session-detail.tsx
  - src/app/(app)/kiem-ke/[id]/page.tsx
  - src/features/products/components/stock-card-columns.tsx
  - src/features/products/components/stock-card.tsx
autonomous: true
requirements: [KKE-01, KKE-02, KKE-03, KKE-04, DLIEU-06]

must_haves:
  truths:
    - "/kiem-ke/<id> gom ba đường đếm (điện thoại/máy tính/Excel) và bảng lệch + duyệt vào MỘT phiên (D-04)"
    - "Trên điện thoại mặc định mở màn đếm điện thoại; trên máy tính mặc định bảng đếm — chuyển qua lại được"
    - "Nút duyệt nhận quyền từ canApproveStocktake của người đang đăng nhập (D-14); phiên chưa duyệt hủy được kèm lý do; phiên đã duyệt chỉ đọc"
    - "Thẻ kho: số phiếu KIEM_KE bấm được sang /kiem-ke/<id>; không còn nhãn/ghi chú về dòng KiotViet cũ (D-11)"
  artifacts:
    - path: "src/app/(app)/kiem-ke/[id]/page.tsx"
      provides: "Route chi tiết phiên"
      contains: "canApproveStocktake"
    - path: "src/features/stocktake/components/session-detail.tsx"
      provides: "Tabs Đếm / Bảng lệch & duyệt / Nhập Excel"
      contains: "Tabs"
  key_links:
    - from: "src/features/products/components/stock-card-columns.tsx"
      to: "/kiem-ke/[id]"
      via: "DOC_TYPE_TO_ROUTE.KIEM_KE"
      pattern: "KIEM_KE: \"/kiem-ke\""
---

<objective>
Trang chi tiết phiên kiểm kê: ghép các component của 06-11, 06-12, 06-14 dưới một đầu phiên; nối link thẻ kho.

Purpose: đây là chỗ người dùng thật làm việc cả buổi đếm. Và dọn phần hiển thị KiotViet cũ trong thẻ kho sau khi 0064 đã bỏ
dòng đó khỏi `the_kho_san_pham` (D-11).

Output: session-header, session-detail, route `/kiem-ke/[id]`, sửa hai file thẻ kho.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-kiem-ke-go-live/06-CONTEXT.md
@.planning/phases/06-kiem-ke-go-live/06-09-PLAN.md
@design/kiem-ke.html

<interfaces>
Từ 06-09: useStocktakeSession(id) → StocktakeSession | null; useVoidSession(); voidSessionSchema; sessionStatus, SESSION_STATUS_LABELS/COLORS.
Từ 06-11: <CountMobile sessionId editable />, <CountDeskTable sessionId editable />.
Từ 06-12: <DiscrepancyTable sessionId editable canApprove />, <UncountedPanel sessionId editable canApprove />.
Từ 06-14: <CountExcelImport sessionId editable />.
Từ 06-06: CurrentUser.canApproveStocktake.
Khuôn trang chi tiết: src/app/(app)/nhap-kho/[id]/page.tsx (UUID check → notFound, requirePermission, permissions props),
  src/features/stock-in/components/receipt-detail.tsx, receipt-header.tsx, void-receipt-dialog.tsx.
stock-card-columns.tsx: DOC_TYPE_TO_ROUTE (NHAP, XUAT, TRA_KHACH, TRA_NCC), SOURCE_LABELS (HE_THONG, KIOTVIET_NHAP, KIOTVIET_BAN), comment dòng 57
  "Dòng KiotViet không có documentId…". stock-card.tsx dòng 49-55 Alert "Dữ liệu KiotViet cũ không gắn kho…", dòng ~90 chú thích dấu "—".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Đầu phiên + trang chi tiết + route</name>
  <files>src/features/stocktake/components/session-header.tsx, src/features/stocktake/components/session-detail.tsx, src/app/(app)/kiem-ke/[id]/page.tsx</files>
  <read_first>
    - `src/app/(app)/nhap-kho/[id]/page.tsx`, `src/features/stock-in/components/receipt-detail.tsx`, `receipt-header.tsx`, `void-receipt-dialog.tsx`.
    - Các component của 06-11, 06-12, 06-14 (props thật đã viết).
    - `design/kiem-ke.html` — ba bước Danh sách/Đếm/Bảng lệch.
    - CLAUDE.md bẫy 9, 11, 19 (khung trình duyệt ẩn → Suspense trắng, không phải lỗi app).
  </read_first>
  <action>
    `session-header.tsx` ("use client"): props `{ session: StocktakeSession; canVoid: boolean }`. Hiện số phiên, kho, phạm vi ("Toàn kho" hoặc tên nhóm),
    ngày mở, người mở, `Tag` trạng thái theo `sessionStatus(...)`, `Progress` "{counted}/{scope}", "Chờ đếm lại: {n}". Nút "Hủy phiên" (khi
    `canVoid && state === "NHAP_LIEU"`) mở `Modal` form RHF `voidSessionSchema` (lý do bắt buộc) → `useVoidSession` → `router.push("/kiem-ke")`;
    nút loading khi gửi; lỗi map về field `reason`. Phiên HOAN_THANH: `Alert` success "Đã duyệt lúc … — tồn đã về đúng số đếm; phiếu không sửa được".

    `session-detail.tsx` ("use client"): props `{ sessionId: string; canCount: boolean; canApprove: boolean }`. `useStocktakeSession(sessionId)` bọc
    `QueryState` (null → trạng thái rỗng "Không tìm thấy phiên kiểm kê này, hoặc phiên thuộc kho bạn không được phân" + link về `/kiem-ke`).
    `editable = canCount && state === "NHAP_LIEU"`. `Tabs` ba mục: "Đếm" (một `Segmented` "Điện thoại / Bảng" chọn `CountMobile` hoặc
    `CountDeskTable`; mặc định theo `Grid.useBreakpoint()` — `md` trở lên là Bảng), "Bảng lệch & duyệt" (`UncountedPanel` trên, `DiscrepancyTable`
    dưới), "Nhập Excel" (`CountExcelImport`). Tab đang chọn giữ trên URL `?tab=dem|lech|excel` (URL tiếng Việt không dấu) để F5 không mất.

    `src/app/(app)/kiem-ke/[id]/page.tsx` (Server Component, không antd): UUID sai → `notFound()`; `const user = await requirePermission("view-catalog")`;
    metadata "Phiên kiểm kê"; render `<SessionDetail sessionId={id} canCount={user.role !== "chi_xem"} canApprove={user.canApproveStocktake} />` trong
    `Suspense`. Không `hasPermission` cho `canApprove` (quyền theo người).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` thoát mã 0.
    - `grep -c "canApproveStocktake" "src/app/(app)/kiem-ke/[id]/page.tsx"` = 1; `grep -c "notFound" …` ≥ 1; không `"use client"` trong page.
    - `wc -l` mỗi component ≤ 200.
    - Trên trình duyệt (`npm run dev`), với một phiên thử phạm vi MỘT nhóm hàng nhỏ: (a) cỡ 375px — tab Đếm mặc định màn điện thoại, gõ mã → Enter → số → Enter 3 mã liên tiếp chỉ bằng bàn phím, không tràn ngang; (b) cỡ máy tính — Bảng, gõ số dòng 1 Enter nhảy dòng 2; (c) tab Bảng lệch thấy lệch và "Tồn KiotViet tạm" nếu đã nạp; `vanphong` chưa bật công tắc thấy nút duyệt disable kèm tooltip; (d) tab Nhập Excel tải file mẫu, mở thấy 4 cột không có tồn; (e) Hủy phiên thử để không để rác. Console không cảnh báo antd. Nếu trang trắng không request nào: kiểm `document.hidden` (bẫy 19) trước khi sửa code. Ghi từng bước vào SUMMARY.
  </acceptance_criteria>
  <done>Trang chi tiết phiên chạy đủ ba đường đếm + bảng lệch + duyệt; đã kiểm trên trình duyệt ở hai cỡ màn.</done>
</task>

<task type="auto">
  <name>Task 2: Thẻ kho — link phiếu kiểm kê, dọn hiển thị KiotViet cũ</name>
  <files>src/features/products/components/stock-card-columns.tsx, src/features/products/components/stock-card.tsx</files>
  <read_first>
    - `src/features/products/components/stock-card-columns.tsx` toàn bộ; `stock-card.tsx` toàn bộ; `src/features/products/types.ts` quanh `toStockCardRow` (dòng ~60-70, ~220-230).
    - `supabase/migrations/0064_lich_su_kiotviet.sql` phần `the_kho_san_pham` — xác nhận không còn nguồn `KIOTVIET_*`.
  </read_first>
  <action>
    `stock-card-columns.tsx`: thêm `KIEM_KE: "/kiem-ke"` vào `DOC_TYPE_TO_ROUTE` (route `/kiem-ke/[id]` đã có ở Task 1) và sửa comment "Chưa có route" còn
    CHUYEN_KHO, DIEU_CHINH. Xóa `KIOTVIET_NHAP`, `KIOTVIET_BAN` khỏi `SOURCE_LABELS` và comment về dòng KiotViet không link (không còn nguồn đó — D-11).
    `stock-card.tsx`: xóa `Alert` "Dữ liệu KiotViet cũ không gắn kho…" và câu chú thích dấu "—" ở cột Tồn lũy kế; thay bằng một câu ngắn "Lịch sử bán/nhập
    trên KiotViet xem ở tab “Lịch sử KiotViet”." CHỈ khi câu đó không gây hiểu nhầm cho người không có công tắc — nếu component không biết quyền thì bỏ hẳn,
    không thêm câu. Xóa import `Alert` nếu không còn dùng (bẫy build lint). Không đổi `types.ts` trừ khi comment nói về dòng KiotViet — sửa comment cho đúng
    hiện trạng, giữ logic.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; grep -q 'KIEM_KE: "/kiem-ke"' src/features/products/components/stock-card-columns.tsx &amp;&amp; ! grep -q "KIOTVIET_BAN" src/features/products/components/stock-card-columns.tsx &amp;&amp; echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Lệnh gate in `GATE-OK`.
    - `grep -c "Dữ liệu KiotViet cũ không gắn kho" src/features/products/components/stock-card.tsx` = 0.
    - Trên trình duyệt: mở một mã hàng, tab Thẻ kho không còn dòng nguồn KiotViet; nếu mã có phiếu KIEM_KE (từ pgTAP thì không — chỉ khi đã duyệt thật) bấm số phiếu mở `/kiem-ke/<id>`; console sạch.
  </acceptance_criteria>
  <done>Thẻ kho link được phiếu kiểm kê, không còn dấu vết hiển thị KiotViet cũ.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → /kiem-ke/<id> | id do người dùng gõ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-64 | Information Disclosure | Thủ kho mở id phiên kho khác | mitigate | `danh_sach_phien_kiem_ke`/`bang_dem_kiem_ke` lọc/42501 theo kho (0066); UI hiện trạng thái rỗng, không lộ dữ liệu |
| T-06-65 | Tampering | id không phải uuid gây lỗi 400 lặp | mitigate | UUID regex → `notFound()` trước khi render; hook `enabled` |
| T-06-66 | Elevation of Privilege | Hủy phiên đã duyệt từ UI | mitigate | Nút chỉ hiện NHAP_LIEU; `huy_chung_tu` chỉ quản lý cho KIEM_KE HOAN_THANH (0066) |
</threat_model>

<verification>
- `npm run check` xanh; kiểm trình duyệt hai cỡ màn + console.
</verification>

<success_criteria>
- Tiêu chí 1-3 của ROADMAP Phase 6 thao tác được trên giao diện.
- Commit: `feat(kiem-ke): trang chi tiet phien`, `refactor(the-kho): link phieu kiem ke, bo hien thi KiotViet cu`.
</success_criteria>

<output>
Create `.planning/phases/06-kiem-ke-go-live/06-15-SUMMARY.md` when done
</output>
