---
phase: quick-260919-dm4
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DS-01, DS-02, DS-03]
files_modified:
  - src/providers/antd-theme.ts
  - src/app/globals.css
  - src/shared/lib/mau-thiet-ke.ts
  - src/shared/lib/dieu-huong.ts
  - src/shared/components/app-shell.tsx
  - src/shared/components/top-nav.tsx
  - src/shared/components/thanh-tab-day.tsx
  - src/shared/components/icon-dieu-huong.tsx
  - src/shared/components/bo-cuc-danh-sach.tsx
  - src/shared/components/hang-tong-cong.tsx
  - src/features/danh-muc/schemas/bo-loc.schema.ts
  - src/features/danh-muc/components/thanh-cong-cu-san-pham.tsx
  - src/features/danh-muc/components/panel-loc-san-pham.tsx
  - src/features/danh-muc/components/thanh-loc-san-pham.tsx
  - src/features/danh-muc/components/bang-san-pham.tsx
  - src/features/danh-muc/components/cot-san-pham.tsx
  - src/features/doi-tac/types.ts
  - src/features/doi-tac/components/thanh-cong-cu-doi-tac.tsx
  - src/features/doi-tac/components/panel-loc-doi-tac.tsx
  - src/features/doi-tac/components/thanh-loc-doi-tac.tsx
  - src/features/doi-tac/components/bang-doi-tac.tsx

must_haves:
  truths:
    - "Nền mọi trang trong app là #F0F2F5, card/bảng nổi trắng lên trên — không còn nền trắng phẳng"
    - "Nút chính và link đổi sang xanh #1652F0 ở MỌI màn hình mà không phải sửa từng component"
    - "Điều hướng nằm trên thanh ngang dạng viên thuốc ở đỉnh màn hình, mục đang mở là chip sáng hơn"
    - "Dưới 992px điều hướng nằm ở thanh tab cố định dưới đáy, hàng cuối bảng không bị thanh tab che"
    - "Thủ kho vẫn không thấy mục Cài đặt (luật coQuyen giữ nguyên)"
    - "/danh-muc và /doi-tac có panel lọc cố định bên trái và một hàng tổng cộng ngay dưới header bảng"
    - "Dưới 992px panel lọc sập vào ngăn kéo, bảng cuộn ngang trong khung riêng, trang không tràn ngang"
    - "Bốn trạng thái loading/error/empty/success vẫn đủ trên cả hai trang danh sách"
  artifacts:
    - path: "src/providers/antd-theme.ts"
      provides: "Token màu/bo góc/đổ bóng theo bảng màu KiotViet"
      contains: "#1652F0"
    - path: "src/app/globals.css"
      provides: "Biến Tailwind --color-brand-* và màu nền/viền khớp token antd"
      contains: "--color-brand-600: #1652F0"
    - path: "src/shared/lib/mau-thiet-ke.ts"
      provides: "Dãy màu biểu đồ + màu ngữ nghĩa dùng chung, file KHÔNG có use client"
      exports: ["MAU_BIEU_DO", "MAU_NGU_NGHIA"]
    - path: "src/shared/lib/dieu-huong.ts"
      provides: "Danh sách mục menu + hàm thuần, file KHÔNG có use client"
      exports: ["MUC_DIEU_HUONG", "timMucDangMo", "locTheoQuyen", "tachMucMobile"]
    - path: "src/shared/components/top-nav.tsx"
      provides: "Thanh điều hướng ngang dạng pill"
    - path: "src/shared/components/thanh-tab-day.tsx"
      provides: "Thanh tab cố định đáy cho màn hình dưới 992px"
    - path: "src/shared/components/bo-cuc-danh-sach.tsx"
      provides: "Bố cục panel lọc trái + vùng bảng phải, sập thành ngăn kéo trên mobile"
    - path: "src/shared/components/hang-tong-cong.tsx"
      provides: "Hàng tổng cộng dựng từ chính mảng cột truyền cho Table"
  key_links:
    - from: "src/providers/antd-theme.ts"
      to: "src/app/globals.css"
      via: "cùng giá trị brand-600"
      pattern: "1652F0"
    - from: "src/shared/components/app-shell.tsx"
      to: "src/shared/lib/quyen.ts"
      via: "locTheoQuyen gọi coQuyen — luật phân quyền không đổi"
      pattern: "coQuyen"
    - from: "src/features/danh-muc/components/bang-san-pham.tsx"
      to: "src/shared/components/bo-cuc-danh-sach.tsx"
      via: "import BoCucDanhSach bọc QueryState"
      pattern: "BoCucDanhSach"
---

<objective>
Đổi toàn bộ design system của dự án sang ngôn ngữ thiết kế KiotViet: bảng màu xanh
#1652F0, nền trang xám #F0F2F5, bo góc tách bậc, shell điều hướng ngang + tab đáy,
và bố cục trang danh sách kiểu panel lọc trái + bảng phải + hàng tổng cộng.

**Purpose:** Người dùng đang chuyển từ KiotViet sang hệ mới. Giao diện gần với cái họ
quen giảm thời gian đào tạo và giảm phản kháng ngày go-live.

**Output:** 3 commit — token → shell → trang danh sách.

**Yêu cầu (định danh dùng trong task):**
- **DS-01** — Token thiết kế: màu, bo góc, đổ bóng, typography.
- **DS-02** — Shell: top-nav dạng pill + thanh tab cố định ở đáy dưới 992px.
- **DS-03** — Bố cục trang danh sách `/danh-muc` và `/doi-tac`.

**Phạm vi đã chốt, KHÔNG hỏi lại:** đủ cả 3 phần; điều hướng mobile là thanh tab đáy
(người dùng đã loại phương án drawer hamburger — thủ kho cầm điện thoại một tay).
</objective>

<context>
@CLAUDE.md
@.planning/STATE.md

Đọc trước khi sửa:
@src/providers/antd-theme.ts
@src/providers/app-providers.tsx
@src/app/globals.css
@src/shared/components/app-shell.tsx
@src/shared/lib/quyen.ts
@src/shared/components/query-state.tsx
@src/features/danh-muc/components/bang-san-pham.tsx
@src/features/danh-muc/components/thanh-loc-san-pham.tsx
@src/features/danh-muc/components/cot-san-pham.tsx
@src/features/doi-tac/components/bang-doi-tac.tsx
@src/features/doi-tac/components/thanh-loc-doi-tac.tsx

<bang_mau_da_chot>
Dùng ĐÚNG các giá trị này, không tự chế thêm màu.

brand-50 #EEF4FF · brand-100 #DBE7FF · brand-200 #BBD0FF · brand-300 #8FB0FF
brand-400 #5C87FB · brand-500 #2D68F8 · brand-600 #1652F0 · brand-700 #0F3FC4
brand-800 #0E3499 · brand-900 #102C6E

colorPrimary = colorLink = colorInfo = brand-600 #1652F0
Nền trang #F0F2F5 · card/bảng #FFFFFF · header bảng #E8F0FE chữ #344054
Chữ chính #1F2A37 · chữ phụ #6B7280 · viền #E5E7EB · đường chia dòng #F0F0F0
Nền hàng tổng cộng #F7F8FA · nền tag trung tính #F1F3F5

success #16A34A · warning #F59E0B · error #E5484D
Dãy màu biểu đồ: #2D68F8, #22C55E, #FB923C, #EF4444, #EC4899

Bo góc: nút/input/select 8 · card/khối 12 · modal 16 · tag/badge 6 · nav pill 9999
Đổ bóng card: 0 1px 2px rgba(16,24,40,.04), 0 1px 3px rgba(16,24,40,.06)
Typography: giữ Be_Vietnam_Pro đã nạp; tiêu đề trang 20/600 · body 14 · nhãn phụ 13;
số trong bảng canh phải + tabular-nums.
</bang_mau_da_chot>

<rang_buoc_bat_buoc>
Vi phạm bất kỳ dòng nào dưới đây là hỏng giao diện — đã vấp một lần rồi.

1. **antd v6 chỉ chạy trong Client Component.** File nào import từ `antd` PHẢI có
   `"use client"` ở dòng đầu (bẫy 1 trong CLAUDE.md).
2. **Hằng số và hàm thuần phải ở file KHÔNG có `"use client"`** (`src/shared/lib/*.ts`,
   `src/features/<x>/lib|schemas/*.ts`) để Server Component import được (bẫy 9).
3. **Prop đã bỏ của antd v5** — build vẫn xanh, chỉ cảnh báo lúc chạy (bẫy 11):
   `Alert` dùng `title` (không `message`) · `Modal` dùng `mask={{closable}}` ·
   `Drawer` dùng `size` (không `width`/`height`) · không dùng `Dropdown.Button`
   (thay bằng `Space.Compact` + `Dropdown` + `Button`) · `Select` không nhận
   `options=[{value: null}]` (dùng `""` làm "tất cả") · `Descriptions` bỏ `span` cố định.
4. **Đổi màu/bo góc phải sửa token trong `antd-theme.ts`**, KHÔNG rải class Tailwind
   lên từng component antd. Xuất hiện `!` trước class Tailwind trên component antd là
   dấu hiệu chữa cháy sai chỗ. (Tailwind dùng cho layout/khoảng cách trên thẻ HTML
   thường thì vẫn đúng — top-nav dựng bằng `<a>`/`<nav>` nên được phép.)
5. **GIỮ NGUYÊN** dòng `@layer theme, base, antd, components, utilities;` đứng TRƯỚC
   `@import "tailwindcss"` trong `globals.css`, và prop `layer` của `AntdRegistry`
   trong `app-providers.tsx`. Sửa một chỗ là vỡ.
6. **GIỮ NGUYÊN** luật phân quyền `coQuyen(vaiTro, quyen)` — chỉ đổi cách trình bày.
7. **KHÔNG cài thư viện mới.** Chỉ antd v6 + Tailwind v4 + @ant-design/icons đã có.
8. Cấm `any`, cấm `@ts-ignore`. Token nào antd v6 không nhận thì **bỏ hẳn**, không ép kiểu.
9. Không component nào vượt ~200 dòng.
10. Mỗi task chạy `npm run check` rồi mới commit. Commit Conventional Commits, mô tả
    tiếng Việt, mỗi task một commit.
</rang_buoc_bat_buoc>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rút token thiết kế theo bảng màu KiotViet (DS-01)</name>

  <files>
src/providers/antd-theme.ts (viết lại)
src/app/globals.css (viết lại khối @theme + thêm nền body)
src/shared/lib/mau-thiet-ke.ts (TẠO MỚI — không có "use client")
  </files>

  <action>
**1. `src/providers/antd-theme.ts`** — viết lại `antdTheme`, giữ nguyên kiểu `ThemeConfig`:

`token`:
- `colorPrimary`, `colorLink`, `colorInfo` = `"#1652F0"`
- `colorSuccess` `"#16A34A"` · `colorWarning` `"#F59E0B"` · `colorError` `"#E5484D"`
- `colorBgLayout` `"#F0F2F5"` · `colorBgContainer` `"#FFFFFF"`
- `colorText` `"#1F2A37"` · `colorTextSecondary` `"#6B7280"`
- `colorBorder` `"#E5E7EB"` · `colorBorderSecondary` `"#F0F0F0"`
- `borderRadius: 8` (nút/input/select) · `borderRadiusLG: 12` (card/khối) ·
  `borderRadiusSM: 6` (tag/badge)
- `fontSize: 14`
- `boxShadowTertiary: "0 1px 2px rgba(16,24,40,.04), 0 1px 3px rgba(16,24,40,.06)"`

`components`:
- `Table`: `headerBg "#E8F0FE"`, `headerColor "#344054"`, `borderColor "#F0F0F0"`,
  `rowHoverBg "#F7F9FC"`, `cellPaddingBlock: 10`
- `Layout`: `bodyBg "#F0F2F5"`, `headerBg "#FFFFFF"`, `headerHeight: 56`
  (bỏ `siderBg` — Task 2 xoá Sider)
- `Modal`: `borderRadiusLG: 16`, `titleFontSize: 18`
- `Tag`: `defaultBg "#F1F3F5"`, `defaultColor "#6B7280"` (badge trung tính trong ảnh
  là nền xám nhạt, chữ xám, không viền)
- `Menu`: `itemHeight: 40`
- `Button`: `primaryShadow "none"`, `defaultShadow "none"` (nút trong ảnh phẳng)

Sửa comment đầu file: đang ghi "Giữ `colorPrimary` trùng với `--color-brand-500`" →
phải thành **`--color-brand-600`** (cặp ràng buộc hai chiều với `globals.css`).

Token nào TypeScript báo không tồn tại trong antd v6 thì **xoá dòng đó**, không ép kiểu.

**2. `src/app/globals.css`** — KHÔNG đụng vào dòng `@layer ...` và `@import "tailwindcss"`.
Chỉ viết lại khối `@theme`:
- giữ `--font-sans` nguyên trạng
- thay 6 biến brand cũ bằng đủ 10 bậc `--color-brand-50` … `--color-brand-900` theo
  `<bang_mau_da_chot>`
- thêm: `--color-nen-trang: #F0F2F5` · `--color-nen-the: #FFFFFF` ·
  `--color-vien: #E5E7EB` · `--color-chu-chinh: #1F2A37` · `--color-chu-phu: #6B7280` ·
  `--color-header-bang: #E8F0FE` · `--color-nen-tong: #F7F8FA`
- thêm `--shadow-the: 0 1px 2px rgba(16,24,40,.04), 0 1px 3px rgba(16,24,40,.06);`
  (sinh ra utility `shadow-the`)
- thêm `--radius-the: 12px;` (sinh ra `rounded-the`)

Cập nhật comment trên khối brand để nói rõ: trùng khít với token trong
`src/providers/antd-theme.ts`, `colorPrimary` = `--color-brand-600`.

Thêm `background: var(--color-nen-trang);` vào rule `body` — trang `/dang-nhap` và
`/doi-mat-khau` nằm ngoài `Layout` của antd nên không nhận `colorBgLayout`.

**3. `src/shared/lib/mau-thiet-ke.ts`** — TẠO MỚI, **không** có `"use client"`, **không**
import gì từ `antd` (để Server Component dùng được — bẫy 9):

```ts
/** Dãy màu biểu đồ Recharts. Dashboard (Phase 5) import từ đây, không tự đặt màu. */
export const MAU_BIEU_DO = ["#2D68F8", "#22C55E", "#FB923C", "#EF4444", "#EC4899"] as const;

/** Màu ngữ nghĩa — dùng cho viền trái card KPI và trạng thái. */
export const MAU_NGU_NGHIA = {
  chinh: "#1652F0",
  tot: "#16A34A",
  canhBao: "#F59E0B",
  xau: "#E5484D",
} as const;
```

Không sửa file nào khác ở task này.
  </action>

  <verify>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && npm run check</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && grep -c "1652F0" src/providers/antd-theme.ts src/app/globals.css</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && head -20 src/app/globals.css | grep -n "@layer theme, base, antd, components, utilities;" && grep -n "AntdRegistry layer" src/providers/app-providers.tsx</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && ! grep -q "use client" src/shared/lib/mau-thiet-ke.ts && echo "OK: mau-thiet-ke.ts la file thuan"</automated>
  </verify>

  <done>
`npm run check` xanh. `#1652F0` xuất hiện ở cả `antd-theme.ts` và `globals.css`.
Thứ tự layer và prop `layer` của AntdRegistry còn nguyên. `mau-thiet-ke.ts` không có
`"use client"`. Đã commit: `feat(ui): rút token thiết kế theo bảng màu KiotViet`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Đổi shell sang top-nav dạng pill + thanh tab đáy (DS-02)</name>

  <files>
src/shared/lib/dieu-huong.ts (TẠO MỚI — không có "use client")
src/shared/components/icon-dieu-huong.tsx (TẠO MỚI)
src/shared/components/top-nav.tsx (TẠO MỚI)
src/shared/components/thanh-tab-day.tsx (TẠO MỚI)
src/shared/components/app-shell.tsx (viết lại — bỏ Sider)
  </files>

  <action>
**1. `src/shared/lib/dieu-huong.ts`** — TẠO MỚI, **không** `"use client"`, **không**
import từ `antd` hay `@ant-design/icons` (icon để dạng mã chuỗi, ánh xạ sang element ở
file client — đây là cách chắc chắn tránh bẫy 9).

```ts
import { coQuyen, type Quyen, type VaiTro } from "@/shared/lib/quyen";

export type MaIcon = "tong-quan" | "danh-muc" | "doi-tac" | "cai-dat";

export type MucDieuHuong = {
  duongDan: string;
  nhan: string;
  /** Nhãn ngắn cho thanh tab đáy — chỗ hẹp, tối đa ~8 ký tự. */
  nhanNgan: string;
  icon: MaIcon;
  quyen: Quyen;
  /** null = không vào thanh tab đáy, nằm trong mục "Khác". */
  uuTienMobile: number | null;
};
```

`MUC_DIEU_HUONG` chép đúng 4 mục đang có trong `app-shell.tsx` hiện tại (đường dẫn,
nhãn, `quyen` giữ nguyên không đổi một chữ), thêm:
- `/` — nhanNgan "Tổng quan", icon `"tong-quan"`, uuTienMobile 1
- `/danh-muc` — nhanNgan "Hàng", icon `"danh-muc"`, uuTienMobile 2
- `/doi-tac` — nhanNgan "Đối tác", icon `"doi-tac"`, uuTienMobile 3
- `/cai-dat` — nhanNgan "Cài đặt", icon `"cai-dat"`, uuTienMobile `null`
  (thủ kho hiếm dùng, đẩy vào "Khác")

Bốn hàm thuần:
- `timMucDangMo(pathname, muc): string` — **chép nguyên** logic đang có trong
  `app-shell.tsx` (`"/"` chỉ khớp chính nó; mục khác khớp cả route con, lấy `.at(-1)`).
- `locTheoQuyen(vaiTro, muc): MucDieuHuong[]` — `muc.filter(m => coQuyen(vaiTro, m.quyen))`.
  **Đây là chỗ duy nhất giữ luật phân quyền, không đổi luật.**
- `tachMucMobile(muc): { chinh: MucDieuHuong[]; khac: MucDieuHuong[] }` — mục có
  `uuTienMobile !== null` sắp tăng dần, lấy tối đa 4 vào `chinh`; phần còn lại (kể cả
  mục bị cắt vì quá 4) vào `khac`.
- `soCotTabDay(chinh, khac): number` — `chinh.length + (khac.length > 0 ? 1 : 0)`.
  Cần vì Tailwind không sinh được class động `grid-cols-${n}` — bên tab đáy sẽ dùng
  `style={{ gridTemplateColumns: \`repeat(\${n}, minmax(0,1fr))\` }}`.

**2. `src/shared/components/icon-dieu-huong.tsx`** — `"use client"`. Map
`Record<MaIcon, ReactNode>` tên `ICON_DIEU_HUONG`, dùng đúng 4 icon đang có trong
`app-shell.tsx`: `DashboardOutlined`, `AppstoreOutlined`, `TeamOutlined`,
`SettingOutlined`. File ~15 dòng, để riêng vì cả top-nav lẫn tab đáy đều cần.

**3. `src/shared/components/top-nav.tsx`** — `"use client"`. Props:
`{ nguoiDung: { hoTen: string; vaiTro: VaiTro }; muc: MucDieuHuong[]; dangMo: string }`.

Cấu trúc (dựng bằng thẻ HTML thường + `next/link`, KHÔNG dùng antd `Menu` — pill nền
xanh không ép được qua token antd; vì không phải component antd nên Tailwind ở đây là
đúng chỗ, không cần `!`):

```
<header className="sticky top-0 z-20 border-b border-vien bg-nen-the">
  <div className="flex h-14 items-center gap-3 px-4 lg:px-6">
    <Link href="/" className="shrink-0 font-semibold text-chu-chinh">Kho Minh Vũ</Link>

    {/* Pill nav — chỉ hiện từ 992px, dưới đó đã có thanh tab đáy */}
    <nav className="hidden items-center gap-1 rounded-full bg-brand-600 p-1 lg:flex">
      {muc.map(m => (
        <Link key={m.duongDan} href={m.duongDan}
          aria-current={m.duongDan === dangMo ? "page" : undefined}
          className={[
            "flex items-center gap-2 rounded-full px-3 py-1.5 text-sm transition-colors",
            m.duongDan === dangMo
              ? "bg-brand-400 font-medium text-white"
              : "text-white/80 hover:bg-brand-500 hover:text-white",
          ].join(" ")}>
          {ICON_DIEU_HUONG[m.icon]}
          {m.nhan}
        </Link>
      ))}
    </nav>

    <div className="ms-auto"><MenuTaiKhoan nguoiDung={nguoiDung} /></div>
  </div>
</header>
```

Giữ nguyên `MenuTaiKhoan` hiện có, không sửa file đó.

**4. `src/shared/components/thanh-tab-day.tsx`** — `"use client"`. Props:
`{ chinh: MucDieuHuong[]; khac: MucDieuHuong[]; dangMo: string }`.

- `<nav className="fixed inset-x-0 bottom-0 z-30 border-t border-vien bg-nen-the lg:hidden"`
  kèm `style={{ paddingBottom: "env(safe-area-inset-bottom)" }}` (iPhone có thanh gạch dưới).
- Bên trong: `<div className="grid" style={{ gridTemplateColumns: repeat(soCot, minmax(0,1fr)) }}>`.
- Mỗi ô: `<Link>` dọc — icon 20px trên, `nhanNgan` 11px dưới, `py-2 text-center`;
  đang mở thì `text-brand-600 font-medium`, còn lại `text-chu-phu`.
- Nếu `khac.length > 0`: ô cuối là `<button>` "Khác" (`EllipsisOutlined`) mở
  `Drawer` của antd với **`placement="bottom"` và `size="default"` — TUYỆT ĐỐI không
  dùng prop `width`/`height` (antd v6 đã bỏ, bẫy 11)**. Nội dung ngăn kéo là danh sách
  `<Link>` dọc của `khac`; bấm vào một mục thì `setMo(false)` rồi điều hướng.
- Nếu `khac.length === 0` thì không render ô "Khác" và không render `Drawer`.

**5. `src/shared/components/app-shell.tsx`** — viết lại, **xoá hẳn** `Sider`, state
`thuGon`, hai icon `MenuFold/MenuUnfold`, và mảng `MUC_MENU` + hàm `timMucDangMo` cục bộ
(đã chuyển sang `dieu-huong.ts`). Kết quả ~40 dòng:

```tsx
"use client";
// import Layout từ antd, usePathname, các thành phần vừa tạo

const { Content } = Layout;

export function AppShell({ nguoiDung, children }: AppShellProps) {
  const pathname = usePathname();
  // D-07: menu chỉ hiện mục vai trò có quyền — ẩn hẳn, không chỉ disable.
  const muc = locTheoQuyen(nguoiDung.vaiTro, MUC_DIEU_HUONG);
  const dangMo = timMucDangMo(pathname, muc);
  const { chinh, khac } = tachMucMobile(muc);

  return (
    <Layout className="min-h-screen">
      <TopNav nguoiDung={nguoiDung} muc={muc} dangMo={dangMo} />
      {/* pb-24 chừa chỗ cho thanh tab đáy — thiếu là hàng cuối bảng bị che. */}
      <Content className="px-4 pt-4 pb-24 lg:px-6 lg:pt-6 lg:pb-6">{children}</Content>
      <ThanhTabDay chinh={chinh} khac={khac} dangMo={dangMo} />
    </Layout>
  );
}
```

Giữ nguyên comment "D-07" để không mất dấu vết quyết định.

**Bonus bắt buộc:** blocker trong STATE.md ghi `app-shell.tsx` còn mô tả phạm vi cũ
(theo dõi sản xuất 5 xưởng). Viết lại xong thì kiểm `grep -rn "xưởng\|sản xuất" src/shared/components/`
— còn sót thì sửa cho khớp phạm vi kho thương mại.
  </action>

  <verify>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && npm run check</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && ! grep -q "use client" src/shared/lib/dieu-huong.ts && ! grep -q "antd" src/shared/lib/dieu-huong.ts && echo "OK: dieu-huong.ts la file thuan"</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && for f in $(grep -rl 'from "antd"' src --include=*.tsx); do head -1 "$f" | grep -q 'use client' || echo "THIEU use client: $f"; done; echo "--- het ---"</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && ! grep -rn "Dropdown.Button" src/ && ! grep -rn "Drawer" src/ | grep -E "width=|height=" && echo "OK: khong dung prop antd v5 da bo"</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && grep -n "coQuyen" src/shared/lib/dieu-huong.ts</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && (npm run dev > /tmp/dev-dm4.log 2>&1 &) && sleep 20 && npx tsx scripts/kiem-tra-quyen-route.ts; pkill -f "next dev" || true</automated>
    <manual>
MỞ TRÌNH DUYỆT — `npm run check` xanh KHÔNG chứng minh giao diện chạy (bẫy 11: antd v6
chỉ cảnh báo prop đã bỏ lúc CHẠY). Chạy `npm run dev`, đăng nhập, rồi:
1. Mở Console (F12), đi lần lượt `/`, `/danh-muc`, `/doi-tac`, `/cai-dat` —
   phải KHÔNG có warning nào chứa "antd", "deprecated", "Warning:".
2. Desktop: thanh pill xanh ở đỉnh, mục đang mở là chip sáng hơn nền pill.
3. Thu cửa sổ còn 390px: pill biến mất, thanh tab cố định hiện ở đáy, cuộn xuống hết
   bảng vẫn thấy đủ hàng cuối (không bị thanh tab che). Bấm "Khác" ra ngăn kéo từ dưới lên.
4. Đăng nhập bằng tài khoản thủ kho: không thấy mục "Cài đặt" ở cả pill lẫn tab đáy.
    </manual>
  </verify>

  <done>
`npm run check` xanh; `scripts/kiem-tra-quyen-route.ts` vẫn xanh toàn bộ ma trận.
Không file `.tsx` nào import antd mà thiếu `"use client"`. `dieu-huong.ts` là file thuần.
Console trình duyệt sạch warning antd trên cả 4 route, ở cả desktop lẫn 390px.
Đã commit: `feat(ui): đổi shell sang thanh điều hướng ngang và tab đáy cho mobile`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dựng lại bố cục trang danh sách theo kiểu KiotViet (DS-03)</name>

  <files>
src/shared/components/bo-cuc-danh-sach.tsx (TẠO MỚI)
src/shared/components/hang-tong-cong.tsx (TẠO MỚI)
src/features/danh-muc/schemas/bo-loc.schema.ts (thêm demDieuKien)
src/features/danh-muc/components/thanh-cong-cu-san-pham.tsx (TẠO MỚI)
src/features/danh-muc/components/panel-loc-san-pham.tsx (TẠO MỚI)
src/features/danh-muc/components/thanh-loc-san-pham.tsx (XOÁ)
src/features/danh-muc/components/bang-san-pham.tsx (sửa)
src/features/danh-muc/components/cot-san-pham.tsx (sửa nhẹ)
src/features/doi-tac/types.ts (thêm demDieuKienDoiTac)
src/features/doi-tac/components/thanh-cong-cu-doi-tac.tsx (TẠO MỚI)
src/features/doi-tac/components/panel-loc-doi-tac.tsx (TẠO MỚI)
src/features/doi-tac/components/thanh-loc-doi-tac.tsx (XOÁ)
src/features/doi-tac/components/bang-doi-tac.tsx (sửa)
  </files>

  <action>
Làm `/danh-muc` trước cho xong hẳn, rồi lặp đúng khuôn mẫu đó cho `/doi-tac`.

**1. `src/shared/components/bo-cuc-danh-sach.tsx`** — `"use client"`. Props:
`{ panelLoc: ReactNode; thanhCongCu: ReactNode; soDieuKien: number; children: ReactNode }`.

```
<div className="flex items-start gap-4">
  {/* Panel lọc cố định trái, 264px, chỉ từ 992px */}
  <aside className="hidden w-[264px] shrink-0 rounded-the bg-nen-the p-4 shadow-the lg:block">
    {panelLoc}
  </aside>

  <section className="min-w-0 flex-1 rounded-the bg-nen-the p-3 shadow-the lg:p-4">
    <div className="mb-3 flex flex-wrap items-center gap-2">
      <Button className="lg:hidden" icon={<FilterOutlined />} onClick={() => setMo(true)}>
        Bộ lọc{soDieuKien > 0 ? ` (${soDieuKien})` : ""}
      </Button>
      <div className="min-w-0 flex-1">{thanhCongCu}</div>
    </div>
    {/* Bảng cuộn ngang TRONG khung này, không để cả trang tràn ngang */}
    <div className="overflow-x-auto">{children}</div>
  </section>
</div>

<Drawer title="Bộ lọc" placement="bottom" size="large" open={mo} onClose={() => setMo(false)}>
  {panelLoc}
</Drawer>
```

**`size="large"`, KHÔNG dùng `height`/`width`** (antd v6 đã bỏ — bẫy 11).
`children` là `<QueryState>` nguyên vẹn → panel + thanh công cụ vẫn hiện ở cả 4 trạng
thái loading/error/empty/success. **Không được bỏ `QueryState` khi đổi bố cục.**

**2. `src/shared/components/hang-tong-cong.tsx`** — `"use client"`. Dựng hàng tổng cộng
**từ chính mảng cột truyền cho `Table`** để chỉ số ô không bao giờ lệch khi cột bật/tắt
theo quyền (`Giá vốn`, `Sửa`) hoặc khi có `rowSelection`.

```tsx
type Props = {
  /** Đúng mảng đang truyền vào prop `columns` của Table. */
  cot: ReadonlyArray<{ key?: React.Key; dataIndex?: unknown }>;
  /** Table có bật rowSelection không — nếu có thì cột 0 là ô chọn. */
  coChon: boolean;
  /** Nội dung ô đầu tiên, vd "Tổng cộng — 3.266 mã". */
  nhan: ReactNode;
  /** dataIndex -> tổng của các dòng ĐANG HIỂN THỊ. */
  cong?: Record<string, number>;
};
```

Render `<Table.Summary fixed="top"><Table.Summary.Row className="bg-nen-tong font-semibold">`:
- nếu `coChon`, ô `index={0}` rỗng, các ô sau lệch 1
- ô của cột đầu tiên → `nhan`
- ô có `dataIndex` nằm trong `cong` → `<Tooltip title="Cộng các dòng đang hiển thị trên
  trang này">` bọc số đã `toLocaleString("vi-VN")`, `className="block text-right tabular-nums"`
- các ô còn lại rỗng

Nhãn phải nói đúng phạm vi cộng: tổng số bản ghi lấy từ `tong` của RPC (toàn bộ bộ lọc),
còn các cột số chỉ cộng **trang đang xem** — nói rõ bằng Tooltip, **không bịa** là tổng
toàn kho.

**3. `bo-loc.schema.ts`** (file thuần, không `"use client"`) — thêm:
```ts
/** Đếm điều kiện đang bật, KHÔNG tính ô tìm kiếm (ô tìm nằm ngoài panel). */
export function demDieuKien(b: BoLocSanPham): number
```
đếm: `nhomHangId`, `congDoanId`, `dvtId`, `trangThaiTon` khác null; `canRa` true;
`kinhDoanh !== BO_LOC_MAC_DINH.kinhDoanh`.

**4. `thanh-cong-cu-san-pham.tsx`** (MỚI) — tách phần trên của `thanh-loc-san-pham.tsx`:
ô tìm + `hanhDongPhu` + `nutThem`. **Chép nguyên** logic debounce 300ms, state `tuKhoa`/
`qTruoc` chỉnh trong lúc render, cleanup `useEffect`, `onPressEnter` — đừng viết lại.
Ô tìm theo ảnh: `<Input prefix={<SearchOutlined/>} allowClear autoFocus className="w-full
sm:max-w-md" />` (bo 8 đã do token lo). `hanhDongPhu`/`nutThem` gom bên phải bằng `ms-auto`.

**5. `panel-loc-san-pham.tsx`** (MỚI) — các `Select` xếp DỌC, mỗi cái một nhóm có nhãn:
```tsx
function NhomLoc({ nhan, children }: { nhan: string; children: ReactNode }) {
  return (
    <div>
      <label className="mb-1 block text-[13px] text-chu-phu">{nhan}</label>
      {children}
    </div>
  );
}
```
Bọc lần lượt: Nhóm hàng · Công đoạn · Đơn vị tính · Tồn · Kinh doanh — chép nguyên props
của từng `Select` từ file cũ (`allowClear`, `showSearch`, `optionFilterProp`,
`optionRender` chấm màu công đoạn), chỉ đổi `className="w-56"`→`className="w-full"`.
Cuối panel: `<Button block>` "Xóa bộ lọc", `disabled` khi `demDieuKien(boLoc) === 0`.
**Không thêm option `{ value: null }`** vào bất kỳ `Select` nào (bẫy 11) — giữ nguyên
cách hiện tại là `allowClear` + `value` có thể null.

**6. XOÁ `thanh-loc-san-pham.tsx`** sau khi đã chuyển hết sang 2 file trên.

**7. `bang-san-pham.tsx`** (SỬA):
- thay `<ThanhLocSanPham .../>` bằng `<BoCucDanhSach panelLoc={<PanelLocSanPham .../>}
  thanhCongCu={<ThanhCongCuSanPham .../>} soDieuKien={demDieuKien(boLoc)}>` bọc quanh
  `<QueryState>` hiện có
- **xoá** `<div className="overflow-x-auto">` bên trong (BoCucDanhSach đã có, tránh
  lồng hai lớp cuộn)
- thêm vào `<Table>`: `summary={(dongHienThi) => <HangTongCong cot={cot} coChon={quyen.sua}
  nhan={\`Tổng cộng — \${tong.toLocaleString("vi-VN")} mã\`} cong={{ tong_ton: Σ Number(d.tong_ton ?? 0) }} />}`.
  Cần tách `const cot = taoCot({...})` ra biến để truyền được cho cả `columns` lẫn `summary`.
- `Alert` "Cần rà" và `ThanhGanHangLoat` đặt bên trong `BoCucDanhSach` (trên bảng, dưới
  thanh công cụ) — truyền qua `children` phía trên `QueryState`. Giữ nguyên `Alert` dùng
  prop `title` (KHÔNG đổi thành `message`).
- giữ nguyên toàn bộ logic `doiBoLoc`, `onBangDoi`, effect quay về trang 1, `NganKeo*`.

**8. `cot-san-pham.tsx`** (SỬA NHẸ) — thêm `className: "tabular-nums"` cho 3 cột số
`Tồn`, `Giá bán`, `Giá vốn` (đã có `align: "right"`). Không đụng gì khác.

**9–13. `/doi-tac`** — lặp đúng khuôn mẫu trên:
- `types.ts` (file thuần): thêm `demDieuKienDoiTac(b: BoLocDoiTac): number` — đếm
  `loai !== null` và `hoatDong !== BO_LOC_DOI_TAC_MAC_DINH.hoatDong`.
- `thanh-cong-cu-doi-tac.tsx`: **đổi `Input.Search` → `Input` + `prefix={<SearchOutlined/>}`**
  cho khớp ảnh và khớp trang danh mục; chép nguyên debounce 300ms; nút "Thêm đối tác"
  (`type="primary"`) đặt `ms-auto`.
- `panel-loc-doi-tac.tsx`: `Segmented` loại đối tác dùng `block` xếp dọc trong `NhomLoc`
  nhãn "Loại đối tác"; `Select` trạng thái `className="w-full"` trong `NhomLoc` nhãn
  "Trạng thái"; `<Button block>` "Xóa bộ lọc".
- XOÁ `thanh-loc-doi-tac.tsx`.
- `bang-doi-tac.tsx`: bọc `BoCucDanhSach`, bỏ `overflow-x-auto` lồng, thêm
  `summary` → `<HangTongCong cot={cot} coChon={false} nhan={\`Tổng cộng — \${tong} đối tác\`} />`
  (bảng này không có cột số nên `cong` để trống).
  **Tag:** theo ảnh, badge là nền xám nhạt không viền → thêm `bordered={false}` cho mọi
  `<Tag>` trong file; giữ `MAU_LOAI_DOI_TAC` cho cột "Loại" (màu còn mang thông tin),
  nhưng cột "Trạng thái" đổi sang `<Tag bordered={false}>` trung tính cho cả hai giá trị,
  phân biệt bằng chữ "Đang dùng"/"Ngừng". *(Giả định — ghi vào báo cáo cuối.)*

**Kiểm tra 200 dòng:** file nào chạm ngưỡng thì tách tiếp theo trách nhiệm.
  </action>

  <verify>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && npm run check</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && grep -c "QueryState" src/features/danh-muc/components/bang-san-pham.tsx src/features/doi-tac/components/bang-doi-tac.tsx</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && ! ls src/features/danh-muc/components/thanh-loc-san-pham.tsx src/features/doi-tac/components/thanh-loc-doi-tac.tsx 2>/dev/null && echo "OK: da xoa file cu"</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && for f in $(grep -rl 'from "antd"' src --include=*.tsx); do head -1 "$f" | grep -q 'use client' || echo "THIEU use client: $f"; done; echo "--- het ---"</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && ! grep -rnE 'className="[^"]*![a-z-]+' src/ && echo "OK: khong co ! Tailwind chua chay sai cho"</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && ! grep -rn "Input.Search" src/features/doi-tac/ && ! grep -rn "options=\[{ value: null" src/ && echo "OK"</automated>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && awk 'END{print FILENAME, NR}' src/shared/components/bo-cuc-danh-sach.tsx; wc -l src/features/danh-muc/components/*.tsx src/features/doi-tac/components/*.tsx src/shared/components/*.tsx | sort -rn | head -8</automated>
    <manual>
BẮT BUỘC MỞ TRÌNH DUYỆT VÀ XEM CONSOLE — `npm run check` xanh KHÔNG chứng minh giao diện
chạy. antd v6 chỉ cảnh báo prop đã bỏ lúc CHẠY, build vẫn xanh (bẫy 11 trong CLAUDE.md).

`npm run dev`, đăng nhập, mở Console (F12):
1. `/danh-muc` và `/doi-tac` — Console KHÔNG có dòng nào chứa "antd", "deprecated",
   "Warning:". Có thì sửa hết rồi mới commit.
2. Desktop ≥1280px: panel lọc đứng yên bên trái 264px, bảng bên phải trong card trắng
   bo 12 có bóng nhẹ; nền trang xám #F0F2F5; header bảng xanh nhạt; hàng "Tổng cộng"
   nền xám ngay dưới header, in đậm, số canh phải.
3. Đổi bộ lọc trong panel → bảng đổi, URL đổi, về trang 1.
4. Đủ 4 trạng thái: tải lại trang (skeleton) · tắt mạng rồi Thử lại (Alert + nút) ·
   gõ từ khóa rác (Empty có hướng dẫn) · gõ lại (dữ liệu).
5. Thu còn 390px: panel biến mất, nút "Bộ lọc (n)" hiện, bấm ra ngăn kéo từ dưới lên;
   bảng cuộn ngang TRONG khung, thanh cuộn ngang của cả trang KHÔNG xuất hiện; cuộn
   xuống cuối vẫn thấy đủ hàng cuối, thanh tab đáy không che.
    </manual>
  </verify>

  <done>
`npm run check` xanh. Hai file `thanh-loc-*.tsx` cũ đã xoá. `QueryState` còn nguyên ở cả
hai bảng. Không file `.tsx` nào import antd mà thiếu `"use client"`. Không có `!` trước
class Tailwind. Không file nào vượt ~200 dòng. Console trình duyệt sạch warning antd trên
`/danh-muc` và `/doi-tac`, ở cả 1280px lẫn 390px; đủ 4 trạng thái; trang không tràn ngang.
Đã commit: `feat(ui): dựng lại bố cục trang danh sách theo giao diện KiotViet`.
  </done>
</task>

</tasks>

<verification>
Sau cả 3 task:

1. `npm run check` xanh.
2. `npx tsx scripts/kiem-tra-quyen-route.ts` xanh toàn bộ ma trận (quyền không đổi).
3. `git log --oneline -3` cho đúng 3 commit, mỗi commit một task.
4. Ràng buộc cặp còn nguyên:
   `head -20 src/app/globals.css | grep "@layer theme, base, antd"` và
   `grep "AntdRegistry layer" src/providers/app-providers.tsx`.
5. `grep -c "1652F0" src/providers/antd-theme.ts src/app/globals.css` — cả hai > 0.
6. Không có thư viện mới: `git diff --stat HEAD~3 -- package.json` rỗng.
7. Console trình duyệt sạch warning antd trên `/`, `/danh-muc`, `/doi-tac`, `/cai-dat`
   ở cả desktop lẫn 390px.
</verification>

<success_criteria>
- Nền app xám #F0F2F5, nút/link xanh #1652F0 ở mọi màn hình mà không sửa component lẻ nào.
- Bo góc tách bậc: nút 8 · card 12 · modal 16 · tag 6.
- Top-nav pill xanh ở desktop; thanh tab cố định đáy ở <992px với mục "Khác"; nội dung
  trang chừa đủ khoảng dưới để không bị che.
- Thủ kho vẫn không thấy Cài đặt — `coQuyen` không đổi một dòng.
- `/danh-muc` + `/doi-tac`: panel lọc trái 264px, bảng trong card trắng, hàng tổng cộng
  dưới header, số canh phải tabular-nums; mobile sập panel vào ngăn kéo, bảng cuộn trong khung.
- Đủ 4 trạng thái qua `QueryState` trên cả hai trang.
- 0 warning antd trong Console; 0 `any`, 0 `@ts-ignore`, 0 `!` Tailwind trên component antd.
</success_criteria>

<output>
Xong thì ghi tóm tắt vào
`.planning/quick/260919-dm4-update-design-system-theo-giao-dien-kiot/260919-dm4-SUMMARY.md`,
nêu rõ: file đã tạo/sửa/xoá, **giả định đã đặt** (đáng chú ý: Tag trạng thái đối tác đổi
sang trung tính không viền; hàng tổng cộng chỉ cộng trang đang xem), và phần nào chưa làm
(card KPI viền trái màu + dãy màu biểu đồ mới chỉ có hằng số `MAU_BIEU_DO`/`MAU_NGU_NGHIA`,
chưa có màn hình dashboard nào dùng — chờ Phase 5).
</output>
