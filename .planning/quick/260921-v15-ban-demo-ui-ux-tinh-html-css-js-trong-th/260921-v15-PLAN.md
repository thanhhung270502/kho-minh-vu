---
phase: quick-260921-v15
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DEMO-01, DEMO-02, DEMO-03]
files_modified:
  - design/README.md
  - design/check.mjs
  - design/assets/tokens.css
  - design/assets/app.css
  - design/assets/app.js
  - design/assets/data.js
  - design/styleguide.html
  - design/dang-nhap.html
  - design/doi-mat-khau.html
  - design/khong-du-quyen.html
  - design/index.html
  - design/nhap-kho.html
  - design/nhap-kho-chi-tiet.html
  - design/dat-hang.html
  - design/dat-hang-chi-tiet.html
  - design/xuat-kho.html
  - design/xuat-kho-chi-tiet.html
  - design/tra-hang.html
  - design/tra-hang-chi-tiet.html
  - design/in-phieu.html
  - design/danh-muc.html
  - design/danh-muc-chi-tiet.html
  - design/doi-tac.html
  - design/doi-tac-chi-tiet.html
  - design/ton-kho.html
  - design/the-kho.html
  - design/kiem-ke.html
  - design/chuyen-kho.html
  - design/bao-cao.html
  - design/cai-dat.html

must_haves:
  truths:
    - "Nhấp đúp bất kỳ file design/*.html (giao thức file://, không server, không mạng) là thấy màn hình đủ khung, đủ dữ liệu mẫu, console không có lỗi"
    - "Từ 992px trở lên có top nav pill gradient xanh, mục đang mở sáng hơn và có gạch trắng bên dưới; dưới 992px pill biến mất, có thanh tab đáy 4 ô + 'Khác' mở ngăn kéo đáy"
    - "Màu, bo góc, đổ bóng trùng khít token trong src/providers/antd-theme.ts và src/app/globals.css (primary #0070F4, header bảng #E6F1FE, nền #F7F8F9, card bo 12, nút bo 12, modal bo 24)"
    - "Trên phiếu nhập/xuất: gõ mã → Enter → số lượng → Enter thì thêm được dòng; bấm Ghi sổ hiện hộp tóm tắt rồi phiếu chuyển NHAP_LIEU → HOAN_THANH và khóa sửa; Hủy phiếu chuyển DA_HUY kèm ghi chú bút toán đảo"
    - "Phiếu xuất có dòng vượt tồn bị tô màu, nút Ghi sổ bị khóa cho tới khi chọn một trong bốn lý do xuất âm"
    - "Không màn nào có ô sửa trực tiếp số tồn — tồn chỉ đổi qua chứng từ (kiểm kê sinh phiếu điều chỉnh)"
    - "Ở 375px không trang nào tràn ngang; bảng cuộn ngang trong khung riêng; màn kiểm kê dùng được một tay (nút ≥44px)"
    - "git status chỉ có thay đổi dưới design/ — package.json, eslint, tsconfig, src/ không bị đụng"
  artifacts:
    - path: "design/assets/tokens.css"
      provides: "Biến CSS chép đúng giá trị từ antd-theme.ts + globals.css"
      contains: "#0070F4"
    - path: "design/assets/app.js"
      provides: "window.KMV: render khung (top nav + tab đáy + menu tài khoản), drawer/modal/toast, lọc bảng, state phiên"
      contains: "KMV"
    - path: "design/assets/data.js"
      provides: "window.KMV_DATA: dữ liệu mẫu phụ tùng xe máy tiếng Việt"
      contains: "KMV_DATA"
    - path: "design/assets/app.css"
      provides: "Component CSS: nút, tag, bảng, form, card, drawer, modal, toast, skeleton, list-layout, print"
    - path: "design/styleguide.html"
      provides: "Mục lục mọi màn + styleguide token/nút/tag/bảng/form/4 trạng thái"
    - path: "design/README.md"
      provides: "Cách mở, sơ đồ file, quy ước, ghi chú eslint/prettier"
    - path: "design/check.mjs"
      provides: "Bộ kiểm tĩnh: đủ file, không URL ngoài, link nội bộ không gãy, không type=module/fetch"
  key_links:
    - from: "design/*.html"
      to: "design/assets/app.js"
      via: "<body data-page=... data-nav=...> + <script src> thường (KHÔNG type=module)"
      pattern: "assets/app.js"
    - from: "design/assets/tokens.css"
      to: "src/providers/antd-theme.ts"
      via: "cùng giá trị hex — đối chiếu tay, không import"
      pattern: "0070F4|E6F1FE|F7F8F9"
    - from: "design/assets/app.js"
      to: "design/assets/data.js"
      via: "đọc window.KMV_DATA, ghi đè thay đổi phiên vào sessionStorage"
      pattern: "KMV_DATA"
---

<objective>
Tạo thư mục `design/` ở gốc repo: bản demo UI/UX TĨNH cho toàn bộ hệ thống Kho Minh Vũ —
HTML + CSS + JS thuần, không build, không framework, không CDN, mở bằng `file://` là chạy.

**Purpose:** Có một bản nhìn-thấy-được của CẢ hệ thống (kể cả các màn Phase 5–6 chưa làm:
tồn kho, thẻ kho, kiểm kê, chuyển kho, tổng quan) để chủ doanh nghiệp và thủ kho góp ý
bố cục trước khi tốn công code thật; đồng thời là bản tham chiếu thị giác cho các phase sau.

**Output:** 3 commit — (1) nền tảng dùng chung + styleguide, (2) các màn chứng từ,
(3) các màn danh mục / tồn kho / quản trị. Mỗi commit tự mở xem được.

**Yêu cầu (định danh dùng trong task):**
- **DEMO-01** — Nền tảng dùng chung: token, component CSS, khung render bằng JS, dữ liệu mẫu, styleguide, README.
- **DEMO-02** — Luồng chứng từ: nhập kho, đặt hàng, xuất kho, trả hàng, mẫu in.
- **DEMO-03** — Danh mục, đối tác, tồn kho, thẻ kho, kiểm kê, chuyển kho, tổng quan, báo cáo, cài đặt.

**Ranh giới cứng, KHÔNG hỏi lại:**
- Chỉ tạo/sửa file dưới `design/`. Không đụng `src/`, `package.json`, `eslint.config.*`,
  `tsconfig.json`, `.prettierignore`, `.gitignore`. Không `npm install` gì.
- Đây là demo tĩnh, KHÔNG phải code ứng dụng → các quy tắc "Bước 1–7" của CLAUDE.md về
  feature folder / TanStack Query / mapper KHÔNG áp dụng. Vẫn áp dụng: năm nguyên tắc kiến
  trúc (thể hiện qua UI), bốn trạng thái loading/error/empty/success, thông báo lỗi nói
  rõ làm gì tiếp, chuỗi hiển thị tiếng Việt có dấu, tên file = URL tiếng Việt không dấu.
- Ngoài phạm vi v1 nên KHÔNG vẽ: công nợ, lô/hạn dùng, hóa đơn điện tử, sản xuất/WIP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

Nguồn token — chép ĐÚNG giá trị, không ước lượng:
@src/providers/antd-theme.ts
@src/app/globals.css

Khung ứng dụng cần tái hiện:
@src/shared/lib/navigation.ts
@src/shared/components/top-nav.tsx
@src/shared/components/bottom-tab-bar.tsx
@src/shared/components/list-layout.tsx
@src/shared/components/page-header.tsx
@src/shared/components/query-state.tsx
@src/shared/components/posting-summary.tsx

Liếc (không cần đọc hết) để lấy cột bảng / bộ lọc / nhãn trạng thái thật:
`src/features/*/components/*-table-body.tsx`, `*-columns.tsx`, `*-filter-panel.tsx`,
`src/features/products/components/stock-card.tsx`,
`src/features/documents/components/negative-stock-panel.tsx`,
`src/features/sales-order/lib/order-status.ts`, `src/features/settings/components/settings-tabs.tsx`.

<interfaces>
<!-- Đã trích sẵn từ codebase. Dùng thẳng, không cần đi tìm lại. -->

**Token (antd-theme.ts + globals.css):**
primary #0070F4 · hover #005AC3 · active #004392 · success #00B63E · warning #FF8800 · error #FF0000
chữ #15171A / phụ #525D6A / mờ #85909D / rất mờ #A4ACB5 · viền #D1D5DA / viền nhạt #E8EAED
nền trang #F7F8F9 · nền thẻ #FFFFFF · fill #F0F1F3 · header bảng #E6F1FE · hàng hover #F0F1F3 ·
hàng chọn #CCE2FD · hàng tổng #F0F1F3
thang brand: 25 #F2F8FE, 50 #E6F1FE, 75 #D9EAFD, 100 #CCE2FD, 150 #B3D4FC, 200 #99C6FB, 300 #66A9F8,
400 #338DF6, 500 #0070F4, 600 #005AC3, 700 #004392, 800 #002D62, 900 #001631
font: Inter, Roboto, Helvetica, Arial, sans-serif (KHÔNG tải webfont — dùng font có sẵn của máy)
cỡ chữ 14 / nhỏ 12 / lớn 16, line-height 1.4286 · control cao 32px (mobile nâng lên 44px)
bo góc: 9 (input/select) · 12 (card, nút, header bảng) · 7 (tag) · 4 · modal 24
bóng thẻ: -8px 8px 24px 0 rgba(0,0,0,.04) · bóng nút primary: 0 2px 12px 0 rgba(0,112,244,.15)
focus input: viền #0070F4 + bóng 0 2px 12px 0 rgba(0,112,244,.35) · modal: 0 8px 16px rgba(0,0,0,.15)
nút: font-weight 600 · ô bảng padding 6px 10px · label form 12px · khoảng cách field 16px
Tag mặc định: VIỀN, nền trong suốt, chữ #3E464F.

**Top nav (≥992px):** header trắng dính đỉnh, cao 56px, viền dưới #D1D5DA; trái là chữ "Kho Minh Vũ"
đậm; giữa là `<nav>` bo tròn hết cỡ, padding 1px, nền `linear-gradient(0deg,#0070F4 0%,#338DF6 100%)`,
viền 1px #0070F4, bóng `0 0 4px 0 rgba(0,112,244,.15)`; mỗi mục: icon + nhãn trắng đậm 14px, bo tròn,
padding 10px 8px, hover/active nền `rgba(255,255,255,.25)`; mục active có gạch trắng 40×3px bo tròn
ngay dưới nhãn; phải là menu tài khoản (tên + vai trò).

**Tab đáy (<992px):** `position:fixed` đáy, nền trắng, viền trên, `padding-bottom:env(safe-area-inset-bottom)`,
lưới N cột đều; ô = icon 20px + nhãn 11px; active chữ #005AC3 đậm vừa, còn lại #525D6A.
4 ô theo mobilePriority: Tổng quan · Xuất · Nhập · Đặt hàng, + ô "Khác" (dấu ba chấm) mở ngăn kéo đáy
liệt kê phần còn lại. Nội dung trang phải chừa đáy để hàng cuối bảng không bị che.

**List layout:** `aside` lọc 264px bên trái (card trắng, padding 16, chỉ ≥992px) + `section` bảng bên phải
(card trắng, padding 12/16); dưới 992px nút "Bộ lọc (n)" mở ngăn kéo ĐÁY chứa đúng panel lọc đó;
bảng bọc trong `overflow-x:auto`. Ngay dưới header bảng có một hàng tổng cộng nền #F0F1F3.

**Page header:** tiêu đề h4 + mô tả phụ bên trái, nhóm nút hành động bên phải, `flex-wrap`, mb 20px.

**Trạng thái chứng từ:** `NHAP_LIEU` "Nhập liệu" (tag vàng/gold) · `HOAN_THANH` "Hoàn thành" (tag xanh lá) ·
`DA_HUY` "Đã hủy" (tag xám/đỏ nhạt, chữ gạch ngang ở số phiếu).
**Trạng thái đơn đặt:** `TAM` "Đơn tạm" (gold) → `DA_XAC_NHAN` "Đã xác nhận" (blue) → `HOAN_THANH`; có
"mở khóa" và "đóng sớm" (bắt nhập lý do), cột Tiến độ = đã xuất / đã đặt.
**Tiền tố số chứng từ:** PN (nhập) · PX (xuất) · TN (trả NCC) · TK (trả khách) · DH (đơn) — dạng `PX26-000123`.
Chuyển kho / kiểm kê / điều chỉnh: dùng CK / KK / DC (giả định của demo — ghi rõ trong README).
**Bốn lý do xuất âm (đúng thứ tự):** "Mã bị tách / xuất nhầm mã" · "Hàng đã về, chưa nhập phiếu" ·
"Lệch tồn, chờ kiểm kê" · "Khác".
**Bốn vai trò:** quan_ly "Quản lý" · van_phong "Văn phòng" · thu_kho "Thủ kho" · chi_xem "Chỉ xem".
Thủ kho/chỉ xem KHÔNG thấy "Cài đặt"; chỉ quản lý thấy cột Giá vốn và được Hủy phiếu đã ghi sổ;
chỉ xem không có nút tạo/sửa nào.

**Cột bảng thật:**
- Phiếu nhập: Số phiếu · Ngày · Nguồn · Nhà cung cấp · Kho · Số dòng · Tổng số lượng · Trạng thái · Người tạo
- Phiếu xuất: Số phiếu · Ngày · Người nhận · Đơn gốc · Kho · Số dòng · Tổng số lượng · Trạng thái · Người tạo
- Đơn đặt: Số đơn · Ngày đơn · Người nhận · Ngày giao dự kiến · Tiến độ · Trạng thái · Người tạo
- Danh mục: Mã hàng · Tên hàng · Nhóm hàng · ĐVT · Công đoạn · Tồn · Giá bán · Giá vốn · Trạng thái
- Đối tác: Mã · Tên đối tác · Loại (NCC/Khách/Cả hai) · Điện thoại · Địa chỉ · Trạng thái
- Thẻ kho: Ngày giờ · Nguồn · Số phiếu · Kho · Đối tác / Ghi chú · Nhập · Xuất · (Tồn lũy kế) · Giá vốn lúc đó
- Dòng phiếu xuất: Mã hàng · Tên hàng · ĐVT · Kho (theo từng dòng) · Số lượng (+ tooltip "Tồn kho X còn N")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nền tảng dùng chung — token, component CSS, khung JS, dữ liệu mẫu, styleguide, README (DEMO-01)</name>
  <files>design/assets/tokens.css, design/assets/app.css, design/assets/app.js, design/assets/data.js, design/styleguide.html, design/dang-nhap.html, design/doi-mat-khau.html, design/khong-du-quyen.html, design/README.md, design/check.mjs</files>
  <action>
**Luật kỹ thuật cho MỌI file (cả Task 2, 3):**
- `file://` chặn ES module và `fetch()` file cục bộ → chỉ dùng `<script src="assets/data.js"></script>` rồi
  `<script src="assets/app.js"></script>` dạng thường ở cuối `<body>`. CẤM `type="module"`, `import`, `fetch(`, `XMLHttpRequest`.
- CẤM mọi URL ngoài (`http://`, `https://`, `//cdn`) trong `src`/`href`/`url()`/`@import`. Icon = SVG inline sinh từ app.js. Biểu đồ = SVG/CSS tự vẽ. Không webfont.
- Mọi đường dẫn là tương đối (`assets/...`, `nhap-kho.html`) — không bắt đầu bằng `/`.
- Mỗi trang HTML chỉ chứa phần NỘI DUNG trong `<main id="page">`; khung do JS dựng. Khuôn trang:
  `<body data-page="nhap-kho" data-nav="nhap-kho" data-shell="app|bare|print">`. `bare` = đăng nhập/đổi mật khẩu (không khung), `print` = mẫu in.
- JS viết để eslint của repo khỏi kêu: bọc IIFE, `"use strict"`, chỉ `const`/`let`, không biến thừa, chỉ lộ đúng hai global `window.KMV` và `window.KMV_DATA`. Logic riêng của từng trang đăng ký qua `KMV.page("ten-trang", fn)` đặt trong một thẻ `<script>` cuối trang đó (ngắn), phần dùng lại thì nằm trong app.js.
- `sessionStorage` bọc `try/catch` (một số trình duyệt chặn trên file://) — hỏng thì chạy bằng bộ nhớ, không văng lỗi.

**tokens.css** — `:root` khai toàn bộ biến theo khối `<interfaces>`: `--brand-25..900`, `--neutral-*` (đủ thang trung tính trong globals.css), `--color-primary/-hover/-active`, `--color-success/-warning/-error`, `--text/-2/-3/-4`, `--border/-2`, `--bg-layout/-card/-fill`, `--table-head`, `--row-hover`, `--row-selected`, `--radius-xs:4px/-sm:7px/-md:9px/-lg:12px/-modal:24px`, `--shadow-card`, `--shadow-primary`, `--shadow-focus`, `--shadow-modal`, `--font`, `--fs:14px/-sm:12px/-lg:16px`, `--lh:1.4286`, `--ctl-h:32px`, `--nav-h:56px`, `--bp-lg:992px` (chỉ để ghi chú; media query viết số thẳng). Đầu file có comment: "Chép từ src/providers/antd-theme.ts + src/app/globals.css — sửa ở đó trước, rồi sửa ở đây".

**app.css** — reset gọn + component, đặt tên lớp tiền tố `kv-`: `kv-shell`, `kv-topnav` (+pill, mục active có gạch trắng bằng `::after`), `kv-tabbar`, `kv-page-header`, `kv-card`, `kv-list-layout` (aside 264px + section, sập <992px), `kv-btn` (primary/default/danger/text/link, `is-loading`, `:disabled`), `kv-tag` (mặc định viền + biến thể gold/green/blue/red/gray), `kv-table` (header #E6F1FE bo 12 hai góc trên, ô 6×10, hover, hàng chọn, `kv-row-total`, `kv-row-warn` cho dòng vượt tồn, cột số căn phải + `font-variant-numeric: tabular-nums`, bọc `kv-table-wrap{overflow-x:auto}`), `kv-form` (label 12px, field cách 16px, lỗi theo field màu đỏ dưới ô), `kv-input/select/textarea` (focus đúng token), `kv-drawer` (phải trên desktop, ĐÁY trên mobile), `kv-modal` (bo 24), `kv-toast`, `kv-alert` (info/warn/error/success), `kv-skeleton` (shimmer), `kv-empty`, `kv-error` (có nút "Thử lại"), `kv-tabs`, `kv-stat` (ô số liệu dashboard), `kv-pagination`, `kv-dropdown`, `kv-desc` (danh sách mô tả 2–3 cột, 1 cột trên mobile), `kv-tooltip` (thuần CSS qua `data-tip`). Responsive: mốc duy nhất 992px cho khung; <992px control cao 44px, `main` có `padding-bottom: calc(64px + env(safe-area-inset-bottom))`; `html,body{overflow-x:hidden}` KHÔNG được dùng để che tràn — phải sửa tận gốc bằng `min-width:0` + wrap. `@media print { [data-no-print]{display:none!important} }` + khối style cho mẫu in A4/A5.

**data.js** — `window.KMV_DATA = {...}` tiếng Việt thực tế, ngành phụ tùng xe máy:
- `warehouses`: "Kho 1", "Kho 2". `roles`: 4 vai trò + 4 người dùng mẫu (quanly@, vanphong@, thukho@, chixem@khominhvu.local).
- `groups` ≥12 nhóm (Nhông sên dĩa, Bố thắng, Lọc gió, Bugi, Vỏ – ruột xe, Bình ắc quy, Dây curoa, Bạc đạn, Nhớt, Gương – đèn, Phuộc – giảm xóc, Ốc vít – phụ kiện…), `units` (Cái, Bộ, Sợi, Cặp, Lít, Hộp, Chai), `stages` (3–4 công đoạn mẫu).
- `products` ≥60 mã, mã kiểu thật (`LGPCX`, `HSP-20A-I`, `VITAL165`, `NSD-428-WAVE`, `BT-AB125`…), có tên, nhóm, ĐVT, giá bán, giá vốn, định mức tối thiểu, `stock: {k1, k2}` — trong đó ≥6 mã tồn ≤ 0 (kịch bản xuất âm), ≥8 mã dưới định mức, ≥5 mã không luân chuyển >30 ngày, 2 mã tên gần giống nhau ("Nhông xích 428" / "Nhong xich 428 loai 2" — gợi ý gộp mã), vài mã ngừng kinh doanh, vài mã "cần rà soát".
- `partners` ≥25: NCC (có "Nhà máy Vũ Trụ L.An" — chỉ là MỘT nhà cung cấp), khách sỉ, "Khách lẻ", vài đối tác loại "Cả hai".
- `receipts` ≥12, `issues` ≥20, `orders` ≥10, `returns` ≥4 (TN + TK), `transfers` ≥3, `stocktakes` ≥2 (một phiên đang đếm, một đã duyệt) — đủ cả ba trạng thái; mỗi chứng từ có mảng `lines`. Ngày quanh 09/2026. `movements` đủ để dựng thẻ kho cho ít nhất 3 mã (có bút toán đảo của phiếu đã hủy). `negativeReasons` đúng 4 lý do. `docNumbering` cấu hình tiền tố/số chữ số từng loại. `auditLog` mẫu.

**app.js** — `window.KMV`:
- `NAV` của demo (mở rộng từ NAV_ITEMS thật, ghi chú điều này): Tổng quan(index) · Nhập kho · Đặt hàng · Xuất kho (menu con: Phiếu xuất, Trả hàng) · Tồn kho (menu con: Tồn kho, Thẻ kho, Kiểm kê, Chuyển kho) · Danh mục hàng · Đối tác · Báo cáo · Cài đặt (chỉ quan_ly/van_phong). Pill quá rộng ở 992–1279px thì thu còn icon + nhãn mục active (không tràn ngang). Tab đáy: Tổng quan · Xuất · Nhập · Đặt hàng + "Khác" (ngăn kéo đáy liệt kê TẤT CẢ mục còn lại, kể cả mục con). Active lấy từ `body[data-nav]`.
- Menu tài khoản: tên + vai trò, **bộ chuyển vai trò** (4 vai trò — lưu phiên, tải lại trang; ẩn "Cài đặt", cột giá vốn `[data-role-only="quan_ly"]`, nút ghi `[data-perm="write"]` theo vai trò), "Đổi mật khẩu", "Đăng xuất" → dang-nhap.html, link "Styleguide".
- Tiện ích: `icon(name)` (bộ SVG inline ~25 icon), `fmtNumber` (dấu chấm ngàn kiểu vi-VN), `fmtDate` (dd/MM/yyyy), `statusTag(code)`, `renderTable(el,{columns,rows,totalRow,emptyText,onRowClick})`, `bindFilters(panelEl, tableApi)` (lọc theo ô tìm + select + checkbox, cập nhật đếm "Bộ lọc (n)" và hàng tổng), phân trang giả lập 20 dòng/trang, `openDrawer/closeDrawer`, `openModal/confirm`, `toast(type,msg)`, `fakeLoad(el, renderFn, ms=500)` (skeleton → nội dung), `?state=loading|error|empty` trên URL ép trang danh sách sang trạng thái đó để demo 4 trạng thái, `store.get/set` (sessionStorage, try/catch) giữ chứng từ vừa đổi trạng thái / dòng vừa thêm giữa các trang, `qs(name)` đọc tham số URL, `productSearch(inputEl,{onSelect})` — gợi ý mã khi gõ, **Enter chọn mã khớp tuyệt đối trước rồi mới tới kết quả đầu** (bẫy 15), `lineEntry(tableEl, opts)` — hàng nhập liệu bàn phím mã → Enter → số lượng → Enter → thêm dòng, con trỏ quay về ô mã. Esc đóng drawer/modal, bấm nền đóng, khóa cuộn nền khi mở, trả focus về nút gọi.

**styleguide.html** — hai phần: (A) **Mục lục** dạng lưới thẻ tới MỌI file .html trong `files_modified`, nhóm theo Khung / Chứng từ / Danh mục & tồn / Quản trị, mỗi thẻ ghi nhóm người dùng chính (thủ kho · văn phòng · quản lý) và đánh dấu "chưa có trong app" cho màn Phase 5–6; (B) **Styleguide**: bảng màu (ô màu + hex + tên biến), bo góc, đổ bóng, typography, nút (mọi biến thể + loading + disabled), tag trạng thái `NHAP_LIEU`/`HOAN_THANH`/`DA_HUY` + trạng thái đơn, form (field thường, lỗi theo field, disabled), bảng mẫu có hàng tổng + dòng cảnh báo, alert, **bốn trạng thái loading/error/empty/success đặt cạnh nhau** (error có nút "Thử lại" và câu chữ nói rõ làm gì tiếp — cấm "Có lỗi xảy ra"), nút mở thử drawer / modal / toast. Link trong Task 2–3 chưa có file thì vẫn để sẵn (check.mjs chỉ báo gãy link ở lần chạy cuối — xem dưới).

**dang-nhap.html / doi-mat-khau.html** (`data-shell="bare"`): thẻ giữa màn hình nền #F7F8F9; email + mật khẩu, validate theo field, nút loading khi submit, sai mật khẩu báo lỗi đúng field và GIỮ dữ liệu đã nhập, có 4 nút "đăng nhập nhanh" theo vai trò (chỉ có ở demo — ghi chú rõ); `?loi=vo-hieu-hoa` hiện alert tài khoản bị khóa; `?tiep_tuc=` được giữ lại khi chuyển trang. Đổi mật khẩu: thông báo mật khẩu tạm, 2 ô mới + xác nhận, quy tắc độ dài. **khong-du-quyen.html**: trong khung, thông báo "liên hệ quản trị" + nút về Tổng quan (khác luồng hết phiên).

**README.md** — cách mở (nhấp đúp `styleguide.html`), sơ đồ file, bảng ánh xạ file ↔ route thật của app (`nhap-kho-chi-tiet.html?id=` ↔ `/nhap-kho/[id]`, `in-phieu.html?loai=` ↔ `/…/[id]/in`), quy ước `data-page/data-nav/data-shell`, cách thêm màn mới, tham số demo (`?state=`, chuyển vai trò), danh sách **giả định của demo** (tiền tố CK/KK/DC, menu con trên pill, màn Phase 5–6 là đề xuất chưa chốt), và mục **"Lưu ý công cụ"**: `eslint.config.mjs` không ignore `design/` nên `npm run lint` / `npm run check` SẼ quét `design/**/*.js|mjs`; `npm run format` (prettier --write .) sẽ định dạng lại cả `design/`. Ghi lại kết quả thật của `npx eslint design` vào đây. KHÔNG sửa config — nếu có cảnh báo không tự sửa được trong `design/` thì nêu đề xuất (thêm `design/**` vào `globalIgnores`) để người dùng quyết.

**check.mjs** (Node thuần, không dependency): (1) mọi file trong danh sách kỳ vọng tồn tại — nhận cờ `--stage=1|2|3` để chỉ đòi file của các task đã xong; (2) quét mọi `.html/.css/.js` dưới `design/` báo lỗi nếu có `http://`, `https://` (trừ trong comment README và chuỗi `xmlns="http://www.w3.org/2000/svg"`), `type="module"`, `fetch(`, `XMLHttpRequest`, `@import url(`; (3) mọi `.html` có `assets/tokens.css`, `assets/app.css`, `assets/data.js`, `assets/app.js`, `<meta name="viewport"`, `lang="vi"`, thuộc tính `data-page`; (4) với `--stage=3`: mọi `href="*.html"` nội bộ (bỏ phần `?`/`#`) trỏ tới file có thật, kể cả link sinh trong app.js (quét chuỗi `"xxx.html"`); (5) `node --check` được cả `app.js` và `data.js` (dùng `child_process`); (6) `git status --porcelain` không có dòng nào ngoài `design/` và `.planning/`. Thoát mã ≠ 0 kèm danh sách lỗi đọc được.

Commit: `feat(design): nen tang demo tinh — token, khung, du lieu mau, styleguide`.
  </action>
  <verify>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && node design/check.mjs --stage=1 && grep -q "#0070F4" design/assets/tokens.css && grep -q "#E6F1FE" design/assets/tokens.css && npx eslint design; echo "eslint exit=$? (ghi vao README, khong sua config)"</automated>
  </verify>
  <done>check.mjs --stage=1 thoát 0. Mở `design/styleguide.html` bằng file:// thấy top nav pill (≥992px) và tab đáy + "Khác" (<992px), chuyển vai trò sang Thủ kho thì mục Cài đặt biến mất, drawer/modal/toast mở-đóng được bằng chuột và Esc, 4 trạng thái hiện cạnh nhau. README có mục "Lưu ý công cụ" với kết quả eslint thật. Không file nào ngoài `design/` thay đổi.</done>
</task>

<task type="auto">
  <name>Task 2: Luồng chứng từ — nhập kho, đặt hàng, xuất kho (xuất âm), trả hàng, mẫu in (DEMO-02)</name>
  <files>design/nhap-kho.html, design/nhap-kho-chi-tiet.html, design/dat-hang.html, design/dat-hang-chi-tiet.html, design/xuat-kho.html, design/xuat-kho-chi-tiet.html, design/tra-hang.html, design/tra-hang-chi-tiet.html, design/in-phieu.html, design/assets/app.js, design/assets/app.css</files>
  <action>
Dùng lại 100% khung và tiện ích của Task 1; thiếu gì dùng chung (vd. `postingSummary`, `voidDialog`, `docHeader`) thì BỔ SUNG vào app.js/app.css, không chép lặp giữa các trang. Trang chi tiết đọc `?id=` (thiếu id → lấy chứng từ `NHAP_LIEU` đầu tiên; id lạ → trạng thái "không tìm thấy" có nút về danh sách).

**Bốn trang danh sách** (nhap-kho, dat-hang, xuat-kho, tra-hang) — cùng khuôn `kv-list-layout`: panel lọc trái (ô tìm số phiếu/đối tác, khoảng ngày, kho, trạng thái dạng checkbox, người tạo; nút "Xóa bộ lọc"), thanh công cụ (ô tìm nhanh, nút chính "Tạo phiếu nhập" / "Tạo đơn" / "Tạo phiếu xuất", "Xuất Excel" → toast giả lập), bảng đúng cột trong `<interfaces>`, hàng tổng cộng dưới header (tổng số dòng, tổng số lượng theo bộ lọc hiện tại), tag trạng thái, số phiếu là link sang chi tiết, số phiếu `DA_HUY` gạch ngang, phân trang, skeleton khi vào, empty riêng khi lọc không ra ("Không có phiếu nào khớp bộ lọc — thử nới khoảng ngày"), `?state=error` có "Thử lại". Nút tạo: cấp số mới theo tiền tố, lưu phiên, chuyển sang trang chi tiết. `tra-hang.html` không có nút tạo — alert giải thích "Phiếu trả tạo từ chứng từ gốc đã ghi sổ" kèm link sang nhập/xuất kho. `dat-hang.html` có cột Tiến độ dạng thanh nhỏ + "12/20".

**nhap-kho-chi-tiet.html** — đầu phiếu sửa tại chỗ (NCC có ô tìm + "tạo đối tác mới tại chỗ", kho, ngày, nguồn, ghi chú) trong `kv-desc`; bảng dòng có cột Đơn giá + Thành tiền; **hàng nhập liệu bàn phím** (`lineEntry`): mã → Enter → số lượng → Enter → đơn giá → Enter → thêm dòng, toast nhẹ, con trỏ về ô mã; sửa số lượng tại ô, xóa dòng có xác nhận. **Ghi sổ**: modal tóm tắt hậu quả ("Tồn Kho 1 sẽ TĂNG ở 7 mã, tổng +340", giá vốn bình quân của 2–3 mã sẽ đổi từ X → Y — chỉ quan_ly thấy số giá vốn) → xác nhận → nút loading, khóa bấm lặp → trạng thái `HOAN_THANH`, mọi ô sửa biến thành chữ, hàng nhập liệu biến mất, alert "Phiếu đã ghi sổ và bị khóa". **Hủy phiếu** (chỉ quan_ly, chỉ khi `HOAN_THANH`): modal bắt nhập lý do → `DA_HUY` + alert "Đã sinh bút toán đảo — sổ kho không xóa dòng nào" + khối hiển thị các dòng đảo. Nút "In phiếu" → `in-phieu.html?loai=nhap&id=`. Nút "Trả hàng NCC" chỉ hiện khi `HOAN_THANH` → tạo TN trong phiên, sang `tra-hang-chi-tiet.html`. Cuối trang: nhật ký sửa (auditLog).

**dat-hang-chi-tiet.html** — đầu đơn (người nhận, ngày đơn, ngày giao dự kiến, ghi chú) + bảng dòng gõ bàn phím (mã, SL đặt, đã xuất, còn lại, kho mặc định của mã); thanh hành động theo trạng thái và vai trò: `TAM` → "Xác nhận đơn"; `DA_XAC_NHAN` → "In phiếu đi lấy hàng" (`in-phieu.html?loai=lay-hang`), "Tạo phiếu xuất" (chặn kèm thông báo rõ nếu có mã thiếu kho mặc định — nêu mã nào, sửa ở đâu), "Mở khóa" / "Đóng sớm" (modal bắt nhập lý do); đơn đã xác nhận thì khóa sửa dòng. Khối "Phiếu xuất liên quan" liệt kê PX sinh từ đơn. Bước trạng thái dạng stepper `TAM → DA_XAC_NHAN → HOAN_THANH`.

**xuat-kho-chi-tiet.html** — như phiếu nhập nhưng KHÔNG có cột giá; mỗi dòng có select Kho riêng + hiển thị tồn của kho đó; dòng có SL > tồn tô `kv-row-warn` + tooltip "Tồn Kho 1 còn 3". Khi có ≥1 dòng vượt tồn hiện **panel xuất âm**: giải thích ngắn vì sao được phép xuất âm, 4 lý do dạng radio lớn đúng thứ tự, chọn "Khác" bắt nhập ghi chú, nút "Bỏ chọn lý do"; với lý do "Mã bị tách" hiện **gợi ý mã gần giống** còn tồn + nút "Đề nghị gộp hai mã" (toast "Đã ghi đề nghị", bấm lại báo "Đã ghi từ trước"). **Nút Ghi sổ bị disabled + dòng chữ lý do** cho tới khi đã chọn lý do; sửa dòng làm hết vượt tồn thì panel tự biến mất. Modal tóm tắt trước ghi sổ nhắc lại từng dòng vượt tồn + lý do + tiến độ đơn gốc sẽ đổi. Có link "Đơn gốc" nếu sinh từ đơn. Nút "In phiếu giao hàng" (bản nháp in chữ chìm "BẢN NHÁP"), "Khách trả hàng" khi `HOAN_THANH`. Ở <992px: đầu phiếu xếp 1 cột, bảng dòng chuyển thành **danh sách thẻ** (mã đậm, tên, kho, bộ +/- số lượng to) — không tràn ngang.

**tra-hang-chi-tiet.html** — hai biến thể theo loại chứng từ: `TK` khách trả (tồn TĂNG, không bao giờ hỏi lý do xuất âm) và `TN` trả NCC (tồn GIẢM, vượt tồn thì dùng lại đúng panel xuất âm). Hiện "Từ chứng từ gốc PXxx/PNxx" có link, SL trả không vượt SL gốc (lỗi theo field), ghi sổ / hủy như trên.

**in-phieu.html** (`data-shell="print"`, `?loai=nhap|giao-hang|lay-hang&id=`) — tờ A4 trắng giữa nền xám, thanh công cụ `data-no-print` (In → `window.print()`, Quay lại). Tiêu đề công ty "CTY TNHH SX-TM P.TÙNG XE MÁY MINH VŨ", số phiếu, ngày, đối tác, bảng dòng có STT, ô ký tên 3 cột. `lay-hang`: dòng **gom theo kho** rồi theo mã, có ô tick để thủ kho đánh dấu, nhóm "Chưa gán kho" nằm CUỐI. `giao-hang`: không có giá. `@media print` ẩn thanh công cụ, bỏ nền/bóng, `@page` lề 12mm.

Commit: `feat(design): man chung tu — nhap, dat hang, xuat (xuat am), tra hang, mau in`.
  </action>
  <verify>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && node design/check.mjs --stage=2 && grep -q "Mã bị tách / xuất nhầm mã" design/assets/data.js && grep -lE "kv-row-warn" design/xuat-kho-chi-tiet.html design/assets/app.js design/assets/app.css | grep -q . && ! grep -rnE "type=\"number\"[^>]*(name|id)=\"(ton|stock)" design/*.html</automated>
  </verify>
  <done>check.mjs --stage=2 thoát 0. Mở `xuat-kho-chi-tiet.html`: thêm dòng bằng mã→Enter→SL→Enter; dòng vượt tồn đổi màu, panel xuất âm hiện, Ghi sổ khóa tới khi chọn lý do; ghi sổ xong phiếu khóa, quay về `xuat-kho.html` thấy trạng thái đã đổi (giữ qua sessionStorage). Phiếu nhập: ghi sổ → hủy (vai trò Quản lý) → `DA_HUY` kèm khối bút toán đảo. `in-phieu.html?loai=lay-hang` gom dòng theo kho; xem trước bản in không có thanh công cụ. Ở 375px không trang nào cuộn ngang.</done>
</task>

<task type="auto">
  <name>Task 3: Tổng quan, danh mục, đối tác, tồn kho, thẻ kho, kiểm kê, chuyển kho, báo cáo, cài đặt + chốt kiểm (DEMO-03)</name>
  <files>design/index.html, design/danh-muc.html, design/danh-muc-chi-tiet.html, design/doi-tac.html, design/doi-tac-chi-tiet.html, design/ton-kho.html, design/the-kho.html, design/kiem-ke.html, design/chuyen-kho.html, design/bao-cao.html, design/cai-dat.html, design/assets/app.js, design/assets/app.css, design/README.md</files>
  <action>
Cùng luật kỹ thuật ở Task 1. **Nguyên tắc 1 áp vào mọi màn ở đây: KHÔNG có ô nào cho gõ thẳng số tồn** — tồn luôn là chữ chỉ đọc, kèm link "Xem thẻ kho"; muốn chỉnh thì dẫn sang kiểm kê.

**index.html (Tổng quan, quản lý)** — hàng `kv-stat`: Tổng giá trị tồn (chỉ quan_ly; vai trò khác thấy "—" kèm tooltip), Số mã đang kinh doanh, Mã dưới định mức, Mã tồn âm, Phiếu xuất hôm nay, Phiếu chờ ghi sổ. Biểu đồ cột đôi nhập–xuất 30 ngày bằng **SVG tự vẽ từ data.js** (trục, chú giải, tooltip `<title>`). Tồn theo nhóm hàng (thanh ngang) + chuyển tab "Theo công đoạn". Ba bảng ngắn có "Xem tất cả": mã dưới định mức → `ton-kho.html?loc=duoi-dinh-muc`, hàng không luân chuyển >30 ngày, **xuất âm hôm nay kèm lý do** (TQAN-06). Chọn kho (Tất cả / Kho 1 / Kho 2) đổi số liệu. Mobile: stat 2 cột, biểu đồ cuộn ngang trong khung.

**danh-muc.html** — list layout: lọc theo nhóm (cây/ô tìm trong 90 nhóm), ĐVT, công đoạn, tình trạng kinh doanh, tồn (còn hàng / hết / âm), "cần rà soát"; bảng đúng cột, giá vốn `data-role-only="quan_ly"`, tag cảnh báo có tooltip; chọn nhiều dòng → thanh hành động hàng loạt (gán nhóm/ĐVT/công đoạn); sửa tại ô cho tên/giá bán (Enter lưu, Esc hủy). **Ngăn kéo Thêm/Sửa mã hàng**: mã (unique — nhập mã đã có báo lỗi ĐÚNG field "Mã LGPCX đã tồn tại"), tên, nhóm, ĐVT, công đoạn, giá bán, định mức tối thiểu, kho mặc định, đang kinh doanh; KHÔNG có ô tồn và ô giá vốn (ghi chú nhỏ giải thích: tồn đến từ chứng từ, giá vốn do hệ thống tính bình quân); submit loading, lỗi giữ dữ liệu. **Modal Import Excel 3 bước**: chọn file (giả lập) → xem trước (bảng có dòng hợp lệ / lỗi tô đỏ + lý do từng dòng, đếm "1.204 hợp lệ · 12 lỗi", nút "Tải file lỗi CSV") → kết quả. **danh-muc-chi-tiet.html** (`?id=`): mô tả mã, tồn theo từng kho (chỉ đọc), tab **Thẻ kho** (đúng cột, lọc kho + khoảng ngày, có tồn lũy kế, số phiếu là link sang chứng từ, dòng đảo của phiếu hủy in nghiêng; alert "Dữ liệu KiotViet cũ không gắn kho nên chỉ hiện khi xem Tất cả kho"), tab Nhật ký sửa, nút Sửa mở lại ngăn kéo.

**doi-tac.html / doi-tac-chi-tiet.html** — list layout (lọc loại NCC/Khách/Cả hai, trạng thái), ngăn kéo thêm/sửa (mã tự cấp NCC…/KH…, tên, loại, điện thoại, địa chỉ, ghi chú); chi tiết: thông tin + **Lịch sử giao dịch** (bảng chứng từ của đối tác, lọc loại + khoảng ngày, link sang phiếu) + ghi chú. KHÔNG có số dư / công nợ (ngoài phạm vi v1).

**ton-kho.html** — ma trận **mã × kho**: Mã · Tên · Nhóm · ĐVT · Kho 1 · Kho 2 · Tổng · Định mức · Tình trạng; lọc nhóm, kho, tình trạng (đủ / sắp hết / hết / âm), `?loc=` từ dashboard; tồn âm chữ đỏ, dưới định mức tag cam "Sắp hết"; hàng tổng cộng; mỗi mã có link "Thẻ kho". Tab phụ **"Đề xuất định mức"**: bảng mức đề xuất từ lượng xuất 30 ngày, quản lý duyệt từng dòng / hàng loạt (đổi tag, toast). Mobile: danh sách thẻ, hai kho cạnh nhau.

**the-kho.html** — chọn mã bằng `productSearch` (+ `?ma=`), kho, khoảng ngày → bảng thẻ kho đầy đủ cột + tồn đầu kỳ / cuối kỳ + cột tồn lũy kế; alert nhắc "Sổ kho chỉ thêm, không sửa, không xóa — phiếu hủy hiện thành dòng đảo"; nút Xuất Excel (toast). Chưa chọn mã → empty hướng dẫn.

**kiem-ke.html — ƯU TIÊN MOBILE, thiết kế ở 375px trước.** Ba khung nhìn trong một trang (tab hoặc `?buoc=`): (1) **Danh sách phiên** + "Mở phiên kiểm kê" (kho, nhóm hàng; ghi chú "hệ thống chốt tồn sổ tại thời điểm mở phiên"); (2) **Đếm**: khung camera GIẢ LẬP (khung ngắm + vạch quét chạy bằng CSS, nút "Giả lập quét" chọn ngẫu nhiên một mã chưa đếm) + ô gõ mã tay, thẻ mã vừa quét to rõ, bộ **− [số] +** cao ≥44px và bàn phím số (`inputmode="decimal"`), nút "Lưu & quét tiếp" dính đáy phía trên tab bar, thanh tiến độ "Đã đếm 37/120", danh sách đã đếm sửa lại được; KHÔNG hiện tồn sổ lúc đếm (đếm mù) — ghi chú điều này; (3) **Bảng lệch**: Mã · Tồn sổ · Thực đếm · Lệch (đỏ/xanh), lọc "chỉ dòng lệch", mã chưa đếm đánh dấu riêng; nút **"Duyệt phiên"** (chỉ quan_ly) → modal tóm tắt "sẽ sinh phiếu điều chỉnh DCxx cho 14 mã" → phiên chuyển Đã duyệt, hiện link phiếu điều chỉnh. Desktop dùng lại đúng các khối đó, giãn 2 cột.

**chuyen-kho.html** — danh sách phiếu CK + tạo/chi tiết trong cùng trang (`?id=`): kho đi → kho đến (không cho trùng nhau — lỗi theo field), bảng dòng gõ bàn phím có cột "Tồn kho đi", vượt tồn cảnh báo theo đúng khuôn xuất âm, ghi sổ một lần tóm tắt "Kho 1 −N, Kho 2 +N trong MỘT chứng từ".

**bao-cao.html** — tab: Nhập–xuất–tồn theo kỳ (đầu kỳ · nhập · xuất · cuối kỳ theo mã, lọc nhóm/kho/kỳ, hàng tổng), Xuất âm kèm lý do, Hàng không luân chuyển, Giá trị tồn theo nhóm (chỉ quan_ly). Mỗi tab có "Xuất Excel" (toast). Ghi rõ trong trang + README: đây là đề xuất bố cục, v1 chỉ cam kết các mục TQAN.

**cai-dat.html** — `kv-tabs` đổi theo `?tab=` (URL tiếng Việt: `nguoi-dung`, `kho`, `nhom-hang`, `don-vi-tinh`, `cong-doan`, `so-chung-tu`): **Người dùng** (bảng + ngăn kéo: họ tên, email, vai trò, kho được gán nhiều lựa chọn, khóa/mở; đặt lại mật khẩu hiện mật khẩu tạm một lần có nút sao chép; alert đúng bẫy 6: "Thu hẹp quyền có hiệu lực ngay; mở rộng quyền cần người dùng tải lại trang hoặc chờ tối đa 60 phút"), **Kho**, **Nhóm hàng** (xóa nhóm đang có mã → lỗi rõ "Nhóm đang có 124 mã hàng — chuyển mã sang nhóm khác trước"), **ĐVT**, **Công đoạn** (bảng tra + ngăn kéo dùng chung), **Số chứng từ** (mỗi loại: tiền tố, số chữ số, xem trước `PX26-000124`, chặn giảm số chữ số dưới độ dài đang chạy kèm thông báo). Vai trò thu_kho/chi_xem vào trang này → chuyển sang `khong-du-quyen.html`; van_phong chỉ thấy các tab bảng tra (không thấy Người dùng).

**Chốt:** rà cả 24 trang ở 375px và 1280px (dùng DevTools nếu có trình duyệt; không có thì soát CSS: mọi bảng nằm trong `kv-table-wrap`, mọi hàng flex có `flex-wrap`/`min-width:0`). Cập nhật README: bảng trạng thái từng màn (đã có trong app / đề xuất Phase 5–6), kết quả `npx eslint design` cuối cùng, và mục "Chưa làm / giả định". Chạy `npm run lint` MỘT lần để biết `design/` có làm đỏ `npm run check` không — CHỈ báo cáo kết quả trong README và SUMMARY, không sửa config; lỗi nằm trong file của `design/` thì sửa file đó.

Commit: `feat(design): man tong quan, danh muc, doi tac, ton kho, kiem ke, chuyen kho, bao cao, cai dat`.
  </action>
  <verify>
    <automated>cd /Users/hungly/Desktop/Projects/kiotviet && node design/check.mjs --stage=3 && test "$(ls design/*.html | wc -l | tr -d ' ')" -ge 24 && test -z "$(git status --porcelain | grep -vE '^.. (design/|\.planning/)')" && git diff --quiet HEAD -- package.json package-lock.json eslint.config.mjs tsconfig.json && npm run lint; echo "npm run lint exit=$? (bao cao trong README + SUMMARY)"</automated>
  </verify>
  <done>check.mjs --stage=3 thoát 0 (đủ 24 file .html, không URL ngoài, không link nội bộ gãy, JS qua `node --check`). Mục lục trong `styleguide.html` mở được mọi màn. `kiem-ke.html` ở 375px: giả lập quét → nhập số → lưu → tiến độ tăng → bảng lệch → quản lý duyệt sinh phiếu điều chỉnh. Không màn nào có ô sửa trực tiếp số tồn. `git status` chỉ có `design/` (và `.planning/`). README ghi rõ kết quả eslint/`npm run lint` và đề xuất (nếu cần) để NGƯỜI DÙNG tự quyết việc ignore `design/`.</done>
</task>

</tasks>

<verification>
1. `node design/check.mjs --stage=3` thoát 0.
2. Không mạng: ngắt Wi-Fi, nhấp đúp `design/styleguide.html` → đi hết mục lục, console sạch ở mọi trang.
3. Đối chiếu token: mở `styleguide.html` cạnh app thật (`npm run dev`) — màu primary, header bảng, bo góc nút/card, pill nav phải trùng.
4. Luồng chính chạy trọn bằng bàn phím: tạo phiếu xuất → thêm 3 dòng → một dòng vượt tồn → chọn lý do → ghi sổ → về danh sách thấy `HOAN_THANH`.
5. 375px: không trang nào cuộn ngang; tab đáy không che nút hành động cuối trang.
6. `git status --porcelain` chỉ liệt kê `design/` (+ `.planning/`).
</verification>

<success_criteria>
- 24 trang HTML + 4 file assets + README + check.mjs, tất cả dưới `design/`, mở bằng `file://`, không phụ thuộc mạng/build.
- Khung (top nav pill / tab đáy / menu tài khoản) render từ MỘT chỗ trong `assets/app.js`; không trang nào chép HTML khung.
- Token trùng `antd-theme.ts` + `globals.css`.
- Năm nguyên tắc kiến trúc nhìn thấy được trên UI: không sửa tồn trực tiếp; sổ kho append-only (dòng đảo); một khuôn chứng từ cho mọi loại; hai trạng thái sống + hủy bằng bút toán đảo; mã hàng là khóa nghiệp vụ (tìm/gõ theo mã).
- Xuất âm: được phép nhưng bắt buộc chọn lý do mới ghi sổ được.
- Không file nào ngoài `design/` bị sửa; không dependency mới; config lint/tsconfig nguyên vẹn; ảnh hưởng của eslint/prettier lên `design/` được GHI CHÚ cho người dùng, không tự xử.
</success_criteria>

<output>
Sau khi xong, tạo `.planning/quick/260921-v15-ban-demo-ui-ux-tinh-html-css-js-trong-th/260921-v15-SUMMARY.md`
— nêu rõ: file đã tạo, giả định đã đặt (tiền tố CK/KK/DC, menu con trên pill, bố cục các màn Phase 5–6),
kết quả `npx eslint design` / `npm run lint`, và phần chưa làm.
</output>
